# Binder config lives on the `notebooks` branch (auto-pushed by CI).
# See .github/workflows/deploy.yml for the active requirements.
# The notebooks branch also contains runtime.txt for Python version.
#
# For Docker/local Jupyter, see binder/jupyter-requirements.txt instead.
