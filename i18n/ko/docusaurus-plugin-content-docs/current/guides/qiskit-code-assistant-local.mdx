---
title: 로컬 모드에서 Qiskit Code Assistant 사용하기
description: Qiskit Code Assistant 모델을 로컬에 배포하고 사용하는 방법을 알아봅니다.
sidebar_label: 로컬 모드에서 Qiskit Code Assistant 사용하기
---

{/* doqumentation-source-hash: a98b4d3a */}

{/* cspell:ignore ollama, gguf, modelfile, safetensors, winget, MBPP */}

# 로컬 모드에서 Qiskit Code Assistant 사용하기 {#use-qiskit-code-assistant-in-local-mode}

로컬 머신에 Qiskit Code Assistant 모델을 설치, 구성 및 사용하는 방법을 알아봅니다.

<Admonition type="note" title="참고 사항">
    - Qiskit Code Assistant는 미리 보기 릴리스 상태이며 변경될 수 있습니다.
    - 피드백이 있거나 개발팀에 문의하고 싶다면 [Qiskit Slack Workspace 채널](https://qiskit.enterprise.slack.com/archives/C07LYA6PL83) 또는 관련 공개 GitHub 저장소를 이용해 주세요.
</Admonition>

## 빠른 시작 (권장) {#quick-start-recommended}

로컬 모드에서 Qiskit Code Assistant를 시작하는 가장 쉬운 방법은 VS Code 또는 JupyterLab 익스텐션용 자동 설정 스크립트를 사용하는 것입니다. 이 스크립트는 LLM을 실행하기 위한 [Ollama](https://ollama.com/) 설치, 권장 모델 다운로드, 익스텐션 구성을 자동으로 처리합니다.

### VS Code 익스텐션 설정 {#vs-code-extension-setup}

터미널에서 다음 명령어를 실행하세요:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Qiskit/qiskit-code-assistant-vscode/main/setup_local.sh)
```

이 스크립트는 다음 단계를 수행합니다:
- Ollama 설치 (아직 설치되지 않은 경우)
- 권장 Qiskit Code Assistant 모델 다운로드 및 구성
- 로컬 배포와 연동되도록 VS Code 익스텐션 설정

### JupyterLab 익스텐션 설정 {#jupyterlab-extension-setup}

터미널에서 다음 명령어를 실행하세요:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Qiskit/qiskit-code-assistant-jupyterlab/main/setup_local.sh)
```

이 스크립트는 다음을 수행합니다:
- Ollama 설치 (아직 설치되지 않은 경우)
- 권장 Qiskit Code Assistant 모델 다운로드 및 구성
- 로컬 배포와 연동되도록 JupyterLab 익스텐션 설정

## 사용 가능한 모델 {#available-models}

### 현재 모델 {#current-models}

다음은 Qiskit Code Assistant에서 사용하도록 권장하는 최신 모델입니다:

1. [Qiskit/mistral-small-3.2-24b-qiskit](https://huggingface.co/Qiskit/mistral-small-3.2-24b-qiskit) - 2025년 10월 출시
2. [qiskit/qwen2.5-coder-14b-qiskit](https://huggingface.co/Qiskit/qwen2.5-coder-14b-qiskit) - 2025년 6월 출시
3. [qiskit/granite-3.3-8b-qiskit](https://huggingface.co/Qiskit/granite-3.3-8b-qiskit) - 2025년 6월 출시
4. [qiskit/granite-3.2-8b-qiskit](https://huggingface.co/Qiskit/granite-3.2-8b-qiskit) - 2025년 6월 출시

### GGUF 모델 (개인 환경/노트북에 권장) {#gguf-models-recommended-for-personal-environments-laptops}

GGUF 형식 모델은 로컬 사용에 최적화되어 있으며 더 적은 컴퓨팅 자원을 필요로 합니다:

1. [mistral-small-3.2-24b-qiskit-GGUF](https://huggingface.co/Qiskit/mistral-small-3.2-24b-qiskit-GGUF) – **2025년 10월** 출시  
   *Qiskit 버전 **2.1**까지의 데이터로 학습됨*  

2. [qiskit/qwen2.5-coder-14b-qiskit-GGUF](https://huggingface.co/Qiskit/qwen2.5-coder-14b-qiskit-GGUF) – **2025년 6월** 출시  
   *Qiskit 버전 **2.0**까지의 데이터로 학습됨*  

3. [qiskit/granite-3.3-8b-qiskit-GGUF](https://huggingface.co/Qiskit/granite-3.3-8b-qiskit-GGUF) – **2025년 6월** 출시  
   *Qiskit 버전 **2.0**까지의 데이터로 학습됨*  

4. [qiskit/granite-3.2-8b-qiskit-GGUF](https://huggingface.co/Qiskit/granite-3.2-8b-qiskit-GGUF) – **2025년 6월** 출시  
   *Qiskit 버전 **2.0**까지의 데이터로 학습됨*  

오픈 소스 Qiskit Code Assistant 모델은 <DefinitionTooltip definition="Safetensors is a file format designed specifically for storing machine learning model weights and tensors in a secure and efficient manner.">safetensors</DefinitionTooltip> 또는 <DefinitionTooltip definition="GGUF is a binary format that is designed for quickly loading and saving models, and for readability.">GGUF 파일 형식</DefinitionTooltip>으로 제공되며 아래에 설명된 대로 Hugging Face에서 다운로드할 수 있습니다.

### 학습에 사용된 Qiskit 버전 {#qiskit-versions-used-for-training}

| **모델** | &nbsp; | &nbsp; | &nbsp; | &nbsp; | &nbsp; | **벤치마크 지표** | &nbsp; | &nbsp; | &nbsp; | &nbsp; | **출시일** | **학습된 Qiskit 버전** |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| &nbsp; | **QiskitHumanEval-Hard** | **QiskitHumanEval** | **HumanEval** | **ASDiv** | **MathQA** | **SciQ** | **MBPP** | **IFEval** | **CrowsPairs (English)** | **TruthfulQA (MC1 acc)** | &nbsp; | &nbsp; |
| **mistral-small-3.2-24b-qiskit** | 32.45 | 47.02 | 77.49 | 3.77 | 49.68 | 97.50 | 64.00 | 48.44 | 67.08 | 39.41 | **2026년 1월** | **2.2** |
| qwen2.5-coder-14b-qiskit | 25.17 | 49.01 | 91.46 | 4.21 | 53.90 | 97.00 | 77.60 | 49.64 | 65.18 | 37.82 | 2025년 6월 | 2.0 |
| granite-3.3-8b-qiskit | 14.57 | 27.15 | 62.80 | 0.48 | 38.66 | 93.30 | 52.40 | 59.71 | 59.75 | 39.05 | 2025년 6월 | 2.0 |
| granite-3.2-8b-qiskit | 9.93 | 24.50 | 57.32 | 0.09 | 41.41 | 96.30 | 51.80 | 60.79 | 66.79 | 40.51 | 2025년 6월 | 2.0 |
| granite-8b-qiskit-rc-0.10 | 15.89 | 38.41 | 59.76 | — | — | — | — | — | — | — | 2025년 2월 | 1.3 |
| granite-8b-qiskit | 17.88 | 44.37 | 53.66 | — | — | — | — | — | — | — | 2024년 11월 | 1.2 |

*참고: 벤치마크 표에 나열된 모든 모델은 Hugging Face 모델에 정의된 각 모델의 시스템 프롬프트를 사용하여 평가되었습니다.*

### 더 이상 사용되지 않는 모델 {#deprecated-models}

이 모델들은 더 이상 적극적으로 유지 관리되지 않지만 계속 사용할 수 있습니다:

1. [qiskit/granite-8b-qiskit-rc-0.10](https://huggingface.co/Qiskit/granite-8b-qiskit-rc-0.10) - 2025년 2월 출시 (지원 종료)
2. [qiskit/granite-8b-qiskit](https://huggingface.co/Qiskit/granite-8b-qiskit) - 2024년 11월 출시 (지원 종료)

## 고급 설정 {#advanced-setup}

로컬 설정을 수동으로 구성하거나 설치 과정을 더 세밀하게 제어하고 싶다면 아래 섹션을 펼쳐서 확인하세요.

<details>

<summary>Hugging Face 웹사이트에서 다운로드</summary>

Hugging Face 웹사이트에서 Qiskit Code Assistant 관련 모델을 다운로드하려면 다음 단계를 따르세요:

1. Hugging Face에서 원하는 Qiskit 모델 페이지로 이동합니다.
2. **Files and Versions** 탭으로 이동하여 safetensors 또는 GGUF 모델 파일을 다운로드합니다.

</details>

<details>

<summary>Hugging Face CLI를 사용하여 다운로드</summary>

Hugging Face CLI를 사용하여 사용 가능한 Qiskit Code Assistant 모델을 다운로드하려면 다음 단계를 따르세요:

1. [Hugging Face CLI](https://huggingface.co/docs/huggingface_hub/main/en/guides/cli)를 설치합니다.
1. Hugging Face 계정에 로그인합니다.

    ```
    huggingface-cli login
    ```

1. 이전 목록에서 원하는 모델을 다운로드합니다.

    ```
    huggingface-cli download <HF REPO NAME> <MODEL PATH> --local-dir <LOCAL PATH>
    ```

</details>

<details>

<summary>Ollama를 통해 로컬에 Qiskit Code Assistant 모델 수동 배포</summary>

다운로드한 Qiskit Code Assistant 모델을 배포하고 상호 작용하는 방법은 여러 가지가 있습니다. 이 가이드는 [Ollama](https://ollama.com)를 사용하는 방법을 설명합니다. Hugging Face Hub 통합 또는 로컬 모델을 사용하는 [Ollama 애플리케이션](#using-the-ollama-application)을 이용하거나 [`llama-cpp-python` 패키지](#use-llama-cpp-python)를 사용할 수 있습니다.

### Ollama 애플리케이션 사용 {#using-the-ollama-application}

Ollama 애플리케이션은 LLM을 로컬에서 실행하는 간단한 솔루션을 제공합니다. 전체 설정 과정, 모델 관리 및 상호 작용을 상당히 간단하게 만들어 주는 CLI를 갖추고 있어 사용하기 쉽습니다. 빠른 실험 및 기술적인 세부 사항을 최소화하고자 하는 사용자에게 이상적입니다.

#### Ollama 설치 {#install-ollama}

1. [Ollama 애플리케이션](https://ollama.com/download)을 다운로드합니다.
1. 다운로드한 파일을 설치합니다.
1. 설치된 Ollama 애플리케이션을 실행합니다.

    <Admonition type="info">데스크톱 메뉴 바에 Ollama 아이콘이 나타나면 애플리케이션이 성공적으로 실행 중인 것입니다. `http://localhost:11434/`에 접속하여 서비스가 실행 중인지 확인할 수도 있습니다.</Admonition>

1. 터미널에서 Ollama를 실행해 보고 모델을 시작합니다. 예시:

    ```
    ollama run hf.co/Qiskit/Qwen2.5-Coder-14B-Qiskit
    ```

#### Hugging Face Hub 통합을 사용하여 Ollama 설정 {#set-up-ollama-using-the-hugging-face-hub-integration}

[Ollama/Hugging Face Hub 통합](https://huggingface.co/docs/hub/ollama)은 새 modelfile을 생성하거나 GGUF 또는 safetensors 파일을 수동으로 다운로드하지 않고도 Hugging Face Hub에 호스팅된 모델과 상호 작용할 수 있는 방법을 제공합니다. 기본 `template` 및 `params` 파일이 Hugging Face Hub의 모델에 이미 포함되어 있습니다.

1. Ollama 애플리케이션이 실행 중인지 확인합니다.

1. 원하는 모델 페이지로 이동하여 URL을 복사합니다. 예: https://huggingface.co/Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF

1. 터미널에서 다음 명령어를 실행합니다:

    ```
    ollama run hf.co/Qiskit/Qwen2.5-Coder-14B-Qiskit
    ```

`hf.co/Qiskit/Qwen2.5-Coder-14B-Qiskit` 모델 또는 현재 권장되는 다른 공식 GGUF 모델인 `hf.co/Qiskit/mistral-small-3.2-24b-qiskit-GGUF` 또는 `hf.co/Qiskit/granite-3.3-8b-qiskit-GGUF`를 사용할 수 있습니다.

#### 수동으로 다운로드한 Qiskit Code Assistant GGUF 모델로 Ollama 설정 {#set-up-ollama-with-a-manually-downloaded-qiskit-code-assistant-gguf-model}

https://huggingface.co/Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF와 같은 GGUF 모델을 수동으로 다운로드하고 다양한 템플릿 및 파라미터로 실험하려면 다음 단계를 따라 로컬 Ollama 애플리케이션에 로드할 수 있습니다.

1. 다음 내용을 입력하여 `Modelfile`을 만들고, `<PATH-TO-GGUF-FILE>`을 다운로드한 모델의 실제 경로로 업데이트하세요.

    ```
    FROM <PATH-TO-GGUF-FILE>
    TEMPLATE """{{ if .System }}
    System:
    {{ .System }}

    {{ end }}{{ if .Prompt }}Question:
    {{ .Prompt }}

    {{ end }}Answer:
    ```python{{ .Response }}
    """

    PARAMETER stop "Question:"
    PARAMETER stop "Answer:"
    PARAMETER stop "System:"
    PARAMETER stop "```"

    PARAMETER temperature 0
    PARAMETER top_k 1
    ```

1. Run the following command to create a custom model instance based on the `Modelfile`.

    ```
    ollama create Qwen2.5-Coder-14B-Qiskit -f ./path-to-model-file
    ```

    <Admonition type="note">This process may take some time for Ollama to read the model file, initialize the model instance, and configure it according to the specifications provided.</Admonition>

#### Run the Qiskit Code Assistant model manually downloaded in Ollama

After the `Qwen2.5-Coder-14B-Qiskit` model has been set up in Ollama, run the following command to launch the model and interact with it in the terminal (in chat mode).

```
ollama run Qwen2.5-Coder-14B-Qiskit
```

Some useful commands:

- `ollama list` - List models on your computer
- `ollama rm Qwen2.5-Coder-14B-Qiskit` - Delete the model
- `ollama show Qwen2.5-Coder-14B-Qiskit` - Show model information
- `ollama stop Qwen2.5-Coder-14B-Qiskit` - Stop a model that is currently running
- `ollama ps` - List which models are currently loaded

</details>

<details>

<summary>Manually deploy the Qiskit Code Assistant models in local through the `llama-cpp-python` package</summary>

<span id="use-llama-cpp-python"></span>
An alternative to the Ollama application is the `llama-cpp-python` package, which is a Python binding for `llama.cpp`. It gives you more control and flexibility to run the GGUF model locally, and is ideal for users who wish to integrate the local model in their workflows and Python applications.

1. Install [`llama-cpp-python`](https://pypi.org/project/llama-cpp-python/)
2. Interact with the model from within your application using `llama_cpp`. For example:

```python
from llama_cpp import Llama

model_path = <PATH-TO-GGUF-FILE>

model = Llama(
        model_path,
        seed=17,
        n_ctx=10000,
        n_gpu_layers=37, # to offload in gpu, but put 0 if all in cpu
    )

input = 'Generate a quantum circuit with 2 qubits'
raw_pred = model(input)["choices"][0]["text"]
```

You can also add text generation parameters to the model to customize the inference:

```python
generation_kwargs = {
        "max_tokens": 512,
        "echo": False, # Echo the prompt in the output
        "top_k": 1
    }

raw_pred = model(input, **generation_kwargs)["choices"][0]["text"]
```

</details>

<details>

<summary>Manually deploy the Qiskit Code Assistant models in local through llama.cpp</summary>
### Use the `llama.cpp` library

Another alternative is to use `llama.cpp`, an open-source library for performing LLM inference on a CPU with minimal setup.
It provides low-level control over the model execution and is typically run from the command line, pointing to a local GGUF model file.

There are several ways to install `llama.cpp` on your machine:

- Install llama.cpp using [brew, nix, or winget](https://github.com/ggml-org/llama.cpp/blob/master/docs/install.md)
- Run with Docker: See out the [Docker documentation by `llama.cpp` team](https://github.com/ggml-org/llama.cpp/blob/master/docs/docker.md)
- Download pre-built binaries from the [releases page](https://github.com/ggml-org/llama.cpp/releases)
- Build from source by [cloning this repository](https://github.com/ggml-org/llama.cpp/blob/master/docs/build.md)

Once installed, you can use `llama.cpp` to interact with GGUF models in conversation mode as follows:
```bash
# Use a local model file
llama-cli -m my_model.gguf -cnv

# Or download and run a model directly from Hugging Face
llama-cli -hf Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF -cnv
```

You can also launch an OpenAI-compatible API server for the model in the following way:
```bash
llama-server -hf Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF
```

#### Advanced parameters
With the `llama-cli` program, you can control the model generation using command-line options. For example, you can provide an initial “system” prompt using the `-p/--prompt` flag. In conversation mode (`-cnv`), this initial prompt acts as the system message. Otherwise, you can simply prepend any desired instruction to your prompt text. You can also adjust sampling parameters - for instance: temperature (`--temp`), top-k (`--top-k`), top-p (`--top-p`), repetition penalty (`--repeat-penalty`), and the seed to use (`--seed`). The following is an example invocation using these options:

```bash
llama-cli -hf Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF \
  -p "You are a friendly assistant." -cnv \
  --temp 0.7 \
  --top-k 50 \
  --top-p 0.95 \
  --repeat-penalty 1.1 \
  --seed 42
```

Qiskit 모델의 올바른 기능을 보장하기 위해 HF GGUF 저장소에 제공된 시스템 프롬프트를 사용하는 것을 권장합니다: [mistral-small-3.2-24b-qiskit-GGUF](https://huggingface.co/Qiskit/mistral-small-3.2-24b-qiskit-GGUF/blob/main/system), [Qwen2.5-Coder-14B-Qiskit-GGUF](https://huggingface.co/Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF/blob/main/system), [granite-3.3-8b-qiskit-GGUF](https://huggingface.co/Qiskit/granite-3.3-8b-qiskit-GGUF/blob/main/system), [granite-3.2-8b-qiskit-GGUF](https://huggingface.co/Qiskit/granite-3.2-8b-qiskit-GGUF/blob/main/system)의 시스템 프롬프트를 참조하세요.

</details>

<details>

<summary>로컬 배포에 익스텐션 수동 연결</summary>

Qiskit Code Assistant용 VS Code 익스텐션 및 JupyterLab 익스텐션을 사용하여 로컬에 배포된 Qiskit Code Assistant 모델에 프롬프트를 입력합니다. [모델이 설정된 Ollama 애플리케이션](#using-the-ollama-application)이 준비되면 익스텐션이 로컬 서비스에 연결되도록 구성할 수 있습니다.

### Qiskit Code Assistant VS Code 익스텐션으로 연결 {#connect-with-the-qiskit-code-assistant-vs-code-extension}

Qiskit Code Assistant VS Code 익스텐션을 사용하면 코드 작성 중 모델과 상호 작용하고 코드 완성을 수행할 수 있습니다. Python 애플리케이션을 위한 Qiskit 코드 작성에 도움을 원하는 사용자에게 효과적입니다.

1. [Qiskit Code Assistant VS Code 익스텐션](/guides/qiskit-code-assistant-vscode)을 설치합니다.
1. VS Code에서 **사용자 설정**으로 이동하여 **Qiskit Code Assistant: Url**을 로컬 Ollama 배포 URL로 설정합니다 (예: `http://localhost:11434`).
1. **보기 > 명령 팔레트...**로 이동하여 **개발자: 창 다시 로드**를 선택하여 VS Code를 다시 로드합니다.

Ollama에 구성된 Qiskit Code Assistant 모델이 상태 표시줄에 나타나며 사용 준비가 완료됩니다.

### Qiskit Code Assistant JupyterLab 익스텐션으로 연결 {#connect-with-the-qiskit-code-assistant-jupyterlab-extension}

Qiskit Code Assistant JupyterLab 익스텐션을 사용하면 Jupyter Notebook에서 직접 모델과 상호 작용하고 코드 완성을 수행할 수 있습니다. 주로 Jupyter Notebook으로 작업하는 사용자는 이 익스텐션을 활용하여 Qiskit 코드 작성 경험을 더욱 향상시킬 수 있습니다.

1. [Qiskit Code Assistant JupyterLab 익스텐션](/guides/qiskit-code-assistant-jupyterlab)을 설치합니다.
1. JupyterLab에서 **설정 편집기**로 이동하여 **Qiskit Code Assistant Service API**를 로컬 Ollama 배포 URL로 설정합니다 (예: `http://localhost:11434`).

Ollama에 구성된 Qiskit Code Assistant 모델이 상태 표시줄에 나타나며 사용 준비가 완료됩니다.

</details>
