---
title: Configurează utilizarea IBM Quantum Platform cu REST API
description: Instrucțiuni de configurare pentru utilizarea IBM Quantum pe IBM Cloud pentru a trimite joburi Qiskit SDK și Qiskit Runtime folosind REST API
platform: cloud
---

{/* doqumentation-source-hash: a3985acf */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore urlendcoded, WQVY */}

# Configurează utilizarea IBM Quantum Platform cu REST API {#set-up-to-use-ibm-quantum-platform-with-rest-api}

Poți accesa procesoarele cuantice prin REST API, ceea ce îți permite să lucrezi cu QPU-uri folosind orice limbaj de programare sau framework.

### 1. Obține acces {#1-get-access}

1. Dacă nu ai deja un cont de utilizator, creează unul pe [pagina de autentificare IBM Quantum.](https://quantum.cloud.ibm.com/login)
1. Creează o cheie API (numită și token) pe [panou de control.](https://quantum.cloud.ibm.com) Reține că aceeași cheie API poate fi folosită pentru oricare dintre regiuni.
1. Generează un token bearer IBM Cloud Identity and Access Management (IAM). Acesta este un token de scurtă durată folosit pentru autentificarea cererilor către REST API. Pentru a-l genera, apelează [IAM Identity Services API](https://cloud.ibm.com/apidocs/iam-identity-token-api#create-api-key) așa cum este arătat în exemplul de cerere de mai jos:
<Tabs>

   <TabItem value="Curl" label="Curl">
   ```bash
   curl -X POST 'https://iam.cloud.ibm.com/identity/token' \
   -H 'Content-Type: application/x-www-form-urlencoded' \
   -d 'grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=MY_APIKEY'
   ```
   Răspuns așteptat
   ```text
   {
      "access_token": "eyJhbGciOiJIUz......sgrKIi8hdFs",
      "refresh_token": "SPrXw5tBE3......KBQ+luWQVY=",
      "token_type": "Bearer",
      "expires_in": 3600,
      "expiration": 1473188353
   }
   ```
   </TabItem>

   <TabItem value="Python" label="Python">
   ```python
   # Use 'service' to invoke operations.
   import requests
   import json

   url = 'https://iam.cloud.ibm.com/identity/token'
   api_key = 'MY_APIKEY'
   headers = {
    'Content-Type': 'application/x-www-form-urlendcoded',
   }
   data = f'grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey={api_key}'
   response = requests.post(url, headers=headers, data=data)

   # Bearer token to authorize requests to the REST API
   bearer_token = response.json()['access_token']
   ```
   </TabItem>
</Tabs>

### 2. Alege o metodă de autentificare {#2-choose-an-authentication-method}

Alege metoda de autentificare potrivită, în funcție de mediul tău de lucru:

* [Creează o variabilă de mediu pentru cheia ta API (medii Python de încredere)](#environment_variable)
* [Folosește cheia API direct (mediu neîncrezător)](/guides/cloud-setup-untrusted#rest-untrusted)

<span id="environment_variable"></span>
#### Creează o variabilă de mediu (mediu de încredere) {#create-an-environment-variable-trusted-environment}

1. Pentru a seta variabila de mediu IQP_API_TOKEN în sistemul tău, poți adăuga următoarea linie în profilul tău de shell (de exemplu, .bashrc sau .zshrc) sau o poți seta direct în terminal:

    ```shell
    export IQP_API_TOKEN=<your-API_KEY> # Use the 44-character API_KEY you created and saved from the IBM Quantum Platform Home dashboard
    ```
    Când apelezi variabila de mediu în codul tău, include `import os`, ca în acest exemplu:

    ```python
    import os
    api_token = os.environ['IQP_API_TOKEN']
    ```
    Reține că atunci când creezi o variabilă de mediu, cheia ta API este în continuare stocată local în text simplu și trebuie protejată corespunzător.

1. Autentifică cererile către Qiskit Runtime REST API includând CRN-ul și tokenul bearer în antetele cererii.
```bash
curl -X 'GET' \
   'https://quantum.cloud.ibm.com/api/v1/usage' \
   '-H accept: application/json' \
   '-H authorization: Bearer <BEARER_TOKEN>' \
   '-H Service-CRN: <INSTANCE_CRN>'
```

### 3. Opțional: Configurează firewall-ul {#3-optional-configure-your-firewall}

    Dacă este necesar, folosește [aceste informații](/guides/quickstart-steps-org#firewall) pentru a activa accesul la endpoint-urile IBM Quantum API.

## Pași următori {#next-steps}

<Admonition type="tip" title="Recomandări">
  -  [Prezentare generală a planurilor disponibile.](plans-overview)
  -  [Configurează Qiskit SDK local.](configure-qiskit-local)
  -  Urmează pașii din [Hello world](/guides/hello-world) pentru a scrie și rula un program cuantic.
  -  Încearcă un [tutorial](https://docs.quantum.ibm.com/tutorials).
</Admonition>
