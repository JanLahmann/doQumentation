---
title: "Estados de referencia"
sidebar_label: "Estados de referencia"
description: "Esta lección describe los estados de referencia: la inicialización de un estado cuántico antes de introducir parámetros variacionales."
notebook_path: "learning/courses/variational-algorithm-design/reference-states.ipynb"
---
{/* doqumentation-source-hash: 2e18844a */}

<OpenInLabBanner notebookPath="learning/courses/variational-algorithm-design/reference-states.ipynb" />



En esta lección aprenderemos cómo inicializar nuestro sistema con un <DefinitionTooltip definition="El punto de partida inicial y fijo para nuestro problema. Típicamente se refiere a |0>, pero podemos configurarlo a diferentes estados que codifiquen adecuadamente nuestro problema.">estado de referencia</DefinitionTooltip> para acelerar la convergencia de nuestro algoritmo variacional. Primero aprenderemos a construir un estado de referencia manualmente y luego exploraremos diferentes opciones estándar que pueden utilizarse en un algoritmo variacional.

![Diagrama de opciones para estados de referencia, incluyendo predeterminado, específico de la aplicación y estados de referencia cuánticos.](/learning/images/courses/variational-algorithm-design/reference-states/reference-workflow.svg)
## Estado predeterminado {#default-state}

Un *estado de referencia* designa el punto de partida inicial y fijo para nuestro problema. Para preparar un estado de referencia, debemos aplicar al inicio de nuestro circuito cuántico el operador unitario no parametrizado apropiado $U_R$, tal que $|\rho\rangle = U_R |0\rangle$. Si tienes una estimación fundamentada o un dato de una solución óptima ya conocida, el algoritmo variacional probablemente convergerá más rápido si lo usas como punto de partida.

El estado de referencia más simple posible es el estado predeterminado, en el que usamos el estado inicial de un circuito cuántico de $n$ qubits: $|0\rangle^{\otimes n}$. Para el estado predeterminado, $U_R \equiv I$. Debido a su simplicidad, el estado predeterminado es un estado de referencia válido que se emplea en muchos escenarios.
## Estado de referencia clásico {#classical-reference-state}

Supongamos que tienes un sistema de tres qubits y deseas comenzar en el estado $|001\rangle$ en lugar del estado predeterminado $|000\rangle$. Este es un ejemplo de un estado de referencia puramente clásico. Para construirlo, solo necesitas aplicar una [puerta X](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.XGate) al qubit $0$ (según el orden de qubits de Qiskit), ya que $|001\rangle = X_0 |000\rangle$.

En este caso, nuestro operador unitario es $U_R \equiv X_0$, lo que resulta en el estado de referencia $|\rho\rangle \equiv |001\rangle$.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3)
qc.x(0)

qc.draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/variational-algorithm-design/reference-states/extracted-outputs/cfd71225-1f9f-4a34-b825-ae19813956a8-0.avif)

## Estado de referencia cuántico {#quantum-reference-state}

Supongamos que deseas comenzar con un estado más complejo que involucre superposición y/o entrelazamiento, como por ejemplo $\frac{1}{\sqrt{2}}(|100\rangle+|111\rangle)$.

Para obtener este estado a partir de $|000\rangle$, se puede aplicar una [puerta Hadamard](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.HGate) al qubit $0$ ($H_0$), una puerta [CNOT (CX)](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.CXGate) con el qubit $0$ como qubit de control y el qubit $1$ como qubit objetivo ($CNOT_{01}$), y finalmente una puerta $X$ en el qubit $2$ ($X_2$).

En este escenario, nuestro operador unitario es $U_{R} \equiv X_2CNOT_{01}H_0|000\rangle$, y nuestro estado de referencia es $|\rho\rangle \equiv \frac{1}{\sqrt{2}}(|100\rangle+|111\rangle)$.

```python
qc = QuantumCircuit(3)
qc.h(0)
qc.cx(0, 1)
qc.x(2)

qc.draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/variational-algorithm-design/reference-states/extracted-outputs/77297158-60d7-469a-8a82-27d38a52d795-0.avif)

## Construcción de estados de referencia usando circuitos plantilla {#constructing-reference-states-using-template-circuits}

También podemos usar diferentes circuitos plantilla, como [`TwoLocal`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.TwoLocal), que permite una representación sencilla de múltiples parámetros ajustables y entrelazamientos. Estos circuitos plantilla los cubriremos con más detalle en la siguiente lección, pero podemos usarlos para nuestros estados de referencia, *siempre que* vinculemos los parámetros:

```python
from qiskit.circuit.library import TwoLocal
from math import pi

reference_circuit = TwoLocal(2, "rx", "cz", entanglement="linear", reps=1)
theta_list = [pi / 2, pi / 3, pi / 3, pi / 2]

reference_circuit = reference_circuit.assign_parameters(theta_list)

reference_circuit.decompose().draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/variational-algorithm-design/reference-states/extracted-outputs/8f3948ad-4caf-4ee6-8a72-bb91ed1bfeae-0.avif)

## Estados de referencia específicos de la aplicación {#application-specific-reference-states}
### Aprendizaje automático cuántico {#quantum-machine-learning}

En el contexto de un clasificador cuántico variacional (VQC), los datos de entrenamiento se codifican en un estado cuántico mediante un circuito parametrizado denominado *feature map*. Cada valor de parámetro representa un punto de datos del conjunto de datos de entrenamiento. La `zz_feature_map` es un tipo de circuito parametrizado que se puede utilizar para pasar nuestros puntos de datos ($x$) a este feature map.

```python
from qiskit.circuit.library import zz_feature_map

data = [0.1, 0.2]

zz_feature_map_reference = zz_feature_map(feature_dimension=2, reps=2)
zz_feature_map_reference = zz_feature_map_reference.assign_parameters(data)
zz_feature_map_reference.decompose().draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/variational-algorithm-design/reference-states/extracted-outputs/4bfd5a76-0db3-499d-bda1-0cb95c3f23c2-0.avif)

## Resumen {#summary}

En esta lección aprendiste cómo inicializar tu sistema utilizando los siguientes métodos:

- Estado de referencia predeterminado
- Estados de referencia clásicos
- Estados de referencia cuánticos
- Estados de referencia específicos de la aplicación

Nuestro flujo de trabajo variacional de alto nivel se ve así:

![Un diagrama de circuito de un operador unitario que prepara un estado de referencia.](/learning/images/courses/variational-algorithm-design/reference-states/reference-circuit.svg)

Mientras que los estados de referencia son puntos de partida fijos, podemos usar una *forma variacional* para definir un *ansatz* que represente una colección de estados parametrizados que nuestro algoritmo variacional puede explorar.
