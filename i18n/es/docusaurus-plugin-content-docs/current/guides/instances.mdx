---
title: Crear y gestionar instancias
description: Crea, actualiza y usa instancias para ejecutar trabajos con planes gratuitos o de pago.
---
{/* doqumentation-source-hash: 30bbb781 */}
# Crear y gestionar instancias {#create-and-manage-instances}

El acceso a los servicios de IBM Quantum Platform está controlado por las **instancias** a las que estás asignado. Los usuarios pueden pertenecer a múltiples instancias.

Las instancias son servidores virtuales (despliegues de Qiskit Runtime) que gestionan la ejecución de tus cargas de trabajo, incluida la ejecución de programas cuánticos y tareas de cómputo clásico (como el procesamiento de mitigación de errores). Las instancias se identifican por su *Cloud Resource Name* (CRN). Este CRN se puede usar al enviar cargas de trabajo desde tus programas de Qiskit.

Puedes ver las instancias a las que tienes acceso en el panel de control, o haciendo clic en la pestaña "Instances" desde el panel de control. Cada instancia aparece listada con su identificador CRN.

Necesitas una instancia diferente para cada plan de servicio (como Open o Pay-As-You-Go) que uses en cada región que quieras utilizar. El administrador de la cuenta y cualquier persona con permisos suficientes puede crear una instancia y definir su configuración, como sus asignaciones, límites de uso, computadoras cuánticas y permisos de usuario.

El [panel de control](https://quantum.cloud.ibm.com/) de IBM Quantum Platform muestra las instancias a las que tienes acceso. Según tu nivel de acceso, puedes ver los detalles completos, editar y eliminar instancias en la [página de Instancias](https://quantum.cloud.ibm.com/instances).

Es importante que entiendas qué instancia estás usando, ya que todas las instancias que no están asociadas con el Plan Open generan un costo (o cuentan para el tiempo asignado de tu plan).

<Admonition type="note">
Los trabajos que ejecutas tú y los trabajos que ejecutan otros colaboradores en la misma instancia cuentan para el uso reportado de esa instancia.

Puedes ver el uso de una instancia en la página de [Instancias](https://quantum.cloud.ibm.com/instances), o, para quienes tengan la autorización adecuada, en la página de [Analytics](https://quantum.cloud.ibm.com/analytics). Ten en cuenta que las páginas pueden mostrar cifras de uso distintas, ya que la página de Instancias muestra el uso total anterior a la marca de tiempo actual, mientras que la página de Analytics usa límites de día completo.
</Admonition>

<span id="open-plan"></span>
## Instancia del Plan Open {#open-plan}

Los usuarios del Plan Open pueden ejecutar circuitos cuánticos en las mejores QPUs del mundo de forma gratuita (hasta 10 minutos por ventana móvil de 28 días). El uso de las QPUs puede consultarse en el [panel de control de la Plataforma](https://quantum.cloud.ibm.com/) y en las páginas de [Workloads](https://quantum.cloud.ibm.com/workloads).

<span id="standard-plan"></span>
## Instancia del Plan Pay-As-You-Go {#standard-plan}

El Plan Pay-As-You-Go es un plan de pago. Para usar este plan, debes crear una instancia y asignarla al plan. Todas las cargas de trabajo enviadas a las QPUs con este plan generan un cargo. Las instancias de pago por uso pueden tener un límite de costo total (opcional).

<span id="flex-plan"></span>
## Instancia del Plan Flex {#flex-plan}

El Plan Flex es un plan de pago. Para usar este plan, primero debes [contactar con IBM](https://www.ibm.com/account/reg/us-en/signup?formid=MAIL-quantum) para registrarte. A continuación, crea una instancia y asígnala al plan. Todas las cargas de trabajo enviadas a las QPUs con este plan cuentan para tu suscripción asignada.

Un administrador puede establecer un límite de uso en una instancia del Plan Flex que se aplica durante la duración del contrato. Si tu administrador ha marcado una instancia como "limitada" (verás una columna "Remaining" en el área de uso) y la instancia supera su asignación (definida por tu administrador), cualquier carga de trabajo activa continuará ejecutándose (incluidas las sesiones), pero las cargas de trabajo pendientes permanecerán en la cola hasta que haya más tiempo disponible. Si una instancia no está limitada y supera su asignación, es probable que los trabajos ejecutados con esa instancia se ejecuten con menor prioridad y experimenten tiempos de espera más largos en la cola.

Se muestra una alerta en el uso de una instancia cuando esta ha superado su asignación.

<span id="premium-plan"></span>
## Instancia del Plan Premium {#premium-plan}

El Plan Premium es un plan de pago. Para usar este plan, primero debes [contactar con IBM](https://www.ibm.com/account/reg/us-en/signup?formid=MAIL-quantum) para registrarte. A continuación, crea una instancia y asígnala al plan. Todas las cargas de trabajo enviadas a las QPUs con este plan cuentan para tu suscripción asignada.

Un administrador puede establecer un límite de uso en una instancia del Plan Premium que se aplica a la ventana de uso móvil de 28 días del plan. Si tu administrador ha marcado una instancia como "limitada" (verás una columna "Remaining" en el área de uso) y la instancia supera su asignación (definida por tu administrador), cualquier carga de trabajo activa continuará ejecutándose (incluidas las sesiones), pero las cargas de trabajo pendientes permanecerán en la cola hasta que haya más tiempo disponible. Si una instancia no está limitada y supera su asignación, es probable que los trabajos ejecutados con esa instancia se ejecuten con menor prioridad y experimenten tiempos de espera más largos en la cola.

Se muestra una alerta en el uso de una instancia cuando esta ha superado su asignación.

<span id="connect-instance"></span>
## Especificar una instancia en tu código {#connect-instance}

Puedes usar el CRN o el nombre de la instancia para especificar qué instancia usar, ya sea al inicializar el servicio o al enviar la carga de trabajo a una QPU (por ejemplo, con [`QiskitRuntimeService.backend()](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/qiskit-runtime-service#backend)). Para obtener instrucciones sobre cómo especificar una instancia en tu código, consulta [Inicializar tu cuenta](/guides/initialize-account).

Cuando se pasa una instancia, solo están disponibles los backends y trabajos de esa instancia (permitido en Qiskit Runtime v0.40.1 y versiones posteriores).

Si no se incluye una instancia al inicializar el servicio, todos los backends y trabajos de todas las instancias de tu cuenta están disponibles. En ese caso, cuando se especifica un backend, se encuentra y usa una instancia que puede acceder a ese backend, tal como se describe en [Selección automática de instancia](/guides/initialize-account#auto-select).

<Admonition type="important" title="Versiones requeridas">
Se requiere Qiskit Runtime v0.40.1 o posterior para lo siguiente:

- Usar el nombre de la instancia para especificar una instancia
- No pasar una instancia
- Establecer un tipo de instancia y región preferidos

Para Qiskit Runtime v0.42 o posterior, los planes gratuitos se priorizan por defecto cuando se usa la [selección automática de instancia](/guides/initialize-account#auto-select).
</Admonition>

<span id="add-instance"></span>
## Crear una instancia {#add-instance}

Sigue estos pasos para crear una instancia y añadirla a tu cuenta. Si eres un administrador que necesita configurar Qiskit Runtime en Cloud para tu organización, consulta [Planificar Qiskit Runtime para una organización](/guides/considerations-set-up-runtime).

<span id="switcher"></span>

1. Para añadir una instancia a tu cuenta, primero asegúrate de tener la cuenta y la región correctas seleccionadas en el selector de cuentas del encabezado.
        * Puedes tener (o formar parte de) múltiples cuentas de IBM Cloud. Si no ves las instancias o los trabajos que esperas, prueba con una cuenta diferente.
        * Puedes acceder a cualquier región usando la misma clave de API, pero solo puedes ver y acceder a las instancias que se crearon en la región en la que has iniciado sesión.
        * Si quieres crear una instancia Open, debes elegir la región us-east.

        ![Se muestra el encabezado de IBM Quantum Platform. El selector de cuentas está inmediatamente a la derecha de la barra de búsqueda. El selector de región está inmediatamente a la derecha del selector de cuentas.](/docs/images/guides/cloud-setup/IQP-Header.svg 'Encabezado de IBM Quantum Platform')

   <span id="regions"></span>

   <Admonition type="note" title="Acerca de las regiones">
   La instancia se creará en la región seleccionada. Esta región determina las siguientes características importantes:
     * Qué QPUs están disponibles.
     * Dónde se realiza el cómputo clásico de los trabajos, como la compilación.
     * Dónde permanecen los datos del flujo de trabajo del usuario.

    Los datos del flujo de trabajo incluyen los circuitos de entrada, los parámetros del circuito y los resultados del cómputo cuántico. No incluyen metadatos, como el número de shots, el tamaño de los circuitos u otras estadísticas de los trabajos.

   Para evitar la exposición de los datos del flujo de trabajo del usuario a una capa global de protección contra Denegación de Servicio Distribuido (DDoS), puedes usar [Virtual Private Endpoints](https://cloud.ibm.com/docs/overview?topic=overview-endpoints-support). `qiskit-ibm-runtime` admite esto a través del [parámetro `private_endpoint`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/qiskit-runtime-service).
   </Admonition>

1. Desde el panel de Instancias del panel de control, haz clic en **Ver todo**, o ve a la página de [Instancias](https://quantum.cloud.ibm.com/instances) desde el menú principal.
1. Desde la página de Instancias, haz clic en **Crear instancia**.
    * Introduce un nombre y, opcionalmente, añade etiquetas.
    * Si tienes acceso a más de un grupo de recursos en IBM Cloud&reg;, puedes cambiar el grupo de recursos al que pertenece esta instancia. Consulta [Gestionar grupos de recursos](https://cloud.ibm.com/docs/account?topic=account-rgs&interface=ui) y [Otorgar acceso a recursos en grupos de recursos](https://cloud.ibm.com/docs/account?topic=account-rgs_manage_access) para más información.
1. Selecciona el plan de precios con el que se asocia esta instancia (por ejemplo, Pay-As-You-Go). Según el plan de precios seleccionado, puedes especificar otras opciones, como el límite de costo o el número de minutos a asignar a esta instancia. Ten en cuenta que si esta instancia no está asociada con un plan gratuito, se incurrirá en un costo cuando se use para ejecutar cargas de trabajo.
1. Haz clic en **Siguiente**. Se muestra una lista de las QPUs a las que la instancia puede acceder.
    - Si quieres que la instancia tenga acceso a todas las QPUs listadas (y a todas las QPUs que se añadan a este plan en el futuro), haz clic en **Siguiente**.
    - Si no estás configurando una instancia open, puedes personalizar a qué QPUs se puede acceder con esta instancia. Haz clic en el interruptor "Customize allocated compute resources". Selecciona las QPUs específicas a las que esta instancia puede acceder y, a continuación, haz clic en **Siguiente**.
        <Admonition type="note">
      Si personalizas los recursos de cómputo asignados, las QPUs disponibles nunca se actualizarán automáticamente, independientemente de los cambios realizados en el plan padre. Sin embargo, puedes añadir o eliminar QPUs manualmente más adelante. Si no las personalizas, siempre tendrás acceso a cualquier QPU a la que la cuenta tenga acceso en el plan. Por tanto, si en el futuro se añade una nueva QPU al plan, la instancia tendrá acceso a ella automáticamente.
        </Admonition>
1. Se abre el panel de grupos de acceso. Se crea automáticamente un grupo de acceso "collaborators" para esta instancia. Los usuarios añadidos a este grupo de acceso pueden usar el tiempo asignado a esta instancia. Puedes [modificar o crear grupos de acceso adicionales](/guides/access-groups) usando la [consola de IBM Cloud](https://cloud.ibm.com/iam/groups). Consulta [Configurar grupos de acceso](https://cloud.ibm.com/docs/account?topic=account-groups&interface=ui) para obtener instrucciones.
1. Haz clic en **Crear instancia**. Ahora puedes ver la instancia en la página de [Instancias](https://quantum.cloud.ibm.com/instances). Si has configurado múltiples planes en tu cuenta, cada tipo de plan tiene su propia pestaña en la tabla de Instancias.

<Admonition type="note" title="Cargas de trabajo pendientes">
La columna Cargas de trabajo pendientes en la tabla de Instancias muestra el número de cargas de trabajo actualmente en cola en esta instancia en todas las QPUs.
</Admonition>

## Editar una instancia y su asignación {#edit-an-instance-and-its-allocation}

Encuentra la instancia en la página de [Instancias](https://quantum.cloud.ibm.com/instances). Haz clic en el menú de desbordamiento al final de la fila de la instancia y selecciona Editar detalles.

<Admonition type="note">
Solo los administradores y propietarios pueden editar la instancia. Consulta la guía [Crear políticas de acceso y grupos de acceso](/guides/access-groups) para obtener información sobre cómo asignar acceso.
</Admonition>

## Instancias archivadas {#archived-instances}

Una instancia archivada es el resultado de uno de los siguientes escenarios.

### Un usuario con privilegios ha archivado la cuenta {#a-privileged-user-has-archived-the-account}

Un usuario con privilegios suficientes para la cuenta ha archivado la instancia.
    - Cualquier trabajo pendiente en la instancia se cancela.
    - Las instancias archivadas aparecen en una sección "Archivo" separada en la página de Instancias.
    - Los trabajos aún pueden recuperarse.
    - Los usuarios no pueden enviar nuevos trabajos a la instancia archivada.
    - La instancia archivada no cuenta para la asignación de tu plan.
    - Un usuario con privilegios suficientes puede reactivar una instancia que ellos (u otro usuario con privilegios) archivaron en cualquier momento.

Para archivar una instancia que gestionas, ve a la página de [Instancias](https://quantum.cloud.ibm.com/instances). Al final de la fila de esa instancia, haz clic en el menú de desbordamiento de tres puntos a la derecha y selecciona *Archivar instancia*.

### La cuenta ya no tiene acceso al plan de la instancia {#account-no-longer-has-access-to-the-instances-plan}

La cuenta ya no tiene acceso al plan bajo el cual se creó la instancia.

- Las instancias archivadas aparecen en una sección "Archivo" separada en la página de Instancias.
- Los trabajos aún pueden recuperarse.
- Los usuarios no pueden enviar nuevos trabajos a la instancia archivada.
- La instancia archivada no cuenta para la asignación de tu plan.
- Las instancias que han sido archivadas porque ya no tienen visibilidad del plan de la instancia no pueden reactivarse.

## Próximos pasos {#next-steps}

<Admonition type="tip" title="Recomendaciones">
- [Trabajar con grupos de acceso](/guides/access-groups).
- [Gestionar usuarios](/guides/invite-and-manage-users).
- [Guardar tus credenciales de inicio de sesión](/guides/save-credentials).
- [Inicializar el servicio Qiskit Runtime](/guides/initialize-account) en un entorno Python de confianza.
- [Inicializar el servicio Qiskit Runtime en un **entorno no confiable**](/guides/cloud-setup-untrusted).
</Admonition>
