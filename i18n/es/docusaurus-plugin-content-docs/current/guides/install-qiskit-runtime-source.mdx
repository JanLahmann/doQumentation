---
description: 'Aprende a instalar la versión de desarrollo del cliente de IBM Quantum Compute.'
title: 'Instalar el cliente de IBM Quantum Compute desde el código fuente'
---

{/* doqumentation-source-hash: 97061531 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore rustup */}

# Instalar el cliente de IBM Quantum Compute desde el código fuente {#install-the-ibm-quantum-compute-client-from-source}

Cuando instalas `qiskit-ibm-runtime` desde el código fuente, puedes acceder a la versión de desarrollo actual, en lugar de usar la versión del repositorio Python Package Index (PyPI). Por lo tanto, puedes inspeccionar y ampliar la última versión del código de manera más eficiente.

## Crear y activar un nuevo entorno virtual {#create-and-activate-a-new-virtual-environment}

1. Navega al directorio de tu proyecto y crea un entorno mínimo con solo Python instalado.


        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">

            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">

            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">

            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

1. Activa tu nuevo entorno.


         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">

             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">

             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">

             ```text
             .venv\Scripts\Activate.ps1
             ```
           </TabItem>
         </OperatingSystemTabs>

## Instalar `qiskit-ibm-runtime` {#install-qiskit-ibm-runtime}

Sigue estos pasos para instalar `qiskit-ibm-runtime`:

1. Clona el repositorio de `qiskit-ibm-runtime`.


```bash
git clone https://github.com/Qiskit/qiskit-ibm-runtime.git
```

2. Cambia al directorio `qiskit-ibm-runtime`.


```bash
cd qiskit-ibm-runtime
```

3. Instala `qiskit-runtime`. Recomendamos usar un [entorno virtual](https://docs.python.org/3/library/venv.html) para evitar contaminar tu instalación global de Python.


  * **Instalación estándar**:


    ```bash
    pip install .
    ```

  * **Modo editable**: En este modo, no necesitas reinstalar Qiskit cuando haya cambios de código en el proyecto.


    ```bash
    pip install -e .
    ```

    En modo editable, las extensiones compiladas se construyen en _modo de depuración_ sin optimizaciones.

4. (Opcional) Si quieres ejecutar pruebas o verificaciones de linting, instala los requisitos de desarrollador. Recomendamos usar un [entorno virtual](https://docs.python.org/3/library/venv.html) para evitar contaminar tu instalación global de Python.


```bash
pip install -e ".[dev]"
```

## Próximos pasos {#next-steps}

<Admonition type="tip" title="Recomendaciones">
  - Lee las [directrices de contribución](https://github.com/Qiskit/qiskit-ibm-runtime/blob/main/CONTRIBUTING.md) para contribuir a `qiskit-ibm-runtime`.
  - [Instala el SDK de Qiskit desde el código fuente](/guides/install-qiskit-source).
  - [Ejecuta tu primer programa](/guides/hello-world).
  - Prueba un tutorial, como [el algoritmo de Grover](/tutorials/grovers-algorithm).
</Admonition>
