---
title: "مرحباً بالعالم: دائرتك الكمية الأولى"
sidebar_label: "مرحباً بالعالم: دائرتك الكمية الأولى"
notebook_path: "hello-world.ipynb"
---
{/* doqumentation-source-hash: ad455993 */}

<OpenInLabBanner notebookPath="hello-world.ipynb" description="تمّ إنشاء هذا الدرس التعليمي لصالح doQumentation." />

أنشئ [حالة بِل](https://en.wikipedia.org/wiki/Bell_state) (qubitان متشابكان) وشغِّلها بثلاث طرق:

1. **المحاكاة المثالية** — نتائج مثالية، لا حاجة إلى حساب
2. **المحاكاة الضوضائية** — تحاكي جهازاً حقيقياً، لا حاجة إلى حساب
3. **العتاد الكمي الحقيقي** — يتطلب حساب IBM Quantum مجانياً (خطوات الإعداد أدناه)
## بناء الدائرة {#build-the-circuit}

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit qiskit-ibm-runtime
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

qc.draw(output="mpl")
```

## الخيار الأول: المحاكاة المثالية (لا حاجة إلى حساب) {#option-1-ideal-simulation-no-account-needed}

يستخدم `StatevectorSampler` — محاكياً محلياً يُعطي نتائج مثالية خالية من الضوضاء.

```python
from qiskit.primitives import StatevectorSampler

result = StatevectorSampler().run([qc], shots=1024).result()
counts = result[0].data.meas.get_counts()
counts
```

```python
from qiskit.visualization import plot_histogram
plot_histogram(counts)
```

## الخيار الثاني: المحاكاة الضوضائية (لا حاجة إلى حساب) {#option-2-noisy-simulation-no-account-needed}

يستخدم `FakeManilaV2` — محاكياً محلياً يُحاكي جهاز IBM الكمي الحقيقي، بما في ذلك خصائص الضوضاء الخاصة به. يجب أولاً تحويل الدائرة (transpile) لتتوافق مع مجموعة البوابات واتصالية qubit الخاصة بالجهاز.

```python
from qiskit_ibm_runtime import SamplerV2
from qiskit_ibm_runtime.fake_provider import FakeManilaV2
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

backend = FakeManilaV2()
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(qc)

result = SamplerV2(mode=backend).run([isa_qc], shots=1024).result()
counts = result[0].data.meas.get_counts()
counts
```

```python
plot_histogram(counts)
```

## الخيار الثالث: العتاد الكمي الحقيقي {#option-3-real-quantum-hardware}

يتطلب حساب IBM Quantum مجانياً. لإنشاء حساب:

1. سجِّل على [quantum.cloud.ibm.com/registration](https://quantum.cloud.ibm.com/registration) — لا بطاقة ائتمانية مطلوبة خلال أول 30 يوماً
2. سجِّل الدخول على [quantum.cloud.ibm.com](https://quantum.cloud.ibm.com) واختر المنطقة **us-east** (مطلوبة للخطة المفتوحة المجانية)
3. أنشئ instance (الخطة المفتوحة المجانية) من صفحة [Instances](https://quantum.cloud.ibm.com/instances) إن لم يكن لديك واحدة بعد
4. أنشئ مفتاح API على [quantum.cloud.ibm.com](https://quantum.cloud.ibm.com) (أو على [cloud.ibm.com/iam/apikeys](https://cloud.ibm.com/iam/apikeys))
5. انسخ **CRN** (Cloud Resource Name) من صفحة [Instances](https://quantum.cloud.ibm.com/instances) الخاصة بك

ثم، إذا لم تكن قد حفظت بيانات الاعتماد بعد في جلسة Binder هذه، شغِّل الخلية أدناه. استبدل `<your-api-key>` بمفتاح API من الخطوة الرابعة، و`<your-crn>` بـ CRN من الخطوة الخامسة.

```python
from qiskit_ibm_runtime import QiskitRuntimeService

QiskitRuntimeService.save_account(
    channel="ibm_quantum_platform",
    token="<your-api-key>",
    instance="<your-crn>",
    set_as_default=True,
    overwrite=True,
)
```

**ملاحظة:** قد تستغرق المهام على العتاد الحقيقي بعض الوقت بحسب أوقات الانتظار في قائمة الانتظار. إذا كانت الخلية لا تزال تعمل، يمكنك التحقق من حالة مهمتك وعرض النتائج على [quantum.cloud.ibm.com/workloads](https://quantum.cloud.ibm.com/workloads?user=me).

```python
from qiskit_ibm_runtime import QiskitRuntimeService, SamplerV2
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)
print(f"Running on {backend.name}")

pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(qc)

result = SamplerV2(mode=backend).run([isa_qc], shots=1024).result()
counts = result[0].data.meas.get_counts()
counts
```

```python
plot_histogram(counts)
```

## ما الخطوة التالية؟ {#what-next}

- **[الدروس التعليمية](https://doqumentation.org/tutorials)** — أدلة خطوة بخطوة حول الخوارزميات، وتخفيف الأخطاء، والتحويل (transpilation)، وغير ذلك
- **[الأدلة المرجعية](https://doqumentation.org/guides)** — مواد مرجعية حول تشغيل الدوائر، والـ primitives، وقنطرة Qiskit Runtime
- **[الدورات](https://doqumentation.org/learning/courses/basics-of-quantum-information)** — مسارات تعلُّم منظَّمة من أساسيات الكم إلى الحوسبة على نطاق واسع
- **[الوحدات](https://doqumentation.org/learning/modules/computer-science)** — وحدات مفاهيمية متعمقة في علوم الحاسوب والميكانيكا الكمية
