---
title: "البداية السريعة مع Executor"
sidebar_label: "البداية السريعة مع Executor"
description: "كيفية استخدام Executor primitive في Qiskit Runtime."
notebook_path: "guides/get-started-with-executor.ipynb"
---

{/* doqumentation-source-hash: 93976e84 */}


<OpenInLabBanner notebookPath="guides/get-started-with-executor.ipynb" />


{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="إصدارات الحزم">

تم تطوير الكود في هذه الصفحة باستخدام المتطلبات التالية.
نوصي باستخدام هذه الإصدارات أو أحدث.

```
qiskit[all]~=2.4.0
qiskit-ibm-runtime~=0.46.1
samplomatic~=0.18.0
```
</AccordionItem>
</Accordion>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit qiskit-ibm-runtime samplomatic
```

على غرار [Sampler](/guides/get-started-with-sampler) primitive، يأخذ Executor عينات من سجلات الإخراج من تنفيذ الـ Circuit الكمية، لكنه لا يتضمن أي تثبيط أو تخفيف مضمَّن للأخطاء. بدلاً من ذلك، فهو جزء من [نموذج التنفيذ الموجَّه](/guides/directed-execution-model) الذي يوفر المكونات اللازمة لالتقاط نوايا التصميم على جانب العميل، وينقل العبء المكلِّف لتوليد متغيرات الـ Circuit إلى جانب الخادم. يتبع Executor التوجيهات المُقدَّمة في تعليقات الـ Circuit والخيارات، ويولِّد قيم المعاملات ويربطها، وينفِّذ الـ Circuits المرتبطة على الأجهزة، ويُعيد نتائج التنفيذ والبيانات الوصفية. لا يتخذ أي قرارات ضمنية نيابةً عنك ويمنحك تحكمًا كاملاً وشفافية.

<Admonition type="note">
حزمة Qiskit لا تمتلك بعد فئة أساسية لـ Executor primitive.
</Admonition>

## قبل البدء {#before-you-begin}

بعض أمثلة الكود في هذه الصفحة تستخدم `samplex`، وهو جزء من حزمة Samplomatic. لذلك، قبل تشغيل تلك الكتل البرمجية، يجب تثبيت Samplomatic كما هو موضح في الكتلة البرمجية التالية. لمزيد من المعلومات، راجع [وثائق Samplomatic](https://qiskit.github.io/samplomatic).

```python
pip install samplomatic

# For visualization support, include the visualization dependencies.
# pip install samplomatic[vis]
```
## خطوات استخدام Executor primitive {#steps-to-use-the-executor-primitive}

### 1. تهيئة الحساب {#1-initialize-the-account}

بما أن Qiskit Runtime خدمة مُدارة، تحتاج أولاً إلى تهيئة حسابك. يمكنك بعد ذلك اختيار وحدة المعالجة الكمية (QPU) التي تريد استخدامها لحساب قيمة التوقع.

اتبع الخطوات في [إعداد حساب IBM Cloud&reg; الخاص بك](/guides/cloud-setup) إن لم يكن لديك حساب بعد.

```python
from qiskit_ibm_runtime import QiskitRuntimeService, Executor
from qiskit_ibm_runtime.quantum_program import QuantumProgram
from qiskit.circuit import QuantumCircuit
from qiskit.transpiler import generate_preset_pass_manager
from samplomatic.transpiler import generate_boxing_pass_manager
from samplomatic import build

# Initialize the service and choose a backend
service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)
```

```python
print(backend)
```

```text
<IBMBackend('ibm_fez')>
```

### 2. إنشاء Circuit وعمل الـ Transpile {#2-create-and-transpile-a-circuit}

تحتاج إلى Circuit واحدة على الأقل لاستخدام Executor primitive. يمكن أن تحتوي اختياريًا على معاملات.

```python
# Generate the circuit
circuit = QuantumCircuit(2)
circuit.h(0)
circuit.h(1)
circuit.cz(0, 1)
circuit.h(1)

# Using `measure_all` automatically creates the necessary
# classical registers.
circuit.measure_all()
```

يجب تحويل الـ Circuit لتستخدم فقط التعليمات المدعومة من قِبَل وحدة المعالجة الكمية (المُشار إليها بـ *دوائر مجموعة تعليمات المعمارية (ISA)*). استخدم الـ Transpiler للقيام بذلك.

```python
# Transpile the circuit
preset_pass_manager = generate_preset_pass_manager(
    backend=backend, optimization_level=0
)
isa_circuit = preset_pass_manager.run(circuit)
```

### 3. تهيئة `QuantumProgram` {#3-initialize-a-quantumprogram}

هيِّئ `QuantumProgram` مع حمل العمل الخاص بك. يتكون `QuantumProgram` من `QuantumProgramItems`. عادةً ما يتضمن كل عنصر Circuit ومجموعة قيم معاملات وربما `samplex` لعشوائية محتوى الـ Circuit. للاطلاع على التفاصيل الكاملة، راجع [مدخلات ومخرجات Executor](/guides/executor-input-output).

الخلية التالية تُهيِّئ `QuantumProgram` وتُحدِّد إجراء 25 تشغيلاً. بعد ذلك، تُلحِق الـ Circuit المستهدفة بعد الـ Transpile.

```python
# Initialize an empty program
program = QuantumProgram(shots=25)

# Append the circuit to the program
program.append_circuit_item(isa_circuit)
```

### 4. اختياري: تجميع البوابات والقياسات في صناديق مُعلَّقة {#4-optional-group-gates-and-measurements-into-annotated-boxes}

تجميع التعليمات في صناديق وتعليقها هو الطريقة الأساسية لتحديد نيتك. في المثال التالي، نستخدم `generate_boxing_pass_manager` ومعاملات التدوير الخاصة به لتجميع بوابات ثنائية الـ Qubit والقياسات في صناديق وتطبيق تعليق التدوير.

```python
# Generate a boxing pass manager to group gates
# and measurements into boxes and add
# a`Twirl` annotation.
boxes_pm = generate_boxing_pass_manager(
    # Add gate twirling
    enable_gates=True,
    # Add measurement twirling
    enable_measures=True,
)

boxed_circuit = boxes_pm.run(isa_circuit)
boxed_circuit.draw("mpl", idle_wires=False)
```

![Output of the previous code cell](/docs/images/guides/get-started-with-executor/extracted-outputs/245a4574-3ce9-4f77-98c8-af32cde8ac01-0.svg)

### 5. اختياري: بناء Circuit نموذجية وsamplex وإضافتهما إلى البرنامج {#5-optional-build-a-template-circuit-and-samplex-and-add-them-to-the-program}

بعد ذلك، استخدم طريقة [build](https://qiskit.github.io/samplomatic/api/auto/samplomatic.build.html#samplomatic.build) في Samplomatic لتوليد زوج _الـ Circuit النموذجية_ و_samplex_. الـ Circuit النموذجية مكافئة هيكليًا للـ Circuit الأصلية. غير أن بواباتها أحادية الـ Qubit تُستبدَل ببوابات ذات معاملات لتطبيق التعليقات المُوصوفة (تدوير البوابات والقياسات في هذا المثال). يُرمِّز الـ samplex جميع المعلومات المطلوبة لتوليد معاملات عشوائية للـ Circuit النموذجية.

بعد توليد زوج الـ Circuit النموذجية والـ samplex، استخدم الطريقة `append_samplex_item` لإضافة الزوج إلى البرنامج.

راجع وثائق [API](https://qiskit.github.io/samplomatic/api/index.html) في Samplomatic للاطلاع على التفاصيل الكاملة حول `samplomatic.samplex.Samplex` وحججه.

```python
# Build the template circuit and the samplex
template_circuit, samplex = build(boxed_circuit)

# Append the template circuit and samplex as a `samplex_item`
program.append_samplex_item(
    template_circuit,
    samplex=samplex,
    shape=(num_randomizations := 20,),
)
```

### 6. استدعاء Executor والحصول على النتائج {#6-invoke-executor-and-get-results}

شغِّل `QuantumProgram` على Backend من IBM&reg; باستخدام `Executor` primitive مع الخيارات الافتراضية. راجع [خيارات Executor](/guides/executor-options) للاطلاع على الخيارات المتاحة.

```python
# Initialize an Executor with the default options
executor = Executor(mode=backend)

# Submit the job
job = executor.run(program)
job
```

```text
<RuntimeJobV2('d8286580bvlc73d1vmsg', 'executor')>
```

```python
# Retrieve the result
result = job.result()
```

النتيجة من النوع [`QuantumProgramResult`](https://qiskit.github.io/qiskit-ibm-runtime/stubs/qiskit_ibm_runtime.quantum_program.QuantumProgramResult.html#qiskit_ibm_runtime.quantum_program.QuantumProgramResult). راجع [مدخلات ومخرجات Executor](/guides/executor-input-output) للاطلاع على كائن النتيجة.
## الخطوات التالية {#next-steps}

<Admonition type="tip" title="توصيات">
- جرِّب بعض [أمثلة Executor](/guides/executor-examples).
- افهم [مدخلات ومخرجات Executor](/guides/executor-input-output).
- تعرَّف على [دلالات بث Executor](/guides/executor-broadcasting).
</Admonition>
