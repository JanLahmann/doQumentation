---
title: "قياس الكيوبتات"
sidebar_label: "قياس الكيوبتات"
description: "تعلّم كيفية قياس الكيوبتات، بما في ذلك القيود المتعلقة بأماكن استخدام القياسات."
notebook_path: "guides/measure-qubits.ipynb"
---

{/* doqumentation-source-hash: 421a17fa */}

<OpenInLabBanner notebookPath="guides/measure-qubits.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="إصدارات الحزم">

تم تطوير الكود في هذه الصفحة باستخدام المتطلبات التالية.
نوصي باستخدام هذه الإصدارات أو أحدث منها.

```
qiskit[all]~=2.4.0
```
</AccordionItem>
</Accordion>
للحصول على معلومات حول حالة الكيوبت، يمكنك _قياسه_ على [بت كلاسيكي](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Clbit). في Qiskit، تُجرى القياسات في الأساس الحسابي، أي أساس Pauli-$Z$ أحادي الكيوبت. لذلك، يُعطي القياس القيمة 0 أو 1، تبعاً للتداخل مع الحالات الذاتية لـ Pauli-$Z$ وهي $|0\rangle$ و$|1\rangle$:

$$
|q\rangle \xrightarrow{measure}\begin{cases}
      0 (\text{outcome}+1), \text{with probability } p_0=|\langle q|0\rangle|^{2}\text{,} \\
      1 (\text{outcome}-1), \text{with probability } p_1=|\langle q|1\rangle|^{2}\text{.}
    \end{cases}
$$

## تطبيق قياس على دائرة {#apply-a-measurement-to-a-circuit}

هناك عدة طرق لتطبيق القياسات على دائرة:

### طريقة `QuantumCircuit.measure` {#quantumcircuitmeasure-method}

استخدم طريقة [`measure`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure) لقياس [`QuantumCircuit`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#quantumcircuit-class).

أمثلة:

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(5, 5)
qc.x(0)
qc.x(1)
qc.x(4)
qc.measure(
    range(5), range(5)
)  #  Measures all qubits into the corresponding clbit.
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c636c20>
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure(1, 0)  # Measure qubit 1 into the classical bit 0.
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c636830>
```

### صنف `Measure` {#measure-class}

يقوم صنف Qiskit [Measure](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure) بقياس الكيوبتات المحددة.

```python
from qiskit.circuit import Measure

qc = QuantumCircuit(3, 1)
qc.x([0, 1])
qc.append(Measure(), [0], [0])  # measure qubit 0 into clbit 0
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c6369e0>
```

### طريقة `QuantumCircuit.measure_all` {#quantumcircuitmeasure_all-method}

لقياس جميع الكيوبتات في البتات الكلاسيكية المقابلة لها، استخدم طريقة [`measure_all`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_all). بشكل افتراضي، تضيف هذه الطريقة بتات كلاسيكية جديدة في `ClassicalRegister` لتخزين هذه القياسات.

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure_all()  # Measure all qubits.
```

### طريقة `QuantumCircuit.measure_active` {#quantumcircuitmeasure_active-method}

لقياس جميع الكيوبتات غير الخاملة، استخدم طريقة [`measure_active`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_active). تنشئ هذه الطريقة `ClassicalRegister` جديداً بحجم يساوي عدد الكيوبتات غير الخاملة المُراد قياسها.

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure_active()  # Measure qubits that are not idle, that is, qubits 0 and 2.
```

## الخطوات التالية {#next-steps}

<Admonition type="tip" title="توصيات">
- صنف [`Measure`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure)
- طريقة [`measure_all`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_all)
- طريقة [`measure_active`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_active)
- طريقة [`random_circuit`](https://docs.quantum.ibm.com/api/qiskit/circuit_random#qiskit.circuit.random.random_circuit)
- [Mid-circuit measurements](/guides/execute-dynamic-circuits#midcircuit) (Available only when using Qiskit Runtime.)
</Admonition>
