---
title: "تحديد خيارات Estimator"
sidebar_label: "تحديد خيارات Estimator"
description: "تحديد الخيارات عند البناء بالأداة البدائية Estimator."
notebook_path: "guides/estimator-options.ipynb"
---

{/* doqumentation-source-hash: cba98481 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<OpenInLabBanner notebookPath="guides/estimator-options.ipynb" />




{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="إصدارات الحزم">

الكود في هذه الصفحة تم تطويره باستخدام المتطلبات التالية.
نوصي باستخدام هذه الإصدارات أو أحدث.

```
qiskit[all]~=2.4.0
qiskit-ibm-runtime~=0.46.1
```
</AccordionItem>
</Accordion>

يمكنك استخدام الخيارات لتخصيص الأداة البدائية Estimator. بينما تكون واجهة طريقة `run()` للأدوات البدائية مشتركة عبر جميع التطبيقات، فإن خياراتها ليست كذلك. راجع مراجع API للحصول على معلومات حول خيارات [`qiskit.primitives.BaseEstimatorV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.primitives.BaseEstimatorV2) و [`qiskit_aer.BaseEstimatorV2`](https://qiskit.github.io/qiskit-aer/stubs/qiskit_aer.primitives.EstimatorV2.html).

<span id="pass-options"></span>
## ضبط خيارات Estimator {#set-estimator-options}

يمكنك ضبط الخيارات عند تهيئة Estimator، أو بعد تهيئته، أو يمكنك تحديث الخيارات بعد تهيئة Estimator. للحصول على تعليمات حول استخدام هذه الأساليب، راجع موضوع [مقدمة في الخيارات](/guides/runtime-options-overview#options-precedence).

بالإضافة إلى ذلك، يمكنك ضبط قيمة `precision` في طريقة `run()`، كما هو موضح في القسم التالي.

<span id="run-method"></span>
### طريقة Run() {#run-method}

القيم الوحيدة التي يمكنك تمريرها إلى `run()` هي تلك المعرّفة في الواجهة. أي، `precision` لـ Estimator. هذا يُستبدل أي قيمة تم ضبطها لـ `default_precision` في التشغيل الحالي.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit qiskit-ibm-runtime
```

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import EstimatorV2 as Estimator
from qiskit.circuit.library import random_iqp
from qiskit.transpiler import generate_preset_pass_manager
from qiskit.quantum_info import SparsePauliOp

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

circuit1 = random_iqp(3)
circuit1.measure_all()
circuit2 = random_iqp(3)
circuit2.measure_all()

observable = SparsePauliOp("Z" * 3)

pass_manager = generate_preset_pass_manager(
    optimization_level=3, backend=backend
)

transpiled1 = pass_manager.run(circuit1)
transpiled2 = pass_manager.run(circuit2)
isa_observable1 = observable.apply_layout(transpiled1.layout)
isa_observable2 = observable.apply_layout(transpiled2.layout)

estimator = Estimator(mode=backend)
# Default precision to use if not specified in run()
estimator.options.default_precision = 0.01
# Run two circuits, requiring a precision of .02 for both.
estimator.run(
    [(transpiled1, isa_observable1), (transpiled2, isa_observable2)],
    precision=0.02,
)
```

```text
<RuntimeJobV2('d82868ftjchs73bnokm0', 'estimator')>
```

### الحالة الخاصة: الدقة {#special-case-precision}

تقبل طريقة `EstimatorV2.run` وسيطتين: قائمة PUBs، يمكن لكل منها تحديد قيمة دقة خاصة بها، ووسيطة keyword للدقة. هذه القيم للدقة هي جزء من واجهة تنفيذ Estimator، وهي مستقلة عن خيارات Runtime Estimator. تأخذ الأولوية على أي قيم محددة كخيارات لتتوافق مع تجريد Estimator.

ومع ذلك، إذا لم يتم تحديد `precision` بواسطة أي PUB أو في وسيطة keyword للتشغيل (أو إذا كانت جميعها `None`)، فسيتم استخدام قيمة الدقة من الخيارات، وأبرزها `default_precision`.

لاحظ أن خيارات Estimator تحتوي على كل من `default_shots` و `default_precision`. ومع ذلك، نظراً لأن gate-twirling مفعّل بشكل افتراضي، فإن حاصل ضرب `num_randomizations` و `shots_per_randomization` يأخذ الأولوية على هذين الخيارين.

تحديداً، لأي Estimator PUB:

1. إذا حدد PUB الدقة، استخدم تلك القيمة.
2. إذا تم تحديد وسيطة keyword للدقة في `run`، استخدم تلك القيمة.
3. إذا كان `twirling` مفعّلاً (True بشكل افتراضي)، فسيتم استخدام حاصل ضرب `num_randomizations` و `shots_per_randomization`، كما هو محدد في [خيارات twirling](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-twirling-options).
4. إذا تم تحديد `estimator.options.default_shots`، استخدم تلك القيمة للتحكم في كمية البيانات.
5. إذا تم تحديد `estimator.options.default_precision`، استخدم تلك القيمة.

على سبيل المثال، إذا تم تحديد الدقة في جميع الأماكن الأربعة، فسيتم استخدام الذي يتمتع بأعلى أولوية (الدقة المحددة في PUB).

<Admonition type="note">
الدقة تتناسب عكسياً مع الاستخدام. أي، كلما كانت الدقة أقل، كلما استغرق تشغيلها وقتاً أطول من QPU.
</Admonition>

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import EstimatorV2 as Estimator
from qiskit.circuit.library import random_iqp
from qiskit.transpiler import generate_preset_pass_manager
from qiskit.quantum_info import SparsePauliOp

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

observable = SparsePauliOp("Z" * 3)

circuit = random_iqp(3)
circuit.measure_all()

pass_manager = generate_preset_pass_manager(
    optimization_level=3, backend=backend
)

isa_circuit = pass_manager.run(circuit)
isa_observable = observable.apply_layout(isa_circuit.layout)

# Setting precision during primitive initialization
estimator = Estimator(mode=backend, options={"default_precision": 0.05})

# Run with precision=0.02, overwriting the default.
estimator.run(
    [(isa_circuit, isa_observable1)],
    precision=0.02,
)
```

```text
<RuntimeJobV2('d8286b00bvlc73d1vn50', 'estimator')>
```

<span id="no-error-mitigation"></span>
## إيقاف تشغيل جميع تخفيف الأخطاء وقمع الأخطاء {#turn-off-all-error-mitigation-and-error-suppression}

يمكنك إيقاف تشغيل جميع تخفيف وقمع الأخطاء إذا كنت، على سبيل المثال، تجري بحثاً في تقنيات التخفيف الخاصة بك. لتحقيق ذلك، قم بضبط `resilience_level = 0`.

مثال:

```python
from qiskit_ibm_runtime import EstimatorV2 as Estimator, QiskitRuntimeService

# Define the service.  This allows you to access an IBM QPU.
service = QiskitRuntimeService()

# Get a backend
backend = service.least_busy(operational=True, simulator=False)

# Define Estimator
estimator = Estimator(backend)

options = estimator.options

# Turn off all error mitigation and suppression
options.resilience_level = 0
```

<span id="options-table"></span>
## الخيارات المتاحة {#available-options}

يوثّق الجدول التالي الخيارات من أحدث إصدار من `qiskit-ibm-runtime`. لمشاهدة إصدارات الخيارات الأقدم، تفضل بزيارة [مرجع API لـ `qiskit-ibm-runtime`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime) واختر إصداراً سابقاً.

<Accordion>
<AccordionItem title="`default_shots`">

إجمالي عدد الـ shots المستخدمة لكل دائرة لكل تكوين.

**الخيارات**: عدد صحيح >= 0

**الافتراضي**: None

[وثائق API لـ `default_shots`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#default_shots)
  </AccordionItem>

<AccordionItem title="`default_precision`">

الدقة الافتراضية لاستخدامها لأي PUB أو استدعاء `run()` لا يحدد واحدة.

**الخيارات**: Float > 0

**الافتراضي**: 0.015625 (1 / sqrt(4096))

[وثائق API لـ `default_precision`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#default_precision)
  </AccordionItem>

<AccordionItem title="`dynamical_decoupling`">

التحكم في إعدادات تخفيف الأخطاء بالفصل الديناميكي.

[وثائق API لـ `dynamical_decoupling`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#dynamical_decoupling)
<Accordion>
<AccordionItem title="`dynamical_decoupling.enable`">

**الخيارات**: `True`, `False`

**الافتراضي**: `False`
  </AccordionItem>

<AccordionItem title="`dynamical_decoupling.extra_slack_distribution`">

**الخيارات**: `middle`, `edges`

**الافتراضي**: `middle`
  </AccordionItem>

<AccordionItem title="`dynamical_decoupling.scheduling_method`">

الخيارات: `asap`, `alap`
الافتراضي: `alap`
  </AccordionItem>

<AccordionItem title="`dynamical_decoupling.sequence_type`">

الخيارات: `XX`, `XpXm`, `XY4`
الافتراضي: `XX`
  </AccordionItem>

<AccordionItem title="`dynamical_decoupling.skip_reset_qubits`">

الخيارات: `True`, `False`
الافتراضي: `False`
  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`environment`">

[وثائق API لـ `environment`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#environment)
<Accordion>
<AccordionItem title="`environment.callback`">

دالة قابلة للاستدعاء تستقبل `Job ID` و `Job result`.

**الخيارات**: None

**الافتراضي**: None
  </AccordionItem>

<AccordionItem title="`environment.job_tags`">

قائمة من العلامات.

**الخيارات**: None

**الافتراضي**: None
  </AccordionItem>

<AccordionItem title="`environment.log_level`">

**الخيارات**: DEBUG, INFO, WARNING, ERROR, CRITICAL

**الافتراضي**: WARNING
  </AccordionItem>

<AccordionItem title="`environment.private`">

**الخيارات**: `True`, `False`

**الافتراضي**: `False`
  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`execution`">

[وثائق API لـ `execution`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#execution)
<Accordion>
<AccordionItem title="`execution.init_qubits`">
هل يتم إعادة ضبط القبتات إلى الحالة الأساسية لكل shot.

**الخيارات**: `True`, `False`

**الافتراضي**: `True`
  </AccordionItem>

<AccordionItem title="`execution.rep_delay`">
التأخير بين قياس ودائرة كمية لاحقة.

**الخيارات**: قيمة في النطاق المقدَّم من `backend.rep_delay_range`

**الافتراضي**: كما هو محدد بواسطة `backend.default_rep_delay`
  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`max_execution_time`">
يحدد المدة التي يمكن أن تعمل فيها المهمة، بالثواني. انظر دليل [الحد الأقصى لوقت التنفيذ](/guides/max-execution-time) للتفاصيل.

**الخيارات**: عدد صحيح للثواني في النطاق [1, 10800]

**الافتراضي**: 10800 (3 ساعات)
  </AccordionItem>

<AccordionItem title="`resilience`">
خيارات متقدمة للمرونة لضبط استراتيجية المرونة بدقة.

[وثائق API لـ `resilience`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#resilience)
<Accordion>
<AccordionItem title="`resilience.layer_noise_learning`">

خيارات لتعلم ضوضاء الطبقة.

[وثائق API لـ `resilience.layer_noise_learning`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-layer-noise-learning-options)
  </AccordionItem>

<AccordionItem title="`resilience.layer_noise_learning.layer_pair_depths`">

**الخيارات**: list[int] بقيمة 2-10 في النطاق [0, 200]

**الافتراضي**: `(0, 1, 2, 4, 16, 32)`

  </AccordionItem>

<AccordionItem title="`resilience.layer_noise_learning.max_layers_to_learn`">

**الخيارات**: None، عدد صحيح >= 1

**الافتراضي**: `4`

  </AccordionItem>

<AccordionItem title="`resilience.layer_noise_learning.num_randomizations`">

**الخيارات**: عدد صحيح >= 1

**الافتراضي**: `32`

  </AccordionItem>

<AccordionItem title="`resilience.layer_noise_learning.shots_per_randomization`">

**الخيارات**: عدد صحيح >= 1

**الافتراضي**: `128`

  </AccordionItem>

<AccordionItem title="`resilience.layer_noise_model`">

**الخيارات**: `NoiseLearnerResult`, `Sequence[LayerError]`

**الافتراضي**: None

  </AccordionItem>

<AccordionItem title="`resilience.measure_mitigation`">

**الخيارات**: `True`, `False`

**الافتراضي**: `True`

  </AccordionItem>

<AccordionItem title="`resilience.measure_noise_learning`">

خيارات لتعلم ضوضاء القياس.

[وثائق API لـ `resilience.measure_noise_learning`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-measure-noise-learning-options)
  </AccordionItem>

<AccordionItem title="`resilience.measure_noise_learning.num_randomizations`">

**الخيارات**: عدد صحيح >= 1

**الافتراضي**: `32`

  </AccordionItem>

<AccordionItem title="`resilience.measure_noise_learning.shots_per_randomization`">

**الخيارات**: عدد صحيح، `auto`

**الافتراضي**: `auto`

  </AccordionItem>

<AccordionItem title="`resilience.pec_mitigation`">

**الخيارات**: `True`, `False`

**الافتراضي**: `False`

  </AccordionItem>

<AccordionItem title="`resilience.pec`">

خيارات تخفيف الأخطاء الاحتمالية.

[وثائق API لـ `resilience.pec`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-pec-options)
  </AccordionItem>

<AccordionItem title="`resilience.pec.max_overhead`">

**الخيارات**: `None`، عدد صحيح >= 1

**الافتراضي**: `100`

  </AccordionItem>

<AccordionItem title="`resilience.pec.noise_gain`">

**الخيارات**: `auto`، float في النطاق [0, 1]

**الافتراضي**: `auto`

  </AccordionItem>

<AccordionItem title="`resilience.zne_mitigation`">

**الخيارات**: `True`, `False`

**الافتراضي**: `False`

  </AccordionItem>

<AccordionItem title="`resilience.zne`">

[وثائق API لـ `resilience.zne`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-zne-options)
  </AccordionItem>

<AccordionItem title="`resilience.zne.amplifier`">

**الخيارات**: `gate_folding`, `gate_folding_front`, `gate_folding_back`, `pea`

**الافتراضي**: `gate_folding`

  </AccordionItem>

<AccordionItem title="`resilience.zne.extrapolated_noise_factors`">

**الخيارات**: قائمة من الأعداد العشرية

**الافتراضي**: `[0, *noise_factors]`

  </AccordionItem>

<AccordionItem title="`resilience.zne.extrapolator`">

**الخيارات**: واحد أو أكثر من: `exponential`, `linear`, `double_exponential`, `polynomial_degree_(1 <= k <= 7)`, `fallback`

**الافتراضي**: `(exponential, linear)`

  </AccordionItem>

<AccordionItem title="`resilience.zne.noise_factors`">

**الخيارات**: قائمة من الأعداد العشرية؛ كل عدد عشري >= 1

**الافتراضي**: `(1, 1.5, 2)` لـ `PEA`، و `(1, 3, 5)` في غير ذلك

  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`resilience_level`">

مقدار المرونة المبنية ضد الأخطاء. المستويات الأعلى تولّد نتائج أكثر دقة على حساب أوقات معالجة أطول. انظر قسم [مستويات المرونة](/guides/estimator-noise-management#resilience) في موضوع إدارة الضوضاء لمزيد من المعلومات.

**الخيارات**: `0`, `1`, `2`

**الافتراضي**: `1`

[وثائق API لـ `resilience_level`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#resilience_level)
  </AccordionItem>

<AccordionItem title="`seed_estimator`">

**الخيارات**: عدد صحيح

**الافتراضي**: None

[`seed_estimator`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#seed_estimator)
  </AccordionItem>

<AccordionItem title="`simulator`">

خيارات لتمريرها عند محاكاة Backend

[وثائق API لـ `simulator`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-simulator-options)
<Accordion>
<AccordionItem title="`simulator.basis_gates`">

**الخيارات**: قائمة من أسماء بوابات الأساس للنقل إليها

**الافتراضي**: مجموعة جميع بوابات الأساس المدعومة من قِبل [محاكي Qiskit Aer](https://qiskit.github.io/qiskit-aer/stubs/qiskit_aer.AerSimulator.html)

  </AccordionItem>

<AccordionItem title="`simulator.coupling_map`">

**الخيارات**: قائمة من التفاعلات ثنائية القبت الموجَّهة

**الافتراضي**: None، مما يعني عدم وجود قيود على الاتصال (اتصال كامل).

  </AccordionItem>

<AccordionItem title="`simulator.noise_model`">

**الخيارات**: [Qiskit Aer NoiseModel](/guides/build-noise-models)، أو تمثيلها

**الافتراضي**: None

  </AccordionItem>

<AccordionItem title="`simulator.seed_simulator`">

**الخيارات**: عدد صحيح

**الافتراضي**: None

  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`twirling`">

خيارات Twirling

[وثائق API لـ `twirling`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-twirling-options)
<Accordion>
<AccordionItem title="`twirling.enable_gates`">

**الخيارات**: True, False

**الافتراضي**: False

  </AccordionItem>

<AccordionItem title="`twirling.enable_measure`">

**الخيارات**: True, False

**الافتراضي**: True

  </AccordionItem>

<AccordionItem title="`twirling.num_randomizations`">

**الخيارات**: `auto`، عدد صحيح >= 1

**الافتراضي**: `auto`

  </AccordionItem>

<AccordionItem title="`twirling.shots_per_randomization`">

**الخيارات**: `auto`، عدد صحيح >= 1

**الافتراضي**: `auto`

  </AccordionItem>

<AccordionItem title="`twirling.strategy`">

**الخيارات**: `active`, `active-circuit`, `active-accum`, `all`

**الافتراضي**: `active-accum`

  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`experimental`">

الخيارات التجريبية، عند توفرها.

  </AccordionItem>

</Accordion>
<span id="options-compatibility-table"></span>
## توافق الميزات {#feature-compatibility}

لا يمكن استخدام بعض ميزات Runtime معاً في مهمة واحدة. انقر على التبويب المناسب للحصول على قائمة الميزات غير المتوافقة مع الميزة المحددة:

<Tabs>

  <TabItem value="Fractional" label="البوابات الكسرية">
  غير متوافق مع:
  - Gate twirling
  - PEA
  - PEC

  </TabItem>

  <TabItem value="ZNE" label="Gate-folding ZNE">
    غير متوافق مع:
  - PEA
  - PEC

  قد لا يعمل عند استخدام بوابات مخصصة.
  </TabItem>
  <TabItem value="Twirling" label="Gate twirling">
  غير متوافق مع البوابات الكسرية أو مع المدّات.

  ملاحظات أخرى:
  - لا يمكن تطبيق Measurement twirling إلا على القياسات النهائية.
  - لا يعمل مع المشابكات غير-Clifford.

  </TabItem>

  <TabItem value="PEA" label="PEA">
    غير متوافق مع:
  - البوابات الكسرية
  - Gate-folding ZNE
  - PEC
  </TabItem>

  <TabItem value="PEC" label="PEC">
    غير متوافق مع:
  - البوابات الكسرية
  - Gate-folding ZNE
  - PEA
  </TabItem>

</Tabs>

## الخطوات التالية {#next-steps}

<Admonition type="tip" title="توصيات">
- راجع دليل [مقدمة في الخيارات](/guides/runtime-options-overview).
- ابحث عن مزيد من التفاصيل حول طرق `EstimatorV2` في [مرجع API لـ Estimator](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/estimator-v2).
- حدد [وضع التنفيذ](/guides/execution-modes) الذي ستشغّل مهمتك فيه.
- تعرف على [إدارة الضوضاء مع Estimator](/guides/estimator-noise-management).
</Admonition>
