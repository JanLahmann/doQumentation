---
title: الإعداد لاستخدام IBM Quantum Platform عبر REST API
description: تعليمات الإعداد لاستخدام IBM Quantum على IBM Cloud لإرسال مهام Qiskit SDK و Qiskit Runtime باستخدام REST API
platform: cloud
---

{/* doqumentation-source-hash: a3985acf */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore urlendcoded, WQVY */}

# الإعداد لاستخدام IBM Quantum Platform عبر REST API {#set-up-to-use-ibm-quantum-platform-with-rest-api}

يمكنك الوصول إلى المعالجات الكمومية عبر REST APIs، مما يتيح لك العمل مع وحدات معالجة الكم (QPUs) باستخدام أي لغة برمجة أو إطار عمل.

### 1. احصل على صلاحية الوصول {#1-get-access}

1. إذا لم يكن لديك حساب مستخدم بعد، أنشئ واحدًا من [صفحة تسجيل الدخول إلى IBM Quantum.](https://quantum.cloud.ibm.com/login)
1. أنشئ مفتاح API (يُعرف أيضًا بالرمز المميز) من [لوحة التحكم.](https://quantum.cloud.ibm.com) لاحظ أن نفس مفتاح API يمكن استخدامه في أي من المنطقتين.
1. أنشئ رمز bearer الخاص بـ IBM Cloud Identity and Access Management (IAM). هذا رمز قصير الأمد يُستخدم للتحقق من طلبات REST API. لإنشائه، استدعِ [واجهة برمجة تطبيقات IAM Identity Services](https://cloud.ibm.com/apidocs/iam-identity-token-api#create-api-key) كما هو موضح في المثال التالي:
<Tabs>

   <TabItem value="Curl" label="Curl">
   ```bash
   curl -X POST 'https://iam.cloud.ibm.com/identity/token' \
   -H 'Content-Type: application/x-www-form-urlencoded' \
   -d 'grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=MY_APIKEY'
   ```
   الاستجابة المتوقعة
   ```text
   {
      "access_token": "eyJhbGciOiJIUz......sgrKIi8hdFs",
      "refresh_token": "SPrXw5tBE3......KBQ+luWQVY=",
      "token_type": "Bearer",
      "expires_in": 3600,
      "expiration": 1473188353
   }
   ```
   </TabItem>

   <TabItem value="Python" label="Python">
   ```python
   # Use 'service' to invoke operations.
   import requests
   import json

   url = 'https://iam.cloud.ibm.com/identity/token'
   api_key = 'MY_APIKEY'
   headers = {
    'Content-Type': 'application/x-www-form-urlendcoded',
   }
   data = f'grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey={api_key}'
   response = requests.post(url, headers=headers, data=data)

   # Bearer token to authorize requests to the REST API
   bearer_token = response.json()['access_token']
   ```
   </TabItem>
</Tabs>

### 2. اختر طريقة المصادقة {#2-choose-an-authentication-method}

اختر طريقة المصادقة المناسبة بحسب بيئة عملك:

* [أنشئ متغير بيئة لمفتاح API الخاص بك (بيئات Python الموثوقة)](#environment_variable)
* [استخدم مفتاح API مباشرة (بيئة غير موثوقة)](/guides/cloud-setup-untrusted#rest-untrusted)

<span id="environment_variable"></span>
#### إنشاء متغير بيئة (بيئة موثوقة) {#create-an-environment-variable-trusted-environment}

1. لضبط متغير البيئة IQP_API_TOKEN على نظامك، يمكنك إضافة السطر التالي إلى ملف إعدادات الشِل الخاص بك (مثل .bashrc أو .zshrc) أو ضبطه مباشرة في الطرفية:

    ```shell
    export IQP_API_TOKEN=<your-API_KEY> # Use the 44-character API_KEY you created and saved from the IBM Quantum Platform Home dashboard
    ```
    عند استدعاء متغير البيئة في الكود الخاص بك، أضف `import os` كما في هذا المثال:

    ```python
    import os
    api_token = os.environ['IQP_API_TOKEN']
    ```
    لاحظ أنه عند إنشاء متغير بيئة، يظل مفتاح API مخزنًا محليًا كنص عادي، لذا يجب حمايته بعناية.

1. صادق على طلبات Qiskit Runtime REST API بتضمين CRN ورمز bearer في ترويسات الطلب.
```bash
curl -X 'GET' \
   'https://quantum.cloud.ibm.com/api/v1/usage' \
   '-H accept: application/json' \
   '-H authorization: Bearer <BEARER_TOKEN>' \
   '-H Service-CRN: <INSTANCE_CRN>'
```

### 3. اختياري: اضبط جدار الحماية الخاص بك {#3-optional-configure-your-firewall}

    إذا لزم الأمر، استخدم [هذه المعلومات](/guides/quickstart-steps-org#firewall) لتفعيل الوصول إلى نقاط نهاية IBM Quantum API.

## الخطوات التالية {#next-steps}

<Admonition type="tip" title="توصيات">
  -  [نظرة عامة على الخطط المتاحة.](plans-overview)
  -  [اضبط Qiskit SDK محليًا.](configure-qiskit-local)
  -  اتبع الخطوات في [Hello world](/guides/hello-world) لكتابة برنامج كمومي وتشغيله.
  -  جرّب أحد [الدروس التعليمية](https://docs.quantum.ibm.com/tutorials).
</Admonition>
