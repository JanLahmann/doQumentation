---
description: 'Una guida rapida su come usare la primitiva IBM Quantum Sampler.'
notebook_path: guides/get-started-with-sampler.ipynb
sidebar_label: 'Avvio rapido con Sampler'
title: 'Avvio rapido con Sampler'
---

{/* doqumentation-source-hash: 86291a21 */}


<OpenInLabBanner notebookPath="guides/get-started-with-sampler.ipynb" />



Il compito principale di Sampler è campionare il registro di output dall'esecuzione di uno o più Circuit quantistici. I [Circuit dinamici](/guides/execute-dynamic-circuits) e i circuiti parametrizzati sono accettati come input (se vengono inviati Circuit parametrizzati, è necessario fornire anche i valori dei parametri). Sampler supporta anche il decoupling dinamico integrato e il twirling per la [soppressione degli errori](/guides/error-mitigation-and-suppression-techniques).

I passaggi in questo argomento descrivono come configurare Sampler, esplorare le opzioni che puoi usare per configurarlo e invocarlo in un programma.
{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Versioni dei pacchetti">

Il codice in questa pagina è stato sviluppato con i seguenti requisiti.
Ti consigliamo di usare queste versioni o versioni più recenti.

```
qiskit[all]~=2.5.2
qiskit-ibm-runtime~=0.47.0
```
</AccordionItem>
</Accordion>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-ibm-runtime
```

## Passaggi per utilizzare la primitiva Sampler {#steps-to-use-the-sampler-primitive}

### 1. Inizializza l'account {#1-initialize-the-account}

Prima devi inizializzare il tuo account. Puoi quindi selezionare la QPU che desideri utilizzare per calcolare il valore di aspettazione.

Segui i passaggi nell'argomento [Configura il tuo account IBM Cloud](/guides/cloud-setup) se non hai già un account configurato.

:::note[gate frazionari]


Per utilizzare i [gate frazionari](/guides/fractional-gates) supportati di recente, imposta `use_fractional_gates=True` quando richiedi un backend da un'istanza di `QiskitRuntimeService`. Ad esempio:

```python
service = QiskitRuntimeService()
fractional_gate_backend = service.least_busy(use_fractional_gates=True)
```

Questa è una funzionalità sperimentale e potrebbe cambiare in futuro.

:::


```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
backend = service.least_busy(
    operational=True, simulator=False, min_num_qubits=127
)
```

### 2. Crea un circuito {#2-create-a-circuit}

Hai bisogno di almeno un circuito come input per la primitiva Sampler.

```python
import numpy as np
from qiskit.circuit.library import efficient_su2

circuit = efficient_su2(127, entanglement="linear")
circuit.measure_all()
# The circuit is parametrized, so we will define the parameter values for execution
param_values = np.random.rand(circuit.num_parameters)
```

Il circuito e l'osservabile devono essere trasformati per usare solo le istruzioni supportate dalla QPU (chiamati circuiti *instruction set architecture (ISA)*). Usa il Transpiler per farlo.

```python
from qiskit.transpiler import generate_preset_pass_manager

pm = generate_preset_pass_manager(optimization_level=1, backend=backend)
isa_circuit = pm.run(circuit)
print(f">>> Circuit ops (ISA): {isa_circuit.count_ops()}")
```

```text
>>> Circuit ops (ISA): OrderedDict([('rz', 3036), ('sx', 1769), ('cz', 378), ('measure', 127), ('barrier', 1)])
```

### 3. Inizializza l'IBM Quantum Sampler {#3-initialize-the-ibm-quantum-sampler}

Quando inizializzi Sampler, usa il parametro `mode` per specificare la modalità in cui vuoi che venga eseguito. I valori possibili sono oggetti `batch`, `session` o `backend` per la modalità di esecuzione batch, session e job rispettivamente. Per ulteriori informazioni, consulta [Introduzione alle modalità di esecuzione di IBM Quantum Compute Service](/guides/execution-modes). Nota che gli utenti del piano Open non possono inviare job di Session.

```python
from qiskit_ibm_runtime import SamplerV2 as Sampler

sampler = Sampler(mode=backend)
```

### 4. Invoca Sampler e ottieni i risultati {#4-invoke-sampler-and-get-results}

Poi, invoca il metodo `run()` per generare l'output. Il circuito e i set di valori dei parametri opzionali vengono passati come tuple *primitive unified bloc* (PUB).

```python
job = sampler.run([(isa_circuit, param_values)])
print(f">>> Job ID: {job.job_id()}")
print(f">>> Job Status: {job.status()}")
```

```text
>>> Job ID: dab7lb6rrl7c7386gjgg
```

```text
>>> Job Status: QUEUED
```

```python
result = job.result()

# Get results for the first (and only) PUB
pub_result = result[0]
print(
    f"First ten results for the 'meas' output register: "
    f"{pub_result.data.meas.get_bitstrings()[:10]}"
)
```

```text
First ten results for the 'meas' output register: ['1110001000100100100111011000001001000101010000001100010010110011001100110111100000000110001000011001101000110100101110101010011', '1110101100010101010001010111111011000110011010100101001100110101010010101010101110001010100100000000011110101101011000100000100', '1100110011101000110000000100110111101010110001100010111100001101100001010100111001101010101110000101010001001101010101001000100', '1011001001011000000101010111111010011101001111010100001011110011000101110101011001100110011011110011101010000111001111110110011', '1110111011011111101011110101110101010110011001100110101111100100011101100011011100101100101001001111110101111010101110111111011', '0101001110001110010001010100010111011001000000011110000000000001110000010010101101011001000100000001011011101111010010110111110', '1101101001000100010000110001000101010010101011000011000011001010000111100110010101010110100110011010101100001011100110001111011', '1000110011111111100010101010101111001001100001110111101001100111010101010111110110011000110011010010010000100000100010010010101', '0101010100110011110011010000000100011001111100101100101100101111101110100001101101111010100011011000010011111010001100110000010', '0010000111011100000000110000001110001100010011000100101110111111100011001100011001101010011111111100000000011100101101111010100']
```

## Passi successivi {#next-steps}

<Admonition type="tip" title="Raccomandazioni">
- Scopri come [testare localmente](local-testing-mode) prima di eseguire su computer quantistici.
- Consulta [esempi](sampler-examples) dettagliati.
- Fai pratica con le primitive svolgendo la [lezione sulle funzioni di costo](/learning/courses/variational-algorithm-design/cost-functions) in IBM Quantum Learning.
- Scopri come effettuare la transpilazione localmente nella sezione [Transpila](transpile/).
- Prova la guida [Confronta le impostazioni del Transpiler](/guides/circuit-transpilation-settings#compare-transpiler-settings).
- Scopri come [utilizzare le opzioni delle primitive](/guides/runtime-options-overview).
- Visualizza l'API per le opzioni di [Sampler](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-sampler-options).
</Admonition>
