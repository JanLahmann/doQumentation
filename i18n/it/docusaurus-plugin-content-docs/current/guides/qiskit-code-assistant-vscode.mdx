---
title: Usa Qiskit Code Assistant in VS Code
description: Come installare, usare e configurare Qiskit Code Assistant in Visual Studio Code (VS Code) e negli editor compatibili.
sidebar_label: Qiskit Code Assistant in VS Code
---

{/* doqumentation-source-hash: d031ce50 */}

# Usa Qiskit Code Assistant in Visual Studio Code e negli editor compatibili {#use-qiskit-code-assistant-in-visual-studio-code-and-compatible-editors}

Scopri come installare, usare, configurare e disinstallare l'estensione ufficiale Qiskit Code Assistant in Visual Studio Code (VS Code) e negli editor compatibili.

<Admonition type="note" title="Note">
    - Questa è una funzionalità sperimentale disponibile per gli utenti del piano IBM Quantum&reg; Premium con un account sulla nuova [IBM Quantum Platform.](https://quantum.cloud.ibm.com/)
    - Qiskit Code Assistant è in stato di anteprima ed è soggetto a modifiche.
    - Per inviare feedback o contattare il team di sviluppo, usa il [canale Qiskit Slack Workspace](https://qiskit.enterprise.slack.com/archives/C07LYA6PL83) o i repository GitHub pubblici correlati.
</Admonition>

<IBMVideo id="134371939" title="Un utente usa Qiskit Code Assistant in Visual Studio Code. L'utente ha già delle importazioni e una definizione di funzione chiamata get_random_linear_function, con una docstring che descrive cosa deve fare la funzione. Dopo aver premuto i tasti Ctrl e ., Qiskit Code Assistant completa automaticamente la funzione con il codice per generare una funzione lineare casuale usando Numpy e Qiskit. Il video si conclude con l'utente che esegue il codice generato e ottiene nel terminale una matrice che rappresenta una funzione lineare casuale." />

## Installa l'estensione VS Code {#install-the-vs-code-extension}

Per installare l'[estensione VS Code](https://github.com/Qiskit/qiskit-code-assistant-vscode), segui questi passaggi:

Installa direttamente da VS Code:

1. Avvia VS Code.
1. Clicca sull'icona Estensioni nella barra degli strumenti a sinistra.
1. Cerca `qiskit`
1. Trova "Qiskit Code Assistant (Beta)" e clicca su **Installa**.

In alternativa, l'estensione è disponibile anche tramite il [VS Code Marketplace](https://marketplace.visualstudio.com/items?itemName=qiskit.qiskit-vscode).

L'estensione è pubblicata anche su [Open VSX](https://open-vsx.org/extension/qiskit/qiskit-vscode) per gli utenti di fork di VS Code come VSCodium, Cursor, IBM Bob e altri.

L'estensione si carica automaticamente ed è elencata in fondo alla finestra di VS Code. Se non è elencata, ricarica l'estensione o l'applicazione.

## Configura le impostazioni dell'estensione {#configure-extension-settings}

È possibile configurare le seguenti impostazioni:

- Per modificare le scorciatoie da tastiera, apri le impostazioni delle scorciatoie da tastiera (`Cmd/Ctrl`+`Shift`+`P` -> `Preferences: Open Keyboard Shortcuts (JSON)`) e cerca `qiskit-vscode`.

- Puoi modificare il token API IBM Quantum da usare nella palette dei comandi di VS Code. Per farlo, digita `Cmd/Ctrl`+`Shift`+`P`, cerca `qiskit`, seleziona il comando **Qiskit Code Assistant: Set IBM Quantum API token** e incolla il tuo token API IBM Quantum.

- [Avanzato] Per modificare l'istanza del Qiskit Code Assistant Service che l'estensione deve usare, vai su File -> Preferenze -> Impostazioni. Nella scheda Utente, cerca Qiskit e modifica `Qiskit Code Assistant: Url`.

## Inizia a usare l'estensione Qiskit Code Assistant per VS Code {#get-started-using-the-qiskit-code-assistant-extension-for-vs-code}

### Autenticazione e configurazione {#authentication-and-setup}

Dopo aver installato l'estensione, questa tenta di autenticarti. Per impostazione predefinita, il pacchetto cerca di autenticarsi ai servizi IBM Quantum con la chiave API definita, utilizzando la chiave dalla variabile d'ambiente `QISKIT_IBM_TOKEN` o dal file `~/.qiskit/qiskit-ibm.json` (nella sezione `default-ibm-quantum`). Se hai bisogno di aiuto per configurare il tuo account, segui le istruzioni in [Configura per usare IBM Cloud.](/guides/cloud-setup#cloud)

Per impostazione predefinita, l'estensione usa il modello `mistral-small-3.2-24b-qiskit`, elencato nel selettore di modelli nella barra di stato in basso.

La prima volta che usi il modello `mistral-small-3.2-24b-qiskit`, si apre una finestra modale con alcune restrizioni importanti di cui tenere conto durante l'utilizzo del modello. Clicca su **Accetta** per abilitare il modello per la generazione di codice.

### Genera codice {#generate-code}

Mentre sviluppi il tuo codice con Qiskit, puoi chiedere a Qiskit Code Assistant di aiutarti. In generale, l'Assistant suggerisce codice migliore in risposta a commenti Python o docstring, ma puoi usare l'Assistant in qualsiasi punto del tuo file.

Per ottenere un suggerimento di codice, digita un prompt, poi premi `Ctrl`+`.`. Esistono due tipi di prompt che puoi usare:

  - Inserisci codice parziale e ricevi un suggerimento per completarlo. Esempio:
    ```python
    from qiskit.circuit import QuantumCircuit
    from qiskit.transpiler import generate_preset_pass_manager
    from qiskit_ibm_runtime import SamplerV2 as Sampler
    from qiskit_ibm_runtime.fake_provider import FakeManilaV2

    # Bell Circuit
    qc = QuantumCircuit(2)
    qc.h(0)
    qc.cx(0, 1)

    qc.mea # Type Ctrl + . for a code suggestion.
    ```
  - Digita il simbolo cancelletto (#), poi scrivi il tuo prompt. Esempio: `# Give me a random circuit of 5 qubits and a depth of 4.`

![Viene mostrato un suggerimento di codice, visualizzato in grigio.](/docs/images/guides/qiskit-code-assistant/vscode-inline-suggestion.avif)

### Lavora con i suggerimenti di codice {#work-with-code-suggestions}

Usa le seguenti opzioni per accettare, rifiutare e scorrere i suggerimenti:

- Premi `Tab` per accettare il suggerimento oppure `ESC` per rifiutarlo.
- Dopo aver ricevuto un suggerimento, digita di nuovo `Ctrl` + `.` per generare suggerimenti aggiuntivi.
- Quando viene mostrato un suggerimento, usa `Option/Alt` + `]` e `Option/Alt` + `[` per scorrere rispettivamente in avanti e indietro tra i suggerimenti (se disponibili).
- Passa il cursore sopra il suggerimento per usare la barra degli strumenti a comparsa e scorrere tra i suggerimenti, come mostrato:
  ![Viene mostrata la barra degli strumenti che appare passando il cursore sopra il codice suggerito.](/docs/images/guides/qiskit-code-assistant/vscode-inline-suggestion-toolbar.avif)

## Disinstalla l'estensione VS Code {#uninstall-the-vs-code-extension}

Per rimuovere Qiskit Code Assistant da VS Code, segui questi passaggi:

1. Avvia Visual Studio Code.
1. Clicca sull'icona Estensioni.
1. Cerca `qiskit` e apri Qiskit Code Assistant.
1. Clicca su **Disinstalla**.

## Risoluzione dei problemi {#troubleshooting}

Se non vedi la barra di stato dell'estensione in VS Code, verifica che l'estensione sia installata e abilitata nella scheda delle estensioni.

Se l'estensione è installata e abilitata, ma non riesce a selezionare un modello, verifica che la tua chiave API attuale sia stata aggiunta e che `Qiskit Code Assistant: Url` sia impostato correttamente.

## Contribuisci all'estensione VS Code {#contribute-to-the-vs-code-extension}

Il codice di questa estensione ufficiale è pubblicamente disponibile e open source. Scoprilo su [GitHub.](https://github.com/Qiskit/qiskit-code-assistant-vscode)

## Passi successivi {#next-steps}

<Admonition type="tip" title="Raccomandazioni">
Consulta esempi per usare Qiskit Code Assistant per i [circuiti](/guides/circuit-library), la [configurazione della soppressione degli errori](/guides/configure-error-suppression) e il [transpiling con i pass manager.](/guides/transpile-with-pass-managers)
</Admonition>
