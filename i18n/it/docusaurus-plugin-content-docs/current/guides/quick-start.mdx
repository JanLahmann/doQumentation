---
description: 'Costruisci e visualizza un circuito quantistico in meno di due minuti, senza registrazione né chiave API.'
notebook_path: guides/quick-start.ipynb
sidebar_label: 'Avvio rapido'
title: 'Avvio rapido'
---

{/* doqumentation-source-hash: dc09af91 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<OpenInLabBanner notebookPath="guides/quick-start.ipynb" />




{/* cspell:ignore miniconda */}

{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Versioni dei pacchetti">

Il codice in questa pagina è stato sviluppato con i seguenti requisiti.
Si consiglia di usare queste versioni o versioni più recenti.

```
qiskit[all]~=2.5.2
```
</AccordionItem>
</Accordion>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

Costruisci il tuo primo circuito quantistico in meno di due minuti, nel tuo ambiente locale — senza registrazione né chiave API.

<Admonition type="note" title="Non hai familiarità con Python e gli ambienti virtuali?">

* Scarica Python e usa un ambiente virtuale con Qiskit (consigliato).


<Accordion>
<AccordionItem title="Clicca per espandere e ottenere maggiori informazioni su **Python**.">

- Per installare Python, controlla prima la sezione "Programming Language" nella [pagina del progetto Qiskit su PyPI](https://pypi.org/project/qiskit/) per determinare quali versioni di Python sono supportate dall'ultima release. Per le istruzioni di download, consulta la [Guida Python per principianti](https://wiki.python.org/moin/BeginnersGuide/Download).


<Admonition type="note">
These instructions use the standard Python distribution from [pypi.org](https://pypi.org/). However, you can use other Python distributions, such as [Anaconda](https://docs.anaconda.com/anaconda/) or [miniconda](https://docs.anaconda.com/miniconda/), along with other dependency management workflows like [Poetry](https://python-poetry.org/docs/).
</Admonition>
  </AccordionItem>
</Accordion>

    <Accordion>
    <AccordionItem title="Click to expand for more information on **virtual environments**.">
    - Use [Python virtual environments](https://docs.python.org/3.10/tutorial/venv.html) to separate Qiskit from other applications.
    A Python virtual environment is an isolated space to work with Python for a specific purpose — so you can install whatever packages you wish, and set up libraries, dependencies, and so on, without affecting the "base" Python environment on your machine.

    Un importante vantaggio degli ambienti virtuali è che, se il tuo ambiente Python si corrompe, puoi semplicemente eliminarlo e ricominciare da capo!

    Scegli una posizione preferita in cui salvare le informazioni sui tuoi ambienti virtuali. Di solito vengono salvati in una directory chiamata `.venv` all'interno di ogni directory di progetto.

    Per configurare un ambiente virtuale, spostati nella directory del progetto e crea un ambiente minimale con solo Python installato.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">

            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">

            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">

            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

    Poi attiva il nuovo ambiente.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">

             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">

             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
              Se usi PowerShell:

             ```text
             .venv\Scripts\Activate.ps1
             ```
             Se usi Git Bash:

             ```text
             source .venv/scripts/activate
             ```
              Se usi il prompt dei comandi:

             ```text
             .venv\Scripts\activate
             ```
           </TabItem>
         </OperatingSystemTabs>
      </AccordionItem>
    </Accordion>

</Admonition>

## 1. Installa Qiskit {#1-install-qiskit}

Installa i seguenti pacchetti con il tuo gestore di pacchetti preferito (ad esempio `pip`):

- [`qiskit`](/guides/install-qiskit)

- [`matplotlib`](https://matplotlib.org/stable/users/explain/quick_start.html)

- [`qiskit[visualization]`](https://docs.quantum.ibm.com/api/qiskit/visualization)


## 2. Costruisci il tuo circuito {#2-build-your-circuit}

Apri un ambiente Python, quindi esegui questo codice per costruire uno stato di Bell (due qubit entangled).

```python
from qiskit import QuantumCircuit
from qiskit.primitives import StatevectorSampler

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

sampler = StatevectorSampler()
result = sampler.run([qc], shots=1024).result()
print(result[0].data.meas.get_counts())
```

```text
{'00': 486, '11': 538}
```

Il risultato atteso è una suddivisione quasi uniforme tra '00' e '11'.

## 3. Visualizza i risultati {#3-visualize-your-results}

Per ottenere un istogramma dei risultati, aggiungi il seguente codice al tuo programma.

```python
# Uncomment lines 2 and 8 if you are not using Python in a Jupyter notebook
# import matplotlib.pyplot as plt
from qiskit.visualization import plot_histogram

counts = result[0].data.meas.get_counts()
plot_histogram(counts)

# plt.show()
```

![Output della cella di codice precedente](/docs/images/guides/quick-start/extracted-outputs/dc4ff012-0.svg)

Questo risultato è una firma dell'entanglement quantistico.

## 4. Osserva cosa succede {#4-see-what-happens}

Prova a modificare il codice per vedere come influisce sui risultati. Ad esempio:

- Aggiungi un terzo qubit cambiando in `QuantumCircuit(3)` e aggiungi un secondo gate CX con `qc.cx(1,2)`. Le misurazioni dovrebbero quindi cambiare in 000 e 111, il che significa che tutti e tre i qubit sono stati entangled.


- Osserva come cambiano i risultati aggiungendo `qc.x(1)` alla fine del circuito.


## Passi successivi {#next-steps}

<Admonition type="tip" title="Raccomandazioni">
- Segui i passaggi in [Hello world](/guides/hello-world) per eseguire un circuito su hardware quantistico reale.
- Non sei ancora pronto/a a eseguire su hardware? Inizia il tuo percorso quantistico con il corso [Basi dell'informazione quantistica](/learning/courses/basics-of-quantum-information).
</Admonition>
