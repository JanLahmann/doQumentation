---
title: "מחלקת ה-Operator"
sidebar_label: "מחלקת ה-Operator"
description: "הסבר מעמיק על השימוש במחלקת ה-Operator ב-Qiskit."
notebook_path: "guides/operator-class.ipynb"
---

{/* doqumentation-source-hash: f33a6b98 */}

<OpenInLabBanner notebookPath="guides/operator-class.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>Package versions</b></summary>

The code on this page was developed using the following requirements.
We recommend using these versions or newer.

```
qiskit[all]~=2.3.0
```
</details>
דף זה מראה כיצד להשתמש במחלקה [`Operator`](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator). לסקירה ברמה גבוהה של ייצוגי אופרטורים ב-Qiskit, כולל מחלקת `Operator` ואחרות, ראה [סקירת מחלקות אופרטורים](./operators-overview).

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit
```

```python
import numpy as np
from qiskit.circuit import QuantumCircuit
from qiskit.circuit.library import CXGate, RXGate, XGate
from qiskit.quantum_info import Operator, Pauli, process_fidelity
```

## המרת מחלקות ל-Operators {#convert-classes-to-operators}

מספר מחלקות אחרות ב-Qiskit ניתנות להמרה ישירה לאובייקט `Operator` באמצעות מתודת האתחול של האופרטור. לדוגמה:

* אובייקטי `Pauli`
* אובייקטי `Gate` ו-`Instruction`
* אובייקטי `QuantumCircuit`

שים לב שהנקודה האחרונה אומרת שאתה יכול להשתמש במחלקה `Operator` כסימולטור יוניטרי לחישוב מטריצת היוניטרי הסופית של Circuit קוונטי, מבלי לקרוא ל-Backend סימולטור. אם ה-Circuit מכיל פעולות שאינן נתמכות, תוטל חריגה. פעולות שאינן נתמכות הן: מדידה, איפוס, פעולות מותנות, או Gate שאין לו הגדרת מטריצה או פירוק מבחינת Gates עם הגדרות מטריצה.

```python
# Create an Operator from a Pauli object

pauliXX = Pauli("XX")
Operator(pauliXX)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j],
          [1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an Operator for a Gate object
Operator(CXGate())
```

```text
Operator([[1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an operator from a parameterized Gate object
Operator(RXGate(np.pi / 2))
```

```text
Operator([[0.70710678+0.j        , 0.        -0.70710678j],
          [0.        -0.70710678j, 0.70710678+0.j        ]],
         input_dims=(2,), output_dims=(2,))
```

```python
# Create an operator from a QuantumCircuit object
circ = QuantumCircuit(10)
circ.h(0)
for j in range(1, 10):
    circ.cx(j - 1, j)

# Convert circuit to an operator by implicit unitary simulation
Operator(circ)
```

```text
Operator([[ 0.70710678+0.j,  0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          ...,
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.70710678+0.j, -0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j]],
         input_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2), output_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2))
```

## שימוש ב-Operators בתוך Circuits {#use-operators-in-circuits}

ניתן להכניס `Operators` יוניטריים ישירות ל-`QuantumCircuit` באמצעות מתודת `QuantumCircuit.append`. פעולה זו ממירה את ה-`Operator` לאובייקט `UnitaryGate`, המתווסף ל-Circuit.

אם האופרטור אינו יוניטרי, תוטל חריגה. ניתן לבדוק זאת באמצעות הפונקציה `Operator.is_unitary()`, שמחזירה `True` אם האופרטור יוניטרי ו-`False` אחרת.

```python
# Create an operator
XX = Operator(Pauli("XX"))

# Add to a circuit
circ = QuantumCircuit(2, 2)
circ.append(XX, [0, 1])
circ.measure([0, 1], [0, 1])
circ.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/operator-class/extracted-outputs/223c07c9-1b1d-4a33-a397-091cac2ec9e3-0.svg)

שים לב שבדוגמה לעיל האופרטור מאותחל מאובייקט `Pauli`. עם זאת, ניתן גם להכניס את אובייקט ה-`Pauli` ישירות ל-Circuit עצמו, והוא יומר לרצף של Gates של Pauli חד-Qubit:

```python
# Add to a circuit
circ2 = QuantumCircuit(2, 2)
circ2.append(Pauli("XX"), [0, 1])
circ2.measure([0, 1], [0, 1])
circ2.draw()
```

```text
┌────────────┐┌─┐
q_0: ┤0           ├┤M├───
     │  Pauli(XX) │└╥┘┌─┐
q_1: ┤1           ├─╫─┤M├
     └────────────┘ ║ └╥┘
c: 2/═══════════════╩══╩═
                    0  1
```

## שילוב Operators {#combine-operators}

ניתן לשלב אופרטורים בכמה שיטות.

### מכפלת טנזור {#tensor-product}

שני אופרטורים $A$ ו-$B$ ניתנים לשילוב למכפלת טנזור $A\otimes B$ באמצעות הפונקציה `Operator.tensor`. שים לב שאם גם $A$ וגם $B$ הם אופרטורים חד-Qubit, אז `A.tensor(B)` = $A\otimes B$ יהיה עם תת-מערכות ממוספרות כך שמטריצה $B$ על תת-מערכת 0, ומטריצה $A$ על תת-מערכת 1.

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.tensor(B)
```

```text
Operator([[ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j],
          [ 0.+0.j, -0.+0.j,  0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### הרחבת טנזור {#tensor-expansion}

פעולה קשורה היא `Operator.expand`, הפועלת כמו מכפלת טנזור אך בסדר הפוך. לפיכך, עבור שני אופרטורים $A$ ו-$B$ מתקבל `A.expand(B)` = $B\otimes A$ שבו תת-המערכות ממוספרות כך שמטריצה $A$ על תת-מערכת 0, ומטריצה $B$ על תת-מערכת 1.

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.expand(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j,  0.+0.j, -0.+0.j, -1.+0.j],
          [ 0.+0.j,  0.+0.j, -1.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### קומפוזיציה {#composition}

אתה יכול גם לרכב שני אופרטורים $A$ ו-$B$ לביצוע כפל מטריצות באמצעות מתודת `Operator.compose`. `A.compose(B)` מחזיר את האופרטור עם המטריצה $B.A$:

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j],
          [-1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

אתה יכול גם לרכב בסדר הפוך על ידי החלת $B$ לפני $A$ תוך שימוש בארגומנט `front` של `compose`: `A.compose(B, front=True)` = $A.B$:

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B, front=True)
```

```text
Operator([[ 0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

### קומפוזיציה על תת-מערכת {#subsystem-composition}

שים לב שהרכבה הקודמת דורשת שממד הפלט הכולל של האופרטור הראשון $A$ יהיה שווה לממד הקלט הכולל של האופרטור המורכב $B$ (ובדומה, ממד הפלט של $B$ חייב להיות שווה לממד הקלט של $A$ בעת ריכוב עם `front=True`).

אתה יכול גם לרכב אופרטור קטן יותר עם מבחר של תת-מערכות על אופרטור גדול יותר באמצעות הארגומנט `qargs` של `compose`, עם או בלי `front=True`. במקרה זה, ממדי הקלט והפלט הרלוונטיים של תת-המערכות המורכבות חייבים להתאים. *שים לב שהאופרטור הקטן יותר חייב תמיד להיות הארגומנט של מתודת* `compose`*.*

לדוגמה, כדי לרכב Gate דו-Qubit עם אופרטור של שלושה Qubits:

```python
# Compose XZ with a 3-qubit identity operator
op = Operator(np.eye(2**3))
XZ = Operator(Pauli("XZ"))
op.compose(XZ, qargs=[0, 2])
```

```text
Operator([[ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
           -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

```python
# Compose YX in front of the previous operator
op = Operator(np.eye(2**3))
YX = Operator(Pauli("YX"))
op.compose(YX, qargs=[0, 2], front=True)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j],
          [0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

### צירופים לינאריים {#linear-combinations}

ניתן לשלב אופרטורים גם באמצעות אופרטורים לינאריים סטנדרטיים לחיבור, חיסור, וכפל סקלרי במספרים מרוכבים.

```python
XX = Operator(Pauli("XX"))
YY = Operator(Pauli("YY"))
ZZ = Operator(Pauli("ZZ"))

op = 0.5 * (XX + YY - 3 * ZZ)
op
```

```text
Operator([[-1.5+0.j,  0. +0.j,  0. +0.j,  0. +0.j],
          [ 0. +0.j,  1.5+0.j,  1. +0.j,  0. +0.j],
          [ 0. +0.j,  1. +0.j,  1.5+0.j,  0. +0.j],
          [ 0. +0.j,  0. +0.j,  0. +0.j, -1.5+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

נקודה חשובה היא שבעוד ש-`tensor`, `expand` ו-`compose` שומרים על היוניטריות של אופרטורים יוניטריים, צירופים לינאריים אינם עושים זאת; לכן, חיבור שני אופרטורים יוניטריים יוביל, בדרך כלל, לאופרטור שאינו יוניטרי:

```python
op.is_unitary()
```

```text
False
```

### המרה מרומזת ל-Operators {#implicit-conversion-to-operators}

שים לב שעבור כל המתודות הבאות, אם האובייקט השני אינו כבר אובייקט `Operator`, הוא מומר מרומזת לכזה על ידי המתודה. משמעות הדבר היא שניתן להעביר מטריצות ישירות מבלי להמיר אותן באופן מפורש ל-`Operator` תחילה. אם ההמרה אינה אפשרית, תוטל חריגה.

```python
# Compose with a matrix passed as a list
Operator(np.eye(2)).compose([[0, 1], [1, 0]])
```

```text
Operator([[0.+0.j, 1.+0.j],
          [1.+0.j, 0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

## השוואת Operators {#compare-operators}

אופרטורים מממשים מתודת שוויון שניתן להשתמש בה לבדיקה אם שני אופרטורים הם בערך שווים.

```python
Operator(Pauli("X")) == Operator(XGate())
```

```text
True
```

שים לב שפעולה זו בודקת שכל איבר מטריצה של האופרטורים הוא בערך שווה; שני יוניטריים השונים בפאזה גלובלית אינם נחשבים שווים:

```python
Operator(XGate()) == np.exp(1j * 0.5) * Operator(XGate())
```

```text
False
```

### נאמנות תהליך {#process-fidelity}

אתה יכול גם להשוות אופרטורים באמצעות הפונקציה `process_fidelity` ממודול *Quantum Information*. זוהי כמות תיאורטית-אינפורמציונית לאיזה מידה שני ערוצים קוונטיים קרובים זה לזה, ובמקרה של אופרטורים יוניטריים היא אינה תלויה בפאזה הגלובלית.

```python
# Two operators which differ only by phase
op_a = Operator(XGate())
op_b = np.exp(1j * 0.5) * Operator(XGate())

# Compute process fidelity
F = process_fidelity(op_a, op_b)
print("Process fidelity =", F)
```

```text
Process fidelity = 1.0
```

שים לב שנאמנות תהליך היא בדרך כלל מדד תקף לקרבה רק אם האופרטורים של הקלט הם יוניטריים (או CP במקרה של ערוצים קוונטיים), ותוטל חריגה אם הקלטים אינם CP.
## השלבים הבאים {#next-steps}

<Admonition type="tip" title="המלצות">
  - חקור את ה-[Operator API](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator#operator).
</Admonition>
