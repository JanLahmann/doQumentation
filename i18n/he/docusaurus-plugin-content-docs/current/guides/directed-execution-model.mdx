---
title: מודל הרצה מכוון (בטא)
description: השתמש במודל הרצה מפורש וניתן להרכבה זה עם הניסויים בסדר גודל תועלתי שלך, כדי לכוונן עדין טכניקות הפחתת שגיאות ואחרות.

---
{/* doqumentation-source-hash: 39ac58a0 */}

# מודל הרצה מכוון (בטא) {#directed-execution-model-beta}

<Admonition type="note" title="מהדורת בטא">
כל הרכיבים במודל ההרצה המכוון נמצאים כעת בבטא ועשויים להיות לא יציבים. אתה מוזמן לבדוק אותם ולספק משוב על ידי פתיחת issue במאגרי GitHub של [Samplomatic](https://github.com/Qiskit/samplomatic/issues) או [Qiskit Runtime](https://github.com/Qiskit/qiskit-ibm-runtime/issues).
</Admonition>

ממשקי ה-primitive של Sampler ו-Estimator מספקים הפשטה ברמה גבוהה יותר למפתחי אלגוריתמים, כדי שיוכלו להתמקד יותר בחדשנות ופחות בהמרת נתונים. עם זאת, הם פחות מתאימים למדעני מידע קוונטי, שזקוקים לשליטה וגמישות רבות יותר בניסויים שלהם בסדר גודל תועלתי. **מודל ההרצה המכוון**, הנמצא כעת במהדורת בטא, עונה על צורך זה. מודל הרצה זה מספק את הכלים לתיעוד כוונות עיצוב בצד הלקוח, ומעביר את יצירת גרסאות ה-Circuit היקרה לצד השרת, כך שתוכל לכוונן עדין טכניקות הפחתת שגיאות ואחרות מבלי לפגוע בביצועים. מודל מפורש וניתן להרכבה זה מקל על ניסויים עם טכניקות חדשות, שחזור תוצאות ושיתוף שיטות.

במהדורת הבטא שלו, מודל ההרצה המכוון מתמקד במתן שליטה על טכניקות המובנות ב-Sampler וב-Estimator הקיימים, כולל Pauli twirling, למידת מודל רעש והזרקתו, ושינויי בסיס. תמיכה ביכולות נוספות תתווסף בהדרגה לאורך זמן.

## תהליך עבודה {#workflow}

אחת המטרות העיקריות של מודל ההרצה המכוון היא לספק דרך מודולרית להחיל שיטות הפחתת שגיאות. לדוגמה, ניתן להגדיר אילו שכבות ב-Circuit להפחית, או לכוונן את קצבי הרעש המוזרקים לתוך ה-Circuit.

כדי להחיל הפחתת שגיאות על Circuit במסגרת זו, תהליך העבודה שלך ידרוש בדרך כלל את השלבים הבאים (הכלים המוזכרים כאן מתוארים ביתר פירוט ב[סעיף הבא](#samplomatic-tools)):

1. קבץ הוראות לתוך boxes והחל עליהן annotations. ה-annotations מתעדות את הטרנספורמציה המיועדת מבלי ליצור בפועל את גרסאות ה-Circuit.

2. למד את מודלי הרעש של השכבות הייחודיות, במידת הצורך, באמצעות ה-[NoiseLearnerV3](#noiselearnerv3) החדש.

3. בנה את ה-template circuit ואת ה-samplex מה-Circuit המחולק ל-boxes.

4. הרץ את ה-template circuit ואת ה-samplex עם ה-primitive של [Executor](#executor-primitive), שיייצר וירוץ את גרסאות ה-Circuit בהתאם להוראות.

5. עבד לאחר מכן את תוצאות ההרצה. לדוגמה, ניתן להחיל post-selection, או לחלץ ערכי ציפייה מוגנים מתוצאות ההרצה.

<span id="samplomatic-tools"></span>
## כלים למודל ההרצה המכוון {#tools-for-the-directed-execution-model}

הכלים הבאים יכולים לשמש יחד כדי לממש טכניקת הפחתת שגיאות במודל ההרצה המכוון.

### Samplomatic

[Samplomatic](https://github.com/Qiskit/samplomatic/) היא ספרייה חדשה בקוד פתוח שתומכת בראנדומיזציות דגימה מותאמות אישית. היא משתמשת ב-construct של [box](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#box) כדי לנמק על אוספי פעולות Circuit שיש להתייחס אליהן כבעלות הקשר רעש יציב, ומשתמשת ב-annotations על boxes כדי לאפשר לך להצהיר ולהגדיר כוונות. לדוגמה, ניתן לחלק את ה-Circuit שלך ל-boxes, להוסיף annotation של twirling לכל box, ולציין איזו קבוצת twirling להשתמש בה, כפי שמוצג בתרשים הבא:

![דוגמה לשימוש ב-boxes וב-annotations של twirling](/docs/images/guides/directed-execution-model/execution-model.avif)

Circuit עם boxes מסומנים ניתן לאחר מכן להשתמש בו כדי ליצור *template circuit* ו-*samplex*. ה-template circuit הפלט הוא Circuit פרמטרי שיורץ ללא שינוי נוסף (מלבד ערכי פרמטרים שונים המוקצים לו). ה-samplex, שהוא הסוג המרכזי של ספריית Samplomatic, מייצג התפלגות הסתברות פרמטרית על פרמטרי ה-template circuit ושדות נוספים בעלי ערך מערך. שדות אלה יכולים לשמש לעיבוד נתונים לאחר ביצוע ה-bound template circuit. במילים אחרות, זוג ה-template circuit וה-samplex אומר ל-primitive של Executor (המתואר להלן) בדיוק אילו פרמטרים לייצר ואילו circuits קשורים להריץ. מכיוון שמבנים שניים אלה נוצרים בצד הלקוח, ניתן לבצע בדיקה ודגימה מקומיות לאימות הפלטים לפני שליחתם להרצה על חומרה.

כדי לפשט את תהליך יצירת ה-boxes המסומנים, ספריית Samplomatic מספקת גם Transpiler passes שמקבצים אוטומטית הוראות Circuit ל-boxes מסומנים, בהתאם לאסטרטגיות שאתה מספק.

למידע נוסף על Samplomatic, בקר בתיעוד [המדריכים](https://qiskit.github.io/samplomatic/guides/index.html) ו[הפניית ה-API](https://qiskit.github.io/samplomatic/). אל תהסס לשלוח משוב ולדווח על באגים במאגר [GitHub](https://github.com/Qiskit/samplomatic) שלו.

### ה-primitive של Executor {#executor-primitive}

Executor הוא primitive חדש של Qiskit Runtime שמקבל את זוג ה-template circuit וה-samplex כקלט, מייצר וקושר ערכי פרמטרים בהתאם ל-samplex, מריץ את ה-circuits הקשורים על החומרה, ומחזיר את תוצאות ההרצה ומטא-נתונים. הוא פועל לפי הנחיות זוג הקלט ואינו מקבל שום החלטות משתמעות עבורך, כך שהתהליך שקוף אך גם יעיל.

כדי לגשת ל-`Executor`, התקן את הענף `executor_preview` מ-`qiskit-ibm-runtime`:

```bash
pip install -U git+https://github.com/Qiskit/qiskit-ibm-runtime.git@executor_preview
```
הקלטים והפלט של ה-primitive של Executor שונים מאוד מאלה של Sampler ו-Estimator. עיין ב[הפניית ה-API של Executor](https://qiskit.github.io/qiskit-ibm-runtime/stubs/qiskit_ibm_runtime.Executor.html) למידע נוסף. בנוסף, [מדריך ה-quickstart של Executor](https://qiskit.github.io/qiskit-ibm-runtime/guides/executor_basic.html) מספק סקירה כללית ודוגמאות קוד.

### NoiseLearnerV3

בדומה ל-[`NoiseLearner`](/guides/noise-learning) הנוכחי, תוכנית העזר של Qiskit Runtime זו מחזירה את [מודל הרעש Pauli-Lindblad הדליל](https://arxiv.org/abs/2201.09866) המשמש בשיטות הפחתת שגיאות רבות, כולל [PEC](/guides/error-mitigation-and-suppression-techniques#probabilistic-error-cancellation-pec), [PEA](/guides/error-mitigation-and-suppression-techniques#probabilistic-error-amplification-pea) ו-[PNA](https://github.com/Qiskit/qiskit-addon-pna/tree/main). ב-`NoiseLearner` המקורי, אתה מעביר רשימת circuits, והתוכנית מחלקת את ה-circuits לשכבות ומחזירה את מודל הרעש עבור כל שכבה ייחודית. לעומת זאת, `NoiseLearnerV3` נותן לך שליטה על האופן שבו לחלק את ה-circuits שלך לשכבות, והתוכנית פשוט מקבלת רשימת הוראות Circuit מחולקות ל-boxes (לדוגמה, שכבות ייחודיות) כקלט.

`NoiseLearnerV3` תומך גם בלמידת רעש מדידה. עבור כל קבוצת הוראות ברשימת הקלט, הוא מריץ את פרוטוקול למידת Pauli-Lindblad אם הקבוצה מכילה שערים חד- ודו-Qubit, ואת פרוטוקול [TREX](/guides/error-mitigation-and-suppression-techniques#twirled-readout-error-extinction-trex) אם הקבוצה מכילה מדידות.
כדי לגשת ל-`NoiseLearnerV3`, התקן את הענף `executor_preview` מ-`qiskit-ibm-runtime`:

```bash
pip install -U git+https://github.com/Qiskit/qiskit-ibm-runtime.git@executor_preview
```
למידע נוסף על NoiseLearnerV3, עיין בתיעוד [הפניית ה-API](https://qiskit.github.io/qiskit-ibm-runtime/apidocs/noise_learner_v3.html) שלו.

## הצעדים הבאים {#next-steps}

<Admonition type="tip" title="המלצות">
- בדוק שני Qiskit addons, [Shaded lightcones](https://qiskit.github.io/qiskit-addon-slc/) ו-[Propagated noise absorption](https://github.com/Qiskit/qiskit-addon-pna), שנבנו על גבי מודל הרצה זה.
</Admonition>
