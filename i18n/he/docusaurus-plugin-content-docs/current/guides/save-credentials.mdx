---
description: 'שמור את פרטי הכניסה שלך במקום להזינם ידנית בכל פעם'
title: 'שמירת פרטי הכניסה שלך'
---

{/* doqumentation-source-hash: a0b9574a */}

# שמירת פרטי הכניסה שלך {#save-your-login-credentials}
אם אתה עובד בסביבת Python מהימנה (כגון מחשב נייד אישי או תחנת עבודה), אתה יכול להשתמש במתודה `save_account()` כדי לשמור את פרטי הכניסה שלך באופן מקומי, ולאחר מכן להשתמש בהם לאתחול השירות.

<Admonition type="note" title="הערות">
      * אם אתם משתמשים במחשב ציבורי או בסביבה לא מהימנה אחרת, עקבו במקום זאת אחרי ההוראות ב[אתחול השירות בסביבה לא מהימנה](/guides/cloud-setup-untrusted).
      * עקבו אחרי [ההוראות האלה](/guides/cloud-setup-rest-api) אם אתם רוצים להתחבר דרך REST API במקום דרך Qiskit.
      * אם צריך, השתמשו ב[מידע הזה](/guides/quickstart-steps-org#firewall) כדי להגדיר את חומת האש שלכם לאפשר גישה לנקודות הקצה של IBM Quantum API.
</Admonition>

## לפני שמתחילים {#before-you-begin}

1. ודא שאתה עובד בסביבת Python פעילה עם [`qiskit` ו-`qiskit-ibm-runtime` packages](/guides/install-qiskit#local) מותקנים.

1. הפעל את סביבת ה-virtual Python ו-Python בסביבה הוירטואלית שלך.

1. התחבר ל-[IBM Quantum Platform](https://quantum.cloud.ibm.com).


<span id="find-credentials"></span>

## מציאת פרטי הגישה שלך {#find-your-access-credentials}

1. ודא שהחשבון והאזור הנכונים נבחרו במתג החשבונות בכותרת.

1. מצא את מפתח ה-API שלך. מה-[dashboard](https://quantum.cloud.ibm.com/), צור את מפתח ה-API שלך, העתק אותו למיקום מאובטח כדי שתוכל להשתמש בו לאימות. שים לב שאתה יכול להשתמש באותו מפתח API כדי להתחבר לכל אזור.

1. אופציונלי: מצא את ה-instance שברצונך להשתמש בו מדף [Instances](https://quantum.cloud.ibm.com/instances). רחף מעל ה-CRN שלו, לחץ על הסמל כדי להעתיק אותו, ולאחר מכן שמור אותו במיקום מאובטח כדי שתוכל להשתמש בו לזיהוי ה-instance.


<span id="cloud-save"></span>

## שמירת פרטי הגישה שלך {#save-your-access-credentials}

שמור את פרטי הכניסה שלך על ידי הרצת הקוד המתאים פעם אחת לכל חשבון שברצונך לשמור. לאחר שמירת פרטי הכניסה שלך, טען אותם על ידי ביצוע השלבים ב[אתחול IBM Quantum Compute Service](/guides/initialize-account).

<span id="save-instance"></span>

### שמירת פרטי כניסה לגישה ל-instance ספציפי: {#save-credentials-that-access-a-specific-instance}
אם יש לך מספר instances וברצונך לציין ל-Quantum Compute איזה instance להשתמש, שמור פרטי כניסה הכוללים CRN של instance.

    ```python
    from qiskit_ibm_runtime import QiskitRuntimeService

    QiskitRuntimeService.save_account(
      # For `token`, use the 44-character API_KEY you created 
      # and saved from the IBM Quantum Platform Home dashboard:
      token="<your-api-key>", 
      name="<account-name>", # Optional
      instance="<IBM Cloud CRN or instance name>", # Optional
      set_as_default=True, # Optional
      overwrite=True, # Optional
    )
    ```

<span id="auto-select"></span>

### שמירת פרטי כניסה לבחירת instance אוטומטית: {#save-credentials-for-automatic-instance-selection}
אם אינך מספק CRN של instance ומעביר פרטי כניסה אלה ל-Quantum Compute, instance מתאים ייבחר עבורך בהתאם לאפשרויות שצוינו.

    ```python
    from qiskit_ibm_runtime import QiskitRuntimeService

    QiskitRuntimeService.save_account(
      # For `token`, use the 44-character API_KEY you created 
      # and saved from the IBM Quantum Platform Home dashboard:
      token="<your=api-key>", 
      name="<account-name>", # Optional
      instance="<instance-to-use>", # Optional
      plans_preference="<plan_types>", # Optional
      region="<region>", # Optional
      tags="<instance-tags>", # Optional
      set_as_default=True, # Optional
      overwrite=True, # Optional
    )
   ```

### Available options {#available-options}

- **`token`**: IBM Cloud API key. Your token is confidential. Do not share your token in public code.
- **`instance`**: Optionally specify the instance to use through its IBM Cloud CRN or instance name.
- **`plans_preference`**: Optionally set the types of plans to prioritize. This is ignored if the instance is specified. Available options are `open`, `pay-as-you-go`, `flex`, `premium`, and `on-prem`. Instances of a certain plan type are excluded if the plan name is not specified. For example, if [`open`] is passed in, only Open Plan instances are available. This is ignored if `instance` is specified.
- **`region`**: Optionally set the region to use. Accepted values are `us-east` and `eu-de`. This is ignored if `instance` is specified.
- **`tags`**: Optionally specify the instance's tags. Accepts a list of tag name strings. This is ignored if `instance` is specified.
- **`name`**: Optionally name this set of account credentials.
- **`set_as_default`**: Set the value to `True` to save these as your default credentials. If you save only one account, it is automatically set as the default.
- **`overwrite`**: Set this value to `True` to update your default credentials.

## Examples {#examples}

### Example 1 {#example-1}
This example saves credentials for specific instances, which allow open and premium access.  The open credentials are set as the default.

```python
from qiskit_ibm_runtime import QiskitRuntimeService

QiskitRuntimeService.save_account(token="<API_TOKEN>", 
                                  instance="<CRN_for_premium_instance>",
                                  name="premium")
QiskitRuntimeService.save_account(token="<API_TOKEN>", 
                                  instance="<CRN_for_open_instance>",
                                  name="open", set_as_default=True)
 ```

### דוגמה 2 {#example-2}
דוגמה זו שומרת פרטי כניסה לבחירת instance אוטומטית. Quantum Compute יסתכל רק על instances premium באזור ה-EU הזמינים לחשבון. פרטי כניסה אלה הופכים לברירת המחדל החדשה.

```python
QiskitRuntimeService.save_account(
    token="your-api-key",
    set_as_default=True,
    overwrite = True,

    # Set instance "filters" instead of specifying the instance:
    region="eu-de",
    plans_preference=["premium"]
  )
 ```

## צפייה בפרטי כניסה שמורים {#view-saved-credentials}

כדי לצפות בכל פרטי הכניסה שמרת, הרץ `service.saved_accounts()`. שים לב שאם שמרת פרטי כניסה כברירת מחדל ולא נתת להם שם, הם ייקראו `default-ibm-quantum-platform`. אם נתת שם לפרטי ברירת המחדל שלך, תראה `"is_default_account": true` בפלט.

## שיקולים {#considerations}

* אם אתה שומר מספר חשבונות, השתמש בפרמטר `name` כדי להבחין ביניהם.

* פרטי הכניסה נשמרים ב-`$HOME/.qiskit/qiskit-ibm.json`. אל תערוך קובץ זה ידנית.

* אם אינך שומר את פרטי הכניסה שלך, עליך לציין אותם בכל פעם שאתה מתחיל session חדש (מאתחל את שירות IBM Quantum Compute Service).

* אם אתה מציין ידנית את פרטי הכניסה שלך, חשבון שמור לא ישמש.


# השלבים הבאים {#next-steps}

<Admonition type="tip" title="המלצות">
  -  [אתחלו את שירות IBM Quantum Compute Service](/guides/initialize-account) בסביבת Python מהימנה.
  -  [אתחלו את שירות IBM Quantum Compute Service **בסביבה לא מהימנה**.](/guides/cloud-setup-untrusted)
  -  [ראו אילו QPUs זמינים לכם.](/guides/qpu-information#available)
  -  [הגדירו את Qiskit SDK באופן מקומי.](/guides/configure-qiskit-local)
  -  עקבו אחרי השלבים ב[הרצת ה-Circuit הראשון שלכם בחומרה](/guides/hello-world) כדי לכתוב ולהריץ תוכנית קוונטית.
  -  [הגדירו שימוש ב-IBM Quantum Platform עם REST API.](/guides/cloud-setup-rest-api)
  -  נסו [הדרכה](https://docs.quantum.ibm.com/tutorials).
</Admonition>
