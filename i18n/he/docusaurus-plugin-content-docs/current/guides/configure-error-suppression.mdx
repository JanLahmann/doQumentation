---
title: "הגדרת דיכוי שגיאות"
sidebar_label: "הגדרת דיכוי שגיאות"
description: "איך להגדיר דיכוי שגיאות ב-Qiskit Runtime. (כותרת קודמת - קומפילציה בזמן ריצה)"
notebook_path: "guides/configure-error-suppression.ipynb"
---

{/* doqumentation-source-hash: 938d2f84 */}

<OpenInLabBanner notebookPath="guides/configure-error-suppression.ipynb" />



<Admonition type="note" title="מודל ביצוע חדש, כעת בגרסת בטא">
גרסת הבטא של מודל ביצוע חדש זמינה עכשיו. מודל הביצוע המכוון מספק גמישות רבה יותר בהתאמה אישית של תהליך העבודה שלך להפחתת שגיאות. ראה את המדריך [מודל ביצוע מכוון](/guides/directed-execution-model) למידע נוסף.
</Admonition>
{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>גרסאות חבילות</b></summary>

הקוד בדף זה פותח באמצעות הדרישות הבאות.
אנו ממליצים להשתמש בגרסאות אלה או בגרסאות חדשות יותר.

```
qiskit-ibm-runtime~=0.43.1
```
</details>
דיכוי שגיאות מתייחס לטכניקות שבהן משתמשים בידע על השפעות לא רצויות כדי להכניס התאמות אישיות שיכולות לצפות ולהימנע מהשפעות פוטנציאליות אלה. טכניקות אלה כוללות לרוב שינוי או הוספת אותות בקרה כדי להבטיח שהמעבד הקוונטי מחזיר את התוצאות הרצויות. בדרך כלל הדבר גורר תקורה של עיבוד מקדמי קוונטי; לכן, חשוב למצוא איזון בין שיפור התוצאות לבין הבטחה שהעבודה שלך תסתיים בזמן סביר.

ה-Primitives תומכים במספר טכניקות לדיכוי שגיאות, כולל [דיכוי דינמי](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-dynamical-decoupling-options#dynamicaldecouplingoptions) ו-[Pauli twirling](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-twirling-options). ראה [טכניקות להפחתה ודיכוי שגיאות](error-mitigation-and-suppression-techniques) להסבר על כל אחת מהן. בעת שימוש ב-Primitives, ניתן להפעיל או לכבות שיטות בודדות. ראה את הסעיף [אפשרויות מתקדמות לדיכוי שגיאות](#transpilation-table) לפרטים.

ה-Estimator מפעיל דיכוי שגיאות והפחתת שגיאות כברירת מחדל. אם אינך רוצה שיבוצע עיבוד כלשהו על מעגלי הקלט שלך, עקוב אחר ההוראות בסעיף [כיבוי כל הפחתת השגיאות ודיכוי השגיאות](specify-runtime-options#no-error-mitigation).

<span id="transpilation-table"></span>
## אפשרויות מתקדמות לדיכוי שגיאות {#advanced-error-suppression-options}

ב-Primitives, ניתן להפעיל ולכבות באופן מפורש שיטות בודדות להפחתה ודיכוי שגיאות, כמו דיכוי דינמי.

<Admonition type="note" title="הערות">
- לא כל האפשרויות זמינות לשני ה-Primitives. ראה את הטבלה [אפשרויות זמינות](runtime-options-overview#options-table) לרשימת האפשרויות הזמינות.
- לא כל השיטות עובדות יחד על כל סוגי המעגלים. ראה את הטבלה [תאימות תכונות](runtime-options-overview#options-compatibility-table) לפרטים.
</Admonition>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit-ibm-runtime
```

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import SamplerV2 as Sampler

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

sampler = Sampler(backend)

# Turn on dynamical decoupling with sequence XpXm.
sampler.options.dynamical_decoupling.enable = True
sampler.options.dynamical_decoupling.sequence_type = "XpXm"

print(
    f">>> dynamical decoupling sequence to use: {sampler.options.dynamical_decoupling.sequence_type}"
)
```

```text
>>> dynamical decoupling sequence to use: XpXm
```

<CodeAssistantAdmonition tagLine="Can't remember the right attributes? Try asking Qiskit Code Assistant." />

## כיבוי כל דיכוי השגיאות {#turn-off-all-error-suppression}

להוראות לכיבוי כל דיכוי השגיאות, ראה את הסעיף [כיבוי כל דיכוי השגיאות וההפחתה](specify-runtime-options#no-error-mitigation).

## השלבים הבאים {#next-steps}

<Admonition type="tip" title="המלצות">
    - למד עוד על [טכניקות להפחתה ודיכוי שגיאות.](error-mitigation-and-suppression-techniques)
    - [הגדר הפחתת שגיאות.](configure-error-mitigation)
    - חקור [אפשרויות](runtime-options-overview) נוספות.
    - החלט באיזה [מצב ביצוע](execution-modes) להריץ את העבודה שלך.
</Admonition>
