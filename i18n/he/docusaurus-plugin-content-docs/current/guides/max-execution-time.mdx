---
description: 'מתאר כמה זמן משימת IBM Quantum Compute Service יכולה לרוץ, ללא קשר לאיזה מצב ביצוע בשימוש.'
notebook_path: guides/max-execution-time.ipynb
sidebar_label: 'זמן ביצוע מקסימלי לעומסי עבודה של IBM Quantum Compute Service'
title: 'זמן ביצוע מקסימלי לעומסי עבודה של IBM Quantum Compute Service'
---

{/* doqumentation-source-hash: 735052ff */}




כדי להבטיח הוגנות, קיים [זמן מקסימלי מוטל על ידי השירות](#max-QPU) שכל משימה יכולה לרוץ. אם משימה חורגת ממגבלת זמן זו, היא מבוטלת בכפייה ונזרקת חריגה מסוג `RuntimeJobMaxTimeoutError`. באפשרותך גם [להגדיר זמן ביצוע מקסימלי](#max-job) על המשימות שלך כדי לסייע בשליטה על עלויות.

 <Admonition type="note">
   הסשן או ה-batch מסתיימים מיידית כאשר מגבלת העלות מגיעה לסיומה. כל המשימות שרצות כעת בסשן או ב-batch יסתיימו, אך כל המשימות הממתינות יבוטלו.
</Admonition>

<span id="max-job"></span>

## הגדרת זמן ביצוע מקסימלי {#set-a-maximum-execution-time}

זמן הביצוע המקסימלי למשימה הוא הקטן מבין הערכים הבאים:

- הערך שהוגדר עבור `max_execution_time`

- ערך פסק הזמן המקסימלי של השירות (שלוש שעות)


ערך `max_execution_time` מבוסס על *שימוש ב-QPU* (שימוש קוונטי בלבד), לא על זמן שעון קיר.

הגדר את זמן הביצוע המקסימלי (בשניות) באפשרויות המשימה, כפי שמוצג בדוגמה הבאה. ראה את המדריכים [אפשרויות Estimator](/guides/estimator-options), [אפשרויות Sampler](/guides/sampler-options), או [אפשרויות Executor](/guides/executor-options) לקבלת מידע על הגדרת אפשרויות.

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import EstimatorV2 as Estimator

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

estimator = Estimator(mode=backend)

estimator.options.max_execution_time = 2500
```

באפשרותך גם לגלות כמה שימוש צרכו משימות שהושלמו, על ידי החזרת מדדי המשימה באופן הבא:

```python
# Find QPU usage of a specific job
print(f"QPU usage of job {job.job_id()} was {job.metrics()['usage']['quantum_seconds']} seconds")
```

<span id="max-QPU"></span>

## זמן ביצוע מקסימלי הנאכף על ידי השירות {#service-enforced-maximum-execution-time}

השירות אוכף מגבלת זמן ביצוע מקסימלית של שלוש שעות כדי להבטיח שימוש הוגן במכשיר. אם צוין גם `max_execution_time` עבור המשימה, נעשה שימוש בקטן מבין שני הערכים.

לדוגמה, אם תציין `max_execution_time=5000` (בערך 83 דקות), אז המשימה תבוטל לאחר בערך 83 דקות, אף על פי שהשירות היה יכול לאפשר לה לרוץ שלוש שעות.

<span id="batch-max-time"></span>

## זמן ביצוע מקסימלי של Batch {#batch-maximum-execution-time}

כאשר batch מתחיל, הוא מקבל ערך זמן מקסימלי לחיות (maximum TTL). לאחר שה-TTL הזה מגיע לסיומו, ה-batch מסתיים, כל המשימות שכבר רצות ממשיכות לרוץ, וכל המשימות שבתור שנותרות ב-batch עוברות למצב כשל.

ל-batch יש גם ערך זמן אינטראקטיבי לחיות (interactive TTL) בין משימות שאינו ניתן לקונפיגורציה. אם לא תסגור batch במפורש, הוא יושבת לאחר שה-interactive TTL יפוג וניתן להפעילו מחדש בכל עת עד שיגיע ל-maximum TTL שלו.

להוראות לעבודה עם ערכים אלה, ראה [הרצת משימות ב-batch](/guides/run-jobs-batch).

<span id="session-max-time"></span>

## זמן ביצוע מקסימלי של סשן {#session-maximum-execution-time}

כאשר סשן מתחיל, הוא מקבל ערך maximum TTL הקובע כמה זמן הסשן יכול לרוץ. לאחר שה-TTL הזה מגיע לסיומו, הסשן מסתיים, כל המשימות שכבר רצות ממשיכות לרוץ, וכל המשימות שבתור שנותרות בסשן עוברות למצב כשל.

קיים גם ערך interactive TTL שאינו ניתן לקונפיגורציה. אם לא יועמדו משימות סשן בתור בתוך חלון זמן זה, הסשן יושבת זמנית.

להוראות לעבודה עם ערכים אלה, ראה [הרצת משימות בסשן](/guides/run-jobs-session).

## מגבלות נוספות {#other-limitations}

בתוכנית Open Plan, ניתן להשתמש בעד 10 דקות על QPU בכל חלון גלילה של 28 ימים. באפשרותך לצפות בשימוש של מופע בדף [המופעים](https://quantum.cloud.ibm.com/instances).

## הצעדים הבאים {#next-steps}

<Admonition type="tip" title="Recommendations">
- [שימוש בעומס עבודה](/guides/estimate-job-run-time).
- ראה בטיפים אלה: [צמצום זמן הרצת משימות](/guides/minimize-time).
</Admonition>
