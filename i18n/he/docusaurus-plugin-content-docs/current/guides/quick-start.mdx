---
title: "התחלה מהירה"
sidebar_label: "התחלה מהירה"
description: "בנה ותצוגה של Circuit קוונטי בפחות משתי דקות, ללא כניסה למערכת או מפתח API."
notebook_path: "guides/quick-start.ipynb"
---
{/* doqumentation-source-hash: 4dc6c1d3 */}

<OpenInLabBanner notebookPath="guides/quick-start.ipynb" />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';



{/* cspell:ignore miniconda */}

{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>Package versions</b></summary>

The code on this page was developed using the following requirements.
We recommend using these versions or newer.

```
qiskit[all]~=2.3.0
```
</details>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

בנה את ה-Circuit הקוונטי הראשון שלך בפחות משתי דקות, בסביבה המקומית שלך — ללא כניסה למערכת או מפתח API.

<Admonition type="note" title="חדש ב-Python ובסביבות וירטואליות?">

* הורד את Python והשתמש בסביבה וירטואלית עם Qiskit (מומלץ).

<details>
<summary>לחץ להרחבה לקבלת מידע נוסף על **Python**.</summary>

- כדי להתקין Python, בדוק תחילה את החלק "Programming Language" ב[דף הפרויקט Qiskit ב-PyPI](https://pypi.org/project/qiskit/) כדי לקבוע אילו גרסאות Python נתמכות על ידי המהדורה האחרונה. להוראות הורדה, ראה את [מדריך Python למתחילים](https://wiki.python.org/moin/BeginnersGuide/Download).

<Admonition type="note">
הוראות אלו משתמשות בהפצת Python הסטנדרטית מ-[pypi.org](https://pypi.org/). עם זאת, תוכל להשתמש בהפצות Python אחרות, כגון [Anaconda](https://docs.anaconda.com/anaconda/) או [miniconda](https://docs.anaconda.com/miniconda/), יחד עם זרימות עבודה אחרות לניהול תלויות כמו [Poetry](https://python-poetry.org/docs/).
</Admonition>
</details>

    <details>
    <summary>
    לחץ להרחבה לקבלת מידע נוסף על **סביבות וירטואליות**.
    </summary>
    - השתמש ב[סביבות וירטואליות Python](https://docs.python.org/3.10/tutorial/venv.html) כדי להפריד את Qiskit מיישומים אחרים.
    סביבה וירטואלית Python היא מרחב מבודד לעבודה עם Python למטרה ספציפית — כך שתוכל להתקין כל חבילה שתרצה, ולהגדיר ספריות, תלויות, וכן הלאה, מבלי להשפיע על סביבת ה-Python "הבסיסית" במחשב שלך.

    יתרון חשוב אחד של סביבה וירטואלית הוא שאם סביבת ה-Python שלך מתקלקלת, תוכל בקלות למחוק אותה ולהתחיל מחדש!

    בחר מיקום מועדף לאחסון מידע על הסביבות הווירטואליות שלך. בדרך כלל הן מאוחסנות בתיקייה בשם `.venv` בתוך כל תיקיית פרויקט.

    כדי להגדיר סביבה וירטואלית, נווט לתיקיית הפרויקט שלך וצור סביבה מינימלית עם Python בלבד מותקן בה.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

    לאחר מכן, הפעל את הסביבה החדשה שלך.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
              אם משתמשים ב-PowerShell:

             ```text
             .venv\Scripts\Activate.ps1
             ```
             אם משתמשים ב-Git Bash:

             ```text
             source .venv/scripts/activate
             ```
              אם משתמשים ב-command prompt:

             ```text
             .venv\Scripts\activate
             ```
           </TabItem>
         </OperatingSystemTabs>
    </details>

</Admonition>

## 1. התקן את Qiskit {#1-install-qiskit}

התקן את הפריטים הבאים עם מנהל החבילות המועדף עליך (כגון `pip`):

- [`qiskit`](/guides/install-qiskit)
- [`matplotlib`](https://matplotlib.org/stable/users/explain/quick_start.html)
- [`qiskit[visualization]`](https://docs.quantum.ibm.com/api/qiskit/visualization)
## 2. בנה את ה-Circuit שלך {#2-build-your-circuit}

פתח סביבת Python, ואז הרץ את הקוד הזה כדי לבנות מצב Bell (שני Qubit שזורים).

```python
from qiskit import QuantumCircuit
from qiskit.primitives import StatevectorSampler

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

sampler = StatevectorSampler()
result = sampler.run([qc], shots=1024).result()
print(result[0].data.meas.get_counts())
```

```text
{'11': 534, '00': 490}
```

הפלט הצפוי הוא חלוקה כמעט שווה בין '00' ל-'11'.
## 3. הצג את התוצאות {#3-visualize-your-results}

כדי לקבל היסטוגרמה של התוצאות, הוסף את הקוד הבא לתוכנית שלך.

```python
# Uncomment lines 2 and 8 if you are not using Python in a Jupyter notebook
# import matplotlib.pyplot as plt
from qiskit.visualization import plot_histogram

counts = result[0].data.meas.get_counts()
plot_histogram(counts)

# plt.show()
```

![Output of the previous code cell](/docs/images/guides/quick-start/extracted-outputs/dc4ff012-0.svg)

תוצאה זו היא חתימה של שזירה קוונטית.

## 4. ראה מה קורה {#4-see-what-happens}

נסה לשנות את הקוד כדי לראות כיצד הוא משפיע על התוצאות. לדוגמה:

- הוסף Qubit שלישי על ידי שינוי ל-`QuantumCircuit(3)`, והוסף Gate CX שני עם `qc.cx(1,2)`. המדידות אמורות אז להשתנות ל-000 ו-111, כלומר כל שלושת ה-Qubit הללו שוזרו.

- ראה את התוצאות משתנות על ידי הוספת `qc.x(1)` לסוף ה-Circuit.
## הצעדים הבאים {#next-steps}

<Admonition type="tip" title="המלצות">
- עקוב אחר השלבים ב-[Hello world](/guides/hello-world) כדי להריץ Circuit על חומרה קוונטית אמיתית.
- עדיין לא מוכן להריץ על חומרה? התחל את מסע הקוונטים שלך עם קורס [יסודות המידע הקוונטי](/learning/courses/basics-of-quantum-information).
</Admonition>
