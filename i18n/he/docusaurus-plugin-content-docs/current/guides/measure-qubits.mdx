---
title: "מדידת Qubits"
sidebar_label: "מדידת Qubits"
description: "למד כיצד למדוד Qubits, כולל אילוצים על היכן ניתן להשתמש במדידות."
notebook_path: "guides/measure-qubits.ipynb"
---
{/* doqumentation-source-hash: 421a17fa */}

<OpenInLabBanner notebookPath="guides/measure-qubits.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Package versions">

The code on this page was developed using the following requirements.
We recommend using these versions or newer.

```
qiskit[all]~=2.4.0
```
</AccordionItem>
</Accordion>
כדי לקבל מידע על מצב ה-Qubit, אפשר _למדוד_ אותו על [סיבית קלאסית](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Clbit). ב-Qiskit, מדידות מבוצעות בבסיס החישובי, כלומר בסיס Pauli-$Z$ חד-Qubit. לכן, מדידה מניבה 0 או 1, בהתאם לחפיפה עם מצבי הייגן של Pauli-$Z$, שהם $|0\rangle$ ו-$|1\rangle$:

$$
|q\rangle \xrightarrow{measure}\begin{cases}
      0 (\text{outcome}+1), \text{with probability } p_0=|\langle q|0\rangle|^{2}\text{,} \\
      1 (\text{outcome}-1), \text{with probability } p_1=|\langle q|1\rangle|^{2}\text{.}
    \end{cases}
$$

## החלת מדידה על Circuit {#apply-a-measurement-to-a-circuit}

ישנן מספר דרכים להחיל מדידות על Circuit:

### מתודת `QuantumCircuit.measure` {#quantumcircuitmeasure-method}

השתמש במתודת [`measure`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure) כדי למדוד [`QuantumCircuit`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#quantumcircuit-class).

דוגמאות:

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(5, 5)
qc.x(0)
qc.x(1)
qc.x(4)
qc.measure(
    range(5), range(5)
)  #  Measures all qubits into the corresponding clbit.
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c636c20>
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure(1, 0)  # Measure qubit 1 into the classical bit 0.
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c636830>
```

### מחלקת `Measure` {#measure-class}

מחלקת [Measure](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure) של Qiskit מודדת את ה-Qubits שצוינו.

```python
from qiskit.circuit import Measure

qc = QuantumCircuit(3, 1)
qc.x([0, 1])
qc.append(Measure(), [0], [0])  # measure qubit 0 into clbit 0
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c6369e0>
```

### מתודת `QuantumCircuit.measure_all` {#quantumcircuitmeasure_all-method}

כדי למדוד את כל ה-Qubits לתוך הסיביות הקלאסיות המתאימות, השתמש במתודת [`measure_all`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_all). כברירת מחדל, מתודה זו מוסיפה סיביות קלאסיות חדשות ב-`ClassicalRegister` כדי לאחסן מדידות אלה.

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure_all()  # Measure all qubits.
```

### מתודת `QuantumCircuit.measure_active` {#quantumcircuitmeasure_active-method}

כדי למדוד את כל ה-Qubits שאינם בטלים, השתמש במתודת [`measure_active`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_active). מתודה זו יוצרת `ClassicalRegister` חדש בגודל השווה למספר ה-Qubits הלא-בטלים הנמדדים.

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure_active()  # Measure qubits that are not idle, that is, qubits 0 and 2.
```

## הצעדים הבאים {#next-steps}

<Admonition type="tip" title="Recommendations">
- מחלקת [`Measure`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure)
- מתודת [`measure_all`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_all)
- מתודת [`measure_active`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_active)
- מתודת [`random_circuit`](https://docs.quantum.ibm.com/api/qiskit/circuit_random#qiskit.circuit.random.random_circuit)
- [Mid-circuit measurements](/guides/execute-dynamic-circuits#midcircuit) (Available only when using Qiskit Runtime.)
</Admonition>
