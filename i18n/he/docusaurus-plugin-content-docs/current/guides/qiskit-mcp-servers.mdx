---
title: שרתי Qiskit MCP
description: השתמש בשרתי Qiskit MCP כדי לשלב יכולות מחשוב קוונטי בעוזרי AI ובסוכנים.
---
{/* doqumentation-source-hash: 5171943c */}
{/* cspell:ignore modelcontextprotocol  */}

# שרתי Qiskit MCP {#qiskit-mcp-servers}

שרתי Qiskit MCP הם אוסף של שרתי [Model Context Protocol (MCP)](https://modelcontextprotocol.io/) המשלבים יכולות מחשוב קוונטי במערכות AI. שרתים אלו מאפשרים לעוזרי AI, מודלי שפה גדולים (LLMs) וסוכנים לגשת לשירותי IBM Quantum&reg; ולספריות Qiskit.

<Admonition type="note">
זהו פרויקט קוד פתוח. לקבלת המידע העדכני ביותר, כיסוי API מפורט והגדרות מתקדמות, ראה ב[מאגר GitHub של שרתי Qiskit MCP](https://github.com/Qiskit/mcp-servers).
</Admonition>

## מתי כדאי להשתמש בשרתי Qiskit MCP? {#when-should-i-use-qiskit-mcp-servers}

שרתי Qiskit MCP מאפשרים לעוזרי AI לעזור לך במשימות מחשוב קוונטי, כגון:

- יצירה, מניפולציה וטרנספילציה של מעגלים קוונטיים
- אחזור וחיפוש בתיעוד Qiskit, מדריכים ומדריכי API
- מענה על שאלות בנושא מחשוב קוונטי ישירות ממקורות אמינים
- יצירת קוד קוונטי עם מודעות להקשר
- אופטימיזציה של מעגלים באמצעות טרנספילציה מונעת AI
- התחברות לחומרה קוונטית אמיתית באופן אוטומטי
- ניתוח ובחירת Backend קוונטיים אופטימליים
- הרצת מעגלים קוונטיים ומעקב אחר סטטוס משימות

## מה כלול? {#what-is-included}

שרתי ה-MCP הבאים זמינים:

### שרת Qiskit MCP {#qiskit-mcp-server}

מספק יכולות מחשוב קוונטי בסיסיות לפיתוח מקומי. שרת זה מאפשר לעוזרי AI ליצור, לתפעל ולטרנספל מעגלים קוונטיים, וכן לבצע להם סריאליזציה לפורמטים כמו OpenQASM 3 ו-QPY.

### שרת Qiskit Runtime MCP {#qiskit-runtime-mcp-server}

מספק גישה לשירותי IBM Quantum בענן דרך Qiskit Runtime. זה מאפשר לעוזרי AI לתקשר עם Backend קוונטיים, להגיש משימות ולאחזר תוצאות.

### שרת Qiskit Docs MCP {#qiskit-docs-mcp-server}

מספק לעוזרי AI גישה למערכת התיעוד המלאה של Qiskit, כולל תיעוד מודולי SDK, מדריכי מימוש ושיטות עבודה מומלצות. שרת זה מאפשר אחזור וחיפוש חכם בתיעוד Qiskit ללא צורך באימות.

### שרת Qiskit IBM Transpiler MCP {#qiskit-ibm-transpiler-mcp-server}

מספק אופטימיזציית מעגלים מונעת AI דרך [פאסי Transpiler מונעי AI](/guides/ai-transpiler-passes). שרת זה מאפשר לעוזרי AI לבצע אופטימיזציה למעגלים קוונטיים באמצעות אלגוריתמי ניתוב ואופטימיזציה מתקדמים.

### שרתי קהילה {#community-servers}

שרתי ה-MCP הבאים שהוגשו על ידי הקהילה זמינים גם כן:

#### שרת Qiskit Gym MCP {#qiskit-gym-mcp-server}

מספק יכולות למידה מחיזוק לסינתזת מעגלים קוונטיים באמצעות ספריית [qiskit-gym](https://github.com/AI4quantum/qiskit-gym). שרת זה מאפשר לעוזרי AI להשתמש בטכניקות אופטימיזציה מבוססות RL לטרנספילציה וסינתזה של מעגלים קוונטיים.

## התקנת שרתי Qiskit MCP {#install-qiskit-mcp-servers}

כדי להשתמש בשרתי Qiskit MCP, אתה זקוק ל:

- Python 3.10 ואילך (מומלץ 3.11+)
- חשבון IBM Quantum וטוקן API (ראה [הגדרת חשבון IBM Cloud&reg; שלך](/guides/cloud-setup) להוראות)

התקן את כל שרתי Qiskit MCP הזמינים על ידי הרצת הפקודה הבאה מהטרמינל:

```bash
pip install qiskit-mcp-servers[all]
```

תוכל גם להתקין שרתי MCP בודדים:

```bash
pip install qiskit-mcp-server
pip install qiskit-code-assistant-mcp-server
pip install qiskit-ibm-runtime-mcp-server
pip install qiskit-docs-mcp-server
pip install qiskit-ibm-transpiler-mcp-server
pip install qiskit-gym-mcp-server
```

## הגדרת שרתי Qiskit MCP {#configure-qiskit-mcp-servers}

### הגדרת משתני סביבה {#set-environment-variables}

הגדר את משתני הסביבה הנדרשים לאימות. שרת Qiskit MCP ושרת Qiskit Docs MCP אינם דורשים אימות.

עבור שרתי Qiskit Runtime, או Qiskit IBM Transpiler MCP:

```bash
export QISKIT_IBM_TOKEN="<your_token>"
```

תוכל למצוא את טוקן ה-API שלך ב[הגדרות חשבון IBM Quantum שלך](https://quantum.cloud.ibm.com/).

### הגדרת לקוח ה-MCP {#configure-the-mcp-client}

תוכל להגדיר לקוח תואם-MCP (כגון Claude Desktop, Cursor או כלי AI אחרים) לשימוש בשרתי Qiskit MCP. ההגדרה המדויקת תלויה בלקוח שלך. לדוגמה, קובץ הגדרות לקוח MCP עשוי להיראות כך:

```json
{
  "mcpServers": {
    "qiskit": {
      "command": "qiskit-mcp-server"
    },
    "qiskit-ibm-runtime": {
      "command": "qiskit-ibm-runtime-mcp-server"
    },
    "qiskit-code-assistant": {
      "command": "qiskit-code-assistant-mcp-server"
    },
    "qiskit-docs": {
      "command": "qiskit-docs-mcp-server"
    },
    "qiskit-ibm-transpiler": {
      "command": "qiskit-ibm-transpiler-mcp-server"
    },
    "qiskit-gym": {
      "command": "qiskit-gym-mcp-server"
    }
  }
}
```

ראה בתיעוד של לקוח ה-MCP שלך לקבלת הוראות הגדרה ספציפיות.

## בדיקת שרתי ה-MCP {#test-the-mcp-servers}

תוכל לבדוק את שרתי ה-MCP באופן אינטראקטיבי באמצעות MCP Inspector:

```bash
# Test the Qiskit MCP Server
npx @modelcontextprotocol/inspector qiskit-mcp-server

# Test the Qiskit Runtime MCP Server
npx @modelcontextprotocol/inspector qiskit-ibm-runtime-mcp-server

# Test the Qiskit Docs MCP Server
npx @modelcontextprotocol/inspector qiskit-docs-mcp-server

# Test the Qiskit IBM Transpiler MCP Server
npx @modelcontextprotocol/inspector qiskit-ibm-transpiler-mcp-server

# Test the Qiskit Gym MCP Server
npx @modelcontextprotocol/inspector qiskit-gym-mcp-server
```

## השלבים הבאים {#next-steps}

<Admonition type="info" title="המלצות">

- כנס ל[מאגר GitHub של שרתי Qiskit MCP](https://github.com/Qiskit/mcp-servers) לתיעוד מפורט, כיסוי API ודוגמאות.
- למד על [פאסי Transpiler מונעי AI](/guides/ai-transpiler-passes) כדי להבין את יכולות אופטימיזציית המעגלים מונעות ה-AI.
- הגדר את [חשבון IBM Quantum שלך](/guides/cloud-setup) אם עדיין לא עשית זאת.
- חקור את [תיעוד Model Context Protocol](https://modelcontextprotocol.io/) כדי ללמוד עוד על MCP.

</Admonition>
