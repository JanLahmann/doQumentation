---
title: "מציאת חתכים אוטומטית"
sidebar_label: "מציאת חתכים אוטומטית"
notebook_path: "qiskit-addons/cutting/04_automatic_cut_finding.ipynb"
---
{/* doqumentation-source-hash: b7c579f3 */}

import TutorialFeedback from '@site/src/components/TutorialFeedback';

<OpenInLabBanner notebookPath="qiskit-addons/cutting/04_automatic_cut_finding.ipynb" />


## שלב 1: מיפוי {#step-1-map}

#### יצירת Circuit ואובזרבבלות {#create-a-circuit-and-observables}

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-addon-cutting
```

```python
import numpy as np
from qiskit.circuit.random import random_circuit
from qiskit.quantum_info import SparsePauliOp

circuit = random_circuit(7, 6, max_operands=2, seed=1242)
observable = SparsePauliOp(["ZIIIIII", "IIIZIII", "IIIIIIZ"])

circuit.draw("mpl", scale=0.8)
```

![תרשים Circuit קוונטי](/img/qiskit-addons/cutting/04-automatic-cut-finding/output_1.png)

## שלב 2: אופטימיזציה {#step-2-optimize}

#### מציאת מיקומי החתכים, בהינתן מקסימום של 4 Qubit לכל תת-Circuit. ניתן להפריד Circuit זה לשניים על ידי ביצוע חתך חוט יחיד וחיתוך `CRZGate` אחד {#find-cut-locations-given-a-maximum-of-4-qubits-per-subcircuit}

```python
from qiskit_addon_cutting.automated_cut_finding import (
    find_cuts,
    OptimizationParameters,
    DeviceConstraints,
)

# Specify settings for the cut-finding optimizer
optimization_settings = OptimizationParameters(seed=111)

# Specify the size of the QPUs available
device_constraints = DeviceConstraints(qubits_per_subcircuit=4)

cut_circuit, metadata = find_cuts(circuit, optimization_settings, device_constraints)
print(
    f'Found solution using {len(metadata["cuts"])} cuts with a sampling '
    f'overhead of {metadata["sampling_overhead"]}.\n'
    f'Lowest cost solution found: {metadata["minimum_reached"]}.'
)
for cut in metadata["cuts"]:
    print(f"{cut[0]} at circuit instruction index {cut[1]}")
cut_circuit.draw("mpl", scale=0.8, fold=-1)
```

```text
Found solution using 2 cuts with a sampling overhead of 127.06026169907257.
Lowest cost solution found: True.
Wire Cut at circuit instruction index 19
Gate Cut at circuit instruction index 28
```

![תרשים Circuit קוונטי](/img/qiskit-addons/cutting/04-automatic-cut-finding/output_2.png)

#### הוספת ancillas לחתכי חוט והרחבת האובזרבבלות כדי להתחשב ב-Qubit של ה-ancilla {#add-ancillas-for-wire-cuts-and-expand-the-observables}

```python
from qiskit_addon_cutting import cut_wires, expand_observables

qc_w_ancilla = cut_wires(cut_circuit)
observables_expanded = expand_observables(observable.paulis, circuit, qc_w_ancilla)
qc_w_ancilla.draw("mpl", scale=0.8, fold=-1)
```

![תרשים Circuit קוונטי](/img/qiskit-addons/cutting/04-automatic-cut-finding/output_3.png)

#### חלוקת ה-Circuit והאובזרבבלות לתת-Circuits ותת-אובזרבבלות. חישוב עומס הדגימה הנובע מחיתוך ה-Gate-ים וחוטים אלה. {#partition-the-circuit-and-observables}

```python
from qiskit_addon_cutting import partition_problem

partitioned_problem = partition_problem(
    circuit=qc_w_ancilla, observables=observables_expanded
)
subcircuits = partitioned_problem.subcircuits
subobservables = partitioned_problem.subobservables
print(
    f"Sampling overhead: {np.prod([basis.overhead for basis in partitioned_problem.bases])}"
)
```

```text
Sampling overhead: 127.06026169907257
```

```python
subobservables
```

```text
{0: PauliList(['IIII', 'IZII', 'IIIZ']),
 1: PauliList(['ZIII', 'IIII', 'IIII'])}
```

```python
subcircuits[0].draw("mpl", style="iqp", scale=0.8)
```

![תרשים Circuit קוונטי](/img/qiskit-addons/cutting/04-automatic-cut-finding/output_4.png)

```python
subcircuits[1].draw("mpl", style="iqp", scale=0.8)
```

![תרשים Circuit קוונטי](/img/qiskit-addons/cutting/04-automatic-cut-finding/output_5.png)

#### יצירת הניסויים להרצה על ה-Backend. {#generate-the-experiments-to-run-on-the-backend}

```python
from qiskit_addon_cutting import generate_cutting_experiments

subexperiments, coefficients = generate_cutting_experiments(
    circuits=subcircuits, observables=subobservables, num_samples=1_000
)
print(
    f"{len(subexperiments[0]) + len(subexperiments[1])} total subexperiments to run on backend."
)
```

```text
96 total subexperiments to run on backend.
```

שלבים 3 ו-4 של [תבנית Qiskit](https://quantum.cloud.ibm.com/docs/guides/intro-to-patterns) ניתן לבצע כפי שמוצג בהדרכות הקודמות.

<TutorialFeedback />
