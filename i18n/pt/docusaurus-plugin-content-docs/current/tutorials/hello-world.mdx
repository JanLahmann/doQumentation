---
title: "Hello World: Seu Primeiro Circuito Quântico"
sidebar_label: "Hello World: Seu Primeiro Circuito Quântico"
notebook_path: "hello-world.ipynb"
---
{/* doqumentation-source-hash: ad455993 */}

<OpenInLabBanner notebookPath="hello-world.ipynb" description="Este tutorial foi criado para o doQumentation." />

Construa um [estado de Bell](https://en.wikipedia.org/wiki/Bell_state) (dois qubits emaranhados) e execute-o de três maneiras:

1. **Simulação ideal** — resultados perfeitos, não precisa de conta
2. **Simulação com ruído** — simula um dispositivo real, não precisa de conta
3. **Hardware quântico real** — requer uma conta IBM Quantum gratuita (passos de configuração abaixo)
## Construa o circuito {#build-the-circuit}

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit qiskit-ibm-runtime
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

qc.draw(output="mpl")
```

## Opção 1: Simulação ideal (não precisa de conta) {#option-1-ideal-simulation-no-account-needed}

Usa `StatevectorSampler` — um simulador local com resultados perfeitos, sem ruído.

```python
from qiskit.primitives import StatevectorSampler

result = StatevectorSampler().run([qc], shots=1024).result()
counts = result[0].data.meas.get_counts()
counts
```

```python
from qiskit.visualization import plot_histogram
plot_histogram(counts)
```

## Opção 2: Simulação com ruído (não precisa de conta) {#option-2-noisy-simulation-no-account-needed}

Usa `FakeManilaV2` — um simulador local que imita um dispositivo quântico IBM real, incluindo suas características de ruído. O circuito deve primeiro ser transpilado (adaptado) para o conjunto de portas e conectividade de qubits do dispositivo.

```python
from qiskit_ibm_runtime import SamplerV2
from qiskit_ibm_runtime.fake_provider import FakeManilaV2
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

backend = FakeManilaV2()
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(qc)

result = SamplerV2(mode=backend).run([isa_qc], shots=1024).result()
counts = result[0].data.meas.get_counts()
counts
```

```python
plot_histogram(counts)
```

## Opção 3: Hardware quântico real {#option-3-real-quantum-hardware}

Requer uma conta IBM Quantum gratuita. Para criar uma:

1. Registre-se em [quantum.cloud.ibm.com/registration](https://quantum.cloud.ibm.com/registration) — não é necessário cartão de crédito nos primeiros 30 dias
2. Faça login em [quantum.cloud.ibm.com](https://quantum.cloud.ibm.com) e selecione a região **us-east** (obrigatório para o plano Open gratuito)
3. Crie uma instância (plano Open gratuito) em [Instances](https://quantum.cloud.ibm.com/instances) se ainda não tiver uma
4. Crie uma chave de API em [quantum.cloud.ibm.com](https://quantum.cloud.ibm.com) (ou em [cloud.ibm.com/iam/apikeys](https://cloud.ibm.com/iam/apikeys))
5. Copie seu **CRN** (Cloud Resource Name) da sua página de [Instances](https://quantum.cloud.ibm.com/instances)

Em seguida, se você ainda não salvou suas credenciais nesta sessão do Binder, execute a célula abaixo. Substitua `<your-api-key>` pela chave de API do passo 4 e `<your-crn>` pelo CRN do passo 5.

```python
from qiskit_ibm_runtime import QiskitRuntimeService

QiskitRuntimeService.save_account(
    channel="ibm_quantum_platform",
    token="<your-api-key>",
    instance="<your-crn>",
    set_as_default=True,
    overwrite=True,
)
```

**Observação:** Jobs em hardware real podem levar algum tempo dependendo do tempo de fila. Se a célula ainda estiver em execução, você pode verificar o status do seu job e ver os resultados em [quantum.cloud.ibm.com/workloads](https://quantum.cloud.ibm.com/workloads?user=me).

```python
from qiskit_ibm_runtime import QiskitRuntimeService, SamplerV2
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)
print(f"Running on {backend.name}")

pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(qc)

result = SamplerV2(mode=backend).run([isa_qc], shots=1024).result()
counts = result[0].data.meas.get_counts()
counts
```

```python
plot_histogram(counts)
```

## O que vem a seguir? {#what-next}

- **[Tutorials](https://doqumentation.org/tutorials)** — guias passo a passo sobre algoritmos, mitigação de erros, transpilação e muito mais
- **[Guides](https://doqumentation.org/guides)** — material de referência sobre execução de circuitos, primitivas e o Qiskit Runtime
- **[Courses](https://doqumentation.org/learning/courses/basics-of-quantum-information)** — trilhas de aprendizado estruturadas desde os fundamentos da computação quântica até a computação em escala utilitária
- **[Modules](https://doqumentation.org/learning/modules/computer-science)** — módulos conceituais aprofundados em ciência da computação e mecânica quântica
