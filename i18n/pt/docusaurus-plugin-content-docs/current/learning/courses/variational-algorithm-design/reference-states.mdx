---
title: "Estados de referência"
sidebar_label: "Estados de referência"
description: "Esta lição descreve os estados de referência, a inicialização de um estado quântico antes de quaisquer parâmetros variacionais."
notebook_path: "learning/courses/variational-algorithm-design/reference-states.ipynb"
---
{/* source-hash: 2e18844a */}

<OpenInLabBanner notebookPath="learning/courses/variational-algorithm-design/reference-states.ipynb" />



Nesta lição, vamos explorar como podemos inicializar nosso sistema com um <DefinitionTooltip definition="O ponto de partida inicial e fixo para nosso problema. Normalmente, isso se refere a |0>, mas podemos configurá-lo para uma variedade de estados que codificam adequadamente nosso problema.">estado de referência</DefinitionTooltip> para ajudar nosso algoritmo variacional a convergir mais rapidamente. Primeiro, aprenderemos a construir um estado de referência manualmente e, em seguida, exploraremos várias opções padrão que podem ser usadas em um algoritmo variacional.

![Diagram of options for references states including default, application-specific, and quantum.](/learning/images/courses/variational-algorithm-design/reference-states/reference-workflow.svg)
## Estado padrão {#default-state}

Um *estado de referência* refere-se ao ponto de partida inicial e fixo para nosso problema. Para preparar um estado de referência, precisamos aplicar o unitário não parametrizado apropriado $U_R$ no início de nosso circuito quântico, de modo que $|\rho\rangle = U_R |0\rangle$. Se você tiver uma estimativa fundamentada ou um ponto de dados de uma solução ótima existente, o algoritmo variacional provavelmente convergirá mais rapidamente se você o usar como ponto de partida.

O estado de referência mais simples possível é o estado padrão, onde usamos o estado inicial de um circuito quântico de $n$ qubits: $|0\rangle^{\otimes n}$. Para o estado padrão, nosso operador unitário é $U_R \equiv I$. Devido à sua simplicidade, o estado padrão é um estado de referência válido utilizado em muitos cenários.
## Estado de referência clássico {#classical-reference-state}

Suponha que você tenha um sistema de três qubits e queira começar no estado $|001\rangle$ em vez do estado padrão $|000\rangle$. Este é um exemplo de um estado de referência puramente clássico, e para construí-lo, você simplesmente precisa aplicar uma [porta X](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.XGate) ao qubit $0$ (seguindo a ordenação de qubits do Qiskit), pois $|001\rangle = X_0 |000\rangle$.

Neste caso, nosso operador unitário é $U_R \equiv X_0$, o que resulta no estado de referência $|\rho\rangle \equiv |001\rangle$.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3)
qc.x(0)

qc.draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/variational-algorithm-design/reference-states/extracted-outputs/cfd71225-1f9f-4a34-b825-ae19813956a8-0.avif)

## Estado de referência quântico {#quantum-reference-state}

Suponha que você queira começar com um estado mais complexo que envolva superposição e/ou entrelaçamento, como $\frac{1}{\sqrt{2}}(|100\rangle+|111\rangle)$.

Para obter esse estado a partir de $|000\rangle$, uma abordagem é usar uma [porta Hadamard](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.HGate) no qubit $0$ ($H_0$), uma [porta CNOT (CX)](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.CXGate) com o qubit $0$ como qubit de controle e o qubit $1$ como qubit alvo ($CNOT_{01}$), e por fim uma porta $X$ aplicada ao qubit $2$ ($X_2$).

Neste cenário, nosso operador unitário é $U_{R} \equiv X_2CNOT_{01}H_0|000\rangle$, e nosso estado de referência é $|\rho\rangle \equiv \frac{1}{\sqrt{2}}(|100\rangle+|111\rangle)$.

```python
qc = QuantumCircuit(3)
qc.h(0)
qc.cx(0, 1)
qc.x(2)

qc.draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/variational-algorithm-design/reference-states/extracted-outputs/77297158-60d7-469a-8a82-27d38a52d795-0.avif)

## Construindo estados de referência usando circuitos modelo {#constructing-reference-states-using-template-circuits}

Também podemos usar diversos circuitos modelo, como o [`TwoLocal`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.TwoLocal), que permite expressar múltiplos parâmetros ajustáveis e entrelaçamentos com facilidade. Abordaremos esses circuitos modelo com mais detalhes na próxima lição, mas podemos usá-los para nossos estados de referência *se* vincularmos os parâmetros:

```python
from qiskit.circuit.library import TwoLocal
from math import pi

reference_circuit = TwoLocal(2, "rx", "cz", entanglement="linear", reps=1)
theta_list = [pi / 2, pi / 3, pi / 3, pi / 2]

reference_circuit = reference_circuit.assign_parameters(theta_list)

reference_circuit.decompose().draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/variational-algorithm-design/reference-states/extracted-outputs/8f3948ad-4caf-4ee6-8a72-bb91ed1bfeae-0.avif)

## Estados de referência específicos por aplicação {#application-specific-reference-states}
### Aprendizado de máquina quântico {#quantum-machine-learning}

No contexto de um classificador quântico variacional (VQC), os dados de treinamento são codificados em um estado quântico por meio de um circuito parametrizado conhecido como *mapa de características* (*feature map*), onde cada valor de parâmetro representa um ponto de dados do conjunto de treinamento. O `zz_feature_map` é um tipo de circuito parametrizado que pode ser utilizado para passar nossos pontos de dados ($x$) para este mapa de características.

```python
from qiskit.circuit.library import zz_feature_map

data = [0.1, 0.2]

zz_feature_map_reference = zz_feature_map(feature_dimension=2, reps=2)
zz_feature_map_reference = zz_feature_map_reference.assign_parameters(data)
zz_feature_map_reference.decompose().draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/variational-algorithm-design/reference-states/extracted-outputs/4bfd5a76-0db3-499d-bda1-0cb95c3f23c2-0.avif)

## Resumo {#summary}

Com esta lição, você aprendeu a inicializar seu sistema usando:

- Estado de referência padrão
- Estados de referência clássicos
- Estados de referência quânticos
- Estados de referência específicos por aplicação

Nossa carga de trabalho variacional de alto nível é a seguinte:

![A circuit diagram of a unitary operator preparing a reference state.](/learning/images/courses/variational-algorithm-design/reference-states/reference-circuit.svg)

Enquanto os estados de referência são pontos de partida fixos e iniciais, podemos usar uma *forma variacional* para definir um *ansatz* que represente uma coleção de estados parametrizados para que nosso algoritmo variacional explore.
