---
title: "Medir qubits"
sidebar_label: "Medir qubits"
description: "Aprenda como medir qubits, incluindo restrições sobre onde as medições podem ser usadas."
notebook_path: "guides/measure-qubits.ipynb"
---

{/* doqumentation-source-hash: 421a17fa */}

<OpenInLabBanner notebookPath="guides/measure-qubits.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Package versions">

O código nesta página foi desenvolvido usando os seguintes requisitos.
Recomendamos usar estas versões ou mais recentes.

```
qiskit[all]~=2.4.0
```
</AccordionItem>
</Accordion>
Para obter informações sobre o estado de um qubit, você pode _medi-lo_ em um [bit clássico](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Clbit). No Qiskit, as medições são realizadas na base computacional, ou seja, a base de Pauli-$Z$ de um único qubit. Portanto, uma medição retorna 0 ou 1, dependendo da sobreposição com os autoestados de Pauli-$Z$ $|0\rangle$ e $|1\rangle$:

$$
|q\rangle \xrightarrow{measure}\begin{cases}
      0 (\text{outcome}+1), \text{with probability } p_0=|\langle q|0\rangle|^{2}\text{,} \\
      1 (\text{outcome}-1), \text{with probability } p_1=|\langle q|1\rangle|^{2}\text{.}
    \end{cases}
$$

## Aplicar uma medição a um circuito {#apply-a-measurement-to-a-circuit}

Há várias maneiras de aplicar medições a um circuito:

### Método `QuantumCircuit.measure` {#quantumcircuitmeasure-method}

Use o método [`measure`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure) para medir um [`QuantumCircuit`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#quantumcircuit-class).

Exemplos:

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(5, 5)
qc.x(0)
qc.x(1)
qc.x(4)
qc.measure(
    range(5), range(5)
)  #  Measures all qubits into the corresponding clbit.
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c636c20>
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure(1, 0)  # Measure qubit 1 into the classical bit 0.
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c636830>
```

### Classe `Measure` {#measure-class}

A classe [Measure](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure) do Qiskit mede os qubits especificados.

```python
from qiskit.circuit import Measure

qc = QuantumCircuit(3, 1)
qc.x([0, 1])
qc.append(Measure(), [0], [0])  # measure qubit 0 into clbit 0
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c6369e0>
```

### Método `QuantumCircuit.measure_all` {#quantumcircuitmeasure-all-method}

Para medir todos os qubits nos bits clássicos correspondentes, use o método [`measure_all`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_all). Por padrão, este método adiciona novos bits clássicos em um `ClassicalRegister` para armazenar essas medições.

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure_all()  # Measure all qubits.
```

### Método `QuantumCircuit.measure_active` {#quantumcircuitmeasure-active-method}

Para medir todos os qubits que não estão ociosos, use o método [`measure_active`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_active). Este método cria um novo `ClassicalRegister` com tamanho igual ao número de qubits não ociosos sendo medidos.

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure_active()  # Measure qubits that are not idle, that is, qubits 0 and 2.
```

## Próximos passos {#next-steps}

<Admonition type="tip" title="Recomendações">
- Classe [`Measure`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure)
- Método [`measure_all`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_all)
- Método [`measure_active`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_active)
- Método [`random_circuit`](https://docs.quantum.ibm.com/api/qiskit/circuit_random#qiskit.circuit.random.random_circuit)
- [Medições no meio do circuito](/guides/execute-dynamic-circuits#midcircuit) (Disponível apenas ao usar o Qiskit Runtime.)
</Admonition>
