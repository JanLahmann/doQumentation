---
title: "Construir circuitos"
sidebar_label: "Construir circuitos"
description: "Como construir e visualizar circuitos quânticos no Qiskit."
notebook_path: "guides/construct-circuits.ipynb"
---
{/* doqumentation-source-hash: 18ad7218 */}

<OpenInLabBanner notebookPath="guides/construct-circuits.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Versões dos pacotes">

O código desta página foi desenvolvido usando os seguintes requisitos.
Recomendamos usar essas versões ou mais recentes.

```
qiskit[all]~=2.4.0
```
</AccordionItem>
</Accordion>
Esta página examina mais de perto a classe [`QuantumCircuit`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit) do SDK Qiskit, incluindo alguns métodos mais avançados que você pode usar para criar circuitos quânticos.
## O que é um circuito quântico? {#what-is-a-quantum-circuit}

Um circuito quântico simples é uma coleção de qubits e uma lista de instruções que atuam sobre esses qubits. Para demonstrar, a célula a seguir cria um novo circuito com dois novos qubits e, em seguida, exibe o atributo [`qubits`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#qubits) do circuito, que é uma lista de [`qubits`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Qubit) em ordem do bit menos significativo $q_0$ ao bit mais significativo $q_n$.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(2)
qc.qubits
```

```text
[<Qubit register=(2, "q"), index=0>, <Qubit register=(2, "q"), index=1>]
```

Múltiplos objetos `QuantumRegister` e `ClassicalRegister` podem ser combinados para criar um circuito. Todo [`QuantumRegister`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.QuantumRegister) e [`ClassicalRegister`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.ClassicalRegister) também pode ter um nome.

```python
from qiskit.circuit import QuantumRegister, ClassicalRegister

qr1 = QuantumRegister(2, "qreg1")  # Create a QuantumRegister with 2 qubits
qr2 = QuantumRegister(1, "qreg2")  # Create a QuantumRegister with 1 qubit
cr1 = ClassicalRegister(3, "creg1")  # Create a ClassicalRegister with 3 cbits

combined_circ = QuantumCircuit(
    qr1, qr2, cr1
)  # Create a quantum circuit with 2 QuantumRegisters and 1 ClassicalRegister
combined_circ.qubits
```

```text
[<Qubit register=(2, "qreg1"), index=0>,
 <Qubit register=(2, "qreg1"), index=1>,
 <Qubit register=(1, "qreg2"), index=0>]
```

Você pode encontrar o índice e o registrador de um qubit usando o método [`find_bit`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#qiskit.circuit.QuantumCircuit.find_bit) do circuito e seus atributos.

```python
desired_qubit = qr2[0]  # Qubit 0 of register 'qreg2'

print("Index:", combined_circ.find_bit(desired_qubit).index)
print("Register:", combined_circ.find_bit(desired_qubit).registers)
```

```text
Index: 2
Register: [(QuantumRegister(1, 'qreg2'), 0)]
```

Adicionar uma instrução ao circuito acrescenta a instrução ao atributo [`data`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#data) do circuito. A saída da célula a seguir mostra que `data` é uma lista de objetos [`CircuitInstruction`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.CircuitInstruction), cada um dos quais possui um atributo `operation` e um atributo `qubits`.

```python
qc.x(0)  # Add X-gate to qubit 0
qc.data
```

```text
[CircuitInstruction(operation=Instruction(name='x', num_qubits=1, num_clbits=0, params=[]), qubits=(<Qubit register=(2, "q"), index=0>,), clbits=())]
```

A maneira mais fácil de visualizar essas informações é por meio do método [`draw`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#draw), que retorna uma visualização de um circuito. Veja [Visualizar circuitos](/guides/visualize-circuits) para diferentes formas de exibir circuitos quânticos.

```python
qc.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/43a57258-3e33-4071-8a48-2bf127c8a5be-0.svg)

Os objetos de instrução de circuito podem conter circuitos de "definição" que descrevem a instrução em termos de instruções mais fundamentais. Por exemplo, o [X-gate](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.XGate) é definido como um caso específico do [U3-gate](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.U3Gate), uma porta de qubit único mais geral.

```python
# Draw definition circuit of 0th instruction in `qc`
qc.data[0].operation.definition.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/653e2427-e301-4d2f-84de-1959185ace8e-0.svg)

Instruções e circuitos são semelhantes no sentido de que ambos descrevem operações sobre bits e qubits, mas têm propósitos diferentes:

- As instruções são tratadas como fixas, e seus métodos geralmente retornam novas instruções (sem modificar o objeto original).
- Os circuitos são projetados para serem construídos ao longo de muitas linhas de código, e os métodos de [`QuantumCircuit`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit) frequentemente modificam o objeto existente.
### O que é a profundidade de um circuito? {#what-is-circuit-depth}

A [depth()](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#qiskit.circuit.QuantumCircuit.depth) de um circuito quântico é uma medida do número de "camadas" de portas quânticas, executadas em paralelo, necessárias para completar o cálculo definido pelo circuito. Como as portas quânticas levam tempo para serem implementadas, a profundidade de um circuito corresponde aproximadamente ao tempo que o computador quântico leva para executar o circuito. Portanto, a profundidade de um circuito é uma quantidade importante usada para medir se um circuito quântico pode ser executado em um dispositivo.

O restante desta página ilustra como manipular circuitos quânticos.
## Construir circuitos {#build-circuits}

Métodos como [`QuantumCircuit.h`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#h) e [`QuantumCircuit.cx`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#cx) adicionam instruções específicas aos circuitos. Para adicionar instruções a um circuito de forma mais geral, use o método [`append`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#append). Esse método recebe uma instrução e uma lista de qubits aos quais aplicar a instrução. Veja a [documentação da API da Biblioteca de Circuitos](https://docs.quantum.ibm.com/api/qiskit/circuit_library) para uma lista de instruções suportadas.

```python
from qiskit.circuit.library import HGate

qc = QuantumCircuit(1)
qc.append(
    HGate(),  # New HGate instruction
    [0],  # Apply to qubit 0
)
qc.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/66813cae-9841-47ea-96b7-8fd7b82e9759-0.svg)

Para combinar dois circuitos, use o método [`compose`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#compose). Esse método aceita outro [`QuantumCircuit`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit) e uma lista opcional de mapeamentos de qubits.

<Admonition type="note">
    O método [`compose`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#compose) retorna um novo circuito e **não** modifica nenhum dos circuitos sobre os quais atua. Para modificar o circuito no qual você está chamando o método [`compose`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#compose), use o argumento `inplace=True`.
</Admonition>

```python
qc_a = QuantumCircuit(4)
qc_a.x(0)

qc_b = QuantumCircuit(2, name="qc_b")
qc_b.y(0)
qc_b.z(1)

# compose qubits (0, 1) of qc_a to qubits (1, 3) of qc_b respectively
combined = qc_a.compose(qc_b, qubits=[1, 3])
combined.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/29152dfa-2275-4bc4-aadb-82185b9e0e86-0.svg)

Você também pode querer compilar circuitos em instruções para manter seus circuitos organizados. É possível converter um circuito em uma instrução usando o método [`to_instruction`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#to_instruction), e então acrescentar essa instrução a outro circuito como faria com qualquer outra instrução. O circuito desenhado na célula a seguir é funcionalmente equivalente ao circuito desenhado na célula anterior.

```python
inst = qc_b.to_instruction()
qc_a.append(inst, [1, 3])
qc_a.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/81b682dd-45cb-4492-809e-d9e8ebbf5600-0.svg)

Se o seu circuito for unitário, você pode convertê-lo em um [`Gate`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.Gate) usando o método [`to_gate`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#to_gate). Os objetos [`Gate`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.Gate) são tipos específicos de instruções que possuem alguns recursos extras, como o método [`control`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.Gate#control), que adiciona um controle quântico.

```python
gate = qc_b.to_gate().control()
qc_a.append(gate, [0, 1, 3])
qc_a.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/ed362e64-d6a4-4dfd-a5cf-5e6bdc7a81b5-0.svg)

Para ver o que está acontecendo, você pode usar o método [`decompose`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#decompose) para expandir cada instrução em sua definição.

<Admonition type="note">
    O método [`decompose`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#decompose) retorna um novo circuito e **não** modifica o circuito sobre o qual atua.
</Admonition>

```python
qc_a.decompose().draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/3c0633db-929b-4428-a888-7a3d493bd6dd-0.svg)

<span id="measure-qubits"></span>
## Medir qubits {#measure-qubits}

As medições são usadas para amostrar os estados de qubits individuais e transferir os resultados para um registrador clássico. Note que, se você estiver submetendo circuitos a um primitivo [Sampler](/guides/get-started-with-sampler), as medições são obrigatórias. Porém, circuitos submetidos a um primitivo [Estimator](/guides/get-started-with-estimator) não devem conter medições.

Os qubits podem ser medidos usando três métodos: [`measure`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#qiskit.circuit.QuantumCircuit.measure), [`measure_all`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_all) e [`measure_active`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_active). Para aprender a visualizar resultados de medições, veja a página [Visualizar resultados](/guides/visualize-results).

1. `QuantumCircuit.measure` : mede cada qubit do primeiro argumento no bit clássico fornecido como segundo argumento. Esse método permite controle total sobre onde o resultado da medição é armazenado.

2. `QuantumCircuit.measure_all` : não recebe argumento e pode ser usado para circuitos quânticos sem bits clássicos pré-definidos. Cria fios clássicos e armazena os resultados das medições em ordem. Por exemplo, a medição do qubit $q_i$ é armazenada no cbit $meas_i$). Também adiciona uma barreira antes da medição.

3. `QuantumCircuit.measure_active` : semelhante a `measure_all`, mas mede apenas os qubits que possuem operações.

```python
qc1 = QuantumCircuit(2, 2)
qc1.measure(0, 1)
qc1.draw("mpl", cregbundle=False)
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/0cdb2273-0.svg)

```python
qc2 = QuantumCircuit(2)
qc2.measure_all()
qc2.draw("mpl", cregbundle=False)
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/6f33698c-0.svg)

```python
qc3 = QuantumCircuit(2)
qc3.x(1)
qc3.measure_active()
qc3.draw("mpl", cregbundle=False)
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/ca3f225f-0.svg)

## Circuitos parametrizados {#parameterized-circuits}

Muitos algoritmos quânticos de curto prazo envolvem a execução de muitas variações de um circuito quântico. Como construir e otimizar circuitos grandes pode ser computacionalmente caro, o Qiskit suporta circuitos **parametrizados**. Esses circuitos possuem parâmetros indefinidos, e seus valores não precisam ser definidos até pouco antes da execução do circuito. Isso permite que você mova a construção e otimização do circuito para fora do laço principal do programa. A célula a seguir cria e exibe um circuito parametrizado.

```python
from qiskit.transpiler import generate_preset_pass_manager
from qiskit.circuit import Parameter

angle = Parameter("angle")  # undefined number

# Create and optimize circuit once
qc = QuantumCircuit(1)
qc.rx(angle, 0)
qc = generate_preset_pass_manager(
    optimization_level=3, basis_gates=["u", "cx"]
).run(qc)

qc.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/a580552c-d585-4047-99f0-32aafd06e4f3-0.svg)

A célula a seguir cria muitas variações desse circuito e exibe uma das variações.

```python
circuits = []
for value in range(100):
    circuits.append(qc.assign_parameters({angle: value}))

circuits[0].draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/construct-circuits/extracted-outputs/85af6231-921a-4130-99d3-f6998f761df8-0.svg)

Você pode encontrar uma lista dos parâmetros indefinidos de um circuito em seu atributo `parameters`.

```python
qc.parameters
```

```text
ParameterView([Parameter(angle)])
```

### Alterar o nome de um parâmetro {#change-a-parameters-name}

Por padrão, os nomes dos parâmetros de um circuito parametrizado são prefixados por `x` — por exemplo, `x[0]`. Você pode alterar os nomes após eles serem definidos, conforme mostrado no exemplo a seguir.

```python
from qiskit.circuit.library import z_feature_map
from qiskit.circuit import ParameterVector

# Define a parameterized circuit with default names
# For example, x[0]
circuit = z_feature_map(2)

# Set new parameter names
# They will now be prefixed by `hi` instead
# For example, hi[0]
training_params = ParameterVector("hi", 2)

# Assign parameter names to the quantum circuit
circuit = circuit.assign_parameters(parameters=training_params)
```

## Próximos passos {#next-steps}

<Admonition type="tip" title="Recomendações">
  -  Para aprender sobre algoritmos quânticos de curto prazo, faça o curso [Variational algorithm design](/learning/courses/variational-algorithm-design).
  -  Veja um exemplo de circuitos sendo usados no tutorial [Algoritmo de Grover](/tutorials/grovers-algorithm).
  -  Trabalhe com circuitos simples usando o [IBM Quantum Composer](/guides/composer).
</Admonition>
