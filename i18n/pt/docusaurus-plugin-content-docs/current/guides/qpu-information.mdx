---
description: 'Liste e filtre backends disponíveis e encontre detalhes sobre a QPU, como configuração e dados de calibração.'
notebook_path: guides/qpu-information.ipynb
sidebar_label: 'Ver detalhes do backend'
title: 'Ver detalhes do backend'
---

{/* doqumentation-source-hash: f532978f */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<OpenInLabBanner notebookPath="guides/qpu-information.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Package versions">

The code on this page was developed using the following requirements.
We recommend using these versions or newer.

```
qiskit-ibm-runtime~=0.47.0
```
</AccordionItem>
</Accordion>
Esta página explica como encontrar informações sobre seus backends disponíveis.

## Listar ou filtrar backends {#list-or-filter-backends}

<span id="available"></span>

### Listar os backends disponíveis para você {#list-backends-available-to-you}

Você pode usar o Qiskit ou o IBM Quantum Platform para visualizar uma lista de backends disponíveis ou buscar um backend específico.

<Admonition type="note" title="Nomes de QPUs">
QPUs hospedadas no IBM Cloud&reg; têm nomes que começam com `ibm_*`. Todas as QPUs recebem o nome de uma cidade — por exemplo, `ibm_kingston`. Esse nome não indica onde a QPU real está hospedada.
</Admonition>

<Tabs>
    <TabItem value="Qiskit-available" label="View backends with Qiskit">
        Use the [`QiskitRuntimeService.backends()`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/qiskit-runtime-service#backends) method, as shown in the next code block. This method returns a list of [`IBMBackend`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/ibm-backend) instances.

        To run the following code, be sure you have already authenticated to the service. See [Set up your IBM Cloud account](/guides/cloud-setup) for more details.

        

        To search for a specific backend, use the [`QiskitRuntimeService.backend()`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/qiskit-runtime-service#backend) method (note that this is singular: *backend*), which takes the name of the backend as the input parameter and returns an [`IBMBackend`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/ibm-backend) instance representing that particular backend:

        
    </TabItem>
    <TabItem value="IQP-available" label="View backends on IBM Quantum Platform">
        Para visualizar os backends aos quais você tem acesso, navegue até a lista de backends na [página Recursos de computação](https://quantum.cloud.ibm.com/computers) (note que sua região selecionada pode afetar as QPUs listadas). Clique no ícone de filtro e escolha "All my instances" ou um nome de instância específico para ver as QPUs disponíveis.
    </TabItem>
</Tabs>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit-ibm-runtime
```

```python
# Initialize your account
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()

service.backends()
```

```text
[<IBMBackend('ibm_pittsburgh')>,
 <IBMBackend('ibm_boston')>,
 <IBMBackend('ibm_fez')>,
 <IBMBackend('ibm_miami')>,
 <IBMBackend('ibm_marrakesh')>,
 <IBMBackend('ibm_kingston')>]
```

```python
service.backend("ibm_fez")
```

```text
<IBMBackend('ibm_fez')>
```

```python
# Optionally pass in an instance, region, or both, to
# further filter the backends.
service = QiskitRuntimeService()

service.backends(simulator=False, operational=True, min_num_qubits=100)
```

```text
[<IBMBackend('ibm_pittsburgh')>,
 <IBMBackend('ibm_boston')>,
 <IBMBackend('ibm_fez')>,
 <IBMBackend('ibm_miami')>,
 <IBMBackend('ibm_marrakesh')>,
 <IBMBackend('ibm_kingston')>]
```

<Admonition type="note">
   Se você estiver conectado a uma instância ou região específica, ou se tiver inicializado o serviço com uma instância ou região específica usando `QiskitRuntimeService()`, apenas os backends disponíveis para você nessa instância ou região serão retornados.
</Admonition>

### Filtrar backends {#filter-backends}

<Tabs>
    <TabItem value="Qiskit-filter" label="Filter by properties with Qiskit">
        Você pode filtrar os backends disponíveis pela sua [configuração](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/models-backend-configuration) ou [status](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/models-backend-status). Para filtros mais gerais, defina o argumento `filters` como uma função que aceita um objeto de backend e retorna `True` se ele atender aos seus critérios. Consulte a [documentação da API](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/qiskit-runtime-service#backends) para mais detalhes.

        O código a seguir retorna apenas os backends que atendem a estes critérios e estão disponíveis para você _na sua instância atualmente selecionada_:

        *   São dispositivos quânticos reais (`simulator=False`)

        *   Estão atualmente operacionais (`operational=True`)

        *   Têm pelo menos cem qubits (`min_num_qubits=100`)


        

        Um método similar é [`QiskitRuntimeService.least_busy()`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/qiskit-runtime-service#least_busy), que aceita os mesmos filtros que `backends()`, mas retorna o backend que corresponde aos filtros e tem o menor número de jobs pendentes na fila:

        

    </TabItem>
    <TabItem value="IQP-filter" label="Filter by properties on IBM Quantum Platform">
        Para visualizar os backends aos quais você tem acesso, veja a tabela na [página Recursos de computação](https://quantum.cloud.ibm.com/computers). Clique no ícone de filtro para ver as opções de filtragem. Você pode filtrar por tipo de processador e status. Note também que você pode ordenar a tabela por qualquer coluna passando o cursor sobre o título de uma coluna e clicando nas setas que aparecem.
    </TabItem>
</Tabs>

```python
service.least_busy(operational=True, min_num_qubits=100)
```

```text
<IBMBackend('ibm_fez')>
```

## Informações estáticas do backend {#static-backend-information}

Algumas informações sobre um backend não mudam regularmente, como seu nome, [versão](#qpu-versioning), o número de qubits que ele possui, seu tipo de processador (o nome da família de pássaros, que indica a [topologia](#coupling-map) e a contagem aproximada de qubits) e os tipos de recursos que ele suporta. Essas informações estão disponíveis como atributos do objeto `backend`. Para uma lista completa dos atributos, consulte a [documentação da API `IBMBackend`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/ibm-backend).  Encontre mais informações sobre versionamento na seção [Versionamento de QPU](#qpu-versioning) abaixo.

A região de um backend (a localização do data center onde seus dados e experimentos serão hospedados e processados) está listada no cartão de informações detalhadas no [Compute resources](https://quantum.cloud.ibm.com/computers) no IBM Quantum Platform.

<Tabs>
  <TabItem value="Qiskit-backend" label="View backend information with Qiskit">

    

  </TabItem>
  <TabItem value="IQP-backend" label="View backend information on IBM Quantum Platform">

    Navegue até a página [Recursos de computação](https://quantum.cloud.ibm.com/computers) para ver uma tabela com todas as QPUs e clique no nome de qualquer QPU para abrir seu cartão de informações detalhadas. Encontre as informações do backend na seção Detalhes do cartão.

  </TabItem>
</Tabs>

```python
backend = service.backend("ibm_fez")

print(
    f"Name: {backend.name}\n"
    f"Version: {backend.backend_version}\n"
    f"No. of programmable qubits: {backend.num_qubits}\n"
    f"Processor type: {backend.processor_type}\n"
)
```

```text
Name: ibm_fez
Version: 1.3.37
No. of programmable qubits: 156
Processor type: {'family': 'Heron', 'revision': '2'}
```

### Versionamento de QPU {#qpu-versioning}

Cada QPU possui um número de versão no formato X.Y.Z (major.minor.revision). Um circuito compilado para um determinado número de versão tem execução garantida nessa QPU. Se o número de revisão mudar, o circuito continuará funcionando. Se o número major ou minor mudar, a execução do circuito não é garantida, embora possa ocorrer.

O número de versão de revisão será incrementado para correções que não quebrem o circuito compilado existente.

O exemplo de código apresentado anteriormente [nesta seção](#static-backend-information) demonstra como encontrar a versão de um backend. No IBM Quantum Platform, você pode ver a versão no cartão de informações detalhadas de uma QPU (clique no nome da QPU na tabela [Recursos de computação](https://quantum.cloud.ibm.com/computers) para abrir o cartão).

As condições sob as quais um número de versão ou revisão pode mudar estão listadas na tabela a seguir.

| Versão major | Versão minor | Versão de revisão |
| --- | --- | --- |
|Substituição de amostras| Ciclos de aquecimento/resfriamento | Atualizações de software da QPU |
|Grandes mudanças na eletrônica de controle| Substituição de alguma eletrônica, caso o substituto afete apreciavelmente a operação| Calibrações manuais para melhorar as fidelidades |
|Mudança de localização da QPU, caso resulte em alterações significativas de comportamento| Remoção de uma porta por algum período de tempo devido a problemas de calibração, quando as correções não podem ser feitas facilmente em software| Pequenas mudanças na eletrônica que não afetam a operação |
| |Mudança na direção de uma porta CNOT | |
<span id="native-gates-ops"></span>

### Instruções suportadas {#supported-instructions}

Cada [família de processador](/guides/processor-types) suporta nativamente um conjunto limitado de instruções. Esse conjunto inclui portas de um e dois qubits, bem como operações não unitárias como measure e reset. Toda porta no circuito deve ser traduzida (pelo transpilador) para os elementos do conjunto de instruções suportadas por uma QPU antes de poder ser executada nela.

Você pode visualizar os conjuntos de instruções suportadas por uma QPU com o Qiskit. A página Recursos de computação do IBM Quantum Platform lista apenas as portas unitárias suportadas (portas de base) de uma QPU.

  <Tabs>
  <TabItem value="Qiskit-native" label="Find supported instructions with Qiskit">

    

    </TabItem>
    <TabItem value="IQP-basis" label="Find basis gates on IBM Quantum Platform">

    Navegue até a página [Recursos de computação](https://quantum.cloud.ibm.com/computers) para ver uma tabela com todas as QPUs e clique no nome de qualquer QPU para abrir seu cartão de informações detalhadas. As portas de base dessa QPU estão listadas na seção Detalhes.
    </TabItem>
</Tabs>

<span id="native-gates"></span>

### Tabela de instruções suportadas {#table-of-supported-instructions}

| Categoria de operação |  Nome |
| :--- | :--- |
| Portas de qubit único | [`RZ`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.RZGate), [`SX`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.SXGate), [`X`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.XGate), [`ID`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.IGate), [`delay`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Delay) |
| Portas de dois qubits | [`CZ`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.CZGate), [`ECR`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.ECRGate)|
| Portas fracionárias | [`RX`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.RXGate) (qubit único), [`RZZ`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.RZZGate) (dois qubits) |
| Instruções não unitárias | [`measure`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure), [`reset`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Reset), [`measure_*`](/guides/execute-dynamic-circuits#midcircuit), [`delay`](https://docs.quantum.ibm.com/api/qiskit/circuit#delay) |
| Fluxo de controle | [`if_else`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.IfElseOp) (feedforward clássico) |

```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
backend = service.backend("ibm_kingston")

print(f"Backend: {backend.name}")
print(f"    Processor type: {backend.processor_type}")
print(f"    Supported instructions: {backend.supported_instructions}")
```

```text
Backend: ibm_kingston
```

```text
Processor type: {'family': 'Heron', 'revision': '2'}
    Supported instructions: ['cz', 'id', 'delay', 'measure', 'measure_2', 'reset', 'rz', 'sx', 'x', 'if_else', 'store']
```

## Informações dinâmicas do backend {#dynamic-backend-information}

Os backends também possuem propriedades que mudam sempre que o backend é calibrado, como o tempo de vida dos qubits e as taxas de erro das operações. As propriedades do backend são atualizadas após a conclusão da sequência de calibração. Essas propriedades podem ser usadas na otimização de circuitos quânticos ou para construir modelos de ruído para um simulador clássico. Consulte o guia [Jobs de calibração](/guides/calibration-jobs) para mais informações.

No IBM Quantum Platform, você pode baixar os dados de calibração como um arquivo CSV. Clique em uma QPU na página [Recursos de computação](https://quantum.cloud.ibm.com/computers) para ver seu cartão de informações detalhadas e clique no ícone de download no canto superior direito da seção Dados de calibração.

:::note[Recuperar dados históricos]


Você pode recuperar dados históricos das propriedades do backend no Qiskit com o seguinte código:

```python
backend.properties(datetime=<datetime>)
```

:::


### Propriedades dos qubits {#qubit-properties}

[Ir para a lista de propriedades dos qubits](#view-qubit-properties)

`backend.properties().qubit_property()` retorna informações sobre os atributos físicos dos qubits. Ele contém um dicionário de várias propriedades do qubit, cada uma associada ao seu valor e ao timestamp da última calibração.

Os exemplos de código a seguir demonstram como listar todas as propriedades ou recuperar propriedades específicas de um qubit em particular.

    
    

#### Ver propriedades dos qubits {#view-qubit-properties}

<span id="t1"></span>
<Accordion>
<AccordionItem title="`T1` (Relaxation time)">

O tempo $T_1$ representa a duração média em que um qubit permanece em seu estado excitado $|1\rangle$ antes de decair para seu estado fundamental $|0\rangle$ devido ao relaxamento de energia. Esse parâmetro é usado para caracterizar o comportamento de relaxamento de energia do qubit e é expresso em unidades de segundos (s).

<table>
  <tbody>
    <tr>
      <th scope="row">View with Qiskit</th>
      <td>`backend.properties().t1(<qubit>)`</td>
    </tr>
    <tr>
      <th scope="row">View on IBM Quantum Platform</th>
      <td>Calibration data section: Qubit dropdown menu; find the *$T_1$ median* value in the Details section</td>
    </tr>
  </tbody>
</table>
</AccordionItem>
<span id="t2"></span>
<AccordionItem title="`T2` (Dephasing time)">

O tempo $T_2$ denota a escala de tempo sobre a qual um qubit mantém a coerência de fase de uma superposição entre os estados $|0\rangle$ e $|1\rangle$. Ele leva em conta tanto o relaxamento de energia quanto os processos de defasagem pura, fornecendo insights sobre as propriedades de coerência do qubit. $T_2$ é reportado a partir de uma sequência de eco de Hahn.

<table>
  <tbody>
    <tr>
      <th scope="row">View with Qiskit</th>
      <td>`backend.properties().t2(<qubit>)`</td>
    </tr>
    <tr>
      <th scope="row">View on IBM Quantum Platform</th>
      <td>Calibration data section: Qubit dropdown menu; find the *$T_2$ median* value in the Details section</td>
    </tr>
  </tbody>
</table>
</AccordionItem>
</Accordion>

```python
# fundamental physical properties of qubit 1

backend = service.backend("ibm_fez")

backend.qubit_properties(1)
```

```text
QubitProperties(t1=0.00022010760855770256, t2=0.0002363339150114578, frequency=None)
```

```python
# Retrieve qubit properties
qubit_index = 126  # Replace with your qubit index
qubit_props = backend.properties().qubit_property(qubit_index)

# Access specific properties
t1 = qubit_props.get("T1", (None,))[0]
t2 = qubit_props.get("T2", (None,))[0]
readout_error = qubit_props.get("readout_error", (None,))[0]
prob_meas0_prep1 = qubit_props.get("prob_meas0_prep1", (None,))[0]
prob_meas1_prep0 = qubit_props.get("prob_meas1_prep0", (None,))[0]
readout_length = qubit_props.get("readout_length", (None,))[0]

print(f"Qubit {qubit_index} Properties:")
print(f"  T1: {t1} seconds")
print(f"  T2: {t2} seconds")
print(f"  Readout Error: {readout_error}")
print(f"  P(0 | 1): {prob_meas0_prep1}")
print(f"  P(1 | 0): {prob_meas1_prep0}")
print(f"  Readout Length: {readout_length} seconds")
```

```text
Qubit 126 Properties:
  T1: 0.0001255383150343146 seconds
  T2: 8.665416697592209e-05 seconds
  Readout Error: 0.053466796875
  P(0 | 1): 0.087158203125
  P(1 | 0): 0.019775390625
  Readout Length: 1.66e-06 seconds
```

### Propriedades das instruções {#instruction-properties}

[Ir para a lista de propriedades das instruções](#view-instruction-properties)

O atributo `backend.target` é um objeto [`qiskit.transpiler.Target`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.Target): um objeto que contém todas as informações necessárias para transpilar um circuito para aquele backend. Isso inclui erros e durações das instruções. Por exemplo, a célula a seguir obtém as propriedades de uma [porta `cz`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.CZGate) agindo entre os qubits 1 e 0.

    

A célula a seguir mostra as propriedades de uma operação de medição (incluindo o erro de leitura) no qubit 0.

    

#### Ver propriedades das instruções {#view-instruction-properties}

<Accordion>
<span id="prob-meas0-prep1"></span>
  <AccordionItem title="prob-meas0-prep1">

  This parameter indicates the probability of measuring a qubit in the $|0\rangle$ state when it was intended to be prepared in the $|1\rangle$ state, denoted as $P(0 | 1)$. It reflects errors in state preparation and measurement (SPAM), particularly measurement errors in superconducting qubits.

    <table>
      <tbody>
        <tr>
          <th scope="row">Ver com Qiskit</th>
          <td>`backend.properties().qubit_property(<qubit>, 'prob_meas0_prep1')`</td>
        </tr>
        <tr>
          <th scope="row">Ver na IBM Quantum Platform</th>
          <td>Seção de dados de calibração: menu suspenso Qubit</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="prob-meas1-prep0"></span>
  <AccordionItem title="prob-meas1-prep0">

  Similarly, this parameter represents the probability of measuring a qubit in the $|1\rangle$ state when it was intended to be prepared in the $|0\rangle$ state, denoted as $P(1 | 0)$. Like `prob_meas0_prep1`, it reflects SPAM errors, with measurement errors being the predominant contributor in superconducting qubits.

    <table>
      <tbody>
        <tr>
          <th scope="row">View with Qiskit</th>
          <td>`backend.properties().qubit_property(<qubit>, 'prob_meas0_prep0')`</td>
        </tr>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Calibration data section: Qubit dropdown menu</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="2q-error"></span>
  <AccordionItem title="2Q error (Heron and Nighthawk: CZ, Eagle: ECR)">

O erro de dois qubits por aresta, obtido a partir do mesmo conjunto de medições usado para calcular o erro [mediano 2Q](#2q-error-median). **2Q error (best)** refere-se ao menor erro de dois qubits em qualquer aresta do dispositivo, também obtido a partir desse conjunto de medições.

  <table>
    <tbody>
      <tr>
        <th scope="row">View with Qiskit</th>
        <td>`backend.target['<instruction>'][<qubit 1>, <qubit 2>]`</td>
      </tr>
      <tr>
        <th scope="row">View on IBM Quantum Platform</th>
        <td>Calibration section: hover over the qubit connection in Map view, or find the value in Table view under the CZ error (Heron and Nighthawk) or ECR error (Eagle) column; find the value for *2Q error (best)* in the Details section</td>
      </tr>
    </tbody>
  </table>
  </AccordionItem>
  <span id="2q-error-median"></span>
  <AccordionItem title="Median 2Q error (Heron: CZ, Eagle: ECR)">

[Fidelidade média de porta](https://docs.quantum.ibm.com/api/qiskit/quantum_info#average_gate_fidelity) da operação de dois qubits a partir do benchmarking randomizado. Medida em "isolamento": lotes com separação mínima de dois qubits entre arestas. Esse benchmarking randomizado usa camadas alternadas de Cliffords de qubit único e portas de dois qubits, portanto o valor final do erro 2Q inclui o erro da camada de Cliffords de qubit único.

    <table>
      <tbody>
        <tr>
          <th scope="row">Calculate with Qiskit</th>
          <td>Follow the example in this [Qiskit Community GitHub notebook](https://github.com/qiskit-community/qiskit-device-benchmarking/blob/main/notebooks/device_rb_w_lf.ipynb)</td>
        </tr>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Details section; also find per-edge data in the Calibration data section</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="2q-error-layered"></span>
  <AccordionItem title="2Q error (layered)">

Erro médio por porta em camadas (EPLG) em uma cadeia de 100 qubits. O EPLG médio mede o erro médio de porta em uma cadeia em camadas de $N$ qubits ($N$=100 aqui). É derivado de uma quantidade similar conhecida como fidelidade de camada (LF), onde EPLG$_{100}$ = 4/5(1-LF$^{\frac{1}{99}}$) e a fidelidade de camada é a fidelidade de processo da cadeia em camadas de $N$ qubits. Para detalhes, veja o artigo [Benchmarking quantum processor performance at scale](https://arxiv.org/abs/2311.05933). Note que no artigo o EPLG é definido para erro de processo, mas por consistência com os erros de porta reportados individualmente aqui, ele é citado para erro médio de porta, daí o fator de 4/5.

  Na IBM Quantum Platform, o cartão de informações detalhadas de cada QPU tem uma seção chamada Two-qubit gate error (layered), que fornece a visão expandida do menor `two-qubit gate error (layered)` medido em função do número de qubits na cadeia. O valor final, no comprimento de cadeia 100, é o valor apresentado na seção Details. Na prática, seis cadeias de 100 qubits (pré-selecionadas com base no desempenho ideal esperado) são medidas, e o valor relatado para o número de qubits `N` é o menor erro encontrado em uma cadeia de subconjunto de comprimento `N` pesquisando nas seis cadeias de 100 qubits.

    <table>
      <tbody>
        <tr>
          <th scope="row">Calculate with Qiskit</th>
          <td>Follow the example in this [Qiskit Community GitHub notebook](https://github.com/qiskit-community/qiskit-device-benchmarking/blob/main/notebooks/layer_fidelity_single_chain.ipynb)</td>
        </tr>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Details section, and an expanded view in the Two-qubit gate error (layered) section</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="rzz-error"></span>
  <AccordionItem title="RZZ error (Heron)">

Erro na porta RZZ calculado como média sobre os ângulos RZZ usando uma variante de benchmarking randomizado para unitários arbitrários.

    <table>
      <tbody>
        <tr>
          <th scope="row">View with Qiskit</th>
          <td>Important: Be sure you have set `use_fractional_gates=True` when you load the backend, then you can use `backend.target['rzz'][<qubit 1>, <qubit 2>]`</td>
        </tr>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Calibration section: Select RZZ in the Connection dropdown menu and hover over the qubit connection in Map view. You can also select RZZ error in the Graph output dropdown menu in Graph view, or find the value in Table view under the RZZ error column</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="finite-duration-discrete"></span>
  <AccordionItem title="ID error / √x (sx) error / Pauli-X error / RX error">

Erro nas portas discretas de duração finita de um qubit, medido a partir do benchmarking randomizado. A sequência de benchmarking randomizado inclui as portas SX, ID e X, e presume-se que seus erros sejam iguais. A porta ID é um atraso de duração igual à duração das portas √X e X. A porta RX também possui a mesma duração das portas √X e X com amplitude variável, e por isso é reportada como tendo o mesmo erro que essas portas.

    <table>
      <tbody>
        <tr>
          <th scope="row">View with Qiskit</th>
          <td>`backend.target['<instruction>'][<qubit 1>, ]`</td>
        </tr>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Calibration section: Qubit dropdown menu</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="median-sx-error"></span>
  <AccordionItem title="SX error (median)">

[Fidelidade média de porta](https://docs.quantum.ibm.com/api/qiskit/quantum_info#average_gate_fidelity) da porta √X (SX) a partir do benchmarking randomizado, medida simultaneamente em todos os qubits. A sequência de benchmarking randomizado inclui as portas SX, ID e X, e presume-se que seus erros sejam iguais.

    <table>
      <tbody>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Details section</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="rz-error"></span>
  <AccordionItem title="Z-axis rotation (RZ) error">
  Error in the virtual RZ gate. Reported as all 0 since these are performed in software.

    <table>
      <tbody>
        <tr>
          <th scope="row">View with Qiskit</th>
          <td> `backend.target['<instruction>'][<qubit 1>, ]`</td>
        </tr>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Calibration section: Connection dropdown menu</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="readout-error"></span>
  <AccordionItem title="Readout error">

O erro de leitura quantifica a probabilidade média de medir incorretamente o estado de um qubit. É comumente calculado como a média de `prob_meas0_prep1` e `prob_meas1_prep0`, fornecendo uma única métrica para a fidelidade de medição.

    <table>
      <tbody>
        <tr>
          <th scope="row">View with Qiskit</th>
          <td>`backend.properties().readout_error(<qubit>)`</td>
        </tr>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Calibration data section: Qubit dropdown menu</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="median-readout-error"></span>
  <AccordionItem title="Readout error (median)">

  Fidelity of the readout operation. Readout error is measured by preparing the qubit in the $|0\rangle$ ($|1\rangle$) state and measuring the probability of an output in the $|1\rangle$ ($|0\rangle$) state. The reported value is the average of these two errors. The median is taken over all qubits.

    <table>
      <tbody>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Calibration data section: Details section</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="single-qubit-gate-length"></span>
  <AccordionItem title="Single-qubit gate length">

  Duration of a single-qubit gate operation. Note that values shown on IBM Quantum Platform are in nanoseconds. Values returned in Qiskit are in seconds.
    <table>
      <tbody>
        <tr>
          <th scope="row">View with Qiskit</th>
          <td>`backend.target['<instruction>'][<qubit 1>, ].duration`</td>
        </tr>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Calibration section: Qubit dropdown menu</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="gate-length"></span>
  <AccordionItem title="Gate length (2Q gates)">

  Duração da operação de porta de dois qubits. Observe que os valores mostrados na IBM Quantum Platform estão em nanossegundos. Os valores retornados no Qiskit estão em segundos.

    <table>
      <tbody>
        <tr>
          <th scope="row">Ver com Qiskit</th>
          <td>`backend.target['<instruction>'][<qubit 1>, <qubit 2> ].duration`</td>
        </tr>
        <tr>
          <th scope="row">Ver na IBM Quantum Platform</th>
          <td>Seção de calibração: menu suspenso Qubit</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="readout-length"></span>
  <AccordionItem title="Comprimento de leitura">

O comprimento de leitura especifica a duração da operação de leitura de um qubit. Ele mede o tempo desde o início do pulso de medição até a conclusão da digitalização do sinal, após o qual o sistema está pronto para a próxima operação. Compreender esse parâmetro é fundamental para otimizar a execução de circuitos, especialmente ao incorporar medições em meio ao circuito.

    <table>
      <tbody>
        <tr>
          <th scope="row">View with Qiskit</th>
          <td><ul><li>For `measure`: `backend.properties().readout_length(<qubit>)`</li><li> For `measure_2`: `backend.target['measure_2'][<qubit 1>, ].duration` </li></ul></td>
        </tr>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Calibration data section: Qubit dropdown menu</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
  <span id="init-error"></span>
  <AccordionItem title="Initialization error">

  O erro de inicialização quantifica a porcentagem da população no estado $|1\rangle$ após o rep-delay padrão e o [procedimento de inicialização de qubit](/guides/repetition-rate-execution) quando o experimento anterior preparou o qubit no estado $|1\rangle$. Observe que isso é sutilmente diferente da quantidade [`prob_meas1_prep0`](#prob-meas1-prep0), que mede a porcentagem das medições atribuídas ao estado $|1\rangle$. Esta última é uma medida tanto do erro de inicialização quanto do procedimento de discriminação entre os estados $|0\rangle$ e $|1\rangle$. No limite de medição perfeita, as duas quantidades são iguais.

    <table>
      <tbody>
        <tr>
          <th scope="row">View with Qiskit</th>
          <td>`backend.properties().qubit_property(<qubit>, 'init_error')`</td>
        </tr>
        <tr>
          <th scope="row">View on IBM Quantum Platform</th>
          <td>Calibration data section: Qubit dropdown menu</td>
        </tr>
      </tbody>
    </table>
  </AccordionItem>
</Accordion>

```python
backend.target["cz"][(1, 0)]
```

```text
InstructionProperties(duration=6.8e-08, error=0.0063762001148502945)
```

```python
backend.target["measure"][(0,)]
```

```text
InstructionProperties(duration=1.66e-06, error=0.0126953125)
```

### Propriedades adicionais {#additional-properties}

<span id="mcps"></span>
<Accordion>
<AccordionItem title="Máximo de circuitos por segundo (MCPS)">

Máximo de circuitos por segundo (MCPS) inclui medição, reset e reinicialização de um circuito com uma porta Hadamard e uma única medição.

<table>
  <tbody>
    <tr>
      <th scope="row">Ver com Qiskit</th>
      <td>`mcps = backend.configuration().mcps`</td>
    </tr>
    <tr>
      <th scope="row">Ver na IBM Quantum Platform</th>
      <td>Seção Details</td>
    </tr>
  </tbody>
</table>
</AccordionItem>
<span id="clops"></span>
<AccordionItem title="CLOPS (ou CLOPS_h)">

Operações de camada de circuito por segundo (CLOPS) é uma medida de quantas camadas de um circuito 100x100 (circuito ciente do hardware) uma QPU pode executar por unidade de tempo.

<table>
  <tbody>
    <tr>
      <th scope="row">Calculate with Qiskit</th>
      <td>Find the [CLOPS code](https://github.com/qiskit-community/qiskit-device-benchmarking/tree/main/qiskit_device_benchmarking/clops#readme) in the Qiskit Community GitHub</td>
    </tr>
    <tr>
      <th scope="row">View on IBM Quantum Platform</th>
      <td>Details section</td>
    </tr>
  </tbody>
</table>
</AccordionItem>
<span id="status"></span>
<AccordionItem title="Status">

Com [`BackendStatus`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/models-backend-status), você pode encontrar o status da QPU (por exemplo, `Active`, `Paused`, `Offline`) e o número de jobs pendentes.

<table>
  <tbody>
    <tr>
      <th scope="row">View with Qiskit</th>
      <td>`print(backend.status().status_msg)`, `print(backend.status().pending_jobs)`</td>
    </tr>
    <tr>
      <th scope="row">View on IBM Quantum Platform</th>
      <td>Details section</td>
    </tr>
  </tbody>
</table>
</AccordionItem>
<span id="coupling-map"></span>
<AccordionItem title="Topology diagram or coupling map">

Um diagrama que indica os pares de qubits que suportam operações de porta de dois qubits entre eles. Também é chamado de mapa de acoplamento ou conectividade. Os qubits são representados como círculos e as operações de porta de dois qubits suportadas são exibidas como linhas conectando os qubits.
<table>
  <tbody>
    <tr>
      <th scope="row">View with Qiskit</th>
      <td>`from qiskit.visualization import plot_gate_map` then `plot_gate_map(backend)`</td>
    </tr>
    <tr>
      <th scope="row">View on IBM Quantum Platform</th>
      <td>Calibration data section; Click on Expand for a larger view </td>
    </tr>
  </tbody>
</table>
</AccordionItem>
</Accordion>
<Admonition type="note" title="O que significa `error = 1`?">
   Se o benchmarking de um qubit ou aresta não for bem-sucedido ao longo de vários dias, seja por baixa qualidade de dados ou outros fatores internos, o valor de erro reportado é considerado desatualizado e será reportado como 1. Isso não indica que o qubit ou a aresta está necessariamente com defeito ou que o erro é 1; na verdade, o erro é considerado indefinido e você deve proceder com cautela ao operar esse qubit ou porta.
</Admonition>

## Próximos passos {#next-steps}

<Admonition type="tip" title="Recomendações">
- Saiba como [criar modelos de ruído](/guides/build-noise-models) para fazer simulações clássicas com ruído.
- Revise o tópico [Etapas do transpilador](/guides/transpiler-stages) para aprender como o transpilador usa as propriedades do backend na otimização de circuitos.
- Revise a referência da [API de backend do QiskitRuntime](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/qiskit-runtime-service#backend).
</Admonition>
