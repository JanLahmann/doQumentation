---
title: Instalar o Qiskit SDK a partir do código-fonte
description: Aprenda como instalar a versão de desenvolvimento do Qiskit.

---
{/* doqumentation-source-hash: ae832ae1 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore rustup */}

# Instalar o Qiskit SDK a partir do código-fonte {#install-the-qiskit-sdk-from-source}

Ao instalar o Qiskit SDK a partir do código-fonte, você pode acessar a versão de desenvolvimento atual, em vez de usar a versão disponível no repositório Python Package Index (PyPI). Você pode, portanto, inspecionar e estender a versão mais recente do código do Qiskit com mais eficiência.

## Criar e ativar um novo ambiente virtual {#create-and-activate-a-new-virtual-environment}

1. Navegue até o diretório do seu projeto e crie um ambiente mínimo com apenas o Python instalado.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

1. Ative o novo ambiente.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
             ```text
             .venv\Scripts\Activate.ps1
             ```
           </TabItem>
         </OperatingSystemTabs>

## Instalar o compilador Rust {#install-the-rust-compiler}

Um compilador Rust deve estar instalado no seu sistema para compilar o Qiskit. Para instalar o compilador Rust, use o instalador multiplataforma [`rustup`](https://rustup.rs/) ou [outro método de instalação](https://forge.rust-lang.org/infra/other-installation-methods.html).

## Instalar o Qiskit {#install-qiskit}

Siga estas etapas para instalar o Qiskit:

1. Clone o repositório do Qiskit.

```bash
git clone https://github.com/Qiskit/qiskit.git
```

2. Acesse o diretório `qiskit`.

```bash
cd qiskit
```

3. (Opcional) Se você quiser executar testes ou verificações de linting, instale os requisitos de desenvolvimento.

```bash
pip install -r requirements-dev.txt
```

4. Instale o `qiskit`.

  * **Instalação padrão**:

    ```bash
    pip install .
    ```

  * **Modo editável**: Neste modo, você não precisa reinstalar o Qiskit quando houver alterações no código do projeto.

    ```bash
    pip install -e .
    ```

    No modo editável, as extensões compiladas são geradas em _modo de depuração_ sem otimizações. Isso afeta o desempenho em tempo de execução do código compilado. Para compilar as extensões com otimizações habilitadas, execute o seguinte comando para recompilar o binário em _modo de lançamento_:

    ```bash
    python setup.py build_rust --release --inplace
    ```

    <Admonition type="note">
    Se você estiver trabalhando com código Rust no Qiskit, precisará recompilar o código de extensão toda vez que fizer uma alteração local. No modo editável, a extensão Rust só é compilada quando o comando de instalação é executado, portanto as alterações locais feitas no código Rust não são refletidas no pacote instalado a menos que você recompile a extensão executando novamente o `build_rust` (com ou sem `--release`, dependendo se você quer compilar em modo de lançamento ou de depuração).
    </Admonition>

## Próximas etapas {#next-steps}

<Admonition type="tip" title="Recomendações">
  - Leia as [diretrizes de contribuição](https://github.com/Qiskit/qiskit/blob/main/CONTRIBUTING.md) para contribuir com o Qiskit SDK de código aberto.
  - [Execute seu primeiro circuito quântico](/guides/quick-start).
  - Experimente um tutorial, como o [algoritmo de Grover](/tutorials/grovers-algorithm).
</Admonition>
