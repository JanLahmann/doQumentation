---
description: 'Opisuje, jak długo może działać zadanie IBM Quantum Compute Service, niezależnie od używanego trybu wykonania.'
notebook_path: guides/max-execution-time.ipynb
sidebar_label: 'Maksymalny czas wykonania dla obciążeń IBM Quantum Compute Service'
title: 'Maksymalny czas wykonania dla obciążeń IBM Quantum Compute Service'
---

{/* doqumentation-source-hash: 735052ff */}




Aby zapewnić sprawiedliwy dostęp, istnieje [wymuszany przez usługę](#max-QPU) maksymalny czas działania każdego zadania. Jeśli zadanie przekroczy ten limit czasu, zostaje przymusowo anulowane i zgłaszany jest wyjątek `RuntimeJobMaxTimeoutError`. Możesz również [ustawić maksymalny czas wykonania](#max-job) swoich zadań, aby lepiej kontrolować koszty.

 <Admonition type="note">
   Sesja lub batch jest natychmiast kończona po osiągnięciu limitu kosztów. Wszystkie aktualnie uruchomione zadania w danej sesji lub batchu zostaną ukończone, ale oczekujące zadania zostaną anulowane.
</Admonition>

<span id="max-job"></span>

## Ustawianie maksymalnego czasu wykonania {#set-a-maximum-execution-time}

Maksymalny czas wykonania zadania to mniejsza z poniższych wartości:

- Wartość ustawiona dla `max_execution_time`

- Wartość limitu czasu usługi (trzy godziny)


Wartość `max_execution_time` opiera się na *użyciu QPU* (tylko użycie kwantowe), a nie na czasie zegarowym.

Ustaw maksymalny czas wykonania (w sekundach) w opcjach zadania, jak pokazano w poniższym przykładzie. Informacje o ustawianiu opcji znajdziesz w przewodnikach [Estimator options](/guides/estimator-options), [Sampler options](/guides/sampler-options) lub [Executor options](/guides/executor-options).

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import EstimatorV2 as Estimator

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

estimator = Estimator(mode=backend)

estimator.options.max_execution_time = 2500
```

Możesz również sprawdzić, ile zasobów QPU zostało zużyte przez ukończone zadania, zwracając metryki zadania w następujący sposób:

```python
# Find QPU usage of a specific job
print(f"QPU usage of job {job.job_id()} was {job.metrics()['usage']['quantum_seconds']} seconds")
```

<span id="max-QPU"></span>

## Maksymalny czas wykonania wymuszany przez usługę {#service-enforced-maximum-execution-time}

Usługa wymusza maksymalny limit czasu wykonania wynoszący trzy godziny, aby zapewnić sprawiedliwe korzystanie z urządzenia. Jeśli dla zadania podano również `max_execution_time`, używana jest mniejsza z dwóch wartości.

Na przykład, jeśli podasz `max_execution_time=5000` (około 83 minuty), zadanie zostanie anulowane po około 83 minutach, mimo że usługa mogłaby pozwolić na jego działanie przez trzy godziny.

<span id="batch-max-time"></span>

## Maksymalny czas wykonania dla batch {#batch-maximum-execution-time}

Gdy batch jest uruchamiany, przypisywana mu jest maksymalna wartość czasu życia (maksymalne TTL). Po osiągnięciu tego TTL batch jest kończony, wszystkie już uruchomione zadania kontynuują działanie, a pozostałe zadania w kolejce w danym batchu przechodzą w stan błędu.

Batche mają również wartość interaktywnego czasu życia (interaktywne TTL) między zadaniami, której nie można konfigurować. Jeśli nie zamkniesz batcha jawnie, zostanie on dezaktywowany po wygaśnięciu interaktywnego TTL i może zostać ponownie aktywowany w dowolnym momencie, dopóki nie osiągnie swojego maksymalnego TTL.

Instrukcje dotyczące pracy z tymi wartościami znajdziesz w artykule [Run jobs in a batch](/guides/run-jobs-batch).

<span id="session-max-time"></span>

## Maksymalny czas wykonania dla Session {#session-maximum-execution-time}

Gdy Session jest uruchamiana, przypisywana jej jest maksymalna wartość TTL, która określa, jak długo Session może działać. Po osiągnięciu tego TTL Session jest kończona, wszystkie już uruchomione zadania kontynuują działanie, a pozostałe zadania w kolejce w danej Session przechodzą w stan błędu.

Istnieje również wartość interaktywnego TTL, której nie można konfigurować. Jeśli w tym oknie czasowym nie zostaną dodane do kolejki żadne zadania Session, Session zostaje tymczasowo dezaktywowana.

Instrukcje dotyczące pracy z tymi wartościami znajdziesz w artykule [Run jobs in a session](/guides/run-jobs-session).

## Inne ograniczenia {#other-limitations}

W ramach planu Open możesz korzystać z QPU przez maksymalnie 10 minut w każdym 28-dniowym oknie kroczącym. Wykorzystanie instancji możesz sprawdzić na stronie [Instances](https://quantum.cloud.ibm.com/instances).

## Następne kroki {#next-steps}

<Admonition type="tip" title="Rekomendacje">
- [Workload usage](/guides/estimate-job-run-time).
- Zapoznaj się z tymi wskazówkami: [Minimize job run time](/guides/minimize-time).
</Admonition>
