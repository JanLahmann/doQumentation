---
description: 'Zbuduj i zwizualizuj obwód kwantowy w mniej niż dwie minuty – bez logowania i klucza API.'
notebook_path: guides/quick-start.ipynb
sidebar_label: 'Szybki start'
title: 'Szybki start'
---

{/* doqumentation-source-hash: dc09af91 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<OpenInLabBanner notebookPath="guides/quick-start.ipynb" />




{/* cspell:ignore miniconda */}

{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Wersje pakietów">

Kod na tej stronie został opracowany przy użyciu poniższych wymagań.
Zalecamy korzystanie z tych lub nowszych wersji.

```
qiskit[all]~=2.5.2
```
</AccordionItem>
</Accordion>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

Zbuduj swój pierwszy obwód kwantowy w mniej niż dwie minuty, w lokalnym środowisku – bez logowania i klucza API.

<Admonition type="note" title="Dopiero zaczynasz z Pythonem i środowiskami wirtualnymi?">

* Pobierz Pythona i użyj środowiska wirtualnego razem z Qiskit (zalecane).


<Accordion>
<AccordionItem title="Kliknij, aby rozwinąć więcej informacji o **Pythonie**.">

- Aby zainstalować Pythona, najpierw sprawdź sekcję „Programming Language" na [stronie projektu Qiskit w PyPI](https://pypi.org/project/qiskit/), aby ustalić, które wersje Pythona są obsługiwane przez najnowsze wydanie. Instrukcje dotyczące pobierania znajdziesz w [Przewodniku dla początkujących Pythona](https://wiki.python.org/moin/BeginnersGuide/Download).


<Admonition type="note">
These instructions use the standard Python distribution from [pypi.org](https://pypi.org/). However, you can use other Python distributions, such as [Anaconda](https://docs.anaconda.com/anaconda/) or [miniconda](https://docs.anaconda.com/miniconda/), along with other dependency management workflows like [Poetry](https://python-poetry.org/docs/).
</Admonition>
  </AccordionItem>
</Accordion>

    <Accordion>
    <AccordionItem title="Click to expand for more information on **virtual environments**.">
    - Use [Python virtual environments](https://docs.python.org/3.10/tutorial/venv.html) to separate Qiskit from other applications.
    A Python virtual environment is an isolated space to work with Python for a specific purpose — so you can install whatever packages you wish, and set up libraries, dependencies, and so on, without affecting the "base" Python environment on your machine.

    Jedną z ważnych zalet środowiska wirtualnego jest to, że jeśli twoje środowisko Pythona ulegnie uszkodzeniu, możesz je łatwo usunąć i zacząć od nowa!

    Wybierz preferowaną lokalizację do przechowywania informacji o swoich środowiskach wirtualnych. Zazwyczaj są one przechowywane w katalogu o nazwie `.venv` wewnątrz każdego katalogu projektu.

    Aby skonfigurować środowisko wirtualne, przejdź do katalogu projektu i utwórz minimalne środowisko z zainstalowanym wyłącznie Pythonem.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">

            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">

            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">

            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

    Następnie aktywuj nowe środowisko.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">

             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">

             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
              Jeśli używasz PowerShell:

             ```text
             .venv\Scripts\Activate.ps1
             ```
             Jeśli używasz Git Bash:

             ```text
             source .venv/scripts/activate
             ```
              Jeśli używasz wiersza polecenia:

             ```text
             .venv\Scripts\activate
             ```
           </TabItem>
         </OperatingSystemTabs>
      </AccordionItem>
    </Accordion>

</Admonition>

## 1. Zainstaluj Qiskit {#1-install-qiskit}

Zainstaluj poniższe pakiety za pomocą preferowanego menedżera pakietów (np. `pip`):

- [`qiskit`](/guides/install-qiskit)

- [`matplotlib`](https://matplotlib.org/stable/users/explain/quick_start.html)

- [`qiskit[visualization]`](https://docs.quantum.ibm.com/api/qiskit/visualization)


## 2. Zbuduj swój obwód {#2-build-your-circuit}

Otwórz środowisko Pythona, a następnie uruchom ten kod, aby zbudować stan Bella (dwa splątane Qubit-y).

```python
from qiskit import QuantumCircuit
from qiskit.primitives import StatevectorSampler

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

sampler = StatevectorSampler()
result = sampler.run([qc], shots=1024).result()
print(result[0].data.meas.get_counts())
```

```text
{'00': 486, '11': 538}
```

Oczekiwany wynik to niemal równy podział między '00' i '11'.

## 3. Zwizualizuj swoje wyniki {#3-visualize-your-results}

Aby uzyskać histogram wyników, dodaj do programu poniższy kod.

```python
# Uncomment lines 2 and 8 if you are not using Python in a Jupyter notebook
# import matplotlib.pyplot as plt
from qiskit.visualization import plot_histogram

counts = result[0].data.meas.get_counts()
plot_histogram(counts)

# plt.show()
```

![Wynik poprzedniej komórki kodu](/docs/images/guides/quick-start/extracted-outputs/dc4ff012-0.svg)

Ten wynik jest sygnaturą kwantowego splątania.

## 4. Zobacz, co się dzieje {#4-see-what-happens}

Spróbuj zmodyfikować kod, aby zobaczyć, jak wpływa na wyniki. Na przykład:

- Dodaj trzeci Qubit, zmieniając na `QuantumCircuit(3)`, i dodaj drugą bramkę CX z `qc.cx(1,2)`. Pomiary powinny wtedy zmienić się na 000 i 111, co oznacza, że wszystkie trzy Qubit-y zostały splątane.


- Zobacz, jak zmieniają się wyniki, dodając `qc.x(1)` na końcu Circuit-u.


## Kolejne kroki {#next-steps}

<Admonition type="tip" title="Rekomendacje">
- Wykonaj kroki z przewodnika [Witaj, świecie](/guides/hello-world), aby uruchomić Circuit na prawdziwym sprzęcie kwantowym.
- Nie jesteś gotowy/a na uruchamianie na sprzęcie? Rozpocznij swoją kwantową przygodę od kursu [Podstawy informacji kwantowej](/learning/courses/basics-of-quantum-information).
</Admonition>
