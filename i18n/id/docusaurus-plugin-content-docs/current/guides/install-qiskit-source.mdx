---
title: Install Qiskit SDK dari sumber
description: Pelajari cara menginstal versi pengembangan Qiskit.

---

{/* doqumentation-source-hash: 3602a588 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore rustup */}

# Install Qiskit SDK dan Qiskit Runtime dari sumber {#install-the-qiskit-sdk-and-qiskit-runtime-from-source}

Menginstal Qiskit SDK dari sumber memungkinkan kamu mengakses versi pengembangan terkini, alih-alih menggunakan versi yang tersedia di repositori Python Package Index (PyPI). Dengan cara ini, kamu bisa memeriksa dan memperluas versi terbaru kode Qiskit dengan lebih efisien.

## Buat dan aktifkan virtual environment baru {#create-and-activate-a-new-virtual-environment}

1. Masuk ke direktori proyekmu dan buat environment minimal yang hanya berisi Python.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

1. Aktifkan environment barumu.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
             ```text
             .venv\Scripts\Activate.ps1
             ```
           </TabItem>
         </OperatingSystemTabs>

## Install compiler Rust {#install-the-rust-compiler}

Compiler Rust harus terinstal di sistemmu untuk mengompilasi Qiskit. Untuk menginstal compiler Rust, gunakan installer Rust lintas platform [`rustup`](https://rustup.rs/) atau [metode instalasi lainnya.](https://forge.rust-lang.org/infra/other-installation-methods.html)

## Install Qiskit {#install-qiskit}

Ikuti langkah-langkah berikut untuk menginstal Qiskit:

1. Clone repositori Qiskit.

```bash
git clone https://github.com/Qiskit/qiskit.git
```

2. Masuk ke direktori `qiskit`.

```bash
cd qiskit
```

3. (Opsional) Jika ingin menjalankan tes atau pemeriksaan linting, install persyaratan developer.

```bash
pip install -r requirements-dev.txt
```

4. Install `qiskit`.

  * **Instalasi standar**:

    ```bash
    pip install .
    ```

  * **Mode editable**: Dalam mode ini, kamu tidak perlu menginstal ulang Qiskit setiap kali ada perubahan kode pada proyek.

    ```bash
    pip install -e .
    ```

    Dalam mode editable, ekstensi yang dikompilasi dibangun dalam _debug mode_ tanpa optimasi. Hal ini memengaruhi performa runtime dari kode yang dikompilasi. Untuk membangun ekstensi yang dikompilasi dengan optimasi aktif, jalankan perintah berikut untuk membangun ulang binary dalam _release mode_:

    ```bash
    python setup.py build_rust --release --inplace
    ```

    <Admonition type="note">
    Jika kamu sedang mengerjakan kode Rust di Qiskit, kamu perlu membangun ulang kode ekstensi setiap kali membuat perubahan lokal. Dalam mode editable, ekstensi Rust hanya dibangun saat perintah install dijalankan, sehingga perubahan lokal yang kamu buat pada kode Rust tidak akan tercermin dalam paket yang terinstal kecuali kamu membangun ulang ekstensi dengan menjalankan kembali `build_rust` (dengan atau tanpa `--release`, tergantung apakah kamu ingin membangun dalam release mode atau debug mode).
    </Admonition>

## Install Qiskit Runtime {#install-qiskit-runtime}

Ikuti langkah-langkah berikut jika kamu ingin menginstal Qiskit Runtime:

1. Clone repositori Qiskit Runtime.

```bash
git clone https://github.com/Qiskit/qiskit-ibm-runtime.git
```

2. Masuk ke direktori `qiskit-ibm-runtime`.

```bash
cd qiskit-ibm-runtime
```

3. Install `qiskit-runtime`. Kami merekomendasikan penggunaan [virtual environment](https://docs.python.org/3/library/venv.html) untuk menghindari pencemaran instalasi Python global kamu.

  * **Instalasi standar**:

    ```bash
    pip install .
    ```

  * **Mode editable**: Dalam mode ini, kamu tidak perlu menginstal ulang Qiskit setiap kali ada perubahan kode pada proyek.

    ```bash
    pip install -e .
    ```

    Dalam mode editable, ekstensi yang dikompilasi dibangun dalam _debug mode_ tanpa optimasi.

4. (Opsional) Jika ingin menjalankan tes atau pemeriksaan linting, install persyaratan developer. Kami merekomendasikan penggunaan [virtual environment](https://docs.python.org/3/library/venv.html) untuk menghindari pencemaran instalasi Python global kamu.

```bash
pip install -e ".[dev]"
```

## Langkah berikutnya {#next-steps}

<Admonition type="tip" title="Rekomendasi">
  - Baca [panduan kontribusi](https://github.com/Qiskit/qiskit/blob/main/CONTRIBUTING.md) untuk berkontribusi pada Qiskit SDK open-source.
  - [Jalankan program Hello world](/guides/hello-world).
  - Coba tutorial, seperti [algoritma Grover](/tutorials/grovers-algorithm).
</Admonition>
