---
description: 'Visualisasikan timing circuit yang dihasilkan dengan membuat gambar yang bisa Anda lihat, unduh, atau keduanya.'
notebook_path: guides/visualize-circuit-timing.ipynb
sidebar_label: 'Visualisasi timing circuit'
title: 'Visualisasi timing circuit'
---

{/* doqumentation-source-hash: cd8693a8 */}


<OpenInLabBanner notebookPath="guides/visualize-circuit-timing.ipynb" />



{/* cspell:ignore mactex, backgroundcolor, lightgreen */}

{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Versi paket">

Kode di halaman ini dikembangkan menggunakan persyaratan berikut.
Kami sarankan menggunakan versi ini atau yang lebih baru.

```
qiskit[all]~=2.5.2
```
</AccordionItem>
</Accordion>
In addition to [visualizing instructions on a circuit](/guides/visualize-circuits), you might want to visualize a circuit's scheduling by using the Qiskit [`timeline_drawer`](https://docs.quantum.ibm.com/api/qiskit/qiskit.visualization.timeline_drawer) method. This visualization could help you to quickly spot idling time on qubits, for example. However, this method does not return accurate results for dynamic circuits.  To visualize dynamic circuit scheduling, use the `draw_circuit_schedule_timing` method, as described in the [Visualize circuit timing](/guides/qiskit-runtime-circuit-timing) guide.

## Contoh {#examples}

Untuk memvisualisasikan program Circuit terjadwal, Anda bisa memanggil fungsi ini dengan sekumpulan argumen kontrol. Sebagian besar tampilan gambar output bisa dimodifikasi melalui stylesheet, tapi ini tidak wajib.

### Gambar dengan stylesheet default {#draw-with-the-default-stylesheet}

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

```python
from qiskit import QuantumCircuit
from qiskit.visualization.timeline import draw
from qiskit.providers.fake_provider import GenericBackendV2
from qiskit.transpiler import generate_preset_pass_manager

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)

backend = GenericBackendV2(5)

pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_circuit = pm.run(qc)

draw(isa_circuit, target=backend.target)
```

![Output dari sel kode sebelumnya](/docs/images/guides/visualize-circuit-timing/extracted-outputs/5b21b8c1-0.svg)

### Gambar dengan stylesheet untuk debugging program {#draw-with-a-stylesheet-suited-for-program-debugging}

```python
from qiskit import QuantumCircuit
from qiskit.visualization.timeline import draw, IQXDebugging
from qiskit.providers.fake_provider import GenericBackendV2
from qiskit.transpiler import generate_preset_pass_manager

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

backend = GenericBackendV2(5)
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_circuit = pm.run(qc)
draw(isa_circuit, style=IQXDebugging(), target=backend.target)
```

![Output dari sel kode sebelumnya](/docs/images/guides/visualize-circuit-timing/extracted-outputs/907dc46c-0.svg)

Kamu bisa membuat fungsi generator atau layout kustom dan memperbarui stylesheet yang sudah ada dengan fungsi kustom tersebut. Dengan cara ini, Anda bisa mengontrol sebagian besar tampilan gambar output tanpa memodifikasi codebase dari drawer Circuit terjadwal. Lihat referensi API [`timeline_drawer`](https://docs.quantum.ibm.com/api/qiskit/qiskit.visualization.timeline_drawer) untuk contoh lebih lanjut.

## Langkah berikutnya {#next-steps}

<Admonition type="tip" title="Rekomendasi">
  - [Visualisasi timing Circuit](/guides/qiskit-runtime-circuit-timing) saat menggunakan IBM Quantum Compute Service
  - [Visualisasi Circuit](/guides/visualize-circuits)
</Admonition>
