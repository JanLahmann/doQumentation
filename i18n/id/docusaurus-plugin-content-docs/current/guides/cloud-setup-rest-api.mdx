---
title: Cara menggunakan IBM Quantum Platform dengan REST API
description: Panduan pengaturan untuk menggunakan IBM Quantum di IBM Cloud guna mengirimkan job Qiskit SDK dan Qiskit Runtime menggunakan REST API
platform: cloud
---

{/* doqumentation-source-hash: a3985acf */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore urlendcoded, WQVY */}

# Cara menggunakan IBM Quantum Platform dengan REST API {#set-up-to-use-ibm-quantum-platform-with-rest-api}

Kamu bisa mengakses prosesor kuantum dengan REST API, sehingga kamu bisa bekerja dengan QPU menggunakan bahasa pemrograman atau framework apa pun.

### 1. Dapatkan akses {#1-dapatkan-akses}

1. Kalau belum punya akun pengguna, daftarkan diri di [halaman login IBM Quantum.](https://quantum.cloud.ibm.com/login)
1. Buat API key (disebut juga token) di [dashboard.](https://quantum.cloud.ibm.com) API key yang sama bisa digunakan untuk kedua region.
1. Buat bearer token IBM Cloud Identity and Access Management (IAM). Ini adalah token berumur pendek yang digunakan untuk mengautentikasi permintaan ke REST API. Untuk membuatnya, panggil [IAM Identity Services API](https://cloud.ibm.com/apidocs/iam-identity-token-api#create-api-key) seperti contoh permintaan berikut:
<Tabs>

   <TabItem value="Curl" label="Curl">
   ```bash
   curl -X POST 'https://iam.cloud.ibm.com/identity/token' \
   -H 'Content-Type: application/x-www-form-urlencoded' \
   -d 'grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=MY_APIKEY'
   ```
   Respons yang Diharapkan
   ```text
   {
      "access_token": "eyJhbGciOiJIUz......sgrKIi8hdFs",
      "refresh_token": "SPrXw5tBE3......KBQ+luWQVY=",
      "token_type": "Bearer",
      "expires_in": 3600,
      "expiration": 1473188353
   }
   ```
   </TabItem>

   <TabItem value="Python" label="Python">
   ```python
   # Use 'service' to invoke operations.
   import requests
   import json

   url = 'https://iam.cloud.ibm.com/identity/token'
   api_key = 'MY_APIKEY'
   headers = {
    'Content-Type': 'application/x-www-form-urlendcoded',
   }
   data = f'grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey={api_key}'
   response = requests.post(url, headers=headers, data=data)

   # Bearer token to authorize requests to the REST API
   bearer_token = response.json()['access_token']
   ```
   </TabItem>
</Tabs>

### 2. Pilih metode autentikasi {#2-pilih-metode-autentikasi}

Pilih metode autentikasi yang sesuai, tergantung lingkungan kerjamu:

* [Buat variabel lingkungan untuk API key kamu (lingkungan Python tepercaya)](#environment_variable)
* [Gunakan API key langsung (lingkungan tidak tepercaya)](/guides/cloud-setup-untrusted#rest-untrusted)

<span id="environment_variable"></span>
#### Buat variabel lingkungan (lingkungan tepercaya) {#buat-variabel-lingkungan-lingkungan-tepercaya}

1. Untuk mengatur variabel lingkungan IQP_API_TOKEN di sistemmu, kamu bisa menambahkan baris berikut ke profil shell (misalnya .bashrc atau .zshrc) atau langsung mengaturnya di terminal:

    ```shell
    export IQP_API_TOKEN=<your-API_KEY> # Use the 44-character API_KEY you created and saved from the IBM Quantum Platform Home dashboard
    ```
    Saat memanggil variabel lingkungan di kode, sertakan `import os`, seperti contoh ini:

    ```python
    import os
    api_token = os.environ['IQP_API_TOKEN']
    ```
    Perlu diingat bahwa saat membuat variabel lingkungan, API key kamu masih tersimpan secara lokal dalam teks biasa dan harus dijaga keamanannya.

1. Autentikasi permintaan ke Qiskit Runtime REST API dengan menyertakan CRN dan bearer token di header permintaan.
```bash
curl -X 'GET' \
   'https://quantum.cloud.ibm.com/api/v1/usage' \
   '-H accept: application/json' \
   '-H authorization: Bearer <BEARER_TOKEN>' \
   '-H Service-CRN: <INSTANCE_CRN>'
```

### 3. Opsional: Konfigurasi firewall-mu {#3-opsional-konfigurasi-firewall-mu}

    Jika perlu, gunakan [informasi ini](/guides/quickstart-steps-org#firewall) untuk mengaktifkan akses ke endpoint IBM Quantum API.

## Langkah selanjutnya {#langkah-selanjutnya}

<Admonition type="tip" title="Rekomendasi">
  -  [Gambaran umum paket yang tersedia.](plans-overview)
  -  [Konfigurasi Qiskit SDK secara lokal.](configure-qiskit-local)
  -  Ikuti langkah-langkah di [Hello world](/guides/hello-world) untuk menulis dan menjalankan program kuantum.
  -  Coba sebuah [tutorial](https://docs.quantum.ibm.com/tutorials).
</Admonition>
