---
description: 'Panduan quickstart tentang cara menggunakan primitive IBM Quantum Sampler.'
notebook_path: guides/get-started-with-sampler.ipynb
sidebar_label: 'Quickstart Sampler'
title: 'Quickstart Sampler'
---

{/* doqumentation-source-hash: 86291a21 */}


<OpenInLabBanner notebookPath="guides/get-started-with-sampler.ipynb" />



Tugas inti Sampler adalah mengambil sampel register output dari eksekusi satu atau lebih Circuit kuantum. [Circuit dinamis](/guides/execute-dynamic-circuits) dan Circuit terparameterisasi diterima sebagai input (jika Circuit terparameterisasi dikirimkan, nilai parameter juga harus disertakan). Sampler juga mendukung dynamical decoupling dan twirling bawaan untuk [penekanan error](/guides/error-mitigation-and-suppression-techniques).

Langkah-langkah dalam topik ini menjelaskan cara menyiapkan Sampler, menjelajahi opsi yang bisa kamu gunakan untuk mengonfigurasinya, dan memanggilnya dalam sebuah program.
{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Versi paket">

Kode di halaman ini dikembangkan menggunakan persyaratan berikut.
Kami menyarankan menggunakan versi ini atau yang lebih baru.

```
qiskit[all]~=2.5.2
qiskit-ibm-runtime~=0.47.0
```
</AccordionItem>
</Accordion>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-ibm-runtime
```

## Langkah-langkah menggunakan primitif Sampler {#steps-to-use-the-sampler-primitive}

### 1. Inisialisasi akun {#1-initialize-the-account}

Kamu perlu menginisialisasi akunmu terlebih dahulu. Kemudian kamu bisa memilih QPU yang ingin kamu gunakan untuk menghitung nilai ekspektasi.

Ikuti langkah-langkah di topik [Atur akun IBM Cloud-mu](/guides/cloud-setup) jika kamu belum memiliki akun yang disiapkan.

:::note[Fractional gates]


Untuk menggunakan [fractional gates](/guides/fractional-gates) yang baru didukung, atur `use_fractional_gates=True` saat meminta backend dari instance `QiskitRuntimeService`. Contoh:

```python
service = QiskitRuntimeService()
fractional_gate_backend = service.least_busy(use_fractional_gates=True)
```

Ini adalah fitur eksperimental dan mungkin berubah di masa mendatang.

:::


```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
backend = service.least_busy(
    operational=True, simulator=False, min_num_qubits=127
)
```

### 2. Buat sebuah sirkuit {#2-create-a-circuit}

Kamu membutuhkan setidaknya satu sirkuit sebagai input ke primitif Sampler.

```python
import numpy as np
from qiskit.circuit.library import efficient_su2

circuit = efficient_su2(127, entanglement="linear")
circuit.measure_all()
# The circuit is parametrized, so we will define the parameter values for execution
param_values = np.random.rand(circuit.num_parameters)
```

Circuit dan observabel perlu ditransformasi untuk hanya menggunakan instruksi yang didukung oleh QPU (disebut sebagai Circuit *instruction set architecture (ISA)*). Gunakan Transpiler untuk melakukan ini.

```python
from qiskit.transpiler import generate_preset_pass_manager

pm = generate_preset_pass_manager(optimization_level=1, backend=backend)
isa_circuit = pm.run(circuit)
print(f">>> Circuit ops (ISA): {isa_circuit.count_ops()}")
```

```text
>>> Circuit ops (ISA): OrderedDict([('rz', 3036), ('sx', 1769), ('cz', 378), ('measure', 127), ('barrier', 1)])
```

### 3. Inisialisasi IBM Quantum Sampler {#3-initialize-the-ibm-quantum-sampler}

Saat kamu menginisialisasi Sampler, gunakan parameter `mode` untuk menentukan mode yang ingin dijalankan.  Nilai yang mungkin adalah objek `batch`, `session`, atau `backend` untuk mode eksekusi batch, session, dan job, masing-masing. Untuk info lebih lanjut, lihat [Pengantar mode eksekusi IBM Quantum Compute Service](/guides/execution-modes). Perlu diperhatikan bahwa pengguna Open Plan tidak bisa mengirimkan job session.

```python
from qiskit_ibm_runtime import SamplerV2 as Sampler

sampler = Sampler(mode=backend)
```

### 4. Panggil Sampler dan dapatkan hasilnya {#4-invoke-sampler-and-get-results}

Selanjutnya, panggil metode `run()` untuk menghasilkan output. Circuit dan set nilai parameter opsional dimasukkan sebagai tuple *primitive unified bloc* (PUB).

```python
job = sampler.run([(isa_circuit, param_values)])
print(f">>> Job ID: {job.job_id()}")
print(f">>> Job Status: {job.status()}")
```

```text
>>> Job ID: dab7lb6rrl7c7386gjgg
```

```text
>>> Job Status: QUEUED
```

```python
result = job.result()

# Get results for the first (and only) PUB
pub_result = result[0]
print(
    f"First ten results for the 'meas' output register: "
    f"{pub_result.data.meas.get_bitstrings()[:10]}"
)
```

```text
First ten results for the 'meas' output register: ['1110001000100100100111011000001001000101010000001100010010110011001100110111100000000110001000011001101000110100101110101010011', '1110101100010101010001010111111011000110011010100101001100110101010010101010101110001010100100000000011110101101011000100000100', '1100110011101000110000000100110111101010110001100010111100001101100001010100111001101010101110000101010001001101010101001000100', '1011001001011000000101010111111010011101001111010100001011110011000101110101011001100110011011110011101010000111001111110110011', '1110111011011111101011110101110101010110011001100110101111100100011101100011011100101100101001001111110101111010101110111111011', '0101001110001110010001010100010111011001000000011110000000000001110000010010101101011001000100000001011011101111010010110111110', '1101101001000100010000110001000101010010101011000011000011001010000111100110010101010110100110011010101100001011100110001111011', '1000110011111111100010101010101111001001100001110111101001100111010101010111110110011000110011010010010000100000100010010010101', '0101010100110011110011010000000100011001111100101100101100101111101110100001101101111010100011011000010011111010001100110000010', '0010000111011100000000110000001110001100010011000100101110111111100011001100011001101010011111111100000000011100101101111010100']
```

## Langkah selanjutnya {#next-steps}

<Admonition type="tip" title="Rekomendasi">
- Pelajari cara [menguji secara lokal](local-testing-mode) sebelum menjalankan di komputer kuantum.
- Tinjau [contoh-contoh](sampler-examples) detail.
- Berlatih dengan primitif melalui [pelajaran fungsi biaya](/learning/courses/variational-algorithm-design/cost-functions) di IBM Quantum Learning.
- Pelajari cara transpile secara lokal di bagian [Transpile](transpile/).
- Coba panduan [Bandingkan pengaturan transpiler](/guides/circuit-transpilation-settings#compare-transpiler-settings).
- Pelajari cara [menggunakan opsi primitif](/guides/runtime-options-overview).
- Lihat API untuk opsi [Sampler](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-sampler-options).
</Admonition>
