---
title: "Hello World: Circuit Quantum Pertamamu"
sidebar_label: "Hello World: Circuit Quantum Pertamamu"
notebook_path: "hello-world.ipynb"
---

{/* doqumentation-source-hash: 4ad1b0c6 */}

<OpenInLabBanner notebookPath="hello-world.ipynb" description="Tutorial ini dibuat untuk doQumentation." />

Buat sebuah [Bell state](https://en.wikipedia.org/wiki/Bell_state) (dua qubit yang saling terjerat) dan jalankan dengan tiga cara:

1. **Simulasi ideal** — hasil sempurna, tidak perlu akun
2. **Simulasi dengan noise** — mensimulasikan perangkat nyata, tidak perlu akun
3. **Hardware quantum sungguhan** — memerlukan [akun IBM Quantum](https://janlahmann.github.io/Qiskit-documentation/#setting-up-your-ibm-quantum-account)

## Bangun Circuit {#build-the-circuit}

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit qiskit-ibm-runtime
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

qc.draw(output="mpl")
```

## Opsi 1: Simulasi ideal (tidak perlu akun) {#option-1-ideal-simulation-no-account-needed}

Menggunakan `StatevectorSampler` — simulator lokal dengan hasil sempurna tanpa noise.

```python
from qiskit.primitives import StatevectorSampler

result = StatevectorSampler().run([qc], shots=1024).result()
counts = result[0].data.meas.get_counts()
counts
```

```python
from qiskit.visualization import plot_histogram
plot_histogram(counts)
```

## Opsi 2: Simulasi dengan noise (tidak perlu akun) {#option-2-noisy-simulation-no-account-needed}

Menggunakan `FakeManilaV2` — simulator lokal yang meniru perangkat quantum IBM sungguhan, termasuk karakteristik noise-nya. Circuit harus ditranspilasi terlebih dahulu (disesuaikan) dengan gate set dan konektivitas qubit perangkat tersebut.

```python
from qiskit_ibm_runtime import SamplerV2
from qiskit_ibm_runtime.fake_provider import FakeManilaV2
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

backend = FakeManilaV2()
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(qc)

result = SamplerV2(mode=backend).run([isa_qc], shots=1024).result()
counts = result[0].data.meas.get_counts()
counts
```

```python
plot_histogram(counts)
```

## Opsi 3: Hardware quantum sungguhan {#option-3-real-quantum-hardware}

Memerlukan akun IBM Quantum. Lihat [Menyiapkan akun IBM Quantum](https://janlahmann.github.io/Qiskit-documentation/#setting-up-your-ibm-quantum-account) untuk detailnya.

Jika kamu belum menyimpan kredensial di sesi Binder ini, jalankan perintah berikut terlebih dahulu:

```python
from qiskit_ibm_runtime import QiskitRuntimeService

QiskitRuntimeService.save_account(
    token="<your-api-key>",
    instance="<your-crn>",
    overwrite=True
)
```

**Catatan:** Job pada hardware sungguhan mungkin membutuhkan waktu tergantung antrean. Jika sel masih berjalan, kamu bisa memeriksa status job dan melihat hasilnya di [quantum.cloud.ibm.com/workloads](https://quantum.cloud.ibm.com/workloads?user=me).

```python
from qiskit_ibm_runtime import QiskitRuntimeService, SamplerV2
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)
print(f"Running on {backend.name}")

pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(qc)

result = SamplerV2(mode=backend).run([isa_qc], shots=1024).result()
counts = result[0].data.meas.get_counts()
counts
```

```python
plot_histogram(counts)
```

## Langkah selanjutnya? {#what-next}

- **[Tutorials](https://mybinder.org/v2/gh/JanLahmann/Qiskit-documentation/main?filepath=docs/tutorials)** — panduan langkah demi langkah tentang algoritma, mitigasi error, transpilasi, dan lainnya
- **[Courses](https://mybinder.org/v2/gh/JanLahmann/Qiskit-documentation/main?filepath=learning/courses)** — jalur belajar terstruktur dari dasar quantum hingga komputasi skala utilitas
- **[Mode pengujian lokal](https://janlahmann.github.io/Qiskit-documentation/#no-token-use-local-testing-mode)** — jalankan sebagian besar notebook tanpa akun IBM Quantum
