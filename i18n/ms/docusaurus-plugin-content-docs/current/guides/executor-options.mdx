---
title: "Tentukan pilihan Executor"
sidebar_label: "Tentukan pilihan Executor"
description: "Tentukan pilihan apabila membina dengan primitif Executor."
notebook_path: "guides/executor-options.ipynb"
---

{/* doqumentation-source-hash: 5e287564 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<OpenInLabBanner notebookPath="guides/executor-options.ipynb" />




{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Versi pakej">

Kod di halaman ini dibangunkan menggunakan keperluan berikut.
Kami syorkan menggunakan versi ini atau yang lebih baru.

```
qiskit-ibm-runtime~=0.46.1
```
</AccordionItem>
</Accordion>
Anda boleh menggunakan pilihan untuk menyesuaikan primitif Executor.

<Admonition type="note" title="Nota">

- Anda boleh melihat pilihan yang tersedia dan mengemaskini nilai pilihan semasa atau selepas permulaan primitif.
- Gunakan kaedah `update()` untuk menerapkan perubahan kepada atribut `options`.
- Atribut `options` adalah jenis Python `dataclass`. Anda boleh menggunakan kaedah `asdict` terbina dalam untuk menukarnya kepada kamus.
</Admonition>

<span id="pass-options"></span>
## Tetapkan pilihan Executor {#set-executor-options}

Jika pilihan ditentukan semasa dan selepas permulaan primitif, nilai yang ditetapkan selepas memulakan primitif digunakan.

### Permulaan primitif {#primitive-initialization}

Anda boleh menghantar instans kelas pilihan atau kamus apabila memulakan Executor, yang kemudian membuat salinan pilihan tersebut. Oleh itu, menukar kamus asal atau instans pilihan tidak mempengaruhi pilihan yang dimiliki oleh primitif.

#### Kelas pilihan {#options-class}

Apabila mencipta instans kelas `Executor`, anda boleh menghantar instans kelas pilihan. Pilihan-pilihan tersebut kemudian diterapkan apabila anda menggunakan `run()` untuk melakukan pengiraan. Tentukan pilihan dalam format ini: `options.option.sub-option.sub-sub-option = choice`. Sebagai contoh: `options.environment.log_level = INFO`.

Contoh:

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit-ibm-runtime
```

```python
from qiskit_ibm_runtime import QiskitRuntimeService, Executor
from qiskit_ibm_runtime.options import ExecutorOptions

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

options = ExecutorOptions(
    environment={"log_level": "INFO"},
    execution={"init_qubits": True},
)

# or use the following instead:

options = ExecutorOptions()
options.environment.log_level = "INFO"
options.execution.init_qubits = True

executor = Executor(mode=backend, options=options)
```

#### Kamus {#dictionary}

Anda boleh menentukan pilihan sebagai kamus apabila memulakan Executor.

```python
from qiskit_ibm_runtime import QiskitRuntimeService, Executor

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

# Setting options during primitive initialization
executor = Executor(
    backend,
    options={
        "environment": {"log_level": "INFO"},
        "execution": {"init_qubits": True},
    },
)
```

<span id="options-table"></span>
## Pilihan yang tersedia {#available-options}

Jadual berikut mendokumentasikan pilihan dari versi terbaru `qiskit-ibm-runtime`. Untuk melihat versi pilihan yang lebih lama, layari [rujukan API `qiskit-ibm-runtime`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime) dan pilih versi sebelumnya.

<Accordion>
<AccordionItem title="`environment`">
[Dokumentasi API `environment`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-sampler-options#environment)
<Accordion>
<AccordionItem title="`environment.job_tags`">

Senarai tag.

**Pilihan**: None

**Lalai**: None
  </AccordionItem>

<AccordionItem title="`environment.log_level`">

**Pilihan**: DEBUG, INFO, WARNING, ERROR, CRITICAL

**Lalai**: WARNING
  </AccordionItem>

<AccordionItem title="`environment.private`">

**Pilihan**: `True`, `False`

**Lalai**: `False`
  </AccordionItem>

  <AccordionItem title="`environment.max_execution_time`">

**Pilihan**: Nombor integer saat dalam julat [1, 10800]

**Lalai**: 10800 (3 jam)
  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`execution`">
[Dokumentasi API `execution`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-sampler-options#execution)
<Accordion>
<AccordionItem title="`execution.init_qubits`">
Sama ada untuk menetapkan semula Qubit ke keadaan asas untuk setiap tembakan.

**Pilihan**: `True`, `False`

**Lalai**: `True`
  </AccordionItem>

<AccordionItem title="`execution.rep_delay`">
Kelewatan antara ukuran dan Circuit kuantum berikutnya.

**Pilihan**: Nilai dalam julat yang disediakan oleh `backend.rep_delay_range`

**Lalai**: Diberikan oleh `backend.default_rep_delay`
  </AccordionItem>

<AccordionItem title="`execution.meas_type`">

**Pilihan**: `classified`, `kerneled`, `avg_kerneled`

**Lalai**: `classified`
  </AccordionItem>

</Accordion>

  </AccordionItem>

  <AccordionItem title="`experimental`">

Pilihan eksperimen, apabila tersedia.

  </AccordionItem>

</Accordion>
<span id="options-compatibility-table"></span>
## Keserasian ciri {#feature-compatibility}

Ciri runtime tertentu tidak boleh digunakan bersama dalam satu kerja. Klik tab yang sesuai untuk senarai ciri yang tidak serasi dengan ciri yang dipilih:

<Tabs>
  <TabItem value="Dynamic" label="Circuit dinamik">
  Tidak serasi dengan:
  - ZNE lipatan Gate
  - PEA
  - PEC
  - Dynamical decoupling

  Nota lain:
  - Twirling Gate boleh diterapkan pada Circuit dinamik, tetapi hanya pada Gate yang tidak berada di dalam blok bersyarat. Twirling ukuran hanya boleh diterapkan pada ukuran terminal.
  - Serasi dengan Gate pecahan apabila menggunakan `qiskit-ibm-runtime` v0.42.0 atau lebih baru.

  </TabItem>
  <TabItem value="DD" label="Dynamical decoupling">
  Tidak serasi dengan Circuit dinamik.

  </TabItem>

  <TabItem value="Fractional" label="Gate pecahan">
  Tidak serasi dengan:
  - Twirling Gate
  - PEA
  - PEC

  Serasi dengan Circuit dinamik apabila menggunakan `qiskit-ibm-runtime` v0.42.0 atau lebih baru.

  </TabItem>

  <TabItem value="ZNE" label="ZNE lipatan Gate">
    Tidak serasi dengan:
  - Circuit dinamik
  - PEA
  - PEC

  Mungkin tidak berfungsi apabila menggunakan Gate tersuai.
  </TabItem>
  <TabItem value="Twirling" label="Twirling Gate">
  Tidak serasi dengan Gate pecahan atau dengan regangan.

  Nota lain:
  - Twirling Gate boleh diterapkan pada Circuit dinamik, tetapi hanya pada Gate yang tidak berada di dalam blok bersyarat. Twirling ukuran hanya boleh diterapkan pada ukuran terminal.
  - Tidak berfungsi dengan penjalin bukan-Clifford.

  </TabItem>

  <TabItem value="PEA" label="PEA">
    Tidak serasi dengan:
  - Circuit dinamik
  - Gate pecahan
  - ZNE lipatan Gate
  - PEC
  </TabItem>

  <TabItem value="PEC" label="PEC">
    Tidak serasi dengan:
  - Circuit dinamik
  - Gate pecahan
  - ZNE lipatan Gate
  - PEA
  </TabItem>

</Tabs>
## Langkah seterusnya {#next-steps}

<Admonition type="tip" title="Cadangan">
- Semak dokumentasi API [ExecutionOptionsV2](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-execution-options-v2).
- Semak dokumentasi API [EnvironmentOptions](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-environment-options).
</Admonition>
