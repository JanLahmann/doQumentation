---
title: "Anggaran fasa kuantum"
sidebar_label: "Anggaran fasa kuantum"
description: "Kursus ini akan mengajar kamu tentang QFT, yang memainkan peranan penting dalam banyak algoritma kuantum"
notebook_path: "learning/courses/utility-scale-quantum-computing/quantum-phase-estimation.ipynb"
---

{/* doqumentation-source-hash: b283679b */}

<OpenInLabBanner notebookPath="learning/courses/utility-scale-quantum-computing/quantum-phase-estimation.ipynb" />



# Algoritma kuantum: Anggaran fasa {#quantum-algorithms-phase-estimation}

<Admonition type="note">

Kento Ueda (15 Mei 2024)

Buku nota ini menyediakan konsep asas dan pelaksanaan Transformasi Fourier Kuantum (QFT) dan Anggaran Fasa Kuantum (QPE).

[Muat turun pdf](https://ibm.ent.box.com/public/static/srgnll6u8ame7gk186pkthq7erc7l6rw.zip) syarahan asal. Perlu diingat bahawa beberapa coretan kod mungkin sudah lapuk kerana ia adalah imej statik.

*Anggaran masa QPU untuk menjalankan eksperimen ini ialah 7 saat.*

</Admonition>
## 1. Pengenalan {#1-pengenalan}

**Transformasi Fourier Kuantum (QFT)**

Transformasi Fourier Kuantum ialah rakan sejawat kuantum bagi transformasi Fourier diskret klasik. Ia adalah transformasi linear yang digunakan pada keadaan kuantum, memetakan asas pengiraan kepada perwakilan asas Fourier. QFT memainkan peranan penting dalam banyak algoritma kuantum, menawarkan kaedah cekap untuk mengekstrak maklumat kala dari keadaan kuantum.
QFT boleh dilaksanakan dengan operasi $O(N^2)$ menggunakan Gate kuantum seperti Gate Hadamard dan Gate Kawalan-Fasa untuk $N$ Qubit, membolehkan pecutan eksponen berbanding transformasi Fourier klasik.

- **Aplikasi**: Ia adalah bahagian asas dalam algoritma kuantum seperti algoritma Shor untuk pemfaktoran integer besar dan logaritma diskret.

**Anggaran Fasa Kuantum (QPE)**

Anggaran Fasa Kuantum ialah algoritma kuantum yang digunakan untuk menganggar fasa yang berkaitan dengan vektor eigen bagi pengendali unitary. Algoritma ini menyediakan jambatan antara sifat matematik abstrak keadaan kuantum dan aplikasi pengiraannya.

- **Aplikasi**: Ia boleh menyelesaikan masalah seperti mencari nilai eigen matriks unitary dan mensimulasikan sistem kuantum.

Bersama-sama, QFT dan QPE membentuk tulang belakang penting bagi banyak algoritma kuantum yang menyelesaikan masalah yang tidak dapat dilakukan oleh komputer klasik. Menjelang akhir buku nota ini, kamu akan memahami bagaimana teknik-teknik ini dilaksanakan.
## 2. Asas Transformasi Fourier Kuantum (QFT) {#2-asas-transformasi-fourier-kuantum-qft}

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-aer qiskit-ibm-runtime
```

```python
from qiskit import QuantumCircuit, QuantumRegister, ClassicalRegister
from qiskit_aer import AerSimulator
from qiskit.visualization import plot_histogram, plot_bloch_multivector
from qiskit.quantum_info import Statevector
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager
from qiskit_ibm_runtime import Sampler

from numpy import pi
```

Berdasarkan analogi dengan transformasi Fourier diskret, QFT bertindak pada keadaan kuantum $\vert X\rangle = \sum_{j=0}^{N-1} x_j \vert j \rangle$ untuk $N$ Qubit dan memetakannya kepada keadaan kuantum $\vert Y\rangle = \sum_{k=0}^{N-1} y_k \vert k \rangle$.

$$
QFT_{N}: \vert j \rangle \mapsto \frac{1}{\sqrt{N}}\sum_{k=0}^{N-1}\omega_N^{jk} \vert k \rangle
$$

di mana $\omega_N^{jk} = e^{2\pi i \frac{jk}{N}}$.

Atau perwakilan matriks unitary:

$$
U_{QFT} = \frac{1}{\sqrt{N}} \sum_{j=0}^{N-1} \sum_{k=0}^{N-1} \omega_N^{jk} \vert k \rangle \langle j \vert
$$
### 2.1. Intuisi {#intuition}

Transformasi Fourier kuantum (QFT) mengubah antara dua asas, iaitu asas pengiraan (Z) dan asas Fourier. Tapi apa yang dimaksudkan dengan asas Fourier dalam konteks ini? Kamu mungkin ingat bahawa transformasi Fourier $F(\omega)$ bagi fungsi $f(x)$ menggambarkan konvolusi $f(x)$ pada fungsi sinusoidal dengan frekuensi $\omega$. Dalam bahasa mudah: transformasi Fourier ialah fungsi yang menggambarkan berapa banyak setiap frekuensi $\omega$ yang diperlukan untuk membina fungsi $f(x)$ daripada fungsi sinus (atau kosinus). Untuk memahami maksud QFT dalam konteks ini dengan lebih baik, pertimbangkan imej berperingkat di bawah yang menunjukkan nombor yang dikodkan dalam binari menggunakan empat Qubit:

<IBMVideo id="134399598" title="Video pendek ini menunjukkan bagaimana empat Qubit yang berpusing pada kadar berbeza boleh digunakan untuk mengekod digit."/>

Dalam asas pengiraan, kita menyimpan nombor dalam binari menggunakan keadaan $|0\rangle$ dan $|1\rangle$.

Perhatikan frekuensi Qubit yang berbeza berubah; Qubit paling kiri bertukar setiap kenaikan nombor, yang seterusnya setiap 2 kenaikan, yang ketiga setiap 4 kenaikan, dan seterusnya.

Jika kita menggunakan transformasi Fourier kuantum pada keadaan-keadaan ini, kita memetakan
$$
|\text{Keadaan dalam Asas Pengiraan}\rangle \quad \xrightarrow[]{\text{QFT}} \quad |\text{Keadaan dalam Asas Fourier}\rangle
$$

$$
\text{QFT}|x\rangle = |\widetilde{x}\rangle
$$

(Kita sering menandakan keadaan dalam asas Fourier menggunakan tilde (~)).

Dalam asas Fourier, kita menyimpan nombor menggunakan putaran berbeza di sekitar paksi Z:

IFRAME

Nombor yang ingin kita simpan menentukan sudut setiap Qubit diputar di sekitar paksi Z. Dalam keadaan $|\widetilde{0}\rangle$, semua Qubit berada dalam keadaan $|{+}\rangle$. Seperti yang dilihat dalam contoh di atas, untuk mengekod keadaan $|\widetilde{5}\rangle$ pada 4 Qubit, kita memutar Qubit paling kiri sebanyak $\tfrac{5}{2^n} = \tfrac{5}{16}$ pusingan penuh ($\tfrac{5}{16}\times 2\pi$ radian). Qubit seterusnya diputar dua kali ganda ini ($\tfrac{10}{16}\times 2\pi$ radian, atau $10/16$ pusingan penuh), sudut ini kemudian digandakan untuk Qubit berikutnya, dan seterusnya.

Sekali lagi, perhatikan frekuensi setiap Qubit berubah. Qubit paling kiri (`qubit 0`) dalam kes ini mempunyai frekuensi terendah, dan yang paling kanan mempunyai frekuensi tertinggi.
### 2.2 Contoh: QFT 1-Qubit {#22-contoh-qft-1-qubit}
Mari kita pertimbangkan kes $N = 2^1 = 2$.

Matriks unitary boleh ditulis:

$$
\begin{aligned}
U_{QFT} & = \frac{1}{\sqrt{2}} \sum_{j=0}^{1} \sum_{k=0}^{1} \omega_2^{jk} \vert k \rangle \langle j \vert
\\
& = \frac{1}{\sqrt{2}} (\omega_2^{0} \vert 0 \rangle \langle 0 \vert + \omega_2^{0} \vert 0 \rangle \langle 1 \vert + \omega_2^{0} \vert 1 \rangle \langle 0 \vert + \omega_2^{1} \vert 1 \rangle \langle 1 \vert)
\\
& = \frac{1}{\sqrt{2}} (\vert 0 \rangle \langle 0 \vert + \vert 0 \rangle \langle 1 \vert + \vert 1 \rangle \langle 0 \vert - \vert 1 \rangle \langle 1 \vert)
\\
& = H
\end{aligned}
$$

Operasi ini adalah hasil daripada menggunakan Gate Hadamard ($H$).
### 2.3 Perwakilan hasil darab QFT {#23-perwakilan-hasil-darab-qft}
Mari kita umumkan transformasi untuk $N = 2^n$, $QFT_{N}$ yang bertindak pada keadaan $\vert x \rangle = \vert x_1\ldots x_n \rangle$.
$$
\begin{aligned}
QFT_N\vert x \rangle & = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1}\omega_N^{xy} \vert y \rangle
\\
& = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1} e^{2 \pi i xy / 2^n} \vert y \rangle ~\text{since}\: \omega_N^{xy} = e^{2\pi i \frac{xy}{N}} \:\text{and}\: N = 2^n
\\
& = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1} e^{2 \pi i \left(\sum_{k=1}^n y_k/2^k\right) x} \vert y_1 \ldots y_n \rangle \:\text{rewriting in fractional binary notation}\: y = y_1\ldots y_n, y/2^n = \sum_{k=1}^n y_k/2^k
\\
& = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1} \prod_{k=1}^n e^{2 \pi i x y_k/2^k } \vert y_1 \ldots y_n \rangle \:\text{after expanding the exponential of a sum to a product of exponentials,}
\\
& = \frac{1}{\sqrt{N}} \bigotimes_{k=1}^n  \left(\vert0\rangle + e^{2 \pi i x /2^k } \vert1\rangle \right) \:\text{after rearranging the sum and products, and expanding}
\sum_{y=0}^{N-1} = \sum_{y_1=0}^{1}\sum_{y_2=0}^{1}\ldots\sum_{y_n=0}^{1}
\\
& = \frac{1}{\sqrt{N}}
\left(\vert0\rangle + e^{\frac{2\pi i}{2}x} \vert1\rangle\right)
\otimes
\left(\vert0\rangle + e^{\frac{2\pi i}{2^2}x} \vert1\rangle\right)
\otimes
\ldots
\otimes
\left(\vert0\rangle + e^{\frac{2\pi i}{2^{n-1}}x} \vert1\rangle\right)
\otimes
\left(\vert0\rangle + e^{\frac{2\pi i}{2^n}x} \vert1\rangle\right)
\end{aligned}
$$
### 2.4 Contoh: Pembinaan Circuit QFT 3 Qubit {#24-contoh-pembinaan-circuit-qft-3-qubit}

Daripada penerangan di atas, mungkin tidak jelas bagaimana membina Circuit QFT. Buat masa ini, cukuplah untuk diperhatikan bahawa kita menjangkakan tiga Qubit mempunyai fasa yang berevolusi pada "kadar" yang berbeza. Memahami bagaimana ini diterjemahkan ke dalam Circuit diserahkan sebagai latihan kepada pembaca. Terdapat pelbagai gambar rajah dan contoh dalam pdf syarahan. Sumber tambahan termasuk [pelajaran ini](/learning/courses/fundamentals-of-quantum-algorithms/phase-estimation-and-factoring/introduction) daripada kursus Asas algoritma kuantum.

Kita akan menunjukkan QFT menggunakan simulator sahaja, oleh itu kita tidak akan menggunakan rangka kerja corak Qiskit sehingga kita beralih kepada anggaran fasa kuantum.

```python
# Prepare for 3 qubits circuit
qr = QuantumRegister(3)
cr = ClassicalRegister(3)
qc = QuantumCircuit(qr, cr)
```

```python
qc.h(2)
qc.cp(pi / 2, 1, 2)  # Controlled-phase gate from qubit 1 to qubit 2
qc.cp(pi / 4, 0, 2)  # Controlled-phase gate from qubit 0 to qubit 2
qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/98cc96b3-f2d8-4d0d-bb57-186ea0919f53-0.avif)

```python
qc.h(1)
qc.cp(pi / 2, 0, 1)  # Controlled-phase gate from qubit 0 to qubit 1

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/eaff67fa-82f9-4402-8fa5-d0f0af824616-0.avif)

```python
qc.h(0)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/43e09a6b-ea86-4f02-9e3a-82317c3b787f-0.avif)

```python
qc.swap(0, 2)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/d7dd1cae-f463-4308-866c-8267e015c6ff-0.avif)

Kita cuba menggunakan QFT pada $|5\rangle$ sebagai contoh.

Pertama, kita sahkan notasi binari bagi integer 5 dan cipta Circuit yang mengekod keadaan 5:

```python
bin(5)
```

```text
'0b101'
```

```python
qc = QuantumCircuit(3)

qc.x(0)
qc.x(2)
qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/db631de5-4619-4e8c-b9d1-c6197650ae33-0.avif)

Kita semak keadaan kuantum menggunakan simulator Aer:

```python
statevector = Statevector(qc)
plot_bloch_multivector(statevector)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/83f34fc7-21f3-408b-9beb-a146cb77d27c-0.avif)

Akhirnya, kita tambah QFT dan lihat keadaan akhir Qubit kita:

```python
qc.h(2)
qc.cp(pi / 2, 1, 2)
qc.cp(pi / 4, 0, 2)

qc.h(1)
qc.cp(pi / 2, 0, 1)

qc.h(0)

qc.swap(0, 2)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/524f24ae-3d86-4be4-99cc-c0c8557b8393-0.avif)

```python
statevector = Statevector(qc)
plot_bloch_multivector(statevector)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/cf4d7152-bb06-4017-922a-5fa97075a67f-0.avif)

Kita dapat lihat fungsi QFT kita telah berfungsi dengan betul. Qubit 0 telah diputar sebanyak $\tfrac{5}{8}$ pusingan penuh, Qubit 1 sebanyak $\tfrac{10}{8}$ pusingan penuh (bersamaan dengan $\tfrac{1}{4}$ pusingan penuh), dan Qubit 2 sebanyak $\tfrac{20}{8}$ pusingan penuh (bersamaan dengan $\tfrac{1}{2}$ pusingan penuh).
### 2.5 Latihan: QFT {#25-latihan-qft}
(1) Laksanakan QFT untuk 4 Qubit.

```python
##your code goes here##
```

(2) Gunakan QFT pada $|14\rangle$, simulasikan dan plot vektor keadaan menggunakan sfera Bloch.

```python
##your code goes here##
```

#### Penyelesaian latihan: QFT {#penyelesaian-latihan-qft}
(1)

```python
qr = QuantumRegister(4)
cr = ClassicalRegister(4)
qc = QuantumCircuit(qr, cr)

qc.h(3)
qc.cp(pi / 2, 2, 3)
qc.cp(pi / 4, 1, 3)
qc.cp(pi / 8, 0, 3)

qc.h(2)
qc.cp(pi / 2, 1, 2)
qc.cp(pi / 4, 0, 2)

qc.h(1)
qc.cp(pi / 2, 0, 1)

qc.h(0)

qc.swap(0, 3)
qc.swap(1, 2)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/02e1d50b-6555-4a1e-a2b9-ba77c77840a1-0.avif)

(2)

```python
bin(14)
```

```text
'0b1110'
```

```python
qc = QuantumCircuit(4)

qc.x(1)
qc.x(2)
qc.x(3)
qc.draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/a01ee72e-8486-426d-a654-21b272b958ff-0.avif)

```python
qc.h(3)
qc.cp(pi / 2, 2, 3)
qc.cp(pi / 4, 1, 3)
qc.cp(pi / 8, 0, 3)

qc.h(2)
qc.cp(pi / 2, 1, 2)
qc.cp(pi / 4, 0, 2)

qc.h(1)
qc.cp(pi / 2, 0, 1)
qc.h(0)

qc.swap(0, 3)
qc.swap(1, 2)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/db8a6229-a864-4f5e-889b-8ccd5561eddb-0.avif)

```python
statevector = Statevector(qc)
plot_bloch_multivector(statevector)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/0d0af6c8-73e7-4e49-9d81-75446373d6c8-0.avif)
## 3. Asas Anggaran Fasa Kuantum (QPE) {#3-basics-of-quantum-phase-estimation-qpe}
Diberi operasi unitary $U$, QPE menganggar $\theta$ dalam $U\vert\psi \rangle =e^{\boldsymbol{2\pi i} \theta }|\psi \rangle$ memandangkan $U$ adalah unitary, semua nilai eigennya mempunyai norma 1.
### 3.1 Prosedur {#31-procedure}
#### 1. Persediaan {#1-setup}
$\vert\psi\rangle$ berada dalam satu set daftar Qubit. Satu set tambahan $n$ Qubit membentuk daftar pengiraan di mana kita akan menyimpan nilai $2^n\theta$:

$$
|\psi_0\rangle = \lvert 0 \rangle^{\otimes n} \lvert \psi \rangle
$$
#### 2. Superposisi {#2-superposition}

Guna operasi Gate Hadamard $n$-bit $H^{\otimes n}$ pada daftar pengiraan:

$$
|\psi_1\rangle = {\frac {1}{2^{\frac {n}{2}}}}\left(|0\rangle +|1\rangle \right)^{\otimes n} \lvert \psi \rangle
$$
#### 3. Operasi Unitary Terkawal {#3-controlled-unitary-operations}

Kita perlu memperkenalkan unitary terkawal $CU$ yang menggunakan operator unitary $U$ pada daftar sasaran hanya jika bit kawalan yang sepadan adalah $|1\rangle$. Memandangkan $U$ adalah operator unitary dengan eigenvector $|\psi\rangle$ supaya $U|\psi \rangle =e^{\boldsymbol{2\pi i} \theta }|\psi \rangle$, ini bermakna:

$$
U^{2^{j}}|\psi \rangle =U^{2^{j}-1}U|\psi \rangle =U^{2^{j}-1}e^{2\pi i\theta }|\psi \rangle =\cdots =e^{2\pi i2^{j}\theta }|\psi \rangle
$$
### 3.2 Contoh: QPE Gate-T {#32-example-t-gate-qpe}
Mari kita gunakan Gate $T$ sebagai contoh QPE dan anggar fasa $\theta$ nya.

$$
T|1\rangle =
\begin{pmatrix}
1 & 0\\
0 & e^\frac{i\pi}{4}\\
\end{pmatrix}
\begin{pmatrix}
0\\
1\\
\end{pmatrix}
= e^\frac{i\pi}{4}|1\rangle
$$

Kita jangka untuk mendapat:

$$
\theta = \frac{1}{8}
$$

di mana

$$
T|1\rangle = e^{2i\pi\theta}|1\rangle
$$
Kita mulakan $\vert\psi\rangle = \vert1\rangle$ bagi eigenvector Gate $T$ dengan menggunakan Gate $X$:

```python
qpe = QuantumCircuit(4, 3)
qpe.x(3)
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/2334e6e2-7ae2-4d4c-9014-3e6041f37ee7-0.avif)

Seterusnya, kita guna Gate Hadamard pada Qubit pengiraan:

```python
for qubit in range(3):
    qpe.h(qubit)
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/9c020053-c369-4cc4-829e-72246d79f82c-0.avif)

Kita lakukan operasi unitary terkawal:

```python
repetitions = 1
for counting_qubit in range(3):
    for i in range(repetitions):
        qpe.cp(pi / 4, counting_qubit, 3)  # This is C-U
    repetitions *= 2
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/0b3bedfc-be73-427d-9eed-22a2d7df60cd-0.avif)

Kita guna transformasi Fourier kuantum songsang untuk menukar keadaan daftar pengiraan, kemudian ukur daftar pengiraan:

```python
from qiskit.circuit.library import QFT
```

```python
# Apply inverse QFT
qpe.append(QFT(3, inverse=True), [0, 1, 2])
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/df8ada87-4279-47bb-acd5-cb432bdfc998-0.avif)

```python
for n in range(3):
    qpe.measure(n, n)
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/5fcc8195-8fc9-47bc-9ef3-9d9d444db812-0.avif)

Kita boleh mensimulasikan menggunakan simulator Aer:

```python
aer_sim = AerSimulator()
shots = 2048

pm = generate_preset_pass_manager(backend=aer_sim, optimization_level=1)
t_qpe = pm.run(qpe)

sampler = Sampler(mode=aer_sim)
job = sampler.run([t_qpe], shots=shots)
result = job.result()
answer = result[0].data.c.get_counts()

plot_histogram(answer)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/b0152311-8f60-4c11-a10e-a35691c0ece5-0.avif)

Kita nampak kita dapat satu keputusan (`001`) dengan kepastian, yang diterjemahkan ke dalam perpuluhan: `1`. Kita kini perlu bahagi keputusan kita (`1`) dengan $2^n$ untuk mendapat $\theta$:

$$
\theta = \frac{1}{2^3} = \frac{1}{8}
$$
 Algoritma Shor membolehkan kita memfaktorkan nombor dengan membina Circuit di mana $\theta$ tidak diketahui dan mendapatkan $\theta$.
### 3.3 Latihan {#33-exercise}

Anggar $\theta = 1/3$ menggunakan 3 Qubit untuk pengiraan dan satu Qubit untuk eigenvector.

$P|1\rangle = e^{i\lambda}|1\rangle$. Memandangkan kita mahu melaksanakan $U|1\rangle = e^{2\pi i \tfrac{1}{3}}|1\rangle$, kita perlu tetapkan $\lambda = \tfrac{2 \pi}{3}$.

```python
##your code goes here##
```

#### Penyelesaian latihan: $\theta = 1/3$ {#solution-of-the-exercise-theta-1-3}

```python
# Create and set up circuit
qpe = QuantumCircuit(4, 3)

# Apply H-Gates to counting qubits:
for qubit in range(3):
    qpe.h(qubit)

# Prepare our eigenstate |psi>:
qpe.x(3)

# Do the controlled-U operations:
angle = 2 * pi / 3
repetitions = 1
for counting_qubit in range(3):
    for i in range(repetitions):
        qpe.cp(angle, counting_qubit, 3)
    repetitions *= 2

# Do the inverse QFT:
qpe.append(QFT(3, inverse=True), [0, 1, 2])

for n in range(3):
    qpe.measure(n, n)

qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/9cb90eab-e6ce-4456-b73e-e4082fed2850-0.avif)

```python
aer_sim = AerSimulator()
shots = 4096

pm = generate_preset_pass_manager(backend=aer_sim, optimization_level=1)
t_qpe = pm.run(qpe)

sampler = Sampler(mode=aer_sim)
job = sampler.run([t_qpe], shots=shots)
result = job.result()
answer = result[0].data.c.get_counts()

plot_histogram(answer)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/e118815f-5d92-4ea1-9849-b178ef84d8a4-0.avif)

## 4. Pelaksanaan menggunakan primitif Sampler Qiskit Runtime {#4-execution-using-the-qiskit-runtime-sampler-primitive}

Kita akan melakukan QPE menggunakan peranti kuantum sebenar dan ikut 4 langkah corak Qiskit.

    1. Petakan masalah ke format natif kuantum
    2. Optimumkan Circuit
    3. Laksanakan Circuit sasaran
    4. Proses keputusan

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import Sampler
```

```python
# Loading your IBM Quantum&reg; account(s)

service = QiskitRuntimeService()
```

### 4.1 Langkah 1: Petakan masalah ke Circuit dan operator kuantum {#41-step-1-map-problem-to-quantum-circuits-and-operators}

```python
qpe = QuantumCircuit(4, 3)
qpe.x(3)
for qubit in range(3):
    qpe.h(qubit)
repetitions = 1
for counting_qubit in range(3):
    for i in range(repetitions):
        qpe.cp(pi / 4, counting_qubit, 3)  # This is C-U
    repetitions *= 2
qpe.append(QFT(3, inverse=True), [0, 1, 2])
for n in range(3):
    qpe.measure(n, n)
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/70c92e35-0e13-4552-99be-50d6950dfedc-0.avif)

```python
backend = service.least_busy(simulator=False, operational=True, min_num_qubits=4)

print(backend)
```

```text
<IBMBackend('ibm_strasbourg')>
```

### 4.2 Langkah 2: Optimumkan untuk perkakasan sasaran {#42-step-2-optimize-for-target-hardware}

```python
# Transpile the circuit into basis gates executable on the hardware
pm = generate_preset_pass_manager(backend=backend, optimization_level=2)
qc_compiled = pm.run(qpe)

qc_compiled.draw("mpl", idle_wires=False)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/d4d57d21-00c6-4144-aa3e-4a467a065078-0.avif)

### 4.3 Langkah 3: Laksanakan pada perkakasan sasaran {#43-step-3-execute-on-target-hardware}

```python
real_sampler = Sampler(mode=backend)
job = real_sampler.run([qc_compiled], shots=1024)
job_id = job.job_id()
print("job id:", job_id)
```

```text
job id: d13p4zb5z6q00087ag00
```

```python
job = service.job(job_id)  # Input your job-id between the quotations
job.status()
```

```text
'DONE'
```

```python
result_real = job.result()
print(result_real)
```

```text
PrimitiveResult([SamplerPubResult(data=DataBin(c=BitArray(<shape=(), num_shots=1024, num_bits=3>)), metadata={'circuit_metadata': {}})], metadata={'execution': {'execution_spans': ExecutionSpans([DoubleSliceSpan(<start='2025-06-09 22:39:00', stop='2025-06-09 22:39:00', size=1024>)])}, 'version': 2})
```

### 4.4 Langkah 4: Proses keputusan {#44-step-4-post-process-the-results}

```python
from qiskit.visualization import plot_histogram

plot_histogram(result_real[0].data.c.get_counts())
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/bd9181eb-dc1e-42e1-bdcf-3bb6b128538d-0.avif)

```python
# See the version of Qiskit
import qiskit

qiskit.__version__
```

```text
'2.0.2'
```
