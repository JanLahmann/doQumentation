---
title: "Kvantový odhad fáze"
sidebar_label: "Kvantový odhad fáze"
description: "V tomto kurzu se seznámíš s QFT, která hraje klíčovou roli v mnoha kvantových algoritmech"
notebook_path: "learning/courses/utility-scale-quantum-computing/quantum-phase-estimation.ipynb"
---
{/* doqumentation-source-hash: 538675c7 */}


<OpenInLabBanner notebookPath="learning/courses/utility-scale-quantum-computing/quantum-phase-estimation.ipynb" />



# Kvantové algoritmy: Odhad fáze {#quantum-algorithms-phase-estimation}

<Admonition type="note">

Kento Ueda (15. května 2024)

Tento notebook představuje základní pojmy a implementaci kvantové Fourierovy transformace (QFT) a kvantového odhadu fáze (QPE).

[Stáhni si pdf](https://ibm.ent.box.com/public/static/srgnll6u8ame7gk186pkthq7erc7l6rw.zip) původní přednášky. Pamatuj, že některé úryvky kódu mohou být zastaralé, protože jde o statické obrázky.

*Přibližný čas QPU potřebný pro tento experiment je 7 sekund.*

</Admonition>
## 1. Úvod {#1-introduction}

**Kvantová Fourierova transformace (QFT)**

Kvantová Fourierova transformace je kvantovým protějškem klasické diskrétní Fourierovy transformace. Jde o lineární transformaci aplikovanou na kvantové stavy, která zobrazuje výpočetní báze do jejich reprezentací ve Fourierově bázi. QFT hraje klíčovou roli v mnoha kvantových algoritmech a poskytuje efektivní metodu pro získávání informací o periodicitě z kvantových stavů.
QFT lze implementovat pomocí $O(N^2)$ operací s kvantovými hradly, jako jsou Hadamardova hradla a Control-Phase hradla pro $N$ Qubitů, což umožňuje exponenciální zrychlení oproti klasické Fourierově transformaci.

- **Aplikace**: Tvoří základ kvantových algoritmů, jako je Shorův algoritmus pro faktorizaci velkých celých čísel a diskrétní logaritmus.

**Kvantový odhad fáze (QPE)**

Kvantový odhad fáze je kvantový algoritmus používaný k odhadu fáze spojené s vlastním vektorem unitárního operátoru. Tento algoritmus vytváří most mezi abstraktními matematickými vlastnostmi kvantových stavů a jejich výpočetními aplikacemi.

- **Aplikace**: Umožňuje řešit úlohy, jako je hledání vlastních čísel unitárních matic a simulace kvantových systémů.

QFT a QPE společně tvoří zásadní páteř mnoha kvantových algoritmů řešících problémy, které jsou pro klasické počítače neproveditelné. Na konci tohoto notebooku získáš představu o tom, jak jsou tyto techniky implementovány.
## 2. Základy kvantové Fourierovy transformace (QFT) {#2-basic-of-quantum-fourier-transformation-qft}

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-aer qiskit-ibm-runtime
```

```python
from qiskit import QuantumCircuit, QuantumRegister, ClassicalRegister
from qiskit_aer import AerSimulator
from qiskit.visualization import plot_histogram, plot_bloch_multivector
from qiskit.quantum_info import Statevector
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager
from qiskit_ibm_runtime import Sampler

from numpy import pi
```

Analogicky s diskrétní Fourierovou transformací QFT působí na kvantový stav $\vert X\rangle = \sum_{j=0}^{N-1} x_j \vert j \rangle$ pro $N$ Qubitů a zobrazuje ho na kvantový stav $\vert Y\rangle = \sum_{k=0}^{N-1} y_k \vert k \rangle$.

$$
QFT_{N}: \vert j \rangle \mapsto \frac{1}{\sqrt{N}}\sum_{k=0}^{N-1}\omega_N^{jk} \vert k \rangle
$$

kde $\omega_N^{jk} = e^{2\pi i \frac{jk}{N}}$.

Nebo reprezentace pomocí unitární matice:

$$
U_{QFT} = \frac{1}{\sqrt{N}} \sum_{j=0}^{N-1} \sum_{k=0}^{N-1} \omega_N^{jk} \vert k \rangle \langle j \vert
$$
### 2.1. Intuice <a id="intuition"></a> {#2-1-intuition}

Kvantová Fourierova transformace (QFT) převádí mezi dvěma bázemi: výpočetní (Z) bází a Fourierovou bází. Co však Fourierova báze v tomto kontextu znamená? Nejspíš si vybavíš, že Fourierova transformace $F(\omega)$ funkce $f(x)$ popisuje konvoluci $f(x)$ se sinusoidální funkcí o frekvenci $\omega$. Laicky řečeno: Fourierova transformace je funkce, která popisuje, kolik z každé frekvence $\omega$ bychom potřebovali, abychom sestavili funkci $f(x)$ ze sinových (nebo kosinových) funkcí. Abys lépe pochopil, co v tomto kontextu QFT znamená, podívej se na níže uvedené sekvenční obrázky, které ukazují číslo zakódované binárně pomocí čtyř Qubitů:

<IBMVideo id="134399598" title="This short video shows how four qubits precessing at different rates can be used to encode digits."/>

Ve výpočetní bázi ukládáme čísla binárně pomocí stavů $|0\rangle$ a $|1\rangle$.

Všimni si frekvence, s jakou se jednotlivé Qubity mění; nejlevější Qubit se překlápí při každém zvýšení čísla, další při každých 2 zvýšeních, třetí při každých 4 zvýšeních a tak dále.

Pokud na tyto stavy aplikujeme kvantovou Fourierovu transformaci, zobrazíme
$$
|\text{State in Computational Basis}\rangle \quad \xrightarrow[]{\text{QFT}} \quad |\text{State in Fourier Basis}\rangle
$$

$$
\text{QFT}|x\rangle = |\widetilde{x}\rangle
$$

(Stavy ve Fourierově bázi často označujeme pomocí vlnovky (~)).

Ve Fourierově bázi ukládáme čísla pomocí různých rotací kolem osy Z:

IFRAME

Úhel, o který se každý Qubit kolem osy Z otočí, určuje číslo, které chceme uložit. Ve stavu $|\widetilde{0}\rangle$ jsou všechny Qubity ve stavu $|{+}\rangle$. Jak je vidět ve výše uvedeném příkladu, k zakódování stavu $|\widetilde{5}\rangle$ na 4 Qubitech jsme nejlevější Qubit otočili o $\tfrac{5}{2^n} = \tfrac{5}{16}$ celé otáčky ($\tfrac{5}{16}\times 2\pi$ radiánů). Další Qubit je otočen o dvojnásobek ($\tfrac{10}{16}\times 2\pi$ radiánů, tedy $10/16$ celé otáčky), tento úhel se pak zdvojnásobuje pro další Qubit a tak dále.

Opět si všimni frekvence, s jakou se jednotlivé Qubity mění. Nejlevější Qubit (`qubit 0`) má v tomto případě nejnižší frekvenci a ten nejpravější nejvyšší.
### 2.2 Příklad: 1qubitová QFT {#2-2-example-1-qubit-qft}
Uvažujme případ $N = 2^1 = 2$.

Unitární matici lze zapsat:

$$
\begin{aligned}
U_{QFT} & = \frac{1}{\sqrt{2}} \sum_{j=0}^{1} \sum_{k=0}^{1} \omega_2^{jk} \vert k \rangle \langle j \vert
\\
& = \frac{1}{\sqrt{2}} (\omega_2^{0} \vert 0 \rangle \langle 0 \vert + \omega_2^{0} \vert 0 \rangle \langle 1 \vert + \omega_2^{0} \vert 1 \rangle \langle 0 \vert + \omega_2^{1} \vert 1 \rangle \langle 1 \vert)
\\
& = \frac{1}{\sqrt{2}} (\vert 0 \rangle \langle 0 \vert + \vert 0 \rangle \langle 1 \vert + \vert 1 \rangle \langle 0 \vert - \vert 1 \rangle \langle 1 \vert)
\\
& = H
\end{aligned}
$$

Tato operace je výsledkem aplikace Hadamardova Gate ($H$).
### 2.3 Reprezentace QFT jako součinu {#2-3-product-representation-of-qft}
Zobecněme transformaci pro $N = 2^n$, $QFT_{N}$ působící na stav $\vert x \rangle = \vert x_1\ldots x_n \rangle$.
$$
\begin{aligned}
QFT_N\vert x \rangle & = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1}\omega_N^{xy} \vert y \rangle
\\
& = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1} e^{2 \pi i xy / 2^n} \vert y \rangle ~\text{protože}\: \omega_N^{xy} = e^{2\pi i \frac{xy}{N}} \:\text{a}\: N = 2^n
\\
& = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1} e^{2 \pi i \left(\sum_{k=1}^n y_k/2^k\right) x} \vert y_1 \ldots y_n \rangle \:\text{přepsáno do zlomkové binární notace}\: y = y_1\ldots y_n, y/2^n = \sum_{k=1}^n y_k/2^k
\\
& = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1} \prod_{k=1}^n e^{2 \pi i x y_k/2^k } \vert y_1 \ldots y_n \rangle \:\text{po rozepsání exponenciály součtu na součin exponenciál,}
\\
& = \frac{1}{\sqrt{N}} \bigotimes_{k=1}^n  \left(\vert0\rangle + e^{2 \pi i x /2^k } \vert1\rangle \right) \:\text{po přerovnání sum a součinů a rozepsání}
\sum_{y=0}^{N-1} = \sum_{y_1=0}^{1}\sum_{y_2=0}^{1}\ldots\sum_{y_n=0}^{1}
\\
& = \frac{1}{\sqrt{N}}
\left(\vert0\rangle + e^{\frac{2\pi i}{2}x} \vert1\rangle\right)
\otimes
\left(\vert0\rangle + e^{\frac{2\pi i}{2^2}x} \vert1\rangle\right)
\otimes
\ldots
\otimes
\left(\vert0\rangle + e^{\frac{2\pi i}{2^{n-1}}x} \vert1\rangle\right)
\otimes
\left(\vert0\rangle + e^{\frac{2\pi i}{2^n}x} \vert1\rangle\right)
\end{aligned}
$$
### 2.4 Příklad: Konstrukce obvodu pro 3qubitovou QFT {#2-4-example-circuit-construction-of-3-qubits-qft}

Z výše uvedeného popisu nemusí být zřejmé, jak sestrojit QFT Circuit. Prozatím si jen všimni, že očekáváme, že tři Qubits budou mít fáze, které se vyvíjejí různými „rychlostmi“. Přesné pochopení, jak se to promítá do Circuit, je ponecháno jako cvičení pro čtenáře. V přednáškovém pdf najdeš řadu diagramů a příkladů. Mezi další zdroje patří [tato lekce](/learning/courses/fundamentals-of-quantum-algorithms/phase-estimation-and-factoring/introduction) z kurzu Fundamentals of quantum algorithms.

QFT budeme demonstrovat pouze pomocí simulátorů, a tak nebudeme používat framework Qiskit patterns, dokud nepřejdeme ke kvantovému odhadu fáze.

```python
# Prepare for 3 qubits circuit
qr = QuantumRegister(3)
cr = ClassicalRegister(3)
qc = QuantumCircuit(qr, cr)
```

```python
qc.h(2)
qc.cp(pi / 2, 1, 2)  # Controlled-phase gate from qubit 1 to qubit 2
qc.cp(pi / 4, 0, 2)  # Controlled-phase gate from qubit 0 to qubit 2
qc.draw(output="mpl")
```

![Výstup předchozí buňky kódu](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/98cc96b3-f2d8-4d0d-bb57-186ea0919f53-0.avif)

```python
qc.h(1)
qc.cp(pi / 2, 0, 1)  # Controlled-phase gate from qubit 0 to qubit 1

qc.draw(output="mpl")
```

![Výstup předchozí buňky kódu](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/eaff67fa-82f9-4402-8fa5-d0f0af824616-0.avif)

```python
qc.h(0)

qc.draw(output="mpl")
```

![Výstup předchozí buňky kódu](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/43e09a6b-ea86-4f02-9e3a-82317c3b787f-0.avif)

```python
qc.swap(0, 2)

qc.draw(output="mpl")
```

![Výstup předchozí buňky kódu](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/d7dd1cae-f463-4308-866c-8267e015c6ff-0.avif)

Jako příklad zkusíme aplikovat QFT na $|5\rangle$.

Nejprve potvrdíme binární zápis celého čísla 5 a vytvoříme Circuit kódující stav 5:

```python
bin(5)
```

```text
'0b101'
```

```python
qc = QuantumCircuit(3)

qc.x(0)
qc.x(2)
qc.draw(output="mpl")
```

![Výstup předchozí buňky kódu](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/db631de5-4619-4e8c-b9d1-c6197650ae33-0.avif)

Kvantové stavy zkontrolujeme pomocí simulátoru Aer:

```python
statevector = Statevector(qc)
plot_bloch_multivector(statevector)
```

![Výstup předchozí buňky kódu](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/83f34fc7-21f3-408b-9beb-a146cb77d27c-0.avif)

Nakonec přidáme QFT a podíváme se na výsledný stav našich Qubits:

```python
qc.h(2)
qc.cp(pi / 2, 1, 2)
qc.cp(pi / 4, 0, 2)

qc.h(1)
qc.cp(pi / 2, 0, 1)

qc.h(0)

qc.swap(0, 2)

qc.draw(output="mpl")
```

![Výstup předchozí buňky kódu](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/524f24ae-3d86-4be4-99cc-c0c8557b8393-0.avif)

```python
statevector = Statevector(qc)
plot_bloch_multivector(statevector)
```

![Výstup předchozí buňky kódu](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/cf4d7152-bb06-4017-922a-5fa97075a67f-0.avif)

Vidíme, že naše funkce QFT zafungovala správně. Qubit 0 byl otočen o $\tfrac{5}{8}$ plné otáčky, qubit 1 o $\tfrac{10}{8}$ plné otáčky (což odpovídá $\tfrac{1}{4}$ plné otáčky) a qubit 2 o $\tfrac{20}{8}$ plné otáčky (což odpovídá $\tfrac{1}{2}$ plné otáčky).
### 2.5 Cvičení: QFT {#2-5-exercise-qft}
(1) Implementuj QFT pro 4 qubity.

```python
##your code goes here##
```

(2) Aplikuj QFT na $|14\rangle$, simuluj a vykresli stavový vektor pomocí Blochovy sféry.

```python
##your code goes here##
```

#### Řešení cvičení: QFT {#solution-of-the-exercise-qft}
(1)

```python
qr = QuantumRegister(4)
cr = ClassicalRegister(4)
qc = QuantumCircuit(qr, cr)

qc.h(3)
qc.cp(pi / 2, 2, 3)
qc.cp(pi / 4, 1, 3)
qc.cp(pi / 8, 0, 3)

qc.h(2)
qc.cp(pi / 2, 1, 2)
qc.cp(pi / 4, 0, 2)

qc.h(1)
qc.cp(pi / 2, 0, 1)

qc.h(0)

qc.swap(0, 3)
qc.swap(1, 2)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/02e1d50b-6555-4a1e-a2b9-ba77c77840a1-0.avif)

(2)

```python
bin(14)
```

```text
'0b1110'
```

```python
qc = QuantumCircuit(4)

qc.x(1)
qc.x(2)
qc.x(3)
qc.draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/a01ee72e-8486-426d-a654-21b272b958ff-0.avif)

```python
qc.h(3)
qc.cp(pi / 2, 2, 3)
qc.cp(pi / 4, 1, 3)
qc.cp(pi / 8, 0, 3)

qc.h(2)
qc.cp(pi / 2, 1, 2)
qc.cp(pi / 4, 0, 2)

qc.h(1)
qc.cp(pi / 2, 0, 1)
qc.h(0)

qc.swap(0, 3)
qc.swap(1, 2)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/db8a6229-a864-4f5e-889b-8ccd5561eddb-0.avif)

```python
statevector = Statevector(qc)
plot_bloch_multivector(statevector)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/0d0af6c8-73e7-4e49-9d81-75446373d6c8-0.avif)
## 3. Základy odhadu kvantové fáze (QPE) {#3-basics-of-quantum-phase-estimation-qpe}
Pro danou unitární operaci $U$ QPE odhaduje $\theta$ v $U\vert\psi \rangle =e^{\boldsymbol{2\pi i} \theta }|\psi \rangle$, protože $U$ je unitární, všechny jeho vlastní hodnoty mají normu 1.
### 3.1 Postup {#31-procedure}
#### 1. Nastavení {#1-setup}
$\vert\psi\rangle$ se nachází v jedné sadě qubitových registrů. Další sada $n$ qubitů tvoří čítací registr, do kterého uložíme hodnotu $2^n\theta$:

$$
|\psi_0\rangle = \lvert 0 \rangle^{\otimes n} \lvert \psi \rangle
$$
#### 2. Superpozice {#2-superposition}

Aplikuj $n$-bitovou operaci Hadamardova Gate $H^{\otimes n}$ na čítací registr:

$$
|\psi_1\rangle = {\frac {1}{2^{\frac {n}{2}}}}\left(|0\rangle +|1\rangle \right)^{\otimes n} \lvert \psi \rangle
$$
#### 3. Řízené unitární operace {#3-controlled-unitary-operations}

Potřebujeme zavést řízené unitární $CU$, které aplikuje unitární operátor $U$ na cílový registr pouze tehdy, pokud příslušný řídicí bit je $|1\rangle$. Protože $U$ je unitární operátor s vlastním vektorem $|\psi\rangle$ takovým, že $U|\psi \rangle =e^{\boldsymbol{2\pi i} \theta }|\psi \rangle$, platí:

$$
U^{2^{j}}|\psi \rangle =U^{2^{j}-1}U|\psi \rangle =U^{2^{j}-1}e^{2\pi i\theta }|\psi \rangle =\cdots =e^{2\pi i2^{j}\theta }|\psi \rangle
$$
### 3.2 Příklad: T-gate QPE {#32-example-t-gate-qpe}
Použijme T-gate jako příklad QPE a odhadněme jeho fázi $\theta$.

$$
T|1\rangle =
\begin{pmatrix}
1 & 0\\
0 & e^\frac{i\pi}{4}\\
\end{pmatrix}
\begin{pmatrix}
0\\
1\\
\end{pmatrix}
= e^\frac{i\pi}{4}|1\rangle
$$

Očekáváme, že najdeme:

$$
\theta = \frac{1}{8}
$$

kde

$$
T|1\rangle = e^{2i\pi\theta}|1\rangle
$$
Inicializujeme $\vert\psi\rangle = \vert1\rangle$ vlastního vektoru T-gate aplikací Gate $X$:

```python
qpe = QuantumCircuit(4, 3)
qpe.x(3)
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/2334e6e2-7ae2-4d4c-9014-3e6041f37ee7-0.avif)

Dále aplikujeme Hadamardovy Gate na čítací Qubit:

```python
for qubit in range(3):
    qpe.h(qubit)
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/9c020053-c369-4cc4-829e-72246d79f82c-0.avif)

Provedeme řízené unitární operace:

```python
repetitions = 1
for counting_qubit in range(3):
    for i in range(repetitions):
        qpe.cp(pi / 4, counting_qubit, 3)  # This is C-U
    repetitions *= 2
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/0b3bedfc-be73-427d-9eed-22a2d7df60cd-0.avif)

Aplikujeme inverzní kvantovou Fourierovu transformaci pro převod stavu čítacího registru a poté změříme čítací registr:

```python
from qiskit.circuit.library import QFT
```

```python
# Apply inverse QFT
qpe.append(QFT(3, inverse=True), [0, 1, 2])
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/df8ada87-4279-47bb-acd5-cb432bdfc998-0.avif)

```python
for n in range(3):
    qpe.measure(n, n)
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/5fcc8195-8fc9-47bc-9ef3-9d9d444db812-0.avif)

Můžeme simulovat pomocí simulátoru Aer:

```python
aer_sim = AerSimulator()
shots = 2048

pm = generate_preset_pass_manager(backend=aer_sim, optimization_level=1)
t_qpe = pm.run(qpe)

sampler = Sampler(mode=aer_sim)
job = sampler.run([t_qpe], shots=shots)
result = job.result()
answer = result[0].data.c.get_counts()

plot_histogram(answer)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/b0152311-8f60-4c11-a10e-a35691c0ece5-0.avif)

Vidíme, že s jistotou dostaneme jeden výsledek (`001`), který se převede na desetinné číslo: `1`. Nyní potřebujeme náš výsledek (`1`) vydělit $2^n$, abychom dostali $\theta$:

$$
\theta = \frac{1}{2^3} = \frac{1}{8}
$$
 Shorův algoritmus nám umožňuje faktorizovat číslo tím, že sestavíme Circuit s neznámým $\theta$ a získáme $\theta$.
### 3.3 Cvičení {#3-3-exercise}

Odhadni $\theta = 1/3$ pomocí 3 qubitů pro počítání a jednoho qubitu pro vlastní vektor.

$P|1\rangle = e^{i\lambda}|1\rangle$. Protože chceme implementovat $U|1\rangle = e^{2\pi i \tfrac{1}{3}}|1\rangle$, musíme nastavit $\lambda = \tfrac{2 \pi}{3}$.

```python
##your code goes here##
```

#### Řešení cvičení: $\theta = 1/3$ {#reseni-cviceni-theta-1-3}

```python
# Create and set up circuit
qpe = QuantumCircuit(4, 3)

# Apply H-Gates to counting qubits:
for qubit in range(3):
    qpe.h(qubit)

# Prepare our eigenstate |psi>:
qpe.x(3)

# Do the controlled-U operations:
angle = 2 * pi / 3
repetitions = 1
for counting_qubit in range(3):
    for i in range(repetitions):
        qpe.cp(angle, counting_qubit, 3)
    repetitions *= 2

# Do the inverse QFT:
qpe.append(QFT(3, inverse=True), [0, 1, 2])

for n in range(3):
    qpe.measure(n, n)

qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/9cb90eab-e6ce-4456-b73e-e4082fed2850-0.avif)

```python
aer_sim = AerSimulator()
shots = 4096

pm = generate_preset_pass_manager(backend=aer_sim, optimization_level=1)
t_qpe = pm.run(qpe)

sampler = Sampler(mode=aer_sim)
job = sampler.run([t_qpe], shots=shots)
result = job.result()
answer = result[0].data.c.get_counts()

plot_histogram(answer)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/e118815f-5d92-4ea1-9849-b178ef84d8a4-0.avif)
## 4. Spuštění pomocí primitivy Qiskit Runtime Sampler {#4-execution-using-the-qiskit-runtime-sampler-primitive}

Provedeme QPE na skutečném kvantovém zařízení a budeme postupovat podle 4 kroků Qiskit patterns.

    1. Namapuj problém do kvantově nativního formátu
    2. Optimalizuj obvody
    3. Spusť cílový Circuit
    4. Post-procesuj výsledky

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import Sampler
```

```python
# Loading your IBM Quantum&reg; account(s)

service = QiskitRuntimeService()
```

### 4.1 Krok 1: Namapuj problém na kvantové obvody a operátory {#41-step-1-map-problem-to-quantum-circuits-and-operators}

```python
qpe = QuantumCircuit(4, 3)
qpe.x(3)
for qubit in range(3):
    qpe.h(qubit)
repetitions = 1
for counting_qubit in range(3):
    for i in range(repetitions):
        qpe.cp(pi / 4, counting_qubit, 3)  # This is C-U
    repetitions *= 2
qpe.append(QFT(3, inverse=True), [0, 1, 2])
for n in range(3):
    qpe.measure(n, n)
qpe.draw(output="mpl")
```

![Výstup předchozí buňky s kódem](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/70c92e35-0e13-4552-99be-50d6950dfedc-0.avif)

```python
backend = service.least_busy(simulator=False, operational=True, min_num_qubits=4)

print(backend)
```

```text
<IBMBackend('ibm_strasbourg')>
```

### 4.2 Krok 2: Optimalizace pro cílový hardware {#42-step-2-optimize-for-target-hardware}

```python
# Transpile the circuit into basis gates executable on the hardware
pm = generate_preset_pass_manager(backend=backend, optimization_level=2)
qc_compiled = pm.run(qpe)

qc_compiled.draw("mpl", idle_wires=False)
```

![Výstup předchozí buňky s kódem](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/d4d57d21-00c6-4144-aa3e-4a467a065078-0.avif)

### 4.3 Krok 3: Spuštění na cílovém hardware {#43-step-3-execute-on-target-hardware}

```python
real_sampler = Sampler(mode=backend)
job = real_sampler.run([qc_compiled], shots=1024)
job_id = job.job_id()
print("job id:", job_id)
```

```text
job id: d13p4zb5z6q00087ag00
```

```python
job = service.job(job_id)  # Input your job-id between the quotations
job.status()
```

```text
'DONE'
```

```python
result_real = job.result()
print(result_real)
```

```text
PrimitiveResult([SamplerPubResult(data=DataBin(c=BitArray(<shape=(), num_shots=1024, num_bits=3>)), metadata={'circuit_metadata': {}})], metadata={'execution': {'execution_spans': ExecutionSpans([DoubleSliceSpan(<start='2025-06-09 22:39:00', stop='2025-06-09 22:39:00', size=1024>)])}, 'version': 2})
```

### 4.4 Krok 4: Post-procesování výsledků {#44-step-4-post-process-the-results}

```python
from qiskit.visualization import plot_histogram

plot_histogram(result_real[0].data.c.get_counts())
```

![Výstup předchozí buňky s kódem](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/bd9181eb-dc1e-42e1-bdcf-3bb6b128538d-0.avif)

```python
# See the version of Qiskit
import qiskit

qiskit.__version__
```

```text
'2.0.2'
```
