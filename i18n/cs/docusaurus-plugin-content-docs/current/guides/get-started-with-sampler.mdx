---
description: 'Úvodní příručka, jak používat primitivum IBM Quantum Sampler.'
notebook_path: guides/get-started-with-sampler.ipynb
sidebar_label: 'Rychlý start se Samplerem'
title: 'Rychlý start se Samplerem'
---

{/* doqumentation-source-hash: 86291a21 */}


<OpenInLabBanner notebookPath="guides/get-started-with-sampler.ipynb" />



Hlavním úkolem Sampleru je vzorkování výstupního registru z provádění jednoho nebo více kvantových obvodů. Jako vstup jsou přijímány [dynamické obvody](/guides/execute-dynamic-circuits) a parametrizované obvody (pokud jsou odeslány parametrizované obvody, musí být také poskytnuty hodnoty parametrů). Sampler také podporuje vestavěné dynamické odpojování a twirling pro [potlačení chyb](/guides/error-mitigation-and-suppression-techniques).

Kroky v tomto tématu popisují, jak nastavit Sampler, prozkoumat možnosti, které můžeš použít pro jeho konfiguraci, a spustit ho v programu.
{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Verze balíčků">

Kód na této stránce byl vyvinut s použitím následujících požadavků.
Doporučujeme používat tyto verze nebo novější.

```
qiskit[all]~=2.5.2
qiskit-ibm-runtime~=0.47.0
```
</AccordionItem>
</Accordion>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-ibm-runtime
```

## Kroky k použití primitiva Sampler {#steps-to-use-the-sampler-primitive}

### 1. Inicializace účtu {#1-initialize-the-account}

Nejprve musíš inicializovat svůj účet. Poté můžeš vybrat QPU, které chceš použít pro výpočet střední hodnoty.

Pokud ještě nemáš nastavený účet, postupuj podle kroků v tématu [Nastavení účtu IBM Cloud](/guides/cloud-setup).

:::note[Zlomková hradla]


Chceš-li používat nově podporovaná [zlomková hradla](/guides/fractional-gates), nastav `use_fractional_gates=True` při žádosti o backend z instance `QiskitRuntimeService`. Například:

```python
service = QiskitRuntimeService()
fractional_gate_backend = service.least_busy(use_fractional_gates=True)
```

Toto je experimentální funkce a může se v budoucnu změnit.

:::


```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
backend = service.least_busy(
    operational=True, simulator=False, min_num_qubits=127
)
```

### 2. Vytvoření obvodu {#2-create-a-circuit}

Pro primitivum Sampler potřebuješ alespoň jeden obvod jako vstup.

```python
import numpy as np
from qiskit.circuit.library import efficient_su2

circuit = efficient_su2(127, entanglement="linear")
circuit.measure_all()
# The circuit is parametrized, so we will define the parameter values for execution
param_values = np.random.rand(circuit.num_parameters)
```

Obvod a pozorovatelná veličina musí být transformovány tak, aby používaly pouze instrukce podporované QPU (označované jako obvody *instrukční sady architektury (ISA)*). K tomu použij transpiler.

```python
from qiskit.transpiler import generate_preset_pass_manager

pm = generate_preset_pass_manager(optimization_level=1, backend=backend)
isa_circuit = pm.run(circuit)
print(f">>> Circuit ops (ISA): {isa_circuit.count_ops()}")
```

```text
>>> Circuit ops (ISA): OrderedDict([('rz', 3036), ('sx', 1769), ('cz', 378), ('measure', 127), ('barrier', 1)])
```

### 3. Inicializace IBM Quantum Sampleru {#3-initialize-the-ibm-quantum-sampler}

Při inicializaci Sampleru použij parametr `mode` k určení režimu, ve kterém chceš spustit. Možné hodnoty jsou objekty `batch`, `session` nebo `backend` pro dávkový, session a job prováděcí režim. Více informací najdeš v části [Úvod do IBM Quantum Compute Service prováděcích režimů](/guides/execution-modes). Vezmi v úvahu, že uživatelé plánu Open Plan nemohou odesílat session úlohy.

```python
from qiskit_ibm_runtime import SamplerV2 as Sampler

sampler = Sampler(mode=backend)
```

### 4. Spuštění Sampleru a získání výsledků {#4-invoke-sampler-and-get-results}

Dále spusť metodu `run()` pro generování výstupu. Obvod a volitelné sady hodnot parametrů jsou jako vstup zadány ve formě n-tic *primitive unified bloc* (PUB).

```python
job = sampler.run([(isa_circuit, param_values)])
print(f">>> Job ID: {job.job_id()}")
print(f">>> Job Status: {job.status()}")
```

```text
>>> Job ID: dab7lb6rrl7c7386gjgg
```

```text
>>> Job Status: QUEUED
```

```python
result = job.result()

# Get results for the first (and only) PUB
pub_result = result[0]
print(
    f"First ten results for the 'meas' output register: "
    f"{pub_result.data.meas.get_bitstrings()[:10]}"
)
```

```text
First ten results for the 'meas' output register: ['1110001000100100100111011000001001000101010000001100010010110011001100110111100000000110001000011001101000110100101110101010011', '1110101100010101010001010111111011000110011010100101001100110101010010101010101110001010100100000000011110101101011000100000100', '1100110011101000110000000100110111101010110001100010111100001101100001010100111001101010101110000101010001001101010101001000100', '1011001001011000000101010111111010011101001111010100001011110011000101110101011001100110011011110011101010000111001111110110011', '1110111011011111101011110101110101010110011001100110101111100100011101100011011100101100101001001111110101111010101110111111011', '0101001110001110010001010100010111011001000000011110000000000001110000010010101101011001000100000001011011101111010010110111110', '1101101001000100010000110001000101010010101011000011000011001010000111100110010101010110100110011010101100001011100110001111011', '1000110011111111100010101010101111001001100001110111101001100111010101010111110110011000110011010010010000100000100010010010101', '0101010100110011110011010000000100011001111100101100101100101111101110100001101101111010100011011000010011111010001100110000010', '0010000111011100000000110000001110001100010011000100101110111111100011001100011001101010011111111100000000011100101101111010100']
```

## Další kroky {#next-steps}

<Admonition type="tip" title="Doporučení">
- Zjisti, jak [testovat lokálně](local-testing-mode) před spuštěním na kvantových počítačích.
- Projdi si podrobné [příklady](sampler-examples).
- Procvič práci s primitivy v [lekci cost function](/learning/courses/variational-algorithm-design/cost-functions) v IBM Quantum Learning.
- Zjisti, jak transpilovat lokálně v sekci [Transpilace](transpile/).
- Vyzkoušej průvodce [Porovnat nastavení transpileru](/guides/circuit-transpilation-settings#compare-transpiler-settings).
- Zjisti, jak [používat možnosti primitiv](/guides/runtime-options-overview).
- Zobrazit API pro možnosti [Sampleru](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-sampler-options).
</Admonition>
