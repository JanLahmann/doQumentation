---
description: 'Zadej možnosti při sestavování s primitivem Estimator.'
notebook_path: guides/estimator-options.ipynb
sidebar_label: 'Zadávání možností Estimátoru'
title: 'Zadávání možností Estimátoru'
---

{/* doqumentation-source-hash: 93acc147 */}


<OpenInLabBanner notebookPath="guides/estimator-options.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Verze balíčků">

Kód na této stránce byl vyvinut za použití následujících požadavků.
Doporučujeme používat tyto verze nebo novější.

```
qiskit[all]~=2.5.2
qiskit-ibm-runtime~=0.47.0
```
</AccordionItem>
</Accordion>
Možnosti můžeš použít k přizpůsobení primitivu Estimator. Ačkoli je rozhraní metody `run()` primitivů společné pro všechny implementace, jejich možnosti nikoli. Prostuduj si reference API pro informace o možnostech [`qiskit.primitives.BaseEstimatorV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.primitives.BaseEstimatorV2) a [`qiskit_aer.BaseEstimatorV2`](https://qiskit.github.io/qiskit-aer/stubs/qiskit_aer.primitives.EstimatorV2.html).

Poznámky:
<Admonition type="note" title="Poznámky k zadávání možností v primitivech Estimator">
- Dostupné možnosti si můžeš prohlédnout a aktualizovat jejich hodnoty během inicializace Estimátoru nebo po ní.
- K aplikování změn atributu `options` použij metodu `update()`.
- Pokud pro možnost nezadáš hodnotu, obdrží speciální hodnotu `Unset` a použijí se výchozí hodnoty serveru.
- Atribut `options` je Python typ `dataclass`. Ke konverzi na slovník můžeš použít vestavěnou metodu `asdict`.
</Admonition>

<span id="pass-options"></span>

## Nastavení možností Estimátoru {#set-estimator-options}

Možnosti můžeš nastavit při inicializaci Estimátoru, po inicializaci Estimátoru nebo (pouze pro `precision`) v metodě `run()`.

### Inicializace primitivu {#primitive-initialization}

Při inicializaci Estimátoru můžeš předat instanci třídy options nebo slovník, přičemž Estimator vytvoří kopii těchto možností. Změna původního slovníku nebo instance options proto neovlivní možnosti vlastněné primitivem.

#### Třída Options {#options-class}

Při vytváření instance třídy `EstimatorV2` můžeš předat instanci třídy options. Tyto možnosti budou poté aplikovány při použití metody `run()` k provedení výpočtu. Možnosti zadávej v tomto formátu: `options.option.sub-option.sub-sub-option = choice`. Například: `options.dynamical_decoupling.enable = True`

Příklad:

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit qiskit-ibm-runtime
```

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import EstimatorV2 as Estimator
from qiskit_ibm_runtime.options import EstimatorOptions

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

options = EstimatorOptions(
    resilience_level=2,
    resilience={"zne_mitigation": True, "zne": {"noise_factors": [1, 3, 5]}},
)

# or...
options = EstimatorOptions()
options.resilience_level = 2
options.resilience.zne_mitigation = True
options.resilience.zne.noise_factors = [1, 3, 5]

estimator = Estimator(mode=backend, options=options)
```

#### Slovník {#dictionary}

Možnosti pro inicializaci Estimátoru můžeš zadat jako slovník.

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import EstimatorV2 as Estimator

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

# Setting options during initialization
estimator = Estimator(
    backend,
    options={
        "resilience_level": 2,
        "resilience": {
            "zne_mitigation": True,
            "zne": {"noise_factors": [1, 3, 5]},
        },
    },
)
```

### Aktualizace možností po inicializaci {#update-options-after-initialization}

Možnosti můžeš zadat v tomto formátu: `estimator.options.option.sub-option.sub-sub-option = choice`, abys využil/a automatické doplňování, nebo použij metodu `update()` pro hromadné aktualizace.

Třída options `EstimatorV2` ([`EstimatorOptions`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options)) nemusí být instanciována, pokud nastavuješ možnosti po inicializaci primitivu.

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import EstimatorV2 as Estimator

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

estimator = Estimator(mode=backend)

# Setting options after initialization
# This uses auto-complete.
estimator.options.default_precision = 0.01
# This does bulk update.
estimator.options.update(
    default_precision=0.02, resilience={"zne_mitigation": True}
)
```

<span id="run-method"></span>

### Metoda Run() {#run-method}

Jediné hodnoty, které můžeš předat do `run()`, jsou ty definované v rozhraní, tedy `precision` pro Estimátor. Tímto se přepíše hodnota nastavená pro `default_precision` pro aktuální spuštění.

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import EstimatorV2 as Estimator
from qiskit.circuit.library import random_iqp
from qiskit.transpiler import generate_preset_pass_manager
from qiskit.quantum_info import SparsePauliOp

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

circuit1 = random_iqp(3)
circuit1.measure_all()
circuit2 = random_iqp(3)
circuit2.measure_all()

observable = SparsePauliOp("Z" * 3)

pass_manager = generate_preset_pass_manager(
    optimization_level=3, backend=backend
)

transpiled1 = pass_manager.run(circuit1)
transpiled2 = pass_manager.run(circuit2)
isa_observable1 = observable.apply_layout(transpiled1.layout)
isa_observable2 = observable.apply_layout(transpiled2.layout)

estimator = Estimator(mode=backend)
# Default precision to use if not specified in run()
estimator.options.default_precision = 0.01
# Run two circuits, requiring a precision of .02 for both.
estimator.run(
    [(transpiled1, isa_observable1), (transpiled2, isa_observable2)],
    precision=0.02,
)
```

```text
<RuntimeJobV2('dab7lhjvpcac73ddekcg', 'estimator')>
```

### Speciální případ: přesnost {#special-case-precision}

Metoda `EstimatorV2.run` přijímá dva argumenty: seznam PUBů, z nichž každý může specifikovat PUB-specifickou hodnotu přesnosti, a klíčový argument precision. Tyto hodnoty přesnosti jsou součástí rozhraní pro spouštění Estimátoru a jsou nezávislé na možnostech Estimátoru Runtime. Mají přednost před hodnotami zadanými jako možnosti, aby byla zachována shoda s abstrakcí Estimátoru.

Pokud však `precision` není zadána žádným PUBem ani v klíčovém argumentu run (nebo pokud jsou všechny `None`), použije se hodnota přesnosti z možností, zejména `default_precision`.

<Admonition type="note">
Tyto parametry přesnosti slouží pouze k zadání _cílové_ přesnosti a výsledky nejsou garantovány na dosažení zadané přesnosti.
</Admonition>

Všimni si, že možnosti Estimátoru obsahují jak `default_shots`, tak `default_precision`. Protože je však twirling hradel ve výchozím nastavení povolen, součin `num_randomizations` a `shots_per_randomization` má přednost před těmito dvěma možnostmi.

Konkrétně pro libovolný PUB Estimátoru:

1. Pokud PUB specifikuje přesnost, použij tuto hodnotu.

2. Pokud je klíčový argument precision zadán v `run`, použij tuto hodnotu.

3. Pokud je povoleno `twirling` (ve výchozím nastavení True), použije se součin `num_randomizations` a `shots_per_randomization`, zadaný jako [`twirling` options](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-twirling-options).

4. Pokud je zadáno `estimator.options.default_shots`, použij tuto hodnotu k řízení množství dat.

5. Pokud je zadáno `estimator.options.default_precision`, použij tuto hodnotu.


Například pokud je přesnost zadána na všech čtyřech místech, použije se ta s nejvyšší prioritou (přesnost zadaná v PUBu).

<Admonition type="note">
Ačkoli přesnost zadaná v PUBu a v `run` mají vyšší prioritu, úloha selže, pokud je povoleno `twirling` a součin `num_randomizations` a `shots_per_randomization` je menší než počet shotů potřebných k dosažení dané přesnosti. V tomto scénáři není `EstimatorV2` schopen rozdělit shoty mezi zadané `num_randomizations`.
</Admonition>

<Admonition type="note">
Přesnost se škáluje nepřímo k využití. To znamená, čím nižší přesnost, tím více času QPU je potřeba ke spuštění.
</Admonition>

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import EstimatorV2 as Estimator
from qiskit.circuit.library import random_iqp
from qiskit.transpiler import generate_preset_pass_manager
from qiskit.quantum_info import SparsePauliOp

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

observable = SparsePauliOp("Z" * 3)

circuit = random_iqp(3)
circuit.measure_all()

pass_manager = generate_preset_pass_manager(
    optimization_level=3, backend=backend
)

isa_circuit = pass_manager.run(circuit)
isa_observable = observable.apply_layout(isa_circuit.layout)

# Setting precision during primitive initialization
estimator = Estimator(mode=backend, options={"default_precision": 0.05})

# Run with precision=0.02, overwriting the default.
estimator.run(
    [(isa_circuit, isa_observable1)],
    precision=0.02,
)
```

```text
<RuntimeJobV2('dab7ljjvpcac73ddekgg', 'estimator')>
```

<span id="no-error-mitigation"></span>

## Vypnutí veškerého zmírňování a potlačování chyb {#turn-off-all-error-mitigation-and-error-suppression}

Veškeré zmírňování a potlačování chyb můžeš vypnout, pokud například provádíš výzkum vlastních technik zmírňování. Toho dosáhneš nastavením `resilience_level = 0`.

Příklad:

```python
from qiskit_ibm_runtime import EstimatorV2 as Estimator, QiskitRuntimeService

# Define the service.  This allows you to access an IBM QPU.
service = QiskitRuntimeService()

# Get a backend
backend = service.least_busy(operational=True, simulator=False)

# Define Estimator
estimator = Estimator(backend)

options = estimator.options

# Turn off all error mitigation and suppression
options.resilience_level = 0
```

<span id="options-table"></span>

## Dostupné možnosti {#available-options}

Následující tabulka dokumentuje možnosti z nejnovější verze `qiskit-ibm-runtime`. Chceš-li zobrazit starší verze možností, navštiv [referenci API `qiskit-ibm-runtime`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime) a vyber předchozí verzi.

<Accordion>
<AccordionItem title="`default_shots`">

Celkový počet shotů k použití na obvod na konfiguraci.

**Volby**: Celé číslo >= 0

**Výchozí**: None

[Dokumentace API `default_shots`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#default_shots)
  </AccordionItem>

<AccordionItem title="`default_precision`">

Výchozí přesnost pro libovolný PUB nebo volání `run()`, které ji nespecifikuje.

**Volby**: Float > 0

**Výchozí**: 0.015625 (1 / sqrt(4096))

[Dokumentace API `default_precision`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#default_precision)
  </AccordionItem>

<AccordionItem title="`dynamical_decoupling`">

Ovládání nastavení zmírňování chyb dynamického oddělování.

[Dokumentace API `dynamical_decoupling`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#dynamical_decoupling)
<Accordion>
<AccordionItem title="`dynamical_decoupling.enable`">

**Volby**: `True`, `False`

**Výchozí**: `False`
  </AccordionItem>

<AccordionItem title="`dynamical_decoupling.extra_slack_distribution`">

**Volby**: `middle`, `edges`

**Výchozí**: `middle`
  </AccordionItem>

<AccordionItem title="`dynamical_decoupling.scheduling_method`">

Možnosti: `asap`, `alap`
Výchozí: `alap`
  </AccordionItem>

<AccordionItem title="`dynamical_decoupling.sequence_type`">

Možnosti: `XX`, `XpXm`, `XY4`
Výchozí: `XX`
  </AccordionItem>

<AccordionItem title="`dynamical_decoupling.skip_reset_qubits`">

Možnosti: `True`, `False`
Výchozí: `False`
  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`environment`">

[Dokumentace API `environment`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#environment)
<Accordion>
<AccordionItem title="`environment.callback`">

Volatelná funkce, která přijímá `Job ID` a `Job result`.

**Možnosti**: Žádné

**Výchozí**: Žádná
  </AccordionItem>

<AccordionItem title="`environment.job_tags`">

Seznam značek.

**Možnosti**: Žádné

**Výchozí**: Žádná
  </AccordionItem>

<AccordionItem title="`environment.log_level`">

**Možnosti**: DEBUG, INFO, WARNING, ERROR, CRITICAL

**Výchozí**: WARNING
  </AccordionItem>

<AccordionItem title="`environment.private`">

**Volby**: `True`, `False`

**Výchozí**: `False`
  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`execution`">

[Dokumentace API `execution`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#execution)
<Accordion>
<AccordionItem title="`execution.init_qubits`">
Zda resetovat qubity do základního stavu pro každý shot.

**Volby**: `True`, `False`

**Výchozí**: `True`
  </AccordionItem>

<AccordionItem title="`execution.rep_delay`">
Zpoždění mezi měřením a následujícím kvantovým obvodem.

**Možnosti**: Hodnota v rozsahu daném `backend.rep_delay_range`

**Výchozí**: Dáno hodnotou `backend.default_rep_delay`
  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`max_execution_time`">
Omezuje, jak dlouho může úloha běžet, v sekundách. Podrobnosti najdeš v průvodci [maximální doba provádění](/guides/max-execution-time).

**Možnosti**: Celé číslo sekund v rozsahu [1, 10800]

**Výchozí**: 10800 (3 hodiny)
  </AccordionItem>

<AccordionItem title="`resilience`">
Pokročilé možnosti odolnosti pro doladění strategie odolnosti.

[Dokumentace API `resilience`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#resilience)
<Accordion>
<AccordionItem title="`resilience.layer_noise_learning`">

Možnosti pro učení šumu vrstev.

[Dokumentace API `resilience.layer_noise_learning`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-layer-noise-learning-options)
  </AccordionItem>

<AccordionItem title="`resilience.layer_noise_learning.layer_pair_depths`">

**Možnosti**: list[int] s 2–10 hodnotami v rozsahu [0, 200]

**Default**: `(0, 1, 2, 4, 16, 32)`

  </AccordionItem>

<AccordionItem title="`resilience.layer_noise_learning.max_layers_to_learn`">

**Možnosti**: Žádná, celé číslo >= 1

**Default**: `4`

  </AccordionItem>

<AccordionItem title="`resilience.layer_noise_learning.num_randomizations`">

**Možnosti**: Celé číslo >= 1

**Default**: `32`

  </AccordionItem>

<AccordionItem title="`resilience.layer_noise_learning.shots_per_randomization`">

**Možnosti**: Celé číslo >= 1

**Default**: `128`

  </AccordionItem>

<AccordionItem title="`resilience.layer_noise_model`">

**Choices**: `NoiseLearnerResult`, `Sequence[LayerError]`

**Výchozí**: None

  </AccordionItem>

<AccordionItem title="`resilience.measure_mitigation`">

**Volby**: `True`, `False`

**Default**: `True`

  </AccordionItem>

<AccordionItem title="`resilience.measure_noise_learning`">

Možnosti pro učení šumu měření.

[Dokumentace API `resilience.measure_noise_learning`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-measure-noise-learning-options)
  </AccordionItem>

<AccordionItem title="`resilience.measure_noise_learning.num_randomizations`">

**Možnosti**: Celé číslo >= 1

**Default**: `32`

  </AccordionItem>

<AccordionItem title="`resilience.measure_noise_learning.shots_per_randomization`">

**Možnosti**: Celé číslo, `auto`

**Volby**: Celé číslo, `auto`

  </AccordionItem>

<AccordionItem title="`resilience.pec_mitigation`">

**Volby**: `True`, `False`

**Default**: `False`

  </AccordionItem>

<AccordionItem title="`resilience.pec`">

Možnosti zmírnění pomocí pravděpodobnostního rušení chyb.

[Dokumentace API `resilience.pec`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-pec-options)
  </AccordionItem>

<AccordionItem title="`resilience.pec.max_overhead`">

**Možnosti**: `None`, celé číslo >= 1

**Default**: `100`

  </AccordionItem>

<AccordionItem title="`resilience.pec.noise_gain`">

**Možnosti**: `auto`, desetinné číslo v rozsahu [0, 1]

**Volby**: Celé číslo, `auto`

  </AccordionItem>

<AccordionItem title="`resilience.zne_mitigation`">

**Volby**: `True`, `False`

**Default**: `False`

  </AccordionItem>

<AccordionItem title="`resilience.zne`">

[Dokumentace API `resilience.zne`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-zne-options)
  </AccordionItem>

<AccordionItem title="`resilience.zne.amplifier`">

**Choices**: `gate_folding`, `gate_folding_front`, `gate_folding_back`, `pea`

**Default**: `gate_folding`

  </AccordionItem>

<AccordionItem title="`resilience.zne.extrapolated_noise_factors`">

**Možnosti**: Seznam desetinných čísel

**Default**: `[0, *noise_factors]`

  </AccordionItem>

<AccordionItem title="`resilience.zne.extrapolator`">

**Možnosti**: Jedna nebo více z: `exponential`, `linear`, `double_exponential`, `polynomial_degree_(1 <= k <= 7)`, `fallback`

**Default**: `(exponential, linear)`

  </AccordionItem>

<AccordionItem title="`resilience.zne.noise_factors`">

**Možnosti**: Seznam desetinných čísel; každé desetinné číslo >= 1

**Výchozí**: `(1, 1.5, 2)` pro `PEA`, jinak `(1, 3, 5)`

  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`resilience_level`">

Kolik odolnosti vybudovat proti chybám. Vyšší úrovně generují přesnější výsledky za cenu delší doby zpracování. Více informací najdeš v části [úrovně odolnosti](/guides/estimator-noise-management#resilience) v tématu Správa šumu.

**Choices**: `0`, `1`, `2`

**Default**: `1`

[Dokumentace API `resilience_level`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#resilience_level)
  </AccordionItem>

<AccordionItem title="`seed_estimator`">

**Možnosti**: Celé číslo

**Výchozí**: None

[`seed_estimator`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-estimator-options#seed_estimator)
  </AccordionItem>

<AccordionItem title="`simulator`">

Možnosti k předání při simulaci backendu

[Dokumentace API `simulator`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-simulator-options)
<Accordion>
<AccordionItem title="`simulator.basis_gates`">

**Možnosti**: Seznam názvů základních hradel, na které se má rozbalit

**Výchozí**: Sada všech základních hradel podporovaných [simulátorem Qiskit Aer](https://qiskit.github.io/qiskit-aer/stubs/qiskit_aer.AerSimulator.html)

  </AccordionItem>

<AccordionItem title="`simulator.coupling_map`">

**Možnosti**: Seznam orientovaných dvouqubitových interakcí

**Volby**: Seznam orientovaných dvouqubitových interakcí

  </AccordionItem>

<AccordionItem title="`simulator.noise_model`">

**Možnosti**: [Qiskit Aer NoiseModel](/guides/build-noise-models) nebo jeho reprezentace

**Výchozí**: None

  </AccordionItem>

<AccordionItem title="`simulator.seed_simulator`">

**Možnosti**: Celé číslo

**Výchozí**: None

  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`twirling`">

Možnosti twirlingu

[Dokumentace API `twirling`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-twirling-options)
<Accordion>
<AccordionItem title="`twirling.enable_gates`">

**Možnosti**: True, False

**Volby**: True, False

  </AccordionItem>

<AccordionItem title="`twirling.enable_measure`">

**Možnosti**: True, False

**Výchozí hodnota**: True

  </AccordionItem>

<AccordionItem title="`twirling.num_randomizations`">

**Možnosti**: `auto`, celé číslo >= 1

**Volby**: Celé číslo, `auto`

  </AccordionItem>

<AccordionItem title="`twirling.shots_per_randomization`">

**Možnosti**: `auto`, celé číslo >= 1

**Volby**: Celé číslo, `auto`

  </AccordionItem>

<AccordionItem title="`twirling.strategy`">

**Choices**: `active`, `active-circuit`, `active-accum`, `all`

**Default**: `active-accum`

  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`experimental`">

Experimentální možnosti, pokud jsou k dispozici.

  </AccordionItem>

</Accordion>
<span id="options-compatibility-table"></span>

## Kompatibilita funkcí {#feature-compatibility}

Určité funkce runtime nelze používat společně v jedné úloze. Klikni na příslušnou záložku, kde najdeš seznam funkcí, které jsou nekompatibilní s vybranou funkcí:

<Accordion>
  <AccordionItem title="Frakční hradla">
    Nekompatibilní s:
  - Twirlingem hradel
  - PEA
  - PEC
  </AccordionItem>
  <AccordionItem title="Gate-folding ZNE">
    Může nefungovat při použití vlastních hradel. Nekompatibilní s:
  - PEA
  - PEC
  </AccordionItem>
  <AccordionItem title="Twirling hradel">
    Nekompatibilní s:
  - Frakčními hradly
  - Stretches

  Ostatní poznámky:
  - Twirling měření lze aplikovat pouze na terminální měření.
  - Nefunguje s ne-Cliffordovskými provazujícími hradly.
  </AccordionItem>
  <AccordionItem title="PEA">
    Nekompatibilní s:
  - Frakčními hradly
  - Gate-folding ZNE
  - PEC
  </AccordionItem>
  <AccordionItem title="PEC">
    Nekompatibilní s:
  - Frakčními hradly
  - Gate-folding ZNE
  - PEA
  </AccordionItem>
</Accordion>

## Další kroky {#next-steps}

<Admonition type="tip" title="Doporučení">
- Najdi více podrobností o metodách `EstimatorV2` v [referenci API Estimátoru](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/estimator-v2).
- Rozhodni se, v jakém [režimu spuštění](/guides/execution-modes) chceš spustit svou úlohu.
- Zjisti více o [správě šumu s Estimátorem](/guides/estimator-noise-management).
</Admonition>
