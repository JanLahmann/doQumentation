---
title: Nastavení pro použití IBM Quantum Platform s REST API
description: Pokyny k nastavení pro použití IBM Quantum na IBM Cloud k odesílání úloh Qiskit SDK a Qiskit Runtime pomocí REST API
platform: cloud
---

{/* doqumentation-source-hash: a3985acf */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore urlendcoded, WQVY */}

# Nastavení pro použití IBM Quantum Platform s REST API {#set-up-to-use-ibm-quantum-platform-with-rest-api}

Ke kvantovým procesorům můžeš přistupovat pomocí REST API, což ti umožňuje pracovat s QPU v libovolném programovacím jazyce nebo frameworku.

### 1. Získej přístup {#1-get-access}

1. Pokud ještě nemáš uživatelský účet, vytvoř si ho na [přihlašovací stránce IBM Quantum.](https://quantum.cloud.ibm.com/login)
1. Vytvoř API klíč (také nazývaný token) na [přehledovém panelu.](https://quantum.cloud.ibm.com) Stejný API klíč lze použít pro obě oblasti.
1. Vygeneruj nosný token IBM Cloud Identity and Access Management (IAM). Jedná se o krátkodobý token používaný k ověřování požadavků na REST API. Chceš-li ho vygenerovat, zavolej [IAM Identity Services API](https://cloud.ibm.com/apidocs/iam-identity-token-api#create-api-key), jak ukazuje následující ukázkový požadavek:
<Tabs>

   <TabItem value="Curl" label="Curl">
   ```bash
   curl -X POST 'https://iam.cloud.ibm.com/identity/token' \
   -H 'Content-Type: application/x-www-form-urlencoded' \
   -d 'grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=MY_APIKEY'
   ```
   Očekávaná odpověď
   ```text
   {
      "access_token": "eyJhbGciOiJIUz......sgrKIi8hdFs",
      "refresh_token": "SPrXw5tBE3......KBQ+luWQVY=",
      "token_type": "Bearer",
      "expires_in": 3600,
      "expiration": 1473188353
   }
   ```
   </TabItem>

   <TabItem value="Python" label="Python">
   ```python
   # Use 'service' to invoke operations.
   import requests
   import json

   url = 'https://iam.cloud.ibm.com/identity/token'
   api_key = 'MY_APIKEY'
   headers = {
    'Content-Type': 'application/x-www-form-urlendcoded',
   }
   data = f'grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey={api_key}'
   response = requests.post(url, headers=headers, data=data)

   # Bearer token to authorize requests to the REST API
   bearer_token = response.json()['access_token']
   ```
   </TabItem>
</Tabs>

### 2. Vyber metodu ověřování {#2-choose-an-authentication-method}

Vyber vhodnou metodu ověřování podle svého pracovního prostředí:

* [Vytvoř proměnnou prostředí pro svůj API klíč (důvěryhodná prostředí Python)](#environment_variable)
* [Použij API klíč přímo (nedůvěryhodné prostředí)](/guides/cloud-setup-untrusted#rest-untrusted)

<span id="environment_variable"></span>
#### Vytvoř proměnnou prostředí (důvěryhodné prostředí) {#create-an-environment-variable-trusted-environment}

1. Chceš-li nastavit proměnnou prostředí IQP_API_TOKEN ve svém systému, můžeš přidat následující řádek do svého shell profilu (například .bashrc nebo .zshrc) nebo ji nastavit přímo v terminálu:

    ```shell
    export IQP_API_TOKEN=<your-API_KEY> # Use the 44-character API_KEY you created and saved from the IBM Quantum Platform Home dashboard
    ```
    Když proměnnou prostředí použiješ ve svém kódu, nezapomeň zahrnout `import os`, jako v tomto příkladu:

    ```python
    import os
    api_token = os.environ['IQP_API_TOKEN']
    ```
    Měj na paměti, že při vytváření proměnné prostředí je tvůj API klíč stále uložen lokálně v prostém textu a měl by být chráněn.

1. Ověřuj požadavky na Qiskit Runtime REST API tak, že do hlaviček požadavku uvedeš CRN a nosný token.
```bash
curl -X 'GET' \
   'https://quantum.cloud.ibm.com/api/v1/usage' \
   '-H accept: application/json' \
   '-H authorization: Bearer <BEARER_TOKEN>' \
   '-H Service-CRN: <INSTANCE_CRN>'
```

### 3. Volitelně: Nakonfiguruj svůj firewall {#3-optional-configure-your-firewall}

    V případě potřeby použij [tyto informace](/guides/quickstart-steps-org#firewall) k povolení přístupu ke koncovým bodům IBM Quantum API.

## Další kroky {#next-steps}

<Admonition type="tip" title="Doporučení">
  -  [Přehled dostupných plánů.](plans-overview)
  -  [Nakonfiguruj Qiskit SDK lokálně.](configure-qiskit-local)
  -  Postupuj podle kroků v [Hello world](/guides/hello-world) a napiš a spusť kvantový program.
  -  Vyzkoušej si [tutoriál](https://docs.quantum.ibm.com/tutorials).
</Admonition>
