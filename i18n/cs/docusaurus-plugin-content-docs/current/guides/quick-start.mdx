---
description: 'Sestav a vizualizuj kvantový Circuit za méně než dvě minuty – bez přihlášení nebo API klíče.'
notebook_path: guides/quick-start.ipynb
sidebar_label: 'Rychlý start'
title: 'Rychlý start'
---

{/* doqumentation-source-hash: dc09af91 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<OpenInLabBanner notebookPath="guides/quick-start.ipynb" />




{/* cspell:ignore miniconda */}

{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Verze balíčků">

Kód na této stránce byl vyvinut s použitím následujících požadavků.
Doporučujeme použít tyto verze nebo novější.

```
qiskit[all]~=2.5.2
```
</AccordionItem>
</Accordion>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

Sestav svůj první kvantový Circuit za méně než dvě minuty ve svém lokálním prostředí – bez přihlášení nebo API klíče.

<Admonition type="note" title="Začínáš s Pythonem a virtuálními prostředími?">

* Stáhni si Python a použij virtuální prostředí s Qiskem (doporučeno).


<Accordion>
<AccordionItem title="Klikni pro rozbalení dalších informací o **Pythonu**.">

- Při instalaci Pythonu nejprve zkontroluj sekci „Programming Language" na [stránce projektu Qiskit na PyPI](https://pypi.org/project/qiskit/), abys zjistil/a, které verze Pythonu podporuje nejnovější vydání. Pokyny ke stažení najdeš v [Průvodci pro začátečníky s Pythonem.](https://wiki.python.org/moin/BeginnersGuide/Download)


<Admonition type="note">
These instructions use the standard Python distribution from [pypi.org](https://pypi.org/). However, you can use other Python distributions, such as [Anaconda](https://docs.anaconda.com/anaconda/) or [miniconda](https://docs.anaconda.com/miniconda/), along with other dependency management workflows like [Poetry](https://python-poetry.org/docs/).
</Admonition>
  </AccordionItem>
</Accordion>

    <Accordion>
    <AccordionItem title="Klikni pro rozbalení dalších informací o **virtuálních prostředích**.">
    - Použij [virtuální prostředí Pythonu](https://docs.python.org/3.10/tutorial/venv.html) k oddělení Qiskit od ostatních aplikací.
    Virtuální prostředí Pythonu je izolovaný prostor pro práci s Pythonem pro konkrétní účel — takže si můžeš nainstalovat jakékoli balíčky, které chceš, a nastavit knihovny, závislosti atd., aniž by to ovlivnilo „základní“ prostředí Pythonu na tvém počítači.

    Jednou z důležitých výhod virtuálního prostředí je, že pokud se tvoje prostředí Pythonu poruší, můžeš ho snadno smazat a začít znovu!

    Zvol preferované umístění, kam budeš ukládat informace o svých virtuálních prostředích. Obvykle se ukládají do adresáře s názvem `.venv` uvnitř každého adresáře projektu.

    Abys nastavil/a virtuální prostředí, přejdi do adresáře svého projektu a vytvoř minimální prostředí pouze s nainstalovaným Pythonem.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">

            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">

            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">

            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

    Dále aktivuj své nové prostředí.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">

             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">

             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
              Pokud používáš PowerShell:

             ```text
             .venv\Scripts\Activate.ps1
             ```
             Pokud používáš Git Bash:

             ```text
             source .venv/scripts/activate
             ```
              Pokud používáš příkazový řádek:

             ```text
             .venv\Scripts\activate
             ```
           </TabItem>
         </OperatingSystemTabs>
      </AccordionItem>
    </Accordion>

</Admonition>

## 1. Nainstaluj Qiskit {#1-install-qiskit}

Nainstaluj následující pomocí svého preferovaného správce balíčků (například `pip`):

- [`qiskit`](/guides/install-qiskit)

- [`matplotlib`](https://matplotlib.org/stable/users/explain/quick_start.html)

- [`qiskit[visualization]`](https://docs.quantum.ibm.com/api/qiskit/visualization)


## 2. Sestav svůj Circuit {#2-build-your-circuit}

Otevři prostředí Pythonu a spusť tento kód pro sestavení Bellova stavu (dva entanglované Qubity).

```python
from qiskit import QuantumCircuit
from qiskit.primitives import StatevectorSampler

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

sampler = StatevectorSampler()
result = sampler.run([qc], shots=1024).result()
print(result[0].data.meas.get_counts())
```

```text
{'00': 486, '11': 538}
```

Očekávaný výstup je přibližně rovnoměrné rozdělení mezi '00' a '11'.

## 3. Vizualizuj své výsledky {#3-visualize-your-results}

Pro zobrazení histogramu svých výsledků přidej do programu následující kód.

```python
# Uncomment lines 2 and 8 if you are not using Python in a Jupyter notebook
# import matplotlib.pyplot as plt
from qiskit.visualization import plot_histogram

counts = result[0].data.meas.get_counts()
plot_histogram(counts)

# plt.show()
```

![Output of the previous code cell](/docs/images/guides/quick-start/extracted-outputs/dc4ff012-0.svg)

Tento výsledek je projevem kvantového entanglementu.

## 4. Prozkoumej, co se děje {#4-see-what-happens}

Zkus změnit kód a sleduj, jak to ovlivní výsledky. Například:

- Přidej třetí Qubit změnou na `QuantumCircuit(3)` a přidej druhý CX Gate pomocí `qc.cx(1,2)`. Měření by se pak mělo změnit na 000 a 111, což znamená, že všechny tři tyto Qubity byly entanglovány.


- Sleduj, jak se výsledky mění po přidání `qc.x(1)` na konec Circuitu.


## Další kroky {#next-steps}

<Admonition type="tip" title="Doporučení">
- Postupuj podle kroků v [Hello world](/guides/hello-world) a spusť Circuit na skutečném kvantovém hardwaru.
- Ještě nejsi připraven/a spustit na hardwaru? Začni svou kvantovou cestu s kurzem [Základy kvantových informací](/learning/courses/basics-of-quantum-information).
</Admonition>
