---
description: 'Використовуй дробові гейти, параметризовані гейти, які безпосередньо виконують обертання під довільними кутами, щоб зменшити глибину та тривалість квантових ядрових схем.'
notebook_path: tutorials/fractional-gates.ipynb
sidebar_label: 'Квантові ядра з дробовими гейтами'
title: 'Квантові ядра з дробовими гейтами'
---

{/* doqumentation-source-hash: 58634d2b */}

import TutorialFeedback from '@site/src/components/TutorialFeedback';

<OpenInLabBanner notebookPath="tutorials/fractional-gates.ipynb" />




*Оцінка використання: менше 30 секунд на процесорі Heron r2 (ПРИМІТКА: Це лише оцінка. Твій час виконання може відрізнятися.)*

## Результати навчання {#learning-outcomes}
Пройшовши цей підручник, ти повинен зрозуміти:
- Що таке дробові гейти і як вони зменшують глибину та тривалість схеми на IBM&reg; QPU
- Обмеження, пов'язані з використанням дробових гейтів (зокрема, діапазон кута RZZ)
- Як побудувати робочий процес квантового ядра, що використовує дробові гейти з IBM Quantum Compute Service
- Як порівнювати метрики виконання на апаратному забезпеченні (глибина, тривалість, кількість нелокальних гейтів, точність) з дробовими гейтами та без них
- Як використовувати лише дробові гейти RX, зберігаючи стандартний робочий процес шаблонів Qiskit

## Передумови {#prerequisites}
Ми пропонуємо тобі ознайомитися з наступними темами перед проходженням цього підручника:
- Робочий процес [шаблонів Qiskit](/guides/intro-to-patterns)
- Посібник [Дробові гейти](/guides/fractional-gates)
- Підручник [Навчання квантових ядер](/tutorials/quantum-kernel-training) та урок [Квантові ядра](/learning/courses/quantum-machine-learning/quantum-kernel-methods) курсу Квантового машинного навчання

## Загальна інформація {#background}

### Дробові ворота на IBM QPU {#fractional-gates-on-ibm-qpus}

Дробові ворота - це параметризовані квантові ворота, які дозволяють пряме виконання обертань під довільними кутами (в межах певних обмежень),
усуваючи необхідність розкладати їх на кілька базисних воріт.
Використовуючи природні взаємодії між фізичними кубітами, ти можеш реалізовувати певні унітарні оператори більш ефективно на обладнанні.

IBM Quantum&reg; Heron QPU підтримують наступні дробові ворота:
- $R_{ZZ}(\theta)$ для $0 < \theta < \pi / 2$
- $R_X(\theta)$ для будь-якого дійсного значення $\theta$

Ці ворота можуть значно зменшити як глибину, так і тривалість квантових схем.
Вони особливо вигідні в застосуваннях, які значною мірою покладаються на $R_{ZZ}$ та $R_X$,
таких як гамільтонова симуляція, Алгоритм квантової приблизної оптимізації (QAOA) та методи квантових ядер.
У цьому підручнику ми зосереджуємося на квантовому ядрі як практичному прикладі.

### Обмеження {#limitations}

Дробові ворота наразі є експериментальною функцією і мають кілька обмежень:
- $R_{ZZ}$ обмежений кутами в діапазоні $0 < \theta < \pi / 2$.
- Використання дробових воріт не підтримується для [динамічних схем](/guides/classical-feedforward-and-control-flow), [скручування Паулі](/guides/error-mitigation-and-suppression-techniques#pauli-twirling), [імовірнісного скасування помилок](/guides/error-mitigation-and-suppression-techniques#probabilistic-error-cancellation-pec) (PEC) та [екстраполяції з нульовим шумом](/guides/error-mitigation-and-suppression-techniques#zero-noise-extrapolation-zne) (ZNE) (з використанням [імовірнісного підсилення помилок](/guides/error-mitigation-and-suppression-techniques#probabilistic-error-amplification-pea) (PEA)).

Дробові ворота вимагають іншого робочого процесу порівняно зі стандартним підходом.
Цей підручник пояснює, як працювати з дробовими воротами через практичне застосування.

Дивись наступні джерела для отримання більш детальної інформації про дробові ворота.
- [Дробові ворота](/guides/fractional-gates)
- [Коли *не* використовувати дробові ворота](/guides/fractional-gates#when-not-to-use)

### Підходи до робочого процесу для обмеження кута RZZ {#workflow-approaches-for-the-rzz-angle-constraint}
Робочий процес для використання дробових гейтів загалом слідує робочому процесу [шаблонів Qiskit](/guides/intro-to-patterns).
Ключова відмінність полягає в тому, що всі кути RZZ повинні задовольняти обмеження $0 < \theta \leq \pi/2$.
Існує два підходи для забезпечення виконання цієї умови, як ми обговорюємо нижче. Ми рекомендуємо другий підхід, і в цьому підручнику ми демонструємо його на прикладі, натхненному методом квантового ядра.
Щоб краще зрозуміти, де квантові ядра, ймовірно, будуть корисними, ми рекомендуємо прочитати [Liu, Arunachalam & Temme (2021)](https://www.nature.com/articles/s41567-021-01287-z).

Ти також можеш ознайомитися з підручником [Навчання квантових ядер](/tutorials/quantum-kernel-training) та уроком [Квантові ядра](/learning/courses/quantum-machine-learning/quantum-kernel-methods) у курсі Квантового машинного навчання на IBM Quantum&reg; Learning.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q matplotlib numpy qiskit qiskit-aer qiskit-basis-constructor qiskit-ibm-runtime
```

#### 1. Генерація значень параметрів, які задовольняють обмеження кута RZZ {#1-generate-parameter-values-that-satisfy-the-rzz-angle-constraint}

Якщо ти впевнений, що всі кути RZZ потрапляють у допустимий діапазон, ти можеш слідувати стандартному робочому процесу шаблонів Qiskit.
У цьому випадку ти просто надаєш значення параметрів як частину PUB. Робочий процес виглядає наступним чином.

```python
pm = generate_preset_pass_manager(backend=backend, ...)
t_circuit = pm.run(circuit)
t_observable = observable.apply_layout(t_circuit.layout)
sampler.run([(t_circuit, parameter_values)])
estimator.run([(t_circuit, t_observable, parameter_values)])
```

Якщо ти спробуєш надіслати PUB, який включає ворота RZZ з кутом за межами допустимого діапазону, ти отримаєш повідомлення про помилку, таке як:

```
'The instruction rzz is supported only for angles in the range [0, pi/2], but an angle (20.0) outside of this range has been requested; via parameter value(s) γ[0]=10.0, substituted in parameter expression 2.0*γ[0].'
```
Щоб уникнути цієї помилки, використовуй другий підхід, описаний нижче.

#### 2. Призначення значень параметрів схемам перед транспіляцією {#2-assign-parameter-values-to-circuits-before-transpilation}

Пакет `qiskit-ibm-runtime` надає спеціалізований прохід транспілятора під назвою [`FoldRzzAngle`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/transpiler-passes-fold-rzz-angle).
Цей прохід трансформує квантові схеми так, щоб всі кути RZZ відповідали обмеженню кута RZZ.
Якщо ти надаєш backend до `generate_preset_pass_manager` або `transpile`, Qiskit автоматично застосовує `FoldRzzAngle` до квантових схем.
Цей підхід вимагає від тебе призначити значення параметрів квантовим схемам перед транспіляцією.
Робочий процес виглядає наступним чином.

```python
pm = generate_preset_pass_manager(backend=backend, ...)
b_circuit = circuit.assign_parameters(parameter_values)
t_circuit = pm.run(b_circuit)
t_observable = observable.apply_layout(t_circuit.layout)
sampler.run([(t_circuit,)])
estimator.run([(t_circuit, t_observable)])
```

Зауваж, що цей робочий процес спричиняє вищу обчислювальну вартість порівняно з першим підходом, оскільки він передбачає присвоєння значень параметрів квантовим схемам та локальне зберігання схем зі зв'язаними параметрами.

:::caution


**Зверни увагу на відому проблему в `qiskit-ibm-runtime` v0.47.0**, коли RZZ гейти з недійсними кутами можуть залишатися в схемах навіть після транспіляції за певних сценаріїв.

Дивись [qiskit-ibm-runtime#2441](https://github.com/Qiskit/qiskit-ibm-runtime/issues/2441), щоб відстежувати прогрес щодо цієї проблеми.
Ми рекомендуємо наступне обхідне рішення, доки її не вирішать.

    ```python
    pm = generate_preset_pass_manager(backend=backend, ...)
    pm.post_optimization = PassManager(
        [
            FoldRzzAngle(),
            Optimize1qGatesDecomposition(target=backend.target),
            RemoveIdentityEquivalent(target=backend.target),
        ]
    )
    ... = pm.run(...)
    ```

:::

## Вимоги {#requirements}

Перед початком цього підручника переконайся, що у тебе встановлено наступне:
- Qiskit SDK v2.0 або новіший, з підтримкою [візуалізації](https://docs.quantum.ibm.com/api/qiskit/visualization)
- Qiskit Runtime v0.41 або новіший (`pip install qiskit-ibm-runtime`)
- Qiskit Aer v0.17 або новіший (`pip install qiskit-aer`)
- Qiskit Basis Constructor (`pip install qiskit_basis_constructor`)

## Налаштування {#setup}

```python
import matplotlib.pyplot as plt
import numpy as np
from qiskit import QuantumCircuit, generate_preset_pass_manager
from qiskit.circuit import ParameterVector
from qiskit.circuit.library import UGate, n_local, unitary_overlap
from qiskit.transpiler import Target, PassManager
from qiskit.transpiler.passes import (
    Optimize1qGatesDecomposition,
    RemoveIdentityEquivalent,
)
from qiskit_aer.primitives import SamplerV2 as AerSampler
from qiskit_basis_constructor import DEFAULT_EQUIVALENCE_LIBRARY
from qiskit_ibm_runtime import QiskitRuntimeService, SamplerV2
from qiskit_ibm_runtime.transpiler.passes import FoldRzzAngle
```

### Увімкнення дробових воріт та перевірка базисних воріт {#enable-fractional-gates-and-check-basis-gates}

Щоб використовувати дробові ворота, ти можеш отримати backend, який їх підтримує, встановивши опцію `use_fractional_gates=True`.
Якщо backend підтримує дробові ворота, ти побачиш `rzz` та `rx` серед його базисних воріт.

```python
service = QiskitRuntimeService()
backend = service.least_busy(
    operational=True, simulator=False, min_num_qubits=133
)  # backend should be a heron device or later
backend_name = backend.name
backend_c = service.backend(backend_name)  # w/o fractional gates
backend_f = service.backend(
    backend_name, use_fractional_gates=True
)  # w/ fractional gates
print(f"Backend: {backend_name}")
print(f"No fractional gates: {backend_c.basis_gates}")
print(f"With fractional gates: {backend_f.basis_gates}")
if "rzz" not in backend_f.basis_gates:
    print(f"Backend {backend_name} does not support fractional gates")
```

```text
Backend: ibm_marrakesh
No fractional gates: ['cz', 'id', 'rz', 'sx', 'x']
With fractional gates: ['cz', 'id', 'rx', 'rz', 'rzz', 'sx', 'x']
```

## Приклад малого симулятора {#small-scale-simulator-example}
У цьому розділі ми пройдемо чотири кроки робочого процесу [шаблонів Qiskit](/guides/intro-to-patterns) на симуляторі, використовуючи схему квантового ядра як практичний приклад.

### Крок 1: Відображення класичних входів у квантову задачу {#step-1-map-classical-inputs-to-a-quantum-problem}

#### Схема квантового ядра {#quantum-kernel-circuit}
У цьому розділі ми досліджуємо схему квантового ядра з використанням воріт RZZ, щоб представити робочий процес для дробових воріт.

Ми починаємо з побудови квантової схеми для обчислення окремих елементів матриці ядра.
Це виконується шляхом поєднання схем карти ознак ZZ з унітарним перекриттям.
Функція ядра приймає вектори в просторі, відображеному на ознаки, і повертає їх внутрішній добуток як елемент матриці ядра:
$$K(x, y) = \langle \Phi(x) | \Phi(y) \rangle,$$
де $|\Phi(x)\rangle$ представляє квантовий стан, відображений на ознаки.

Ми вручну конструюємо схему карти ознак ZZ з використанням воріт RZZ.
Хоча Qiskit надає вбудований `zz_feature_map`, він наразі не підтримує ворота RZZ станом на Qiskit v2.4.1 ([див. проблему](https://github.com/Qiskit/qiskit/issues/14469)).

Далі ми обчислюємо функцію ядра для ідентичних входів - наприклад, $K(x, x) = 1$.
На зашумлених квантових комп'ютерах це значення може бути меншим за 1 через шум.
Результат, ближчий до 1, вказує на менший шум під час виконання.
У цьому підручнику ми називаємо це значення *точністю*, визначеною як
$$\text{fidelity} = K(x, x).$$

```python
optimization_level = 2
shots = 2000
reps = 3
rng = np.random.default_rng(seed=123)
```

```python
def my_zz_feature_map(num_qubits: int, reps: int = 1) -> QuantumCircuit:
    x = ParameterVector("x", num_qubits * reps)
    qc = QuantumCircuit(num_qubits)
    qc.h(range(num_qubits))
    for k in range(reps):
        K = k * num_qubits
        for i in range(num_qubits):
            qc.rz(x[i + K], i)
        pairs = [(i, i + 1) for i in range(num_qubits - 1)]
        for i, j in pairs[0::2] + pairs[1::2]:
            qc.rzz((np.pi - x[i + K]) * (np.pi - x[j + K]), i, j)
    return qc

def quantum_kernel(num_qubits: int, reps: int = 1) -> QuantumCircuit:
    qc = my_zz_feature_map(num_qubits, reps=reps)
    inner_product = unitary_overlap(qc, qc, "x", "y", insert_barrier=True)
    inner_product.measure_all()
    return inner_product

def random_parameters(inner_product: QuantumCircuit) -> np.ndarray:
    return np.tile(rng.random(inner_product.num_parameters // 2), 2)

def fidelity(result) -> float:
    ba = result.data.meas
    return ba.get_int_counts().get(0, 0) / ba.num_shots
```

Схеми квантових ядер та відповідні їм значення параметрів генеруються для систем від 4 до 40 кубітів, і їхні точності згодом оцінюються.

```python
qubits = list(range(4, 12, 2))
circuits = [quantum_kernel(i, reps=reps) for i in qubits]
params = [random_parameters(circ) for circ in circuits]
```

Схема з чотирма кубітами візуалізована нижче.

```python
circuits[0].draw("mpl", fold=-1)
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/b3d6341a-0.avif)

У стандартному робочому процесі шаблонів Qiskit значення параметрів зазвичай передаються примітиву Sampler або Estimator як частина PUB.
Однак, при використанні backend, який підтримує дробові ворота, ці значення параметрів повинні бути явно призначені квантовій схемі перед транспіляцією.

```python
b_qc = [
    circ.assign_parameters(param) for circ, param in zip(circuits, params)
]
b_qc[0].draw("mpl", fold=-1)
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/6c9c1977-0.avif)

### Крок 2: Оптимізація задачі для виконання на квантовому обладнанні {#step-2-optimize-problem-for-quantum-hardware-execution}
Потім ми транспілюємо схему, використовуючи менеджер проходів, слідуючи стандартному шаблону Qiskit.
Надаючи backend, який підтримує дробові ворота, до `generate_preset_pass_manager`, спеціалізований прохід під назвою `FoldRzzAngle` автоматично включається.
Цей прохід модифікує схему для відповідності обмеженням кута RZZ.
У результаті ворота RZZ з від'ємними значеннями на попередньому рисунку трансформуються в додатні значення, і додаються деякі додаткові ворота X.

```python
backend_f = service.backend(name=backend_name, use_fractional_gates=True)
# pm_f includes `FoldRzzAngle` pass
pm_f = generate_preset_pass_manager(
    optimization_level=optimization_level, backend=backend_f
)
pm_f.post_optimization = PassManager(
    [
        FoldRzzAngle(),
        Optimize1qGatesDecomposition(target=backend_f.target),
        RemoveIdentityEquivalent(target=backend_f.target),
    ]
)
```

```python
t_qc_f = pm_f.run(b_qc)
print(t_qc_f[0].count_ops())
t_qc_f[0].draw("mpl", fold=-1)
```

```text
OrderedDict({'rz': 35, 'rzz': 18, 'x': 13, 'rx': 9, 'measure': 4, 'barrier': 2})
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/a18e5c70-1.avif)

Щоб оцінити вплив дробових воріт, ми оцінюємо кількість нелокальних воріт (CZ та RZZ для цього backend),
разом із глибиною схем та тривалістю, і порівнюємо ці метрики з метриками зі стандартного робочого процесу пізніше.

```python
nnl_f = [qc.num_nonlocal_gates() for qc in t_qc_f]
depth_f = [qc.depth() for qc in t_qc_f]
duration_f = [
    qc.estimate_duration(backend_f.target, unit="u") for qc in t_qc_f
]
```

### Крок 3: Виконання з використанням примітивів Qiskit {#step-3-execute-using-qiskit-primitives}
Ми запускаємо транспільовану схему з backend, який підтримує дробові ворота.

```python
sampler_f = AerSampler.from_backend(backend_f)
```

```python
job = sampler_f.run(t_qc_f, shots=shots)
print(job.job_id())
```

```text
085ce928-767e-4200-93bf-3905e5411cfe
```

### Крок 4: Постобробка та повернення результату в бажаному класичному форматі {#step-4-post-process-and-return-result-in-desired-classical-format}
Ти можеш отримати значення функції ядра $K(x, x)$ шляхом вимірювання ймовірності бітового рядка з усіма нулями `00...00` у виході.

```python
result = job.result()
fidelity_f = [fidelity(result=res) for res in result]
print(fidelity_f)
```

```text
[0.929, 0.882, 0.8645, 0.817]
```

### Порівняння робочого процесу та схеми без дробових воріт {#comparison-of-workflow-and-circuit-without-fractional-gates}

У цьому розділі ми представляємо стандартний робочий процес шаблонів Qiskit з використанням backend, який не підтримує дробові ворота.
Порівнюючи транспільовані схеми, ти помітиш, що версія з використанням дробових воріт (з попереднього розділу) є більш компактною, ніж версія без дробових воріт.

```python
# step 1: map classical inputs to quantum problem
# `circuits` and `params` from the previous section are reused here
```

```python
# step 2: optimize circuits
backend_c = service.backend(backend_name)  # w/o fractional gates
pm_c = generate_preset_pass_manager(
    optimization_level=optimization_level, backend=backend_c
)
t_qc_c = pm_c.run(circuits)
print(t_qc_c[0].count_ops())
t_qc_c[0].draw("mpl", fold=-1)
```

```text
OrderedDict({'rz': 130, 'sx': 80, 'cz': 36, 'measure': 4, 'barrier': 2})
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/a10f2d95-1.avif)

```python
nnl_c = [qc.num_nonlocal_gates() for qc in t_qc_c]
depth_c = [qc.depth() for qc in t_qc_c]
duration_c = [
    qc.estimate_duration(backend_c.target, unit="u") for qc in t_qc_c
]
```

```python
# step 3: execute
sampler_c = AerSampler.from_backend(backend_c)
```

```python
job = sampler_c.run(pubs=zip(t_qc_c, params), shots=shots)
print(job.job_id())
```

```text
f2cca29d-7263-4976-9e51-13a91b75c3ae
```

```python
# step 4: post-processing
result = job.result()
fidelity_c = [fidelity(res) for res in result]
print(fidelity_c)
```

```text
[0.8625, 0.7605, 0.702, 0.671]
```

### Порівняння глибини, тривалості та точності {#comparison-of-depths-durations-and-fidelities}

У цьому розділі ми порівнюємо кількість нелокальних воріт та точність між схемами з дробовими воротами та без них.
Це підкреслює потенційні переваги використання дробових воріт з точки зору ефективності виконання та якості.

```python
plt.plot(qubits, depth_c, "-o", label="no fractional gates")
plt.plot(qubits, depth_f, "-o", label="with fractional gates")
plt.xlabel("number of qubits")
plt.ylabel("depth")
plt.title("Comparison of depths")
plt.grid()
plt.legend()
```

```text
<matplotlib.legend.Legend at 0x116af3cb0>
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/ef343a53-1.avif)

```python
plt.plot(qubits, duration_c, "-o", label="no fractional gates")
plt.plot(qubits, duration_f, "-o", label="with fractional gates")
plt.xlabel("number of qubits")
plt.ylabel("duration (µs)")
plt.title("Comparison of durations")
plt.grid()
plt.legend()
```

```text
<matplotlib.legend.Legend at 0x11ea4f4d0>
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/98bb2cd0-1.avif)

```python
plt.plot(qubits, nnl_c, "-o", label="no fractional gates")
plt.plot(qubits, nnl_f, "-o", label="with fractional gates")
plt.xlabel("number of qubits")
plt.ylabel("number of non-local gates")
plt.title("Comparison of numbers of non-local gates")
plt.grid()
plt.legend()
```

```text
<matplotlib.legend.Legend at 0x1247fc440>
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/1383b242-1.avif)

```python
plt.plot(qubits, fidelity_c, "-o", label="no fractional gates")
plt.plot(qubits, fidelity_f, "-o", label="with fractional gates")
plt.xlabel("number of qubits")
plt.ylabel("fidelity")
plt.title("Comparison of fidelities")
plt.grid()
plt.legend()
```

```text
<matplotlib.legend.Legend at 0x120b792b0>
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/8b4594f5-1.avif)

## Приклад великомасштабного апаратного забезпечення {#large-scale-hardware-example}

У цьому розділі ми порівнюємо робочий процес квантового ядра з дробовими гейтами та без них на квантовому апаратному забезпеченні з до 40 кубітів.

### Кроки 1-4 разом {#step-1-4-combined}

Робочий процес слідує тій самій структурі, що й приклад малого масштабу. Ми транспілюємо всі схеми з дробовими гейтами та без них, збираємо метрики, а потім надсилаємо схеми на реальне квантове апаратне забезпечення.

```python
# -------------------------Step 1-------------------------
qubits = list(range(4, 44, 4))
circuits = [quantum_kernel(i, reps=reps) for i in qubits]
params = [random_parameters(circ) for circ in circuits]
b_qc = [
    circ.assign_parameters(param) for circ, param in zip(circuits, params)
]

def benchmark(b_qc, backend):
    # -------------------------Step 2-------------------------
    pm = generate_preset_pass_manager(optimization_level, backend=backend)
    if "rzz" in backend.target.operation_names:
        # workaround until https://github.com/Qiskit/qiskit-ibm-runtime/issues/2441 is resolved
        pm.post_optimization = PassManager(
            [
                FoldRzzAngle(),
                Optimize1qGatesDecomposition(target=backend.target),
                RemoveIdentityEquivalent(target=backend.target),
            ]
        )
    t_qc = pm.run(b_qc)
    nnl = [qc.num_nonlocal_gates() for qc in t_qc]
    depth = [qc.depth() for qc in t_qc]
    duration = [
        qc.estimate_duration(backend_f.target, unit="u") for qc in t_qc
    ]

    # -------------------------Step 3-------------------------
    sampler = SamplerV2(mode=backend)
    sampler.options.dynamical_decoupling.enable = True
    sampler.options.dynamical_decoupling.sequence_type = "XY4"
    sampler.options.dynamical_decoupling.skip_reset_qubits = True
    sampler.options.environment.job_tags = ["TUT_FG"]
    job = sampler.run(t_qc, shots=shots)
    job_id = job.job_id()
    return nnl, depth, duration, job_id

def postprocessing(job_id: str):
    # -------------------------Step 4-------------------------
    job = service.job(job_id)
    result = job.result()
    fidelities = [fidelity(result=res) for res in result]
    usage = job.usage()
    return fidelities, usage

backend_f = service.backend(backend_name, use_fractional_gates=True)
nnl_f, depth_f, duration_f, job_id_f = benchmark(
    b_qc, backend_f
)  # step 2 & 3
print("job id (w/ fractional gates):", job_id_f)
fidelity_f, usage_f = postprocessing(job_id_f)  # step 4
```

```text
job id (w/ fractional gates): d8uasitbh0os73eqnpig
```

```python
backend_c = service.backend(backend_name, use_fractional_gates=False)
nnl_c, depth_c, duration_c, job_id_c = benchmark(b_qc, backend_c)
print("job id (w/o fractional gates):", job_id_c)
fidelity_c, usage_c = postprocessing(job_id_c)
```

```text
job id (w/o fractional gates): d8uav3lposuc738pruug
```

Потім ми порівнюємо метрики.

```python
plt.plot(qubits, depth_c, "-o", label="no fractional gates")
plt.plot(qubits, depth_f, "-o", label="with fractional gates")
plt.xlabel("number of qubits")
plt.ylabel("depth")
plt.title("Comparison of depths")
plt.grid()
plt.legend()
```

```text
<matplotlib.legend.Legend at 0x12461e660>
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/b409e8d3-1.avif)

```python
plt.plot(qubits, duration_c, "-o", label="no fractional gates")
plt.plot(qubits, duration_f, "-o", label="with fractional gates")
plt.xlabel("number of qubits")
plt.ylabel("duration (µs)")
plt.title("Comparison of durations")
plt.grid()
plt.legend()
```

```text
<matplotlib.legend.Legend at 0x11f2ac980>
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/09f91f0f-1.avif)

```python
plt.plot(qubits, nnl_c, "-o", label="no fractional gates")
plt.plot(qubits, nnl_f, "-o", label="with fractional gates")
plt.xlabel("number of qubits")
plt.ylabel("number of non-local gates")
plt.title("Comparison of numbers of non-local gates")
plt.grid()
plt.legend()
```

```text
<matplotlib.legend.Legend at 0x125c91be0>
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/c9308517-1.avif)

```python
plt.plot(qubits, fidelity_c, "-o", label="no fractional gates")
plt.plot(qubits, fidelity_f, "-o", label="with fractional gates")
plt.xlabel("number of qubits")
plt.ylabel("fidelity")
plt.title("Comparison of fidelities")
plt.grid()
plt.legend()
```

```text
<matplotlib.legend.Legend at 0x11fcf6e40>
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/234731d4-1.avif)

Ми порівнюємо час використання QPU з дробовими воротами та без них. Результати в наступній комірці показують, що часи використання QPU майже ідентичні.

```python
print(f"no fractional gates: {usage_c} seconds")
print(f"fractional gates: {usage_f} seconds")
```

```text
no fractional gates: 8 seconds
fractional gates: 8 seconds
```

## Додаткова тема: Використання лише дробових RX воріт {#advanced-topic-using-only-fractional-rx-gates}

Необхідність модифікованого робочого процесу при використанні дробових воріт в основному випливає з обмеження на кути RZZ воріт.
Однак, якщо ти використовуєш лише дробові RX ворота та виключаєш дробові RZZ ворота, ти можеш продовжувати дотримуватися стандартного робочого процесу Qiskit patterns.
Цей підхід все ще може запропонувати значні переваги, особливо в схемах, які містять велику кількість RX воріт та U воріт, зменшуючи загальну кількість воріт і потенційно покращуючи продуктивність.
У цьому розділі ми демонструємо, як оптимізувати твої схеми, використовуючи лише дробові RX ворота, виключаючи RZZ ворота.

Для підтримки цього ми надаємо допоміжну функцію, яка дозволяє тобі вимкнути конкретні базові ворота в об'єкті Target.
Тут ми використовуємо її для вимкнення RZZ воріт.

```python
def remove_instruction_from_target(target: Target, gate_name: str) -> Target:
    new_target = Target(
        description=target.description,
        num_qubits=target.num_qubits,
        dt=target.dt,
        granularity=target.granularity,
        min_length=target.min_length,
        pulse_alignment=target.pulse_alignment,
        acquire_alignment=target.acquire_alignment,
        qubit_properties=target.qubit_properties,
        concurrent_measurements=target.concurrent_measurements,
    )

    for name, qarg_map in target.items():
        if name == gate_name:
            continue
        instruction = target.operation_from_name(name)
        if qarg_map == {None: None}:
            qarg_map = None
        new_target.add_instruction(instruction, qarg_map, name=name)
    return new_target
```

Ми використовуємо схему, що складається з U, CZ та RZZ воріт, як приклад.

```python
qc = n_local(3, "u", "cz", "linear", reps=1)
qc.rzz(1.1, 0, 1)
qc.draw("mpl")
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/6b812497-0.avif)

Спочатку ми транспілюємо схему для backend, який не підтримує дробові ворота.

```python
pm_c = generate_preset_pass_manager(
    optimization_level=optimization_level, backend=backend_c
)
t_qc = pm_c.run(qc)
print(t_qc.count_ops())
t_qc.draw("mpl")
```

```text
OrderedDict({'rz': 23, 'sx': 16, 'cz': 4})
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/9e8e0709-1.avif)

Потім ми транспілюємо ту саму схему, використовуючи дробові RX ворота, виключаючи RZZ ворота.
Це призводить до незначного зменшення загальної кількості воріт завдяки більш ефективній реалізації RX воріт.

```python
backend_f = service.backend(backend_name, use_fractional_gates=True)
target = remove_instruction_from_target(backend_f.target, "rzz")
pm_f = generate_preset_pass_manager(
    optimization_level=optimization_level,
    target=target,
)
t_qc = pm_f.run(qc)
print(t_qc.count_ops())
t_qc.draw("mpl")
```

```text
OrderedDict({'rz': 22, 'sx': 14, 'cz': 4, 'rx': 1})
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/db45feb0-1.avif)

### Оптимізація U воріт з дробовими RX воротами {#optimize-u-gates-with-fractional-rx-gates}

У цьому розділі ми демонструємо, як оптимізувати U гейти за допомогою дробових гейтів RX, спираючись на ту саму схему, представлену в попередньому розділі.
Ми транспілюємо схему, використовуючи лише дробові RX ворота, виключаючи RZZ ворота.
Вводячи користувацьке правило декомпозиції, як показано нижче,
ми можемо зменшити кількість однокубітних воріт, необхідних для реалізації U воріт.

Ця функція наразі обговорюється в цій [GitHub issue.](https://github.com/Qiskit/qiskit/issues/13455)

```python
# special decomposition rule for UGate
x = ParameterVector("x", 3)
zxz = QuantumCircuit(1)
zxz.rz(x[2] - np.pi / 2, 0)
zxz.rx(x[0], 0)
zxz.rz(x[1] + np.pi / 2, 0)
DEFAULT_EQUIVALENCE_LIBRARY.add_equivalence(UGate(x[0], x[1], x[2]), zxz)
```

Далі ми застосовуємо транспілятор, використовуючи трансляцію `constructor-beta`, надану пакетом `qiskit-basis-constructor`.
В результаті загальна кількість воріт зменшується порівняно з попередньою транспіляцією.

```python
pm_f = generate_preset_pass_manager(
    optimization_level=optimization_level,
    target=target,
    translation_method="constructor-beta",
)
t_qc = pm_f.run(qc)
print(t_qc.count_ops())
t_qc.draw("mpl")
```

```text
OrderedDict({'rz': 16, 'rx': 9, 'cz': 4})
```

![Output of the previous code cell](/docs/images/tutorials/fractional-gates/extracted-outputs/b19aae7c-1.avif)

## Наступні кроки {#next-steps}
<Admonition type="tip" title="Рекомендації">
Якщо ця робота зацікавила тебе, тобі можуть бути цікаві наступні матеріали:
- Посібник [Дробові гейти](/guides/fractional-gates)
- [Коли *не* використовувати дробові гейти](/guides/fractional-gates#when-not-to-use)
- Довідник API про транспілерний прохід [`FoldRzzAngle`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/transpiler-passes-fold-rzz-angle)
- Підручник [Навчання квантових ядер](/tutorials/quantum-kernel-training)
- Урок [Квантові ядра](/learning/courses/quantum-machine-learning/quantum-kernel-methods) курсу Квантового машинного навчання
</Admonition>

<TutorialFeedback />
