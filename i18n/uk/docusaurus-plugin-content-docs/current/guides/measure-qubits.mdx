---
title: "Вимірювання кубітів"
sidebar_label: "Вимірювання кубітів"
description: "Дізнайся, як вимірювати кубіти, включно з обмеженнями щодо того, де можна використовувати вимірювання."
notebook_path: "guides/measure-qubits.ipynb"
---

{/* doqumentation-source-hash: 421a17fa */}

<OpenInLabBanner notebookPath="guides/measure-qubits.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Версії пакетів">

Код на цій сторінці розроблено з використанням наведених нижче залежностей.
Рекомендуємо використовувати ці або новіші версії.

```
qiskit[all]~=2.4.0
```
</AccordionItem>
</Accordion>
Щоб отримати інформацію про стан кубіта, можна _виміряти_ його на [класичний біт](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Clbit). У Qiskit вимірювання виконуються в обчислювальному базисі, тобто в однокубітному базисі Паулі-$Z$. Тому вимірювання дає результат 0 або 1, залежно від перекриття з власними станами Паулі-$Z$ $|0\rangle$ і $|1\rangle$:

$$
|q\rangle \xrightarrow{measure}\begin{cases}
      0 (\text{outcome}+1), \text{with probability } p_0=|\langle q|0\rangle|^{2}\text{,} \\
      1 (\text{outcome}-1), \text{with probability } p_1=|\langle q|1\rangle|^{2}\text{.}
    \end{cases}
$$

## Додавання вимірювання до схеми {#apply-a-measurement-to-a-circuit}

Є кілька способів додати вимірювання до схеми:

### Метод `QuantumCircuit.measure` {#quantumcircuitmeasure-method}

Використовуй метод [`measure`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure), щоб виміряти [`QuantumCircuit`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#quantumcircuit-class).

Приклади:

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(5, 5)
qc.x(0)
qc.x(1)
qc.x(4)
qc.measure(
    range(5), range(5)
)  #  Measures all qubits into the corresponding clbit.
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c636c20>
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure(1, 0)  # Measure qubit 1 into the classical bit 0.
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c636830>
```

### Клас `Measure` {#measure-class}

Клас Qiskit [Measure](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure) вимірює вказані кубіти.

```python
from qiskit.circuit import Measure

qc = QuantumCircuit(3, 1)
qc.x([0, 1])
qc.append(Measure(), [0], [0])  # measure qubit 0 into clbit 0
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c6369e0>
```

### Метод `QuantumCircuit.measure_all` {#quantumcircuitmeasure-all-method}

Щоб виміряти всі кубіти у відповідні класичні біти, використовуй метод [`measure_all`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_all). За замовчуванням цей метод додає нові класичні біти в `ClassicalRegister` для збереження результатів вимірювань.

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure_all()  # Measure all qubits.
```

### Метод `QuantumCircuit.measure_active` {#quantumcircuitmeasure-active-method}

Щоб виміряти всі кубіти, які не перебувають у стані простою, використовуй метод [`measure_active`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_active). Цей метод створює новий `ClassicalRegister` розміром, що дорівнює кількості активних кубітів, які вимірюються.

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure_active()  # Measure qubits that are not idle, that is, qubits 0 and 2.
```

## Наступні кроки {#next-steps}

<Admonition type="tip" title="Рекомендації">
- Клас [`Measure`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure)
- Метод [`measure_all`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_all)
- Метод [`measure_active`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_active)
- Метод [`random_circuit`](https://docs.quantum.ibm.com/api/qiskit/circuit_random#qiskit.circuit.random.random_circuit)
- [Вимірювання всередині схеми](/guides/execute-dynamic-circuits#midcircuit) (доступно лише при використанні Qiskit Runtime)
</Admonition>
