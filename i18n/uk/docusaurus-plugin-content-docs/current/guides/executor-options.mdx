---
description: 'Вкажи опції при роботі з примітивом Executor.'
notebook_path: guides/executor-options.ipynb
sidebar_label: 'Визначення опцій Executor'
title: 'Визначення опцій Executor'
---

{/* doqumentation-source-hash: 750a63c3 */}


<OpenInLabBanner notebookPath="guides/executor-options.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Версії пакетів">

Код на цій сторінці було розроблено з використанням таких вимог.
Рекомендуємо використовувати ці або новіші версії.

```
qiskit-ibm-runtime~=0.46.1
```
</AccordionItem>
</Accordion>
You can use options to customize the Executor primitive. Unlike Sampler and Estimator, Executor has very few options since the majority of the design intents is encoded in the samplex.

<Admonition type="note" title="Примітки">

- Можна переглядати доступні опції та оновлювати їх значення під час або після ініціалізації примітива.

- Використовуй метод `update()` для застосування змін до атрибуту `options`.

- - Атрибут `options` має тип Python `dataclass`. Можна використовувати вбудований метод `asdict` для конвертування у словник.

- Executor може обробляти лише інформацію, закодовану в samplex. Зверніться до [посібників Samplomatic](https://qiskit.github.io/samplomatic/guides/index), щоб зрозуміти обмеження.
</Admonition>

<span id="pass-options"></span>

## Встановлення опцій Executor {#set-executor-options}

Якщо опція вказана як під час, так і після ініціалізації примітива, використовується значення, встановлене після ініціалізації примітива.

### Ініціалізація примітива {#primitive-initialization}

При ініціалізації Executor можна передати екземпляр класу опцій або словник, після чого він робить копію цих опцій. Таким чином, зміна оригінального словника або екземпляра опцій не впливає на опції, що належать примітиву.

#### Клас опцій {#options-class}

При створенні екземпляра класу `Executor` можна передати екземпляр класу опцій. Ці опції потім застосовуються при використанні `run()` для виконання обчислення. Вкажи опції у такому форматі: `options.option.sub-option.sub-sub-option = choice`. Наприклад: `options.environment.log_level = INFO`.

Приклад:

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit-ibm-runtime
```

```python
from qiskit_ibm_runtime import QiskitRuntimeService, Executor
from qiskit_ibm_runtime.options import ExecutorOptions

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

options = ExecutorOptions(
    environment={"log_level": "INFO"},
    execution={"init_qubits": True},
)

# or use the following instead:

options = ExecutorOptions()
options.environment.log_level = "INFO"
options.execution.init_qubits = True

executor = Executor(mode=backend, options=options)
```

#### Словник {#dictionary}

Можна вказати опції як словник при ініціалізації Executor.

```python
from qiskit_ibm_runtime import QiskitRuntimeService, Executor

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)

# Setting options during primitive initialization
executor = Executor(
    backend,
    options={
        "environment": {"log_level": "INFO"},
        "execution": {"init_qubits": True},
    },
)
```

<span id="options-table"></span>

## Доступні опції {#available-options}

Наступна таблиця документує опції з останньої версії `qiskit-ibm-runtime`. Щоб переглянути старіші версії опцій, відвідай [довідник API `qiskit-ibm-runtime`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime) та обери попередню версію.

<Accordion>
<AccordionItem title="`environment`">
[Документація API `environment`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-sampler-options#environment)
<Accordion>
<AccordionItem title="`environment.job_tags`">

Список тегів.

**Варіанти**: None

**За замовчуванням**: None
  </AccordionItem>

<AccordionItem title="`environment.log_level`">

**Варіанти**: DEBUG, INFO, WARNING, ERROR, CRITICAL

**За замовчуванням**: WARNING
  </AccordionItem>

<AccordionItem title="`environment.private`">

**Варіанти**: `True`, `False`

**За замовчуванням**: `False`
  </AccordionItem>

  <AccordionItem title="`environment.max_execution_time`">

**Варіанти**: Ціле число секунд у діапазоні [1, 10800]

**За замовчуванням**: 10800 (3 години)
  </AccordionItem>

</Accordion>

  </AccordionItem>

<AccordionItem title="`execution`">
[Документація API `execution`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-sampler-options#execution)
<Accordion>
<AccordionItem title="`execution.init_qubits`">
Чи скидати кубіти до основного стану для кожного вимірювання.

**Варіанти**: `True`, `False`

**За замовчуванням**: `True`
  </AccordionItem>

<AccordionItem title="`execution.rep_delay`">
Затримка між вимірюванням та наступною квантовою схемою.

**Варіанти**: Значення у діапазоні, наданому `backend.rep_delay_range`

**За замовчуванням**: Визначається `backend.default_rep_delay`
  </AccordionItem>

<AccordionItem title="`execution.meas_type`">

**Варіанти**: `classified`, `kerneled`, `avg_kerneled`

**За замовчуванням**: `classified`
  </AccordionItem>

</Accordion>

  </AccordionItem>

  <AccordionItem title="`experimental`">

Експериментальні опції, коли доступні.

  </AccordionItem>

</Accordion>

## Наступні кроки {#next-steps}

<Admonition type="tip" title="Рекомендації">
- Ознайомся з документацією API [ExecutionOptionsV2](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-execution-options-v2).
- Ознайомся з документацією API [EnvironmentOptions](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-environment-options).
</Admonition>
