---
title: ติดตั้ง Qiskit SDK จากซอร์ส
description: เรียนรู้วิธีติดตั้ง Qiskit เวอร์ชัน development

---
{/* doqumentation-source-hash: 3602a588 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore rustup */}

# ติดตั้ง Qiskit SDK และ Qiskit Runtime จากซอร์ส {#install-the-qiskit-sdk-and-qiskit-runtime-from-source}

การติดตั้ง Qiskit SDK จากซอร์สช่วยให้เข้าถึงเวอร์ชัน development ปัจจุบันได้ แทนที่จะใช้เวอร์ชันจาก Python Package Index (PyPI) ซึ่งทำให้สามารถตรวจสอบและขยายโค้ด Qiskit เวอร์ชันล่าสุดได้อย่างมีประสิทธิภาพมากขึ้น

## สร้างและเปิดใช้งาน virtual environment ใหม่ {#create-and-activate-a-new-virtual-environment}

1. ไปยัง project directory และสร้าง environment เบื้องต้นที่มีแค่ Python ติดตั้งอยู่

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

1. เปิดใช้งาน environment ใหม่

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
             ```text
             .venv\Scripts\Activate.ps1
             ```
           </TabItem>
         </OperatingSystemTabs>

## ติดตั้ง Rust compiler {#install-the-rust-compiler}

ต้องติดตั้ง Rust compiler บนระบบเพื่อ compile Qiskit ใช้ [`rustup`](https://rustup.rs/) ซึ่งเป็น Rust installer ข้ามแพลตฟอร์ม หรือ [วิธีติดตั้งอื่น](https://forge.rust-lang.org/infra/other-installation-methods.html)

## ติดตั้ง Qiskit {#install-qiskit}

ทำตามขั้นตอนเหล่านี้เพื่อติดตั้ง Qiskit:

1. Clone Qiskit repository

```bash
git clone https://github.com/Qiskit/qiskit.git
```

2. เข้าไปยัง directory `qiskit`

```bash
cd qiskit
```

3. (ไม่บังคับ) ถ้าต้องการรัน tests หรือ linting checks ให้ติดตั้ง developer requirements

```bash
pip install -r requirements-dev.txt
```

4. ติดตั้ง `qiskit`

  * **Standard install**:

    ```bash
    pip install .
    ```

  * **Editable mode**: ในโหมดนี้ไม่จำเป็นต้องติดตั้ง Qiskit ใหม่เมื่อมีการเปลี่ยนแปลงโค้ดในโปรเจกต์

    ```bash
    pip install -e .
    ```

    ใน editable mode extension ที่ compile แล้วจะถูก build ใน _debug mode_ โดยไม่มีการ optimize ซึ่งส่งผลต่อ runtime performance ของโค้ดที่ compile แล้ว เพื่อ build extension เหล่านั้นพร้อมการ optimize ให้รันคำสั่งต่อไปนี้เพื่อ rebuild binary ใน _release mode_:

    ```bash
    python setup.py build_rust --release --inplace
    ```

    <Admonition type="note">
    ถ้ากำลังทำงานกับโค้ด Rust ใน Qiskit จะต้อง rebuild extension code ทุกครั้งที่แก้ไขโค้ดใน local ใน editable mode Rust extension จะถูก build เฉพาะตอนรันคำสั่งติดตั้งเท่านั้น ดังนั้นการเปลี่ยนแปลงโค้ด Rust ใน local จะไม่สะท้อนใน package ที่ติดตั้งจนกว่าจะ rebuild extension โดยรัน `build_rust` ใหม่อีกครั้ง (ใช้หรือไม่ใช้ `--release` ขึ้นอยู่กับว่าต้องการ build ใน release หรือ debug mode)
    </Admonition>

## ติดตั้ง Qiskit Runtime {#install-qiskit-runtime}

ทำตามขั้นตอนเหล่านี้ถ้าต้องการติดตั้ง Qiskit Runtime:

1. Clone Qiskit Runtime repository

```bash
git clone https://github.com/Qiskit/qiskit-ibm-runtime.git
```

2. เข้าไปยัง directory `qiskit-ibm-runtime`

```bash
cd qiskit-ibm-runtime
```

3. ติดตั้ง `qiskit-runtime` แนะนำให้ใช้ [virtual environment](https://docs.python.org/3/library/venv.html) เพื่อหลีกเลี่ยงการรบกวน Python installation หลักของระบบ

  * **Standard install**:

    ```bash
    pip install .
    ```

  * **Editable mode**: ในโหมดนี้ไม่จำเป็นต้องติดตั้ง Qiskit ใหม่เมื่อมีการเปลี่ยนแปลงโค้ดในโปรเจกต์

    ```bash
    pip install -e .
    ```

    ใน editable mode extension ที่ compile แล้วจะถูก build ใน _debug mode_ โดยไม่มีการ optimize

4. (ไม่บังคับ) ถ้าต้องการรัน tests หรือ linting checks ให้ติดตั้ง developer requirements แนะนำให้ใช้ [virtual environment](https://docs.python.org/3/library/venv.html) เพื่อหลีกเลี่ยงการรบกวน Python installation หลักของระบบ

```bash
pip install -e ".[dev]"
```

## ขั้นตอนถัดไป {#next-steps}

<Admonition type="tip" title="Recommendations">
  - อ่าน [contributing guidelines](https://github.com/Qiskit/qiskit/blob/main/CONTRIBUTING.md) เพื่อมีส่วนร่วมกับ Qiskit SDK แบบ open-source
  - [รัน Hello world program](/guides/hello-world)
  - ลอง tutorial เช่น [Grover's algorithm](/tutorials/grovers-algorithm)
</Admonition>
