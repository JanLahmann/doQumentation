---
title: "คลาส Operator"
sidebar_label: "คลาส Operator"
description: "คำอธิบายเชิงลึกเกี่ยวกับการใช้คลาส Operator ใน Qiskit"
notebook_path: "guides/operator-class.ipynb"
---

{/* doqumentation-source-hash: f33a6b98 */}

<OpenInLabBanner notebookPath="guides/operator-class.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>Package versions</b></summary>

The code on this page was developed using the following requirements.
We recommend using these versions or newer.

```
qiskit[all]~=2.3.0
```
</details>
หน้านี้แสดงวิธีใช้คลาส [`Operator`](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator) สำหรับภาพรวมระดับสูงของการแทนค่า operator ใน Qiskit รวมถึงคลาส `Operator` และคลาสอื่นๆ ดูที่ [ภาพรวมของคลาส operator](./operators-overview)

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit
```

```python
import numpy as np
from qiskit.circuit import QuantumCircuit
from qiskit.circuit.library import CXGate, RXGate, XGate
from qiskit.quantum_info import Operator, Pauli, process_fidelity
```

## แปลงคลาสต่างๆ เป็น Operator {#convert-classes-to-operators}

คลาสอื่นๆ หลายคลาสใน Qiskit สามารถแปลงเป็น object `Operator` ได้โดยตรงโดยใช้วิธี operator initialization ตัวอย่างเช่น:

* object `Pauli`
* object `Gate` และ `Instruction`
* object `QuantumCircuit`

โปรดทราบว่าข้อสุดท้ายหมายความว่าคุณสามารถใช้คลาส `Operator` เป็น unitary simulator เพื่อคำนวณ unitary matrix สุดท้ายสำหรับ quantum circuit โดยไม่ต้องเรียก simulator Backend ถ้า Circuit มีการดำเนินการที่ไม่รองรับ จะเกิด exception การดำเนินการที่ไม่รองรับได้แก่: measure, reset, conditional operations หรือ Gate ที่ไม่มีนิยาม matrix หรือการแตกตัวในรูปของ Gate ที่มีนิยาม matrix

```python
# Create an Operator from a Pauli object

pauliXX = Pauli("XX")
Operator(pauliXX)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j],
          [1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an Operator for a Gate object
Operator(CXGate())
```

```text
Operator([[1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an operator from a parameterized Gate object
Operator(RXGate(np.pi / 2))
```

```text
Operator([[0.70710678+0.j        , 0.        -0.70710678j],
          [0.        -0.70710678j, 0.70710678+0.j        ]],
         input_dims=(2,), output_dims=(2,))
```

```python
# Create an operator from a QuantumCircuit object
circ = QuantumCircuit(10)
circ.h(0)
for j in range(1, 10):
    circ.cx(j - 1, j)

# Convert circuit to an operator by implicit unitary simulation
Operator(circ)
```

```text
Operator([[ 0.70710678+0.j,  0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          ...,
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.70710678+0.j, -0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j]],
         input_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2), output_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2))
```

## ใช้ Operator ใน Circuit {#use-operators-in-circuits}

`Operator` แบบ unitary สามารถแทรกเข้าไปใน `QuantumCircuit` ได้โดยตรงโดยใช้เมธอด `QuantumCircuit.append` ซึ่งจะแปลง `Operator` เป็น object `UnitaryGate` แล้วเพิ่มเข้าไปใน Circuit

ถ้า operator ไม่ใช่ unitary จะเกิด exception สามารถตรวจสอบได้โดยใช้ฟังก์ชัน `Operator.is_unitary()` ซึ่งคืนค่า `True` ถ้า operator เป็น unitary และ `False` หากไม่ใช่

```python
# Create an operator
XX = Operator(Pauli("XX"))

# Add to a circuit
circ = QuantumCircuit(2, 2)
circ.append(XX, [0, 1])
circ.measure([0, 1], [0, 1])
circ.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/operator-class/extracted-outputs/223c07c9-1b1d-4a33-a397-091cac2ec9e3-0.svg)

โปรดทราบว่าในตัวอย่างข้างต้น operator ถูก initialize จาก object `Pauli` แต่ object `Pauli` ก็สามารถแทรกเข้าไปใน Circuit โดยตรงได้เช่นกันและจะถูกแปลงเป็นลำดับของ single-qubit Pauli Gate:

```python
# Add to a circuit
circ2 = QuantumCircuit(2, 2)
circ2.append(Pauli("XX"), [0, 1])
circ2.measure([0, 1], [0, 1])
circ2.draw()
```

```text
┌────────────┐┌─┐
q_0: ┤0           ├┤M├───
     │  Pauli(XX) │└╥┘┌─┐
q_1: ┤1           ├─╫─┤M├
     └────────────┘ ║ └╥┘
c: 2/═══════════════╩══╩═
                    0  1
```

## รวม Operator {#combine-operators}

Operator สามารถรวมกันได้หลายวิธี

### Tensor product {#tensor-product}

Operator สอง ตัว $A$ และ $B$ สามารถรวมกันเป็น tensor product operator $A\otimes B$ ได้โดยใช้ฟังก์ชัน `Operator.tensor` โปรดทราบว่าถ้าทั้ง $A$ และ $B$ เป็น single-qubit operator แล้ว `A.tensor(B)` = $A\otimes B$ จะมี subsystem ที่ index ว่าเมทริกซ์ $B$ อยู่ที่ subsystem 0 และเมทริกซ์ $A$ อยู่ที่ subsystem 1

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.tensor(B)
```

```text
Operator([[ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j],
          [ 0.+0.j, -0.+0.j,  0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### Tensor expansion {#tensor-expansion}

การดำเนินการที่เกี่ยวข้องใกล้ชิดคือ `Operator.expand` ซึ่งทำงานเหมือน tensor product แต่ในลำดับย้อนกลับ ดังนั้น สำหรับ operator สองตัว $A$ และ $B$ คุณจะได้ `A.expand(B)` = $B\otimes A$ โดย subsystem จะ index ว่าเมทริกซ์ $A$ อยู่ที่ subsystem 0 และเมทริกซ์ $B$ อยู่ที่ subsystem 1

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.expand(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j,  0.+0.j, -0.+0.j, -1.+0.j],
          [ 0.+0.j,  0.+0.j, -1.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### Composition {#composition}

คุณยังสามารถ compose operator สองตัว $A$ และ $B$ เพื่อทำการคูณเมทริกซ์โดยใช้เมธอด `Operator.compose` โดย `A.compose(B)` คืนค่า operator ที่มีเมทริกซ์ $B.A$:

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j],
          [-1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

คุณยังสามารถ compose ในลำดับย้อนกลับได้โดยใช้ $B$ ด้านหน้า $A$ ผ่าน kwarg `front` ของ `compose`: `A.compose(B, front=True)` = $A.B$:

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B, front=True)
```

```text
Operator([[ 0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

### Subsystem composition {#subsystem-composition}

โปรดทราบว่า compose ก่อนหน้าต้องการให้ output dimension รวมของ operator แรก $A$ เท่ากับ input dimension รวมของ operator ที่ compose $B$ (และในทำนองเดียวกัน output dimension ของ $B$ ต้องเท่ากับ input dimension ของ $A$ เมื่อ compose ด้วย `front=True`)

คุณยังสามารถ compose operator ขนาดเล็กกว่ากับ subsystem ที่เลือกของ operator ขนาดใหญ่กว่าได้โดยใช้ kwarg `qargs` ของ `compose` ไม่ว่าจะใช้หรือไม่ใช้ `front=True` ในกรณีนี้ input และ output dimension ที่เกี่ยวข้องของ subsystem ที่ถูก compose ต้องตรงกัน *โปรดทราบว่า operator ขนาดเล็กต้องเป็น argument ของเมธอด* `compose` *เสมอ*

ตัวอย่างเช่น การ compose two-qubit Gate กับ three-qubit operator:

```python
# Compose XZ with a 3-qubit identity operator
op = Operator(np.eye(2**3))
XZ = Operator(Pauli("XZ"))
op.compose(XZ, qargs=[0, 2])
```

```text
Operator([[ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
           -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

```python
# Compose YX in front of the previous operator
op = Operator(np.eye(2**3))
YX = Operator(Pauli("YX"))
op.compose(YX, qargs=[0, 2], front=True)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j],
          [0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

### การรวมเชิงเส้น {#linear-combinations}

Operator ยังสามารถรวมกันได้โดยใช้ตัวดำเนินการเชิงเส้นมาตรฐานสำหรับการบวก การลบ และการคูณสเกลาร์ด้วยจำนวนเชิงซ้อน

```python
XX = Operator(Pauli("XX"))
YY = Operator(Pauli("YY"))
ZZ = Operator(Pauli("ZZ"))

op = 0.5 * (XX + YY - 3 * ZZ)
op
```

```text
Operator([[-1.5+0.j,  0. +0.j,  0. +0.j,  0. +0.j],
          [ 0. +0.j,  1.5+0.j,  1. +0.j,  0. +0.j],
          [ 0. +0.j,  1. +0.j,  1.5+0.j,  0. +0.j],
          [ 0. +0.j,  0. +0.j,  0. +0.j, -1.5+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

สิ่งสำคัญคือ ในขณะที่ `tensor`, `expand` และ `compose` รักษาความเป็น unitary ของ unitary operator ไว้ แต่การรวมเชิงเส้นไม่รักษา ดังนั้น การบวก unitary operator สองตัวโดยทั่วไปจะได้ผลลัพธ์เป็น non-unitary operator:

```python
op.is_unitary()
```

```text
False
```

### การแปลงโดยนัยเป็น Operator {#implicit-conversion-to-operators}

โปรดทราบว่าสำหรับเมธอดต่อไปนี้ทั้งหมด ถ้า object ที่สองยังไม่ใช่ object `Operator` จะถูกแปลงเป็น `Operator` โดยนัยผ่านเมธอดนั้น ซึ่งหมายความว่าสามารถส่งเมทริกซ์เข้าไปได้โดยตรงโดยไม่ต้องแปลงเป็น `Operator` ก่อนอย่างชัดเจน ถ้าไม่สามารถแปลงได้ จะเกิด exception

```python
# Compose with a matrix passed as a list
Operator(np.eye(2)).compose([[0, 1], [1, 0]])
```

```text
Operator([[0.+0.j, 1.+0.j],
          [1.+0.j, 0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

## เปรียบเทียบ Operator {#compare-operators}

Operator มีเมธอดความเท่าเทียมที่ใช้ตรวจสอบว่า operator สองตัวเท่ากันโดยประมาณหรือไม่

```python
Operator(Pauli("X")) == Operator(XGate())
```

```text
True
```

โปรดทราบว่านี่จะตรวจสอบว่าแต่ละ element ของเมทริกซ์ของ operator เท่ากันโดยประมาณ unitary สองตัวที่ต่างกันเพียง global phase จะไม่ถือว่าเท่ากัน:

```python
Operator(XGate()) == np.exp(1j * 0.5) * Operator(XGate())
```

```text
False
```

### Process fidelity {#process-fidelity}

คุณยังสามารถเปรียบเทียบ operator โดยใช้ฟังก์ชัน `process_fidelity` จากโมดูล *Quantum Information* นี่คือปริมาณเชิงทฤษฎีสารสนเทศที่บอกว่า quantum channel สองช่องมีความใกล้เคียงกันแค่ไหน และในกรณีของ unitary operator จะไม่ขึ้นอยู่กับ global phase

```python
# Two operators which differ only by phase
op_a = Operator(XGate())
op_b = np.exp(1j * 0.5) * Operator(XGate())

# Compute process fidelity
F = process_fidelity(op_a, op_b)
print("Process fidelity =", F)
```

```text
Process fidelity = 1.0
```

โปรดทราบว่า process fidelity โดยทั่วไปเป็นตัวชี้วัดความใกล้เคียงที่ถูกต้องก็ต่อเมื่อ operator input เป็น unitary (หรือ CP ในกรณีของ quantum channel) และจะเกิด exception ถ้า input ไม่ใช่ CP
## ขั้นตอนถัดไป {#next-steps}

<Admonition type="tip" title="คำแนะนำ">
  - สำรวจ reference [Operator API](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator#operator)
</Admonition>
