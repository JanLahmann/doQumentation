---
description: 'เรียนรู้วิธีติดตั้งเวอร์ชัน development ของ IBM Quantum Compute client'
title: 'ติดตั้ง IBM Quantum Compute client จาก source'
---

{/* doqumentation-source-hash: 97061531 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore rustup */}

# ติดตั้ง IBM Quantum Compute client จาก source {#install-the-ibm-quantum-compute-client-from-source}

เมื่อติดตั้ง `qiskit-ibm-runtime` จาก source คุณสามารถเข้าถึงเวอร์ชัน development ปัจจุบันแทนที่จะใช้เวอร์ชันใน Python Package Index (PyPI) repository ดังนั้น คุณสามารถตรวจสอบและขยายโค้ดเวอร์ชันล่าสุดได้อย่างมีประสิทธิภาพมากขึ้น

## สร้างและเปิดใช้งาน virtual environment ใหม่ {#create-and-activate-a-new-virtual-environment}

1. ไปยัง directory โครงการของคุณและสร้างสภาพแวดล้อมขั้นต่ำที่มีเฉพาะ Python ติดตั้งอยู่


        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">

            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">

            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">

            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

1. เปิดใช้งาน environment ใหม่ของคุณ


         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">

             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">

             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">

             ```text
             .venv\Scripts\Activate.ps1
             ```
           </TabItem>
         </OperatingSystemTabs>

## ติดตั้ง `qiskit-ibm-runtime` {#install-qiskit-ibm-runtime}

ทำตามขั้นตอนเหล่านี้เพื่อติดตั้ง `qiskit-ibm-runtime`:

1. Clone repository `qiskit-ibm-runtime`


```bash
git clone https://github.com/Qiskit/qiskit-ibm-runtime.git
```

2. เปลี่ยนไปยัง directory `qiskit-ibm-runtime`


```bash
cd qiskit-ibm-runtime
```

3. ติดตั้ง `qiskit-runtime` แนะนำให้ใช้ [virtual environment](https://docs.python.org/3/library/venv.html) เพื่อหลีกเลี่ยงการรบกวน Python installation ทั่วโลกของคุณ


  * **การติดตั้งแบบมาตรฐาน**:


    ```bash
    pip install .
    ```

  * **Editable mode**: ในโหมดนี้ คุณไม่ต้องติดตั้ง Qiskit ใหม่เมื่อมีการเปลี่ยนแปลงโค้ดในโครงการ


    ```bash
    pip install -e .
    ```

    ในโหมด editable, compiled extensions จะถูก build ใน _debug mode_ โดยไม่มีการ optimize

4. (ทางเลือก) หากต้องการรัน tests หรือ linting checks ให้ติดตั้ง developer requirements แนะนำให้ใช้ [virtual environment](https://docs.python.org/3/library/venv.html) เพื่อหลีกเลี่ยงการรบกวน Python installation ทั่วโลกของคุณ


```bash
pip install -e ".[dev]"
```

## ขั้นตอนถัดไป {#next-steps}

<Admonition type="tip" title="คำแนะนำ">
  - อ่าน [contributing guidelines](https://github.com/Qiskit/qiskit-ibm-runtime/blob/main/CONTRIBUTING.md) เพื่อมีส่วนร่วมกับ `qiskit-ibm-runtime`
  - [ติดตั้ง Qiskit SDK จาก source](/guides/install-qiskit-source)
  - [รันโปรแกรมแรกของคุณ](/guides/hello-world)
  - ลองทำ tutorial เช่น [Grover's algorithm](/tutorials/grovers-algorithm)
</Admonition>
