---
title: แนะนำ Qiskit Serverless
description: ใช้ Qiskit Serverless เพื่อรันงาน quantum-classical ข้ามคลาวด์และบนซูเปอร์คอมพิวเตอร์เชิงควอนตัม
---

{/* doqumentation-source-hash: 332f255e */}

# Qiskit Serverless คืออะไร? {#what-is-qiskit-serverless}

Qiskit Serverless มอบอินเทอร์เฟซที่ใช้งานง่ายสำหรับการรันงานบนทรัพยากร quantum-classical ซึ่งรวมถึงการ deploy โปรแกรมไปยัง IBM Quantum&reg; Platform และการรันงานจากระยะไกล รวมถึงการจัดการทรัพยากรที่สะดวกสำหรับกรณีการใช้งาน multi-cloud และ quantum-centric supercomputing

ผู้ใช้ระดับ Premium สามารถสร้าง deploy และรันงานของตัวเองจากระยะไกลบนทรัพยากรการคำนวณแบบคลาสสิกที่ IBM Quantum Platform จัดเตรียมให้

<Admonition type="note">
    นี่คือฟีเจอร์ทดลองที่พร้อมใช้งานเฉพาะสำหรับผู้ใช้ IBM Quantum Premium Plan, Flex Plan และ On-Prem (ผ่าน IBM Quantum Platform API) Plan เท่านั้น
</Admonition>

## ควรใช้ Qiskit Serverless เมื่อไหร่? {#when-should-i-use-qiskit-serverless}

Qiskit Serverless ช่วยจัดการทรัพยากรทั้งแบบคลาสสิกและควอนตัมตลอดกระบวนการทำงาน [Qiskit patterns](./intro-to-patterns) ทั้งหมด ตัวอย่างบางส่วนได้แก่:

- การทำงานแบบขนานสำหรับงานคลาสสิก เช่น การประมวลผลล่วงหน้าและการประมวลผลหลังจาก
- การคงงานที่ใช้เวลานานไว้บนคลาวด์ แม้ว่าจะปิดแล็ปท็อปแล้ว
- การ deploy โปรแกรมที่นำมาใช้ซ้ำได้

## มีอะไรรวมอยู่บ้าง? {#what-is-included}

ทรัพยากรการคำนวณต่อไปนี้รวมอยู่ในสภาพแวดล้อม Qiskit Serverless โดยค่าเริ่มต้น ผู้ใช้สามารถติดต่อ [support](/guides/support) เพื่อเพิ่มขีดจำกัดเหล่านี้

- CPU เสมือน 16 cores
- RAM 64GB
- งานที่ active พร้อมกันสูงสุด 2 งานต่อผู้ใช้

## ติดตั้ง Qiskit Serverless {#install-qiskit-serverless}

เพื่อใช้ Qiskit Serverless บน IBM Quantum Platform ให้ติดตั้งแพ็กเกจต่อไปนี้:

```bash
pip install qiskit-ibm-catalog qiskit_serverless
```

`qiskit_ibm_catalog` มอบเครื่องมือฝั่ง client สำหรับการอัปโหลดและรันโปรแกรมจากระยะไกล ในขณะที่ `qiskit_serverless` มอบเครื่องมือฝั่ง server สำหรับการกระจายการคำนวณและการจัดการข้อมูล แพ็กเกจเหล่านี้ต้องการ `python` 3.11 ขึ้นไป สำหรับผู้ใช้และองค์กรที่ต้องการรัน Qiskit Serverless บนโครงสร้างพื้นฐานแบบกำหนดเอง ให้ทำตามคู่มือ [Cloud infrastructure setup](https://qiskit.github.io/qiskit-serverless/)

<Admonition type="tip">
ปัจจุบัน ตารางงาน IBM Quantum แสดงเฉพาะงาน Qiskit Runtime เท่านั้น ใช้ `job.status()` เพื่อดูสถานะปัจจุบันของงาน Qiskit Serverless
</Admonition>

## ขั้นตอนต่อไป {#next-steps}

<Admonition type="info" title="คำแนะนำ">

- สำรวจวิธี [เขียนโปรแกรม Qiskit Serverless แรกของคุณ](./serverless-first-program)
- ลองทำ [tutorials](https://docs.quantum.ibm.com/tutorials) บางส่วน

</Admonition>
