---
title: ソースから Qiskit Runtime クライアントをインストールする
description: Qiskit Runtime クライアントの開発バージョンのインストール方法を説明します。
sidebar_label: ソースからのインストール
---

{/* doqumentation-source-hash: 0d914e9e */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore rustup */}

# ソースから Qiskit Runtime クライアントをインストールする {#install-the-qiskit-runtime-client-from-source}

ソースから Qiskit Runtime をインストールすると、Python Package Index（PyPI）リポジトリのバージョンを使用する代わりに、現在の開発バージョンにアクセスできます。これにより、最新バージョンのコードをより効率的に調査・拡張できます。

## 新しい仮想環境の作成とアクティブ化 {#create-and-activate-a-new-virtual-environment}

1. プロジェクト・ディレクトリに移動し、Python のみがインストールされた最小限の環境を作成します。

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

1. 新しい環境をアクティブ化します。

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
             ```text
             .venv\Scripts\Activate.ps1
             ```
           </TabItem>
         </OperatingSystemTabs>

## Qiskit Runtime のインストール {#install-qiskit-runtime}

以下の手順で Qiskit Runtime をインストールします：

1. Qiskit Runtime リポジトリをクローンします。

```bash
git clone https://github.com/Qiskit/qiskit-ibm-runtime.git
```

2. `qiskit-ibm-runtime` ディレクトリに移動します。

```bash
cd qiskit-ibm-runtime
```

3. `qiskit-runtime` をインストールします。グローバルな Python インストールを汚染しないために、[仮想環境](https://docs.python.org/3/library/venv.html)の使用をお勧めします。

  * **標準インストール**：

    ```bash
    pip install .
    ```

  * **編集可能モード**：このモードでは、プロジェクトにコードの変更があっても Qiskit を再インストールする必要はありません。

    ```bash
    pip install -e .
    ```

    編集可能モードでは、コンパイルされた拡張機能は最適化なしの _デバッグ・モード_ でビルドされます。

4. （オプション）テストやリンティング・チェックを実行する場合は、開発者向け要件をインストールします。グローバルな Python インストールを汚染しないために、[仮想環境](https://docs.python.org/3/library/venv.html)の使用をお勧めします。

```bash
pip install -e ".[dev]"
```

## 次のステップ {#next-steps}

<Admonition type="tip" title="おすすめ">
  - `qiskit-ibm-runtime` に貢献するために[コントリビューション・ガイドライン](https://github.com/Qiskit/qiskit-ibm-runtime/blob/main/CONTRIBUTING.md)を読みます。
  - [ソースから Qiskit SDK をインストールする](/guides/install-qiskit-source)。
  - [最初のプログラムを実行する](/guides/hello-world)。
  - [グローバーのアルゴリズム](/tutorials/grovers-algorithm)などのチュートリアルを試してみてください。
</Admonition>
