---
unlisted: true
sidebar_class_name: hidden
title: "Qiskit 2によるDiVincenzo基準ハンズオン入門"
sidebar_label: "Qiskit 2によるDiVincenzo基準ハンズオン入門"
notebook_path: "workshop/06_divincenzo_criteria_lab-2_solution.ipynb"
source_file: "06_divincenzo_criteria_lab-2_solution.ipynb"
description: "物理学者David DiVincenzoは、量子コンピュータのあらゆる物理的実装に必要な5つの主要要件と、量子通信のための2つの追加基準を示しました。"
---

{/* doqumentation-source-hash: 85cd7e8d */}

<OpenInLabBanner notebookPath="workshop/06_divincenzo_criteria_lab-2_solution.ipynb" />

## はじめに {#introduction}

物理学者David DiVincenzoは、量子コンピュータのあらゆる物理的実装に必要な5つの主要要件と、量子通信のための2つの追加基準を示しました。このノートブックでは、**実践的なQiskitデモンストレーションを通じて、各DiVincenzo基準を体験**します。理論の深掘りは行わず、各セクションで1つの基準を簡潔に説明したうえで、Qiskit 2を使ったコード演習を提供します。シミュレータや実際のIBM Quantumデバイスでサーキットを実行し、**各原理をハンズオンで探求**していただきます。

**DiVincenzoの量子計算に関する5つの基準**：

1. **よく特性評価されたQubitを持つ、スケーラブルな物理システム。**
2. **Qubitを単純な基準状態**（例：|00…0〉）に**初期化する能力。**
3. **長いデコヒーレンス時間**（Qubitのコヒーレンスがゲート操作時間よりもはるかに長いこと）。
4. **普遍的な量子Gateのセット**（任意のユニタリ演算を実行できること）。
5. **Qubit固有の測定能力**（各Qubitの状態を読み出すこと）。

*(DiVincenzoは量子通信のための2つの基準も述べています。定常Qubitと「フライング」Qubitを相互変換する能力、およびフライングQubitを場所間で忠実に転送する能力です。これらはこのノートブックの最後の推奨アクティビティに含まれています。)*

以下の各セクションは1つの基準に対応しています。Qiskitを使い、コードと**インタラクティブな実験**でそのコンセプトを説明します。たとえば、Qubitの数やCircuitの深さを増やすとどのように結果が変わるか（基準1）、Qubitの状態のリセットと準備の方法（基準2）、シミュレータと実デバイスでのQubitsの測定方法（基準4）、Qiskitが普遍Gateをどのように構成するか（基準3）、有限のコヒーレンス（T₁、T₂）がどのように計算に影響するか（基準5）を見ていきます。最終的には、各DiVincenzo基準が実際に何を意味し、Qiskitがどのようにそれらの実験を可能にするかについて、より深い直感を得ることができるでしょう。

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy
```

```python
# Install necessary packages
!pip install qiskit[visualization] qiskit-ibm-runtime qiskit-aer qiskit_ibm_runtime
```

## 1. 基準1 – スケーラブルでよく特性評価されたQubit {#1-criterion-1-scalable-well-characterized-qubits}

**基準1：** *「よく特性評価されたQubitを持つ、スケーラブルな物理システム。」* これは、**Qubit数を増やしても**信頼性高く制御できる量子ハードウェアプラットフォームが必要であることを意味します。各Qubitの特性（エネルギー準位、エラー率、接続性など）がよく理解されている必要があります。本質的には、システムが崩壊することなく、より大きなCircuitを構築したいのです。実際には、Qubit数やCircuitの深さをスケールアップすると、エラーとデコヒーレンスが蓄積されるため、*スケーラビリティ*を実証することは、サイズの増加がパフォーマンスにどう影響するかを理解することも意味します。

**デモの目標：** Qiskitを使って、Circuitのスケールアップ（Qubit数またはGateの深さ）が出力の忠実度に与える影響を示します。理想的なシナリオとノイズのあるシナリオをシミュレートし、システムや深いCircuitが大きくなるにつれてデコヒーレンスやエラーにどう影響されるかを見てみましょう。

まず、3つのQubitで小さなエンタングル状態（GHZ状態）を構築し、次に5つのQubitでより大きな状態を構築して、シンプルなスケーリングテストとします。*n*個のQubitのGHZ状態は $\frac{1}{\sqrt{2}}(|0...0\rangle + |1...1\rangle)$ です。理想的なシミュレーションでは、n-QubitのGHZを測定すると、等確率の2つの結果（すべて0またはすべて1）のみが得られます。nまたはCircuitの深さを増やすにつれて、**理想的な出力**と**ノイズのある出力**を比較します。

```python
from qiskit import QuantumCircuit
from qiskit_aer import AerSimulator
from qiskit.visualization import plot_histogram
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager
from qiskit_ibm_runtime import SamplerV2 as Sampler

# 3-qubit GHZ circuit
qc3 = QuantumCircuit(3, 3)
qc3.h(0)
qc3.cx(0, 1)
qc3.cx(1, 2)
qc3.measure([0, 1, 2], [0, 1, 2])

# 5-qubit GHZ circuit (scaling up the number of qubits)
qc5 = QuantumCircuit(5, 5)
qc5.h(0)
qc5.cx(0, range(1, 5))    # entangle qubit 0 with all others
qc5.measure(range(5), range(5))

# Transpile for a simulator backend
sim_backend = AerSimulator()
pm = generate_preset_pass_manager(backend=sim_backend, optimization_level=1)
isa_qc3 = pm.run(qc3)
isa_qc5 = pm.run(qc5)

# Run ideal simulations (no noise)
sampler = Sampler(mode=sim_backend)

job3 = sampler.run([isa_qc3], shots=1024)
result3 = job3.result()
counts3 = result3[0].data.c.get_counts()

job5 = sampler.run([isa_qc5], shots=1024)
result5 = job5.result()
counts5 = result5[0].data.c.get_counts()

print("3-qubit GHZ counts (ideal):", counts3)
plot_histogram(counts3, legend=['3-qubit ideal'], figsize=(6,4))
```

```python
print("5-qubit GHZ counts (ideal):", counts5)
plot_histogram(counts5, legend=['5-qubit ideal'], figsize=(6,4))
```

**期待される結果（理想的なケース）：** 3-Qubit GHZは理想的には、カウント内でおよそ50%の`000`と50%の`111`を生じさせます。5-Qubit GHZは約50%の`00000`と50%の`11111`を生じさせます。状態が理想的に完全にコヒーレントでエンタングルされているため、他のビット列は現れません。各Circuitのヒストグラムに、すべてゼロとすべてイチの結果に対応する2本の高いバーが表示されるはずです。

次に、**ノイズのある環境**ではどうなるかを見てみましょう。Qiskit Aerのノイズモデル機能を使って、実際のデバイスのエラーを模倣します。たとえば、IBMバックエンドの特性を使って、Gateエラー、有限のGate時間、Qubitの緩和（T₁）、位相緩和（T₂）、および読み出しエラーを含むノイズモデルを作成できます。ここでは、IBM Quantum Brisbaneデバイスを表す**フェイクバックエンド**を使用してノイズモデルを生成し、そこにGHZ Circuitを再実行します。
### 演習1a：ノイズありのシミュレーション {#exercise-1a-simulate-with-noise}
以下のコードを完成させて、`FakeBrisbane`バックエンドに基づくノイズありのシミュレータでGHZ Circuitをシミュレートしてください。これにより、現実的なノイズ環境でシステムがスケールアップするにつれてパフォーマンスがどのように低下するかを確認できます。

```python
from qiskit_ibm_runtime.fake_provider import FakeBrisbane

# We will reuse the ideal circuits qc3 and qc5 and their results from the previous cell.

# 1. Create a fake backend for IBM Quantum Brisbane
brisbane_backend = FakeBrisbane()

# 2. Create a noisy AerSimulator from the fake backend's properties
noisy_sim = AerSimulator.from_backend(brisbane_backend)

# 3. Transpile the circuits for the noisy simulator
pm = generate_preset_pass_manager(backend=noisy_sim, optimization_level=1)

isa_qc3_noisy = pm.run(qc3)

isa_qc5_noisy = pm.run(qc5)

# 4. Run the noisy simulations using the Sampler and get the counts
sampler = Sampler(mode=noisy_sim)

job3 = sampler.run([isa_qc3_noisy], shots=1024)

result3_noisy = job3.result()

counts3_noisy = result3_noisy[0].data.meas.get_counts()

job5 = sampler.run([isa_qc5_noisy], shots=1024)

result5_noisy = job5.result()

counts5_noisy = result5_noisy[0].data.meas.get_counts()

# --- END YOUR CODE ---

# This part is done for you to visualize the results.
print("3-qubit GHZ counts (noisy):", counts3_noisy)
plot_histogram(counts3_noisy, legend=['3-qubit noisy'], figsize=(6,4))
```

```python
print("5-qubit GHZ counts (noisy):", counts5_noisy)
plot_histogram(counts5_noisy, legend=['5-qubit noisy'], figsize=(6,4))
```

### 演習1b：実際のIBM Quantum コンピュータで実行する {#exercise-1b-run-on-real-ibm-quantum-computer}
以下のコードは、実際のIBM Quantum コンピュータでGHZ Circuitを実行します。これにより、実際のデバイスでパフォーマンスがどのように低下するかを確認できます。

```python
# your_api_key = "deleteThisAndPasteYourAPIKeyHere"
# your_crn = "deleteThisAndPasteYourCRNHere"

# QiskitRuntimeService.save_account(
#     channel="ibm_quantum_platform",
#     token=your_api_key,
#     instance=your_crn,
#     name="fallfest-2025",
# )

# Check that the account has been saved properly
# service = QiskitRuntimeService(name="fallfest-2025")
# print(service.saved_accounts())

# We will reuse the ideal circuits qc3 and qc5 and their results from the previous cell.

from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService(name="fallfest-2025")
real_backend = service.least_busy(operational=True, simulator=False)
print("Running on " + real_backend.name)

pm = generate_preset_pass_manager(backend=real_backend, optimization_level=1)
isa_qc3r = pm.run(qc3)
isa_qc5r = pm.run(qc5)

sampler = Sampler(mode=real_backend)

job3r = sampler.run([isa_qc3r], shots=1024)
result3r = job3r.result()
counts3r = result3r[0].data.c.get_counts()

job5r = sampler.run([isa_qc5r], shots=1024)
result5r = job5r.result()
counts5r = result5r[0].data.c.get_counts()

print("3-qubit GHZ counts (real):", counts3r)
plot_histogram(counts3r, legend=['3-qubit real'], figsize=(6,4))
```

```python
print("5-qubit GHZ counts (real):", counts5r)
plot_histogram(counts5r, legend=['5-qubit real'], figsize=(6,4))
```

**期待される結果（ノイズありと理想的な場合の比較）：** ノイズがある場合、シミュレートされたものであれ実際のデバイスであれ、GHZ状態は**完璧ではなく**なります。すべて0とすべて1以外の追加の結果が表示されます。3つのQubitでは、`000`/`111`の100%の代わりに、GateエラーやデコヒーレンスによっていくつかのQubitが反転するため、一部の確率が他のビット列（例：`001`、`010`など）に漏れ出します。5つのQubitでは、この影響はさらに顕著になります。より大きなCircuit（より多くのQubitとCNOT Gate）はより多くのエラーを蓄積するため、すべて0とすべて1のピークが低くなり、他の多くの結果が現れます。この傾向は*スケーラビリティ*の課題を示しています。スケールアップするにつれて、エラー訂正なしに高い忠実度を維持することがより困難になります。

**インサイト：** スケーラブルな量子コンピュータは、システムが成長するにつれて量子相関を保持する必要があります。この例では、Qubit数やGateの深さを増やすと、ノイズがある場合に結果の忠実度がどのように低下するかを示しています。残りの基準は、スケールアップするにつれてそれらのQubitを適切に動作させること（エラーが少なく、初期化可能など）に取り組みます。
## 2. 基準2 – Qubitの初期化 {#2-criterion-2-qubit-initialization}

**基準2：** *「|000…〉のような単純な基準状態にQubitの状態を初期化する能力。」* すべてのQubitは既知の参照状態（通常は各Qubitのグラウンド状態|0〉）から確実に開始される必要があります。初期化は、アルゴリズムがクリーンな状態から開始できるようにするために不可欠です。実際には、IBM量子デバイスでは、各Circuit実行の開始時に各Qubitが自動的に|0〉にリセットされます。Qiskitはまた、計算中にQubitをリセットしたりカスタム状態を準備したりする命令も提供しています。

**デモの目標：** Qiskitでの開始時および回路途中でのQubitの初期化方法を示します。`reset`命令と状態準備メソッドの使用方法をデモします。
### 演習2：特定の状態の準備 {#exercise-2-prepare-a-specific-state}
以下のコードブロックで、$|10\rangle$の状態を準備するための`QuantumCircuit`を完成させてください。これは、Qubit 0が状態$|0\rangle$、Qubit 1が状態$|1\rangle$であることを意味します。これを達成するために適切なGateと命令を使用してください。

```python
from qiskit import QuantumCircuit
from qiskit_aer import AerSimulator

# Create a circuit to initialize qubits to |10> and verify by measurement
qc_init = QuantumCircuit(2, 2)

# --- YOUR CODE HERE ---

# 1. Set qubit 1 to the |1> state
qc_init.x(1)

# 2. Explicitly reset qubit 0 to the |0> state
qc_init.reset(0)

# --- END YOUR CODE ---

qc_init.measure([0, 1], [0, 1])
qc_init.draw('mpl')
```

```python
# Run the circuit and check the outcome
sim_backend = AerSimulator()
pm = generate_preset_pass_manager(backend=sim_backend, optimization_level=1)
isa_qc_init = pm.run(qc_init)

sampler = Sampler(mode=sim_backend)

job = sampler.run([isa_qc_init], shots=1024)
result = job.result()
counts = result[0].data.c.get_counts()

print("Outcome of |10> state measured in Z-basis:", counts)
plot_histogram(counts)
```

シミュレーションから、`10`（Qubit1=1、Qubit0=0のバイナリ）が100%の確率で表示されるはずです。これはQubit 1が|1〉に、Qubit 0が|0〉に正常に準備されたことを意味します。

より一般的な状態準備については、Qiskitの`initialize`メソッドを使って任意の状態への初期化が可能です。たとえば、重ね合わせ状態である$|+\rangle = (|0\rangle+|1\rangle)/\sqrt{2}$の状態にQubitを準備し、2つのQubitをベル状態$(|00\rangle+|11\rangle)/\sqrt{2}$に準備してみましょう：

```python
import numpy as np

# Initialize a single qubit in |+> state and measure in Z-basis
qc_plus = QuantumCircuit(1, 1)
state_plus = [1/np.sqrt(2), 1/np.sqrt(2)]   # amplitude for |0> and |1>
qc_plus.initialize(state_plus, 0)
qc_plus.measure(0, 0)

# Initialize two qubits in a Bell state manually
qc_bell = QuantumCircuit(2, 2)
bell_state = [1/np.sqrt(2), 0, 0, 1/np.sqrt(2)]  # amplitudes for |00>,|01>,|10>,|11>
qc_bell.initialize(bell_state, [0, 1])
qc_bell.measure([0, 1], [0, 1])

# Transpile and run the initialization circuits
isa_qc_plus = pm.run(qc_plus)
job_plus = sampler.run([isa_qc_plus], shots=1024)
result_plus = job_plus.result()
counts_plus = result_plus[0].data.c.get_counts()

print("Outcome of |+> state measured in Z-basis:", counts_plus)

isa_qc_bell = pm.run(qc_bell)
job_bell = sampler.run([isa_qc_bell], shots=1024)
result_bell = job_bell.result()
counts_bell = result_bell[0].data.c.get_counts()

print("Outcome of Bell state measured in Z-basis:", counts_bell)
```

**期待される結果：** 単一Qubitの|+〉状態は、測定するとそれぞれ約50%の確率で`0`と`1`を生じさせます。ベル状態の測定は約50%の`00`と50%の`11`を生じさせるはずです。これらが確認できれば、それらの状態への初期化が成功したことが確認されます。

**回路途中の初期化：** Qiskitの`reset`は、Circuit途中で使用して、Qubitをオンザフライで|0〉に再初期化できます。たとえば、エラー訂正コードや反復アルゴリズムでは、Qubitを測定してから再利用のためにリセットすることがよくあります。`reset`操作は決定論的で、既存の状態を廃棄してQubitをグラウンド状態に冷却します。

**デバイスの例：** **ibmq_brisbane**（127 Qubit）などのハードウェアやIBMデバイスでは、ジョブが実行されるときにすべてのQubitがデフォルトで|0〉から始まります。別の開始状態が必要な場合は、最初にGateを適用します（|1〉を得るためにXを使用したように）。継続的な再初期化（量子エラー訂正のための）は、素早く行うことが困難であるため、現在も活発な研究トピックです。幸い、基本的な使用では|0…0〉からクリーンに開始する能力が利用可能であり、他の望ましい開始状態を達成する方法も示しました。
## 3. 基準3 – 長いコヒーレンス時間（デコヒーレンスとゲート時間） {#3-criterion-3-long-coherence-time-decoherence-vs-gate-time}

**基準3：** *「ゲート操作時間よりもはるかに長い、適切なデコヒーレンス時間。」* これは、必要な操作を実行するのに十分な時間、Qubitが量子状態を維持する必要性に対処するものです。各Qubitには **T₁時間**（エネルギー緩和時間：|1〉が|0〉に崩壊する速さ）と**T₂時間**（デファイジング時間：相対的な位相コヒーレンスが失われる速さ）があります。量子コンピュータが正しく動作するためには、これらの時間スケールがゲート操作の持続時間をはるかに上回る必要があります。

**デモの目標：** 実行時間が長くなるにつれてデコヒーレンスがCircuitの結果にどう影響するかを示すことで、Qiskit内のQubitコヒーレンスを調査します。既知のT1/T2時間を持つフェイクBackendを使用して、この効果をシミュレートします。

**有限コヒーレンスの影響を実証する**ために、T1崩壊実験をシミュレートします。Qubitを|1〉状態に準備し、`delay`命令を使用してしばらく待機してから、測定します。遅延時間が増加するにつれて、|1〉を測定する確率が低下することが期待されます。

```python
# This part is done for you. We are creating a list of circuits,
# each with a different delay time.

time_delays_ns = [0, 50000, 100000, 150000, 200000, 250000, 300000]  # delay durations in ns

decay_expts = []
for delay in time_delays_ns:
    qc = QuantumCircuit(1, 1)
    qc.x(0)  # initialize qubit to |1>
    if delay > 0:
        qc.delay(delay, 0, unit='ns')  # wait 'delay' nanoseconds
    qc.measure(0, 0)
    decay_expts.append(qc)

decay_expts[1].draw('mpl') # Visualize one of the circuits
```

### 演習3：T1崩壊実験のシミュレート {#exercise-3-simulate-a-t1-decay-experiment}

次に、`FakeVigo`（T1時間が約50〜100 µs）に基づくノイズありシミュレータを使用して、これらのCircuitを実行します。シミュレータは`delay`命令の実行中にT1/T2エラーを自動的に適用します。このBackend向けにCircuitをトランスパイルして実行してください。

```python
from qiskit_ibm_runtime.fake_provider import FakeVigoV2 as FakeVigo
from qiskit_aer import AerSimulator

# --- YOUR CODE HERE ---

# 1. Create a noisy simulator from the FakeVigo backend
sim_vigo = AerSimulator.from_backend(FakeVigo())

# 2. Transpile the list of circuits for this simulator
pm = generate_preset_pass_manager(backend=sim_vigo, optimization_level=1)

isa_decay_expts = pm.run(decay_expts)

# 3. Use the Sampler to run all the transpiled circuits in a single job
sampler = Sampler(mode=sim_vigo)

job = sampler.run(isa_decay_expts, shots=1000)

result = job.result()

# --- END YOUR CODE ---

# This part is done for you to analyze and print the results.
for idx, (delay, qc) in enumerate(zip(time_delays_ns, isa_decay_expts)):
    counts = result[idx].data.c.get_counts()
    p1 = counts.get('1', 0) / 1000  # Assuming 1000 shots
    print(f"Delay {delay} ns: P(qubit=1) = {p1:.3f}")
```

## 4. 基準4 – 普遍的な量子Gateのセット {#4-criterion-4-universal-set-of-quantum-gates}

**基準4：** *「量子Gateの『普遍的な』セット。」* これは、有限の基本Gateのセットを組み合わせることで、ハードウェアが*任意の*量子計算を実行できる必要があることを意味します。古典コンピュータではNANDが普遍的ですが、量子においては普遍的なGateセットの選択肢が多数あります（例：\{H, T, CNOT\}または特定のマシンのネイティブGate）。たとえば、IBMデバイスには特定のQubit間の任意の単一Qubit回転とCNOTなどのネイティブ操作のセットがあり、これらを組み合わせることで普遍的になります。Qiskitの役割はしばしば、**高レベルのGateをこれらの基底Gateにコンパイルする**ことです。

**デモの目標：** GateがQiskitによってどのように分解されるかを示すことで、Gateの普遍性を説明します。ネイティブでないGate（3-Qubit Toffoli Gate、CCXなど）を取り上げ、それがデバイスの基底Gateにどのように分解されるかを見ます。これにより、提供されたGateセットが確かに*普遍的*であること、つまりより複雑な操作を生成できることが示されます。

まず、典型的なIBM BackendのベースGateを確認しましょう。デバイス（またはそのフェイクバージョン）の設定をクエリします。たとえば、ibmq_brisbaneの基底Gateは以下の通りです：
`P(qubit=1)`の確率が遅延時間の増加とともに低下し、T1緩和を特徴とする指数的崩壊曲線をたどることが確認できるはずです。これは、Circuitの実行時間が長すぎる場合に有限コヒーレンス時間がいかに計算エラーを引き起こすかを直接示しています。

**アルゴリズムへの影響：** 長いアルゴリズム（多数の連続したGateを含む）を試みると、総実行時間がT2に近づくかそれを超え、終了前に状態がコヒーレンスを失う可能性があります。そのため、コヒーレンス時間の改善とGateの高速化が、量子ハードウェア研究における最も重要な目標の二つです。

```python
from qiskit_ibm_runtime.fake_provider import FakeBrisbane
fake_brisbane = FakeBrisbane()
print("Basis gates for ibmq_brisbane:", fake_brisbane.configuration().basis_gates)
```

これにより、`['id', 'rz', 'sx', 'x', 'ecr']`のような出力が得られる場合があります。これらはハードウェアがネイティブにサポートするプリミティブ操作です（Identity/no-op、RZ回転、sqrt(X) Gate、X Gate、そしてcontrolled-X）。他のGateはこれらから構成する必要があります。このセットは量子計算において普遍的であることが知られています（本質的に単一Qubit回転とエンタングルする2-Qubit Gateが普遍的なセットを形成します）。

次に、テストケースとして**Toffoli（CCX）Gate**を取り上げます。CCXは、2つの制御Qubitが両方とも1である場合にのみターゲットQubitを反転させます。これはIBMハードウェアのネイティブGateではありません。Qiskitは`ccx`命令を提供していますが、内部的にはそれを分解します。
### 演習4：Toffoli Gateの分解 {#exercise-4-decompose-a-toffoli-gate}

以下のコードを完成させて、Toffoli（CCX）Gateを含むCircuitを構築し、Qiskitを使用して`FakeBrisbane` BackendのネイティブベースGateに分解してください。

```python
from qiskit import QuantumCircuit
from qiskit_ibm_runtime.fake_provider import FakeBrisbane

# The fake_brisbane backend from the previous cell is reused here.

# --- YOUR CODE HERE ---

# 1. Create a circuit that can accommodate a Toffoli gate
qc_toffoli = QuantumCircuit(3)

# Apply a CCX gate with controls on qubits 0, 1 and target on qubit 2
qc_toffoli.ccx(0, 1, 2)

# 2. Transpile the circuit to the fake Brisbane backend
pm = generate_preset_pass_manager(backend=FakeBrisbane(), optimization_level=1)

isa_qc_toffoli = pm.run(qc_toffoli)

# --- END YOUR CODE ---

print("Toffoli circuit before decomposition:")
print(qc_toffoli)

print("\nToffoli circuit after transpiling to Brisbane basis:")
# The .draw() method will now show the decomposed circuit
print(isa_qc_toffoli.draw(fold=120))
```

トランスパイル後の出力では、CCXが`rz`、`sx`、`ecr`などのより基本的なGateのシーケンスに置き換えられているはずです。これにより、ネイティブGateがToffoliを表現するのに十分であることが証明されます。

**実践における普遍性：** 上記の演習は、複雑な3-Qubit Gateがよりシンプルなものから構築されたことを示しています。一般に、**任意の**多Qubitユニタリは1-Qubitおよび2-Qubit Gateから構成できます。Transpilerは量子ソフトウェアスタックの重要なコンポーネントであり、実行したい抽象的なアルゴリズムと特定の量子デバイスが実際に実行できる物理的な操作との間のギャップを埋めます。

**デバイスの例：** **ibmq_brisbane** デバイスはEagleアーキテクチャを使用し、上記に示した基底Gateを持ちます。つまり、それらのマシンに送られるアルゴリズムはすべて、それらの操作のシーケンスに変換されます。この基準は本質的に**制御可能性**に関するもので、Qubitに対して必要な操作を実行するための十分な制御手段を持っていることを意味します。
## 5. 基準5 – Qubitの測定 {#5-criterion-5-qubit-measurement}

**基準5：** *「Qubit固有の測定能力。」* 各Qubitの状態は測定可能でなければなりません（通常は計算基底、|0〉または|1〉で）。言い換えると、量子Circuitを実行した後、各Qubitを0/1の古典的なビットとして読み出す必要があります。この基準は、各Qubitに対して信頼性の高い検出器を持ち、測定するQubitを選択できることに関するものです。

**デモの目標：** Qiskitでシミュレータおよび実際のデバイスで測定を実行する方法を示し、違い（測定ノイズなど）を強調します。さまざまな状態にあるいくつかのQubitを測定し、結果を検討します。また、シミュレータとハードウェアの結果を比較することで、読み出しエラーがどのように現れるかを示します。

まず、シンプルな測定の例を見てみましょう：

```python
qc_measure = QuantumCircuit(2, 2)
qc_measure.x(0)              # qubit 0 -> |1>, qubit 1 stays |0>
qc_measure.measure([0, 1], [0, 1])
qc_measure.draw('mpl')
```

```python
sim_backend = AerSimulator()
pm = generate_preset_pass_manager(backend=sim_backend, optimization_level=1)
isa_qc_measure = pm.run(qc_measure)
job = sampler.run([isa_qc_measure], shots=1000)
result = job.result()
counts = result[0].data.c.get_counts()

print("Simulator measurement counts:", counts)
```

シミュレータでは`01`の1000カウントが期待されます。次に、これをシミュレートすることで**測定エラー**の影響を見てみましょう。AerシミュレータにReadoutErrorを追加することができます。Qiskit Aerでは、`ReadoutError`を定義してノイズモデルのQubitに付加することができます。
### 演習5：読み出しエラーのシミュレート {#exercise-5-simulate-readout-error}

各Qubitが2%の確率で誤って測定される（0が1として読み取られる、または1が0として読み取られる）シンプルな読み出しエラーモデルを定義するコードを完成させてください。次に、このノイズモデルで測定Circuitを実行してください。

```python
from qiskit_aer.noise import NoiseModel, ReadoutError

# --- YOUR CODE HERE ---

# 1. Define a 2% readout error for each single qubit.
# [[P(0|0), P(1|0)], [P(0|1), P(1|1)]]
ro_error = ReadoutError([[0.98, 0.02], [0.02, 0.98]])

# 2. Create a new noise model
noise_model_ro = NoiseModel()

# 3. Add the readout error to all qubits in the noise model
noise_model_ro.add_all_qubit_readout_error(ro_error)

# --- END YOUR CODE ---

sim_backend.set_options(noise_model=noise_model_ro)
pm = generate_preset_pass_manager(backend=sim_backend, optimization_level=1)
isa_qc_measure = pm.run(qc_measure)

# Run the measurement circuit with readout noise
sampler = Sampler(mode=sim_backend)

job = sampler.run([isa_qc_measure], shots=1024)
result = job.result()
counts = result[0].data.c.get_counts()

print("Simulation with 2% readout error:", counts)
```

このシミュレートされた出力は、実際のハードウェアが生成する可能性のあるものと同様に、誤ったカウント（`11`、`00`、`10`など）を示し、不完全な測定の影響を実証します。

**デバイスの例：** **ibmq_brisbane**などの実際のデバイスでは、同じCircuitを実行すると、不正確な結果に対して同様のゼロでないカウントが見られる可能性が高いです。デバイスのキャリブレーションデータには、各Qubitの読み出しエラーが記載されています。特定のQubitをターゲットにして読み取る能力は非常に重要であり、そのエラー特性を理解することが意味のある結果を得るための鍵です。実際のデバイスでの実行については、**演習1b：実際のIBM Quantum コンピュータでの実行**で示しました。
## 量子通信基準（フライング・Qubit） {#quantum-communication-criteria-flying-qubits}

DiVincenzoは、ネットワーク接続された量子コンピュータを構築する場合に重要な、量子通信に特化した2つの基準も挙げています：

6. **静止Qubitとフライング・Qubitを相互変換する能力。**（例：プロセッサ内のQubitを移動できる光子にマッピングする。）
7. **フライング・Qubitを場所間で忠実に伝送する能力。**（例：量子情報を失わずに光ファイバーを通じて光子Qubitを送信する。）

Qiskitは主にチップ上の静止Qubitを扱うため、これらは標準的なQiskitの使用範囲を超えています。ただし、**量子テレポーテーション**というシンプルな例でこれらの基準の*概念*を示すことができます。テレポーテーションは、静止Qubitの状態をエンタングルしたペア（「フライング」部分）によって運ばれる情報と古典的な通信に変換し、それを使用して別の場所にある別の静止Qubitで状態を再構成することを示しています。
### 推奨アクティビティ：Qiskit in Classrooms の*量子テレポーテーション*モジュールを受講する {#recommended-activity-take-the-qiskit-in-classrooms-quantum-teleportation-module}

Katie McCormick博士によるQiskit in Classrooms [量子テレポーテーション](https://quantum.cloud.ibm.com/learning/en/modules/computer-science/quantum-teleportation)モジュールは、量子情報において最も魅力的なプロトコルの一つである量子テレポーテーション（エンタングルメントとわずか2つの古典ビットを使用して、AliceからBobに量子状態（Qubit）を送信する）を順を追って案内します。エンタングルしたBellペアの準備方法、Aliceの側でBell基底測定を実行する方法、古典的な結果を送信する方法、そしてBobのQubitに正しい量子Gateを適用して元の状態を完全に回復する方法など、テレポーテーション手順全体をステップバイステップで学びます。その過程で、Qubitをテレポートさせる情報が不可複製定理に違反したり光速を超えたりしない理由も探求します。IBM Quantumハードウェアまたはシミュレータを使用したハンズオン演習を通じて、実際の測定、エンタングルメント、フィードフォワード制御の実用的な理解を深めます。

量子テレポーテーションをマスターすることで、異なるノード間で量子情報をエンコード、送信、回復する方法を理解し、量子ネットワーク、リピータシステム、安全な通信スキーム、およびスケーラブルなモジュール型量子コンピューティングの基盤を築くことができます。
**これが基準6と7にどう関連するか：** 実際の量子ネットワークでは、共有されたエンタングルペアは「フライング」Qubit（光子など）をAliceとBobの場所間に分散させることで生成されます（基準7：忠実な伝送）。テレポーテーションプロトコル自体は、AliceのQubit状態をエンタングルペアの彼女の半分にマッピングし、効果的にBobに「送信」する方法として機能します（基準6：相互変換）。Qiskitはプロトコルのロジックを完全にシミュレートでき、これらの基準が通信アーキテクチャでどのように満たされるかの概念モデルを提供します。
## まとめ {#conclusion-summary}

私たちは、Qiskitを使用してDiVincenzo基準を説明することに焦点を当てた一連のコード演習を設計しました。これらのハンズオンの例を通じて、実際の量子コンピュータプラットフォームが各要件をどのように満たすかを探求しました：

- **スケーラビリティ**：より多くのQubitでCircuitを構築し、ノイズのスケーリングを理解する。
- **初期化**：リセットと状態準備を使用して、既知の状態で確実に計算を開始する。
- **普遍的なGate**：複雑な操作をマシンの基底Gateにトランスパイルし、任意の計算を実行できることを証明する。
- **測定**：Qubitを読み出し、現実的な読み出しエラーに対処する。
- **コヒーレンス**：アルゴリズムの忠実度に対する有限T₁、T₂の影響と、デコヒーレンスに対して操作が速い必要性を確認する。

完全性のために、Qiskit in Classrooms [量子テレポーテーション](https://quantum.cloud.ibm.com/learning/en/modules/computer-science/quantum-teleportation)モジュールを通じて量子通信の側面にも触れ、最後の2つの基準（フライング・Qubit）と結びつけました。

最後に、これらの基準がIBMの実際の量子コンピュータでどのように組み合わさるかを指摘する価値があります。**ibmq_brisbane**のようなデバイスは127の超伝導Qubit（基準1）を持ち、各Qubitは|0〉から開始し（基準2）、普遍性のためのキャリブレートされたGateセットとコンパイラ（基準4）、各Qubitのマイクロ波読み出し共振器（基準5）、そしてナノ秒単位の操作に対して数百マイクロ秒オーダーのコヒーレンス時間（基準3）を持っています。量子ネットワーキング実験のために、IBMや他の機関はフライング・Qubitのためのマイクロ波から光学への変換や、遠隔Qubitのエンタングル（基準6と7）を探求しています；これらは活発な研究分野です。

このノートブックの演習を完了することで、DiVincenzo基準の定義を見るだけでなく、コードを通じて*実際に体験した*ことになります；実際の量子ハードウェアとアルゴリズムに対して各要件が何を意味するかの直感を築いています。これらの実験を自由に拡張して、量子コンピューティングをお楽しみください！
