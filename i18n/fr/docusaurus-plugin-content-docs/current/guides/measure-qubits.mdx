---
title: "Mesurer des qubits"
sidebar_label: "Mesurer des qubits"
description: "Apprends comment mesurer des qubits, y compris les contraintes sur l'utilisation des mesures."
notebook_path: "guides/measure-qubits.ipynb"
---

{/* doqumentation-source-hash: 421a17fa */}

<OpenInLabBanner notebookPath="guides/measure-qubits.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<Accordion>
<AccordionItem title="Package versions">

Le code de cette page a été développé avec les dépendances suivantes.
Nous recommandons d'utiliser ces versions ou des versions plus récentes.

```
qiskit[all]~=2.4.0
```
</AccordionItem>
</Accordion>

Pour obtenir des informations sur l'état d'un qubit, tu peux le _mesurer_ sur un [bit classique](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Clbit). Dans Qiskit, les mesures sont effectuées dans la base computationnelle, c'est-à-dire la base de Pauli-$Z$ à un qubit. Ainsi, une mesure donne 0 ou 1, selon le recouvrement avec les états propres de Pauli-$Z$ $|0\rangle$ et $|1\rangle$ :

$$
|q\rangle \xrightarrow{measure}\begin{cases}
      0 (\text{outcome}+1), \text{with probability } p_0=|\langle q|0\rangle|^{2}\text{,} \\
      1 (\text{outcome}-1), \text{with probability } p_1=|\langle q|1\rangle|^{2}\text{.}
    \end{cases}
$$

## Appliquer une mesure à un circuit {#apply-a-measurement-to-a-circuit}

Il existe plusieurs façons d'appliquer des mesures à un circuit :

### Méthode `QuantumCircuit.measure` {#quantumcircuitmeasure-method}

Utilise la méthode [`measure`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure) pour mesurer un [`QuantumCircuit`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#quantumcircuit-class).

Exemples :

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(5, 5)
qc.x(0)
qc.x(1)
qc.x(4)
qc.measure(
    range(5), range(5)
)  #  Measures all qubits into the corresponding clbit.
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c636c20>
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure(1, 0)  # Measure qubit 1 into the classical bit 0.
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c636830>
```

### Classe `Measure` {#measure-class}

La classe Qiskit [Measure](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure) mesure les qubits spécifiés.

```python
from qiskit.circuit import Measure

qc = QuantumCircuit(3, 1)
qc.x([0, 1])
qc.append(Measure(), [0], [0])  # measure qubit 0 into clbit 0
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7eff8c6369e0>
```

### Méthode `QuantumCircuit.measure_all` {#quantumcircuitmeasure-all-method}

Pour mesurer tous les qubits dans les bits classiques correspondants, utilise la méthode [`measure_all`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_all). Par défaut, cette méthode ajoute de nouveaux bits classiques dans un `ClassicalRegister` pour stocker ces mesures.

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure_all()  # Measure all qubits.
```

### Méthode `QuantumCircuit.measure_active` {#quantumcircuitmeasure-active-method}

Pour mesurer tous les qubits qui ne sont pas inactifs, utilise la méthode [`measure_active`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_active). Cette méthode crée un nouveau `ClassicalRegister` dont la taille est égale au nombre de qubits non inactifs mesurés.

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3, 1)
qc.x([0, 2])
qc.measure_active()  # Measure qubits that are not idle, that is, qubits 0 and 2.
```

## Prochaines étapes {#next-steps}

<Admonition type="tip" title="Recommandations">
- Classe [`Measure`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure)
- Méthode [`measure_all`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_all)
- Méthode [`measure_active`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.QuantumCircuit#measure_active)
- Méthode [`random_circuit`](https://docs.quantum.ibm.com/api/qiskit/circuit_random#qiskit.circuit.random.random_circuit)
- [Mid-circuit measurements](/guides/execute-dynamic-circuits#midcircuit) (Available only when using Qiskit Runtime.)
</Admonition>
