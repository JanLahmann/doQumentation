---
title: "La classe Operator"
sidebar_label: "La classe Operator"
description: "Explication approfondie de l'utilisation de la classe Operator dans Qiskit."
notebook_path: "guides/operator-class.ipynb"
---

{/* doqumentation-source-hash: f33a6b98 */}

<OpenInLabBanner notebookPath="guides/operator-class.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>Versions des packages</b></summary>

Le code de cette page a été développé avec les dépendances suivantes.
Nous recommandons d'utiliser ces versions ou des versions plus récentes.

```
qiskit[all]~=2.3.0
```
</details>
Cette page montre comment utiliser la classe [`Operator`](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator). Pour une vue d'ensemble des représentations d'opérateurs dans Qiskit, incluant la classe `Operator` et d'autres, consulte [Vue d'ensemble des classes d'opérateurs](./operators-overview).

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit
```

```python
import numpy as np
from qiskit.circuit import QuantumCircuit
from qiskit.circuit.library import CXGate, RXGate, XGate
from qiskit.quantum_info import Operator, Pauli, process_fidelity
```

## Convertir des classes en Operators {#convert-classes-to-operators}

Plusieurs autres classes dans Qiskit peuvent être directement converties en objet `Operator` via la méthode d'initialisation de l'opérateur. Par exemple :

* les objets `Pauli`
* les objets `Gate` et `Instruction`
* les objets `QuantumCircuit`

Note que ce dernier point signifie que tu peux utiliser la classe `Operator` comme simulateur unitaire pour calculer la matrice unitaire finale d'un circuit quantique, sans avoir à appeler un backend simulateur. Si le circuit contient des opérations non supportées, une exception est levée. Les opérations non supportées sont : measure, reset, les opérations conditionnelles, ou une porte qui n'a pas de définition matricielle ni de décomposition en portes avec définitions matricielles.

```python
# Create an Operator from a Pauli object

pauliXX = Pauli("XX")
Operator(pauliXX)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j],
          [1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an Operator for a Gate object
Operator(CXGate())
```

```text
Operator([[1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an operator from a parameterized Gate object
Operator(RXGate(np.pi / 2))
```

```text
Operator([[0.70710678+0.j        , 0.        -0.70710678j],
          [0.        -0.70710678j, 0.70710678+0.j        ]],
         input_dims=(2,), output_dims=(2,))
```

```python
# Create an operator from a QuantumCircuit object
circ = QuantumCircuit(10)
circ.h(0)
for j in range(1, 10):
    circ.cx(j - 1, j)

# Convert circuit to an operator by implicit unitary simulation
Operator(circ)
```

```text
Operator([[ 0.70710678+0.j,  0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          ...,
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.70710678+0.j, -0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j]],
         input_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2), output_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2))
```

## Utiliser des Operators dans des circuits {#use-operators-in-circuits}

Les `Operators` unitaires peuvent être directement insérés dans un `QuantumCircuit` via la méthode `QuantumCircuit.append`. Cela convertit l'`Operator` en objet `UnitaryGate`, qui est ajouté au circuit.

Si l'opérateur n'est pas unitaire, une exception est levée. Cela peut être vérifié avec la fonction `Operator.is_unitary()`, qui renvoie `True` si l'opérateur est unitaire et `False` sinon.

```python
# Create an operator
XX = Operator(Pauli("XX"))

# Add to a circuit
circ = QuantumCircuit(2, 2)
circ.append(XX, [0, 1])
circ.measure([0, 1], [0, 1])
circ.draw("mpl")
```

![Sortie de la cellule de code précédente](/docs/images/guides/operator-class/extracted-outputs/223c07c9-1b1d-4a33-a397-091cac2ec9e3-0.svg)

Note que dans l'exemple ci-dessus, l'opérateur est initialisé à partir d'un objet `Pauli`. Cependant, l'objet `Pauli` peut aussi être directement inséré dans le circuit lui-même et sera converti en une séquence de portes Pauli à qubit unique :

```python
# Add to a circuit
circ2 = QuantumCircuit(2, 2)
circ2.append(Pauli("XX"), [0, 1])
circ2.measure([0, 1], [0, 1])
circ2.draw()
```

```text
┌────────────┐┌─┐
q_0: ┤0           ├┤M├───
     │  Pauli(XX) │└╥┘┌─┐
q_1: ┤1           ├─╫─┤M├
     └────────────┘ ║ └╥┘
c: 2/═══════════════╩══╩═
                    0  1
```

## Combiner des Operators {#combine-operators}

Les opérateurs peuvent être combinés de plusieurs façons.

### Produit tensoriel {#tensor-product}

Deux opérateurs $A$ et $B$ peuvent être combinés en un opérateur produit tensoriel $A\otimes B$ via la fonction `Operator.tensor`. Note que si $A$ et $B$ sont tous deux des opérateurs à qubit unique, alors `A.tensor(B)` = $A\otimes B$ aura les sous-systèmes indexés comme la matrice $B$ sur le sous-système 0, et la matrice $A$ sur le sous-système 1.

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.tensor(B)
```

```text
Operator([[ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j],
          [ 0.+0.j, -0.+0.j,  0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### Expansion tensorielle {#tensor-expansion}

Une opération étroitement liée est `Operator.expand`, qui fonctionne comme un produit tensoriel mais dans l'ordre inverse. Ainsi, pour deux opérateurs $A$ et $B$, on a `A.expand(B)` = $B\otimes A$ où les sous-systèmes sont indexés comme la matrice $A$ sur le sous-système 0, et la matrice $B$ sur le sous-système 1.

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.expand(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j,  0.+0.j, -0.+0.j, -1.+0.j],
          [ 0.+0.j,  0.+0.j, -1.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### Composition {#composition}

Tu peux aussi composer deux opérateurs $A$ et $B$ pour effectuer une multiplication matricielle avec la méthode `Operator.compose`. `A.compose(B)` renvoie l'opérateur de matrice $B.A$ :

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j],
          [-1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

Tu peux aussi composer dans l'ordre inverse en appliquant $B$ devant $A$ via l'argument `front` de `compose` : `A.compose(B, front=True)` = $A.B$ :

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B, front=True)
```

```text
Operator([[ 0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

### Composition sur des sous-systèmes {#subsystem-composition}

Note que la composition précédente requiert que la dimension de sortie totale du premier opérateur $A$ soit égale à la dimension d'entrée totale de l'opérateur composé $B$ (et de même, la dimension de sortie de $B$ doit être égale à la dimension d'entrée de $A$ lors d'une composition avec `front=True`).

Tu peux aussi composer un opérateur plus petit avec une sélection de sous-systèmes d'un opérateur plus grand via l'argument `qargs` de `compose`, avec ou sans `front=True`. Dans ce cas, les dimensions d'entrée et de sortie des sous-systèmes composés doivent correspondre. *Note que le plus petit opérateur doit toujours être l'argument de la méthode* `compose`*.*

Par exemple, pour composer une porte à deux qubits avec un opérateur à trois qubits :

```python
# Compose XZ with a 3-qubit identity operator
op = Operator(np.eye(2**3))
XZ = Operator(Pauli("XZ"))
op.compose(XZ, qargs=[0, 2])
```

```text
Operator([[ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
           -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

```python
# Compose YX in front of the previous operator
op = Operator(np.eye(2**3))
YX = Operator(Pauli("YX"))
op.compose(YX, qargs=[0, 2], front=True)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j],
          [0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

### Combinaisons linéaires {#linear-combinations}

Les opérateurs peuvent aussi être combinés à l'aide des opérateurs linéaires standards : addition, soustraction et multiplication scalaire par des nombres complexes.

```python
XX = Operator(Pauli("XX"))
YY = Operator(Pauli("YY"))
ZZ = Operator(Pauli("ZZ"))

op = 0.5 * (XX + YY - 3 * ZZ)
op
```

```text
Operator([[-1.5+0.j,  0. +0.j,  0. +0.j,  0. +0.j],
          [ 0. +0.j,  1.5+0.j,  1. +0.j,  0. +0.j],
          [ 0. +0.j,  1. +0.j,  1.5+0.j,  0. +0.j],
          [ 0. +0.j,  0. +0.j,  0. +0.j, -1.5+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

Un point important : alors que `tensor`, `expand` et `compose` préservent l'unitarité des opérateurs unitaires, les combinaisons linéaires ne le font pas ; ainsi, additionner deux opérateurs unitaires donnera en général un opérateur non unitaire :

```python
op.is_unitary()
```

```text
False
```

### Conversion implicite en Operators {#implicit-conversion-to-operators}

Note que pour toutes les méthodes suivantes, si le second objet n'est pas déjà un objet `Operator`, il est implicitement converti en un par la méthode. Cela signifie que des matrices peuvent être passées directement sans être explicitement converties en `Operator` au préalable. Si la conversion est impossible, une exception est levée.

```python
# Compose with a matrix passed as a list
Operator(np.eye(2)).compose([[0, 1], [1, 0]])
```

```text
Operator([[0.+0.j, 1.+0.j],
          [1.+0.j, 0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

## Comparer des Operators {#compare-operators}

Les opérateurs implémentent une méthode d'égalité qui permet de vérifier si deux opérateurs sont approximativement égaux.

```python
Operator(Pauli("X")) == Operator(XGate())
```

```text
True
```

Note que cette vérification porte sur chaque élément matriciel des opérateurs de façon approximative ; deux unitaires qui ne diffèrent que par une phase globale ne sont pas considérés comme égaux :

```python
Operator(XGate()) == np.exp(1j * 0.5) * Operator(XGate())
```

```text
False
```

### Fidélité de processus {#process-fidelity}

Tu peux aussi comparer des opérateurs à l'aide de la fonction `process_fidelity` du module *Quantum Information*. Il s'agit d'une quantité issue de la théorie de l'information qui mesure à quel point deux canaux quantiques sont proches l'un de l'autre ; dans le cas d'opérateurs unitaires, elle ne dépend pas de la phase globale.

```python
# Two operators which differ only by phase
op_a = Operator(XGate())
op_b = np.exp(1j * 0.5) * Operator(XGate())

# Compute process fidelity
F = process_fidelity(op_a, op_b)
print("Process fidelity =", F)
```

```text
Process fidelity = 1.0
```

Note que la fidélité de processus n'est généralement une mesure valide de proximité que si les opérateurs en entrée sont unitaires (ou CP dans le cas de canaux quantiques), et une exception est levée si les entrées ne sont pas CP.
## Prochaines étapes {#next-steps}

<Admonition type="tip" title="Recommandations">
  - Explore la référence de l'[API Operator](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator#operator).
</Admonition>
