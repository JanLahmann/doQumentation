---
title: ใช้ Qiskit Code Assistant ในโหมด local
description: เรียนรู้วิธี deploy และใช้โมเดล Qiskit Code Assistant ในเครื่องของคุณ
---
{/* doqumentation-source-hash: a98b4d3a */}

{/* cspell:ignore ollama, gguf, modelfile, safetensors, winget, MBPP */}

# ใช้ Qiskit Code Assistant ในโหมด local {#use-qiskit-code-assistant-in-local-mode}

เรียนรู้วิธีติดตั้ง กำหนดค่า และใช้โมเดล Qiskit Code Assistant ใดก็ได้บนเครื่องของคุณ

<Admonition type="note" title="หมายเหตุ">
    - Qiskit Code Assistant อยู่ในสถานะ preview release และอาจมีการเปลี่ยนแปลง
    - หากคุณมีความคิดเห็นหรือต้องการติดต่อทีมนักพัฒนา ใช้ [ช่อง Qiskit Slack Workspace](https://qiskit.enterprise.slack.com/archives/C07LYA6PL83) หรือ public GitHub repositories ที่เกี่ยวข้อง
</Admonition>

## เริ่มต้นใช้งานด่วน (แนะนำ) {#quick-start-recommended}

วิธีที่ง่ายที่สุดในการเริ่มต้นใช้ Qiskit Code Assistant ในโหมด local คือการใช้ script ตั้งค่าอัตโนมัติสำหรับ VS Code หรือ JupyterLab extension script เหล่านี้จะติดตั้ง [Ollama](https://ollama.com/) เพื่อรัน LLM โดยอัตโนมัติ ดาวน์โหลดโมเดลที่แนะนำ และกำหนดค่า extension ให้คุณ

### การตั้งค่า VS Code extension {#vs-code-extension-setup}

รันคำสั่งต่อไปนี้ใน terminal ของคุณ:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Qiskit/qiskit-code-assistant-vscode/main/setup_local.sh)
```

script นี้ดำเนินการขั้นตอนต่อไปนี้:
- ติดตั้ง Ollama (หากยังไม่ได้ติดตั้ง)
- ดาวน์โหลดและกำหนดค่าโมเดล Qiskit Code Assistant ที่แนะนำ
- ตั้งค่า VS Code extension ให้ทำงานกับ local deployment ของคุณ

### การตั้งค่า JupyterLab extension {#jupyterlab-extension-setup}

รันคำสั่งต่อไปนี้ใน terminal ของคุณ:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Qiskit/qiskit-code-assistant-jupyterlab/main/setup_local.sh)
```

script นี้จะ:
- ติดตั้ง Ollama (หากยังไม่ได้ติดตั้ง)
- ดาวน์โหลดและกำหนดค่าโมเดล Qiskit Code Assistant ที่แนะนำ
- ตั้งค่า JupyterLab extension ให้ทำงานกับ local deployment ของคุณ

## โมเดลที่ใช้ได้ {#available-models}

### โมเดลปัจจุบัน {#current-models}

เหล่านี้คือโมเดลล่าสุดที่แนะนำสำหรับใช้กับ Qiskit Code Assistant:

1. [Qiskit/mistral-small-3.2-24b-qiskit](https://huggingface.co/Qiskit/mistral-small-3.2-24b-qiskit) - เปิดตัว October 2025
2. [qiskit/qwen2.5-coder-14b-qiskit](https://huggingface.co/Qiskit/qwen2.5-coder-14b-qiskit) - เปิดตัว June 2025
3. [qiskit/granite-3.3-8b-qiskit](https://huggingface.co/Qiskit/granite-3.3-8b-qiskit) - เปิดตัว June 2025
4. [qiskit/granite-3.2-8b-qiskit](https://huggingface.co/Qiskit/granite-3.2-8b-qiskit) - เปิดตัว June 2025

### โมเดล GGUF (แนะนำสำหรับ personal environments/laptops) {#gguf-models-recommended-for-personal-environmentslaptops}

โมเดลรูปแบบ GGUF ได้รับการปรับแต่งสำหรับการใช้งาน local และต้องใช้ทรัพยากรการประมวลผลน้อยกว่า:

1. [mistral-small-3.2-24b-qiskit-GGUF](https://huggingface.co/Qiskit/mistral-small-3.2-24b-qiskit-GGUF) – เปิดตัว **October 2025**
   *ฝึกด้วยข้อมูล Qiskit จนถึงเวอร์ชัน **2.1***

2. [qiskit/qwen2.5-coder-14b-qiskit-GGUF](https://huggingface.co/Qiskit/qwen2.5-coder-14b-qiskit-GGUF) – เปิดตัว **June 2025**
   *ฝึกด้วยข้อมูล Qiskit จนถึงเวอร์ชัน **2.0***

3. [qiskit/granite-3.3-8b-qiskit-GGUF](https://huggingface.co/Qiskit/granite-3.3-8b-qiskit-GGUF) – เปิดตัว **June 2025**
   *ฝึกด้วยข้อมูล Qiskit จนถึงเวอร์ชัน **2.0***

4. [qiskit/granite-3.2-8b-qiskit-GGUF](https://huggingface.co/Qiskit/granite-3.2-8b-qiskit-GGUF) – เปิดตัว **June 2025**
   *ฝึกด้วยข้อมูล Qiskit จนถึงเวอร์ชัน **2.0***

โมเดล Qiskit Code Assistant แบบ Open Source มีให้ในรูปแบบ <DefinitionTooltip definition="Safetensors is a file format designed specifically for storing machine learning model weights and tensors in a secure and efficient manner.">safetensors</DefinitionTooltip> หรือ <DefinitionTooltip definition="GGUF is a binary format that is designed for quickly loading and saving models, and for readability.">GGUF file format</DefinitionTooltip> และสามารถดาวน์โหลดได้จาก Hugging Face ตามที่อธิบายด้านล่าง

### เวอร์ชัน Qiskit ที่ใช้ฝึกโมเดล {#qiskit-versions-used-for-training}

| **โมเดล** | &nbsp; | &nbsp; | &nbsp; | &nbsp; | &nbsp; | **Benchmark Metrics** | &nbsp; | &nbsp; | &nbsp; | &nbsp; | **วันที่เปิดตัว** | **ฝึกบน Qiskit เวอร์ชัน** |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| &nbsp; | **QiskitHumanEval-Hard** | **QiskitHumanEval** | **HumanEval** | **ASDiv** | **MathQA** | **SciQ** | **MBPP** | **IFEval** | **CrowsPairs (English)** | **TruthfulQA (MC1 acc)** | &nbsp; | &nbsp; |
| **mistral-small-3.2-24b-qiskit** | 32.45 | 47.02 | 77.49 | 3.77 | 49.68 | 97.50 | 64.00 | 48.44 | 67.08 | 39.41 | **January 2026** | **2.2** |
| qwen2.5-coder-14b-qiskit | 25.17 | 49.01 | 91.46 | 4.21 | 53.90 | 97.00 | 77.60 | 49.64 | 65.18 | 37.82 | June 2025 | 2.0 |
| granite-3.3-8b-qiskit | 14.57 | 27.15 | 62.80 | 0.48 | 38.66 | 93.30 | 52.40 | 59.71 | 59.75 | 39.05 | June 2025 | 2.0 |
| granite-3.2-8b-qiskit | 9.93 | 24.50 | 57.32 | 0.09 | 41.41 | 96.30 | 51.80 | 60.79 | 66.79 | 40.51 | June 2025 | 2.0 |
| granite-8b-qiskit-rc-0.10 | 15.89 | 38.41 | 59.76 | — | — | — | — | — | — | — | February 2025 | 1.3 |
| granite-8b-qiskit | 17.88 | 44.37 | 53.66 | — | — | — | — | — | — | — | November 2024 | 1.2 |

*หมายเหตุ: โมเดลทั้งหมดที่แสดงในตาราง benchmark ได้รับการประเมินโดยใช้ system prompt ของตน ซึ่งกำหนดไว้ในโมเดล Hugging Face*

### โมเดลที่ deprecated แล้ว {#deprecated-models}

โมเดลเหล่านี้ไม่ได้รับการดูแลอย่างต่อเนื่องอีกต่อไป แต่ยังคงใช้งานได้:

1. [qiskit/granite-8b-qiskit-rc-0.10](https://huggingface.co/Qiskit/granite-8b-qiskit-rc-0.10) - เปิดตัว February 2025 (deprecated)
2. [qiskit/granite-8b-qiskit](https://huggingface.co/Qiskit/granite-8b-qiskit) - เปิดตัว November 2024 (deprecated)

## การตั้งค่าขั้นสูง {#advanced-setup}

หากคุณต้องการกำหนดค่า local setup ด้วยตนเองหรือต้องการควบคุมกระบวนการติดตั้งมากขึ้น ให้ขยาย section ด้านล่าง

<details>

<summary>ดาวน์โหลดจากเว็บไซต์ Hugging Face</summary>

ทำตามขั้นตอนเหล่านี้เพื่อดาวน์โหลดโมเดลที่เกี่ยวข้องกับ Qiskit Code Assistant จากเว็บไซต์ Hugging Face:

1. ไปที่หน้าโมเดล Qiskit ที่ต้องการบน Hugging Face
2. ไปที่แท็บ **Files and Versions** และดาวน์โหลดไฟล์โมเดล safetensors หรือ GGUF

</details>

<details>

<summary>ดาวน์โหลดโดยใช้ Hugging Face CLI</summary>

ในการดาวน์โหลดโมเดล Qiskit Code Assistant ที่ใช้ได้โดยใช้ Hugging Face CLI ให้ทำตามขั้นตอนเหล่านี้:

1. ติดตั้ง [Hugging Face CLI](https://huggingface.co/docs/huggingface_hub/main/en/guides/cli)
1. เข้าสู่ระบบ Hugging Face account ของคุณ

    ```
    huggingface-cli login
    ```

1. ดาวน์โหลดโมเดลที่คุณต้องการจากรายการก่อนหน้า

    ```
    huggingface-cli download <HF REPO NAME> <MODEL PATH> --local-dir <LOCAL PATH>
    ```

</details>

<details>

<summary>Deploy โมเดล Qiskit Code Assistant ใน local ด้วย Ollama ด้วยตนเอง</summary>

มีหลายวิธีในการ deploy และโต้ตอบกับโมเดล Qiskit Code Assistant ที่ดาวน์โหลดมา คู่มือนี้แสดงการใช้ [Ollama](https://ollama.com) ดังนี้: ด้วย [Ollama application](#using-the-ollama-application) โดยใช้ Hugging Face Hub integration หรือโมเดล local หรือด้วย [แพ็กเกจ `llama-cpp-python`](#use-llama-cpp-python)

### การใช้ Ollama application {#using-the-ollama-application}

Ollama application ให้วิธีแก้ปัญหาที่ง่ายในการรัน LLM ใน local ง่ายต่อการใช้งาน พร้อม CLI ที่ทำให้กระบวนการตั้งค่าทั้งหมด การจัดการโมเดล และการโต้ตอบค่อนข้างตรงไปตรงมา เหมาะสำหรับการทดลองอย่างรวดเร็วและสำหรับผู้ใช้ที่ต้องการรายละเอียดทางเทคนิคน้อยกว่า

#### ติดตั้ง Ollama {#install-ollama}

1. ดาวน์โหลด [Ollama application](https://ollama.com/download)
1. ติดตั้งไฟล์ที่ดาวน์โหลด
1. เปิดใช้งาน Ollama application ที่ติดตั้ง

    <Admonition type="info">application ทำงานสำเร็จเมื่อไอคอน Ollama ปรากฏใน desktop menu bar คุณยังสามารถตรวจสอบว่า service ทำงานอยู่โดยไปที่ `http://localhost:11434/`</Admonition>

1. ลอง Ollama ใน terminal ของคุณและเริ่มรันโมเดล ตัวอย่าง:

    ```
    ollama run hf.co/Qiskit/Qwen2.5-Coder-14B-Qiskit
    ```

#### ตั้งค่า Ollama โดยใช้ Hugging Face Hub integration {#set-up-ollama-using-the-hugging-face-hub-integration}

[Ollama/Hugging Face Hub integration](https://huggingface.co/docs/hub/ollama) ให้วิธีโต้ตอบกับโมเดลที่โฮสต์บน Hugging Face Hub โดยไม่จำเป็นต้องสร้าง modelfile ใหม่หรือดาวน์โหลดไฟล์ GGUF หรือ safetensors ด้วยตนเอง ไฟล์ `template` และ `params` เริ่มต้นรวมอยู่ในโมเดลบน Hugging Face Hub แล้ว

1. ตรวจสอบให้แน่ใจว่า Ollama application ทำงานอยู่

1. ไปที่หน้าโมเดลที่ต้องการ และคัดลอก URL ตัวอย่าง https://huggingface.co/Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF

1. จาก terminal ของคุณ รันคำสั่ง:

    ```
    ollama run hf.co/Qiskit/Qwen2.5-Coder-14B-Qiskit
    ```

คุณสามารถใช้โมเดล `hf.co/Qiskit/Qwen2.5-Coder-14B-Qiskit` หรือโมเดล GGUF อย่างเป็นทางการที่แนะนำอื่น ๆ `hf.co/Qiskit/mistral-small-3.2-24b-qiskit-GGUF` หรือ `hf.co/Qiskit/granite-3.3-8b-qiskit-GGUF`

#### ตั้งค่า Ollama ด้วยโมเดล Qiskit Code Assistant GGUF ที่ดาวน์โหลดด้วยตนเอง {#set-up-ollama-with-a-manually-downloaded-qiskit-code-assistant-gguf-model}

หากคุณดาวน์โหลดโมเดล GGUF ด้วยตนเอง เช่น https://huggingface.co/Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF และต้องการทดลองด้วย template และ parameter ต่างกัน คุณสามารถทำตามขั้นตอนเหล่านี้เพื่อโหลดเข้า Ollama application ใน local ของคุณ

1. สร้าง `Modelfile` โดยป้อนเนื้อหาต่อไปนี้ และอย่าลืมอัปเดต `<PATH-TO-GGUF-FILE>` เป็นเส้นทางจริงของโมเดลที่ดาวน์โหลด

    ```
    FROM <PATH-TO-GGUF-FILE>
    TEMPLATE """{{ if .System }}
    System:
    {{ .System }}

    {{ end }}{{ if .Prompt }}Question:
    {{ .Prompt }}

    {{ end }}Answer:
    ```python{{ .Response }}
    """

    PARAMETER stop "Question:"
    PARAMETER stop "Answer:"
    PARAMETER stop "System:"
    PARAMETER stop "```"

    PARAMETER temperature 0
    PARAMETER top_k 1
    ```

1. รันคำสั่งต่อไปนี้เพื่อสร้าง instance โมเดลแบบกำหนดเองตาม `Modelfile`

    ```
    ollama create Qwen2.5-Coder-14B-Qiskit -f ./path-to-model-file
    ```

    <Admonition type="note">กระบวนการนี้อาจใช้เวลาสักครู่เพื่อให้ Ollama อ่านไฟล์โมเดล เริ่มต้น instance โมเดล และกำหนดค่าตามข้อกำหนดที่ระบุ</Admonition>

#### รันโมเดล Qiskit Code Assistant ที่ดาวน์โหลดด้วยตนเองใน Ollama {#run-the-qiskit-code-assistant-model-manually-downloaded-in-ollama}

หลังจากตั้งค่าโมเดล `Qwen2.5-Coder-14B-Qiskit` ใน Ollama แล้ว รันคำสั่งต่อไปนี้เพื่อเปิดโมเดลและโต้ตอบกับมันใน terminal (ในโหมด chat)

```
ollama run Qwen2.5-Coder-14B-Qiskit
```

คำสั่งที่มีประโยชน์:

- `ollama list` - แสดงรายการโมเดลบนคอมพิวเตอร์ของคุณ
- `ollama rm Qwen2.5-Coder-14B-Qiskit` - ลบโมเดล
- `ollama show Qwen2.5-Coder-14B-Qiskit` - แสดงข้อมูลโมเดล
- `ollama stop Qwen2.5-Coder-14B-Qiskit` - หยุดโมเดลที่กำลังทำงานอยู่
- `ollama ps` - แสดงรายการโมเดลที่โหลดอยู่ในขณะนี้

</details>

<details>

<summary>Deploy โมเดล Qiskit Code Assistant ใน local ผ่านแพ็กเกจ `llama-cpp-python` ด้วยตนเอง</summary>

<span id="use-llama-cpp-python"></span>
ทางเลือกอื่นนอกจาก Ollama application คือแพ็กเกจ `llama-cpp-python` ซึ่งเป็น Python binding สำหรับ `llama.cpp` ให้การควบคุมและความยืดหยุ่นมากขึ้นในการรันโมเดล GGUF ใน local และเหมาะสำหรับผู้ใช้ที่ต้องการรวมโมเดล local ไว้ใน workflow และ Python application ของตน

1. ติดตั้ง [`llama-cpp-python`](https://pypi.org/project/llama-cpp-python/)
2. โต้ตอบกับโมเดลจากภายใน application ของคุณโดยใช้ `llama_cpp` ตัวอย่าง:

```python
from llama_cpp import Llama

model_path = <PATH-TO-GGUF-FILE>

model = Llama(
        model_path,
        seed=17,
        n_ctx=10000,
        n_gpu_layers=37, # to offload in gpu, but put 0 if all in cpu
    )

input = 'Generate a quantum circuit with 2 qubits'
raw_pred = model(input)["choices"][0]["text"]
```

คุณยังสามารถเพิ่ม parameter การสร้างข้อความให้กับโมเดลเพื่อปรับแต่ง inference:

```python
generation_kwargs = {
        "max_tokens": 512,
        "echo": False, # Echo the prompt in the output
        "top_k": 1
    }

raw_pred = model(input, **generation_kwargs)["choices"][0]["text"]
```

</details>

<details>

<summary>Deploy โมเดล Qiskit Code Assistant ใน local ผ่าน llama.cpp ด้วยตนเอง</summary>

### ใช้ไลบรารี `llama.cpp` {#use-the-llamacpp-library}

ทางเลือกอื่นคือการใช้ `llama.cpp` ซึ่งเป็นไลบรารี open-source สำหรับทำ LLM inference บน CPU โดยใช้การตั้งค่าน้อยที่สุด มันให้การควบคุมระดับต่ำเหนือการรันโมเดลและมักรันจาก command line โดยชี้ไปยังไฟล์โมเดล GGUF ใน local

มีหลายวิธีในการติดตั้ง `llama.cpp` บนเครื่องของคุณ:

- ติดตั้ง llama.cpp โดยใช้ [brew, nix, หรือ winget](https://github.com/ggml-org/llama.cpp/blob/master/docs/install.md)
- รันด้วย Docker: ดู [เอกสาร Docker โดยทีม `llama.cpp`](https://github.com/ggml-org/llama.cpp/blob/master/docs/docker.md)
- ดาวน์โหลด pre-built binaries จาก [releases page](https://github.com/ggml-org/llama.cpp/releases)
- Build จาก source โดย [clone repository นี้](https://github.com/ggml-org/llama.cpp/blob/master/docs/build.md)

เมื่อติดตั้งแล้ว คุณสามารถใช้ `llama.cpp` เพื่อโต้ตอบกับโมเดล GGUF ในโหมด conversation ดังนี้:
```bash
# Use a local model file
llama-cli -m my_model.gguf -cnv

# Or download and run a model directly from Hugging Face
llama-cli -hf Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF -cnv
```

คุณยังสามารถเปิด API server ที่รองรับ OpenAI สำหรับโมเดลได้ดังนี้:
```bash
llama-server -hf Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF
```

#### Parameter ขั้นสูง {#advanced-parameters}

ด้วยโปรแกรม `llama-cli` คุณสามารถควบคุมการสร้างโมเดลโดยใช้ command-line option ตัวอย่างเช่น คุณสามารถระบุ "system" prompt เริ่มต้นโดยใช้ flag `-p/--prompt` ในโหมด conversation (`-cnv`) prompt เริ่มต้นนี้ทำหน้าที่เป็น system message มิฉะนั้น คุณสามารถเพียงเพิ่ม instruction ที่ต้องการไว้ด้านหน้าข้อความ prompt ของคุณ คุณยังสามารถปรับ sampling parameter ตัวอย่างเช่น: temperature (`--temp`), top-k (`--top-k`), top-p (`--top-p`), repetition penalty (`--repeat-penalty`), และ seed ที่ใช้ (`--seed`) ต่อไปนี้คือตัวอย่างการเรียกใช้ด้วย option เหล่านี้:

```bash
llama-cli -hf Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF \
  -p "You are a friendly assistant." -cnv \
  --temp 0.7 \
  --top-k 50 \
  --top-p 0.95 \
  --repeat-penalty 1.1 \
  --seed 42
```

เพื่อให้โมเดล Qiskit ของเราทำงานได้อย่างถูกต้อง เราแนะนำให้ใช้ system prompt ที่ระบุใน HF GGUF repositories ของเรา: system prompt สำหรับ [mistral-small-3.2-24b-qiskit-GGUF](https://huggingface.co/Qiskit/mistral-small-3.2-24b-qiskit-GGUF/blob/main/system), [Qwen2.5-Coder-14B-Qiskit-GGUF](https://huggingface.co/Qiskit/Qwen2.5-Coder-14B-Qiskit-GGUF/blob/main/system), [granite-3.3-8b-qiskit-GGUF](https://huggingface.co/Qiskit/granite-3.3-8b-qiskit-GGUF/blob/main/system), และ [granite-3.2-8b-qiskit-GGUF](https://huggingface.co/Qiskit/granite-3.2-8b-qiskit-GGUF/blob/main/system)

</details>

<details>

<summary>เชื่อมต่อ extension กับ local deployment ด้วยตนเอง</summary>

ใช้ VS Code extension และ JupyterLab extension สำหรับ Qiskit Code Assistant เพื่อ prompt โมเดล Qiskit Code Assistant ที่ deploy ใน local เมื่อคุณ[ตั้งค่า Ollama application กับโมเดล](#using-the-ollama-application) แล้ว คุณสามารถกำหนดค่า extension เพื่อเชื่อมต่อกับ local service ได้

### เชื่อมต่อด้วย Qiskit Code Assistant VS Code extension {#connect-with-the-qiskit-code-assistant-vs-code-extension}

ด้วย Qiskit Code Assistant VS Code extension คุณสามารถโต้ตอบกับโมเดลและทำ code completion ขณะเขียนโค้ดได้ ซึ่งเหมาะสำหรับผู้ใช้ที่ต้องการความช่วยเหลือในการเขียนโค้ด Qiskit สำหรับ Python application ของตน

1. ติดตั้ง [Qiskit Code Assistant VS Code extension](/guides/qiskit-code-assistant-vscode)
1. ใน VS Code ไปที่ **User Settings** และตั้งค่า **Qiskit Code Assistant: Url** เป็น URL ของ local Ollama deployment (ตัวอย่าง `http://localhost:11434`)
1. โหลด VS Code ใหม่โดยไปที่ **View > Command Palette...** และเลือก **Developer: Reload Window**

โมเดล Qiskit Code Assistant ที่กำหนดค่าใน Ollama จะปรากฏใน status bar และพร้อมใช้งาน

### เชื่อมต่อด้วย Qiskit Code Assistant JupyterLab extension {#connect-with-the-qiskit-code-assistant-jupyterlab-extension}

ด้วย Qiskit Code Assistant JupyterLab extension คุณสามารถโต้ตอบกับโมเดลและทำ code completion โดยตรงใน Jupyter Notebook ได้ ผู้ใช้ที่ทำงานกับ Jupyter Notebook เป็นหลักสามารถใช้ประโยชน์จาก extension นี้เพื่อเพิ่มประสิทธิภาพในการเขียนโค้ด Qiskit

1. ติดตั้ง [Qiskit Code Assistant JupyterLab extension](/guides/qiskit-code-assistant-jupyterlab)
1. ใน JupyterLab ไปที่ **Settings Editor** และตั้งค่า **Qiskit Code Assistant Service API** เป็น URL ของ local Ollama deployment (ตัวอย่าง `http://localhost:11434`)

โมเดล Qiskit Code Assistant ที่กำหนดค่าใน Ollama จะปรากฏใน status bar และพร้อมใช้งาน

</details>
