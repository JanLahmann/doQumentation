---
title: "Quickstart"
sidebar_label: "Quickstart"
description: "สร้างและแสดงผล quantum circuit ในเวลาไม่ถึงสองนาที ไม่ต้องลงชื่อเข้าใช้หรือมี API key"
notebook_path: "guides/quick-start.ipynb"
---

{/* doqumentation-source-hash: 4dc6c1d3 */}

<OpenInLabBanner notebookPath="guides/quick-start.ipynb" />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';



{/* cspell:ignore miniconda */}

{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>เวอร์ชันของแพ็กเกจ</b></summary>

โค้ดในหน้านี้พัฒนาโดยใช้ requirements ต่อไปนี้
แนะนำให้ใช้เวอร์ชันนี้หรือใหม่กว่า

```
qiskit[all]~=2.3.0
```
</details>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

สร้าง quantum circuit แรกของคุณในเวลาไม่ถึงสองนาที บนสภาพแวดล้อมในเครื่องของคุณ — ไม่ต้องลงชื่อเข้าใช้หรือมี API key

<Admonition type="note" title="ยังใหม่กับ Python และ virtual environment?">

* ดาวน์โหลด Python และใช้ virtual environment กับ Qiskit (แนะนำ)

<details>
<summary>คลิกเพื่อขยายข้อมูลเพิ่มเติมเกี่ยวกับ **Python**</summary>

- ในการติดตั้ง Python ให้ตรวจสอบส่วน "Programming Language" ใน [หน้าโปรเจกต์ Qiskit บน PyPI](https://pypi.org/project/qiskit/) ก่อน เพื่อดูว่าเวอร์ชัน Python ใดบ้างที่รองรับรุ่นล่าสุด สำหรับคำแนะนำการดาวน์โหลด ดูที่ [Python Beginners Guide](https://wiki.python.org/moin/BeginnersGuide/Download)

<Admonition type="note">
คำแนะนำเหล่านี้ใช้ Python distribution มาตรฐานจาก [pypi.org](https://pypi.org/) อย่างไรก็ตาม คุณสามารถใช้ Python distribution อื่นได้ เช่น [Anaconda](https://docs.anaconda.com/anaconda/) หรือ [miniconda](https://docs.anaconda.com/miniconda/) รวมถึง workflow การจัดการ dependency อื่น ๆ เช่น [Poetry](https://python-poetry.org/docs/)
</Admonition>
</details>

    <details>
    <summary>
    คลิกเพื่อขยายข้อมูลเพิ่มเติมเกี่ยวกับ **virtual environment**
    </summary>
    - ใช้ [Python virtual environment](https://docs.python.org/3.10/tutorial/venv.html) เพื่อแยก Qiskit ออกจากแอปพลิเคชันอื่น
    Python virtual environment คือพื้นที่ทำงานแบบแยกส่วนสำหรับ Python ตามวัตถุประสงค์เฉพาะ — คุณสามารถติดตั้งแพ็กเกจใดก็ได้ตามต้องการ และตั้งค่า library, dependency ต่าง ๆ โดยไม่ส่งผลกระทบต่อสภาพแวดล้อม Python "หลัก" บนเครื่องของคุณ

    ข้อดีสำคัญอย่างหนึ่งของ virtual environment คือหากสภาพแวดล้อม Python ของคุณเสียหาย คุณสามารถลบและเริ่มต้นใหม่ได้ง่าย ๆ

    เลือกตำแหน่งที่ต้องการเก็บข้อมูล virtual environment ของคุณ โดยปกติจะเก็บไว้ในไดเรกทอรีชื่อ `.venv` ภายในไดเรกทอรีโปรเจกต์แต่ละอัน

    ในการสร้าง virtual environment ให้นำทางไปยังไดเรกทอรีโปรเจกต์ของคุณ แล้วสร้างสภาพแวดล้อมขั้นต่ำที่มีเฉพาะ Python เท่านั้น

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

    จากนั้น activate สภาพแวดล้อมใหม่ของคุณ

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
              หากใช้ PowerShell:

             ```text
             .venv\Scripts\Activate.ps1
             ```
             หากใช้ Git Bash:

             ```text
             source .venv/scripts/activate
             ```
              หากใช้ command prompt:

             ```text
             .venv\Scripts\activate
             ```
           </TabItem>
         </OperatingSystemTabs>
    </details>

</Admonition>

## 1. ติดตั้ง Qiskit {#1-install-qiskit}

ติดตั้งสิ่งต่อไปนี้ด้วย package manager ที่คุณชอบ (เช่น `pip`):

- [`qiskit`](/guides/install-qiskit)
- [`matplotlib`](https://matplotlib.org/stable/users/explain/quick_start.html)
- [`qiskit[visualization]`](https://docs.quantum.ibm.com/api/qiskit/visualization)
## 2. สร้าง Circuit ของคุณ {#2-build-your-circuit}

เปิดสภาพแวดล้อม Python จากนั้นรันโค้ดนี้เพื่อสร้าง Bell state (Qubit สองตัวที่ entangled กัน)

```python
from qiskit import QuantumCircuit
from qiskit.primitives import StatevectorSampler

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

sampler = StatevectorSampler()
result = sampler.run([qc], shots=1024).result()
print(result[0].data.meas.get_counts())
```

```text
{'11': 534, '00': 490}
```

ผลลัพธ์ที่คาดไว้คือการแบ่งเกือบเท่ากันระหว่าง '00' และ '11'
## 3. แสดงผลลัพธ์ {#3-visualize-your-results}

ในการรับ histogram ของผลลัพธ์ ให้เพิ่มโค้ดต่อไปนี้ในโปรแกรมของคุณ

```python
# Uncomment lines 2 and 8 if you are not using Python in a Jupyter notebook
# import matplotlib.pyplot as plt
from qiskit.visualization import plot_histogram

counts = result[0].data.meas.get_counts()
plot_histogram(counts)

# plt.show()
```

![ผลลัพธ์ของ code cell ก่อนหน้า](/docs/images/guides/quick-start/extracted-outputs/dc4ff012-0.svg)

ผลลัพธ์นี้เป็นสัญลักษณ์ของ quantum entanglement

## 4. ดูสิ่งที่เกิดขึ้น {#4-see-what-happens}

ลองเปลี่ยนโค้ดเพื่อดูผลกระทบต่อผลลัพธ์ ตัวอย่างเช่น:

- เพิ่ม Qubit ตัวที่สามโดยเปลี่ยนเป็น `QuantumCircuit(3)` และเพิ่ม CX Gate ตัวที่สองด้วย `qc.cx(1,2)` ผลการวัดควรเปลี่ยนเป็น 000 และ 111 ซึ่งหมายความว่า Qubit ทั้งสามตัวถูก entangle เข้าด้วยกัน

- ดูผลลัพธ์เปลี่ยนแปลงโดยเพิ่ม `qc.x(1)` ที่ท้าย Circuit
## ขั้นตอนถัดไป {#next-steps}

<Admonition type="tip" title="คำแนะนำ">
- ทำตามขั้นตอนใน [Hello world](/guides/hello-world) เพื่อรัน Circuit บน quantum hardware จริง
- ยังไม่พร้อมรันบน hardware? เริ่มต้นการเดินทางด้าน quantum กับคอร์ส [Basics of quantum information](/learning/courses/basics-of-quantum-information)
</Admonition>
