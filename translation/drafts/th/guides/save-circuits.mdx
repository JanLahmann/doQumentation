---
title: "บันทึกและดึงออบเจ็กต์ Qiskit"
sidebar_label: "บันทึกและดึงออบเจ็กต์ Qiskit"
description: "จัดเก็บออบเจ็กต์ Qiskit บนดิสก์หรือบนคลาวด์เพื่อให้คุณทำต่อจากที่ค้างไว้ได้"
notebook_path: "guides/save-circuits.ipynb"
---
{/* doqumentation-source-hash: 9a873505 */}

<OpenInLabBanner notebookPath="guides/save-circuits.ipynb" />



# บันทึก Circuit ลงดิสก์ {#save-circuits-to-disk}
{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>เวอร์ชันของแพ็กเกจ</b></summary>

โค้ดในหน้านี้พัฒนาโดยใช้ข้อกำหนดต่อไปนี้
แนะนำให้ใช้เวอร์ชันเหล่านี้หรือใหม่กว่า

```
qiskit[all]~=2.3.0
```
</details>
ใช้ [QPY serialization](https://docs.quantum.ibm.com/api/qiskit/qpy) เพื่อบันทึก Circuit ของคุณลงไฟล์ ไฟล์ QPY จัดเก็บออบเจ็กต์ Qiskit Circuit แบบเต็มรูปแบบและจะเข้ากันได้กับ Qiskit เวอร์ชันใหม่กว่า (แม้ว่าอาจไม่เข้ากันได้กับ Qiskit เวอร์ชันเก่ากว่าก็ตาม)

เพื่อสาธิต เซลล์ต่อไปนี้จะสร้าง quantum circuit อย่างง่าย

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()
```

หากต้องการบันทึกไฟล์นี้ลงดิสก์ ใช้ฟังก์ชัน `qpy.dump` คุณยังสามารถบันทึกรายการของ Circuit ได้ด้วย

```python
from qiskit import qpy

with open("test.qpy", "wb") as file:
    qpy.dump(qc, file)
```

ตอนนี้ Circuit นี้ถูกบันทึกลงไฟล์ `test.qpy` แล้ว หากคุณรีสตาร์ท Python kernel คุณสามารถโหลด Circuit ใหม่ได้โดยใช้ฟังก์ชัน `qpy.load` โปรดสังเกตว่าฟังก์ชันนี้จะคืนค่าเป็นรายการของ Circuit เสมอ แม้ว่าคุณจะ serialize เพียง Circuit เดียว

```python
with open("test.qpy", "rb") as handle:
    qc = qpy.load(handle)

qc[0].draw("mpl")
```

![ผลลัพธ์ของเซลล์โค้ดก่อนหน้า](/docs/images/guides/save-circuits/extracted-outputs/210d05c8-c9eb-4135-bb3f-94c281708f4b-0.svg)
