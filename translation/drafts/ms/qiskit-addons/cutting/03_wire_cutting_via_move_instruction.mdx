---
title: "Wire Cutting Dinyatakan sebagai Arahan `Move` Dua-Qubit"
sidebar_label: "Wire Cutting Dinyatakan sebagai Arahan `Move` Dua-Qubit"
notebook_path: "qiskit-addons/cutting/03_wire_cutting_via_move_instruction.ipynb"
---

{/* doqumentation-source-hash: cf00a5bc */}

import TutorialFeedback from '@site/src/components/TutorialFeedback';

<OpenInLabBanner notebookPath="qiskit-addons/cutting/03_wire_cutting_via_move_instruction.ipynb" />


Dalam tutorial ini, kita akan membina semula nilai jangkaan bagi sebuah Circuit tujuh-Qubit dengan memecahkannya kepada dua Circuit empat-Qubit menggunakan wire cutting.

Berikut adalah langkah-langkah yang akan kita ambil dalam [corak Qiskit](https://quantum.cloud.ibm.com/docs/guides/intro-to-patterns) ini:

- **Langkah 1: Petakan masalah kepada litar kuantum dan pengoperasi**:
    - Petakan hamiltonian kepada sebuah Circuit kuantum.
- **Langkah 2: Optimumkan untuk perkakasan sasaran** [_Menggunakan cutting addon_]:
    - <font color='#0F62FE'>Potong Circuit dan observable.</font>
    - Transpile subeksperimen untuk perkakasan.
- **Langkah 3: Laksanakan pada perkakasan sasaran**:
    - Jalankan subeksperimen yang diperoleh dalam Langkah 2 menggunakan primitif `Sampler`.
- **Langkah 4: Proses keputusan** [_Menggunakan cutting addon_]:
    - <font color='#0F62FE'>Gabungkan keputusan Langkah 3 untuk membina semula nilai jangkaan bagi observable yang berkenaan.</font>
## Langkah 1: Petakan {#step-1-map}

### Buat sebuah Circuit untuk dipotong {#create-a-circuit-to-cut}

Pertama, kita mulakan dengan sebuah Circuit yang diilhamkan daripada Rajah 1(a) dalam [arXiv:2302.03366v1](https://arxiv.org/abs/2302.03366v1).

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-addon-cutting qiskit-aer qiskit-ibm-runtime
```

```python
import numpy as np
from qiskit import QuantumCircuit

qc_0 = QuantumCircuit(7)
for i in range(7):
    qc_0.rx(np.pi / 4, i)
qc_0.cx(0, 3)
qc_0.cx(1, 3)
qc_0.cx(2, 3)
qc_0.cx(3, 4)
qc_0.cx(3, 5)
qc_0.cx(3, 6)
qc_0.cx(0, 3)
qc_0.cx(1, 3)
qc_0.cx(2, 3)
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7f16ab191a80>
```

```python
qc_0.draw("mpl")
```

![Quantum circuit diagram](./_03_wire_cutting_via_move_instruction_imgs/output_1.png)

### Tentukan sebuah observable {#specify-an-observable}

```python
from qiskit.quantum_info import SparsePauliOp

observable = SparsePauliOp(["ZIIIIII", "IIIZIII", "IIIIIIZ"])
```

## Langkah 2: Optimumkan {#step-2-optimize}

### Buat Circuit baharu dengan arahan `Move` yang diletakkan di lokasi potongan yang dikehendaki {#create-a-new-circuit-where-move-instructions-have-been-placed-at-the-desired-cut-locations}

Berdasarkan Circuit di atas, kita ingin meletakkan dua wire cut pada garisan Qubit tengah, supaya Circuit boleh dipisahkan kepada dua Circuit masing-masing empat Qubit. Satu cara untuk melakukan ini ialah dengan meletakkan secara manual arahan `Move` dua-Qubit yang memindahkan keadaan dari satu wayar Qubit ke wayar yang lain. Sebuah arahan `Move` secara konsepnya setara dengan operasi set semula pada Qubit kedua, diikuti oleh Gate SWAP. Kesan arahan ini ialah memindahkan keadaan Qubit pertama (sumber) kepada Qubit kedua (destinasi), sambil membuang keadaan masuk Qubit kedua. Supaya ini berfungsi seperti yang dimaksudkan, adalah penting bahawa Qubit kedua (destinasi) tidak berkongsi sebarang keterjeratan dengan baki sistem; jika tidak, operasi set semula akan menyebabkan keadaan baki sistem runtuh sebahagiannya.

Di sini, kita bina sebuah Circuit baharu dengan satu Qubit tambahan dan operasi `Move` di tempat yang betul. Dalam contoh ini, kita boleh guna semula sebuah Qubit: Qubit sumber bagi `Move` pertama menjadi Qubit destinasi bagi operasi `Move` kedua.

Nota: Sebagai alternatif kepada penggunaan terus arahan `Move`, seseorang boleh memilih untuk menanda wire cut menggunakan arahan `CutWire` satu-Qubit. Fungsi `cut_wires` wujud untuk mengubah `CutWire` kepada arahan `Move` pada Qubit yang baru diperuntukkan. Walau bagaimanapun, berbeza dengan kaedah manual, kaedah automatik ini tidak membenarkan penggunaan semula wayar Qubit. Lihat [panduan cara-cara](../how-tos/how_to_specify_cut_wires.ipynb) `CutWire` untuk butiran lanjut.

```python
from qiskit_addon_cutting.instructions import Move

qc_1 = QuantumCircuit(8)
for i in [*range(4), *range(5, 8)]:
    qc_1.rx(np.pi / 4, i)
qc_1.cx(0, 3)
qc_1.cx(1, 3)
qc_1.cx(2, 3)
qc_1.append(Move(), [3, 4])
qc_1.cx(4, 5)
qc_1.cx(4, 6)
qc_1.cx(4, 7)
qc_1.append(Move(), [4, 3])
qc_1.cx(0, 3)
qc_1.cx(1, 3)
qc_1.cx(2, 3)

qc_1.draw("mpl")
```

![Quantum circuit diagram](./_03_wire_cutting_via_move_instruction_imgs/output_2.png)

### Buat observable untuk Circuit baharu {#create-observable-to-go-with-the-new-circuit}

Observable ini berpadanan dengan `observable`, tetapi kita perlu mengambil kira dengan betul wayar Qubit tambahan yang telah ditambah (iaitu, kita sisipkan "I" pada indeks 4). Perhatikan bahawa dalam Qiskit, watak Pauli paling kanan dalam perwakilan rentetan sepadan dengan Qubit-0.

```python
observable_expanded = SparsePauliOp(["ZIIIIIII", "IIIIZIII", "IIIIIIIZ"])
```

### Pisahkan Circuit dan observable {#separate-the-circuit-and-observables}

Seperti dalam tutorial sebelumnya, Qubit yang berkongsi label partition yang sama akan dikumpulkan bersama, dan Gate bukan setempat yang merentangi lebih daripada satu partition akan dipotong.

```python
from qiskit_addon_cutting import partition_problem

partitioned_problem = partition_problem(
    circuit=qc_1, partition_labels="AAAABBBB", observables=observable_expanded.paulis
)
subcircuits = partitioned_problem.subcircuits
subobservables = partitioned_problem.subobservables
bases = partitioned_problem.bases
```

### Visualkan masalah yang telah didekomposisikan {#visualize-the-decomposed-problem}

```python
subobservables
```

```text
{'A': PauliList(['IIII', 'ZIII', 'IIIZ']),
 'B': PauliList(['ZIII', 'IIII', 'IIII'])}
```

```python
subcircuits["A"].draw("mpl")
```

![Quantum circuit diagram](./_03_wire_cutting_via_move_instruction_imgs/output_3.png)

```python
subcircuits["B"].draw("mpl")
```

![Quantum circuit diagram](./_03_wire_cutting_via_move_instruction_imgs/output_4.png)

### Kira overhed pensampelan untuk potongan yang dipilih {#calculate-the-sampling-overhead-for-the-chosen-cuts}

Di sini kita memotong dua wayar, menghasilkan overhed pensampelan sebesar $4^4$.

Untuk maklumat lanjut tentang overhed pensampelan yang ditanggung oleh circuit cutting, rujuk [bahan penerangan](../explanation/index.rst).

```python
print(f"Sampling overhead: {np.prod([basis.overhead for basis in bases])}")
```

```text
Sampling overhead: 256.0
```

### Jana subeksperimen untuk dijalankan pada Backend {#generate-the-subexperiments-to-run-on-the-backend}

`generate_cutting_experiments` menerima argumen `circuits`/`observables` sebagai kamus yang memetakan label partition Qubit kepada `subcircuit`/`subobservables` masing-masing.

Untuk mensimulasikan nilai jangkaan Circuit bersaiz penuh, banyak subeksperimen dijana daripada taburan kuasikemungkinan bersama Gate yang didekomposisikan dan kemudiannya dilaksanakan pada satu atau lebih Backend. Bilangan sampel yang diambil daripada taburan dikawal oleh `num_samples`, dan satu pekali gabungan diberikan bagi setiap sampel unik. Untuk maklumat lanjut tentang cara pekali dikira, rujuk [bahan penerangan](../explanation/index.rst).

```python
from qiskit_addon_cutting import generate_cutting_experiments

subexperiments, coefficients = generate_cutting_experiments(
    circuits=subcircuits, observables=subobservables, num_samples=np.inf
)
```

### Pilih sebuah Backend {#choose-a-backend}

Di sini kita menggunakan Backend palsu, yang akan menyebabkan Qiskit Runtime berjalan dalam mod tempatan (iaitu, pada simulator tempatan).

```python
from qiskit_ibm_runtime.fake_provider import FakeManilaV2

backend = FakeManilaV2()
```

### Sediakan subeksperimen untuk Backend {#prepare-the-subexperiments-for-the-backend}

Kita perlu men-transpile Circuit dengan Backend kita sebagai sasaran sebelum menghantarnya ke Qiskit Runtime.

```python
from qiskit.transpiler import generate_preset_pass_manager

# Transpile the subexperiments to ISA circuits
pass_manager = generate_preset_pass_manager(optimization_level=1, backend=backend)
isa_subexperiments = {
    label: pass_manager.run(partition_subexpts)
    for label, partition_subexpts in subexperiments.items()
}
```

## Langkah 3: Laksanakan {#step-3-execute}

### Jalankan subeksperimen menggunakan primitif Sampler Qiskit Runtime {#run-the-subexperiments-using-the-qiskit-runtime-sampler-primitive}

```python
from qiskit_ibm_runtime import SamplerV2, Batch

# Submit each partition's subexperiments to the Qiskit Runtime Sampler
# primitive, in a single batch so that the jobs will run back-to-back.
with Batch(backend=backend) as batch:
    sampler = SamplerV2(mode=batch)
    jobs = {
        label: sampler.run(subsystem_subexpts, shots=2**12)
        for label, subsystem_subexpts in isa_subexperiments.items()
    }
```

```text
/home/garrison/Qiskit/qiskit-ibm-runtime/qiskit_ibm_runtime/session.py:157: UserWarning: Session is not supported in local testing mode or when using a simulator.
  warnings.warn(
```

```python
# Retrieve results
results = {label: job.result() for label, job in jobs.items()}
```

## Langkah 4: Proses Keputusan {#step-4-post-process}

### Bina semula nilai jangkaan {#reconstruct-the-expectation-value}

Bina semula nilai jangkaan bagi setiap sebutan observable dan gabungkannya untuk membina semula nilai jangkaan bagi observable asal.

```python
from qiskit_addon_cutting import reconstruct_expectation_values

reconstructed_expval_terms = reconstruct_expectation_values(
    results,
    coefficients,
    subobservables,
)
reconstructed_expval = np.dot(reconstructed_expval_terms, observable.coeffs)
```

### Bandingkan nilai jangkaan yang dibina semula dengan nilai jangkaan tepat daripada Circuit dan observable asal {#compare-the-reconstructed-expectation-value-with-the-exact-expectation-value-from-the-original-circuit-and-observable}

```python
from qiskit_aer.primitives import EstimatorV2

estimator = EstimatorV2()
exact_expval = estimator.run([(qc_0, observable)]).result()[0].data.evs
print(f"Reconstructed expectation value: {np.real(np.round(reconstructed_expval, 8))}")
print(f"Exact expectation value: {np.round(exact_expval, 8)}")
print(f"Error in estimation: {np.real(np.round(reconstructed_expval-exact_expval, 8))}")
print(
    f"Relative error in estimation: {np.real(np.round((reconstructed_expval-exact_expval) / exact_expval, 8))}"
)
```

```text
Reconstructed expectation value: 1.51319069
Exact expectation value: 1.59099026
Error in estimation: -0.07779957
Relative error in estimation: -0.04890009
```

<TutorialFeedback />
