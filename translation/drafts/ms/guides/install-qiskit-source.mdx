---
title: Pasang Qiskit SDK dari sumber
description: Ketahui cara memasang versi pembangunan Qiskit.

---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore rustup */}

# Pasang Qiskit SDK dan Qiskit Runtime dari sumber

Memasang Qiskit SDK dari sumber membolehkan anda mengakses versi pembangunan semasa, berbanding menggunakan versi dalam repositori Python Package Index (PyPI). Ini membolehkan anda memeriksa dan melanjutkan versi terkini kod Qiskit dengan lebih cekap.

## Cipta dan aktifkan persekitaran maya baharu {#create-and-activate-a-new-virtual-environment}

1. Navigasi ke direktori projek anda dan cipta persekitaran minimal yang hanya mengandungi Python.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

1. Aktifkan persekitaran baharu anda.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
             ```text
             .venv\Scripts\Activate.ps1
             ```
           </TabItem>
         </OperatingSystemTabs>

## Pasang pengkompil Rust {#install-the-rust-compiler}

Pengkompil Rust mesti dipasang pada sistem anda untuk mengkompil Qiskit. Untuk memasang pengkompil Rust, gunakan pemasang Rust merentas platform [`rustup`](https://rustup.rs/) atau [kaedah pemasangan lain.](https://forge.rust-lang.org/infra/other-installation-methods.html)

## Pasang Qiskit {#install-qiskit}

Ikut langkah-langkah ini untuk memasang Qiskit:

1. Klon repositori Qiskit.

```bash
git clone https://github.com/Qiskit/qiskit.git
```

2. Tukar ke direktori `qiskit`.

```bash
cd qiskit
```

3. (Pilihan) Jika anda ingin menjalankan ujian atau semakan linting, pasang keperluan pembangun.

```bash
pip install -r requirements-dev.txt
```

4. Pasang `qiskit`.

  * **Pemasangan standard**:

    ```bash
    pip install .
    ```

  * **Mod boleh sunting**: Dalam mod ini, anda tidak perlu memasang semula Qiskit apabila ada perubahan kod pada projek.

    ```bash
    pip install -e .
    ```

    Dalam mod boleh sunting, sambungan yang dikompil dibina dalam _mod nyahpepijat_ tanpa pengoptimuman. Ini mempengaruhi prestasi runtime kod yang dikompil. Untuk membina sambungan yang dikompil dengan pengoptimuman diaktifkan, jalankan arahan berikut untuk membina semula binari dalam _mod keluaran_:

    ```bash
    python setup.py build_rust --release --inplace
    ```

    <Admonition type="note">
    Jika anda sedang mengerjakan kod Rust dalam Qiskit, anda perlu membina semula kod sambungan setiap kali anda membuat perubahan setempat. Dalam mod boleh sunting, sambungan Rust hanya dibina apabila arahan pasang dijalankan, jadi perubahan setempat yang anda buat pada kod Rust tidak tercermin dalam pakej yang dipasang melainkan anda membina semula sambungan dengan menjalankan semula `build_rust` (dengan atau tanpa `--release`, bergantung pada sama ada anda ingin membina dalam mod keluaran atau nyahpepijat).
    </Admonition>

## Pasang Qiskit Runtime {#install-qiskit-runtime}

Ikut langkah-langkah ini jika anda ingin memasang Qiskit Runtime:

1. Klon repositori Qiskit Runtime.

```bash
git clone https://github.com/Qiskit/qiskit-ibm-runtime.git
```

2. Tukar ke direktori `qiskit-ibm-runtime`.

```bash
cd qiskit-ibm-runtime
```

3. Pasang `qiskit-runtime`. Kami mengesyorkan menggunakan [persekitaran maya](https://docs.python.org/3/library/venv.html) untuk mengelakkan pencemaran pemasangan Python global anda.

  * **Pemasangan standard**:

    ```bash
    pip install .
    ```

  * **Mod boleh sunting**: Dalam mod ini, anda tidak perlu memasang semula Qiskit apabila ada perubahan kod pada projek.

    ```bash
    pip install -e .
    ```

    Dalam mod boleh sunting, sambungan yang dikompil dibina dalam _mod nyahpepijat_ tanpa pengoptimuman.

4. (Pilihan) Jika anda ingin menjalankan ujian atau semakan linting, pasang keperluan pembangun. Kami mengesyorkan menggunakan [persekitaran maya](https://docs.python.org/3/library/venv.html) untuk mengelakkan pencemaran pemasangan Python global anda.

```bash
pip install -e ".[dev]"
```

## Langkah seterusnya {#next-steps}

<Admonition type="tip" title="Cadangan">
  - Baca [garis panduan penyumbang](https://github.com/Qiskit/qiskit/blob/main/CONTRIBUTING.md) untuk menyumbang kepada Qiskit SDK sumber terbuka.
  - [Jalankan program Hello world](/guides/hello-world).
  - Cuba tutorial, seperti [Algoritma Grover](/tutorials/grovers-algorithm).
</Admonition>
