---
title: "La clase Operator"
sidebar_label: "La clase Operator"
description: "Explicación detallada del uso de la clase Operator en Qiskit."
notebook_path: "guides/operator-class.ipynb"
---
{/* doqumentation-source-hash: f33a6b98 */}

<OpenInLabBanner notebookPath="guides/operator-class.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>Versiones de paquetes</b></summary>

El código de esta página fue desarrollado con los siguientes requisitos.
Recomendamos usar estas versiones o versiones más recientes.

```
qiskit[all]~=2.3.0
```
</details>
Esta página muestra cómo usar la clase [`Operator`](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator). Para obtener una descripción general de alto nivel de las representaciones de operadores en Qiskit, incluyendo la clase `Operator` y otras, consulta [Descripción general de las clases de operadores](./operators-overview).

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit
```

```python
import numpy as np
from qiskit.circuit import QuantumCircuit
from qiskit.circuit.library import CXGate, RXGate, XGate
from qiskit.quantum_info import Operator, Pauli, process_fidelity
```

## Convertir clases a Operators {#convert-classes-to-operators}

Varias otras clases en Qiskit pueden convertirse directamente en un objeto `Operator` usando el método de inicialización del operador. Por ejemplo:

* Objetos `Pauli`
* Objetos `Gate` e `Instruction`
* Objetos `QuantumCircuit`

Ten en cuenta que el último punto significa que puedes usar la clase `Operator` como un simulador unitario para calcular la matriz unitaria final de un circuito cuántico, sin necesidad de llamar a un backend simulador. Si el circuito contiene operaciones no compatibles, se lanza una excepción. Las operaciones no compatibles son: medición, reinicio, operaciones condicionales, o una compuerta que no tiene definición de matriz ni descomposición en términos de compuertas con definiciones de matriz.

```python
# Create an Operator from a Pauli object

pauliXX = Pauli("XX")
Operator(pauliXX)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j],
          [1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an Operator for a Gate object
Operator(CXGate())
```

```text
Operator([[1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an operator from a parameterized Gate object
Operator(RXGate(np.pi / 2))
```

```text
Operator([[0.70710678+0.j        , 0.        -0.70710678j],
          [0.        -0.70710678j, 0.70710678+0.j        ]],
         input_dims=(2,), output_dims=(2,))
```

```python
# Create an operator from a QuantumCircuit object
circ = QuantumCircuit(10)
circ.h(0)
for j in range(1, 10):
    circ.cx(j - 1, j)

# Convert circuit to an operator by implicit unitary simulation
Operator(circ)
```

```text
Operator([[ 0.70710678+0.j,  0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          ...,
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.70710678+0.j, -0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j]],
         input_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2), output_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2))
```

## Usar Operators en circuitos {#use-operators-in-circuits}

Los `Operators` unitarios pueden insertarse directamente en un `QuantumCircuit` usando el método `QuantumCircuit.append`. Esto convierte el `Operator` en un objeto `UnitaryGate`, que se añade al circuito.

Si el operador no es unitario, se lanza una excepción. Esto se puede verificar usando la función `Operator.is_unitary()`, que devuelve `True` si el operador es unitario y `False` en caso contrario.

```python
# Create an operator
XX = Operator(Pauli("XX"))

# Add to a circuit
circ = QuantumCircuit(2, 2)
circ.append(XX, [0, 1])
circ.measure([0, 1], [0, 1])
circ.draw("mpl")
```

![Salida de la celda de código anterior](/docs/images/guides/operator-class/extracted-outputs/223c07c9-1b1d-4a33-a397-091cac2ec9e3-0.svg)

Ten en cuenta que en el ejemplo anterior el operador se inicializa a partir de un objeto `Pauli`. Sin embargo, el objeto `Pauli` también puede insertarse directamente en el circuito y se convertirá en una secuencia de compuertas Pauli de un solo qubit:

```python
# Add to a circuit
circ2 = QuantumCircuit(2, 2)
circ2.append(Pauli("XX"), [0, 1])
circ2.measure([0, 1], [0, 1])
circ2.draw()
```

```text
┌────────────┐┌─┐
q_0: ┤0           ├┤M├───
     │  Pauli(XX) │└╥┘┌─┐
q_1: ┤1           ├─╫─┤M├
     └────────────┘ ║ └╥┘
c: 2/═══════════════╩══╩═
                    0  1
```

## Combinar Operators {#combine-operators}

Los operadores pueden combinarse usando varios métodos.

### Producto tensorial {#tensor-product}

Dos operadores $A$ y $B$ pueden combinarse en un operador producto tensorial $A\otimes B$ usando la función `Operator.tensor`. Ten en cuenta que si tanto $A$ como $B$ son operadores de un solo qubit, entonces `A.tensor(B)` = $A\otimes B$ tendrá los subsistemas indexados como la matriz $B$ en el subsistema 0, y la matriz $A$ en el subsistema 1.

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.tensor(B)
```

```text
Operator([[ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j],
          [ 0.+0.j, -0.+0.j,  0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### Expansión tensorial {#tensor-expansion}

Una operación estrechamente relacionada es `Operator.expand`, que actúa como un producto tensorial pero en orden inverso. Por lo tanto, para dos operadores $A$ y $B$, tienes `A.expand(B)` = $B\otimes A$, donde los subsistemas están indexados como la matriz $A$ en el subsistema 0, y la matriz $B$ en el subsistema 1.

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.expand(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j,  0.+0.j, -0.+0.j, -1.+0.j],
          [ 0.+0.j,  0.+0.j, -1.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### Composición {#composition}

También puedes componer dos operadores $A$ y $B$ para implementar multiplicación de matrices usando el método `Operator.compose`. `A.compose(B)` devuelve el operador con la matriz $B.A$:

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j],
          [-1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

También puedes componer en orden inverso aplicando $B$ delante de $A$ usando el argumento `front` de `compose`: `A.compose(B, front=True)` = $A.B$:

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B, front=True)
```

```text
Operator([[ 0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

### Composición de subsistemas {#subsystem-composition}

Ten en cuenta que la composición anterior requiere que la dimensión de salida total del primer operador $A$ sea igual a la dimensión de entrada total del operador compuesto $B$ (y de manera similar, la dimensión de salida de $B$ debe ser igual a la dimensión de entrada de $A$ cuando se compone con `front=True`).

También puedes componer un operador más pequeño con una selección de subsistemas de un operador más grande usando el argumento `qargs` de `compose`, con o sin `front=True`. En este caso, las dimensiones de entrada y salida relevantes de los subsistemas que se componen deben coincidir. *Ten en cuenta que el operador más pequeño siempre debe ser el argumento del método* `compose`*.*

Por ejemplo, para componer una compuerta de dos qubits con un operador de tres qubits:

```python
# Compose XZ with a 3-qubit identity operator
op = Operator(np.eye(2**3))
XZ = Operator(Pauli("XZ"))
op.compose(XZ, qargs=[0, 2])
```

```text
Operator([[ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
           -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

```python
# Compose YX in front of the previous operator
op = Operator(np.eye(2**3))
YX = Operator(Pauli("YX"))
op.compose(YX, qargs=[0, 2], front=True)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j],
          [0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

### Combinaciones lineales {#linear-combinations}

Los operadores también pueden combinarse usando operadores lineales estándar para la suma, la resta y la multiplicación escalar por números complejos.

```python
XX = Operator(Pauli("XX"))
YY = Operator(Pauli("YY"))
ZZ = Operator(Pauli("ZZ"))

op = 0.5 * (XX + YY - 3 * ZZ)
op
```

```text
Operator([[-1.5+0.j,  0. +0.j,  0. +0.j,  0. +0.j],
          [ 0. +0.j,  1.5+0.j,  1. +0.j,  0. +0.j],
          [ 0. +0.j,  1. +0.j,  1.5+0.j,  0. +0.j],
          [ 0. +0.j,  0. +0.j,  0. +0.j, -1.5+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

Un punto importante es que, mientras que `tensor`, `expand` y `compose` preservan la unitariedad de los operadores unitarios, las combinaciones lineales no lo hacen; por lo tanto, sumar dos operadores unitarios producirá, en general, un operador no unitario:

```python
op.is_unitary()
```

```text
False
```

### Conversión implícita a Operators {#implicit-conversion-to-operators}

Ten en cuenta que para todos los métodos siguientes, si el segundo objeto no es ya un objeto `Operator`, el método lo convierte implícitamente en uno. Esto significa que las matrices pueden pasarse directamente sin necesidad de convertirlas explícitamente a `Operator` primero. Si la conversión no es posible, se lanza una excepción.

```python
# Compose with a matrix passed as a list
Operator(np.eye(2)).compose([[0, 1], [1, 0]])
```

```text
Operator([[0.+0.j, 1.+0.j],
          [1.+0.j, 0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

## Comparar Operators {#compare-operators}

Los operadores implementan un método de igualdad que puede usarse para comprobar si dos operadores son aproximadamente iguales.

```python
Operator(Pauli("X")) == Operator(XGate())
```

```text
True
```

Ten en cuenta que esto verifica que cada elemento de la matriz de los operadores sea aproximadamente igual; dos unitarios que difieren por una fase global no se consideran iguales:

```python
Operator(XGate()) == np.exp(1j * 0.5) * Operator(XGate())
```

```text
False
```

### Fidelidad de proceso {#process-fidelity}

También puedes comparar operadores usando la función `process_fidelity` del módulo de *Información Cuántica*. Esta es una magnitud teórico-informacional que mide la proximidad entre dos canales cuánticos y, en el caso de operadores unitarios, no depende de la fase global.

```python
# Two operators which differ only by phase
op_a = Operator(XGate())
op_b = np.exp(1j * 0.5) * Operator(XGate())

# Compute process fidelity
F = process_fidelity(op_a, op_b)
print("Process fidelity =", F)
```

```text
Process fidelity = 1.0
```

Ten en cuenta que la fidelidad de proceso es, en general, una medida de proximidad válida solo si los operadores de entrada son unitarios (o CP en el caso de canales cuánticos), y se lanza una excepción si las entradas no son CP.
## Próximos pasos {#next-steps}

<Admonition type="tip" title="Recomendaciones">
  - Explora la referencia de la [API de Operator](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator#operator).
</Admonition>
