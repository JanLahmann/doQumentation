---
title: Instalar el SDK de Qiskit desde el código fuente
description: Aprende cómo instalar la versión de desarrollo de Qiskit.

---

{/* doqumentation-source-hash: 3602a588 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore rustup */}

# Instalar el SDK de Qiskit y Qiskit Runtime desde el código fuente

Instalar el SDK de Qiskit desde el código fuente te permite acceder a la versión de desarrollo actual, en lugar de usar la versión disponible en el repositorio del Python Package Index (PyPI). Esto te permite inspeccionar y ampliar la última versión del código de Qiskit de forma más eficiente.

## Crear y activar un nuevo entorno virtual {#create-and-activate-a-new-virtual-environment}

1. Navega al directorio de tu proyecto y crea un entorno mínimo con solo Python instalado.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

1. Activa tu nuevo entorno.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
             ```text
             .venv\Scripts\Activate.ps1
             ```
           </TabItem>
         </OperatingSystemTabs>

## Instalar el compilador de Rust {#install-the-rust-compiler}

Es necesario tener un compilador de Rust instalado en tu sistema para compilar Qiskit. Para instalarlo, usa el instalador multiplataforma [`rustup`](https://rustup.rs/) u [otro método de instalación.](https://forge.rust-lang.org/infra/other-installation-methods.html)

## Instalar Qiskit {#install-qiskit}

Sigue estos pasos para instalar Qiskit:

1. Clona el repositorio de Qiskit.

```bash
git clone https://github.com/Qiskit/qiskit.git
```

2. Cambia al directorio `qiskit`.

```bash
cd qiskit
```

3. (Opcional) Si quieres ejecutar pruebas o verificaciones de estilo de código, instala los requisitos para desarrolladores.

```bash
pip install -r requirements-dev.txt
```

4. Instala `qiskit`.

  * **Instalación estándar**:

    ```bash
    pip install .
    ```

  * **Modo editable**: En este modo, no necesitas reinstalar Qiskit cuando haya cambios en el código del proyecto.

    ```bash
    pip install -e .
    ```

    En modo editable, las extensiones compiladas se construyen en _modo debug_ sin optimizaciones. Esto afecta al rendimiento en tiempo de ejecución del código compilado. Para compilar las extensiones con optimizaciones habilitadas, ejecuta el siguiente comando para reconstruir el binario en _modo release_:

    ```bash
    python setup.py build_rust --release --inplace
    ```

    <Admonition type="note">
    Si estás trabajando en código Rust dentro de Qiskit, necesitas reconstruir el código de la extensión cada vez que hagas un cambio local. En modo editable, la extensión de Rust solo se compila cuando se ejecuta el comando de instalación, por lo que los cambios locales que hagas en el código Rust no se reflejan en el paquete instalado a menos que reconstruyas la extensión volviendo a ejecutar `build_rust` (con o sin `--release`, según si quieres compilar en modo release o debug).
    </Admonition>

## Instalar Qiskit Runtime {#install-qiskit-runtime}

Sigue estos pasos si quieres instalar Qiskit Runtime:

1. Clona el repositorio de Qiskit Runtime.

```bash
git clone https://github.com/Qiskit/qiskit-ibm-runtime.git
```

2. Cambia al directorio `qiskit-ibm-runtime`.

```bash
cd qiskit-ibm-runtime
```

3. Instala `qiskit-runtime`. Recomendamos usar un [entorno virtual](https://docs.python.org/3/library/venv.html) para evitar contaminar tu instalación global de Python.

  * **Instalación estándar**:

    ```bash
    pip install .
    ```

  * **Modo editable**: En este modo, no necesitas reinstalar Qiskit cuando haya cambios en el código del proyecto.

    ```bash
    pip install -e .
    ```

    En modo editable, las extensiones compiladas se construyen en _modo debug_ sin optimizaciones.

4. (Opcional) Si quieres ejecutar pruebas o verificaciones de estilo de código, instala los requisitos para desarrolladores. Recomendamos usar un [entorno virtual](https://docs.python.org/3/library/venv.html) para evitar contaminar tu instalación global de Python.

```bash
pip install -e ".[dev]"
```

## Próximos pasos {#next-steps}

<Admonition type="tip" title="Recomendaciones">
  - Lee las [pautas de contribución](https://github.com/Qiskit/qiskit/blob/main/CONTRIBUTING.md) para contribuir al SDK de Qiskit de código abierto.
  - [Ejecuta el programa Hola mundo](/guides/hello-world).
  - Prueba un tutorial, como el [algoritmo de Grover](/tutorials/grovers-algorithm).
</Admonition>
