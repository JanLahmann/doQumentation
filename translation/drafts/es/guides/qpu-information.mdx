---
title: Información sobre la QPU
description: Información sobre la calibración, las propiedades y el versionado de la QPU
in_page_toc_min_heading_level: 2
in_page_toc_max_heading_level: 2
---
{/* doqumentation-source-hash: 233dc279 */}

# Información sobre la QPU {#qpu-information}

IBM&reg; ofrece acceso a una amplia variedad de unidades de procesamiento cuántico (QPUs). Todas las QPUs desplegadas por IBM se basan en tecnología de qubits superconductores, ya que el control y la escalabilidad de esta tecnología abren un camino claro hacia la ventaja cuántica con estas QPUs.

Explora todas las QPUs públicas de IBM navegando a la página [Compute resources](https://quantum.cloud.ibm.com/computers) en IBM Quantum&reg; Platform. Haz clic en cualquier QPU para abrir su tarjeta de información detallada.

Esta página describe la información detallada que encontrarás en la tarjeta de información de la QPU.

## Versionado de la QPU {#qpu-versioning}

Cada QPU tiene un número de versión con la forma X.Y.Z (mayor.menor.revisión). Un circuito compilado para un número de versión determinado tiene garantizado que se ejecutará en esa QPU. Si el número de revisión cambia, el circuito seguirá ejecutándose. Si el número mayor o menor cambia, no se garantiza que el circuito se ejecute, aunque podría hacerlo. A continuación se indican las condiciones bajo las cuales puede cambiar un número de versión:

### Versión mayor {#major-version}

La versión mayor se incrementará por cambios como:

*   Cambios en la muestra (sample).
*   Cambios importantes en la electrónica de control.
*   Traslado de la QPU a una nueva ubicación, si como resultado se producen cambios de comportamiento significativos.

### Versión menor {#minor-version}

La versión menor se incrementará por cambios como:

*   Ciclos de calentamiento / enfriamiento.
*   Sustitución de algún componente electrónico, si el reemplazo afecta apreciablemente a la operación.
*   Cambio en la dirección de una puerta NOT controlada.
*   Eliminación de una puerta durante cierto período de tiempo por problemas de calibración cuando las correcciones no pueden realizarse fácilmente por software.

### Versión de revisión {#revision-version}

El número de versión de revisión se incrementará por correcciones que no rompan el circuito compilado existente. Estos cambios incluyen:

*   Calibraciones manuales para mejorar las fidelidades.
*   Cambios pequeños en la electrónica que no afectan a la operación.
*   Actualizaciones de software de la QPU.

## Detalles de la QPU {#qpu-details}

La primera sección de la tarjeta de información de la QPU proporciona los siguientes detalles:

[Nombre](#name) | [Qubits](#qubits) | [Error 2Q (mejor)](#2q-error-best) | [Error 2Q (en capas)](#2q-error-layered) | [CLOPS (o CLOPS_h)](#clops-or-clops_h) | [Estado](#status) | [Región](#region) | [Versión de la QPU](#qpu-version) | [Tipo de procesador](#processor-type) | [Puertas base](#native-gates-on-platform) | [Total de trabajos pendientes](#total-pending-jobs) | [Error 2Q mediano](#median-2q-error-heron-cz-eagle-ecr) |
[Error SX mediano](#median-sx-error) | [Error de lectura mediano](#median-readout-error) | [T1 mediano (tiempo de relajación)](#median-t1-relaxation-time) | [T2 mediano (tiempo de desfasamiento)](#median-t2-dephasing-time)

### Nombre {#name}

El nombre único asignado a una QPU específica. Las QPUs alojadas en IBM Cloud&reg; tienen nombres que comienzan con `ibm_*`. A todas las QPUs se les asigna el nombre de una ciudad, por ejemplo, `ibm_kingston`. Este nombre no indica dónde está alojada físicamente la QPU. Están nombradas en honor a ubicaciones de IBM&reg; alrededor del mundo.

### Qubits {#qubits}

El número de qubits físicos en una QPU.

### Error 2Q (mejor) {#2q-error-best}

El menor error de dos qubits (2Q) en cualquier arista del dispositivo, obtenido del mismo lote de mediciones utilizado para calcular la mediana (véase [Error 2Q mediano](#median-2q-error-heron-cz-eagle-ecr)).

### Error 2Q (en capas) {#2q-error-layered}

Error promedio por puerta en capas (EPLG) en una cadena de 100 qubits. El EPLG promedio mide el error de puerta promedio en una cadena en capas de $N$ qubits ($N$=100 aquí). Se deriva de una cantidad similar conocida como fidelidad de capa (LF), donde EPLG$_{100}$ = 4/5(1-LF$^{\frac{1}{99}}$) y la fidelidad de capa es la fidelidad de proceso de la cadena en capas de $N$ qubits. Para más detalles, consulta el artículo [Benchmarking quantum processor performance at scale](https://arxiv.org/abs/2311.05933). Ten en cuenta que en el artículo el EPLG se define para el error de proceso, pero por coherencia con los errores de puerta reportados individualmente aquí, se cita para el error de puerta promedio, de ahí el factor 4/5. Encuentra un [cuaderno de ejemplo](https://github.com/qiskit-community/qiskit-device-benchmarking/blob/main/notebooks/layer_fidelity_single_chain.ipynb) en el GitHub de la comunidad de Qiskit.

### CLOPS (o CLOPS_h) {#clops-or-clops_h}

Las operaciones de capa de circuito por segundo (Circuit layer operations per second) son una medida de cuántas capas de un circuito 100x100 (circuito consciente del hardware) puede ejecutar una QPU (unidad de procesamiento cuántico) por unidad de tiempo. Encuentra el [código de CLOPS](https://github.com/qiskit-community/qiskit-device-benchmarking/tree/main/qiskit_device_benchmarking/clops#readme) en el GitHub de la comunidad de Qiskit.

### Estado {#status}

El estado de la QPU; por ejemplo, `Online`, `Paused`, `Offline`, entre otros.

### Región {#region}

Ubicación del centro de datos donde se alojarán y procesarán tus datos y experimentos.

### Versión de la QPU {#qpu-version}

El número de versión de una QPU con la forma `mayor.menor.revisión`. Consulta [Versionado de la QPU](#qpu-versioning) para más detalles sobre cómo se asigna este número.

### Tipo de procesador {#processor-type}

Refleja la [topología](#coupling-map) e indica el conteo aproximado de qubits.

<span id="native-gates-on-platform"></span>
### Puertas base {#basis-gates}

Cada [familia de procesadores](/guides/processor-types) tiene un conjunto de puertas nativas. Por defecto, las QPUs de cada familia solo admiten la ejecución de las puertas y operaciones del conjunto de puertas nativas. Por lo tanto, cada puerta del circuito debe ser traducida (por el transpilador) a los elementos de este conjunto. Ten en cuenta que las operaciones no unitarias no se listan aquí; usa el [método en Qiskit](/guides/get-qpu-information#native-gates-with-qiskit) para ver todas las puertas y operaciones nativas de una QPU. Consulta la lista de todas las puertas nativas en [esta tabla](#native-gates).

### Total de trabajos pendientes {#total-pending-jobs}

El número total de trabajos que has enviado a esta QPU.

### Error 2Q mediano (Heron: CZ, Eagle: ECR) {#median-2q-error-heron-cz-eagle-ecr}

[Fidelidad de puerta promedio](https://docs.quantum.ibm.com/api/qiskit/quantum_info#average_gate_fidelity) de la operación de dos qubits obtenida a partir de benchmarking aleatorizado. Medido en "aislamiento": lotes con una separación mínima de dos qubits entre aristas. Este benchmarking aleatorizado usa capas alternadas de Cliffords de un qubit y puertas de dos qubits, por lo que el valor final del error 2Q incluye el error de la capa de Cliffords de un qubit. Encuentra un [cuaderno de ejemplo](https://github.com/qiskit-community/qiskit-device-benchmarking/blob/main/notebooks/device_rb_w_lf.ipynb) en el GitHub de la comunidad de Qiskit. Encuentra los datos por arista en la sección de [datos de calibración](#calibration-data) de la tarjeta de información de la QPU.

### Error SX mediano {#median-sx-error}

[Fidelidad de puerta promedio](https://docs.quantum.ibm.com/api/qiskit/quantum_info#average_gate_fidelity) de la puerta √X (SX) obtenida a partir de benchmarking aleatorizado, medida simultáneamente en todos los qubits. La secuencia de benchmarking aleatorizado incluye puertas SX, ID y X, y se asume que sus errores son iguales.

### Error de lectura mediano {#median-readout-error}

Fidelidad de la operación de lectura. El error de lectura se mide preparando el qubit en el estado 0 (1) y midiendo la probabilidad de obtener una salida en el estado 1 (0). El valor reportado es el promedio de estos dos errores. La mediana se calcula sobre todos los qubits.

### T1 mediano (tiempo de relajación) {#median-t1-relaxation-time}

El tiempo T1 representa la duración promedio que un qubit permanece en su estado excitado $|1\rangle$ antes de decaer a su estado fundamental $|0\rangle$ debido a la relajación de energía. Este parámetro se utiliza para caracterizar el comportamiento de relajación de energía del qubit, y se expresa en unidades de segundos (s).

### T2 mediano (tiempo de desfasamiento) {#median-t2-dephasing-time}

El tiempo T2 denota la escala de tiempo en la que un qubit mantiene la coherencia de fase de una superposición entre los estados $|0\rangle$ y $|1\rangle$. Tiene en cuenta tanto la relajación de energía como los procesos de desfasamiento puro, proporcionando información sobre las propiedades de coherencia del qubit. T2 se reporta a partir de una secuencia de eco de Hahn.

## Datos de calibración {#calibration-data}

<Admonition type="note" title="¿Qué significa `error = 1`?">
Si el benchmarking de un qubit o arista no tiene éxito durante varios días, ya sea por mala calidad de los datos u otros factores internos, el valor de error reportado se considera obsoleto y se reportará como 1. Esto no indica que el qubit o la arista no funcionen necesariamente, ni que el error sea 1; más bien, el error se considera indefinido y debes proceder con cautela al operar ese qubit o puerta.
</Admonition>

La segunda sección, Datos de calibración, proporciona datos de qubits, conectividad y puertas. Puedes elegir visualizar la información como mapa, gráfico o tabla.

Puedes personalizar los datos que se muestran en cada vista usando los menús desplegables. Por ejemplo, en la vista de mapa, puedes elegir los datos que quieres ver para qubits y conexiones. Las barras de colores asociadas al diagrama o gráfico indican el rango mostrado, con el valor promedio marcado. El máximo y mínimo del color cambian dependiendo de la QPU.

Para descargar los datos de calibración como archivo CSV, haz clic en el ícono de descarga en la esquina superior derecha de la sección Datos de calibración.

Además de la información proporcionada en la sección Detalles de la tarjeta, la sección Datos de calibración también incluye lo siguiente:

[Diagrama de topología o mapa de acoplamiento](#coupling-map) | [Error de asignación de lectura](#readout-assignment-error) | [Prob meas0 prep1](#prob-meas0-prep1) | [Prob meas1 prep0](#prob-meas1-prep0) | [Longitud de lectura (ns)](#readout-length-ns) | [Error ID / error √x (sx) / error Pauli-X / error RX](#finite-duration-discrete) | [Longitud de puerta de un qubit (ns)](#single-qubit-gate-length-ns)| [Error de rotación en el eje Z (RZ)](#z-axis-rotation-rz-error) | [Operacional](#operational) | [Longitud de puerta (ns)](#gate-length-ns) | [Error 2Q](#2q-error) | [Error RZZ](#rzz-error-heron)

<span id="coupling-map"></span>
### Diagrama de topología o mapa de acoplamiento {#topology-diagram-or-coupling-map}

Un diagrama que indica los pares de qubits que admiten operaciones de puertas de dos qubits entre ellos. También se denomina mapa de acoplamiento o conectividad. Los qubits se representan como círculos y las operaciones de puertas de dos qubits admitidas se muestran como líneas que conectan los qubits.

### Error de asignación de lectura {#readout-assignment-error}

El error de lectura cuantifica la probabilidad promedio de medir incorrectamente el estado de un qubit. Generalmente se calcula como la media de prob_meas0_prep1 y prob_meas1_prep0, proporcionando una única métrica para la fidelidad de medición.

### Prob meas0 prep1 {#prob-meas0-prep1}

Este parámetro indica la probabilidad de medir un qubit en el estado $|0\rangle$ cuando se pretendía prepararlo en el estado $|1\rangle$, denotada como $P(0|1)$. Refleja errores en la preparación de estados y la medición (SPAM), en particular errores de medición en qubits superconductores.

### Prob meas1 prep0 {#prob-meas1-prep0}

De manera similar, este parámetro representa la probabilidad de medir un qubit en el estado $|1\rangle$ cuando se pretendía prepararlo en el estado $|0\rangle$, denotada como $P(1|0)$. Al igual que prob_meas0_prep1, refleja errores SPAM, siendo los errores de medición el contribuyente predominante en los qubits superconductores.

### Longitud de lectura (ns) {#readout-length-ns}

El readout_length especifica la duración de la operación de lectura para un qubit. Mide el tiempo desde el inicio del pulso de medición hasta la finalización de la digitalización de la señal, tras lo cual el sistema está listo para la siguiente operación. Comprender este parámetro es fundamental para optimizar la ejecución de circuitos, especialmente cuando se incorporan mediciones a mitad del circuito.

<span id="finite-duration-discrete"></span>
### Error ID / error √x (sx) / error Pauli-X / error RX {#id-error--x-sx-error--pauli-x-error--rx-error}

Error en las puertas discretas de un qubit de duración finita, medido a partir de benchmarking aleatorizado. La secuencia de benchmarking aleatorizado incluye puertas SX, ID y X, y se asume que sus errores son iguales. La puerta ID es un retardo de duración igual a la duración de las puertas √X y X. La puerta RX también tiene la misma duración que las puertas √X y X con amplitud variable, por lo que se reporta con el mismo error que estas puertas.

### Longitud de puerta de un qubit (ns) {#single-qubit-gate-length-ns}

Duración de una operación de puerta de un qubit.

### Error de rotación en el eje Z (RZ) {#z-axis-rotation-rz-error}

Error en la puerta RZ virtual. Se reporta como 0 en todos los casos, ya que estas operaciones se realizan por software.

### Operacional {#operational}

Indica si el qubit puede utilizarse en circuitos.

### Longitud de puerta (ns) {#gate-length-ns}

Duración de la operación de puerta de dos qubits.

<span id="2q-error"></span>
### Error 2Q (Heron: CZ, Eagle: ECR) {#2q-error-heron-cz-eagle-ecr}

El error 2Q por arista del mismo lote de mediciones utilizado para calcular los errores [mediano 2Q](#median-2q-error-heron-cz-eagle-ecr) y [mejor 2Q](#2q-error-best).

### Error RZZ (Heron) {#rzz-error-heron}

Error en la puerta RZZ promediado sobre los ángulos RZZ usando una variante de benchmarking aleatorizado para unitarios arbitrarios.

## Error de puerta de dos qubits (en capas) {#two-qubit-gate-error-layered}

La tercera sección proporciona la vista ampliada del menor `error de puerta de dos qubits (en capas)` medido en función del número de qubits en la cadena. El valor final, para una longitud de cadena de 100, es el valor presentado en la sección Detalles. En la práctica, se miden seis cadenas de 100 qubits (preseleccionadas en función del rendimiento óptimo esperado), y el valor reportado para el número de qubits `N` es el menor error encontrado en una subcadena de longitud `N` buscando entre las seis cadenas de 100 qubits.

<span id="available"></span>
## Ver tus recursos {#view-your-resources}

Para encontrar tus QPUs disponibles, abre la página [Compute resources](https://quantum.cloud.ibm.com/computers) (asegúrate de haber iniciado sesión). Ten en cuenta que la región seleccionada puede afectar a las QPUs listadas. Haz clic en una QPU para ver sus detalles.

También puedes ver tus QPUs disponibles usando la [API de backends.](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/qiskit-runtime-service#backends) Por ejemplo, el siguiente código devolverá todos los backends a los que la instancia especificada (`my_instance`) puede acceder:

```python
   QiskitRuntimeService(instance="my_instance_CRN")
   service.backends()
```

<span id="native-gates"></span>
## Tabla de puertas y operaciones nativas {#table-of-native-gates-and-operations}

| Categoría de operación |  Nombre |
| :--- | :--- |
| Puertas de un qubit | [`RZ`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.RZGate), [`SX`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.SXGate), [`X`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.XGate), [`ID`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.IGate), [`delay`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Delay) |
| Puertas de dos qubits | [`CZ`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.CZGate), [`ECR`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.ECRGate)|
| Puertas fraccionarias | [`RX`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.RXGate) (un qubit), [`RZZ`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.RZZGate) (dos qubits) |
| Instrucciones no unitarias | [`measure`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Measure), [`reset`](https://docs.quantum.ibm.com/api/qiskit/circuit#qiskit.circuit.Reset) |
| Flujo de control | [`if_else`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.IfElseOp) (retroalimentación clásica) |
