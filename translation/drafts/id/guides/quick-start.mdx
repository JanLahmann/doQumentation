---
title: "Quickstart"
sidebar_label: "Quickstart"
description: "Buat dan visualisasikan quantum circuit dalam dua menit, tanpa perlu login atau API key."
notebook_path: "guides/quick-start.ipynb"
---

{/* doqumentation-source-hash: 4dc6c1d3 */}

<OpenInLabBanner notebookPath="guides/quick-start.ipynb" />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';



{/* cspell:ignore miniconda */}

{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>Versi paket</b></summary>

Kode di halaman ini dikembangkan menggunakan persyaratan berikut.
Kami merekomendasikan menggunakan versi ini atau yang lebih baru.

```
qiskit[all]~=2.3.0
```
</details>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

Buat quantum circuit pertamamu dalam dua menit, di lingkungan lokal — tanpa perlu login atau API key.

<Admonition type="note" title="Baru mengenal Python dan virtual environment?">

* Unduh Python dan gunakan virtual environment dengan Qiskit (direkomendasikan).

<details>
<summary>Klik untuk melihat informasi lebih lanjut tentang **Python**.</summary>

- Untuk menginstal Python, cek dulu bagian "Programming Language" di [halaman proyek Qiskit di PyPI](https://pypi.org/project/qiskit/) untuk mengetahui versi Python mana yang didukung oleh rilis terbaru. Untuk instruksi unduhan, lihat [Panduan Python untuk Pemula.](https://wiki.python.org/moin/BeginnersGuide/Download)

<Admonition type="note">
Instruksi ini menggunakan distribusi Python standar dari [pypi.org](https://pypi.org/). Tapi kamu bisa juga menggunakan distribusi Python lain, seperti [Anaconda](https://docs.anaconda.com/anaconda/) atau [miniconda](https://docs.anaconda.com/miniconda/), beserta workflow manajemen dependensi lain seperti [Poetry](https://python-poetry.org/docs/).
</Admonition>
</details>

    <details>
    <summary>
    Klik untuk melihat informasi lebih lanjut tentang **virtual environment**.
    </summary>
    - Gunakan [Python virtual environment](https://docs.python.org/3.10/tutorial/venv.html) untuk memisahkan Qiskit dari aplikasi lain.
    Virtual environment Python adalah ruang terisolasi untuk bekerja dengan Python untuk tujuan tertentu — jadi kamu bisa menginstal paket apa pun yang diinginkan, serta mengatur library, dependensi, dan lain-lain, tanpa memengaruhi lingkungan Python "dasar" di mesinmu.

    Salah satu keunggulan penting dari virtual environment adalah jika lingkungan Python-mu rusak, kamu bisa dengan mudah menghapusnya dan mulai dari awal!

    Pilih lokasi yang kamu suka untuk menyimpan informasi tentang virtual environment-mu. Biasanya disimpan di direktori bernama `.venv` di dalam setiap direktori proyek.

    Untuk membuat virtual environment, navigasi ke direktori proyekmu dan buat lingkungan minimal yang hanya berisi Python.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

    Selanjutnya, aktifkan lingkungan barumu.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
              Jika menggunakan PowerShell:

             ```text
             .venv\Scripts\Activate.ps1
             ```
             Jika menggunakan Git Bash:

             ```text
             source .venv/scripts/activate
             ```
              Jika menggunakan command prompt:

             ```text
             .venv\Scripts\activate
             ```
           </TabItem>
         </OperatingSystemTabs>
    </details>

</Admonition>

## 1. Instal Qiskit {#1-install-qiskit}

Instal yang berikut ini dengan package manager pilihanmu (seperti `pip`):

- [`qiskit`](/guides/install-qiskit)
- [`matplotlib`](https://matplotlib.org/stable/users/explain/quick_start.html)
- [`qiskit[visualization]`](https://docs.quantum.ibm.com/api/qiskit/visualization)
## 2. Buat Circuit-mu {#2-build-your-circuit}

Buka lingkungan Python, lalu jalankan kode ini untuk membuat Bell state (dua qubit yang saling terjerat).

```python
from qiskit import QuantumCircuit
from qiskit.primitives import StatevectorSampler

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

sampler = StatevectorSampler()
result = sampler.run([qc], shots=1024).result()
print(result[0].data.meas.get_counts())
```

```text
{'11': 534, '00': 490}
```

Output yang diharapkan adalah pembagian yang hampir merata antara '00' dan '11'.
## 3. Visualisasikan Hasilmu {#3-visualize-your-results}

Untuk mendapatkan histogram dari hasilmu, tambahkan kode berikut ke programmu.

```python
# Uncomment lines 2 and 8 if you are not using Python in a Jupyter notebook
# import matplotlib.pyplot as plt
from qiskit.visualization import plot_histogram

counts = result[0].data.meas.get_counts()
plot_histogram(counts)

# plt.show()
```

![Output of the previous code cell](/docs/images/guides/quick-start/extracted-outputs/dc4ff012-0.svg)

Hasil ini adalah tanda dari quantum entanglement.

## 4. Lihat Apa yang Terjadi {#4-see-what-happens}

Coba ubah kode untuk melihat bagaimana pengaruhnya terhadap hasil. Misalnya:

- Tambahkan qubit ketiga dengan mengubah ke `QuantumCircuit(3)`, dan tambahkan CX gate kedua dengan `qc.cx(1,2)`. Pengukuran seharusnya berubah menjadi 000 dan 111, yang berarti ketiga qubit ini telah saling terjerat.

- Lihat hasilmu bergeser dengan menambahkan `qc.x(1)` di akhir Circuit.
## Langkah Selanjutnya {#next-steps}

<Admonition type="tip" title="Rekomendasi">
- Ikuti langkah-langkah di [Hello world](/guides/hello-world) untuk menjalankan Circuit di hardware quantum nyata.
- Belum siap menjalankan di hardware? Mulai perjalanan quantum-mu dengan kursus [Basics of quantum information](/learning/courses/basics-of-quantum-information).
</Admonition>
