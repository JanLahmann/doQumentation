---
title: "Pengantar Praktis Kriteria DiVincenzo dengan Qiskit 2"
sidebar_label: "Pengantar Praktis Kriteria DiVincenzo dengan Qiskit 2"
notebook_path: "workshop/06_divincenzo_criteria_lab-2.ipynb"
source_file: "06_divincenzo_criteria_lab-2.ipynb"
description: "Fisikawan David DiVincenzo menguraikan lima persyaratan utama untuk setiap implementasi fisik komputer kuantum, ditambah dua kriteria tambahan untuk komunikasi kuantum..."
---

{/* doqumentation-source-hash: baae5bd5 */}

<OpenInLabBanner notebookPath="workshop/06_divincenzo_criteria_lab-2.ipynb" />

## Pendahuluan

Fisikawan David DiVincenzo menguraikan lima persyaratan utama untuk setiap implementasi fisik komputer kuantum, ditambah dua kriteria tambahan untuk komunikasi kuantum. Di notebook ini, kita akan **merasakan langsung setiap kriteria DiVincenzo lewat demonstrasi Qiskit yang praktis**. Daripada masuk terlalu dalam ke teori, setiap bagian menjelaskan satu kriteria secara singkat lalu menyajikan latihan kode menggunakan Qiskit 2. Kamu akan menjalankan Circuit di simulator dan perangkat IBM Quantum sungguhan untuk **menjelajahi setiap prinsip secara langsung**.

**Lima Kriteria DiVincenzo untuk Komputasi Kuantum**:

1. **Sistem fisik yang dapat diskalakan dengan Qubit yang terkarakterisasi dengan baik.**
2. **Kemampuan menginisialisasi Qubit** ke keadaan fiducial sederhana (mis. |00…0〉).
3. **Waktu dekoheren yang panjang** (koherensi Qubit jauh lebih lama dari waktu operasi Gate).
4. **Sekumpulan Gate kuantum universal** (mampu melakukan operasi uniter sembarang).
5. **Kemampuan pengukuran spesifik per Qubit** (membaca keadaan setiap Qubit).

*(DiVincenzo juga mendeskripsikan dua kriteria untuk komunikasi kuantum: kemampuan menginterkonversi Qubit stasioner dan Qubit "terbang", serta mengirimkan Qubit terbang secara andal antar lokasi. Kita sertakan ini dalam aktivitas yang direkomendasikan di akhir notebook ini.)*

Setiap bagian berikut berkaitan dengan satu kriteria. Kita akan pakai Qiskit untuk mengilustrasikan konsepnya dengan kode dan **eksperimen interaktif** yang bisa kamu coba. Misalnya, kita akan melihat bagaimana menambah jumlah Qubit dan kedalaman Circuit memengaruhi hasil (Kriteria 1), cara mereset dan menyiapkan keadaan Qubit (Kriteria 2), cara mengukur Qubit di simulator vs perangkat nyata (Kriteria 4), bagaimana Qiskit menyusun Gate universal (Kriteria 3), dan bagaimana koherensi terbatas (T₁, T₂) berdampak pada komputasi (Kriteria 5). Di akhir, kamu akan punya intuisi yang lebih dalam tentang apa arti setiap kriteria DiVincenzo dalam praktik dan bagaimana Qiskit memungkinkan eksperimen dengannya.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy
```

```python
# Install necessary packages
!pip install qiskit[visualization] qiskit-ibm-runtime qiskit-aer qiskit_ibm_runtime
```

## 1. Kriteria 1 – Qubit yang Dapat Diskalakan dan Terkarakterisasi dengan Baik

**Kriteria 1:** *"Sistem fisik yang dapat diskalakan dengan Qubit yang terkarakterisasi dengan baik."* Ini berarti kita butuh platform hardware kuantum di mana kita bisa **menambah jumlah Qubit** dan tetap mengendalikannya secara andal. Setiap properti Qubit (tingkat energi, tingkat kesalahan, konektivitas, dll.) harus dipahami dengan baik. Intinya, kita ingin membangun Circuit yang lebih besar tanpa sistem menjadi kacau. Dalam praktiknya, saat kita menambah jumlah Qubit atau kedalaman Circuit, kesalahan dan dekoheren menumpuk, sehingga mendemonstrasikan *skalabilitas* juga berarti memahami bagaimana peningkatan ukuran memengaruhi performa.

**Tujuan Demo:** Gunakan Qiskit untuk menunjukkan efek penambahan skala Circuit (dalam jumlah Qubit atau kedalaman Gate) terhadap fidelitas output. Kita akan mensimulasikan skenario ideal vs berisik untuk melihat bagaimana sistem yang lebih besar atau Circuit yang lebih dalam terdampak dekoheren dan kesalahan.

Pertama, mari kita bangun keadaan terjalin kecil (keadaan GHZ) dengan 3 Qubit, lalu yang lebih besar dengan 5 Qubit, sebagai uji skalabilitas sederhana. Keadaan GHZ dengan *n* Qubit adalah $\frac{1}{\sqrt{2}}(|0...0\rangle + |1...1\rangle)$. Dalam simulasi ideal, mengukur GHZ n-Qubit hanya menghasilkan dua kemungkinan (semua 0 atau semua 1) dengan probabilitas sama. Kita akan membandingkan **output ideal** dengan **output berisik** saat kita menambah n atau kedalaman Circuit.

```python
from qiskit import QuantumCircuit
from qiskit_aer import AerSimulator
from qiskit.visualization import plot_histogram
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager
from qiskit_ibm_runtime import SamplerV2 as Sampler

# 3-qubit GHZ circuit
qc3 = QuantumCircuit(3, 3)
qc3.h(0)
qc3.cx(0, 1)
qc3.cx(1, 2)
qc3.measure([0, 1, 2], [0, 1, 2])

# 5-qubit GHZ circuit (scaling up the number of qubits)
qc5 = QuantumCircuit(5, 5)
qc5.h(0)
qc5.cx(0, range(1, 5))    # entangle qubit 0 with all others
qc5.measure(range(5), range(5))

# Transpile for a simulator backend
sim_backend = AerSimulator()
pm = generate_preset_pass_manager(backend=sim_backend, optimization_level=1)
isa_qc3 = pm.run(qc3)
isa_qc5 = pm.run(qc5)

# Run ideal simulations (no noise)
sampler = Sampler(mode=sim_backend)

job3 = sampler.run([isa_qc3], shots=1024)
result3 = job3.result()
counts3 = result3[0].data.c.get_counts()

job5 = sampler.run([isa_qc5], shots=1024)
result5 = job5.result()
counts5 = result5[0].data.c.get_counts()

print("3-qubit GHZ counts (ideal):", counts3)
plot_histogram(counts3, legend=['3-qubit ideal'], figsize=(6,4))
```

```text
3-qubit GHZ counts (ideal): {'000': 531, '111': 493}
```

![Quantum circuit diagram](./_06_divincenzo_criteria_lab-2_imgs/output_1.png)

```python
print("5-qubit GHZ counts (ideal):", counts5)
plot_histogram(counts5, legend=['5-qubit ideal'], figsize=(6,4))
```

```text
5-qubit GHZ counts (ideal): {'11111': 535, '00000': 489}
```

![Code output](./_06_divincenzo_criteria_lab-2_imgs/output_2.png)

**Hasil yang diharapkan (kasus ideal):** GHZ 3-Qubit secara ideal menghasilkan sekitar 50% `000` dan 50% `111` dalam hitungan. GHZ 5-Qubit menghasilkan ~50% `00000` dan 50% `11111`. Tidak ada bitstring lain yang muncul karena keadaannya secara ideal sepenuhnya koheren dan terjalin. Kamu seharusnya melihat dua batang tinggi di histogram untuk setiap Circuit yang bersesuaian dengan hasil semua-nol dan semua-satu.

Berikutnya, mari kita lihat apa yang terjadi di **lingkungan berisik**. Kita akan menggunakan kemampuan noise model Qiskit Aer untuk meniru kesalahan perangkat nyata. Misalnya, kita bisa mengambil properti Backend IBM untuk membuat noise model yang mencakup kesalahan Gate, waktu Gate terbatas, relaksasi Qubit (T₁), dephasing (T₂), dan kesalahan readout. Di sini, kita akan menggunakan **fake backend** yang merepresentasikan perangkat IBM Quantum Brisbane untuk menghasilkan noise model, dan menjalankan ulang Circuit GHZ melaluinya.
### Latihan 1a: Simulasi dengan Noise
Lengkapi kode di bawah ini untuk mensimulasikan Circuit GHZ pada simulator berisik berbasis Backend `FakeBrisbane`. Ini akan menunjukkan bagaimana performa menurun saat sistem diskalakan di lingkungan noise yang realistis.

```python
from qiskit_ibm_runtime.fake_provider import FakeBrisbane

# We will reuse the ideal circuits qc3 and qc5 and their results from the previous cell.

# --- YOUR CODE HERE ---

# 1. Create a fake backend for IBM Quantum Brisbane
###brisbane_backend = ...

# 2. Create a noisy AerSimulator from the fake backend's properties
###noisy_sim = ...

# 3. Transpile the circuits for the noisy simulator (this adapts them to the device's specific gates and connectivity)
###pm = ...

###isa_qc3_noisy = ...

###isa_qc5_noisy = ...

# 4. Run the noisy simulations using the Sampler and get the counts
###sampler = ...

###job3 = ...

###result3_noisy = ...

###counts3_noisy = ...

###job5 = ...

###result5_noisy = ...

###counts5_noisy = ...

# --- END YOUR CODE ---

# This part is done for you to print and plot the results:
print("3-qubit GHZ counts (noisy):", counts3_noisy)
plot_histogram(counts3_noisy, legend=['3-qubit noisy'], figsize=(6,4))
```

```python
print("5-qubit GHZ counts (noisy):", counts5_noisy)
plot_histogram(counts5_noisy, legend=['5-qubit noisy'], figsize=(6,4))
```

### Latihan 1b: Jalankan di komputer IBM Quantum sungguhan
Kode di bawah ini menjalankan Circuit GHZ di komputer IBM Quantum sungguhan. Ini akan menunjukkan bagaimana performa menurun di perangkat nyata.

```python
# your_api_key = "deleteThisAndPasteYourAPIKeyHere"
# your_crn = "deleteThisAndPasteYourCRNHere"

# QiskitRuntimeService.save_account(
#     channel="ibm_quantum_platform",
#     token=your_api_key,
#     instance=your_crn,
#     name="fallfest-2025",
# )

# Check that the account has been saved properly
# service = QiskitRuntimeService(name="fallfest-2025")
# print(service.saved_accounts())

# We will reuse the ideal circuits qc3 and qc5 and their results from the previous cell.

from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService(name="fallfest-2025")
real_backend = service.least_busy(operational=True, simulator=False)
print("Running on " + real_backend.name)

pm = generate_preset_pass_manager(backend=real_backend, optimization_level=1)
isa_qc3r = pm.run(qc3)
isa_qc5r = pm.run(qc5)

sampler = Sampler(mode=real_backend)

job3r = sampler.run([isa_qc3r], shots=1024)
result3r = job3r.result()
counts3r = result3r[0].data.c.get_counts()

job5r = sampler.run([isa_qc5r], shots=1024)
result5r = job5r.result()
counts5r = result5r[0].data.c.get_counts()

print("3-qubit GHZ counts (real):", counts3r)
plot_histogram(counts3r, legend=['3-qubit real'], figsize=(6,4))
```

```python
print("5-qubit GHZ counts (real):", counts5r)
plot_histogram(counts5r, legend=['5-qubit real'], figsize=(6,4))
```

**Hasil yang diharapkan (berisik vs ideal):** Dengan noise, baik disimulasikan maupun di perangkat nyata, keadaan GHZ **tidak sempurna**. Kamu akan melihat hasil tambahan di luar semua-0 dan semua-1. Untuk 3 Qubit, alih-alih 100% di `000`/`111`, sebagian probabilitas bocor ke bitstring lain (mis. `001`, `010`, dll.) akibat kesalahan Gate atau dekoheren yang membalik beberapa Qubit. Untuk 5 Qubit, efeknya bahkan lebih terasa; Circuit yang lebih besar (lebih banyak Qubit dan Gate CNOT) mengakumulasi lebih banyak kesalahan, sehingga puncak semua-0 dan semua-1 lebih rendah, dan banyak hasil lain muncul. Tren ini mengilustrasikan tantangan *skalabilitas*: saat kita menambah skala, mempertahankan fidelitas tinggi semakin sulit tanpa koreksi kesalahan.

**Wawasan:** Komputer kuantum yang dapat diskalakan perlu mempertahankan korelasi kuantum saat sistem berkembang. Contoh kita menunjukkan bagaimana menambah jumlah Qubit/kedalaman Gate menyebabkan fidelitas hasil menurun saat noise hadir. Kriteria-kriteria berikutnya akan membahas cara menjaga Qubit tetap berperilaku baik (kesalahan rendah, dapat diinisialisasi, dll.) saat kita menambah skala.
## 2. Kriteria 2 – Inisialisasi Qubit

**Kriteria 2:** *"Kemampuan untuk menginisialisasi keadaan qubit ke sebuah fiducial state yang sederhana, seperti |000…〉."* Semua Qubit harus bisa dimulai secara andal dari state referensi yang sudah diketahui (biasanya ground state |0〉 untuk setiap Qubit). Inisialisasi ini penting supaya algoritma bisa mulai dari kondisi yang bersih. Dalam praktiknya, pada perangkat IBM Quantum setiap Qubit secara otomatis direset ke |0〉 setiap kali sirkuit dijalankan. Qiskit juga menyediakan instruksi untuk mereset Qubit atau menyiapkan state kustom di tengah komputasi.

**Tujuan Demo:** Memperlihatkan cara menginisialisasi Qubit di Qiskit, baik di awal maupun di tengah Circuit. Kita akan demonstrasikan penggunaan instruksi `reset` dan metode persiapan state.
### Latihan 2: Siapkan State Tertentu
Di blok kode di bawah ini, lengkapi `QuantumCircuit` untuk menyiapkan state $|10\rangle$. Artinya Qubit 0 harus berada di state $|0\rangle$ dan Qubit 1 di state $|1\rangle$. Gunakan gate dan instruksi yang tepat untuk mencapai ini.

```python
from qiskit import QuantumCircuit
from qiskit_aer import AerSimulator

# Create a circuit to initialize qubits to |10> and verify by measurement
qc_init = QuantumCircuit(2, 2)

# --- YOUR CODE HERE ---

# 1. Set qubit 1 to the |1> state

# 2. Explicitly reset qubit 0 to the |0> state

# --- END YOUR CODE ---

qc_init.measure([0, 1], [0, 1])
qc_init.draw('mpl')
```

```python
# Run the circuit and check the outcome
sim_backend = AerSimulator()
pm = generate_preset_pass_manager(backend=sim_backend, optimization_level=1)
isa_qc_init = pm.run(qc_init)

sampler = Sampler(mode=sim_backend)

job = sampler.run([isa_qc_init], shots=1024)
result = job.result()
counts = result[0].data.c.get_counts()

print("Outcome of |10> state measured in Z-basis:", counts)
plot_histogram(counts)
```

Kamu harusnya melihat `10` (biner untuk qubit1=1, qubit0=0) dengan probabilitas 100% dari simulasi, yang berarti Qubit 1 berhasil disiapkan di |1〉 dan Qubit 0 di |0〉.

Sekarang, untuk persiapan state yang lebih umum, Qiskit memungkinkan inisialisasi ke state sembarang menggunakan metode `initialize`. Misalnya, kita siapkan sebuah Qubit dalam state $|+\rangle = (|0\rangle+|1\rangle)/\sqrt{2}$, yang merupakan state superposisi, dan sepasang Qubit dalam Bell state $(|00\rangle+|11\rangle)/\sqrt{2}$:

```python
import numpy as np

# Initialize a single qubit in |+> state and measure in Z-basis
qc_plus = QuantumCircuit(1, 1)
state_plus = [1/np.sqrt(2), 1/np.sqrt(2)]   # amplitude for |0> and |1>
qc_plus.initialize(state_plus, 0)
qc_plus.measure(0, 0)

# Initialize two qubits in a Bell state manually
qc_bell = QuantumCircuit(2, 2)
bell_state = [1/np.sqrt(2), 0, 0, 1/np.sqrt(2)]  # amplitudes for |00>,|01>,|10>,|11>
qc_bell.initialize(bell_state, [0, 1])
qc_bell.measure([0, 1], [0, 1])

# Transpile and run the initialization circuits
isa_qc_plus = pm.run(qc_plus)
job_plus = sampler.run([isa_qc_plus], shots=1024)
result_plus = job_plus.result()
counts_plus = result_plus[0].data.c.get_counts()

print("Outcome of |+> state measured in Z-basis:", counts_plus)

isa_qc_bell = pm.run(qc_bell)
job_bell = sampler.run([isa_qc_bell], shots=1024)
result_bell = job_bell.result()
counts_bell = result_bell[0].data.c.get_counts()

print("Outcome of Bell state measured in Z-basis:", counts_bell)
```

```text
Outcome of |+> state measured in Z-basis: {'1': 499, '0': 525}
Outcome of Bell state measured in Z-basis: {'00': 508, '11': 516}
```

**Hasil yang diharapkan:** State |+〉 pada satu Qubit, saat diukur, akan menghasilkan `0` dan `1` dengan probabilitas masing-masing sekitar 50%. Pengukuran Bell state seharusnya menghasilkan sekitar 50% `00` dan 50% `11`. Kalau kamu melihat hasil ini, berarti inisialisasi ke state-state tersebut berhasil.

**Inisialisasi di tengah Circuit:** `reset` milik Qiskit bisa dipakai di tengah Circuit untuk menginisialisasi ulang sebuah Qubit ke |0〉 secara langsung. Misalnya, dalam kode koreksi error atau algoritma iteratif, kita sering mengukur sebuah Qubit lalu meresatnya untuk digunakan kembali. Operasi `reset` bersifat deterministik; ia membuang state yang ada dan mendinginkan Qubit kembali ke ground state.

**Contoh perangkat:** Pada hardware seperti **ibmq_brisbane** (127 Qubit) atau perangkat IBM lainnya, semua Qubit dimulai di |0〉 secara default saat sebuah job dijalankan. Kalau kamu butuh state awal yang berbeda, kamu tinggal menerapkan gate di awal Circuit (seperti yang kita lakukan dengan X untuk mendapat |1〉). Inisialisasi ulang secara terus-menerus (untuk koreksi error kuantum) adalah topik penelitian aktif karena melakukannya dengan cepat merupakan tantangan tersendiri. Untungnya, untuk penggunaan dasar, kemampuan untuk mulai dari kondisi segar di |0…0〉 sudah tersedia dan kita sudah mendemonstrasikan cara mencapai state awal lain yang diinginkan.
## 3. Kriteria 3 – Waktu Koherensi yang Panjang (Dekoherensi vs Waktu Gate)

**Kriteria 3:** *"Waktu dekoherensi yang relevan, jauh lebih panjang dari waktu operasi gate."* Ini menyangkut kebutuhan agar Qubit bisa mempertahankan state kuantumnya cukup lama untuk melakukan operasi yang diperlukan. Setiap Qubit memiliki **waktu T₁** (waktu relaksasi energi, seberapa cepat |1〉 meluruh ke |0〉) dan **waktu T₂** (waktu dephasing, seberapa cepat koherensi fase relatif hilang). Agar komputer kuantum bisa bekerja, skala waktu ini harus jauh melebihi durasi operasi gate.

**Tujuan Demo:** Menyelidiki koherensi Qubit di Qiskit dengan menunjukkan bagaimana dekoherensi memengaruhi hasil Circuit seiring bertambahnya panjang eksekusi. Kita akan menggunakan fake Backend dengan nilai T1/T2 yang diketahui untuk mensimulasikan efek ini.

Untuk **mendemonstrasikan dampak koherensi yang terbatas**, kita akan mensimulasikan eksperimen peluruhan T1. Kita akan menyiapkan sebuah Qubit dalam state |1〉, menunggu beberapa waktu menggunakan instruksi `delay`, lalu mengukurnya. Kita berharap probabilitas mengukur |1〉 akan menurun seiring bertambahnya delay.

```python
# This part is done for you. We are creating a list of circuits,
# each with a different delay time.

time_delays_ns = [0, 50000, 100000, 150000, 200000, 250000, 300000]  # delay durations in ns

decay_expts = []
for delay in time_delays_ns:
    qc = QuantumCircuit(1, 1)
    qc.x(0)  # initialize qubit to |1>
    if delay > 0:
        qc.delay(delay, 0, unit='ns')  # wait 'delay' nanoseconds
    qc.measure(0, 0)
    decay_expts.append(qc)

decay_expts[1].draw('mpl') # Visualize one of the circuits
```

![Quantum circuit diagram](./_06_divincenzo_criteria_lab-2_imgs/output_3.png)

### Latihan 3: Simulasikan Eksperimen Peluruhan T1

Sekarang, gunakan simulator berisik berdasarkan `FakeVigo` (yang punya waktu T1 sekitar 50–100 µs) untuk menjalankan Circuit-Circuit ini. Simulator akan secara otomatis menerapkan error T1/T2 selama instruksi `delay`. Transpile Circuit-Circuit tersebut untuk Backend ini dan jalankan.

```python
from qiskit_ibm_runtime.fake_provider import FakeVigoV2 as FakeVigo
from qiskit_aer import AerSimulator

# --- YOUR CODE HERE ---

# 1. Create a noisy simulator from the FakeVigo backend
###sim_vigo = ...

# 2. Transpile the list of circuits for this simulator
###pm = ...

###isa_decay_expts = ...

# 3. Use the Sampler to run all the transpiled circuits in a single job
###sampler = ...

###job = ...

###result = ...

# --- END YOUR CODE ---

# This part is done for you to analyze and print the results.
for idx, (delay, qc) in enumerate(zip(time_delays_ns, isa_decay_expts)):
    counts = result[idx].data.c.get_counts()
    p1 = counts.get('1', 0) / 1000  # Assuming 1000 shots
    print(f"Delay {delay} ns: P(qubit=1) = {p1:.3f}")
```

## 4. Kriteria 4 – Kumpulan Gate Kuantum yang Universal

**Kriteria 4:** *"Sebuah kumpulan gate kuantum yang 'universal'."* Ini berarti hardware kita harus memungkinkan kita melakukan *sembarang* komputasi kuantum dengan mengomposisi sekumpulan gate dasar yang terbatas. Dalam komputasi klasik, NAND bersifat universal; dalam komputasi kuantum, ada banyak pilihan kumpulan gate universal (mis. \{H, T, CNOT\} atau gate-gate native suatu mesin tertentu). Perangkat IBM, misalnya, memiliki sekumpulan operasi native seperti rotasi single-Qubit sembarang dan CNOT antara Qubit-Qubit tertentu, yang bersama-sama bersifat universal. Tugas Qiskit sering kali adalah **mengompilasi gate-gate tingkat tinggi ke dalam gate-gate basis ini**.

**Tujuan Demo:** Mengilustrasikan universalitas gate dengan menunjukkan bagaimana Qiskit mengurai gate-gate. Kita akan mengambil sebuah gate non-native (seperti Toffoli Gate 3-Qubit, CCX) dan melihat bagaimana ia dipecah ke gate-gate basis perangkat. Ini membuktikan bahwa kumpulan gate yang tersedia memang *universal* – ia bisa menghasilkan operasi yang lebih kompleks.

Pertama, kita lihat dulu apa saja gate basis untuk Backend IBM yang umum. Kita akan mengkueri konfigurasi sebuah perangkat (atau versi fake-nya). Misalnya, gate basis ibmq_brisbane:
Kamu seharusnya mengamati probabilitas `P(qubit=1)` yang menurun seiring bertambahnya waktu delay, mengikuti kurva peluruhan eksponensial yang merupakan ciri khas relaksasi T1. Ini secara langsung mendemonstrasikan bagaimana waktu koherensi yang terbatas menyebabkan error komputasi jika Circuit berjalan terlalu lama.

**Dampak pada algoritma:** Kalau kamu mencoba algoritma yang lebih panjang (dengan banyak gate berurutan), total waktu eksekusi mungkin mendekati atau melebihi T2, menyebabkan state kehilangan koherensi sebelum selesai. Itulah kenapa meningkatkan waktu koherensi dan membuat gate lebih cepat adalah dua tujuan paling kritis dalam penelitian hardware kuantum.

```python
from qiskit_ibm_runtime.fake_provider import FakeBrisbane
fake_brisbane = FakeBrisbane()
print("Basis gates for ibmq_brisbane:", fake_brisbane.configuration().basis_gates)
```

```text
Basis gates for ibmq_brisbane: ['ecr', 'id', 'rz', 'sx', 'x']
```

Ini mungkin menampilkan sesuatu seperti `['id', 'rz', 'sx', 'x', 'ecr']`. Itulah operasi primitif yang didukung hardware secara native (Identity/no-op, rotasi RZ, Gate sqrt(X), Gate X, dan controlled-X). Gate lain apapun harus dikomposisi dari ini. Kumpulan ini diketahui bersifat universal untuk komputasi kuantum (pada dasarnya rotasi single-Qubit ditambah gate dua-Qubit yang menghasilkan entanglement membentuk kumpulan yang universal).

Sekarang, ambil **Gate Toffoli (CCX)** sebagai kasus uji. CCX membalik Qubit target hanya jika dua Qubit kontrol keduanya bernilai 1. Ini bukan gate native pada hardware IBM. Qiskit menyediakan instruksi `ccx`, tapi di balik layar ia akan mengurainya.
### Latihan 4: Urai Gate Toffoli
Lengkapi kode di bawah ini untuk membangun Circuit dengan Gate Toffoli (CCX) lalu gunakan Qiskit untuk memecahnya ke dalam gate-gate basis native Backend `FakeBrisbane`.

```python
from qiskit import QuantumCircuit
from qiskit_ibm_runtime.fake_provider import FakeBrisbane

# The fake_brisbane backend from the previous cell is reused here.

# --- YOUR CODE HERE ---

# 1. Create a circuit that can accommodate a Toffoli gate
###qc_toffoli = ...

# Apply a CCX gate with controls on qubits 0, 1 and target on qubit 2

# 2. Transpile the circuit to the fake Brisbane backend
###pm = ...

###isa_qc_toffoli = ...

# --- END YOUR CODE ---

print("Toffoli circuit before decomposition:")
print(qc_toffoli)

print("\nToffoli circuit after transpiling to Brisbane basis:")
# The .draw() method will now show the decomposed circuit
print(isa_qc_toffoli.draw(fold=120))
```

Pada output hasil transpilasi, kamu seharusnya melihat CCX digantikan oleh rangkaian gate yang lebih dasar seperti `rz`, `sx`, dan `ecr`. Ini membuktikan bahwa gate-gate native sudah cukup untuk mengekspresikan Toffoli.

**Universalitas dalam praktik:** Latihan di atas menunjukkan bahwa sebuah gate kompleks 3-Qubit dibangun dari gate-gate yang lebih sederhana. Secara umum, **sembarang** unitary multi-Qubit bisa dikomposisi dari gate 1- dan 2-Qubit. Transpiler adalah komponen krusial dari setiap software stack kuantum, karena ia menjembatani kesenjangan antara algoritma abstrak yang ingin kita jalankan dan operasi fisik yang bisa benar-benar dilakukan oleh perangkat kuantum tertentu.

**Contoh perangkat:** Perangkat **ibmq_brisbane** menggunakan arsitektur Eagle dengan gate basis seperti yang ditunjukkan di atas. Artinya setiap algoritma yang dikirim ke mesin-mesin itu akan dikonversi menjadi rangkaian operasi-operasi tersebut. Kriteria ini pada dasarnya tentang **kontrol**; kita punya cukup tombol kontrol untuk melakukan operasi apapun yang dibutuhkan pada Qubit-Qubit kita.
## 5. Kriteria 5 – Pengukuran Qubit

**Kriteria 5:** *"Kemampuan pengukuran yang spesifik per Qubit."* Setiap state Qubit harus bisa diukur (biasanya dalam basis komputasi, |0〉 atau |1〉). Dengan kata lain, setelah menjalankan Circuit kuantum, kita perlu membaca setiap Qubit sebagai bit klasik 0/1. Kriteria ini tentang memiliki detektor yang andal untuk setiap Qubit dan kemampuan memilih Qubit mana yang akan diukur.

**Tujuan Demo:** Menunjukkan cara melakukan pengukuran di Qiskit pada simulator dan perangkat nyata, serta menyoroti perbedaannya (seperti noise pengukuran). Kita akan mengukur beberapa Qubit dalam berbagai state dan memeriksa hasilnya. Kita juga akan mendemonstrasikan bagaimana error readout mungkin muncul dengan membandingkan hasil simulator vs hardware.

Pertama, contoh pengukuran sederhana:

```python
qc_measure = QuantumCircuit(2, 2)
qc_measure.x(0)              # qubit 0 -> |1>, qubit 1 stays |0>
qc_measure.measure([0, 1], [0, 1])
qc_measure.draw('mpl')
```

![Quantum circuit diagram](./_06_divincenzo_criteria_lab-2_imgs/output_4.png)

```python
sim_backend = AerSimulator()
pm = generate_preset_pass_manager(backend=sim_backend, optimization_level=1)
isa_qc_measure = pm.run(qc_measure)
job = sampler.run([isa_qc_measure], shots=1000)
result = job.result()
counts = result[0].data.c.get_counts()

print("Simulator measurement counts:", counts)
```

```text
Simulator measurement counts: {'01': 1000}
```

Kita mengharapkan 1000 hitungan `01` pada simulator. Sekarang, kita lihat **error pengukuran** secara langsung dengan mensimulasikannya. Kita bisa menambahkan readout error ke simulator Aer kita. Qiskit Aer memungkinkan kita mendefinisikan `ReadoutError` dan melampirkannya ke Qubit-Qubit dalam sebuah noise model.
### Latihan 5: Simulasikan Readout Error
Lengkapi kode untuk mendefinisikan model readout error sederhana di mana setiap Qubit punya peluang 2% diukur dengan salah (0 terbaca sebagai 1, atau 1 terbaca sebagai 0). Kemudian, jalankan Circuit pengukuran dengan noise model ini.

```python
from qiskit_aer.noise import NoiseModel, ReadoutError

# --- YOUR CODE HERE ---

# 1. Define a 2% readout error for each single qubit.
# The format is a list of lists of probabilities: [[P(0|0), P(1|0)], [P(0|1), P(1|1)]]
# P(A|B) is the probability of measuring A given the state was |B>.
###ro_error = ...

# 2. Create a new noise model
###noise_model_ro = ...

# 3. Add the readout error to all qubits in the noise model
... # Hint: Use the add_all_qubit_readout_error method

# --- END YOUR CODE ---

sim_backend.set_options(noise_model=noise_model_ro)
pm = generate_preset_pass_manager(backend=sim_backend, optimization_level=1)
isa_qc_measure = pm.run(qc_measure)

# Run the measurement circuit with readout noise
sampler = Sampler(mode=sim_backend)

job = sampler.run([isa_qc_measure], shots=1024)
result = job.result()
counts = result[0].data.c.get_counts()

print("Simulation with 2% readout error:", counts)
```

Output simulasi ini akan menampilkan beberapa hitungan yang salah (seperti `11`, `00`, `10`) mirip dengan yang mungkin dihasilkan hardware nyata, yang mendemonstrasikan dampak pengukuran yang tidak sempurna.

**Contoh perangkat:** Pada perangkat nyata seperti **ibmq_brisbane**, kamu bisa menjalankan Circuit yang sama dan kemungkinan akan melihat hitungan non-nol yang serupa untuk hasil yang salah. Data kalibrasi perangkat mencantumkan readout error untuk setiap Qubit. Kemampuan untuk menarget dan membaca Qubit tertentu sangat penting, dan memahami karakteristik errornya adalah kunci untuk mendapatkan hasil yang bermakna. Menjalankan pada perangkat nyata sudah didemonstrasikan di **Latihan 1b: Jalankan di komputer IBM Quantum nyata**.
## Kriteria Komunikasi Kuantum (Flying Qubit)

DiVincenzo juga mencantumkan dua kriteria khusus untuk komunikasi kuantum, yang penting jika membangun komputer kuantum berbasis jaringan:

6. **Kemampuan untuk saling mengkonversi antara stationary Qubit dan flying Qubit.** (Mis., memetakan Qubit di prosesor ke foton yang bisa berpindah.)
7. **Kemampuan untuk mengirimkan flying Qubit antar lokasi secara andal.** (Mis., mengirim Qubit foton melalui serat tanpa kehilangan informasi kuantum.)

Ini melampaui penggunaan Qiskit standar karena Qiskit terutama menangani Qubit stasioner di chip. Namun, kita bisa mengilustrasikan *konsep* dari kriteria-kriteria ini dengan contoh sederhana: **teleportasi kuantum**. Teleportasi menunjukkan konversi state Qubit stasioner menjadi informasi yang dibawa oleh pasangan entangled (bagian "flying") dan komunikasi klasik, yang kemudian digunakan untuk merekonstruksi state pada Qubit stasioner lain di tempat lain.
### Aktivitas yang Direkomendasikan: Ikuti modul *Quantum Teleportation* dari Qiskit in Classrooms

Modul [Quantum Teleportation](https://quantum.cloud.ibm.com/learning/en/modules/computer-science/quantum-teleportation) dari Qiskit in Classrooms oleh Dr. Katie McCormick akan membimbingmu melalui salah satu protokol paling menarik dalam informasi kuantum: teleportasi kuantum, di mana sebuah state kuantum (sebuah Qubit) dikirim dari Alice ke Bob menggunakan entanglement dan hanya dua bit klasik. Kamu akan mempelajari prosedur teleportasi lengkap langkah demi langkah—cara menyiapkan pasangan Bell yang terentangled, melakukan pengukuran dalam basis Bell di sisi Alice, mengirimkan hasil klasik, dan menerapkan Gate kuantum yang tepat pada Qubit Bob untuk memulihkan state aslinya secara sempurna. Sepanjang jalan, kamu akan mengeksplorasi mengapa menteleportasikan informasi sebuah Qubit tidak melanggar teorema no-cloning atau melampaui kecepatan cahaya. Melalui latihan langsung menggunakan hardware IBM Quantum atau simulator, kamu akan mendapat pemahaman praktis tentang pengukuran, entanglement, dan feed-forward control dalam aksi.

Dengan menguasai teleportasi kuantum, kamu akan memahami cara mengkodekan, mengirimkan, dan memulihkan informasi kuantum antara node-node yang berbeda—meletakkan fondasi untuk jaringan kuantum, sistem repeater, skema komunikasi aman, dan komputasi kuantum modular yang skalabel.
**Kaitannya dengan kriteria 6 & 7:** Dalam jaringan kuantum nyata, pasangan entangled yang dibagikan akan dibuat dengan mendistribusikan "flying" Qubit (seperti foton) antara lokasi Alice dan Bob (Kriteria 7: transmisi yang andal). Protokol teleportasi itu sendiri kemudian berfungsi sebagai cara untuk memetakan state Qubit stasioner Alice ke setengah dari pasangan entangled-nya, secara efektif 'mengirimkannya' ke Bob (Kriteria 6: interkonversi). Qiskit memungkinkan kita mensimulasikan logika protokol dengan sempurna, memberikan model konseptual tentang bagaimana kriteria-kriteria ini dipenuhi dalam arsitektur komunikasi.
## Kesimpulan & Ringkasan

Kita telah merancang serangkaian latihan berbasis kode untuk mengilustrasikan kriteria DiVincenzo menggunakan Qiskit. Melalui contoh-contoh langsung ini, kamu telah mengeksplorasi bagaimana platform komputasi kuantum nyata memenuhi setiap persyaratan:

- **Skalabilitas**: membangun Circuit pada lebih banyak Qubit dan memahami scaling noise.
- **Inisialisasi**: menggunakan reset dan persiapan state untuk memulai komputasi secara andal dari state yang diketahui.
- **Gate Universal**: mentranspilasi operasi kompleks ke gate-gate basis mesin, membuktikan kita bisa melakukan komputasi apapun.
- **Pengukuran**: membaca Qubit dan menangani readout error yang realistis.
- **Koherensi**: melihat efek T₁, T₂ yang terbatas pada fidelitas algoritma dan kebutuhan agar operasi berlangsung cepat relatif terhadap dekoherensi.

Sebagai pelengkap, kita juga menyentuh aspek komunikasi kuantum melalui modul [Quantum Teleportation](https://quantum.cloud.ibm.com/learning/en/modules/computer-science/quantum-teleportation) dari Qiskit in Classrooms, yang menghubungkan dua kriteria terakhir (flying Qubit).

Terakhir, perlu dicatat bagaimana kriteria-kriteria ini berpadu dalam komputer kuantum nyata seperti milik IBM. Perangkat seperti **ibmq_brisbane** memiliki 127 Qubit superkonduktor (Kriteria 1), masing-masing dimulai di |0〉 (Kriteria 2), dengan kumpulan gate terkalibrasi dan kompiler untuk universalitas (Kriteria 4), resonator readout microwave untuk setiap Qubit (Kriteria 5), dan waktu koherensi dalam orde ratusan mikrodetik vs operasi dalam orde nanodetik (Kriteria 3). Untuk eksperimen jaringan kuantum, IBM dan lainnya sedang mengeksplorasi transduksi microwave-ke-optik untuk flying Qubit, dan mengentangled Qubit-Qubit yang jauh (Kriteria 6 & 7); itulah area penelitian yang aktif.

Dengan menyelesaikan latihan-latihan dalam notebook ini, kamu tidak hanya melihat definisi dari kriteria DiVincenzo, tapi *menyentuhnya* melalui kode; membangun intuisi tentang apa arti setiap persyaratan untuk hardware dan algoritma kuantum nyata. Jangan ragu untuk memperluas eksperimen-eksperimen ini, dan selamat komputasi kuantum!
