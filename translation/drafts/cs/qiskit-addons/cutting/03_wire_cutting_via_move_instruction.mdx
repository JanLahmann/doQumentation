---
title: "Řezání drátů formulované jako dvouqubitová instrukce `Move`"
sidebar_label: "Řezání drátů formulované jako dvouqubitová instrukce `Move`"
notebook_path: "qiskit-addons/cutting/03_wire_cutting_via_move_instruction.ipynb"
---
{/* doqumentation-source-hash: 781aa50d */}

import TutorialFeedback from '@site/src/components/TutorialFeedback';

<OpenInLabBanner notebookPath="qiskit-addons/cutting/03_wire_cutting_via_move_instruction.ipynb" />


V tomto tutoriálu rekonstruujeme očekávané hodnoty sedmiqubitového Circuit tím, že ho rozdělíme na dva čtyřqubitové Circuit pomocí řezání drátů.

Toto jsou kroky, které provedeme v rámci tohoto [Qiskit vzoru](https://quantum.cloud.ibm.com/docs/guides/intro-to-patterns):

- **Krok 1: Mapování problému na kvantové Circuit a operátory**:
    - Mapování hamiltoniánu na kvantový Circuit.
- **Krok 2: Optimalizace pro cílový hardware** [_Používá cutting addon_]:
    - <font color='#0F62FE'>Řezání Circuit a observablu.</font>
    - Transpilace dílčích experimentů pro hardware.
- **Krok 3: Spuštění na cílovém hardware**:
    - Spuštění dílčích experimentů získaných v Kroku 2 pomocí primitivu `Sampler`.
- **Krok 4: Následné zpracování výsledků** [_Používá cutting addon_]:
    - <font color='#0F62FE'>Kombinace výsledků z Kroku 3 za účelem rekonstrukce očekávané hodnoty sledovaného observablu.</font>
## Krok 1: Mapování {#krok-1-mapování}

### Vytvoření Circuit k řezání {#vytvoření-circuit-k-řezání}

Nejprve začneme s Circuit inspirovaným obr. 1(a) z [arXiv:2302.03366v1](https://arxiv.org/abs/2302.03366v1).

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-addon-cutting qiskit-aer qiskit-ibm-runtime
```

```python
import numpy as np
from qiskit import QuantumCircuit

qc_0 = QuantumCircuit(7)
for i in range(7):
    qc_0.rx(np.pi / 4, i)
qc_0.cx(0, 3)
qc_0.cx(1, 3)
qc_0.cx(2, 3)
qc_0.cx(3, 4)
qc_0.cx(3, 5)
qc_0.cx(3, 6)
qc_0.cx(0, 3)
qc_0.cx(1, 3)
qc_0.cx(2, 3)
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7f16ab191a80>
```

```python
qc_0.draw("mpl")
```

![Quantum circuit diagram](./output_1.png)

### Zadání observablu {#zadání-observablu}

```python
from qiskit.quantum_info import SparsePauliOp

observable = SparsePauliOp(["ZIIIIII", "IIIZIII", "IIIIIIZ"])
```

## Krok 2: Optimalizace {#krok-2-optimalizace}

### Vytvoření nového Circuit s instrukcemi `Move` umístěnými na požadovaných místech řezu {#vytvoření-nového-circuit-s-instrukcemi-move-umístěnými-na-požadovaných-místech-řezu}

S ohledem na výše uvedený Circuit chceme umístit dva řezy drátů na prostřední qubitovou linku, aby se Circuit mohl rozdělit na dva Circuit po čtyřech Qubitech. Jedním ze způsobů, jak toho dosáhnout, je ručně umístit dvouqubitové instrukce `Move`, které přesouvají stav z jednoho qubitového drátu na jiný. Instrukce `Move` je konceptuálně ekvivalentní operaci reset na druhém Qubitu, za níž následuje Gate SWAP. Efektem této instrukce je přenos stavu prvního (zdrojového) Qubitu na druhý (cílový) Qubit, přičemž příchozí stav druhého Qubitu je zahozen. Aby toto fungovalo podle záměru, je důležité, aby druhý (cílový) Qubit nesdílel žádné provázání se zbytkem systému; v opačném případě způsobí operace reset částečný kolaps stavu zbytku systému.

Zde sestavíme nový Circuit s jedním dodatečným Qubitem a operacemi `Move` na svém místě. V tomto příkladu jsme schopni znovu použít Qubit: zdrojový Qubit prvního `Move` se stane cílovým Qubitem druhé operace `Move`.

Poznámka: Jako alternativu k přímé práci s instrukcemi `Move` lze zvolit označení řezů drátů pomocí jednoqubitové instrukce `CutWire`. Funkce `cut_wires` existuje pro transformaci instrukcí `CutWire` na instrukce `Move` na nově přidělených Qubitech. Na rozdíl od ruční metody však tato automatická metoda neumožňuje opětovné použití qubitových drátů. Podrobnosti najdeš v [návodu](../how-tos/how_to_specify_cut_wires.ipynb) pro `CutWire`.

```python
from qiskit_addon_cutting.instructions import Move

qc_1 = QuantumCircuit(8)
for i in [*range(4), *range(5, 8)]:
    qc_1.rx(np.pi / 4, i)
qc_1.cx(0, 3)
qc_1.cx(1, 3)
qc_1.cx(2, 3)
qc_1.append(Move(), [3, 4])
qc_1.cx(4, 5)
qc_1.cx(4, 6)
qc_1.cx(4, 7)
qc_1.append(Move(), [4, 3])
qc_1.cx(0, 3)
qc_1.cx(1, 3)
qc_1.cx(2, 3)

qc_1.draw("mpl")
```

![Quantum circuit diagram](./output_2.png)

### Vytvoření observablu k novému Circuit {#vytvoření-observablu-k-novému-circuit}

Tento observable odpovídá `observable`, ale musíme správně zohlednit přidaný extra qubitový drát (tj. vložíme „I" na index 4). Všimni si, že v Qiskit odpovídá ve stringové reprezentaci qubit-0 nejpravějšímu znaku Pauliho operátoru.

```python
observable_expanded = SparsePauliOp(["ZIIIIIII", "IIIIZIII", "IIIIIIIZ"])
```

### Rozdělení Circuit a observablů {#rozdělení-circuit-a-observablů}

Stejně jako v předchozích tutoriálech budou Qubity sdílející společný štítek oddílu seskupeny dohromady a nelocální Gate překlenující více než jeden oddíl budou přeříznuty.

```python
from qiskit_addon_cutting import partition_problem

partitioned_problem = partition_problem(
    circuit=qc_1, partition_labels="AAAABBBB", observables=observable_expanded.paulis
)
subcircuits = partitioned_problem.subcircuits
subobservables = partitioned_problem.subobservables
bases = partitioned_problem.bases
```

### Vizualizace rozloženého problému {#vizualizace-rozloženého-problému}

```python
subobservables
```

```text
{'A': PauliList(['IIII', 'ZIII', 'IIIZ']),
 'B': PauliList(['ZIII', 'IIII', 'IIII'])}
```

```python
subcircuits["A"].draw("mpl")
```

![Quantum circuit diagram](./output_3.png)

```python
subcircuits["B"].draw("mpl")
```

![Quantum circuit diagram](./output_4.png)

### Výpočet vzorkovací režie pro zvolené řezy {#výpočet-vzorkovací-režie-pro-zvolené-řezy}

Zde řežeme dva dráty, čímž vzniká vzorkovací režie $4^4$.

Více informací o vzorkovací režii způsobené řezáním Circuit najdeš ve [vysvětlujícím materiálu](../explanation/index.rst).

```python
print(f"Sampling overhead: {np.prod([basis.overhead for basis in bases])}")
```

```text
Sampling overhead: 256.0
```

### Generování dílčích experimentů ke spuštění na Backend {#generování-dílčích-experimentů-ke-spuštění-na-backend}

`generate_cutting_experiments` přijímá argumenty `circuits`/`observables` jako slovníky mapující štítky qubitových oddílů na příslušné `subcircuit`/`subobservables`.

Pro simulaci očekávané hodnoty Circuit v plné velikosti je z rozložené společné kvazipravděpodobnostní distribuce Gate generováno mnoho dílčích experimentů, které jsou následně spuštěny na jednom nebo více Backend. Počet vzorků odebraných z distribuce je řízen parametrem `num_samples` a pro každý jedinečný vzorek je uveden jeden kombinovaný koeficient. Více informací o způsobu výpočtu koeficientů najdeš ve [vysvětlujícím materiálu](../explanation/index.rst).

```python
from qiskit_addon_cutting import generate_cutting_experiments

subexperiments, coefficients = generate_cutting_experiments(
    circuits=subcircuits, observables=subobservables, num_samples=np.inf
)
```

### Výběr Backend {#výběr-backend}

Zde používáme falešný Backend, který způsobí, že Qiskit Runtime poběží v lokálním režimu (tj. na lokálním simulátoru).

```python
from qiskit_ibm_runtime.fake_provider import FakeManilaV2

backend = FakeManilaV2()
```

### Příprava dílčích experimentů pro Backend {#příprava-dílčích-experimentů-pro-backend}

Před odesláním do Qiskit Runtime musíme transpilovat Circuit s naším Backend jako cílem.

```python
from qiskit.transpiler import generate_preset_pass_manager

# Transpile the subexperiments to ISA circuits
pass_manager = generate_preset_pass_manager(optimization_level=1, backend=backend)
isa_subexperiments = {
    label: pass_manager.run(partition_subexpts)
    for label, partition_subexpts in subexperiments.items()
}
```

## Krok 3: Spuštění {#krok-3-spuštění}

### Spuštění dílčích experimentů pomocí primitiva Qiskit Runtime Sampler {#spuštění-dílčích-experimentů-pomocí-primitiva-qiskit-runtime-sampler}

```python
from qiskit_ibm_runtime import SamplerV2, Batch

# Submit each partition's subexperiments to the Qiskit Runtime Sampler
# primitive, in a single batch so that the jobs will run back-to-back.
with Batch(backend=backend) as batch:
    sampler = SamplerV2(mode=batch)
    jobs = {
        label: sampler.run(subsystem_subexpts, shots=2**12)
        for label, subsystem_subexpts in isa_subexperiments.items()
    }
```

```text
/home/garrison/Qiskit/qiskit-ibm-runtime/qiskit_ibm_runtime/session.py:157: UserWarning: Session is not supported in local testing mode or when using a simulator.
  warnings.warn(
```

```python
# Retrieve results
results = {label: job.result() for label, job in jobs.items()}
```

## Krok 4: Následné zpracování {#krok-4-následné-zpracování}

### Rekonstrukce očekávané hodnoty {#rekonstrukce-očekávané-hodnoty}

Rekonstruujeme očekávané hodnoty pro každý člen observablu a zkombinujeme je, abychom rekonstruovali očekávanou hodnotu původního observablu.

```python
from qiskit_addon_cutting import reconstruct_expectation_values

reconstructed_expval_terms = reconstruct_expectation_values(
    results,
    coefficients,
    subobservables,
)
reconstructed_expval = np.dot(reconstructed_expval_terms, observable.coeffs)
```

### Porovnání rekonstruované očekávané hodnoty s přesnou očekávanou hodnotou z původního Circuit a observablu {#porovnání-rekonstruované-očekávané-hodnoty-s-přesnou-očekávanou-hodnotou-z-původního-circuit-a-observablu}

```python
from qiskit_aer.primitives import EstimatorV2

estimator = EstimatorV2()
exact_expval = estimator.run([(qc_0, observable)]).result()[0].data.evs
print(f"Reconstructed expectation value: {np.real(np.round(reconstructed_expval, 8))}")
print(f"Exact expectation value: {np.round(exact_expval, 8)}")
print(f"Error in estimation: {np.real(np.round(reconstructed_expval-exact_expval, 8))}")
print(
    f"Relative error in estimation: {np.real(np.round((reconstructed_expval-exact_expval) / exact_expval, 8))}"
)
```

```text
Reconstructed expectation value: 1.51319069
Exact expectation value: 1.59099026
Error in estimation: -0.07779957
Relative error in estimation: -0.04890009
```

<TutorialFeedback />
