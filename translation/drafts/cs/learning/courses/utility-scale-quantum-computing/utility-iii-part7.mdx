#### Krok 3: Spuštění na cílovém hardwaru {#step-3-execute-on-target-hardware}

```python
res = execute_ghz_fidelity(
    ghz_circuit=ghz3,
    physical_qubits=best_qubits,
    backend=backend,
    sampler_options=opts,
)
```

```python
job_s = service.job(res[0])  # Use your job id showed above.
job_e = service.job(res[1])
print(job_s.status(), job_e.status())
```

```text
DONE DONE
```

#### Krok 4: Post-processing výsledků {#step-4-post-process-results}

```python
N = 30
# Check fidelity from job IDs
res = check_ghz_fidelity_from_jobs(
    sampler_job=job_s,
    estimator_job=job_e,
    num_qubits=N,
)
```

```text
N=30: |00..0>: 4, |11..1>: 218, |3rd>: 265 (111111111111111011111111111111)
P(|00..0>)=0.0001, P(|11..1>)=0.00545
REM: Coherence (non-diagonal): 0.187073
GHZ fidelity = 0.096312 ± 0.003254
GME (genuinely multipartite entangled) test: Failed
```

Jak vidíš, tento výsledek nesplnil kritéria.

```python
# It will take some time
result = job_s.result()
plot_histogram(result[0].data.c.get_counts(), figsize=(30, 5))
```

![Výstup předchozí buňky s kódem](/learning/images/courses/utility-scale-quantum-computing/utility-iii/extracted-outputs/66c87870-0.avif)

## 4. Strategie 3: Spuštění s možnostmi potlačení chyb {#strategy-3-run-with-the-error-suppression-options}

V Sampleru V2 můžeš nastavit možnosti potlačení chyb. Více informací najdeš v průvodci [Konfigurace mitigace chyb](/guides/configure-error-mitigation#advanced-error) a v referenci API pro [`ExecutionOptionsV2`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/options-execution-options-v2).

```python
opts = SamplerOptions()
opts.dynamical_decoupling.enable = True
opts.execution.rep_delay = 0.0005
opts.twirling.enable_gates = True
```

```python
res = execute_ghz_fidelity(
    ghz_circuit=ghz3,
    physical_qubits=best_qubits,
    backend=backend,
    sampler_options=opts,
)
```

```python
job_s = service.job(res[0])  # Use your job id showed above.
job_e = service.job(res[1])
print(job_s.status(), job_e.status())
```

```text
DONE DONE
```

```python
N = 30
```

```python
# Check fidelity from job IDs
res = check_ghz_fidelity_from_jobs(
    sampler_job=job_s,
    estimator_job=job_e,
    num_qubits=N,
)
```

```text
N=30: |00..0>: 1459, |11..1>: 1543, |3rd>: 359 (111111111111111111111111111110)
P(|00..0>)=0.036475, P(|11..1>)=0.038575
REM: Coherence (non-diagonal): 0.165532
GHZ fidelity = 0.120291 ± 0.003369
GME (genuinely multipartite entangled) test: Failed
```

```python
# It will take some time
result = job_s.result()
plot_histogram(result[0].data.c.get_counts(), figsize=(30, 5))
```

![Výstup předchozí buňky s kódem](/learning/images/courses/utility-scale-quantum-computing/utility-iii/extracted-outputs/166856be-ad3a-40d6-837c-3eedda374891-0.avif)

Výsledek se zlepšil, ale stále nesplnil kritéria.

Viděli jsme zatím tři nápady. Tyto nápady můžeš kombinovat a rozšiřovat, nebo přijít s vlastními a vytvořit tak lepší GHZ Circuit. Teď si znovu projdeme cíl.
