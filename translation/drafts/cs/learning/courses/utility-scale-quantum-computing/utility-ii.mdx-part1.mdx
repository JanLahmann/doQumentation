---
title: "Utility II"
sidebar_label: "Utility II"
description: "Tento notebook navazuje na metody a techniky z lekce 7. Naším cílem je numericky vyřešit časově závislou Schrödingerovu rovnici."
notebook_path: "learning/courses/utility-scale-quantum-computing/utility-ii.ipynb"
---
{/* doqumentation-source-hash: 244eb62e */}


<OpenInLabBanner notebookPath="learning/courses/utility-scale-quantum-computing/utility-ii.ipynb" />



{/* cspell:ignore sharex */}

# Experiment v měřítku užitečnosti II

<Admonition type="note">

Yukio Kawashima (12. července 2024)

[Stáhněte si pdf](https://ibm.ent.box.com/s/bipgoms7gr6b6vhkoc1uw6oi4wsanfoq) původní přednášky. Mějte na paměti, že některé ukázky kódu mohou být zastaralé, protože jde o statické obrázky.

*Přibližný čas QPU potřebný ke spuštění tohoto experimentu je 2 min 30 s.*

(Upozorňujeme, že tento notebook využíval texty, ilustrace a kód z již zastaralého [tutoriálového notebooku](https://github.com/qiskit-community/qiskit-algorithms/blob/main/docs/tutorials/13_trotterQRTE.ipynb) pro Qiskit Algorithms.)

</Admonition>
## 1. Úvod a přehled časové evoluce {#1-introduction-and-review-of-time-evolution}

Tento notebook navazuje na metody a techniky z lekce 7. Naším cílem je numericky vyřešit časově závislou Schrödingerovu rovnici. Jak jsme probírali v lekci 7, Trotterizace spočívá v postupné aplikaci kvantové brány nebo bran, které jsou zvoleny tak, aby aproximovaly časovou evoluci systému pro dané časové okno. Tuto diskuzi zde pro pohodlí opakujeme. Pokud jsi lekci 7 nedávno prošel, klidně přeskoč rovnou na buňky s kódem níže.

Ze Schrödingerovy rovnice vyplývá, že časová evoluce systému, který je zpočátku ve stavu $\vert\psi(0)\rangle$, má tvar:

$$
\vert \psi(t) \rangle = e^{-i H t} \vert \psi(0) \rangle \text{,}
$$

kde $H$ je časově nezávislý Hamiltonián popisující systém. Uvažujeme Hamiltonián, který lze zapsat jako vážený součet Pauliho členů $H=\sum_j a_j P_j$, kde $P_j$ představuje tenzorový součin Pauliho členů působících na $n$ qubitech. Tyto Pauliho členy mohou mezi sebou komutovat, nebo také ne. Jak získáme stav systému $|\psi(t)\rangle$ v pozdějším čase pomocí kvantového počítače, pokud známe stav v čase $t=0$? Exponenciálu operátoru nejsnáze pochopíme přes její Taylorovu řadu:

$$
e^{-i H t} = 1-iHt-\frac{1}{2}H^2t^2+...
$$

Některé velmi jednoduché exponenciály, například $e^{iZ}$, lze na kvantových počítačích snadno implementovat pomocí kompaktní sady kvantových bran. Většina zajímavých Hamiltoniánů ale nebude mít jen jeden člen, nýbrž mnoho členů. Všimni si, co se stane, pokud $H = H_1+H_2$:

$$
e^{-i H t} = 1-i(H_1+H_2)t-\frac{1}{2}(H_1+H_2)^2t^2+...
$$

Když $H_1$ a $H_2$ komutují, máme známý případ (platný i pro čísla a proměnné $a$ a $b$ níže):

$$
e^{-i (a+b) t} = e^{-i a t}e^{-i b t}
$$

Když ale operátory nekomutují, členy v Taylorově řadě nelze přeuspořádat tak, aby se takto zjednodušily. Vyjádření složitějších Hamiltoniánů pomocí kvantových bran je proto výzva.

Jedním z řešení je uvažovat velmi malý čas $t$, takže dominuje člen prvního řádu v Taylorově rozvoji. Za tohoto předpokladu:

$$
e^{-i (H_1+H_2) t} \approx 1-i(H_1+H_2)t \approx (1-i H_1 t)(1-i H_2 t) \approx e^{-i H_1 t}e^{-i H_2 t}
$$

Samozřejmě můžeme potřebovat vyvíjet náš stav po delší čas. Toho dosáhneme použitím mnoha takových malých časových kroků. Tento proces se nazývá Trotterizace:

$$
\vert \psi(t) \rangle \approx \left(\prod_j e^{-i a_j P_j t/r} \right)^r \vert\psi(0) \rangle \text{,}
$$

Zde $t/r$ je časové okno (krok evoluce), které si volíme. Výsledkem je brána, která se aplikuje $r$krát. Menší časový krok vede k přesnější aproximaci. To ale zároveň vede k hlubším obvodům, což v praxi znamená větší nahromadění chyb (což je na současných kvantových zařízeních nezanedbatelný problém).

Dnes budeme studovat časovou evoluci [Isingova modelu](https://en.wikipedia.org/wiki/Ising_model) na lineárních mřížkách o $N=2$ a $N=6$ uzlech. Tyto mřížky tvoří pole spinů $\sigma_i$, které interagují pouze se svými nejbližšími sousedy. Tyto spiny mohou mít dvě orientace: $\uparrow$ a $\downarrow$, které odpovídají magnetizaci $+1$ a $-1$.

$$
H = - J \sum_{i=0}^{N-2} Z_i Z_{i+1} - h \sum_{i=0}^{N-1} X_i  \text{,}
$$

kde $J$ popisuje interakční energii a $h$ velikost vnějšího pole (ve směru x výše, ale budeme to upravovat). Zapišme tento výraz pomocí Pauliho matic a s ohledem na to, že vnější pole svírá úhel $\alpha$ vůči transverzálnímu směru,

$$
H = -J \sum_{i=0}^{N-2} Z_i Z_{i+1} -h \sum_{i=0}^{N-1} (\sin\alpha Z_i + \cos\alpha X_i) \text{.}
$$

Tento Hamiltonián je užitečný tím, že nám umožňuje snadno studovat vliv vnějšího pole. Ve výpočetní bázi bude systém zakódován takto:

| Kvantový stav            | Reprezentace spinů                         |
|:------------------------:|:------------------------------------------:|
| $\lvert 0 0 0 0 \rangle$ | $\uparrow\uparrow\uparrow\uparrow$         |
| $\lvert 1 0 0 0 \rangle$ | $\downarrow\uparrow\uparrow\uparrow$       |
| $\ldots$                 | $\ldots$                                   |
| $\lvert 1 1 1 1 \rangle$ | $\downarrow\downarrow\downarrow\downarrow$ |

Začneme zkoumat časovou evoluci takového kvantového systému. Konkrétně si vizualizujeme časovou evoluci některých vlastností systému, jako je magnetizace.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q matplotlib numpy qiskit qiskit-aer qiskit-ibm-runtime
```

```python
# Check the version of Qiskit
import qiskit

qiskit.__version__
```

```text
'2.0.2'
```

```python
# Import the qiskit library

import numpy as np
import warnings

from qiskit import QuantumCircuit, QuantumRegister
from qiskit.circuit.library import PauliEvolutionGate
from qiskit.quantum_info import SparsePauliOp
from qiskit.synthesis import LieTrotter
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

from qiskit_aer import AerSimulator
from qiskit_ibm_runtime import QiskitRuntimeService, Estimator

warnings.filterwarnings("ignore")
```

