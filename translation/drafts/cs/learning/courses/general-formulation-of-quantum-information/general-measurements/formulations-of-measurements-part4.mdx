Pro tyto stejné matice $P_0,\ldots,P_{m-1}$ tedy můžeme kanál $\Phi$ vyjádřit následovně.

$$
\Phi(\rho) = \sum_{a = 0}^{m-1} \operatorname{Tr}(P_a \rho) \vert a\rangle\langle a\vert
$$

Tento výraz je konzistentní s naším popisem obecných měření pomocí matic, protože vidíme, že každý výsledek měření se objevuje s pravděpodobností $\operatorname{Tr}(P_a \rho).$

Nyní si všimněme, že dvě vlastnosti požadované od kolekce matic $\{P_0,\ldots,P_{m-1}\}$ pro popis obecného měření jsou skutečně splněny.
První vlastností je, že všechny jsou pozitivně semidefinitní matice.
Jeden způsob, jak to vidět, je pozorovat, že pro každý vektor $\vert \psi\rangle$ s prvky odpovídajícími klasickému stavu $\mathsf{X}$ máme

$$
\langle \psi \vert P_a \vert \psi\rangle
= \sum_{k = 0}^{N-1} \langle \psi \vert A_k^{\dagger} \vert a\rangle\langle a \vert A_k\vert \psi\rangle
=  \sum_{k = 0}^{N-1} \bigl\vert\langle a \vert A_k\vert \psi\rangle\bigr\vert^2 \geq 0.
$$

Druhou vlastností je, že součet těchto matic dává jednotkovou matici.

$$
\begin{aligned}
\sum_{a = 0}^{m-1} P_a
& = \sum_{a = 0}^{m-1} \sum_{k = 0}^{N-1} A_k^{\dagger} \vert a\rangle\langle a \vert A_k \\
& = \sum_{k = 0}^{N-1} A_k^{\dagger} \Biggl(\sum_{a = 0}^{m-1} \vert a\rangle\langle a \vert\Biggr) A_k \\
& = \sum_{k = 0}^{N-1} A_k^{\dagger} A_k \\
& = \mathbb{I}_{\mathsf{X}}
\end{aligned}
$$

Poslední rovnost plyne z toho, že $\Phi$ je kanál, takže jeho Krausovy matice musí tuto podmínku splňovat.

### Z matic na kanály {#matrices-to-channels}

Nyní ověřme, že pro libovolnou kolekci $\{P_0,\ldots,P_{m-1}\}$ pozitivně semidefinitních matic splňující $P_0 + \cdots + P_{m-1} = \mathbb{I}_{\mathsf{X}}$ je zobrazení definované jako

$$
\Phi(\rho) = \sum_{a = 0}^{m-1} \operatorname{Tr}(P_a \rho) \vert a \rangle\langle a\vert
$$

skutečně platným kanálem ze systému $\mathsf{X}$ do $\mathsf{Y}.$

Jeden způsob, jak to udělat, je spočítat Choiho reprezentaci tohoto zobrazení.

$$
\begin{aligned}
J(\Phi) & = \sum_{b,c = 0}^{n-1} \vert b \rangle \langle c \vert \otimes \Phi(\vert b \rangle \langle c \vert)\\[1mm]
& = \sum_{b,c = 0}^{n-1} \sum_{a = 0}^{m-1} \vert b \rangle \langle c \vert \otimes
\operatorname{Tr}(P_a \vert b \rangle \langle c \vert) \vert a \rangle\langle a\vert\\[1mm]
& = \sum_{b,c = 0}^{n-1} \sum_{a = 0}^{m-1} \vert b \rangle \langle b \vert P_a^T \vert c \rangle \langle c \vert \otimes
\vert a \rangle\langle a\vert\\[1mm]
& = \sum_{a = 0}^{m-1}  P_a^T \otimes \vert a \rangle\langle a\vert
\end{aligned}
$$

Transpozice každého $P_a$ se zavádí při třetí rovnosti, protože

$$
\langle c \vert P_a \vert b\rangle = \langle b \vert P_a^T \vert c\rangle.
$$

To umožňuje, aby se objevily výrazy $\vert b \rangle \langle b \vert$ a $\vert c \rangle \langle c \vert$, které se po sečtení přes $b$ a $c$ zjednoduší na jednotkovou matici.

Z předpokladu, že $P_0,\ldots,P_{m-1}$ jsou pozitivně semidefinitní, jsou pozitivně semidefinitní i $P_0^{T},\ldots,P_{m-1}^{T}.$
Konkrétně, transpozice hermitovské matice dává opět hermitovskou matici a vlastní čísla libovolné čtvercové matice a její transpozice se vždy shodují.
Z toho plyne, že $J(\Phi)$ je pozitivně semidefinitní.
Vytrasováním výstupního systému $\mathsf{Y}$ (což je systém napravo) dostaneme

$$
\operatorname{Tr}_{\mathsf{Y}} (J(\Phi)) = \sum_{a = 0}^{m-1}  P_a^T = \mathbb{I}_{\mathsf{X}}^T = \mathbb{I}_{\mathsf{X}},
$$

a tak docházíme k závěru, že $\Phi$ je kanál.

## Parciální měření {#partial-measurements}

Předpokládejme, že máme více systémů, které jsou kolektivně v kvantovém stavu, a na jednom z nich se provede obecné měření.
Výsledkem je jeden z výsledků měření, vybraný náhodně podle pravděpodobností určených měřením a stavem systému před měřením.
Výsledný stav zbývajících systémů pak obecně závisí na tom, jaký výsledek měření byl získán.

Podívejme se, jak to funguje pro dvojici systémů $(\mathsf{X},\mathsf{Z})$, když se měří systém $\mathsf{X}.$
(Systém napravo pojmenováváme $\mathsf{Z}$, protože $\mathsf{Y}$ budeme používat jako systém reprezentující klasický výstup měření, když ho chápeme jako kanál.)
Potom můžeme snadno zobecnit na situaci, kdy jsou systémy prohozeny, i na tři či více systémů.

Předpokládejme, že stav $(\mathsf{X},\mathsf{Z})$ před měřením je popsán maticí hustoty $\rho,$ kterou můžeme zapsat následovně.

$$
\rho = \sum_{b,c = 0}^{n-1} \vert b\rangle\langle c\vert \otimes \rho_{b,c}
$$

V tomto výrazu předpokládáme, že klasické stavy systému $\mathsf{X}$ jsou $0,\ldots,n-1.$

Budeme předpokládat, že samotné měření je popsáno kolekcí matic
$\{P_0,\ldots,P_{m-1}\}.$
Toto měření lze alternativně popsat jako kanál $\Phi$ ze systému $\mathsf{X}$ do $\mathsf{Y},$ kde $\mathsf{Y}$ je nový systém s klasickou množinou stavů $\{0,\ldots,m-1\}.$
Konkrétně, působení tohoto kanálu lze vyjádřit následovně.

$$
\Phi(\xi) = \sum_{a = 0}^{m-1} \operatorname{Tr}(P_a \xi)\, \vert a \rangle \langle a \vert
$$

### Pravděpodobnosti výsledků {#outcome-probabilities}

Uvažujeme měření systému $\mathsf{X},$ takže pravděpodobnosti, s jakými jsou získány různé výsledky měření, mohou záviset pouze na $\rho_{\mathsf{X}},$ redukovaném stavu systému $\mathsf{X}.$
Konkrétně, pravděpodobnost pro každý výsledek $a\in\{0,\ldots,m-1\}$ lze vyjádřit třemi ekvivalentními způsoby.

$$
\operatorname{Tr}\bigl( P_a \rho_{\mathsf{X}}\bigr) =
\operatorname{Tr}\bigl( P_a \operatorname{Tr}_{\mathsf{Z}}(\rho)\bigr) =
\operatorname{Tr}\bigl( (P_a \otimes \mathbb{I}_{\mathsf{Z}}) \rho \bigr)
$$