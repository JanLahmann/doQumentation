Konkrétněji, tyto dva případy vypadají následovně.

$$
\begin{aligned}
\vert 0 \oplus 0\rangle - \vert 1 \oplus 0\rangle
& = \vert 0 \rangle - \vert 1 \rangle
= (-1)^0 \bigl( \vert 0\rangle - \vert 1\rangle \bigr)\\
\vert 0 \oplus 1\rangle - \vert 1 \oplus 1\rangle & = \vert 1 \rangle - \vert 0\rangle
= (-1)^1 \bigl( \vert 0\rangle - \vert 1\rangle \bigr)
\end{aligned}
$$

Takže můžeme alternativně vyjádřit $\vert\pi_2\rangle$ takto:

$$
\begin{aligned}
  \vert\pi_2\rangle
  & = \frac{1}{2} (-1)^{f(0)} \bigl( \vert 0 \rangle - \vert 1 \rangle \bigr) \vert 0 \rangle
  + \frac{1}{2} (-1)^{f(1)} \bigl( \vert 0 \rangle - \vert 1 \rangle \bigr) \vert 1 \rangle \\
  & = \vert - \rangle \biggl( \frac{(-1)^{f(0)} \vert 0\rangle + (-1)^{f(1)} \vert 1\rangle}{\sqrt{2}}\biggr).
\end{aligned}
$$

Právě se stalo něco zajímavého!
Ačkoli působení Gate $U_f$ na standardní bázové stavy ponechává horní/nejpravější Qubit beze změny a XORuje hodnotu funkce na spodní/nejlevější Qubit, zde vidíme, že se stav horního/nejpravějšího Qubitu (obecně) změnil, zatímco stav spodního/nejlevějšího Qubitu zůstal stejný — konkrétně ve stavu $\vert - \rangle$ před i po provedení Gate $U_f$.
Tento jev je známý jako *phase kickback* (fázový zpětný ráz) a za chvíli si o něm řekneme víc.

S jedním posledním zjednodušením, kterým je vytažení faktoru $(-1)^{f(0)}$ před součet, získáme tento výraz stavu $\vert\pi_2\rangle$:

$$
\begin{aligned}
  \vert\pi_2\rangle
  & = (-1)^{f(0)} \vert - \rangle
      \biggl( \frac{\vert 0\rangle + (-1)^{f(0) \oplus f(1)} \vert 1\rangle}{\sqrt{2}}\biggr) \\
  & = \begin{cases}
        (-1)^{f(0)} \vert - \rangle \vert + \rangle & \text{if $f(0) \oplus f(1) = 0$}\\[1mm]
        (-1)^{f(0)} \vert - \rangle \vert - \rangle & \text{if $f(0) \oplus f(1) = 1$}.
      \end{cases}
\end{aligned}
$$

Všimni si, že v tomto výrazu máme $f(0) \oplus f(1)$ v exponentu $-1$ místo $f(1) - f(0),$ což bychom mohli očekávat z čistě algebraického hlediska, ale výsledek je v obou případech stejný.
Je to proto, že hodnota $(-1)^k$ pro libovolné celé číslo $k$ závisí pouze na tom, zda je $k$ sudé nebo liché.

Aplikací poslední Hadamardovy Gate na horní Qubit získáme stav

$$
\vert \pi_3 \rangle =
\begin{cases}
  (-1)^{f(0)} \vert - \rangle \vert 0 \rangle & \text{if $f(0) \oplus f(1) = 0$}\\[1mm]
  (-1)^{f(0)} \vert - \rangle \vert 1 \rangle & \text{if $f(0) \oplus f(1) = 1$},
\end{cases}
$$

což vede ke správnému výsledku s pravděpodobností $1$ při měření pravého/horního Qubitu.

## Další poznámky k phase kickbacku {#further-remarks-on-the-phase-kickback}

Než budeme pokračovat, podívejme se na výše uvedenou analýzu z mírně odlišného úhlu, který může osvětlit jev phase kickbacku.

Nejprve si všimni, že následující vzorec platí pro všechny volby bitů $b,c\in\Sigma.$

$$
\vert b \oplus c\rangle = X^c \vert b \rangle
$$

To lze ověřit kontrolou pro dvě možné hodnoty $c = 0$ a $c = 1$:

$$
\begin{aligned}
\vert b \oplus 0 \rangle & = \vert b\rangle = \mathbb{I} \vert b \rangle = X^0 \vert b \rangle\\
\vert b \oplus 1 \rangle & = \vert \neg b\rangle = X \vert b \rangle = X^1 \vert b \rangle.
\end{aligned}
$$

Pomocí tohoto vzorce vidíme, že

$$
U_f \bigl(\vert b\rangle \vert a \rangle\bigr)
= \vert b \oplus f(a) \rangle \vert a \rangle
= \bigl(X^{f(a)}\vert b \rangle\bigr) \vert a \rangle
$$

pro každou volbu bitů $a,b\in\Sigma.$
Protože tento vzorec platí pro $b=0$ a $b=1,$ z linearity vyplývá, že

$$
U_f \bigl( \vert \psi \rangle \vert a \rangle \bigr) = \bigl(X^{f(a)}\vert \psi \rangle\bigr) \vert a \rangle
$$

pro všechny stavové vektory Qubitu $\vert \psi\rangle,$ a proto

$$
U_f \bigl( \vert - \rangle \vert a \rangle \bigr) = \bigl(X^{f(a)} \vert - \rangle \bigr) \vert a \rangle
= (-1)^{f(a)} \vert - \rangle \vert a \rangle.
$$

Klíčem, díky kterému to funguje, je to, že $X\vert - \rangle = - \vert - \rangle.$
V matematické terminologii je vektor $\vert - \rangle$ *vlastním vektorem* matice $X$ s *vlastním číslem* $-1.$

Vlastní vektory a vlastní čísla probereme podrobněji v nadcházející lekci o *odhadu fáze a faktorizaci*, kde je jev phase kickbacku zobecněn na další unitární operace.

S vědomím, že skaláry volně procházejí tenzorovými součiny, najdeme alternativní způsob uvažování o tom, jak operace $U_f$ transformuje $\vert \pi_1\rangle$ na $\vert \pi_2\rangle$ ve výše uvedené analýze:

$$
\begin{aligned}
  \vert \pi_2 \rangle
  & = U_f \bigl( \vert - \rangle \vert + \rangle \bigr)\\
  & = \frac{1}{\sqrt{2}} U_f \bigl(\vert - \rangle \vert 0\rangle \bigr)
    + \frac{1}{\sqrt{2}} U_f \bigl(\vert - \rangle \vert 1\rangle \bigr)\\
  & = \vert - \rangle \biggl( \frac{(-1)^{f(0)} \vert 0\rangle + (-1)^{f(1)} \vert 1\rangle}{\sqrt{2}}\biggr).
