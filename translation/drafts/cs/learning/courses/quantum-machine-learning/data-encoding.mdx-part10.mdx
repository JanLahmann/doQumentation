### Mapa příznaků Pauli {#pauli-feature-map}

Mapa příznaků Pauli (PFM) je zobecněním ZFM a ZZFM pro použití libovolných Pauli Gate. Mapa příznaků Pauli má velmi podobnou formu jako předchozí dvě feature map. Pro $r$ opakování kódování $N$ příznaků vektoru $\vec{x},$

$$
\mathscr{U}_{\text{PFM}}(\vec{x}) = \prod_{s=1}^{r} U(\vec{x}) H^{\otimes n}.
$$

V případě PFM je $U(\vec{x})$ zobecněno na unitární operátor Pauliho rozkladu. Zde uvádíme obecnější formu feature map, které jsme dosud uvažovali:

$$
U(\vec{x}) = \exp\left(i \sum_{S \in\mathcal{I}} \phi_S(\vec{x}) \prod_{i \in S} \sigma_i \right),
$$

kde $\sigma_i$ je Pauliho operátor, $\sigma_i \in {I,X,Y,Z}$. Zde $\mathcal{I}$ je množina všech konektivit Qubit, jak je určeno feature map, včetně množiny Qubit, na které působí jednoqubitové Gate. To znamená, že pro feature map, v níž na Qubit 0 působil fázový Gate a na Qubit 2 a 3 působil Gate $R_{ZZ}$, by množina $\mathcal{I}$ obsahovala $\{\{0\},\{2,3\}\}$. $S$ prochází všemi prvky této množiny. V předchozích feature map byla funkce $\phi_S(\vec{x})$ spojena buď výhradně s jednoqubitovými Gate, nebo výhradně s dvouqubitovými Gate. Zde ji definujeme obecně:
$$
\phi_S(\vec{x})=
    \begin{cases}
      x_i & \text{if } S= \{i\} \text{ (single-qubit)}\\
      \prod_{j\in{S}}(\pi-x_j) & \text{if } |S|\ge2 \text{ (multi-qubit)}\\
    \end{cases}
$$

Dokumentaci najdeš v [dokumentaci třídy `Pauli feature map` pro Qiskit](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.PauliFeatureMap)). V ZZFM je operátor $\sigma_i$ omezen na $Z_i$.

Jeden způsob, jak porozumět výše uvedenému unitárnímu operátoru, je analogie s propagátorem ve fyzikálním systému. Výše uvedený unitární operátor je operátor unitárního vývoje, $\exp(it\mathcal{H})$, pro Hamiltonián, $\mathcal{H}$, podobný Isingovu modelu, kde je časový parametr, $t$, nahrazen datovými hodnotami, které řídí vývoj. Rozvinutí tohoto unitárního operátoru dává Circuit PFM. Propletenecké konektivity v $S$ lze interpretovat jako Isingova vazby ve spinové mřížce.
Uvažujme příklad Pauli operátorů $Y$ a $XX$ reprezentujících tyto interakce Isingova typu. Qiskit poskytuje třídu `pauli_feature_map` pro vytváření instancí PFM s volbou jednoqubitových a $n$-qubitových Gate, které budou v tomto příkladu předány jako Pauliho řetězce `'Y'` a `'XX'`. Typicky je $n$ rovno 1 nebo 2 pro jednoqubitové, respektive dvouqubitové interakce. Schéma propletení je „lineární", což znamená, že jsou vázány pouze sousední Qubit v Circuit. Všimni si, že to neodpovídá sousedním Qubit na samotném kvantovém počítači, protože tento Circuit je abstrakční vrstvou.

```python
from qiskit.circuit.library import pauli_feature_map

feature_dim = 3
pfmap = pauli_feature_map(
    feature_dimension=feature_dim, entanglement="linear", reps=1, paulis=["Y", "XX"]
)

pfmap.decompose().draw("mpl", scale=1.5)
```

![Output of the previous code cell](/learning/images/courses/quantum-machine-learning/data-encoding/extracted-outputs/5ba5df82-83c1-428c-a269-baea5f75c3dc-0.avif)

Qiskit poskytuje parametr $\alpha$ v Pauli feature map pro řízení škálování Pauliho rotací.

$$
U(\bar{x}) = \exp\left(i \alpha \sum_{S\subseteq[n]} \phi_S(\bar{x}) \prod_{i \in S} \sigma_i \right)
$$

Výchozí hodnota $\alpha$ je $2$. Optimalizací její hodnoty v intervalu, například $[0,4],$ lze lépe přizpůsobit kvantové jádro datům.
