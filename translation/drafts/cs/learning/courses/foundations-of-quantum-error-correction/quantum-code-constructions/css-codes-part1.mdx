

{/* cspell:ignore subseteq */}
{/* cspell:ignore mapsto */}

# CSS kódy {#css-codes}

## Klasické lineární kódy {#classical-linear-codes}

Klasické kódy opravující chyby se začaly studovat ve 40. letech 20. století a od té doby je známo mnoho různých kódů. Nejčastěji studované a používané kódy spadají do kategorie známé jako *lineární kódy*.
Za chvíli přesně uvidíme, co slovo „lineární" v tomto kontextu znamená, ale velmi jednoduchý způsob, jak lineární kódy vyjádřit, je ten, že jde o stabilizátorové kódy, které jsou náhodou klasické.
CSS kódy jsou v podstatě *páry* klasických lineárních kódů, které se kombinují dohromady za účelem vytvoření kvantového kódu opravujícího chyby.
Takže pro účely následující diskuse budeme potřebovat porozumět několika základním věcem o klasických lineárních kódech.

Nechť $\Sigma$ je binární abeceda pro celou tuto diskusi.
Když mluvíme o *klasickém lineárním kódu*, máme na mysli neprázdnou množinu $\mathcal{C}\subseteq\Sigma^n$ binárních řetězců délky $n$ pro nějaké kladné celé číslo $n,$ která musí splňovat jednu základní vlastnost:
pokud $u$ a $v$ jsou binární řetězce v $\mathcal{C},$ pak řetězec $u\oplus v$ je také v $\mathcal{C}.$
Zde $u\oplus v$ označuje bitovou operaci exkluzivní-OR řetězců $u$ a $v,$ se kterou jsme se setkali několikrát v kurzu
„Fundamentals of quantum algorithms".

V podstatě, když o klasickém kódu opravujícím chyby mluvíme jako o *lineárním*, představujeme si binární řetězce délky $n$ jako $n$-rozměrné vektory, jejichž složky jsou buď $0,$ nebo $1,$ a požadujeme, aby samotný kód tvořil lineární podprostor.
Místo běžného vektorového sčítání nad reálnými nebo komplexními čísly však používáme sčítání modulo $2,$ což je prostě exkluzivní-OR.
To znamená, že pokud máme dvě *kódová slova* $u$ a $v,$ tedy binární řetězce v $\mathcal{C},$ pak $u + v$ modulo 2, tedy $u\oplus v,$ musí být také kódovým slovem v $\mathcal{C}.$
Všimni si zejména, že tato implikace musí platit i v případě $u = v.$
Z toho vyplývá, že $\mathcal{C}$ musí obsahovat samý nulový řetězec $0^n,$ protože bitový exkluzivní-OR jakéhokoli řetězce se sebou samým je samý nulový řetězec.

### Příklad: 3-bitový opakovací kód {#example-the-3-bit-repetition-code}

3-bitový opakovací kód je příkladem klasického lineárního kódu.
Konkrétně máme $\mathcal{C} = \{000,111\},$ takže vzhledem k podmínce linearity existují pouze dvě možné volby pro $u$ a dvě možné volby pro $v.$
Je triviální projít čtyři možné páry a ověřit, že při bitovém exkluzivním-OR vždy dostaneme kódové slovo:

$$
000 \oplus 000 = 000, \quad
000 \oplus 111 = 111, \quad
111 \oplus 000 = 111, \quad
111 \oplus 111 = 000.
$$

### Příklad: $[7,4,3]$-Hammingův kód {#example-the-743-hamming-code}

Zde je další příklad klasického lineárního kódu nazývaného $[7,4,3]$-Hammingův kód.
Byl jedním z vůbec prvních klasických kódů opravujících chyby, které byly kdy objeveny, a skládá se z těchto 16 binárních řetězců délky 7.
(Někdy se $[7,4,3]$-Hammingův kód chápe jako kód s těmito řetězci pozpátku, ale my budeme používat kód obsahující řetězce zobrazené zde.)

$$
\begin{array}{cccc}
0000000 & 1100001 & 1010010 & 0110011\\[1mm]
0110100 & 1010101 & 1100110 & 0000111\\[1mm]
1111000 & 0011001 & 0101010 & 1001011\\[1mm]
1001100 & 0101101 & 0011110 & 1111111
\end{array}
$$

Za výběrem těchto řetězců stojí velmi jednoduchá logika, ale to je vedlejší záležitost této lekce a nebude zde vysvětlena.
Prozatím stačí pozorovat, že se jedná o klasický lineární kód: XOR jakýchkoli dvou těchto řetězců vždy dá další řetězec v kódu.

Zápis $[7,4,3]$ (v jednoduchých hranatých závorkách) znamená něco analogického k zápisu ve dvojitých hranatých závorkách pro stabilizátorové kódy zmíněnému v předchozí lekci, ale zde jde o klasické lineární kódy.
Konkrétně kódová slova mají $7$ bitů, pomocí kódu můžeme zakódovat $4$ bity (protože je $16 = 2^4$ kódových slov) a jedná se o kód se vzdáleností $3,$ což znamená, že jakákoli dvě různá kódová slova se musí lišit alespoň ve $3$ pozicích — takže k přeměně jednoho kódového slova na jiné je třeba překlopit alespoň $3$ bity.
Skutečnost, že se jedná o kód se vzdáleností $3,$ implikuje, že dokáže opravit až jednu chybu překlopení bitu.

### Popis klasických lineárních kódů {#describing-classical-linear-codes}

Právě uvedené příklady jsou velmi jednoduché příklady klasických lineárních kódů, ale i $[7,4,3]$-Hammingův kód vypadá poněkud záhadně, když jsou kódová slova jednoduše vypsána.
Existují lepší a efektivnější způsoby popisu klasických lineárních kódů, včetně následujících dvou způsobů.

1. *Generátory*. Jedním ze způsobů popisu klasického lineárního kódu je minimální seznam kódových slov, který kód *generuje*, což znamená, že vzájemným XORováním všech možných podmnožin těchto kódových slov dostaneme celý kód.

   Podrobněji, řetězce $u_1,\ldots,u_m\in\Sigma^n$ generují klasický lineární kód $\mathcal{C},$ pokud

   $$
   \mathcal{C} = \bigl\{\alpha_1 u_1 \oplus \cdots \oplus \alpha_m u_m\,:\,\alpha_1,\ldots,\alpha_m\in\{0,1\}\bigr\},
   $$

   s tím, že $\alpha u = u$ když $\alpha = 1$ a $\alpha u = 0^n$ když $\alpha = 0,$
   a říkáme, že tento seznam je *minimální*, pokud odstraněním jednoho z řetězců dostaneme menší kód.
   Přirozený způsob, jak o takovém popisu přemýšlet, je ten, že kolekce $\{u_1,\ldots,u_m\}$ tvoří *bázi* pro $\mathcal{C}$ jako podprostor, kde si řetězce představujeme jako vektory s binárními hodnotami složek, přičemž máme na paměti, že pracujeme ve vektorovém prostoru, kde se aritmetika provádí modulo $2.$

2. *Kontroly parity*.
   Dalším přirozeným způsobem popisu klasického lineárního kódu jsou *kontroly parity* — tedy minimální seznam binárních řetězců, pro které jsou řetězce v kódu právě ty, jejichž *binární skalární součin* s každým z těchto řetězců kontroly parity je nula. (Podobně jako bitový exkluzivní-OR se binární skalární součin několikrát objevil v „Fundamentals of quantum algorithms".)

   To znamená, že řetězce $v_1,\ldots,v_r\in\Sigma^n$ jsou řetězce kontroly parity pro klasický lineární kód $\mathcal{C},$ pokud

   $$
   \mathcal{C} = \bigl\{ u\in \Sigma^n\,:\, u\cdot v_1 = \cdots = u \cdot v_r = 0 \bigr\},
   $$

   a tato množina řetězců je *minimální*, pokud odstraněním jednoho dostaneme větší kód.
   Nazývají se řetězce kontroly parity, protože $u$ má binární skalární součin rovný nule s $v$ právě tehdy, když bity $u$ na pozicích, kde $v$ má jedničky, mají sudou paritu.
   Takže pro určení, zda řetězec $u$ patří do kódu $\mathcal{C},$ stačí zkontrolovat paritu určitých podmnožin bitů $u.$

   Důležité je si zde všimnout, že binární skalární součin není ve formálním smyslu vnitřním součinem. Zejména když dva řetězce mají binární skalární součin rovný nule, neznamená to, že jsou ortogonální obvyklým způsobem, jakým o ortogonalitě přemýšlíme. Například binární skalární součin řetězce $11$ se sebou samým je nula — takže je možné, že řetězec kontroly parity pro klasický lineární kód je sám v kódu obsažen.

Klasické lineární kódy nad binární abecedou vždy obsahují počet řetězců, který je mocninou $2$ — a pro jeden klasický lineární kód popsaný dvěma právě uvedenými způsoby bude vždy platit $n = m + r.$
Konkrétně, pokud máme minimální množinu $m$ generátorů, pak kód kóduje $m$ bitů a nutně budeme mít $2^m$ kódových slov;
a pokud máme minimální množinu $r$ řetězců kontroly parity, budeme mít $2^{n-r}$ kódových slov.
Takže každý generátor zdvojnásobí velikost kódového prostoru, zatímco každý řetězec kontroly parity zmenší velikost kódového prostoru na polovinu.

Například 3-bitový opakovací kód je lineární kód, takže jej lze popsat oběma těmito způsoby.
Konkrétně existuje jediná volba generátoru, která funguje: $111.$
Alternativně můžeme kód popsat dvěma řetězci kontroly parity, například $110$ a $011$ — což by mělo vypadat povědomě z našich předchozích diskusí o tomto kódu — nebo bychom mohli vzít řetězce kontroly parity $110$ a $101,$ nebo $101$ a $011.$ (Generátory a řetězce kontroly parity obecně nejsou pro daný klasický lineární kód jednoznačné.)

Jako druhý příklad uvažuj $[7,4,3]$-Hammingův kód.
Zde je jedna volba seznamu generátorů, která funguje.

$$
\begin{array}{c}
1111000\\[1mm]
0110100\\[1mm]
1010010\\[1mm]
1100001
\end{array}
$$

A zde je volba seznamu kontrol parity pro tento kód.

$$
\begin{array}{c}
1111000\\[1mm]
1100110\\[1mm]
1010101
\end{array}
$$

Zde mimochodem vidíme, že *všechny* naše řetězce kontroly parity jsou samy obsaženy v kódu.

Jedna závěrečná poznámka o klasických lineárních kódech, která je propojuje se stabilizátorovým formalismem, je ta, že řetězce kontroly parity jsou ekvivalentní stabilizátorovým generátorům, které se skládají pouze z $Z$ a jednotkových Pauliho matic.
Například řetězce kontroly parity $110$ a $011$ pro 3-bitový opakovací kód odpovídají přesně stabilizátorovým generátorům $Z\otimes Z\otimes \mathbb{I}$ a $\mathbb{I}\otimes Z\otimes Z,$ což je v souladu s diskusemi o Pauliho observáblech z předchozí lekce.

## Definice CSS kódů {#definition-of-css-codes}

CSS kódy jsou stabilizátorové kódy získané kombinací určitých *párů* klasických lineárních kódů.
Toto nefunguje pro dva libovolné klasické lineární kódy — tyto dva kódy musí mít určitý vzájemný vztah.
Přesto tato konstrukce otevírá mnoho možností pro kvantové kódy opravující chyby, založených částečně na více než 75 letech klasické teorie kódování.

Ve stabilizátorovém formalismu jsou stabilizátorové generátory obsahující pouze $Z$ a jednotkové Pauliho matice ekvivalentní kontrolám parity, jak jsme právě pozorovali u 3-bitového opakovacího kódu.
Jako další příklad uvažuj následující řetězce kontroly parity pro $[7,4,3]$-Hammingův kód.

$$
\begin{array}{c}
1111000\\[1mm]
1100110\\[1mm]
1010101
\end{array}
$$
