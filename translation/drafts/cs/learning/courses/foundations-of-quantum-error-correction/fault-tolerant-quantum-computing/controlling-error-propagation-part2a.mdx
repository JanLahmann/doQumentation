
Začněme tím, že se podíváme na Gate $S$ a $T$, které mají následující maticový popis.

$$
S = \begin{pmatrix}
1 & 0\\
0 & i
\end{pmatrix}
= \begin{pmatrix}
1 & 0\\
0 & e^{i\pi/2}
\end{pmatrix}
\qquad\text{and}\qquad
T = \begin{pmatrix}
1 & 0\\
0 & \frac{1+i}{\sqrt{2}}
\end{pmatrix}
= \begin{pmatrix}
1 & 0\\
0 & e^{i\pi/4}
\end{pmatrix}
$$

Podle definice je $S$ Cliffordova operace, zatímco $T$ není;
Gate $T$ není možné implementovat pomocí Circuit složeného z Cliffordových Gate (Gate $H$, Gate $S$ a CNOT Gate).

Nicméně je možné implementovat Gate $T$ (až na globální fázi) pomocí Circuit složeného z Cliffordových Gate, pokud navíc máme k dispozici kopii stavu

$$
T\vert {+} \rangle = \frac{1}{\sqrt{2}} \vert 0 \rangle + \frac{e^{i\pi/4}}{\sqrt{2}} \vert 1\rangle,
$$

a pokud povolíme měření ve standardní bázi a klasicky řízené Gate.
Konkrétně následující Circuit ukazuje jeden způsob, jak toho dosáhnout.
Jev, který zde vidíme, je poněkud zjednodušený příklad *teleportace kvantového Gate*.

![A circuit diagram depicting magic state injection](/learning/images/courses/foundations-of-quantum-error-correction/fault-tolerant-quantum-computing/magic-state-injection.svg)

Abychom ověřili, že tento Circuit funguje správně, můžeme nejprve spočítat účinek CNOT Gate na vstup.

$$
T \vert {+} \rangle \otimes \vert\psi\rangle
\stackrel{\text{CNOT}}{\longmapsto}
\frac{1}{\sqrt{2}} \vert 0\rangle \otimes T \vert \psi\rangle + \frac{1+i}{2} \vert 1\rangle \otimes T^{\dagger} \vert \psi\rangle
$$

Měření proto dává výsledky $0$ a $1$ se stejnou pravděpodobností.
Pokud je výsledek $0,$ Gate $S$ se neprovede a výstupní stav je $T\vert\psi\rangle;$ a
pokud je výsledek $1,$ Gate $S$ se provede a výstupní stav je $ST^{\dagger}\vert\psi\rangle = T\vert \psi\rangle.$

Stav $T\vert {+}\rangle$ se v tomto kontextu nazývá *magický stav*, i když není v tomto ohledu jedinečný: i jiné stavy se nazývají magické stavy, když je lze použít podobným způsobem (případně pro jiné Gate a s použitím jiných Circuit).
Například záměnou stavu $T\vert{+}\rangle$ za stav $S\vert{+}\rangle$ a nahrazením Gate $S$ ve výše uvedeném Circuit za Gate $Z$ implementujeme Gate $S$ — což je potenciálně užitečné pro výpočet odolný proti chybám pomocí kódu, pro který nelze Gate $S$ implementovat transversálně.

### Gadgety odolné proti chybám z magických stavů {#fault-tolerant-gadgets-from-magic-states}

Nemusí být zřejmé, že použití magických stavů k implementaci Gate je užitečné pro odolnost proti chybám.
Například u výše popsané implementace Gate $T$ se zdá, že stále potřebujeme aplikovat Gate $T$ na stav $\vert{+}\rangle$, abychom získali magický stav, který pak použijeme k implementaci Gate $T$.
Jaká je tedy výhoda použití tohoto přístupu pro odolnost proti chybám?

Zde jsou tři klíčové body, které na tuto otázku odpovídají.

1. Vytváření magických stavů nevyžaduje aplikaci Gate, který se snažíme implementovat, na konkrétní stav. Například aplikace Gate $T$ na stav $\vert {+} \rangle$ není jediný způsob, jak získat stav $T\vert{+}\rangle$.

2. Vytváření magických stavů lze provádět odděleně od výpočtu, ve kterém se používají. To znamená, že chyby, které vzniknou v procesu vytváření magického stavu, se nebudou šířit do samotného prováděného výpočtu.

3. Pokud lze jednotlivé Gate v Circuit implementujícím zvolený Gate pomocí magického stavu implementovat odolně proti chybám a předpokládáme dostupnost magických stavů, získáme implementaci zvoleného Gate odolnou proti chybám.

Pro zjednodušení následující diskuse se zaměřme konkrétně na Gate $T$ — s tím, že metodologii lze rozšířit i na jiné Gate.
Implementace Gate $T$ odolná proti chybám pomocí magických stavů má podobu naznačenou následujícím obrázkem.

![A circuit diagram depicting magic state injection on an encoded qubit](/learning/images/courses/foundations-of-quantum-error-correction/fault-tolerant-quantum-computing/encoded-magic-state-injection.svg)
