$$
\operatorname{dim}(\mathcal{C}) =
\operatorname{Tr}(\Pi_1\cdots\Pi_r) =
\frac{1}{2^r} \sum_{a_1,\ldots,a_r \in \{0,1\}} \operatorname{Tr}(P_1^{a_1}\cdots P_r^{a_r})
$$

Tento výraz můžeme vyhodnotit pomocí několika základních faktů.

- Máme $P_1^0 \cdots P_r^0 = \mathbb{I}^{\otimes n}$, a tedy

  $$
  \operatorname{Tr}(P_1^{0}\cdots P_r^{0}) = 2^n.
  $$

- Pro $(a_1,\ldots,a_r) \neq (0,\ldots,0)$ musí být součin $P_1^{a_1}\cdots P_r^{a_r}$ roven $\pm 1$ krát nějaká Pauliho operace — nemůžeme ale získat $\mathbb{I}^{\otimes n}$, protože by to bylo v rozporu s minimalitou množiny $\{P_1,\ldots,P_r\}$, a nemůžeme získat $-\mathbb{I}^{\otimes n}$, protože to zakazuje třetí podmínka na generátory stabilizéru. Proto, jelikož stopa každé neidentické Pauliho operace je nulová, dostáváme

  $$
  \operatorname{Tr}(P_1^{a_1}\cdots P_r^{a_r}) = 0.
  $$

Dimenze kódového prostoru je tedy $2^{n-r}$, jak bylo tvrzeno:

$$
\begin{aligned}
\operatorname{dim}(\mathcal{C}) & =
\frac{1}{2^r} \sum_{a_1,\ldots,a_r \in \{0,1\}} \operatorname{Tr}(P_1^{a_1}\cdots P_r^{a_r}) \\
& = \frac{1}{2^r} \operatorname{Tr}(P_1^{0}\cdots P_r^{0}) \\
& = 2^{n-r}.
\end{aligned}
$$

Na okraj poznamenejme, že nyní vidíme, že předpoklad, že $-\mathbb{I}^{\otimes n}$ není obsažen ve stabilizéru, implikuje, že kódový prostor musí obsahovat alespoň jeden kvantový stavový vektor.
Je to proto, že, jak jsme právě ověřili, tento předpoklad implikuje, že kódový prostor má dimenzi $2^{n-r}$, která nemůže být nulová.
Opačná implikace je triviální: pokud $-\mathbb{I}^{\otimes n}$ je obsažen ve stabilizéru, pak kódový prostor nemůže obsahovat žádné kvantové stavové vektory, protože tato operace nemá žádné nenulové fixní vektory.

## Cliffordovy operace a kódování {#clifford-operations-and-encodings}

Dále stručně probereme, jak lze Qubit kódovat pomocí stabilizérových kódů, ale nejprve musíme zavést *Cliffordovy operace*.

### Cliffordovy operace {#clifford-operations}

Cliffordovy operace jsou unitární operace na libovolném počtu Qubitů, které lze implementovat kvantovými Circuit s omezenou sadou Gate:
- Hadamardovy Gate
- $S$ Gate
- CNOT Gate

Všimni si, že $T$ Gate nejsou zahrnuty, stejně jako Toffoliho Gate a Fredkinovy Gate.
Tyto Gate nejsou jen nezahrnuty v seznamu, ale ve skutečnosti je nelze implementovat pomocí zde uvedených Gate; nejsou to Cliffordovy operace.
Pauliho operace naproti tomu Cliffordovy operace jsou, protože je lze implementovat pomocí sekvencí Hadamardových a $S$ Gate.

To je jednoduchý způsob, jak definovat Cliffordovy operace, ale nevysvětluje to, proč jsou definovány právě takto ani co je na této konkrétní kolekci Gate zvláštního.
*Skutečný* důvod, proč jsou Cliffordovy operace definovány právě takto, je ten, že až na globální fázové faktory jsou Cliffordovy operace přesně ty unitární operace, které vždy transformují Pauliho operace na Pauliho operace pomocí konjugace.
Přesněji řečeno, $n$-Qubitová unitární operace $U$ je ekvivalentní Cliffordově operaci až na fázový faktor právě tehdy, když pro *každou* $n$-Qubitovou Pauliho operaci $P$ platí

$$
U P U^{\dagger} = \pm Q
$$

pro nějakou $n$-Qubitovou Pauliho operaci $Q.$

(Poznamenejme, že není možné mít $U P U^{\dagger} = \alpha Q$ pro $\alpha\notin\{+1,-1\}$, když $U$ je unitární a $P$ a $Q$ jsou Pauliho operace.
Vyplývá to z faktu, že matice na levé straně příslušné rovnice je unitární i hermitovská, a $+1$ a $-1$ jsou jediné volby pro $\alpha$, které umožňují, aby pravá strana byla rovněž unitární a hermitovská.)

Konjugační vlastnost právě popsanou je snadné ověřit, když $U$ je Hadamardova, $S$ nebo CNOT Gate.
Zejména to je snadné pro Hadamardovy Gate,

$$
H X H^{\dagger} = Z, \qquad
H Y H^{\dagger} = -Y, \qquad
H Z H^{\dagger} = X,
$$

a $S$ Gate,

$$
S X S^{\dagger} = Y, \qquad
S Y S^{\dagger} = -X, \qquad
S Z S^{\dagger} = Z.
$$

Pro CNOT Gate existuje 15 neidentických Pauliho operací na dvou Qubitech, které je třeba ověřit.
Samozřejmě je lze ověřit jednotlivě — ale vztahy mezi CNOT Gate a $X$ a $Z$ Gate uvedené (ve formě Circuit) v předchozí lekci, spolu s pravidly násobení Pauliho matic, nabízejí zkratku ke stejnému závěru.

Jakmile víme, že tato konjugační vlastnost platí pro Hadamardovy, $S$ a CNOT Gate, můžeme okamžitě usoudit, že platí i pro *Circuit* složené z těchto Gate — to znamená pro všechny Cliffordovy operace.

Obtížnější je dokázat, že vztah funguje i v opačném směru, tedy že pokud daná unitární operace $U$ splňuje konjugační vlastnost pro Pauliho operace, pak ji musí být možné implementovat (až na globální fázi) pouze pomocí Hadamardových, $S$ a CNOT Gate.
To v této lekci vysvětlovat nebudeme, ale je to pravda.

Cliffordovy operace nejsou univerzální pro kvantový výpočet; na rozdíl od univerzálních sad kvantových Gate není možné aproximovat libovolné unitární operace na požadovanou úroveň přesnosti pomocí Cliffordových operací.
Pro danou hodnotu $n$ totiž existuje pouze *konečně* mnoho $n$-Qubitových Cliffordových operací (až na fázové faktory).
Provádění Cliffordových operací na stavech standardní báze následované měřením ve standardní bázi také neumožňuje provádět výpočty, které jsou mimo dosah klasických algoritmů — protože výpočty tohoto typu lze *efektivně simulovat* klasicky.
Tento fakt je znám jako *Gottesman-Knillův teorém*.

### Enkodéry pro stabilizérové kódy {#encoders-for-stabilizer-codes}

Stabilizérový kód definuje kódový prostor určité dimenze a máme svobodu tento kódový prostor používat, jak chceme —
nic nás nenutí kódovat Qubit do tohoto kódového prostoru konkrétním způsobem.
Vždy je však možné použít jako enkodér *Cliffordovu operaci*, pokud se tak rozhodneme.
Přesněji řečeno, pro jakýkoli stabilizérový kód, který umožňuje zakódovat $m$ Qubitů do $n$ Qubitů, existuje $n$-Qubitová Cliffordova operace $U$ taková, že pro jakýkoli $m$-Qubitový kvantový stavový vektor $\vert\phi\rangle$ platí, že

$$
\vert\psi\rangle = U \bigl(\vert 0^{n-m} \rangle \otimes \vert \phi\rangle\bigr)
$$

je kvantový stavový vektor v kódovém prostoru našeho kódu, který můžeme interpretovat jako kódování $\vert\phi\rangle.$

To je dobrá zpráva, protože Cliffordovy operace jsou relativně jednoduché ve srovnání s libovolnými unitárními operacemi a existují způsoby, jak optimalizovat jejich implementaci pomocí technik podobných těm, které se nacházejí v důkazu Gottesman-Knillova teorému.
Výsledkem je, že Circuit pro kódování stavů pomocí stabilizérových kódů nikdy nemusí být příliš velké.
Konkrétně je vždy možné provést kódování pro $n$-Qubitový stabilizérový kód pomocí Cliffordovy operace, která vyžaduje $O(n^2/\log(n))$ Gate.
Je to proto, že *každou* Cliffordovu operaci na $n$ Qubitech lze implementovat Circuit této velikosti.

Zde je například enkodér pro 7-Qubitový Steaneův kód.
Skutečně se jedná o Cliffordovu operaci a jak se ukazuje, tento enkodér ani nepotřebuje $S$ Gate.

![Cliffordův enkodér pro 7-Qubitový Steaneův kód](/learning/images/courses/foundations-of-quantum-error-correction/stabilizer-formalism/Steane-encoder.svg)

## Detekce chyb {#detecting-errors}

Pro $n$-Qubitový stabilizérový kód popsaný generátory stabilizéru $P_1,\ldots, P_r$ funguje detekce chyb následujícím způsobem.

K detekci chyb se měří všechny generátory stabilizéru jako pozorovatelné veličiny.
Máme $r$ generátorů stabilizéru, a tedy $r$ výsledků měření, přičemž každý z nich je $+1$ nebo $-1$ (nebo binární hodnota, pokud se rozhodneme přiřadit $0$ k $+1$ a $1$ k $-1$).
Těchto $r$ výsledků interpretujeme kolektivně, jako vektor nebo řetězec, jako *syndrom*.
Syndrom $(+1,\ldots,+1)$ indikuje, že nebyla detekována žádná chyba, zatímco alespoň jedna $-1$ kdekoli v syndromu indikuje, že chyba byla detekována.

Předpokládejme konkrétně, že $E$ je $n$-Qubitová Pauliho operace reprezentující hypotetickou chybu.
(Jako chyby uvažujeme pouze Pauliho operace, protože diskretizace chyb funguje pro libovolné stabilizérové kódy stejně jako pro 9-Qubitový Shorův kód.)
Existují tři případy, které určují, zda je $E$ detekována jako chyba, či nikoli.

### Případy detekce chyb {#error-detection-cases}

1. Operace $E$ je úměrná prvku stabilizéru.

   $$
   E = \pm Q \; \text{for some}\; Q \in \langle P_1,\ldots,P_r\rangle
   $$

   V tomto případě $E$ musí komutovat s každým generátorem stabilizéru, takže dostáváme syndrom $(+1,\ldots,+1).$ To znamená, že $E$ není detekována jako chyba.

2. Operace $E$ není úměrná prvku stabilizéru, ale přesto komutuje s každým generátorem stabilizéru.

   $$
   E\neq \pm Q\; \text{for} \; Q \in \langle P_1,\ldots,P_r\rangle, \;\text{but}\;
   E P_k = P_k E \;\text{for every}\; k\in\{1,\ldots,r\}
   $$

   Jedná se o chybu, která mění vektory v kódovém prostoru nějakým netriviálním způsobem. Protože ale $E$ komutuje s každým generátorem stabilizéru, syndrom je $(+1,\ldots,+1),$ takže $E$ zůstane kódem nedetekována.

3. Operace $E$ antikomutuje s alespoň jedním generátorem stabilizéru.

   $$
   P_k E = -E P_k \; \text{for at least one} \; k\in\{1,\ldots,r\}
   $$
