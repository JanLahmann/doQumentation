### Otestuj své znalosti {#check-your-understanding}
Přečti si otázku níže, zamysli se nad odpovědí a pak klikni na trojúhelník, abys odhalil řešení.
<details>
<summary>

Vypočítej neurčitost $X$ ve stavu $|+\rangle_y = |+i\rangle$ ručně.

</summary>

__Odpověď:__

$$
\Delta X =\sqrt{\langle+i| X^2 |+i\rangle - \langle+i| X |+i\rangle^2}
$$
V daném stavu to dává:
$$
\begin{aligned}
\Delta X & =\sqrt{\frac{1}{\sqrt{2}}\begin{pmatrix}1 & -i\end{pmatrix} \begin{pmatrix}0 & 1 \\ 1 & 0\end{pmatrix} \begin{pmatrix}0 & 1 \\ 1 & 0\end{pmatrix} \frac{1}{\sqrt{2}}\begin{pmatrix}1 \\ i\end{pmatrix} - \left(\frac{1}{\sqrt{2}}\begin{pmatrix}1 & -i\end{pmatrix} \begin{pmatrix}0 & 1 \\ 1 & 0\end{pmatrix} \frac{1}{\sqrt{2}}\begin{pmatrix}1 \\ i\end{pmatrix}\right)^2}\\

\Delta X & =\sqrt{\frac{1}{2}\begin{pmatrix}1 & -i\end{pmatrix} \begin{pmatrix}1 & 0 \\ 0 & 1\end{pmatrix} \begin{pmatrix}1 \\ i\end{pmatrix} - \left(\frac{1}{2}\begin{pmatrix}1 & -i\end{pmatrix} \begin{pmatrix}i \\ 1\end{pmatrix}\right)^2}\\

\Delta X & =\sqrt{\frac{1}{2}\begin{pmatrix}1 & -i\end{pmatrix} \begin{pmatrix}1 \\ i\end{pmatrix} - \left(\frac{1}{2}(0))\right)^2}\\

\Delta X & =\sqrt{\frac{1}{2}(2)} = 1
\end{aligned}
$$

</details>

Libovolný počáteční stav můžeme vytvořit pomocí ```qc.initialize()```. Všimni si, že syntaxe pro imaginární jednotku je zde $1j$.

```python
# Krok 1: Namapuj problém do kvantového obvodu

from qiskit.quantum_info import SparsePauliOp
import numpy as np

obs = SparsePauliOp("X")

# Definuj registry
qr = QuantumRegister(1, "q")
cr = ClassicalRegister(1, "c")
qc = QuantumCircuit(qr, cr)

# Inicializuj stav
qc.initialize([1, 1j] / np.sqrt(2))

# Krok 2: Transpiluj Circuit

pm = generate_preset_pass_manager(target=target, optimization_level=3)
qc_isa = pm.run(qc)
obs_isa = obs.apply_layout(layout=qc_isa.layout)

# Krok 3: Spusť Circuit na skutečném kvantovém počítači

estimator = Estimator(mode=backend)
pubs = [(qc_isa, obs_isa)]
job = estimator.run([[qc_isa, obs_isa]])
res = job.result()

# Spusť úlohu na simulátoru Aer s modelem šumu ze skutečného Backend
# job = noisy_estimator.run([[qc_isa,obs_isa]])
# res=job.result()

# Krok 4: Vrať výsledek v klasické podobě a analyzuj ho.

print(res[0].data.evs)
```

```text
-0.02408454165642664
```

Podle naší rovnice výše,
$$(\Delta X)^2 = 1 - \langle X \rangle^2 = 1-(0.0015...)^2 \rightarrow \Delta X = 0.999...$$
Zůstaňme u stejného stavu, ale tentokrát zjistíme střední hodnotu $Z$:

```python
# Krok 1: Namapuj problém do kvantového obvodu

obs = SparsePauliOp("Z")

# Definuj registry
qr = QuantumRegister(1, "q")
cr = ClassicalRegister(1, "c")
qc = QuantumCircuit(qr, cr)

# Inicializuj stav na |+>_y
qc.initialize([1, 1j] / np.sqrt(2))

# Krok 2: Transpiluj Circuit

pm = generate_preset_pass_manager(target=target, optimization_level=3)
qc_isa = pm.run(qc)
obs_isa = obs.apply_layout(layout=qc_isa.layout)

# Krok 3: Spusť Circuit na skutečném kvantovém počítači

estimator = Estimator(mode=backend)
pubs = [(qc_isa, obs_isa)]
job = estimator.run(pubs)
res = job.result()

# Spusť úlohu na simulátoru Aer s modelem šumu ze skutečného Backend
# job = noisy_estimator.run([[qc_isa,obs_isa]])
# res=job.result()

# Krok 4: Vrať výsledek v klasické podobě a analyzuj ho.

print(res[0].data.evs)
```

```text
0.04958271968581247
```

Mohli bychom provést stejné výpočty jako předtím, ale viděli bychom, že rozptyl je opět velmi blízký 1,0. Mohli bychom tedy konstatovat, že $\Delta X \Delta Z \approx 1{,}0$. Pro námi zvolený stav to skutečně přibližně platí. Ale lze toho dosáhnout lépe? Nebo hůře?

Vzpomeň si, že mezi polohou v jednom směru, $x,$ a hybností ve stejném směru, $p_x,$ platí relace neurčitosti. Pro tyto veličiny je nejznámější tvar pravděpodobně
$$\Delta x \Delta p_x \geq \hbar/2$$
Pokud si pamatujeme jen tohle, mohli bychom být v pokušení myslet si, že $\Delta X$ a $\Delta Z$ mají také takové fundamentální omezení neurčitosti. Možná je nemožné, aby součin $\Delta X \Delta Z$ dosáhl nuly? Zkusme jiný stav a uvidíme, zda to platí. Tentokrát použijeme $|\psi\rangle = \frac{1}{\sqrt{2}}\begin{pmatrix} 1 \\ 1 \end{pmatrix}.$ Podívejme se, co se stane. Všimni si, že v níže uvedeném kódu může Estimator přijmout dvě sady obvodů a pozorovatelných veličin ve stejném odeslaném jobu.

```python
# Krok 1: Namapuj problém do kvantového obvodu

obs1 = SparsePauliOp("X")
obs2 = SparsePauliOp("Z")

# Definuj registry

qr = QuantumRegister(1, "q")
cr = ClassicalRegister(1, "c")
qc = QuantumCircuit(qr, cr)

# Inicializuj stav
qc.initialize([1, 1] / np.sqrt(2))

# Krok 2: Transpiluj Circuit

pm = generate_preset_pass_manager(target=target, optimization_level=3)
qc_isa = pm.run(qc)
obs1_isa = obs1.apply_layout(layout=qc_isa.layout)
obs2_isa = obs2.apply_layout(layout=qc_isa.layout)

# Krok 3: Spusť Circuit na skutečném kvantovém počítači

with Batch(backend=backend) as batch:
    estimator = Estimator(mode=batch)
    pubs = [(qc_isa, obs1_isa), (qc_isa, obs2_isa)]
    job = estimator.run(pubs)
    res = job.result()
batch.close()

# Spusť úlohu na simulátoru Aer s modelem šumu ze skutečného Backend

# job = noisy_estimator.run([[qc,obs1],[qc,obs2]])
# res=job.result()

# Krok 4: Vrať výsledek v klasické podobě a analyzuj ho.

print("Střední hodnota první pozorovatelné veličiny je: ", res[0].data.evs)
print("Střední hodnota druhé pozorovatelné veličiny je: ", res[1].data.evs)
```

```text
The expectation value of the first observable is:  1.0011036174126302
The expectation value of the second observable is:  0.0029429797670141016
```

Střední hodnota $X$ by měla být blízká 1,0, ale neměla by překročit 1,0. Neznepokojuj se, pokud ji překročí o velmi malou částku. To lze přičíst faktorům, jako je šum nebo chyba čtení. Ačkoli se jedná o velmi důležité téma, prozatím ho můžeme ignorovat.

Získali jsme střední hodnotu $X$ velmi blízkou 1,0 (což odpovídá velmi nízkému rozptylu pro $X$). Díky tomu je součin obou rozptylů poměrně malý:

$$
\Delta X \Delta Z = \sqrt{1-(0.9853)^2} \times \sqrt{1-(-0.00195)^2} = 0.171.
$$

Přestože to není přesně nula, tato hodnota je ve srovnání s vlastními hodnotami Pauliho operátorů ($\pm 1$) malá. Možná si vzpomínáš, že relaci neurčitosti mezi lineární polohou a hybností lze zapsat jinak — s explicitním použitím komutátoru operátorů $x$ a $p_x$:

$$
\Delta x \Delta p_x \geq \frac{1}{2}|\langle [x,p_x] \rangle|
$$

kde

$$
[x,p_x] = xp_x-p_xx
$$

je komutátor $x$ a $p_x$.

Tento tvar lze nejsnáze rozšířit na Pauliho operátory. Obecně platí pro dva operátory $A$ a $B$:
$$
\Delta A \Delta B \geq \frac{1}{2}|\langle [A,B] \rangle|.
$$
A v případě Pauliho matic $X$ a $Z$ potřebujeme $[X,Z]$, abychom mohli vypočítat
$$
\Delta X \Delta Z \geq \frac{1}{2}|\langle [X,Z] \rangle|.
$$
Ukážeme to zde a podobné výpočty necháváme čtenáři jako cvičení:
$$
[X,Z] = XZ-ZX = \begin{pmatrix}0 & 1 \\ 1 & 0\end{pmatrix}\begin{pmatrix}1 & 0 \\ 0 & -1\end{pmatrix}-\begin{pmatrix}1 & 0 \\ 0 & -1\end{pmatrix}\begin{pmatrix}0 & 1 \\ 1 & 0\end{pmatrix}
$$
$$
[X,Z] = \begin{pmatrix}0 & -1 \\ 1 & 0\end{pmatrix}-\begin{pmatrix}0 & 1 \\ -1 & 0\end{pmatrix} = 2\begin{pmatrix}0 & -1 \\ 1 & 0\end{pmatrix}
$$
Toto je zcela přijatelná odpověď, ale jedním dalším krokem vidíme

$$
[X,Z] = -2i\begin{pmatrix}0 & -i \\ i & 0\end{pmatrix}=-2iY
$$

Naše relace neurčitosti se tak stává

$$
\Delta X \Delta Z \geq |\langle Y \rangle|.
$$
