---
title: "Třída Operator"
sidebar_label: "Třída Operator"
description: "Podrobné vysvětlení použití třídy Operator v Qiskitu."
notebook_path: "guides/operator-class.ipynb"
---

{/* doqumentation-source-hash: f33a6b98 */}

<OpenInLabBanner notebookPath="guides/operator-class.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>Verze balíčků</b></summary>

Kód na této stránce byl vyvinut s použitím následujících požadavků.
Doporučujeme používat tyto nebo novější verze.

```
qiskit[all]~=2.3.0
```
</details>
Tato stránka ukazuje, jak používat třídu [`Operator`](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator). Pro přehled reprezentací operátorů v Qiskitu na vysoké úrovni, včetně třídy `Operator` a dalších, viz [Přehled tříd operátorů](./operators-overview).

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit
```

```python
import numpy as np
from qiskit.circuit import QuantumCircuit
from qiskit.circuit.library import CXGate, RXGate, XGate
from qiskit.quantum_info import Operator, Pauli, process_fidelity
```

## Převod tříd na Operators {#convert-classes-to-operators}

Několik dalších tříd v Qiskitu lze přímo převést na objekt `Operator` pomocí metody inicializace operátoru. Například:

* objekty `Pauli`
* objekty `Gate` a `Instruction`
* objekty `QuantumCircuit`

Všimni si, že poslední bod znamená, že třídu `Operator` lze použít jako unitární simulátor pro výpočet výsledné unitární matice kvantového Circuit, aniž bys musel/a volat Backend simulátoru. Pokud Circuit obsahuje nepodporované operace, je vyvolána výjimka. Nepodporované operace jsou: measure, reset, podmíněné operace nebo Gate, která nemá definici matice ani rozklad pomocí Gates s definicemi matic.

```python
# Create an Operator from a Pauli object

pauliXX = Pauli("XX")
Operator(pauliXX)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j],
          [1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an Operator for a Gate object
Operator(CXGate())
```

```text
Operator([[1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an operator from a parameterized Gate object
Operator(RXGate(np.pi / 2))
```

```text
Operator([[0.70710678+0.j        , 0.        -0.70710678j],
          [0.        -0.70710678j, 0.70710678+0.j        ]],
         input_dims=(2,), output_dims=(2,))
```

```python
# Create an operator from a QuantumCircuit object
circ = QuantumCircuit(10)
circ.h(0)
for j in range(1, 10):
    circ.cx(j - 1, j)

# Convert circuit to an operator by implicit unitary simulation
Operator(circ)
```

```text
Operator([[ 0.70710678+0.j,  0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          ...,
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.70710678+0.j, -0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j]],
         input_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2), output_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2))
```

## Použití Operators v Circuits {#use-operators-in-circuits}

Unitární `Operators` lze přímo vkládat do `QuantumCircuit` pomocí metody `QuantumCircuit.append`. Tím se `Operator` převede na objekt `UnitaryGate`, který je přidán do Circuit.

Pokud operátor není unitární, je vyvolána výjimka. To lze zkontrolovat pomocí funkce `Operator.is_unitary()`, která vrátí `True`, pokud je operátor unitární, a `False` v opačném případě.

```python
# Create an operator
XX = Operator(Pauli("XX"))

# Add to a circuit
circ = QuantumCircuit(2, 2)
circ.append(XX, [0, 1])
circ.measure([0, 1], [0, 1])
circ.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/operator-class/extracted-outputs/223c07c9-1b1d-4a33-a397-091cac2ec9e3-0.svg)

Všimni si, že v předchozím příkladu je operátor inicializován z objektu `Pauli`. Nicméně objekt `Pauli` lze také přímo vložit do Circuit samotného a bude převeden na posloupnost jednoqubitových Pauli gates:

```python
# Add to a circuit
circ2 = QuantumCircuit(2, 2)
circ2.append(Pauli("XX"), [0, 1])
circ2.measure([0, 1], [0, 1])
circ2.draw()
```

```text
┌────────────┐┌─┐   
q_0: ┤0           ├┤M├───
     │  Pauli(XX) │└╥┘┌─┐
q_1: ┤1           ├─╫─┤M├
     └────────────┘ ║ └╥┘
c: 2/═══════════════╩══╩═
                    0  1
```

## Kombinování Operators {#combine-operators}

Operators lze kombinovat několika způsoby.

### Tenzorový součin {#tensor-product}

Dva operátory $A$ a $B$ lze zkombinovat do tenzorového součinu $A\otimes B$ pomocí funkce `Operator.tensor`. Všimni si, že pokud jsou $A$ i $B$ jednoqubitové operátory, pak `A.tensor(B)` = $A\otimes B$ bude mít subsystémy indexované jako matice $B$ na subsystému 0 a matice $A$ na subsystému 1.

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.tensor(B)
```

```text
Operator([[ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j],
          [ 0.+0.j, -0.+0.j,  0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### Tenzorové rozvinutí {#tensor-expansion}

Úzce příbuznou operací je `Operator.expand`, která funguje jako tenzorový součin, ale v opačném pořadí. Pro dva operátory $A$ a $B$ platí `A.expand(B)` = $B\otimes A$, kde subsystémy jsou indexovány jako matice $A$ na subsystému 0 a matice $B$ na subsystému 1.

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.expand(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j,  0.+0.j, -0.+0.j, -1.+0.j],
          [ 0.+0.j,  0.+0.j, -1.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### Kompozice {#composition}

Dva operátory $A$ a $B$ lze také skládat pro provádění maticového násobení pomocí metody `Operator.compose`. `A.compose(B)` vrátí operátor s maticí $B.A$:

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j],
          [-1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

Lze také skládat v opačném pořadí aplikací $B$ před $A$ pomocí argumentu `front` metody `compose`: `A.compose(B, front=True)` = $A.B$:

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B, front=True)
```

```text
Operator([[ 0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

### Kompozice subsystémů {#subsystem-composition}

Všimni si, že předchozí kompozice vyžaduje, aby celková výstupní dimenze prvního operátoru $A$ byla rovna celkové vstupní dimenzi skládaného operátoru $B$ (a podobně výstupní dimenze $B$ musí být rovna vstupní dimenzi $A$ při skládání s `front=True`).

Lze také skládat menší operátor s výběrem subsystémů na větším operátoru pomocí argumentu `qargs` metody `compose`, a to s `front=True` i bez něj. V tomto případě musí relevantní vstupní a výstupní dimenze skládaných subsystémů souhlasit. *Všimni si, že menší operátor musí být vždy argumentem metody* `compose`*.*

Například pro složení dvouqubitového Gate s tříqubitovým operátorem:

```python
# Compose XZ with a 3-qubit identity operator
op = Operator(np.eye(2**3))
XZ = Operator(Pauli("XZ"))
op.compose(XZ, qargs=[0, 2])
```

```text
Operator([[ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
           -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

```python
# Compose YX in front of the previous operator
op = Operator(np.eye(2**3))
YX = Operator(Pauli("YX"))
op.compose(YX, qargs=[0, 2], front=True)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j],
          [0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

### Lineární kombinace {#linear-combinations}

Operators lze také kombinovat pomocí standardních lineárních operátorů pro sčítání, odčítání a skalární násobení komplexními čísly.

```python
XX = Operator(Pauli("XX"))
YY = Operator(Pauli("YY"))
ZZ = Operator(Pauli("ZZ"))

op = 0.5 * (XX + YY - 3 * ZZ)
op
```

```text
Operator([[-1.5+0.j,  0. +0.j,  0. +0.j,  0. +0.j],
          [ 0. +0.j,  1.5+0.j,  1. +0.j,  0. +0.j],
          [ 0. +0.j,  1. +0.j,  1.5+0.j,  0. +0.j],
          [ 0. +0.j,  0. +0.j,  0. +0.j, -1.5+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

Důležitou poznámkou je, že zatímco `tensor`, `expand` a `compose` zachovávají unitaritu unitárních operátorů, lineární kombinace to nezaručují; sčítání dvou unitárních operátorů obecně vede k neunitárnímu operátoru:

```python
op.is_unitary()
```

```text
False
```

### Implicitní převod na Operators {#implicit-conversion-to-operators}

Všimni si, že pro všechny následující metody platí, že pokud druhý objekt již není objektem `Operator`, je metodou implicitně převeden. To znamená, že matice lze předávat přímo, aniž by bylo nutné je explicitně převádět na `Operator`. Pokud převod není možný, je vyvolána výjimka.

```python
# Compose with a matrix passed as a list
Operator(np.eye(2)).compose([[0, 1], [1, 0]])
```

```text
Operator([[0.+0.j, 1.+0.j],
          [1.+0.j, 0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

## Porovnání Operators {#compare-operators}

Operators implementují metodu rovnosti, kterou lze použít ke kontrole, zda jsou dva operátory přibližně stejné.

```python
Operator(Pauli("X")) == Operator(XGate())
```

```text
True
```

Všimni si, že tato kontrola ověřuje, zda je každý prvek matice operátorů přibližně stejný; dva unitární operátory, které se liší o globální fázi, nejsou považovány za stejné:

```python
Operator(XGate()) == np.exp(1j * 0.5) * Operator(XGate())
```

```text
False
```

### Process fidelity {#process-fidelity}

Operators lze také porovnávat pomocí funkce `process_fidelity` z modulu *Quantum Information*. Jde o informačně-teoretickou veličinu vyjadřující, jak blízko jsou si dva kvantové kanály, a v případě unitárních operátorů nezávisí na globální fázi.

```python
# Two operators which differ only by phase
op_a = Operator(XGate())
op_b = np.exp(1j * 0.5) * Operator(XGate())

# Compute process fidelity
F = process_fidelity(op_a, op_b)
print("Process fidelity =", F)
```

```text
Process fidelity = 1.0
```

Všimni si, že process fidelity je obecně platnou mírou blízkosti pouze tehdy, pokud jsou vstupní operátory unitární (nebo CP v případě kvantových kanálů), a pokud vstupy nejsou CP, je vyvolána výjimka.
## Další kroky {#next-steps}

<Admonition type="tip" title="Doporučení">
  - Prozkoumej referenci [Operator API](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator#operator).
</Admonition>
