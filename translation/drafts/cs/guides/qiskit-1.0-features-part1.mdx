---
title: Průvodce migrací funkcí Qiskit 1.0
description: Popis změn funkcí zavedených v Qiskit 1.0 a jak aktualizovat svůj kód, aby s nimi fungoval.
---

{/* doqumentation-source-hash: 424ae580 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore nbqa, ipynb, tqdm */}

# Změny funkcí v Qiskit 1.0

Tento průvodce popisuje migrační cesty pro nejdůležitější změny funkcí v Qiskit 1.0, uspořádané podle modulů.
Pro přechod na modul, který tě zajímá, použij obsah na pravé straně.

## Nástroj pro migraci na Qiskit 1.0 {#qiskit-10-migration-tool}

Pro usnadnění procesu migrace můžeš použít nástroj
[`flake8-qiskit-migration`](https://github.com/qiskit-community/flake8-qiskit-migration),
který detekuje odstraněné importní cesty v tvém kódu a navrhuje alternativy.

<Tabs>
  <TabItem value="flake8-pipx" label="Spustit s pipx" default>
      Pokud máš nainstalovaný [`pipx`](https://github.com/pypa/pipx), jednoduše spusť
      následující příkaz.
      ```sh
      pipx run flake8-qiskit-migration <path-to-source-directory>
      ```
      Tím se balíček nainstaluje do dočasného virtuálního prostředí a spustí
      se na tvém kódu.
  </TabItem>
  <TabItem value="flake8-venv" label="Spustit s venv" default>
      Pokud nechceš používat `pipx`, můžeš ručně vytvořit nové
      prostředí pro nástroj. Tento přístup ti také umožní použít
      [`nbqa`](https://github.com/nbQA-dev/nbQA) ke kontrole ukázek kódu v
      Jupyter noteboocích. Až skončíš, prostředí smaž.
      ```sh
      # Make new environment and install
      python -m venv .flake8-qiskit-migration-venv
      source .flake8-qiskit-migration-venv/bin/activate
      pip install flake8-qiskit-migration

      # Run plugin on Python code
      flake8 --select QKT100 <path-to-source-directory>  # e.g. `src/`

      # (Optional) run plugin on notebooks
      pip install nbqa
      nbqa flake8 ./**/*.ipynb --select QKT100

      # Deactivate and delete environment
      deactivate
      rm -r .flake8-qiskit-migration-venv
      ```
  </TabItem>
</Tabs>

<Admonition type="note" title="Omezení">
Tento nástroj detekuje pouze odstraněné importní cesty. Nezjišťuje použití odstraněných
metod (jako například `QuantumCircuit.qasm`) ani argumentů. Také nedokáže sledovat
přiřazení jako `qk = qiskit`, i když zvládá aliasy jako
`import qiskit as qk`.
</Admonition>

Více informací najdeš v [repozitáři projektu](https://github.com/qiskit-community/flake8-qiskit-migration).

<span id="qiskit.global"></span>
