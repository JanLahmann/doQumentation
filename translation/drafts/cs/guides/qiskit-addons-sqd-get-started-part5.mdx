## Spuštění smyčky obnovy konfigurace {#run-configuration-recovery-loop}

Naměřené vzorky lze poté zpřesnit opakovanou obnovou konfigurace a diagonalizací sad dílčích vzorků pro přiblížení základního stavu, dokud nedojde ke konvergenci.

Nejprve zadej tyto možnosti:

- `ITERATIONS`: Počet iterací samokonzistentní obnovy konfigurace
- `NUM_BATCHES`: Počet sad dílčích vzorků k diagonalizaci
- `SAMPLES_PER_BATCH`: Počet vzorků zahrnutých do každé dávky
- `MAX_DAVIDSON_CYCLES`: Počet iterací řešiče vlastních stavů

```python
# SQD options
ITERATIONS = 5

# Eigenstate solver options
NUM_BATCHES = 1
SAMPLES_PER_BATCH = 500
MAX_DAVIDSON_CYCLES = 200
```

Dále, kvůli vykreslení konvergence, definuj pole pro ukládání přiblížení energie základního stavu, střední hodnoty $\langle S \rangle ^2$ a obsazenosti orbitalů molekuly.

```python
# Self-consistent configuration recovery loop
energy_hist = np.zeros((ITERATIONS, NUM_BATCHES))  # energy history
spin_sq_hist = np.zeros((ITERATIONS, NUM_BATCHES))  # spin history
occupancy_hist = []
avg_occupancy = None
```

Nyní spusť smyčku obnovy konfigurace. Každá smyčka se skládá ze tří kroků:

1. Pomocí funkce `recover_configurations()` získej zpřesněnou matici bitových řetězců a pole pravděpodobností na základě průměrné obsazenosti orbitalů.
1. Pomocí `postselect_and_subsample()` shromáždi dávky dílčích vzorků k diagonalizaci.
1. Pak použij dávky dílčích vzorků jako argumenty funkce `solve_fermion()` pro získání přiblížení základního stavu.

Je důležité si uvědomit, jak zacházet s první iterací smyčky obnovy konfigurace. Protože průměrná obsazenost orbitalů ještě není k dispozici, volá se pouze funkce `postselect_and_subsample()`. Ta odstraní všechny nefyzikální vzorky (vzorky s nesprávnou Hammingovou váhou) před spuštěním řešiče vlastních stavů, `solve_fermion()`. Poté se průměrná obsazenost orbitalů vypočítá přes všechny dávky a použije se jako vstup metody `recover_configurations()`, která probabilisticky překlápí jednotlivé bity na základě tohoto průměru. Více informací najdeš v části **II-A** doplňkových informací v [článku SQD](https://arxiv.org/abs/2405.05068).

```python
from qiskit_addon_sqd.configuration_recovery import recover_configurations
from qiskit_addon_sqd.subsampling import postselect_and_subsample
from qiskit_addon_sqd.fermion import (
    bitstring_matrix_to_ci_strs,
    solve_fermion,
)

for i in range(ITERATIONS):
    print(f"\nStarting configuration recovery iteration {i}")
    # On the first iteration, we have no orbital occupancy information from the
    # solver, so we just post-select from the full bitstring set based on Hamming weight.
    if avg_occupancy is None:
        bitstring_matrix_tmp = bitstring_matrix_full
        probs_array_tmp = probs_array_full

    # If there is average orbital occupancy information, use it to refine the full set of noisy configurations
    else:
        bitstring_matrix_tmp, probs_array_tmp = recover_configurations(
            bitstring_matrix_full,
            probs_array_full,
            avg_occupancy,
            num_alpha,
            num_beta,
            rand_seed=rng,
        )

    # Throw out configurations with an incorrect particle number in either the spin-up or spin-down systems
    batches = postselect_and_subsample(
        bitstring_matrix_tmp,
        probs_array_tmp,
        hamming_right=num_alpha,
        hamming_left=num_beta,
        samples_per_batch=SAMPLES_PER_BATCH,
        num_batches=NUM_BATCHES,
        rand_seed=rng,
    )

    # Run eigenstate solvers in a loop. This loop should be parallelized for larger problems.
    e_tmp = np.zeros(NUM_BATCHES)
    s_tmp = np.zeros(NUM_BATCHES)
    occs_tmp = []
    coeffs = []
    for j in range(NUM_BATCHES):
        strs_a, strs_b = bitstring_matrix_to_ci_strs(batches[j])
        print(f"Batch {j} subspace dimension: {len(strs_a) * len(strs_b)}")
        energy_sci, coeffs_sci, avg_occs, spin = solve_fermion(
            batches[j],
            core_hamiltonian,
            electron_repulsion_integrals,
            open_shell=open_shell,
            spin_sq=spin_sq,
            max_cycle=MAX_DAVIDSON_CYCLES,
        )
        energy_sci += nuclear_repulsion_energy
        e_tmp[j] = energy_sci
        s_tmp[j] = spin
        occs_tmp.append(avg_occs)
        coeffs.append(coeffs_sci)

    # Combine batch results
    avg_occupancy = tuple(np.mean(occs_tmp, axis=0))

    # Track optimization history
    energy_hist[i, :] = e_tmp
    spin_sq_hist[i, :] = s_tmp
    occupancy_hist.append(avg_occupancy)
    print()
```

```text
Starting configuration recovery iteration 0
Batch 0 subspace dimension: 6400

Starting configuration recovery iteration 1
Batch 0 subspace dimension: 555025

Starting configuration recovery iteration 2
Batch 0 subspace dimension: 573049

Starting configuration recovery iteration 3
Batch 0 subspace dimension: 518400

Starting configuration recovery iteration 4
Batch 0 subspace dimension: 562500
```
