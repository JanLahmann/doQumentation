### Převod na operátory QuantumChannel a zpět {#converting-to-and-from-quantumchannel-operators}

Objekty `QuantumError` v Qiskit Aer lze také převádět na objekty `QuantumChannel` v Qiskit a zpět.

```python
# Convert to Kraus operator
bit_flip_kraus = Kraus(bit_flip)
print(bit_flip_kraus)
```

```text
Kraus([[[-9.74679434e-01+0.j,  0.00000000e+00+0.j],
        [ 0.00000000e+00+0.j, -9.74679434e-01+0.j]],

       [[ 0.00000000e+00+0.j,  2.23606798e-01+0.j],
        [ 2.23606798e-01+0.j, -4.96506831e-17+0.j]]],
      input_dims=(2,), output_dims=(2,))
```

```python
# Convert to Superoperator
phase_flip_sop = SuperOp(phase_flip)
print(phase_flip_sop)
```

```text
SuperOp([[1. +0.j, 0. +0.j, 0. +0.j, 0. +0.j],
         [0. +0.j, 0.9+0.j, 0. +0.j, 0. +0.j],
         [0. +0.j, 0. +0.j, 0.9+0.j, 0. +0.j],
         [0. +0.j, 0. +0.j, 0. +0.j, 1. +0.j]],
        input_dims=(2,), output_dims=(2,))
```

```python
# Convert back to a quantum error
print(QuantumError(bit_flip_kraus))

# Check conversion is equivalent to original error
QuantumError(bit_flip_kraus) == bit_flip
```

```text
QuantumError on 1 qubits. Noise circuits:
  P(0) = 1.0, Circuit = 
   ┌───────┐
q: ┤ kraus ├
   └───────┘
```

```text
True
```

### Chyba čtení {#readout-error}

Klasické chyby čtení jsou specifikovány seznamem vektorů pravděpodobnosti přiřazení $P(A|B)$:

  * $A$ je *zaznamenaná* hodnota klasického bitu
  * $B$ je *skutečná* hodnota bitu vrácená z měření

Například pro jeden Qubit: $ P(A|B) = [P(A|0), P(A|1)]$.

```python
# Measurement misassignment probabilities
p0given1 = 0.1
p1given0 = 0.05

ReadoutError([[1 - p1given0, p1given0], [p0given1, 1 - p0given1]])
```

```text
ReadoutError([[0.95 0.05]
 [0.1  0.9 ]])
```

Chyby čtení lze také kombinovat pomocí `compose`, `tensor` a `expand`, stejně jako u kvantových chyb.
## Přidání chyb do modelu šumu {#add-errors-to-a-noise-model}

Při přidávání kvantové chyby do modelu šumu musíme specifikovat typ *instrukce*, na kterou působí, a na které Qubity ji aplikovat. Existují dva případy kvantových chyb:

 1. Kvantová chyba pro všechny Qubity
 2. Kvantová chyba pro konkrétní Qubity

### 1. Kvantová chyba pro všechny Qubity {#1-all-qubit-quantum-error}

Tato chyba aplikuje stejnou chybu na každý výskyt instrukce, bez ohledu na to, na které Qubity působí.

Přidává se jako `noise_model.add_all_qubit_quantum_error(error, instructions)`:

```python
# Create an empty noise model
noise_model = NoiseModel()

# Add depolarizing error to all single qubit u1, u2, u3 gates
error = depolarizing_error(0.05, 1)
noise_model.add_all_qubit_quantum_error(error, ["u1", "u2", "u3"])

# Print noise model info
print(noise_model)
```

```text
NoiseModel:
  Basis gates: ['cx', 'id', 'rz', 'sx', 'u1', 'u2', 'u3']
  Instructions with noise: ['u3', 'u2', 'u1']
  All-qubits errors: ['u1', 'u2', 'u3']
```

### 2. Kvantová chyba pro konkrétní Qubity {#2-specific-qubit-quantum-error}

Tato chyba aplikuje chybu na každý výskyt instrukce působící na zadaný seznam Qubitů. Všimni si, že na pořadí Qubitů záleží: například chyba aplikovaná na Qubity [0, 1] pro dvouqubitovou Gate se liší od chyby aplikované na Qubity [1, 0].

Přidává se jako `noise_model.add_quantum_error(error, instructions, qubits)`:

```python
# Create an empty noise model
noise_model = NoiseModel()

# Add depolarizing error to all single qubit u1, u2, u3 gates on qubit 0 only
error = depolarizing_error(0.05, 1)
noise_model.add_quantum_error(error, ["u1", "u2", "u3"], [0])

# Print noise model info
print(noise_model)
```

```text
NoiseModel:
  Basis gates: ['cx', 'id', 'rz', 'sx', 'u1', 'u2', 'u3']
  Instructions with noise: ['u3', 'u2', 'u1']
  Qubits with noise: [0]
  Specific qubit errors: [('u1', (0,)), ('u2', (0,)), ('u3', (0,))]
```

### Poznámka k nelokální kvantové chybě Qubitů {#note-on-non-local-qubit-quantum-error}

`NoiseModel` nepodporuje přidávání nelokálních kvantových chyb Qubitů. Ty by měly být řešeny mimo `NoiseModel`. To znamená, že pokud potřebuješ vložit své kvantové chyby do Circuit za vlastních podmínek, měl(a) bys [napsat vlastní transpiler pass](./custom-transpiler-pass) (`TransformationPass`) a spustit ho těsně před spuštěním simulátoru.
### Spuštění šumové simulace s modelem šumu {#execute-a-noisy-simulation-with-a-noise-model}

Příkaz `AerSimulator(noise_model=noise_model)` vrátí simulátor nakonfigurovaný podle daného modelu šumu. Kromě nastavení modelu šumu simulátoru také přepíše základní Gate simulátoru podle Gate modelu šumu.
