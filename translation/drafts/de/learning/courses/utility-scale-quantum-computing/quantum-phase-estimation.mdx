---
title: "Quantenphasenschätzung"
sidebar_label: "Quantenphasenschätzung"
description: "Dieser Kurs vermittelt dir die QFT, die eine zentrale Rolle in vielen Quantenalgorithmen spielt"
notebook_path: "learning/courses/utility-scale-quantum-computing/quantum-phase-estimation.ipynb"
---
{/* doqumentation-source-hash: cb50d20f */}

<OpenInLabBanner notebookPath="learning/courses/utility-scale-quantum-computing/quantum-phase-estimation.ipynb" />

# Quantenalgorithmen: Phasenschätzung

<Admonition type="note">

Kento Ueda (15. Mai 2024)

Dieses Notebook vermittelt die grundlegenden Konzepte und die Implementierung der Quanten-Fourier-Transformation (QFT) und der Quantenphasenschätzung (QPE).

[PDF der Originalvorlesung herunterladen](https://ibm.ent.box.com/public/static/srgnll6u8ame7gk186pkthq7erc7l6rw.zip). Beachte, dass manche Code-Schnipsel veraltet sein können, da es sich um statische Bilder handelt.

*Ungefähre QPU-Zeit für dieses Experiment: 7 Sekunden.*

</Admonition>
## 1. Einführung {#1-introduction}

**Quanten-Fourier-Transformation (QFT)**

Die Quanten-Fourier-Transformation ist das quantenmechanische Pendant zur klassischen diskreten Fourier-Transformation. Es handelt sich um eine lineare Transformation, die auf Quantenzustände angewendet wird und Rechenbasisdarstellungen in ihre Fourier-Basis-Darstellungen überführt. Die QFT spielt eine zentrale Rolle in vielen Quantenalgorithmen und bietet eine effiziente Methode, Periodizitätsinformationen aus Quantenzuständen zu extrahieren.
Die QFT kann mit $O(N^2)$ Operationen durch Quantengates wie Hadamard-Gates und Control-Phase-Gates für $N$ Qubits implementiert werden, was einen exponentiellen Speedup gegenüber der klassischen Fourier-Transformation ermöglicht.

- **Anwendungen**: Sie ist ein grundlegender Baustein in Quantenalgorithmen wie Shors Algorithmus zur Faktorisierung großer ganzer Zahlen und zum diskreten Logarithmus.

**Quantenphasenschätzung (QPE)**

Die Quantenphasenschätzung ist ein Quantenalgorithmus, der die Phase eines Eigenvektors eines unitären Operators schätzt. Dieser Algorithmus bildet eine Brücke zwischen den abstrakten mathematischen Eigenschaften von Quantenzuständen und ihren rechnerischen Anwendungen.

- **Anwendungen**: Er kann Probleme lösen wie das Finden von Eigenwerten unitärer Matrizen und die Simulation von Quantensystemen.

Zusammen bilden QFT und QPE das wesentliche Rückgrat vieler Quantenalgorithmen, die Probleme lösen, die für klassische Computer nicht handhabbar sind. Am Ende dieses Notebooks wirst du verstehen, wie diese Techniken implementiert werden.
## 2. Grundlagen der Quanten-Fourier-Transformation (QFT) {#2-basic-of-quantum-fourier-transformation}

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-aer qiskit-ibm-runtime
```

```python
from qiskit import QuantumCircuit, QuantumRegister, ClassicalRegister
from qiskit_aer import AerSimulator
from qiskit.visualization import plot_histogram, plot_bloch_multivector
from qiskit.quantum_info import Statevector
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager
from qiskit_ibm_runtime import Sampler

from numpy import pi
```

In Analogie zur diskreten Fourier-Transformation wirkt die QFT auf einen Quantenzustand $\vert X\rangle = \sum_{j=0}^{N-1} x_j \vert j \rangle$ für $N$ Qubits und bildet ihn auf den Quantenzustand $\vert Y\rangle = \sum_{k=0}^{N-1} y_k \vert k \rangle$ ab.

$$
QFT_{N}: \vert j \rangle \mapsto \frac{1}{\sqrt{N}}\sum_{k=0}^{N-1}\omega_N^{jk} \vert k \rangle
$$

wobei $\omega_N^{jk} = e^{2\pi i \frac{jk}{N}}$.

Oder als unitäre Matrix dargestellt:

$$
U_{QFT} = \frac{1}{\sqrt{N}} \sum_{j=0}^{N-1} \sum_{k=0}^{N-1} \omega_N^{jk} \vert k \rangle \langle j \vert
$$
### 2.1. Intuition <a id="intuition"></a>

Die Quanten-Fourier-Transformation (QFT) transformiert zwischen zwei Basen: der Rechengrundlage (Z-Basis) und der Fourier-Basis. Was bedeutet die Fourier-Basis in diesem Kontext? Du erinnerst dich sicher, dass die Fourier-Transformation $F(\omega)$ einer Funktion $f(x)$ die Faltung von $f(x)$ mit einer Sinusfunktion der Frequenz $\omega$ beschreibt. Vereinfacht gesagt: Die Fourier-Transformation ist eine Funktion, die beschreibt, wie viel von jeder Frequenz $\omega$ nötig wäre, um $f(x)$ aus Sinusfunktionen (oder Kosinusfunktionen) aufzubauen. Um ein besseres Gefühl dafür zu bekommen, was die QFT in diesem Kontext bedeutet, betrachte die schrittweisen Bilder unten, die eine in Binär kodierte Zahl mit vier Qubits zeigen:

<IBMVideo id="134399598" title="Dieses kurze Video zeigt, wie vier Qubits, die mit unterschiedlichen Frequenzen präzessieren, zur Kodierung von Ziffern verwendet werden können."/>

In der Rechenbasis speichern wir Zahlen in Binärform mithilfe der Zustände $|0\rangle$ und $|1\rangle$.

Beachte die Frequenz, mit der die verschiedenen Qubits wechseln: Das linkeste Qubit wechselt bei jeder Erhöhung der Zahl, das nächste bei jeder zweiten, das dritte bei jeder vierten Erhöhung usw.

Wenn wir eine Quanten-Fourier-Transformation auf diese Zustände anwenden, bilden wir ab:
$$
|\text{Zustand in Rechenbasis}\rangle \quad \xrightarrow[]{\text{QFT}} \quad |\text{Zustand in Fourier-Basis}\rangle
$$

$$
\text{QFT}|x\rangle = |\widetilde{x}\rangle
$$

(Zustände in der Fourier-Basis werden oft mit einer Tilde (~) bezeichnet.)

In der Fourier-Basis speichern wir Zahlen durch verschiedene Rotationen um die Z-Achse:

IFRAME

Die zu speichernde Zahl bestimmt den Winkel, um den jedes Qubit um die Z-Achse gedreht wird. Im Zustand $|\widetilde{0}\rangle$ befinden sich alle Qubits im Zustand $|{+}\rangle$. Wie im obigen Beispiel zu sehen, wurde zur Kodierung des Zustands $|\widetilde{5}\rangle$ auf 4 Qubits das linkeste Qubit um $\tfrac{5}{2^n} = \tfrac{5}{16}$ volle Umdrehungen ($\tfrac{5}{16}\times 2\pi$ Radiant) gedreht. Das nächste Qubit wird doppelt so weit gedreht ($\tfrac{10}{16}\times 2\pi$ Radiant bzw. $10/16$ volle Umdrehungen), dieser Winkel wird für das nächste Qubit wieder verdoppelt usw.

Beachte erneut die Frequenz, mit der jedes Qubit wechselt. Das linkeste Qubit (`qubit 0`) hat in diesem Fall die niedrigste Frequenz, das rechteste die höchste.
### 2.2 Beispiel: 1-Qubit-QFT {#22-example-1-qubit-qft}
Betrachten wir den Fall $N = 2^1 = 2$.

Die unitäre Matrix lässt sich schreiben als:

$$
\begin{aligned}
U_{QFT} & = \frac{1}{\sqrt{2}} \sum_{j=0}^{1} \sum_{k=0}^{1} \omega_2^{jk} \vert k \rangle \langle j \vert
\\
& = \frac{1}{\sqrt{2}} (\omega_2^{0} \vert 0 \rangle \langle 0 \vert + \omega_2^{0} \vert 0 \rangle \langle 1 \vert + \omega_2^{0} \vert 1 \rangle \langle 0 \vert + \omega_2^{1} \vert 1 \rangle \langle 1 \vert)
\\
& = \frac{1}{\sqrt{2}} (\vert 0 \rangle \langle 0 \vert + \vert 0 \rangle \langle 1 \vert + \vert 1 \rangle \langle 0 \vert - \vert 1 \rangle \langle 1 \vert)
\\
& = H
\end{aligned}
$$

Diese Operation entspricht dem Ergebnis der Anwendung des Hadamard-Gates ($H$).
### 2.3 Produktdarstellung der QFT {#23-product-representation-of-qft}
Verallgemeinern wir die Transformation für $N = 2^n$, $QFT_{N}$ angewendet auf den Zustand $\vert x \rangle = \vert x_1\ldots x_n \rangle$.
$$
\begin{aligned}
QFT_N\vert x \rangle & = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1}\omega_N^{xy} \vert y \rangle
\\
& = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1} e^{2 \pi i xy / 2^n} \vert y \rangle ~\text{da}\: \omega_N^{xy} = e^{2\pi i \frac{xy}{N}} \:\text{und}\: N = 2^n
\\
& = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1} e^{2 \pi i \left(\sum_{k=1}^n y_k/2^k\right) x} \vert y_1 \ldots y_n \rangle \:\text{umgeschrieben in gebrochener Binärnotation}\: y = y_1\ldots y_n, y/2^n = \sum_{k=1}^n y_k/2^k
\\
& = \frac{1}{\sqrt{N}} \sum_{y=0}^{N-1} \prod_{k=1}^n e^{2 \pi i x y_k/2^k } \vert y_1 \ldots y_n \rangle \:\text{nach Entwicklung des Exponentials einer Summe als Produkt von Exponentialen,}
\\
& = \frac{1}{\sqrt{N}} \bigotimes_{k=1}^n  \left(\vert0\rangle + e^{2 \pi i x /2^k } \vert1\rangle \right) \:\text{nach Umordnung der Summen und Produkte sowie Entwicklung}
\sum_{y=0}^{N-1} = \sum_{y_1=0}^{1}\sum_{y_2=0}^{1}\ldots\sum_{y_n=0}^{1}
\\
& = \frac{1}{\sqrt{N}}
\left(\vert0\rangle + e^{\frac{2\pi i}{2}x} \vert1\rangle\right)
\otimes
\left(\vert0\rangle + e^{\frac{2\pi i}{2^2}x} \vert1\rangle\right)
\otimes
\ldots
\otimes
\left(\vert0\rangle + e^{\frac{2\pi i}{2^{n-1}}x} \vert1\rangle\right)
\otimes
\left(\vert0\rangle + e^{\frac{2\pi i}{2^n}x} \vert1\rangle\right)
\end{aligned}
$$
### 2.4 Beispiel: Circuit-Konstruktion der 3-Qubit-QFT {#24-example-circuit-construction-of-3-qubits-qft}

Aus der obigen Beschreibung ist möglicherweise nicht sofort klar, wie ein QFT-Circuit konstruiert werden soll. Beachte zunächst, dass wir bei drei Qubits Phasen erwarten, die sich mit unterschiedlichen „Raten" entwickeln. Wie das genau in einen Circuit übersetzt wird, bleibt als Übung dem Leser überlassen. Es gibt mehrere Diagramme und Beispiele im Vorlesungs-PDF. Weitere Ressourcen findest du in [dieser Lektion](/learning/courses/fundamentals-of-quantum-algorithms/phase-estimation-and-factoring/introduction) des Kurses „Grundlagen der Quantenalgorithmen".

Wir demonstrieren die QFT nur mit Simulatoren und verwenden das Qiskit-Patterns-Framework erst, wenn wir zur Quantenphasenschätzung übergehen.

```python
# Prepare for 3 qubits circuit
qr = QuantumRegister(3)
cr = ClassicalRegister(3)
qc = QuantumCircuit(qr, cr)
```

```python
qc.h(2)
qc.cp(pi / 2, 1, 2)  # Controlled-phase gate from qubit 1 to qubit 2
qc.cp(pi / 4, 0, 2)  # Controlled-phase gate from qubit 0 to qubit 2
qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/98cc96b3-f2d8-4d0d-bb57-186ea0919f53-0.avif)

```python
qc.h(1)
qc.cp(pi / 2, 0, 1)  # Controlled-phase gate from qubit 0 to qubit 1

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/eaff67fa-82f9-4402-8fa5-d0f0af824616-0.avif)

```python
qc.h(0)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/43e09a6b-ea86-4f02-9e3a-82317c3b787f-0.avif)

```python
qc.swap(0, 2)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/d7dd1cae-f463-4308-866c-8267e015c6ff-0.avif)

Als Beispiel wenden wir die QFT auf $|5\rangle$ an.

Zunächst bestätigen wir die Binärdarstellung der Ganzzahl 5 und erstellen den Circuit, der den Zustand 5 kodiert:

```python
bin(5)
```

```text
'0b101'
```

```python
qc = QuantumCircuit(3)

qc.x(0)
qc.x(2)
qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/db631de5-4619-4e8c-b9d1-c6197650ae33-0.avif)

Wir überprüfen die Quantenzustände mit dem Aer-Simulator:

```python
statevector = Statevector(qc)
plot_bloch_multivector(statevector)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/83f34fc7-21f3-408b-9beb-a146cb77d27c-0.avif)

Schließlich fügen wir die QFT hinzu und sehen den Endzustand unserer Qubits:

```python
qc.h(2)
qc.cp(pi / 2, 1, 2)
qc.cp(pi / 4, 0, 2)

qc.h(1)
qc.cp(pi / 2, 0, 1)

qc.h(0)

qc.swap(0, 2)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/524f24ae-3d86-4be4-99cc-c0c8557b8393-0.avif)

```python
statevector = Statevector(qc)
plot_bloch_multivector(statevector)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/cf4d7152-bb06-4017-922a-5fa97075a67f-0.avif)

Wir sehen, dass die QFT-Funktion korrekt funktioniert hat. Qubit 0 wurde um $\tfrac{5}{8}$ einer vollen Umdrehung gedreht, Qubit 1 um $\tfrac{10}{8}$ volle Umdrehungen (entspricht $\tfrac{1}{4}$ einer vollen Umdrehung) und Qubit 2 um $\tfrac{20}{8}$ volle Umdrehungen (entspricht $\tfrac{1}{2}$ einer vollen Umdrehung).
### 2.5 Übung: QFT {#25-exercise-qft}
(1) Implementiere die QFT für 4 Qubits.

```python
##your code goes here##
```

(2) Wende die QFT auf $|14\rangle$ an, simuliere und zeichne den Zustandsvektor auf der Bloch-Sphäre.

```python
##your code goes here##
```

#### Lösung der Übung: QFT {#solution-of-the-exercise-qft}
(1)

```python
qr = QuantumRegister(4)
cr = ClassicalRegister(4)
qc = QuantumCircuit(qr, cr)

qc.h(3)
qc.cp(pi / 2, 2, 3)
qc.cp(pi / 4, 1, 3)
qc.cp(pi / 8, 0, 3)

qc.h(2)
qc.cp(pi / 2, 1, 2)
qc.cp(pi / 4, 0, 2)

qc.h(1)
qc.cp(pi / 2, 0, 1)

qc.h(0)

qc.swap(0, 3)
qc.swap(1, 2)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/02e1d50b-6555-4a1e-a2b9-ba77c77840a1-0.avif)

(2)

```python
bin(14)
```

```text
'0b1110'
```

```python
qc = QuantumCircuit(4)

qc.x(1)
qc.x(2)
qc.x(3)
qc.draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/a01ee72e-8486-426d-a654-21b272b958ff-0.avif)

```python
qc.h(3)
qc.cp(pi / 2, 2, 3)
qc.cp(pi / 4, 1, 3)
qc.cp(pi / 8, 0, 3)

qc.h(2)
qc.cp(pi / 2, 1, 2)
qc.cp(pi / 4, 0, 2)

qc.h(1)
qc.cp(pi / 2, 0, 1)
qc.h(0)

qc.swap(0, 3)
qc.swap(1, 2)

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/db8a6229-a864-4f5e-889b-8ccd5561eddb-0.avif)

```python
statevector = Statevector(qc)
plot_bloch_multivector(statevector)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/0d0af6c8-73e7-4e49-9d81-75446373d6c8-0.avif)

## 3. Grundlagen der Quantenphasenschätzung (QPE) {#3-basics-of-quantum-phase-estimation}
Gegeben eine unitäre Operation $U$, schätzt die QPE $\theta$ in $U\vert\psi \rangle =e^{\boldsymbol{2\pi i} \theta }|\psi \rangle$; da $U$ unitär ist, haben alle Eigenwerte den Betrag 1.
### 3.1 Vorgehen {#31-procedure}
#### 1. Aufbau {#1-setup}
$\vert\psi\rangle$ befindet sich in einem Qubit-Register. Ein weiteres Register aus $n$ Qubits bildet das Zählregister, in dem wir den Wert $2^n\theta$ speichern:

$$
|\psi_0\rangle = \lvert 0 \rangle^{\otimes n} \lvert \psi \rangle
$$
#### 2. Superposition {#2-superposition}

Wende eine $n$-Bit-Hadamard-Gate-Operation $H^{\otimes n}$ auf das Zählregister an:

$$
|\psi_1\rangle = {\frac {1}{2^{\frac {n}{2}}}}\left(|0\rangle +|1\rangle \right)^{\otimes n} \lvert \psi \rangle
$$
#### 3. Gesteuerte unitäre Operationen {#3-controlled-unitary-operations}

Wir führen das gesteuerte Unitäre $CU$ ein, das den unitären Operator $U$ auf das Zielregister anwendet, wenn das entsprechende Steuer-Bit $|1\rangle$ ist. Da $U$ ein unitärer Operator mit Eigenvektor $|\psi\rangle$ ist, sodass $U|\psi \rangle =e^{\boldsymbol{2\pi i} \theta }|\psi \rangle$, gilt:

$$
U^{2^{j}}|\psi \rangle =U^{2^{j}-1}U|\psi \rangle =U^{2^{j}-1}e^{2\pi i\theta }|\psi \rangle =\cdots =e^{2\pi i2^{j}\theta }|\psi \rangle
$$
### 3.2 Beispiel: T-Gate-QPE {#32-example-t-gate-qpe}
Verwenden wir das $T$-Gate als Beispiel für die QPE und schätzen wir seine Phase $\theta$.

$$
T|1\rangle =
\begin{pmatrix}
1 & 0\\
0 & e^\frac{i\pi}{4}\\
\end{pmatrix}
\begin{pmatrix}
0\\
1\\
\end{pmatrix}
= e^\frac{i\pi}{4}|1\rangle
$$

Wir erwarten:

$$
\theta = \frac{1}{8}
$$

wobei

$$
T|1\rangle = e^{2i\pi\theta}|1\rangle
$$
Wir initialisieren $\vert\psi\rangle = \vert1\rangle$ als Eigenvektor des $T$-Gates durch Anwendung eines $X$-Gates:

```python
qpe = QuantumCircuit(4, 3)
qpe.x(3)
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/2334e6e2-7ae2-4d4c-9014-3e6041f37ee7-0.avif)

Als nächstes wenden wir Hadamard-Gates auf die Zähl-Qubits an:

```python
for qubit in range(3):
    qpe.h(qubit)
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/9c020053-c369-4cc4-829e-72246d79f82c-0.avif)

Wir führen die gesteuerten unitären Operationen durch:

```python
repetitions = 1
for counting_qubit in range(3):
    for i in range(repetitions):
        qpe.cp(pi / 4, counting_qubit, 3)  # This is C-U
    repetitions *= 2
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/0b3bedfc-be73-427d-9eed-22a2d7df60cd-0.avif)

Wir wenden die inverse Quanten-Fourier-Transformation an, um den Zustand des Zählregisters zu konvertieren, und messen dann das Zählregister:

```python
from qiskit.circuit.library import QFT
```

```python
# Apply inverse QFT
qpe.append(QFT(3, inverse=True), [0, 1, 2])
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/df8ada87-4279-47bb-acd5-cb432bdfc998-0.avif)

```python
for n in range(3):
    qpe.measure(n, n)
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/5fcc8195-8fc9-47bc-9ef3-9d9d444db812-0.avif)

Wir können mit dem Aer-Simulator simulieren:

```python
aer_sim = AerSimulator()
shots = 2048

pm = generate_preset_pass_manager(backend=aer_sim, optimization_level=1)
t_qpe = pm.run(qpe)

sampler = Sampler(mode=aer_sim)
job = sampler.run([t_qpe], shots=shots)
result = job.result()
answer = result[0].data.c.get_counts()

plot_histogram(answer)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/b0152311-8f60-4c11-a10e-a35691c0ece5-0.avif)

Wir erhalten ein Ergebnis (`001`) mit Sicherheit, was als Dezimalzahl `1` ergibt. Nun müssen wir unser Ergebnis (`1`) durch $2^n$ teilen, um $\theta$ zu erhalten:

$$
\theta = \frac{1}{2^3} = \frac{1}{8}
$$
Shors Algorithmus ermöglicht es uns, eine Zahl zu faktorisieren, indem wir einen Circuit mit unbekanntem $\theta$ konstruieren und $\theta$ bestimmen.
### 3.3 Übung {#33-exercise}

Schätze $\theta = 1/3$ mit 3 Qubits zum Zählen und einem Qubit für einen Eigenvektor.

$P|1\rangle = e^{i\lambda}|1\rangle$. Da wir $U|1\rangle = e^{2\pi i \tfrac{1}{3}}|1\rangle$ implementieren wollen, müssen wir $\lambda = \tfrac{2 \pi}{3}$ setzen.

```python
##your code goes here##
```

#### Lösung der Übung: $\theta = 1/3$ {#solution-of-the-exercise-theta-1-3}

```python
# Create and set up circuit
qpe = QuantumCircuit(4, 3)

# Apply H-Gates to counting qubits:
for qubit in range(3):
    qpe.h(qubit)

# Prepare our eigenstate |psi>:
qpe.x(3)

# Do the controlled-U operations:
angle = 2 * pi / 3
repetitions = 1
for counting_qubit in range(3):
    for i in range(repetitions):
        qpe.cp(angle, counting_qubit, 3)
    repetitions *= 2

# Do the inverse QFT:
qpe.append(QFT(3, inverse=True), [0, 1, 2])

for n in range(3):
    qpe.measure(n, n)

qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/9cb90eab-e6ce-4456-b73e-e4082fed2850-0.avif)

```python
aer_sim = AerSimulator()
shots = 4096

pm = generate_preset_pass_manager(backend=aer_sim, optimization_level=1)
t_qpe = pm.run(qpe)

sampler = Sampler(mode=aer_sim)
job = sampler.run([t_qpe], shots=shots)
result = job.result()
answer = result[0].data.c.get_counts()

plot_histogram(answer)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/e118815f-5d92-4ea1-9849-b178ef84d8a4-0.avif)

## 4. Ausführung mit dem Qiskit Runtime Sampler Primitive {#4-execution-using-the-qiskit-runtime-sampler-primitive}

Wir werden QPE auf einem echten Quantengerät durchführen und dabei den 4 Schritten der Qiskit-Patterns folgen.

    1. Problem in ein quantennatives Format überführen
    2. Die Circuits optimieren
    3. Den Ziel-Circuit ausführen
    4. Die Ergebnisse nachbearbeiten

```python
from qiskit_ibm_runtime import QiskitRuntimeService
from qiskit_ibm_runtime import Sampler
```

```python
# Loading your IBM Quantum&reg; account(s)

service = QiskitRuntimeService()
```

### 4.1 Schritt 1: Problem auf Quantencircuits und Operatoren abbilden {#41-step-1-map-problem-to-quantum-circuits-and-operators}

```python
qpe = QuantumCircuit(4, 3)
qpe.x(3)
for qubit in range(3):
    qpe.h(qubit)
repetitions = 1
for counting_qubit in range(3):
    for i in range(repetitions):
        qpe.cp(pi / 4, counting_qubit, 3)  # This is C-U
    repetitions *= 2
qpe.append(QFT(3, inverse=True), [0, 1, 2])
for n in range(3):
    qpe.measure(n, n)
qpe.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/70c92e35-0e13-4552-99be-50d6950dfedc-0.avif)

```python
backend = service.least_busy(simulator=False, operational=True, min_num_qubits=4)

print(backend)
```

```text
<IBMBackend('ibm_strasbourg')>
```

### 4.2 Schritt 2: Für Zielhardware optimieren {#42-step-2-optimize-for-target-hardware}

```python
# Transpile the circuit into basis gates executable on the hardware
pm = generate_preset_pass_manager(backend=backend, optimization_level=2)
qc_compiled = pm.run(qpe)

qc_compiled.draw("mpl", idle_wires=False)
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/d4d57d21-00c6-4144-aa3e-4a467a065078-0.avif)

### 4.3 Schritt 3: Auf Zielhardware ausführen {#43-step-3-execute-on-target-hardware}

```python
real_sampler = Sampler(mode=backend)
job = real_sampler.run([qc_compiled], shots=1024)
job_id = job.job_id()
print("job id:", job_id)
```

```text
job id: d13p4zb5z6q00087ag00
```

```python
job = service.job(job_id)  # Input your job-id between the quotations
job.status()
```

```text
'DONE'
```

```python
result_real = job.result()
print(result_real)
```

```text
PrimitiveResult([SamplerPubResult(data=DataBin(c=BitArray(<shape=(), num_shots=1024, num_bits=3>)), metadata={'circuit_metadata': {}})], metadata={'execution': {'execution_spans': ExecutionSpans([DoubleSliceSpan(<start='2025-06-09 22:39:00', stop='2025-06-09 22:39:00', size=1024>)])}, 'version': 2})
```

### 4.4 Schritt 4: Ergebnisse nachbearbeiten {#44-step-4-post-process-the-results}

```python
from qiskit.visualization import plot_histogram

plot_histogram(result_real[0].data.c.get_counts())
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/quantum-phase-estimation/extracted-outputs/bd9181eb-dc1e-42e1-bdcf-3bb6b128538d-0.avif)

```python
# See the version of Qiskit
import qiskit

qiskit.__version__
```

```text
'2.0.2'
```
