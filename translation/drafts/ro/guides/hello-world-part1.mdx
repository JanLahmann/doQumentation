---
title: "Salut lume"
sidebar_label: "Salut lume"
description: "Începe să folosești Qiskit cu hardware-ul IBM Quantum în acest exemplu Hello World"
notebook_path: "guides/hello-world.ipynb"
---
{/* doqumentation-source-hash: 3ddf8cdb */}

<OpenInLabBanner notebookPath="guides/hello-world.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>Versiuni de pachete</b></summary>

Codul de pe această pagină a fost dezvoltat folosind următoarele cerințe.
Recomandăm să folosești aceste versiuni sau unele mai noi.

```
qiskit[all]~=2.3.0
qiskit-ibm-runtime~=0.43.1
```
</details>
Acest exemplu conține două părți. Mai întâi vei crea un program cuantic simplu și îl vei rula pe o unitate de procesare cuantică (QPU). Deoarece cercetarea cuantică reală necesită programe mult mai robuste, în a doua secțiune ([Scalare la un număr mare de qubiți](#scale-to-large-numbers-of-qubits)), vei scala programul simplu până la nivelul de utilitate.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q matplotlib qiskit qiskit-ibm-runtime
```

## Instalare și autentificare {#install-and-authenticate}

1. Dacă nu ai instalat deja Qiskit, găsești instrucțiunile în ghidul [Quickstart](/guides/quick-start).

    - Instalează Qiskit Runtime pentru a rula joburi pe hardware cuantic:

        ```bash
        pip install qiskit-ibm-runtime
        ```

    - Configurează un mediu pentru a rula notebook-uri Jupyter local:

        ```bash
        pip install jupyter
        ```

2. Configurează-ți autentificarea pentru accesul la hardware-ul cuantic prin intermediul [Open Plan](/guides/plans-overview#open-plan) gratuit.

    (Dacă ai primit prin e-mail o invitație pentru a te alătura unui cont, urmează în schimb [pașii pentru utilizatorii invitați](/guides/cloud-setup-invited).)

    - Accesează [IBM Quantum Platform](https://quantum.cloud.ibm.com/) pentru a te autentifica sau a crea un cont.
         <Admonition type="note" title="Important">
         Dacă te conectezi printr-un server proxy, trebuie să folosești Qiskit Runtime v0.44.0 sau o versiune mai recentă.
         </Admonition>
    - Generează-ți cheia API (numită și *token API*) pe [tabloul de bord](https://quantum.cloud.ibm.com/), apoi copiaz-o într-un loc sigur.
    - Accesează pagina [Instances](https://quantum.cloud.ibm.com/instances) și găsește instanța pe care dorești să o folosești. Plasează cursorul peste CRN-ul ei și dă clic pentru a-l copia.

    - Salvează-ți acreditările local cu acest cod:

        ```python
        from qiskit_ibm_runtime import QiskitRuntimeService

        QiskitRuntimeService.save_account(
        token="<your-api-key>", # Use the 44-character API_KEY you created and saved from the IBM Quantum Platform Home dashboard
        instance="<CRN>", # Optional
        )
        ```

3. Acum poți folosi acest cod Python ori de câte ori dorești să te autentifici la Qiskit Runtime Service:
    ```python
        from qiskit_ibm_runtime import QiskitRuntimeService

        # Run every time you need the service
        service = QiskitRuntimeService()
    ```
<Admonition type="info" title="Nu folosești un mediu Python de încredere?">
Dacă folosești un calculator public sau un alt mediu nesecurizat, urmează în schimb [instrucțiunile de autentificare manuală](/guides/cloud-setup-untrusted) pentru a-ți păstra în siguranță acreditările de autentificare.
</Admonition>
