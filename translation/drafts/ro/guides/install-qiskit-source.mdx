---
title: Instalează Qiskit SDK din sursă
description: Află cum să instalezi versiunea de dezvoltare a Qiskit.

---
{/* doqumentation-source-hash: 3602a588 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore rustup */}

# Instalează Qiskit SDK și Qiskit Runtime din sursă

Instalarea Qiskit SDK din sursă îți permite să accesezi versiunea curentă de dezvoltare, în loc să folosești versiunea din depozitul Python Package Index (PyPI). Astfel poți inspecta și extinde mai eficient cea mai recentă versiune a codului Qiskit.

## Creează și activează un mediu virtual nou {#create-and-activate-a-new-virtual-environment}

1. Navighează la directorul proiectului tău și creează un mediu minimal cu doar Python instalat.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

1. Activează noul tău mediu.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
             ```text
             .venv\Scripts\Activate.ps1
             ```
           </TabItem>
         </OperatingSystemTabs>

## Instalează compilatorul Rust {#install-the-rust-compiler}

Pe sistemul tău trebuie să fie instalat un compilator Rust pentru a compila Qiskit. Pentru a instala compilatorul Rust, folosește instalatorul Rust multiplatformă [`rustup`](https://rustup.rs/) sau [o altă metodă de instalare.](https://forge.rust-lang.org/infra/other-installation-methods.html)

## Instalează Qiskit {#install-qiskit}

Urmează acești pași pentru a instala Qiskit:

1. Clonează depozitul Qiskit.

```bash
git clone https://github.com/Qiskit/qiskit.git
```

2. Schimbă directorul la `qiskit`.

```bash
cd qiskit
```

3. (Opțional) Dacă vrei să rulezi teste sau verificări de linting, instalează cerințele pentru dezvoltatori.

```bash
pip install -r requirements-dev.txt
```

4. Instalează `qiskit`.

  * **Instalare standard**:

    ```bash
    pip install .
    ```

  * **Modul editabil**: În acest mod, nu trebuie să reinstalezi Qiskit când există modificări de cod în proiect.

    ```bash
    pip install -e .
    ```

    În modul editabil, extensiile compilate sunt construite în _modul debug_ fără optimizări. Acest lucru afectează performanța la rulare a codului compilat. Pentru a construi extensiile compilate cu optimizări activate, rulează următoarea comandă pentru a recompila binarul în _modul release_:

    ```bash
    python setup.py build_rust --release --inplace
    ```

    <Admonition type="note">
    Dacă lucrezi la codul Rust din Qiskit, trebuie să recompilezi codul extensiei de fiecare dată când faci o modificare locală. În modul editabil, extensia Rust este construită doar când se rulează comanda de instalare, deci modificările locale pe care le faci în codul Rust nu se reflectă în pachetul instalat dacă nu recompilezi extensia rulând din nou `build_rust` (cu sau fără `--release`, în funcție de dacă vrei să construiești în modul release sau debug).
    </Admonition>

## Instalează Qiskit Runtime {#install-qiskit-runtime}

Urmează acești pași dacă vrei să instalezi Qiskit Runtime:

1. Clonează depozitul Qiskit Runtime.

```bash
git clone https://github.com/Qiskit/qiskit-ibm-runtime.git
```

2. Schimbă directorul la `qiskit-ibm-runtime`.

```bash
cd qiskit-ibm-runtime
```

3. Instalează `qiskit-runtime`. Îți recomandăm să folosești un [mediu virtual](https://docs.python.org/3/library/venv.html) pentru a evita poluarea instalării globale Python.

  * **Instalare standard**:

    ```bash
    pip install .
    ```

  * **Modul editabil**: În acest mod, nu trebuie să reinstalezi Qiskit când există modificări de cod în proiect.

    ```bash
    pip install -e .
    ```

    În modul editabil, extensiile compilate sunt construite în _modul debug_ fără optimizări.

4. (Opțional) Dacă vrei să rulezi teste sau verificări de linting, instalează cerințele pentru dezvoltatori. Îți recomandăm să folosești un [mediu virtual](https://docs.python.org/3/library/venv.html) pentru a evita poluarea instalării globale Python.

```bash
pip install -e ".[dev]"
```

## Pași următori {#next-steps}

<Admonition type="tip" title="Recomandări">
  - Citește [ghidul de contribuție](https://github.com/Qiskit/qiskit/blob/main/CONTRIBUTING.md) pentru a contribui la Qiskit SDK open-source.
  - [Rulează programul Hello world](/guides/hello-world).
  - Încearcă un tutorial, cum ar fi [algoritmul lui Grover](/tutorials/grovers-algorithm).
</Admonition>
