---
title: "Geometria moleculară"
sidebar_label: "Geometria moleculară"
description: "În această lecție variăm geometria unei molecule simple, minimizând energia la fiecare pas."
notebook_path: "learning/courses/quantum-chem-with-vqe/geometry.ipynb"
---

{/* doqumentation-source-hash: b2bf1f51 */}

<OpenInLabBanner notebookPath="learning/courses/quantum-chem-with-vqe/geometry.ipynb" />



{/* cspell:ignore pxxr prqs nelecas mcscf chmax Dmax vmax ecore ncas Excp disp */}

# Determinarea geometriei moleculare {#determining-a-molecular-geometry}

În secțiunea anterioară, am implementat VQE pentru a determina energia stării fundamentale a unei molecule. Aceasta este o utilizare validă a calculului cuantic, dar și mai utilă ar fi determinarea structurii unei molecule.

## Pasul 1: Maparea intrărilor clasice la o problemă cuantică {#step-1-map-classical-inputs-to-a-quantum-problem}

Rămânând la exemplul nostru de bază al hidrogenului diatomic, singurul parametru geometric de variat este lungimea legăturii. Pentru a realiza acest lucru, procedăm ca înainte, dar folosind o variabilă în construcția inițială a moleculei (o lungime a legăturii, *x*, ca argument). Aceasta este o modificare destul de simplă, dar necesită ca variabila să fie inclusă în funcții pe parcursul întregului proces, deoarece pornește din construcția Hamiltonianului fermionic și se propagă prin mapare și, în final, la funcția de cost.

Mai întâi, încărcăm câteva dintre pachetele folosite anterior și definim funcția Cholesky.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q matplotlib numpy pyscf qiskit qiskit-aer qiskit-ibm-runtime scipy
```

```python
from qiskit.quantum_info import SparsePauliOp
import matplotlib.pyplot as plt
import numpy as np

#!pip install pyscf==2.4.0
from pyscf import ao2mo, gto, mcscf, scf

def cholesky(V, eps):
    # see https://arxiv.org/pdf/1711.02242.pdf section B2
    # see https://arxiv.org/abs/1808.02625
    # see https://arxiv.org/abs/2104.08957
    no = V.shape[0]
    chmax, ng = 20 * no, 0
    W = V.reshape(no**2, no**2)
    L = np.zeros((no**2, chmax))
    Dmax = np.diagonal(W).copy()
    nu_max = np.argmax(Dmax)
    vmax = Dmax[nu_max]
    while vmax > eps:
        L[:, ng] = W[:, nu_max]
        if ng > 0:
            L[:, ng] -= np.dot(L[:, 0:ng], (L.T)[0:ng, nu_max])
        L[:, ng] /= np.sqrt(vmax)
        Dmax[: no**2] -= L[: no**2, ng] ** 2
        ng += 1
        nu_max = np.argmax(Dmax)
        vmax = Dmax[nu_max]
    L = L[:, :ng].reshape((no, no, ng))
    print(
        "accuracy of Cholesky decomposition ",
        np.abs(np.einsum("prg,qsg->prqs", L, L) - V).max(),
    )
    return L, ng

def identity(n):
    return SparsePauliOp.from_list([("I" * n, 1)])

def creators_destructors(n, mapping="jordan_wigner"):
    c_list = []
    if mapping == "jordan_wigner":
        for p in range(n):
            if p == 0:
                ell, r = "I" * (n - 1), ""
            elif p == n - 1:
                ell, r = "", "Z" * (n - 1)
            else:
                ell, r = "I" * (n - p - 1), "Z" * p
            cp = SparsePauliOp.from_list([(ell + "X" + r, 0.5), (ell + "Y" + r, -0.5j)])
            c_list.append(cp)
    else:
        raise ValueError("Unsupported mapping.")
    d_list = [cp.adjoint() for cp in c_list]
    return c_list, d_list
```

Acum, pentru a defini Hamiltonianul nostru, vom folosi PySCF exact ca în exemplul anterior, dar de data aceasta vom include o variabilă, `x`, care joacă rolul distanței interatomice. Aceasta va returna energia nucleară de bază, energia electronică monoelectronică și energiile bielectronice, ca înainte.

```python
def ham_terms(x: float):
    distance = x
    a = distance / 2
    mol = gto.Mole()
    mol.build(
        verbose=0,
        atom=[
            ["H", (0, 0, -a)],
            ["H", (0, 0, a)],
        ],
        basis="sto-6g",
        spin=0,
