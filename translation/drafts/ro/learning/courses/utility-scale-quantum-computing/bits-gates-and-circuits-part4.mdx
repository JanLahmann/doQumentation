```python
### (2) HH|0> ###
##your code goes here##
qc = QuantumCircuit(1)
qc.h(0)
qc.h(0)
qc.draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/361c09fe-c206-4355-b827-da7ede0848de-0.avif)

```python
# See the statevector
out_vector = Statevector(qc)
print(out_vector)

# Draw a Bloch sphere
plot_bloch_multivector(out_vector)
```

```text
Statevector([1.+0.j, 0.+0.j],
            dims=(2,))
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/a4976a54-77e2-42b5-b492-49c8cdaf9c9f-1.avif)

```python
### (3) HZH|0> ###
##your code goes here##
qc = QuantumCircuit(1)
qc.h(0)
qc.z(0)
qc.h(0)
qc.draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/cd01ed0c-8f9e-4689-b1ae-f028d588f81a-0.avif)

```python
# See the statevector
out_vector = Statevector(qc)
print(out_vector)

# Draw a Bloch sphere
plot_bloch_multivector(out_vector)
```

```text
Statevector([0.+0.j, 1.+0.j],
            dims=(2,))
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/4bc91c2a-e097-44db-95bb-acf0d0ea0844-1.avif)

### 3.3 Măsurarea {#33-measurement}

Măsurarea este teoretic un subiect foarte complex. Dar în termeni practici, efectuarea unei măsurători de-a lungul axei $z$ (cum fac toate calculatoarele cuantice IBM&reg;) forțează pur și simplu starea qubitului $\alpha|0\rangle+\beta|1\rangle \quad (s.t.|\alpha|^2+|\beta|^2=1)$ fie la $|0\rangle$, fie la $|1\rangle$, și observăm rezultatul.
- $|\alpha|^2$ este probabilitatea de a obține $|0\rangle$ atunci când măsurăm.
- $|\beta|^2$ este probabilitatea de a obține $|1\rangle$ atunci când măsurăm.

Prin urmare, $\alpha$ și $\beta$ sunt numite amplitudini de probabilitate. (vezi „regula Born")

De exemplu, $\frac{\sqrt{2}}{2}|0\rangle+\frac{\sqrt{2}}{2}|1\rangle$ are o probabilitate egală de a deveni $|0\rangle$ sau $|1\rangle$ la măsurare. $\frac{\sqrt{3}}{2}|0\rangle-\frac{1}{2}i|1\rangle$ are o șansă de 75% de a deveni $|0\rangle$.
#### Simulatorul Qiskit Aer {#qiskit-aer-simulator}
Acum, hai să măsurăm un circuit care pregătește superpozția cu probabilitate egală de mai sus.
Trebuie să adăugăm porțile de măsurare, deoarece simulatorul Qiskit Aer simulează un hardware cuantic ideal (fără zgomot) în mod implicit. Notă: Simulatorul Aer poate aplica și un model de zgomot bazat pe un calculator cuantic real. Ne vom întoarce la modelele de zgomot mai târziu.

```python
# Create a new circuit with one qubits (first argument) and one classical bits (second argument)
qc = QuantumCircuit(1, 1)
qc.h(0)
qc.measure(0, 0)  # Add the measurement gate

qc.draw(output="mpl")
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/3956e602-9f1b-4083-b58d-26c91b50b75e-0.avif)

Suntem acum gata să rulăm circuitul nostru pe simulatorul Aer. În acest exemplu, vom aplica shots=1024 implicit, ceea ce înseamnă că vom măsura de 1024 de ori. Apoi vom reprezenta acele rezultate într-o histogramă.

```python
# Run the circuit on a simulator to get the results
# Define backend
backend = AerSimulator()

# Transpile to backend
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(qc)

# Run the job
sampler = Sampler(mode=backend)
job = sampler.run([isa_qc])
result = job.result()

# Print the results
counts = result[0].data.c.get_counts()
print(counts)

# Plot the counts in a histogram
plot_histogram(counts)
```

```text
{'0': 521, '1': 503}
```

![Output of the previous code cell](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/7cb7c5ab-7b12-4f77-aaca-1d7e6cff213c-1.avif)

Vedem că 0 și 1 au fost măsurate cu o probabilitate de aproape 50% fiecare. Deși zgomotul nu a fost simulat aici, stările sunt în continuare probabilistice. Deci, deși ne așteptăm la o distribuție de aproximativ 50-50, rareori o vom găsi exact astfel. La fel cum 100 de aruncări de monedă ar da rareori exact 50 de rezultate pentru fiecare față.
