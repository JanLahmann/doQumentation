        Pauli(pauli[::-1].to_label() + "Y")
    )
    expval = expval_real + 1j * expval_imag

    # Fill-in matrix elements
    H_expval += coeff * expval

print(H_expval)
```

```text
(10+0j)
```

Deși am reușit să reducem adâncimea Circuit-ului cu câteva ordine de mărime folosind testul Hadamard eficient, adâncimea este totuși suficientă pentru a necesita tehnici de corecție a erorilor de ultimă generație. Mai jos specificăm atributele mitigării utilizate. Toate metodele folosite sunt importante, dar merită evidențiată în special [amplificarea probabilistică a erorilor (PEA)](/guides/error-mitigation-and-suppression-techniques#probabilistic-error-amplification-pea). Această tehnică puternică vine cu un overhead cuantic semnificativ. Calculul efectuat aici poate dura 20 de minute sau mai mult pe un calculator cuantic real. Poți modifica parametrii de mai jos pentru a crește sau a reduce precizia și, implicit, overhead-ul. Setările implicite de mai jos produc rezultate de înaltă fidelitate.

```python
# Experiment options
num_randomizations = 300
num_randomizations_learning = 20
max_batch_circuits = 20
shots_per_randomization = 100
learning_pair_depths = [0, 4, 24]
noise_factors = [1, 1.3, 1.6]

# Base option formatting
options = {
    # Builtin resilience settings for ZNE
    "resilience": {
        "measure_mitigation": True,
        "zne_mitigation": True,
        "zne": {"noise_factors": noise_factors},
        # TREX noise learning configuration
        "measure_noise_learning": {
            "num_randomizations": num_randomizations_learning,
            "shots_per_randomization": shots_per_randomization,
        },
        # PEA noise model configuration
        "layer_noise_learning": {
            "max_layers_to_learn": 10,
            "layer_pair_depths": learning_pair_depths,
            "shots_per_randomization": shots_per_randomization,
            "num_randomizations": num_randomizations_learning,
        },
    },
    # Randomization configuration
    "twirling": {
        "num_randomizations": num_randomizations,
        "shots_per_randomization": shots_per_randomization,
        "strategy": "all",
    },
    # Experimental settings for PEA method
    "experimental": {
        # # Just in case, disable any further qiskit transpilation not related to twirling / DD
        # "skip_transpilation": True,
        # Execution configuration
        "execution": {
            "max_pubs_per_batch_job": max_batch_circuits,
            "fast_parametric_update": True,
        },
        # Error Mitigation configuration
        "resilience": {
            # ZNE Configuration
            "zne": {
                "amplifier": "pea",
                "return_all_extrapolated": True,
                "return_unextrapolated": True,
                "extrapolated_noise_factors": [0] + noise_factors,
            }
        },
    },
}
```

În final, executăm Circuit-urile pentru $\tilde{S}$ și $\tilde{H}$ cu Estimator.

```python
# This job required 17 minutes of QPU time to run on a Heron r2 processor. This is only an estimate. Your execution time may vary.

with Batch(backend=backend) as batch:
    # Estimator
    estimator = Estimator(mode=batch, options=options)

    job = estimator.run([pub], precision=1)
```

### 4.4 Pasul 4. Post-procesare și analiza rezultatelor {#step-4-post-process-and-analyze-results}

Ceea ce am obținut de la calculatorul cuantic sunt elementele individuale ale matricelor $\tilde{S}$ și ale grupurilor de Pauli care comutează și care alcătuiesc elementele matricei $\tilde{H}$. Acești termeni trebuie combinați pentru a reconstitui matricele noastre, astfel încât să putem rezolva problema generalizată a valorilor proprii.

```python
# Store the outputs as 'results'.
results = job.result()[0]
```

#### Calculul Hamiltonianului efectiv și al matricelor de suprapunere {#calculate-effective-hamiltonian-and-overlap-matrices}

Mai întâi, calculăm faza acumulată de starea $\vert 0 \rangle$ în timpul evoluției temporale necontrolate

```python
prefactors = [
    np.exp(-1j * sum([c for p, c in H_op.to_list() if "Z" in p]) * i * dt)
    for i in range(1, krylov_dim)
]
```

Odată ce avem rezultatele execuțiilor Circuit-ului, putem post-procesa datele pentru a calcula elementele matricei $S$

```python
# Assemble S, the overlap matrix of dimension D:
S_first_row = np.zeros(krylov_dim, dtype=complex)
S_first_row[0] = 1 + 0j

# Add in ancilla-only measurements:
for i in range(krylov_dim - 1):
    # Get expectation values from experiment
    expval_real = results.data.evs[0][0][i]  # automatic extrapolated evs if ZNE is used
    expval_imag = results.data.evs[1][0][i]  # automatic extrapolated evs if ZNE is used

    # Get expectation values
    expval = expval_real + 1j * expval_imag
    S_first_row[i + 1] += prefactors[i] * expval

S_first_row_list = S_first_row.tolist()  # for saving purposes

S_circ = np.zeros((krylov_dim, krylov_dim), dtype=complex)

# Distribute entries from first row across matrix:
for i, j in it.product(range(krylov_dim), repeat=2):
    if i >= j:
        S_circ[j, i] = S_first_row[i - j]
    else:
        S_circ[j, i] = np.conj(S_first_row[j - i])
```
