---
title: "Exemple și aplicații"
sidebar_label: "Exemple și aplicații"
description: "Descriem cum pot fi aplicate algoritmii cuantici variaționali la probleme de chimie, Max-Cut și nu numai!"
notebook_path: "learning/courses/variational-algorithm-design/examples-and-applications.ipynb"
---
{/* doqumentation-source-hash: 4fe58f87 */}

<OpenInLabBanner notebookPath="learning/courses/variational-algorithm-design/examples-and-applications.ipynb" />



{/* cspell:ignore disp eigvalsh realbackend IIZZ IZIZ IZZI ZIIZ ZZII */}

În această lecție, vom explora câteva exemple de algoritmi variaționali și cum să-i aplici:

- Cum să scrii un algoritm variațional personalizat
- Cum să aplici un algoritm variațional pentru a găsi valorile proprii minime
- Cum să folosești algoritmi variaționali pentru a rezolva cazuri de utilizare aplicativă

Rețineți că framework-ul Qiskit patterns poate fi aplicat tuturor problemelor pe care le introducem aici. Totuși, pentru a evita repetițiile, vom indica explicit pașii framework-ului doar într-un singur exemplu, rulat pe hardware real.
## Definirea problemelor {#problem-definitions}

Imaginează-ți că vrem să folosim un algoritm variațional pentru a găsi valoarea proprie a următorului observabil:

$$
\hat{O}_1 = 2 II - 2 XX + 3 YY - 3 ZZ,
$$

Acest observabil are următoarele valori proprii:

$$
\left\{
\begin{array}{c}
\lambda_0 = -6 \\
\lambda_1 = 4 \\
\lambda_2 = 4 \\
\lambda_3 = 6
\end{array}
\right\}
$$

Și stările proprii:

$$
\left\{
\begin{array}{c}
|\phi_0\rangle = \frac{1}{\sqrt{2}}(|00\rangle + |11\rangle)\\
|\phi_1\rangle = \frac{1}{\sqrt{2}}(|00\rangle - |11\rangle)\\
|\phi_2\rangle = \frac{1}{\sqrt{2}}(|01\rangle - |10\rangle)\\
|\phi_3\rangle = \frac{1}{\sqrt{2}}(|01\rangle + |10\rangle)
\end{array}
\right\}
$$

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-ibm-runtime rustworkx scipy
```

```python
from qiskit.quantum_info import SparsePauliOp

observable_1 = SparsePauliOp.from_list([("II", 2), ("XX", -2), ("YY", 3), ("ZZ", -3)])
```

## VQE personalizat {#custom-vqe}

Mai întâi vom explora cum să construiești o instanță VQE manual pentru a găsi cea mai mică valoare proprie a lui $\hat{O}_1$. Aceasta va incorporate o varietate de tehnici pe care le-am acoperit de-a lungul acestui curs.

```python
def cost_func_vqe(params, ansatz, hamiltonian, estimator):
    """Return estimate of energy from estimator

    Parameters:
        params (ndarray): Array of ansatz parameters
        ansatz (QuantumCircuit): Parameterized ansatz circuit
        hamiltonian (SparsePauliOp): Operator representation of Hamiltonian
        estimator (Estimator): Estimator primitive instance

    Returns:
        float: Energy estimate
    """
    pub = (ansatz, hamiltonian, params)
    cost = estimator.run([pub]).result()[0].data.evs

    return cost
```

```python
from qiskit.circuit.library.n_local import n_local
from qiskit import QuantumCircuit

import numpy as np

reference_circuit = QuantumCircuit(2)
reference_circuit.x(0)

variational_form = n_local(
    num_qubits=2,
    rotation_blocks=["rz", "ry"],
    entanglement_blocks="cx",
    entanglement="linear",
    reps=1,
)

raw_ansatz = reference_circuit.compose(variational_form)
raw_ansatz.decompose().draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/variational-algorithm-design/examples-and-applications/extracted-outputs/57c071aa-e8ab-4b39-b5db-773016da2550-0.avif)

Vom începe depanarea pe simulatoare locale.

```python
from qiskit.primitives import StatevectorEstimator as Estimator
from qiskit.primitives import StatevectorSampler as Sampler

estimator = Estimator()
sampler = Sampler()
```
