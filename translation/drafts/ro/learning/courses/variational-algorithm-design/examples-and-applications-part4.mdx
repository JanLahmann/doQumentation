### Experimentarea cu diferiți optimizatori {#experimenting-with-different-optimizers}

Poți ajusta optimizatorul folosind argumentul `method` al funcției `minimize` din SciPy, cu mai multe opțiuni [găsite aici](https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.minimize.html). Inițial am folosit un minimizator cu constrângeri (`COBYLA`). În acest exemplu, vom explora folosirea unui minimizator fără constrângeri (`BFGS`) în schimb

```python
import time

start_time = time.time()

result = minimize(
    cost_func_vqe, x0, args=(raw_ansatz, observable_1, estimator), method="BFGS"
)

end_time = time.time()
execution_time = end_time - start_time
```

```python
print("CHANGED TO BFGS OPTIMIZER:")
print(f"""Number of iterations: {result.nfev}""")
print(f"""Time (s): {execution_time}""")
```

```text
CHANGED TO BFGS OPTIMIZER:
Number of iterations: 117
Time (s): 0.31656408309936523
```

## Exemplu VQD {#vqd-example}
Aici implementăm explicit framework-ul Qiskit patterns.
### Pasul 1: Maparea intrărilor clasice la o problemă cuantică {#step-1-map-classical-inputs-to-a-quantum-problem}

Acum, în loc să căutăm doar cea mai mică valoare proprie a observabililor noștri, vom căuta toate $4$, (unde $k=4$).

Ține minte că funcțiile de cost ale VQD sunt:

$$
C_{l}(\vec{\theta}) :=
\langle \psi(\vec{\theta}) | \hat{H} | \psi(\vec{\theta})\rangle +
\sum_{j=0}^{l-1}\beta_j |\langle \psi(\vec{\theta})| \psi(\vec{\theta^j})\rangle  |^2
\quad \forall l\in\{1,\cdots,k\}=\{1,\cdots,4\}
$$

Acest lucru este deosebit de important deoarece un vector $\vec{\beta}=(\beta_0,\cdots,\beta_{k-1})$ (în acest caz $(\beta_0, \beta_1, \beta_2, \beta_3)$) trebuie transmis ca argument atunci când definim obiectul `VQD`.

De asemenea, în implementarea Qiskit a VQD, în loc să considerăm observabilii efectivi descriși în notebook-ul anterior, fidelitățile $|\langle \psi(\vec{\theta})| \psi(\vec{\theta^j})\rangle  |^2$ sunt calculate direct prin algoritmul `ComputeUncompute`, care folosește un Sampler primitiv pentru a eșantiona probabilitatea de a obține $|0\rangle$ pentru Circuit-ul
$U_A^\dagger(\vec{\theta})U_A(\vec{\theta^j})$. Aceasta funcționează tocmai pentru că această probabilitate este

$$
\begin{aligned}

p_0

& = |\langle 0|U_A^\dagger(\vec{\theta})U_A(\vec{\theta^j})|0\rangle|^2 \\[1mm]

& = |\big(\langle 0|U_A^\dagger(\vec{\theta})\big)\big(U_A(\vec{\theta^j})|0\rangle\big)|^2 \\[1mm]

& = |\langle \psi(\vec{\theta}) |\psi(\vec{\theta^j}) \rangle|^2 \\[1mm]

\end{aligned}
$$

```python
ansatz = n_local(
    num_qubits=2,
    rotation_blocks=["ry", "rz"],
    entanglement_blocks="cz",
    #    entanglement="linear",
    reps=1,
)

ansatz.decompose().draw("mpl")
```

![Output of the previous code cell](/learning/images/courses/variational-algorithm-design/examples-and-applications/extracted-outputs/e4d59381-43ff-4322-a6f2-df55cef18536-0.avif)

Să examinăm următorul observabil:

$$
\hat{O}_2 := 2 II - 3 XX + 2 YY - 4 ZZ
$$

Acest observabil are următoarele valori proprii:

$$
\left\{
\begin{array}{c}
\lambda_0 = -7 \\
\lambda_1 = 3\\
\lambda_2 = 5 \\
\lambda_3 = 7
\end{array}
\right\}
$$

Și stările proprii:

$$
\left\{
\begin{array}{c}
|\phi_0\rangle = \frac{1}{\sqrt{2}}(|00\rangle + |11\rangle)\\
|\phi_1\rangle = \frac{1}{\sqrt{2}}(|00\rangle - |11\rangle)\\
|\phi_2\rangle = \frac{1}{\sqrt{2}}(|01\rangle + |10\rangle)\\
|\phi_3\rangle = \frac{1}{\sqrt{2}}(|01\rangle - |10\rangle)
\end{array}
\right\}
$$

```python
from qiskit.quantum_info import SparsePauliOp

observable_2 = SparsePauliOp.from_list([("II", 2), ("XX", -3), ("YY", 2), ("ZZ", -4)])
```
