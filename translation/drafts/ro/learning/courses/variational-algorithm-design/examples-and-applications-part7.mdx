### Pasul 2: Optimizarea problemei pentru execuție cuantică {#step-2-optimize-problem-for-quantum-execution}

Pentru a rula acest lucru pe hardware real, trebuie să optimizăm circuitele cuantice pentru calculatorul cuantic ales. În scopurile noastre de aici, vom folosi pur și simplu cel mai puțin ocupat Backend.

```python
from qiskit_ibm_runtime import SamplerV2 as Sampler
from qiskit_ibm_runtime import EstimatorV2 as Estimator
from qiskit_ibm_runtime import Session, EstimatorOptions
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
backend = service.least_busy(operational=True, simulator=False)
# Or use a specific backend
# backend = service.backend("ibm_brisbane")
print(backend)
```

```text
<IBMBackend('ibm_brisbane')>
```

Vom transpila circuitul nostru folosind un preset pass manager cu nivelul de optimizare 3.

```python
pm = generate_preset_pass_manager(backend=backend, optimization_level=3)
isa_ansatz = pm.run(ansatz)
isa_observable = observable_2.apply_layout(layout=isa_ansatz.layout)
```

### Pasul 3: Execuție folosind primitivele Qiskit {#step-3-execute-using-qiskit-primitives}

Asigurându-ne că resetăm betas la valori suficient de mari, putem rula acum calculul pe hardware cuantic real.

```python
# Estimated compute resource usage: 25 minutes. Benchmarked at 24 min, 30 sec on an Eagle r3 processor on 5-30-24

k = 2
betas = [30, 50, 80]
x0 = np.zeros(8)

real_prev_states = []
real_prev_opt_parameters = []
real_eigenvalues = []

realbackend = 1

estimator_options = EstimatorOptions(resilience_level=1, default_shots=10_000)

with Session(backend=backend) as session:
    estimator = Estimator(mode=session, options=estimator_options)
    sampler = Sampler(mode=session)

    for step in range(1, k + 1):
        if step > 1:
            real_prev_states.append(isa_ansatz.assign_parameters(prev_opt_parameters))

        result = minimize(
            cost_func_vqd,
            x0,
            args=(
                isa_ansatz,
                real_prev_states,
                step,
                betas,
                estimator,
                sampler,
                isa_observable,
                realbackend,
                backend,
            ),
            method="COBYLA",
            options={"maxiter": 200},
        )
        print(result)

        real_prev_opt_parameters = result.x
        real_eigenvalues.append(result.fun)

session.close()
print(real_eigenvalues)
```

### Pasul 4: Post-procesare, returnarea rezultatului în format clasic {#step-4-post-process-return-result-in-classical-format}

Rezultatul nostru este structural similar cu ceea ce a fost discutat în lecțiile și exemplele anterioare. Există însă ceva problematic în rezultatele de mai sus, din care putem extrage un mesaj de avertisment în contextul stărilor excitate. Pentru a limita timpul de calcul utilizat în acest exemplu de învățare, am setat un număr maxim de iterații pentru optimizatorul clasic care era potențial prea mic: 200 de iterații. Un calcul anterior de mai sus, pe un simulator, nu a converges în 200 de iterații. Al nostru a converges... dar la ce toleranță? Nu am specificat o toleranță pentru ca COBYLA să se considere „converged". O privire rapidă la valoarea funcției și comparația cu rulările anterioare ne spune că COBYLA nu era aproape de convergență la precizia necesară.

Există o altă problemă: energia primei stări excitate pare să fie mai mică decât energia stării fundamentale! Încearcă să explici cum s-ar putea întâmpla asta. Indiciu: este legat de punctul de convergență pe care tocmai l-am abordat. Acest comportament este explicat în detaliu mai jos, după aplicarea VQD moleculei H2.
