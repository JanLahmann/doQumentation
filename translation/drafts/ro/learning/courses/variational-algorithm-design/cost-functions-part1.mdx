---
title: "Funcții de cost"
sidebar_label: "Funcții de cost"
description: "Această lecție explică ce este o funcție de cost, cum este utilizată în algoritmii variaționali și cum poate diferi de Hamiltonian."
notebook_path: "learning/courses/variational-algorithm-design/cost-functions.ipynb"
---
{/* doqumentation-source-hash: ad031a27 */}

<OpenInLabBanner notebookPath="learning/courses/variational-algorithm-design/cost-functions.ipynb" />



{/* cspell:ignore resil nbsp eigenbasis expvals IIZZ IZIZ IZZI ZIIZ ZZII */}

În această lecție vom învăța cum să evaluăm o *funcție de cost*:

- Mai întâi, vom afla despre [primitivele Qiskit Runtime](/guides/primitives)
- Vom defini o *funcție de cost* $C(\vec\theta)$. Aceasta este o funcție specifică problemei care definește obiectivul problemei pentru ca optimizatorul să îl minimizeze (sau maximizeze)
- Vom defini o strategie de măsurare cu primitivele Qiskit Runtime pentru a optimiza viteza față de acuratețe

&nbsp;

![A diagram showing key components of a cost function including using primitives like estimator and sampler.](/learning/images/courses/variational-algorithm-design/cost-functions/cost-function-workflow.svg)
## Primitive {#primitives}

Toate sistemele fizice, fie clasice, fie cuantice, pot exista în stări diferite. De exemplu, o mașină pe un drum poate avea o anumită masă, poziție, viteză sau accelerație care îi caracterizează starea. În mod similar, sistemele cuantice pot avea, de asemenea, configurații sau stări diferite, dar se deosebesc de sistemele clasice prin modul în care tratăm măsurătorile și evoluția stărilor. Aceasta conduce la proprietăți unice, cum ar fi *superpunerea* și *entanglementul*, exclusive mecanicii cuantice. Așa cum putem descrie starea unei mașini folosind proprietăți fizice precum viteza sau accelerația, putem descrie și starea unui sistem cuantic folosind *observabile*, care sunt obiecte matematice.

În mecanica cuantică, stările sunt reprezentate prin vectori coloană complecși normalizați, sau *kets* ($|\psi\rangle$), iar observabilele sunt operatori liniari Hermitian ($\hat{H}=\hat{H}^{\dagger}$) care acționează asupra kets. Un vector propriu ($|\lambda\rangle$) al unui observabil este cunoscut sub numele de *stare proprie*. Măsurarea unui observabil pentru una dintre stările sale proprii ($|\lambda\rangle$) ne va oferi valoarea proprie corespunzătoare ($\lambda$) ca rezultat.

Dacă te întrebi cum să măsori un sistem cuantic și ce poți măsura, Qiskit oferă două <DefinitionTooltip definition="Operații de bază și fundamentale sau tipuri de date. Qiskit dispune de primitivele Sampler și Estimator pentru a servi drept blocuri de construcție pentru a construi cu ușurință sarcini de lucru algoritmice complexe.">primitive</DefinitionTooltip> care pot ajuta:

* `Sampler`: Dată o stare cuantică $|\psi\rangle$, această primitivă obține probabilitatea fiecărei stări posibile din baza computațională.
* `Estimator`: Dat un observabil cuantic $\hat{H}$ și o stare $|\psi\rangle$, această primitivă calculează valoarea așteptată a lui $\hat{H}$.
### Primitiva Sampler {#the-sampler-primitive}

Primitiva `Sampler` calculează probabilitatea de a obține fiecare stare posibilă $|k\rangle$ din baza computațională, dată un circuit cuantic care pregătește starea $|\psi\rangle$. Aceasta calculează

$$
p_k = |\langle k | \psi \rangle|^2 \quad \forall k \in \mathbb{Z}_2^n \equiv \{0,1,\cdots,2^n-1\},
$$

Unde $n$ este numărul de qubiți, iar $k$ este reprezentarea întreagă a oricărui șir binar de ieșire posibil $\{0,1\}^n$ (adică, numere întregi în baza $2$).

Qiskit Runtime [`Sampler`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/sampler-v2) rulează circuitul de mai multe ori pe un dispozitiv cuantic, efectuând măsurători la fiecare rulare, și reconstruind distribuția de probabilitate din șirurile de biți recuperate. Cu cât mai multe rulări (sau *shots*) efectuează, cu atât rezultatele vor fi mai precise, dar aceasta necesită mai mult timp și resurse cuantice.

Cu toate acestea, deoarece numărul de ieșiri posibile crește exponențial cu numărul de qubiți $n$ (adică, $2^n$), numărul de shots va trebui să crească exponențial și el pentru a capta o distribuție de probabilitate _densă_. Prin urmare, `Sampler` este eficient doar pentru distribuții de probabilitate *rare* (sparse); unde starea țintă $|\psi\rangle$ trebuie să fie exprimabilă ca o combinație liniară a stărilor din baza computațională, cu numărul de termeni crescând cel mult polinomial cu numărul de qubiți:

$$
|\psi\rangle = \sum^{\text{Poly}(n)}_k w_k |k\rangle.
$$

`Sampler` poate fi, de asemenea, configurat pentru a recupera probabilitățile dintr-o subsecțiune a circuitului, reprezentând un subset din totalul stărilor posibile.
### Primitiva Estimator {#the-estimator-primitive}

Primitiva `Estimator` calculează valoarea de așteptare a unui observabil $\hat{H}$ pentru o stare cuantică $|\psi\rangle$; unde probabilitățile observabilului pot fi exprimate ca $p_\lambda = |\langle\lambda|\psi\rangle|^2$, fiind $|\lambda\rangle$ stările proprii ale observabilului $\hat{H}$. Valoarea de așteptare este definită ca media tuturor rezultatelor posibile $\lambda$ (adică, valorile proprii ale observabilului) ale unei măsurători a stării $|\psi\rangle$, ponderate de probabilitățile corespunzătoare:

$$
\langle\hat{H}\rangle_\psi := \sum_\lambda p_\lambda \lambda = \langle \psi | \hat{H} | \psi \rangle
$$

Cu toate acestea, calcularea valorii de așteptare a unui observabil nu este întotdeauna posibilă, deoarece adesea nu cunoaștem eigenbasis-ul său. Qiskit Runtime [`Estimator`](https://docs.quantum.ibm.com/api/qiskit-ibm-runtime/estimator-v2) utilizează un proces algebric complex pentru a estima valoarea de așteptare pe un dispozitiv cuantic real, descompunând observabilul într-o combinație de alți observabili al căror eigenbasis îl cunoaștem.

Mai simplu, `Estimator` descompune orice observabil pe care nu știe cum să îl măsoare în observabile mai simple, măsurabile, numite <DefinitionTooltip definition="Set de matrici folosite frecvent în calculul cuantic pentru a reprezenta și manipula stări cuantice, constând din matricea identitate și cele trei matrici Pauli (X, Y și Z).">operatori Pauli</DefinitionTooltip>.
Orice operator poate fi exprimat ca o combinație de $4^n$ operatori Pauli.

$$
\hat{P}_k :=
\sigma_{k_{n-1}}\otimes \cdots \otimes \sigma_{k_0} \quad
\forall k \in \mathbb{Z}_4^n \equiv \{0,1,\cdots,4^n-1\}, \\
$$

astfel că

$$
\hat{H} = \sum^{4^n-1}_{k=0} w_k \hat{P}_k
$$

unde $n$ este numărul de qubiți, $k \equiv k_{n-1} \cdots k_0$ pentru $k_l \in \mathbb{Z}_4 \equiv \{0, 1, 2, 3\}$ (adică, numere întregi în baza $4$), și $(\sigma_0, \sigma_1, \sigma_2, \sigma_3) := (I, X, Y, Z)$.

După efectuarea acestei descompuneri, `Estimator` derivă un nou circuit $V_k|\psi\rangle$ pentru fiecare observabil $\hat{P}_k$ (din circuitul original), pentru a *diagonaliza* efectiv observabilul Pauli în baza computațională și a-l măsura. Putem măsura cu ușurință observabilele Pauli deoarece cunoaștem $V_k$ în avans, ceea ce nu este cazul în general pentru alți observabili.

Pentru fiecare $\hat{P}_{k}$, `Estimator` rulează circuitul corespunzător pe un dispozitiv cuantic de mai multe ori, măsoară starea de ieșire în baza computațională și calculează probabilitatea $p_{kj}$ de a obține fiecare ieșire posibilă $j$. Apoi caută valoarea proprie $\lambda_{kj}$ a lui $P_k$ corespunzătoare fiecărei ieșiri $j$, înmulțește cu $w_k$ și adaugă toate rezultatele împreună pentru a obține valoarea așteptată a observabilului $\hat{H}$ pentru starea dată $|\psi\rangle$.

$$
\langle\hat{H}\rangle_\psi =
\sum_{k=0}^{4^n-1} w_k \sum_{j=0}^{2^n-1}p_{kj} \lambda_{kj},
$$
Deoarece calcularea valorii de așteptare a $4^n$ operatori Pauli este impractică (adică, creștere exponențială), `Estimator` poate fi eficient doar atunci când un număr mare de $w_k$ sunt zero (adică, descompunere Pauli *rară* în loc de *densă*). Formal spunem că, pentru ca această calculare să fie *eficient rezolvabilă*, numărul de termeni nenuli trebuie să crească cel mult polinomial cu numărul de qubiți $n$: $\hat{H} = \sum^{\text{Poly}(n)}_k w_k \hat{P}_k.$

Cititorul poate observa ipoteza implicită că <DefinitionTooltip definition="Procesul de a efectua mai multe măsurători ale unuia sau mai multor lucruri.">eșantionarea</DefinitionTooltip> probabilităților trebuie să fie, de asemenea, eficientă, așa cum s-a explicat pentru `Sampler`, ceea ce înseamnă

$$
\langle\hat{H}\rangle_\psi =
\sum_{k}^{\text{Poly}(n)} w_k \sum_{j}^{\text{Poly}(n)}p_{kj} \lambda_{kj}.
$$
