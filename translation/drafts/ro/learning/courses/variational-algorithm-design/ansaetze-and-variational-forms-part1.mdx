---
title: "ansaetze și forme variationale"
sidebar_label: "ansaetze și forme variationale"
description: "Această lecție explică ce este un ansatz și cum diferă de starea de referință. Sunt prezentate exemple de ansaetze."
notebook_path: "learning/courses/variational-algorithm-design/ansaetze-and-variational-forms.ipynb"
---
{/* doqumentation-source-hash: 02ea34ef */}


<OpenInLabBanner notebookPath="learning/courses/variational-algorithm-design/ansaetze-and-variational-forms.ipynb" />



{/* cspell:ignore ZZII IZZI ZIIZ IZIZ IIZZ ansä */}

# Ansaetze și forme variationale {#ansaetze-and-variational-forms}

În centrul tuturor algoritmilor variationale se află ideea cheie de a analiza diferențele dintre stări, care sunt legate convenabil printr-o mapare bine comportată (de exemplu, continuă, diferențiabilă) dintr-un set de parametri sau variabile — de unde și numele.

Mai întâi, vom explora cum să construim circuite parametrizate manual. Vom folosi aceste circuite pentru a defini o <DefinitionTooltip definition="Straturi de porți parametrizate care sunt repetate de un anumit număr de ori, iar parametrii porților sunt optimizați în timpul algoritmului pentru a minimiza funcția de cost.">formă variatională</DefinitionTooltip> ce reprezintă o colecție de stări parametrizate pe care algoritmul nostru variational le va explora. Apoi, vom construi <DefinitionTooltip definition="Combinația dintre un operator de referință și o formă variatională, pentru a descrie spațiul de căutare pe care îl explorăm.">ansatz</DefinitionTooltip>-ul nostru aplicând această formă variatională asupra stării de referință.

Vom explora, de asemenea, cum să facem compromisuri între viteză și acuratețe în timp ce explorăm acest spațiu de căutare.

![O diagramă care arată componentele cheie ale discuției despre ansatz, inclusiv ansaetze euristice și ansaetze specifice problemei.](/learning/images/courses/variational-algorithm-design/ansaetze-and-variational-forms/ansatz-workflow.svg)
## Circuite Cuantice Parametrizate {#parameterized-quantum-circuits}

Algoritmii variationale funcționează explorând și comparând o gamă de stări cuantice $|\psi(\vec{\theta})\rangle$, care depind de un set finit de $k$ parametri $\vec{\theta} = (\theta^0, \ldots, \theta^{k-1})$. Aceste stări pot fi pregătite folosind un circuit cuantic parametrizat, unde porțile sunt definite cu parametri reglabili. Este posibil să creezi acest circuit parametrizat fără a lega unghiuri specifice încă:

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit rustworkx
```

```python
from qiskit.circuit import QuantumCircuit, Parameter

theta = Parameter("θ")

qc = QuantumCircuit(3)
qc.rx(theta, 0)
qc.cx(0, 1)
qc.x(2)

qc.draw("mpl")
```

![Rezultatul celulei de cod anterioare](/learning/images/courses/variational-algorithm-design/ansaetze-and-variational-forms/extracted-outputs/4c564fab-ed65-4949-b27f-658b0f1ed621-0.avif)

```python
from math import pi

angle_list = [pi / 3, pi / 2]
circuits = [qc.assign_parameters({theta: angle}) for angle in angle_list]

for circuit in circuits:
    display(circuit.draw("mpl"))
```

![Rezultatul celulei de cod anterioare](/learning/images/courses/variational-algorithm-design/ansaetze-and-variational-forms/extracted-outputs/4096d161-2916-472b-8abc-d4a6b78955cf-0.avif)

![Rezultatul celulei de cod anterioare](/learning/images/courses/variational-algorithm-design/ansaetze-and-variational-forms/extracted-outputs/4096d161-2916-472b-8abc-d4a6b78955cf-1.avif)

## Formă Variatională și Ansatz {#variational-form-and-ansatz}

Pentru a optimiza iterativ de la o stare de referință $|\rho\rangle$ la o stare țintă $|\psi(\vec\theta)\rangle$, trebuie să definim o formă variatională $U_V(\vec{\theta})$ ce reprezintă o colecție de stări parametrizate pe care algoritmul nostru variational le va explora:

$$
\begin{aligned}
|0\rangle \xrightarrow{U_R} U_R|0\rangle

& = |\rho\rangle \xrightarrow{U_V(\vec{\theta})} U_A(\vec{\theta})|0\rangle \\[1mm]

& = U_V(\vec{\theta})U_R|0\rangle \\[1mm]

& = U_V(\vec{\theta})|\rho\rangle \\[1mm]

& = |\psi(\vec{\theta})\rangle \\[1mm]

\end{aligned}
$$

Observă că starea parametrizată depinde atât de starea de referință $|\rho\rangle$, care nu depinde de niciun parametru, cât și de forma variatională $U_V(\vec{\theta})$, care depinde întotdeauna de parametri. Ne referim la combinația acestor două jumătăți ca un ansatz: $U_A(\vec\theta) := U_V(\vec\theta)U_R$.

Pe măsură ce construim ansatz-ul nostru pentru a reprezenta o colecție de stări parametrizate pe care algoritmul nostru variational le va explora, realizăm o problemă importantă: dimensionalitatea. Un sistem de $n$ qubiți (adică, spațiul Hilbert) are un număr vast de stări cuantice distincte în spațiul de configurație. Ar fi nevoie de un număr incomod de mare de parametri pentru a-l explora complet. Cantitativ, dimensionalitatea sa este $D = 2^{2n}$. Pentru a înrăutăți lucrurile, complexitatea de execuție a algoritmilor de căutare și a altora similari crește exponențial cu această dimensionalitate, un fenomen adesea numit în literatura de specialitate blestemul dimensionalității.

Pentru a contracara acest inconvenient, este o practică obișnuită să se impună unele constrângeri rezonabile asupra formei variationale, astfel încât să fie explorate doar cele mai relevante stări. Găsirea unui ansatz trunchiat eficient este un domeniu activ de cercetare, dar vom acoperi două modele comune.
## Ansaetze euristice și compromisuri {#heuristic-ansaetze-and-trade-offs}

Dacă nu ai nicio informație despre problema ta particulară care să poată ajuta la reducerea dimensionalității, poți încerca o familie arbitrară de circuite parametrizate cu mai puțin de $2^{2n}$ parametri. Cu toate acestea, există câteva compromisuri de luat în considerare:

- **Viteză**: Prin reducerea spațiului de căutare, algoritmul poate rula mai rapid.
- **Acuratețe**: Reducerea spațiului ar putea risca excluderea soluției reale a problemei, ducând la soluții sub-optime.
- **Zgomot**: Circuitele mai adânci sunt afectate de zgomot, așa că trebuie să experimentăm cu conectivitatea, porțile și fidelitatea porților ansatz-ului nostru.

Există un compromis fundamental între calitate (sau chiar rezolvabilitate) și viteză: cu cât sunt mai mulți parametri, cu atât este mai probabil să găsești un rezultat precis, dar va dura mai mult timp să rulezi algoritmul.
### Circuite N-locale {#n-local-circuits}

Unul dintre cele mai utilizate exemple de ansaetze euristice sunt [circuitele N-locale](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.NLocal), din câteva motive:

- **Implementare eficientă**: Ansatz-ul N-local este de obicei compus din porți simple, locale, care pot fi implementate eficient pe un calculator cuantic, folosind un număr mic de qubiți fizici. Acest lucru face mai ușoară construcția și optimizarea circuitelor cuantice.
- **Surprinde corelații importante**: Ansatz-ul N-local poate surprinde corelații importante între qubiții dintr-un sistem cuantic, chiar și cu un număr mic de porți. Aceasta se datorează faptului că porțile locale pot acționa asupra qubiților vecini și pot crea entanglament între ei, ceea ce poate fi important pentru simularea sistemelor cuantice complexe.

Aceste circuite constau din straturi de rotație și entanglament care se repetă alternativ de una sau mai multe ori, astfel:

- Fiecare strat este format din porți de dimensiune cel mult $N$, unde $N$ trebuie să fie mai mic decât numărul de qubiți.
- Pentru un strat de rotație, porțile sunt stivuite una peste alta. Putem folosi operații de rotație standard, cum ar fi [`RX`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.RXGate) sau [`CRZ`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.CRZGate).
- Pentru un strat de entanglament, putem folosi porți precum [porțile `Toffoli`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.CCXGate) sau [`CX`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.CXGate) cu o strategie de entanglament.
- Ambele tipuri de straturi pot fi parametrizate sau nu, dar cel puțin unul dintre ele trebuie să conțină parametri. Altfel, fără cel puțin un parametru, nu ar exista nicio variație!
- Opțional, un strat suplimentar de rotație este adăugat la sfârșitul circuitului.

De exemplu, să creăm un circuit [`NLocal`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.NLocal) cu cinci qubiți, cu blocuri de rotație formate din porți [`RX`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.RXGate) și [`CRZ`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.CRZGate), blocuri de entanglament formate din [porți `Toffoli`](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.CCXGate) care acționează pe qubiții $[0,1,2]$, $[0,2,3]$, $[4,2,1]$ și $[3,1,0]$, și $2$ repetări ale fiecărui strat.

```python
from qiskit.circuit.library import NLocal, CCXGate, CRZGate, RXGate
from qiskit.circuit import Parameter

theta = Parameter("θ")
ansatz = NLocal(
    num_qubits=5,
    rotation_blocks=[RXGate(theta), CRZGate(theta)],
    entanglement_blocks=CCXGate(),
    entanglement=[[0, 1, 2], [0, 2, 3], [4, 2, 1], [3, 1, 0]],
    reps=2,
    insert_barriers=True,
)
ansatz.decompose().draw("mpl")
```

![Rezultatul celulei de cod anterioare](/learning/images/courses/variational-algorithm-design/ansaetze-and-variational-forms/extracted-outputs/c28d7b53-fbd7-42f5-a619-5a6b0b44e9fc-0.avif)

În exemplul de mai sus, cea mai mare poartă este poarta Toffoli, care acționează pe trei qubiți, făcând circuitul $3$-local. Cel mai frecvent utilizat tip de circuite $N$-locale sunt circuitele [$2$-locale](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.TwoLocal) cu porți de rotație pe un singur qubit și porți de entanglament pe $2$ qubiți.
