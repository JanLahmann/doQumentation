### Verifică-ți înțelegerea

Citește întrebările de mai jos, gândește-te la răspunsuri, apoi apasă pe triunghiuri pentru a vedea soluțiile.
<details>
<summary>

Care este starea $|\pi_1\rangle$?

</summary>

__Răspuns:__

Aplicarea unui Hadamard transformă starea $|0\rangle$ în $\frac{1}{\sqrt{2}}(|0\rangle+|1\rangle)$ și starea $|1\rangle$ în $\frac{1}{\sqrt{2}}(|0\rangle-|1\rangle)$. Astfel, starea completă devine: $|\pi_1\rangle = [\frac{|0\rangle-|1\rangle}{\sqrt{2}}][\frac{|0\rangle+|1\rangle}{\sqrt{2}}]$

</details>

<details>
<summary>

Care este starea $|\pi_2\rangle$?

</summary>

__Răspuns:__

Înainte de a aplica $U_f$, să ne amintim ce face aceasta. Ea va schimba starea Qubitului 1 în funcție de starea Qubitului 0. Prin urmare, are sens să factorizăm starea Qubitului 0: $|\pi_1\rangle = \frac{1}{2} (|0\rangle-|1\rangle)|0\rangle+\frac{1}{2}(|0\rangle-|1\rangle)|1\rangle$. Apoi, dacă $f(0)=f(1)$, cele două termene se vor transforma la fel, iar semnul relativ dintre ele rămâne pozitiv; dar dacă $f(0)\neq f(1)$, înseamnă că al doilea termen va dobândi un semn minus față de primul, schimbând starea Qubitului 0 din $\frac{1}{\sqrt{2}}(|0\rangle+|1\rangle)$ în $\frac{1}{\sqrt{2}}(|0\rangle-|1\rangle)$. Deci:
$$
|\pi_2\rangle = \begin{cases}
\pm[\frac{|0\rangle-|1\rangle}{\sqrt{2}}][\frac{|0\rangle+|1\rangle}{\sqrt{2}}] & \text{if} & f(0) = f(1) \\
\pm[\frac{|0\rangle-|1\rangle}{\sqrt{2}}][\frac{|0\rangle-|1\rangle}{\sqrt{2}}] &\text{if} & f(0) \neq f(1) \\
\end{cases}
$$

</details>

<details>
<summary>

Care este starea $|\pi_3\rangle$?

</summary>

__Răspuns:__

Acum, starea Qubitului 0 este fie $\frac{1}{\sqrt{2}}(|0\rangle+|1\rangle)$, fie $\frac{1}{\sqrt{2}}(|0\rangle-|1\rangle)$, în funcție de funcție. Aplicarea Hadamard va produce fie $|0\rangle$, fie $|1\rangle$, respectiv.

$$
|\pi_3\rangle = \begin{cases}
\pm[\frac{|0\rangle-|1\rangle}{\sqrt{2}}]|0\rangle & \text{if} & f(0) = f(1) \\
\pm[\frac{|0\rangle-|1\rangle}{\sqrt{2}}]|1\rangle &\text{if} & f(0) \neq f(1) \\
\end{cases}
$$

</details>

Analizând răspunsurile tale la întrebările de mai sus, observi că se întâmplă ceva oarecum surprinzător. Deși $U_f$ nu face nimic explicit asupra stării Qubitului 0, deoarece schimbă Qubitul 1 în funcție de starea Qubitului 0, se poate întâmpla ca acest lucru să cauzeze o schimbare de fază în Qubitul 0. Acest fenomen este cunoscut sub numele de „phase-kickback" și este discutat în detaliu în [Lecția 1: Algoritmi de interogare cuantică](/learning/courses/fundamentals-of-quantum-algorithms/quantum-query-algorithms/introduction) din cursul Fundamentele Algoritmilor Cuantici.

Acum că înțelegem cum funcționează acest algoritm, hai să îl implementăm cu Qiskit.

```python
## Deutsch's algorithm:

## Step 1: Map the problem

# first, convert oracle circuit (above) to a single gate for drawing purposes. otherwise, the circuit is too large to display
blackbox = twobit_function(
    3
).to_gate()  # you may edit the number (1-4) inside "twobit_function()" to select among the four valid functions
blackbox.label = "$U_f$"

qc_deutsch = QuantumCircuit(2, 1)

qc_deutsch.x(1)
qc_deutsch.h(range(2))

qc_deutsch.barrier()
qc_deutsch.compose(twobit_function(2), inplace=True)
qc_deutsch.barrier()

qc_deutsch.h(0)
qc_deutsch.measure(0, 0)

qc_deutsch.draw("mpl")
```

![Output of the previous code cell](/learning/images/modules/computer-science/deutsch-jozsa/extracted-outputs/4d9129df-f2ef-4f94-9508-21ed986fd823-0.avif)

```python
# Step 2: Transpile
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

target = backend.target
pm = generate_preset_pass_manager(target=target, optimization_level=3)

qc_isa = pm.run(qc_deutsch)
```

```python
# Step 3: Run the job on a real quantum computer

job = sampler.run([qc_isa], shots=1)
# job = sampler_sim.run([qc_isa],shots=1) # uncomment this line to run on simulator instead
res = job.result()
counts = res[0].data.c.get_counts()
```

```python
# Step 4: Visualize and analyze results

## Analysis
print(counts)
if "1" in counts:
    print("balanced")
else:
    print("constant")
```

```text
{'1': 1}
balanced
```
