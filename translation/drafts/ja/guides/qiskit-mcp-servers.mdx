---
title: Qiskit MCPサーバー
description: Qiskit MCPサーバーを使用して、量子コンピューティング機能をAIアシスタントやエージェントに統合します。
---
{/* doqumentation-source-hash: d39622e3 */}

{/* cspell:ignore modelcontextprotocol  */}

# Qiskit MCPサーバー

Qiskit MCPサーバーは、量子コンピューティング機能をAIシステムに統合する[Model Context Protocol (MCP)](https://modelcontextprotocol.io/)サーバーのコレクションです。これらのサーバーにより、AIアシスタント、大規模言語モデル（LLM）、およびエージェントがIBM Quantum&reg;サービスとQiskitライブラリにアクセスできるようになります。

<Admonition type="note">
    これはオープンソースプロジェクトです。最新情報、詳細なAPIカバレッジ、および高度な設定については、[Qiskit MCPサーバーのGitHubリポジトリ](https://github.com/Qiskit/mcp-servers)をご参照ください。
</Admonition>

## Qiskit MCPサーバーをいつ使うべきか？ {#when-should-i-use-qiskit-mcp-servers}

Qiskit MCPサーバーを使用すると、AIアシスタントが次のような量子コンピューティングタスクをサポートできるようになります。

- 量子回路の作成、操作、およびトランスパイル
- 信頼できる情報源からの量子コンピューティングに関する質問への直接回答
- コンテキストを考慮した量子コードの生成
- AIを活用したトランスパイルによる回路の最適化
- 実際の量子ハードウェアへの自動接続
- 最適な量子バックエンドの分析と選択
- 量子回路の実行とジョブステータスの追跡

## 含まれる内容 {#what-is-included}

以下のMCPサーバーが利用可能です。

### Qiskit MCPサーバー {#qiskit-mcp-server}

ローカル開発向けのコア量子コンピューティング機能を提供します。このサーバーにより、AIアシスタントが量子回路を作成・操作・トランスパイルし、OpenQASM 3やQPYなどの形式にシリアライズできるようになります。

### Qiskit Code Assistant MCPサーバー {#qiskit-code-assistant-mcp-server}

[Qiskit Code Assistant](/guides/qiskit-code-assistant)を通じて、インテリジェントな量子コード補完とアシスタンスを提供します。このサーバーは、MCP対応のAIツールにQiskit Code Assistantの機能を直接統合します。

Qiskit Code Assistantの他の機能と同様に、このMCPサーバーは[IBM Quantum Platform](https://quantum.cloud.ibm.com/)に登録済みのIBM Quantum Premiumプランユーザーのみが使用できます。

### Qiskit Runtime MCPサーバー {#qiskit-runtime-mcp-server}

Qiskit Runtimeを通じて、IBM Quantumクラウドサービスへのアクセスを提供します。これにより、AIアシスタントが量子バックエンドと対話し、ジョブを送信して結果を取得できるようになります。

### Qiskit IBM Transpiler MCPサーバー {#qiskit-ibm-transpiler-mcp-server}

[AI搭載トランスパイラーパス](/guides/ai-transpiler-passes)を通じて、AIを活用した回路最適化を提供します。このサーバーにより、AIアシスタントが高度なルーティングおよび最適化アルゴリズムを使用して量子回路を最適化できるようになります。

### コミュニティサーバー {#community-servers}

以下のコミュニティ提供のMCPサーバーも利用可能です。

#### Qiskit Gym MCPサーバー {#qiskit-gym-mcp-server}

[qiskit-gym](https://github.com/AI4quantum/qiskit-gym)ライブラリを使用した量子回路合成のための強化学習機能を提供します。このサーバーにより、AIアシスタントがRLベースの最適化手法を量子回路のトランスパイルと合成に活用できるようになります。

## Qiskit MCPサーバーのインストール {#install-qiskit-mcp-servers}

Qiskit MCPサーバーを使用するには、以下が必要です。

- Python 3.10以降（3.11以降を推奨）
- IBM QuantumアカウントとAPIトークン（手順については[IBM Cloud&reg;アカウントのセットアップ](/guides/cloud-setup)を参照）

ターミナルから以下のコマンドを実行して、利用可能なすべてのQiskit MCPサーバーをインストールします。

```bash
pip install qiskit-mcp-servers[all]
```

個別のMCPサーバーをインストールすることもできます。

```bash
pip install qiskit-mcp-server
pip install qiskit-code-assistant-mcp-server
pip install qiskit-ibm-runtime-mcp-server
pip install qiskit-ibm-transpiler-mcp-server
pip install qiskit-gym-mcp-server
```

## Qiskit MCPサーバーの設定 {#configure-qiskit-mcp-servers}

### 環境変数の設定 {#set-environment-variables}

認証に必要な環境変数を設定します。Qiskit MCPサーバーはローカル使用時に認証を必要としません。

Qiskit Runtime、Qiskit Code Assistant、またはQiskit IBM Transpiler MCPサーバーを使用する場合：

```bash
export QISKIT_IBM_TOKEN="<your_token>"
```

APIトークンは[IBM Quantumアカウント設定](https://quantum.cloud.ibm.com/)で確認できます。

### MCPクライアントの設定 {#configure-the-mcp-client}

MCP対応クライアント（Claude Desktop、Cursor、その他のAIツールなど）をQiskit MCPサーバーを使用するように設定できます。具体的な設定方法はクライアントによって異なります。たとえば、MCPクライアントの設定ファイルは次のようになります。

```json
{
  "mcpServers": {
    "qiskit": {
      "command": "qiskit-mcp-server"
    },
    "qiskit-ibm-runtime": {
      "command": "qiskit-ibm-runtime-mcp-server"
    },
    "qiskit-code-assistant": {
      "command": "qiskit-code-assistant-mcp-server"
    },
    "qiskit-ibm-transpiler": {
      "command": "qiskit-ibm-transpiler-mcp-server"
    },
    "qiskit-gym": {
      "command": "qiskit-gym-mcp-server"
    }
  }
}
```

具体的な設定手順については、お使いのMCPクライアントのドキュメントをご参照ください。

## MCPサーバーのテスト {#test-the-mcp-servers}

MCP Inspectorを使用して、MCPサーバーをインタラクティブにテストできます。

```bash
# Test the Qiskit MCP Server
npx @modelcontextprotocol/inspector qiskit-mcp-server

# Test the Qiskit Runtime MCP Server
npx @modelcontextprotocol/inspector qiskit-ibm-runtime-mcp-server

# Test the Qiskit Code Assistant MCP Server
npx @modelcontextprotocol/inspector qiskit-code-assistant-mcp-server

# Test the Qiskit IBM Transpiler MCP Server
npx @modelcontextprotocol/inspector qiskit-ibm-transpiler-mcp-server

# Test the Qiskit Gym MCP Server
npx @modelcontextprotocol/inspector qiskit-gym-mcp-server
```

## 次のステップ {#next-steps}

<Admonition type="info" title="推奨事項">

- 詳細なドキュメント、APIカバレッジ、および例については、[Qiskit MCPサーバーのGitHubリポジトリ](https://github.com/Qiskit/mcp-servers)をご覧ください。
- AIコード補完機能については、[Qiskit Code Assistant](/guides/qiskit-code-assistant)をご参照ください。
- AIを活用した回路最適化機能については、[AI搭載トランスパイラーパス](/guides/ai-transpiler-passes)をご参照ください。
- まだ設定が完了していない場合は、[IBM Quantumアカウント](/guides/cloud-setup)をセットアップしてください。
- MCPについて詳しく学ぶには、[Model Context Protocolドキュメント](https://modelcontextprotocol.io/)をご覧ください。

</Admonition>
