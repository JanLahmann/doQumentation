---
title: "Operatorクラス"
sidebar_label: "Operatorクラス"
description: "QiskitにおけるOperatorクラスの使い方についての詳細な説明です。"
notebook_path: "guides/operator-class.ipynb"
---

{/* source_hash: f33a6b98 */}

<OpenInLabBanner notebookPath="guides/operator-class.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>Package versions</b></summary>

このページのコードは、以下の要件を使用して開発されました。
これらのバージョン以降を使用することをお勧めします。

```
qiskit[all]~=2.3.0
```
</details>
このページでは、[`Operator`](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator)クラスの使い方を説明します。`Operator`クラスを含む、Qiskitにおける演算子表現の概要については、[演算子クラスの概要](./operators-overview)をご参照ください。

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit
```

```python
import numpy as np
from qiskit.circuit import QuantumCircuit
from qiskit.circuit.library import CXGate, RXGate, XGate
from qiskit.quantum_info import Operator, Pauli, process_fidelity
```

## クラスをOperatorに変換する {#convert-classes-to-operators}

Qiskitの他のいくつかのクラスは、演算子の初期化メソッドを使用して直接`Operator`オブジェクトに変換できます。例えば：

* `Pauli`オブジェクト
* `Gate`および`Instruction`オブジェクト
* `QuantumCircuit`オブジェクト

最後の点は、`Operator`クラスをユニタリシミュレータとして使用し、シミュレータバックエンドを呼び出さずに量子回路の最終的なユニタリ行列を計算できることを意味します。回路にサポートされていない操作が含まれている場合は、例外が発生します。サポートされていない操作は、測定、リセット、条件付き操作、または行列の定義や行列の定義を持つゲートによる分解を持たないゲートです。

```python
# Create an Operator from a Pauli object

pauliXX = Pauli("XX")
Operator(pauliXX)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j],
          [1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an Operator for a Gate object
Operator(CXGate())
```

```text
Operator([[1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an operator from a parameterized Gate object
Operator(RXGate(np.pi / 2))
```

```text
Operator([[0.70710678+0.j        , 0.        -0.70710678j],
          [0.        -0.70710678j, 0.70710678+0.j        ]],
         input_dims=(2,), output_dims=(2,))
```

```python
# Create an operator from a QuantumCircuit object
circ = QuantumCircuit(10)
circ.h(0)
for j in range(1, 10):
    circ.cx(j - 1, j)

# Convert circuit to an operator by implicit unitary simulation
Operator(circ)
```

```text
Operator([[ 0.70710678+0.j,  0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          ...,
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.70710678+0.j, -0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j]],
         input_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2), output_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2))
```

## 回路でOperatorを使用する {#use-operators-in-circuits}

ユニタリな`Operators`は、`QuantumCircuit.append`メソッドを使用して`QuantumCircuit`に直接挿入できます。これにより、`Operator`は`UnitaryGate`オブジェクトに変換され、回路に追加されます。

演算子がユニタリでない場合は、例外が発生します。これは`Operator.is_unitary()`関数で確認でき、演算子がユニタリであれば`True`、そうでなければ`False`を返します。

```python
# Create an operator
XX = Operator(Pauli("XX"))

# Add to a circuit
circ = QuantumCircuit(2, 2)
circ.append(XX, [0, 1])
circ.measure([0, 1], [0, 1])
circ.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/operator-class/extracted-outputs/223c07c9-1b1d-4a33-a397-091cac2ec9e3-0.svg)

上記の例では、演算子は`Pauli`オブジェクトから初期化されています。ただし、`Pauli`オブジェクトを回路に直接挿入することもでき、その場合は1量子ビットのPauliゲートのシーケンスに変換されます。

```python
# Add to a circuit
circ2 = QuantumCircuit(2, 2)
circ2.append(Pauli("XX"), [0, 1])
circ2.measure([0, 1], [0, 1])
circ2.draw()
```

```text
┌────────────┐┌─┐
q_0: ┤0           ├┤M├───
     │  Pauli(XX) │└╥┘┌─┐
q_1: ┤1           ├─╫─┤M├
     └────────────┘ ║ └╥┘
c: 2/═══════════════╩══╩═
                    0  1
```

## Operatorを組み合わせる {#combine-operators}

演算子はいくつかの方法で組み合わせることができます。

### テンソル積 {#tensor-product}

2つの演算子 $A$ と $B$ を、`Operator.tensor`関数を使ってテンソル積演算子 $A\otimes B$ に組み合わせることができます。$A$ と $B$ が両方とも1量子ビット演算子である場合、`A.tensor(B)` = $A\otimes B$ では、サブシステム0に行列 $B$、サブシステム1に行列 $A$ がインデックス付けされます。

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.tensor(B)
```

```text
Operator([[ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j],
          [ 0.+0.j, -0.+0.j,  0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### テンソル展開 {#tensor-expansion}

密接に関連する操作として`Operator.expand`があり、テンソル積と同様に機能しますが逆の順序で行われます。したがって、2つの演算子 $A$ と $B$ に対して `A.expand(B)` = $B\otimes A$ となり、サブシステム0に行列 $A$、サブシステム1に行列 $B$ がインデックス付けされます。

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.expand(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j,  0.+0.j, -0.+0.j, -1.+0.j],
          [ 0.+0.j,  0.+0.j, -1.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### 合成 {#composition}

`Operator.compose`メソッドを使用して、2つの演算子 $A$ と $B$ を合成し、行列の乗算を実装することもできます。`A.compose(B)` は行列 $B.A$ を持つ演算子を返します。

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j],
          [-1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

`compose`の`front`キーワード引数を使用して、$A$ の前に $B$ を適用する逆順の合成も行えます。`A.compose(B, front=True)` = $A.B$ となります。

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B, front=True)
```

```text
Operator([[ 0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

### サブシステムの合成 {#subsystem-composition}

前述の合成では、第1の演算子 $A$ の全出力次元が、合成する演算子 $B$ の全入力次元と等しい必要があります（また、`front=True`で合成する場合は、$B$ の出力次元が $A$ の入力次元と等しい必要があります）。

`compose`の`qargs`キーワード引数を使用して、`front=True`の有無にかかわらず、より大きな演算子の選択されたサブシステムにより小さな演算子を合成することもできます。この場合、合成されるサブシステムの関連する入出力次元が一致している必要があります。*なお、より小さな演算子は常に* `compose` *メソッドの引数でなければなりません。*

例えば、2量子ビットゲートを3量子ビット演算子と合成する場合：

```python
# Compose XZ with a 3-qubit identity operator
op = Operator(np.eye(2**3))
XZ = Operator(Pauli("XZ"))
op.compose(XZ, qargs=[0, 2])
```

```text
Operator([[ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
           -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

```python
# Compose YX in front of the previous operator
op = Operator(np.eye(2**3))
YX = Operator(Pauli("YX"))
op.compose(YX, qargs=[0, 2], front=True)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j],
          [0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

### 線形結合 {#linear-combinations}

演算子は、加算、減算、複素数によるスカラー乗算などの標準的な線形演算子を使用して組み合わせることもできます。

```python
XX = Operator(Pauli("XX"))
YY = Operator(Pauli("YY"))
ZZ = Operator(Pauli("ZZ"))

op = 0.5 * (XX + YY - 3 * ZZ)
op
```

```text
Operator([[-1.5+0.j,  0. +0.j,  0. +0.j,  0. +0.j],
          [ 0. +0.j,  1.5+0.j,  1. +0.j,  0. +0.j],
          [ 0. +0.j,  1. +0.j,  1.5+0.j,  0. +0.j],
          [ 0. +0.j,  0. +0.j,  0. +0.j, -1.5+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

重要な点として、`tensor`、`expand`、`compose`はユニタリ演算子のユニタリ性を保持しますが、線形結合はそうではありません。したがって、2つのユニタリ演算子を加算すると、一般的に非ユニタリ演算子になります。

```python
op.is_unitary()
```

```text
False
```

### Operatorへの暗黙的な変換 {#implicit-conversion-to-operators}

以下のすべてのメソッドにおいて、2番目のオブジェクトがすでに`Operator`オブジェクトでない場合、メソッドによって暗黙的に`Operator`オブジェクトに変換されます。つまり、行列を明示的に`Operator`に変換することなく直接渡すことができます。変換が不可能な場合は、例外が発生します。

```python
# Compose with a matrix passed as a list
Operator(np.eye(2)).compose([[0, 1], [1, 0]])
```

```text
Operator([[0.+0.j, 1.+0.j],
          [1.+0.j, 0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

## Operatorを比較する {#compare-operators}

演算子は等値メソッドを実装しており、2つの演算子がほぼ等しいかどうかを確認するために使用できます。

```python
Operator(Pauli("X")) == Operator(XGate())
```

```text
True
```

これは演算子の各行列要素がほぼ等しいかどうかを確認していることに注意してください。グローバル位相が異なる2つのユニタリは等しいとみなされません。

```python
Operator(XGate()) == np.exp(1j * 0.5) * Operator(XGate())
```

```text
False
```

### プロセスフィデリティ {#process-fidelity}

*量子情報*モジュールの`process_fidelity`関数を使用して演算子を比較することもできます。これは2つの量子チャネルがどれほど近いかを表す情報理論的な量であり、ユニタリ演算子の場合はグローバル位相に依存しません。

```python
# Two operators which differ only by phase
op_a = Operator(XGate())
op_b = np.exp(1j * 0.5) * Operator(XGate())

# Compute process fidelity
F = process_fidelity(op_a, op_b)
print("Process fidelity =", F)
```

```text
Process fidelity = 1.0
```

プロセスフィデリティは一般に、入力演算子がユニタリ（または量子チャネルの場合はCP）である場合にのみ有効な近さの尺度であり、入力がCPでない場合は例外が発生します。

## 次のステップ {#next-steps}

<Admonition type="tip" title="Recommendations">
  - [Operator API](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator#operator)リファレンスを参照してください。
</Admonition>
