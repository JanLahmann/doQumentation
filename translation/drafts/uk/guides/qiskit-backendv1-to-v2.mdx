---
title: Міграція з BackendV1 на BackendV2
description: Як оновити свій код для використання BackendV2 замість BackendV1.
---

{/* doqumentation-source-hash: dba2d66b */}

# Міграція з `BackendV1` на `BackendV2`

Клас [`BackendV1`](https://docs.quantum.ibm.com/api/qiskit/1.4/qiskit.providers.BackendV1) у Qiskit оголошено застарілим і буде видалено. Цей посібник із міграції описує невеликі зміни, які потрібно зробити, якщо ти використовуєш провайдер, що оновився з [`BackendV1`](https://docs.quantum.ibm.com/api/qiskit/1.4/qiskit.providers.BackendV1) до [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2).

<Admonition type="note">
 Якщо ти використовуєш виключно `qiskit_ibm_runtime` і `qiskit_aer`, жодних дій не потрібно. Пакет `qiskit_ibm_runtime` завжди використовував `BackendV2`, а `qiskit_aer` — починаючи з версії 0.13.
 </Admonition>

## Ключові зміни в `BackendV2` {#high-level-changes-in-backendv2}

Модель [`Backend`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.Backend) у Qiskit призначена для надання SDK рівня абстракції, що дозволяє міркувати про квантові комп'ютери в рамках SDK. Перша версія моделі була введена з класом [`BackendV1`](https://docs.quantum.ibm.com/api/qiskit/1.4/qiskit.providers.BackendV1). Цей клас зберігав інформацію про бекенд у серії контейнерів даних, а саме в класах [`BackendConfiguration`](https://docs.quantum.ibm.com/api/qiskit/1.4/qiskit.providers.models.BackendConfiguration) і [`BackendProperties`](https://docs.quantum.ibm.com/api/qiskit/1.4/qiskit.providers.models.BackendProperties).

Клас [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2) переосмислив доступ користувача до більшості властивостей бекенду, щоб вони працювали з нативними структурами даних Qiskit і мали більш плоскі патерни доступу. Основою моделі [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2) є клас [`Target`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.Target) — представлення QPU, що містить обмеження транспіляції, які Qiskit може використовувати для оптимізації схем перед виконанням.

SDK Qiskit оновлено для роботи виключно з вхідними даними [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2), і більшість провайдерів перейшли з [`BackendV1`](https://docs.quantum.ibm.com/api/qiskit/1.4/qiskit.providers.BackendV1) на [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2). Очікується, що наявні провайдери, де можливо, оголосять старий спосіб доступу застарілим для плавної міграції, але зрештою користувачам доведеться адаптувати свій код.

Принцип [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2) полягає в тому, що більшість інформації про бекенд міститься в його об'єкті [`Target`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.Target), а атрибути бекенду часто звертаються до атрибута `BackendV2.target` для повернення даних. Проте в багатьох випадках атрибути надають лише частину інформації, що може містити target. Наприклад, `backend.coupling_map` повертає об'єкт [`CouplingMap`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.CouplingMap), побудований із [`Target`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.Target), доступного через атрибут `BackendV2.target`. Однак target може містити інструкції, що діють на більш ніж два кубіти (що не можна представити в [`CouplingMap`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.CouplingMap)), або інструкції, що діють лише на підмножину кубітів (або пар кубітів, для двокубітних інструкцій), які не будуть відображені в повній карті зв'язності, що повертається через `BackendV2.coupling_map`. Тому залежно від твого сценарію використання може знадобитися заглибитися далі, ніж еквівалентний доступ через [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2).

## Конкретні зміни в `BackendV2` {#specific-changes-in-backendv2}

Більшість атрибутів мають пряму заміну, що спрощує міграцію. Єдина розбіжність між інтерфейсами — у [`CouplingMap`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.CouplingMap).

Нижче наведено таблицю прикладів патернів доступу в [`BackendV1`](https://docs.quantum.ibm.com/api/qiskit/1.4/qiskit.providers.BackendV1) та їх нові форми у [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2).

<Admonition type="important">
Прокрути праворуч, щоб побачити важливі примітки.
</Admonition>

|[`BackendV1`](https://docs.quantum.ibm.com/api/qiskit/1.4/qiskit.providers.BackendV1) | [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2) | Notes |
| --- | --- | --- |
|`backend.configuration().n_qubits` | `backend.num_qubits`| |
|`backend.configuration().coupling_map` | `backend.coupling_map` | The return from [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2) is a [`CouplingMap`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.CouplingMap) object. In [`BackendV1`](https://docs.quantum.ibm.com/api/qiskit/1.4/qiskit.providers.BackendV1) it is an edge list. Also, this is just a view of the information contained in `backend.target` which may only be a subset of the information contained in the [`Target`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.Target) object. |
|`backend.configuration().backend_name` | `backend.name` | |
|`backend.configuration().backend_version` | `backend.backend_version` | The `BackendV2.version` attribute represents the version of the abstract [`Backend`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.Backend) interface that the object implements, while `BackendV2.backend_version` is metadata about the version of the backend itself. |
|`backend.configuration().basis_gates` | `backend.operation_names` | The [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2) returns a list of operation names contained in the `backend.target` attribute. The [`Target`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.Target) might contain more information than can be expressed by this list of names. For example, some operations only work on a subset of qubits, and some names implement the same gate with different parameters.|
|`backend.configuration().dt`|`backend.dt`| |
|`backend.configuration().dtm`|`backend.dtm` | |
|`backend.configuration().max_experiments` |`backend.max_circuits` | |
|`backend.configuration().online_date` | `backend.online_date`| |
|`InstructionDurations.from_backend(backend)`|`backend.instruction_durations`| |
|`backend.defaults().instruction_schedule_map`|`backend.instruction_schedule_map`| |
|`backend.properties().t1(0)`|`backend.qubit_properties(0).t1`| |
|`backend.properties().t2(0)`|`backend.qubit_properties(0).t2`| |
|`backend.properties().frequency(0)`|`backend.qubit_properties(0).frequency`| |
|`backend.properties().readout_error(0)`|`backend.target["measure"][(0,)].error`| In [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2), the error rate for the [`Measure`](https://docs.quantum.ibm.com/api/qiskit/circuit#measure) operation on a given qubit is used to model the readout error. However, a [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2) object can implement multiple measurement types and list them separately in a [`Target`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.Target).|
|`backend.properties().readout_length(0)`|`backend.target["measure"][(0,)].duration`| In [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2), the duration for the [`Measure`](https://docs.quantum.ibm.com/api/qiskit/circuit#measure) operation on a given qubit is used to model the readout length. However, a [`BackendV2`](https://docs.quantum.ibm.com/api/qiskit/qiskit.providers.BackendV2) object can implement multiple measurement types and list them separately in a [`Target`](https://docs.quantum.ibm.com/api/qiskit/qiskit.transpiler.Target).|
