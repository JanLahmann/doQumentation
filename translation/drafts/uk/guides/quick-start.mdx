---
title: "Швидкий старт"
sidebar_label: "Швидкий старт"
description: "Збудуй і візуалізуй квантову схему менш ніж за дві хвилини — без реєстрації та API-ключа."
notebook_path: "guides/quick-start.ipynb"
---

{/* doqumentation-source-hash: 4dc6c1d3 */}

<OpenInLabBanner notebookPath="guides/quick-start.ipynb" />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';



{/* cspell:ignore miniconda */}

{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>Версії пакетів</b></summary>

Код на цій сторінці розроблено з використанням таких вимог.
Рекомендуємо використовувати ці версії або новіші.

```
qiskit[all]~=2.3.0
```
</details>

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit
```

Збудуй свою першу квантову схему менш ніж за дві хвилини у локальному середовищі — без реєстрації та API-ключа.

<Admonition type="note" title="Вперше працюєш з Python і віртуальними середовищами?">

* Завантаж Python і використовуй віртуальне середовище з Qiskit (рекомендовано).

<details>
<summary>Натисни, щоб розгорнути додаткову інформацію про **Python**.</summary>

- Щоб встановити Python, спочатку перевір розділ "Programming Language" на [сторінці проекту Qiskit на PyPI](https://pypi.org/project/qiskit/), щоб визначити, які версії Python підтримуються останнім релізом. Інструкції з завантаження дивись у [Python Beginners Guide.](https://wiki.python.org/moin/BeginnersGuide/Download)

<Admonition type="note">
Ці інструкції використовують стандартний дистрибутив Python з [pypi.org](https://pypi.org/). Однак ти можеш використовувати інші дистрибутиви Python, такі як [Anaconda](https://docs.anaconda.com/anaconda/) або [miniconda](https://docs.anaconda.com/miniconda/), а також інші системи управління залежностями, наприклад [Poetry](https://python-poetry.org/docs/).
</Admonition>
</details>

    <details>
    <summary>
    Натисни, щоб розгорнути додаткову інформацію про **віртуальні середовища**.
    </summary>
    - Використовуй [віртуальні середовища Python](https://docs.python.org/3.10/tutorial/venv.html), щоб ізолювати Qiskit від інших програм.
    Віртуальне середовище Python — це ізольований простір для роботи з Python для конкретної мети: ти можеш встановлювати будь-які пакети, налаштовувати бібліотеки, залежності тощо, не змінюючи «базове» середовище Python на своїй машині.

    Одна важлива перевага віртуального середовища: якщо твоє середовище Python пошкодиться, ти легко можеш видалити його та почати заново!

    Вибери зручне місце для зберігання інформації про свої віртуальні середовища. Зазвичай вони зберігаються в директорії `.venv` всередині кожної директорії проекту.

    Щоб налаштувати віртуальне середовище, перейди до директорії проекту та створи мінімальне середовище лише з встановленим Python.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

    Далі активуй своє нове середовище.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
              Якщо використовуєш PowerShell:

             ```text
             .venv\Scripts\Activate.ps1
             ```
             Якщо використовуєш Git Bash:

             ```text
             source .venv/scripts/activate
             ```
              Якщо використовуєш командний рядок:

             ```text
             .venv\Scripts\activate
             ```
           </TabItem>
         </OperatingSystemTabs>
    </details>

</Admonition>

## 1. Встанови Qiskit {#1-install-qiskit}

Встанови наступне за допомогою свого менеджера пакетів (наприклад, `pip`):

- [`qiskit`](/guides/install-qiskit)
- [`matplotlib`](https://matplotlib.org/stable/users/explain/quick_start.html)
- [`qiskit[visualization]`](https://docs.quantum.ibm.com/api/qiskit/visualization)
## 2. Збудуй свою схему {#2-build-your-circuit}

Відкрий середовище Python і запусти цей код, щоб побудувати стан Белла (два заплутані кубіти).

```python
from qiskit import QuantumCircuit
from qiskit.primitives import StatevectorSampler

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0, 1)
qc.measure_all()

sampler = StatevectorSampler()
result = sampler.run([qc], shots=1024).result()
print(result[0].data.meas.get_counts())
```

```text
{'11': 534, '00': 490}
```

Очікуваний результат — приблизно рівний розподіл між '00' та '11'.
## 3. Візуалізуй результати {#3-visualize-your-results}

Щоб отримати гістограму результатів, додай до своєї програми такий код.

```python
# Uncomment lines 2 and 8 if you are not using Python in a Jupyter notebook
# import matplotlib.pyplot as plt
from qiskit.visualization import plot_histogram

counts = result[0].data.meas.get_counts()
plot_histogram(counts)

# plt.show()
```

![Вихід попереднього блоку коду](/docs/images/guides/quick-start/extracted-outputs/dc4ff012-0.svg)

Цей результат є ознакою квантової заплутаності.

## 4. Подивись, що відбувається {#4-see-what-happens}

Спробуй змінити код і подивись, як це впливає на результати. Наприклад:

- Додай третій кубіт, змінивши на `QuantumCircuit(3)`, і додай другий вентиль CX за допомогою `qc.cx(1,2)`. Тоді виміри мають змінитися на 000 та 111, що означає заплутування всіх трьох кубітів.

- Поспостерігай, як зміняться результати, якщо додати `qc.x(1)` в кінець схеми.
## Наступні кроки {#next-steps}

<Admonition type="tip" title="Рекомендації">
- Виконай кроки з [Hello world](/guides/hello-world), щоб запустити схему на реальному квантовому обладнанні.
- Ще не готовий(-а) запускати на обладнанні? Розпочни своє квантове навчання з курсу [Основи квантової інформації](/learning/courses/basics-of-quantum-information).
</Admonition>
