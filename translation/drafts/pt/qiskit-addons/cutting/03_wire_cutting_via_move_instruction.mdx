---
title: "Wire Cutting Formulado como uma Instrução `Move` de Dois Qubits"
sidebar_label: "Wire Cutting Formulado como uma Instrução `Move` de Dois Qubits"
notebook_path: "qiskit-addons/cutting/03_wire_cutting_via_move_instruction.ipynb"
---

{/* doqumentation-source-hash: cf00a5bc */}

import TutorialFeedback from '@site/src/components/TutorialFeedback';

<OpenInLabBanner notebookPath="qiskit-addons/cutting/03_wire_cutting_via_move_instruction.ipynb" />


Neste tutorial, vamos reconstruir os valores esperados de um Circuit de sete Qubits dividindo-o em dois Circuits de quatro Qubits usando wire cutting.

Estes são os passos que vamos seguir neste [padrão Qiskit](https://quantum.cloud.ibm.com/docs/guides/intro-to-patterns):

- **Passo 1: Mapear o problema para circuits quânticos e operadores**:
    - Mapear o hamiltoniano para um circuit quântico.
- **Passo 2: Otimizar para o hardware alvo** [_Usa o addon de cutting_]:
    - <font color='#0F62FE'>Cortar o circuit e o observável.</font>
    - Transpilar os subexperimentos para o hardware.
- **Passo 3: Executar no hardware alvo**:
    - Executar os subexperimentos obtidos no Passo 2 usando a primitiva `Sampler`.
- **Passo 4: Pós-processar os resultados** [_Usa o addon de cutting_]:
    - <font color='#0F62FE'>Combinar os resultados do Passo 3 para reconstruir o valor esperado do observável em questão.</font>
## Passo 1: Mapear

### Criar um circuit para cortar

Primeiro, começamos com um circuit inspirado na Fig. 1(a) de [arXiv:2302.03366v1](https://arxiv.org/abs/2302.03366v1).

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-addon-cutting qiskit-aer qiskit-ibm-runtime
```

```python
import numpy as np
from qiskit import QuantumCircuit

qc_0 = QuantumCircuit(7)
for i in range(7):
    qc_0.rx(np.pi / 4, i)
qc_0.cx(0, 3)
qc_0.cx(1, 3)
qc_0.cx(2, 3)
qc_0.cx(3, 4)
qc_0.cx(3, 5)
qc_0.cx(3, 6)
qc_0.cx(0, 3)
qc_0.cx(1, 3)
qc_0.cx(2, 3)
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7f16ab191a80>
```

```python
qc_0.draw("mpl")
```

![Diagrama do circuit quântico](./_03_wire_cutting_via_move_instruction_imgs/output_1.png)

### Especificar um observável

```python
from qiskit.quantum_info import SparsePauliOp

observable = SparsePauliOp(["ZIIIIII", "IIIZIII", "IIIIIIZ"])
```

## Passo 2: Otimizar

### Criar um novo circuit onde as instruções `Move` foram colocadas nos locais de corte desejados

Dado o circuit acima, gostaríamos de colocar dois wire cuts na linha do Qubit do meio, para que o circuit possa se separar em dois circuits de quatro Qubits cada. Uma forma de fazer isso é colocar manualmente instruções `Move` de dois Qubits que movem o estado de um fio de Qubit para outro. Uma instrução `Move` é conceitualmente equivalente a uma operação de reset no segundo Qubit, seguida de um Gate SWAP. O efeito dessa instrução é transferir o estado do primeiro Qubit (fonte) para o segundo Qubit (destino), enquanto descarta o estado de entrada do segundo Qubit. Para que isso funcione como esperado, é importante que o segundo Qubit (destino) não compartilhe nenhum entrelaçamento com o restante do sistema; caso contrário, a operação de reset fará com que o estado do restante do sistema seja parcialmente colapsado.

Aqui, construímos um novo circuit com um Qubit adicional e as operações `Move` no lugar. Neste exemplo, podemos reutilizar um Qubit: o Qubit fonte do primeiro `Move` torna-se o Qubit de destino da segunda operação `Move`.

Nota: Como alternativa a trabalhar diretamente com instruções `Move`, você pode optar por marcar os wire cuts usando uma instrução `CutWire` de um único Qubit. A função `cut_wires` existe para transformar `CutWire`s em instruções `Move` em Qubits recém-alocados. No entanto, ao contrário do método manual, esse método automático não permite a reutilização de fios de Qubit. Veja o [guia prático](../how-tos/how_to_specify_cut_wires.ipynb) de `CutWire` para mais detalhes.

```python
from qiskit_addon_cutting.instructions import Move

qc_1 = QuantumCircuit(8)
for i in [*range(4), *range(5, 8)]:
    qc_1.rx(np.pi / 4, i)
qc_1.cx(0, 3)
qc_1.cx(1, 3)
qc_1.cx(2, 3)
qc_1.append(Move(), [3, 4])
qc_1.cx(4, 5)
qc_1.cx(4, 6)
qc_1.cx(4, 7)
qc_1.append(Move(), [4, 3])
qc_1.cx(0, 3)
qc_1.cx(1, 3)
qc_1.cx(2, 3)

qc_1.draw("mpl")
```

![Diagrama do circuit quântico](./_03_wire_cutting_via_move_instruction_imgs/output_2.png)

### Criar o observável para o novo circuit

Este observável corresponde a `observable`, mas devemos contabilizar corretamente o fio de Qubit extra que foi adicionado (ou seja, inserimos um "I" no índice 4). Note que no Qiskit, a representação em string do Qubit-0 corresponde ao caractere Pauli mais à direita.

```python
observable_expanded = SparsePauliOp(["ZIIIIIII", "IIIIZIII", "IIIIIIIZ"])
```

### Separar o circuit e os observáveis

Como nos tutoriais anteriores, os Qubits que compartilham um rótulo de partição comum serão agrupados, e os Gates não-locais que abrangem mais de uma partição serão cortados.

```python
from qiskit_addon_cutting import partition_problem

partitioned_problem = partition_problem(
    circuit=qc_1, partition_labels="AAAABBBB", observables=observable_expanded.paulis
)
subcircuits = partitioned_problem.subcircuits
subobservables = partitioned_problem.subobservables
bases = partitioned_problem.bases
```

### Visualizar o problema decomposto

```python
subobservables
```

```text
{'A': PauliList(['IIII', 'ZIII', 'IIIZ']),
 'B': PauliList(['ZIII', 'IIII', 'IIII'])}
```

```python
subcircuits["A"].draw("mpl")
```

![Diagrama do circuit quântico](./_03_wire_cutting_via_move_instruction_imgs/output_3.png)

```python
subcircuits["B"].draw("mpl")
```

![Diagrama do circuit quântico](./_03_wire_cutting_via_move_instruction_imgs/output_4.png)

### Calcular o overhead de amostragem para os cortes escolhidos

Aqui cortamos dois fios, resultando em um overhead de amostragem de $4^4$.

Para mais informações sobre o overhead de amostragem incorrido pelo circuit cutting, consulte o [material explicativo](../explanation/index.rst).

```python
print(f"Sampling overhead: {np.prod([basis.overhead for basis in bases])}")
```

```text
Sampling overhead: 256.0
```

### Gerar os subexperimentos para executar no Backend

`generate_cutting_experiments` aceita os argumentos `circuits`/`observables` como dicionários que mapeiam rótulos de partição de Qubit para os respectivos `subcircuit`/`subobservables`.

Para simular o valor esperado do circuit completo, muitos subexperimentos são gerados a partir da distribuição de quasiprobabilidade conjunta dos Gates decompostos e, em seguida, executados em um ou mais Backends. O número de amostras retiradas da distribuição é controlado por `num_samples`, e um coeficiente combinado é fornecido para cada amostra única. Para mais informações sobre como os coeficientes são calculados, consulte o [material explicativo](../explanation/index.rst).

```python
from qiskit_addon_cutting import generate_cutting_experiments

subexperiments, coefficients = generate_cutting_experiments(
    circuits=subcircuits, observables=subobservables, num_samples=np.inf
)
```

### Escolher um Backend

Aqui estamos usando um Backend falso, o que fará com que o Qiskit Runtime seja executado no modo local (ou seja, em um simulador local).

```python
from qiskit_ibm_runtime.fake_provider import FakeManilaV2

backend = FakeManilaV2()
```

### Preparar os subexperimentos para o Backend

Devemos transpilar os circuits com nosso Backend como alvo antes de submetê-los ao Qiskit Runtime.

```python
from qiskit.transpiler import generate_preset_pass_manager

# Transpile the subexperiments to ISA circuits
pass_manager = generate_preset_pass_manager(optimization_level=1, backend=backend)
isa_subexperiments = {
    label: pass_manager.run(partition_subexpts)
    for label, partition_subexpts in subexperiments.items()
}
```

## Passo 3: Executar

### Executar os subexperimentos usando a primitiva Sampler do Qiskit Runtime

```python
from qiskit_ibm_runtime import SamplerV2, Batch

# Submit each partition's subexperiments to the Qiskit Runtime Sampler
# primitive, in a single batch so that the jobs will run back-to-back.
with Batch(backend=backend) as batch:
    sampler = SamplerV2(mode=batch)
    jobs = {
        label: sampler.run(subsystem_subexpts, shots=2**12)
        for label, subsystem_subexpts in isa_subexperiments.items()
    }
```

```text
/home/garrison/Qiskit/qiskit-ibm-runtime/qiskit_ibm_runtime/session.py:157: UserWarning: Session is not supported in local testing mode or when using a simulator.
  warnings.warn(
```

```python
# Retrieve results
results = {label: job.result() for label, job in jobs.items()}
```

## Passo 4: Pós-processar

### Reconstruir o valor esperado

Reconstruir os valores esperados para cada termo do observável e combiná-los para reconstruir o valor esperado para o observável original.

```python
from qiskit_addon_cutting import reconstruct_expectation_values

reconstructed_expval_terms = reconstruct_expectation_values(
    results,
    coefficients,
    subobservables,
)
reconstructed_expval = np.dot(reconstructed_expval_terms, observable.coeffs)
```

### Comparar o valor esperado reconstruído com o valor esperado exato do circuit e observável originais

```python
from qiskit_aer.primitives import EstimatorV2

estimator = EstimatorV2()
exact_expval = estimator.run([(qc_0, observable)]).result()[0].data.evs
print(f"Reconstructed expectation value: {np.real(np.round(reconstructed_expval, 8))}")
print(f"Exact expectation value: {np.round(exact_expval, 8)}")
print(f"Error in estimation: {np.real(np.round(reconstructed_expval-exact_expval, 8))}")
print(
    f"Relative error in estimation: {np.real(np.round((reconstructed_expval-exact_expval) / exact_expval, 8))}"
)
```

```text
Reconstructed expectation value: 1.51319069
Exact expectation value: 1.59099026
Error in estimation: -0.07779957
Relative error in estimation: -0.04890009
```

<TutorialFeedback />
