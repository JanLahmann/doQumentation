---
title: "Algoritmo de Grover"
sidebar_label: "Algoritmo de Grover"
description: "Este notebook é o quarto de uma série de módulos. Nele, vamos aprender sobre o algoritmo de Grover."
notebook_path: "learning/courses/utility-scale-quantum-computing/grovers-algorithm.ipynb"
---

{/* source-hash: 2c3d77ac068bc174dc4b5a411b21f628a2a83952969e4429ed16a19772060ee2 */}

<OpenInLabBanner notebookPath="learning/courses/utility-scale-quantum-computing/grovers-algorithm.ipynb" />



# Algoritmos quânticos: busca de Grover e aplicações

<Admonition type="note">

Atsushi Matsuo (10 de maio de 2024)

[Baixe o PDF](https://ibm.ent.box.com/public/static/3s99zptw6c7nrfzmneogs4y7ezrx5nbh.zip) da aula original. Alguns trechos de código podem estar desatualizados, pois são imagens estáticas.

*O tempo aproximado de QPU para executar este experimento é de 2 segundos.*

</Admonition>

## 1. Introdução ao algoritmo de Grover {#1-introduction-to-grovers-algorithm}
Este notebook é o quarto de uma série de aulas sobre o Caminho para a Utilidade na Computação Quântica. Nele, vamos aprender sobre o algoritmo de Grover.

O algoritmo de Grover é um dos algoritmos quânticos mais conhecidos, graças ao seu ganho de velocidade quadrático em relação aos métodos clássicos de busca. Na computação clássica, buscar em um banco de dados não ordenado com $N$ elementos requer complexidade de tempo $O(N)$, o que significa que, no pior caso, pode ser necessário examinar cada item individualmente. Porém, o algoritmo de Grover permite realizar essa busca em tempo $O(\sqrt{N})$, aproveitando os princípios da mecânica quântica para identificar o elemento alvo de forma mais eficiente.

O algoritmo utiliza amplificação de amplitude, um processo que aumenta a amplitude de probabilidade do estado correto em uma superposição quântica, permitindo que ele seja medido com maior probabilidade. Esse ganho de velocidade torna o algoritmo de Grover valioso em diversas aplicações além de simples buscas em banco de dados, especialmente quando o tamanho do conjunto de dados é grande. Explicações detalhadas do algoritmo estão disponíveis no [notebook do algoritmo de Grover](/learning/courses/fundamentals-of-quantum-algorithms/grover-algorithm/introduction).

### A estrutura básica do algoritmo de Grover {#the-basic-structure-of-grovers-algorithm}

O algoritmo de Grover é composto por quatro componentes principais:
1. **Inicialização**: configuração da superposição sobre todos os estados possíveis.
2. **Oráculo**: aplicação de uma função oráculo que marca o estado alvo invertendo sua fase.
3. **Operador de difusão**: aplicação de uma série de operações para amplificar a probabilidade do estado marcado.

Cada uma dessas etapas tem um papel fundamental para que o algoritmo funcione de forma eficiente. Explicações detalhadas de cada etapa são fornecidas adiante.

## 2. Implementando o algoritmo de Grover {#2-implementing-grovers-algorithm}

### 2.1 Preparação {#21-preparation}
Importe as bibliotecas necessárias e configure o ambiente para executar o circuito quântico.

```python
# Adicionado pelo doQumentation — pacotes necessários para este notebook
!pip install -q qiskit qiskit-aer qiskit-ibm-runtime
```

```python
%config InlineBackend.figure_format = 'svg' # Deixa as imagens com boa qualidade
```

```python
# importando o Qiskit
from qiskit import QuantumCircuit, ClassicalRegister, QuantumRegister

# importando ferramentas básicas de visualização
from qiskit.visualization import plot_histogram
```

### Passo 1: Mapear o problema em circuitos e operadores quânticos {#step-1-map-problem-to-quantum-circuits-and-operators}

Considere uma lista de 4 elementos, em que o objetivo é identificar o índice de um elemento que atende a uma condição específica. Por exemplo, queremos encontrar o índice do elemento igual a 2. Neste exemplo, o estado quântico $|01\rangle$ representa o índice do elemento que satisfaz essa condição, pois aponta para a posição onde o valor 2 está localizado.

### Passo 2: Otimizar para o hardware alvo {#step-2-optimize-for-target-hardware}

### 1: Inicialização {#1-initialization}

Na etapa de inicialização, criamos uma superposição de todos os estados possíveis. Isso é feito aplicando uma porta Hadamard a cada qubit em um registrador de n qubits, o que resulta em uma superposição uniforme de $2^n$ estados. Matematicamente, isso pode ser representado como:

$$
\frac{1}{\sqrt{N}} \sum_{x=0}^{N-1} |x\rangle
$$

onde $N = 2^n$ é o número total de estados possíveis. Também alteramos o estado do bit ancilla para $|-\rangle$.

```python
def initialization(circuit):
    # Inicialização
    n = circuit.num_qubits
    # Para os qubits de entrada
    for qubit in range(n - 1):
        circuit.h(qubit)
    # Para o bit ancilla
    circuit.x(n - 1)
    circuit.h(n - 1)
    circuit.barrier()
    return circuit
```

```python
n = 2
qr = QuantumRegister(n, "q")
anc = QuantumRegister(1, "ancillary")
initialization_circuit = QuantumCircuit(qr, anc)

initialization(initialization_circuit)
initialization_circuit.draw(output="mpl", idle_wires=False)
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/852b1b8c-688f-4d55-bcf3-91cfd7ef02c5-0.svg)

### 2: Oráculo {#2-oracle}

O oráculo é uma parte fundamental do algoritmo de Grover. Ele marca o estado alvo aplicando uma mudança de fase, tipicamente invertendo o sinal da amplitude associada a esse estado. O oráculo costuma ser específico para o problema e é construído com base nos critérios de identificação do estado alvo. Em termos matemáticos, o oráculo aplica a seguinte transformação:

$
f(x) =
\begin\{cases\}
1, & \text\{if \} x = x_\{\text\{target}\} \\
0, & \text\{otherwise\}
\end\{cases\}
$

Essa inversão de fase é obtida aplicando um sinal negativo à amplitude do estado alvo por meio do phase kickback.

```python
def oracle(circuit):
    circuit.x(1)
    circuit.ccx(0, 1, 2)
    circuit.x(1)
    circuit.barrier()
```

```python
n = 2
qr = QuantumRegister(n, "q")
anc = QuantumRegister(1, "ancillary")
oracle_circuit = QuantumCircuit(qr, anc)

oracle(oracle_circuit)
oracle_circuit.draw(output="mpl", idle_wires=False)
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/e9e331a6-d696-4b95-99ca-56bec2519e25-0.svg)

### 3: Operador de difusão {#3-diffusion-operator}

O processo de amplificação de amplitude é o que diferencia o algoritmo de Grover de uma busca clássica. Após o oráculo marcar o estado alvo, aplicamos uma série de operações que aumentam a amplitude desse estado marcado, tornando mais provável que ele seja observado na medição. Esse processo é realizado pelo **Operador de difusão**, que efetivamente executa uma inversão em torno da amplitude média. A operação matemática é a seguinte:

$
D = 2|\psi\rangle\langle\psi| - I
$

onde $D$ é o operador de difusão, $I$ é a matriz identidade e $|\psi\rangle$ é o estado de superposição uniforme. A combinação do oráculo e do operador de difusão é aplicada aproximadamente $\sqrt{N}$ vezes para atingir a probabilidade máxima de medir o estado marcado.

```python
def diffusion(circuit):
    input_qubits = circuit.num_qubits - 1
    circuit.h(range(0, input_qubits))
    circuit.x(range(0, input_qubits))
    circuit.h(input_qubits - 1)
    circuit.mcx([i for i in range(0, input_qubits - 1)], input_qubits - 1)
    circuit.h(input_qubits - 1)
    circuit.x(range(0, input_qubits))
    circuit.h(range(0, input_qubits))
    circuit.barrier()
```

```python
n = 2
qr = QuantumRegister(n, "q")
anc = QuantumRegister(1, "ancillary")
diffusion_circuit = QuantumCircuit(qr, anc)

diffusion(diffusion_circuit)
diffusion_circuit.draw(output="mpl", idle_wires=False)
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/42661bfd-5abd-40e7-9ca2-10356c54fc7f-0.svg)

### 2.2 Exemplo de busca de Grover com 2 qubits {#22-a-2-qubit-grover-search-example}

```python
n = 2
qr = QuantumRegister(n, "q")
anc = QuantumRegister(1, "ancillary")
meas = ClassicalRegister(3, "meas")
grover_circuit = QuantumCircuit(qr, anc, meas)
```

```python
# número de iterações
num_iterations = 1
```

```python
# Vamos fazer a busca de Grover
initialization(grover_circuit)

for i in range(0, num_iterations):
    oracle(grover_circuit)
    diffusion(grover_circuit)

# Limpar o bit ancilla
grover_circuit.h(n)
grover_circuit.x(n)
grover_circuit.measure_all(add_bits=False)

grover_circuit.draw(output="mpl", idle_wires=False)
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/720d3fa7-a293-45c4-8566-493618381a52-0.svg)

### 2.3 Experimento com Simuladores {#23-experiment-with-simulators}

### Passo 3: Executar o circuito. {#step-3-executing-the-circuit}

```python
from qiskit_aer import AerSimulator
from qiskit_ibm_runtime import Sampler
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

# Definir o backend
backend = AerSimulator()

# Transpilar para o backend
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(grover_circuit)

# Executar o job
sampler = Sampler(mode=backend)
job = sampler.run([isa_qc])
result = job.result()
```

### Passo 4: Pós-processamento dos resultados. {#step-4-post-processing-the-results}

```python
# Imprimir os resultados
counts = result[0].data.meas.get_counts()
print(counts)

# Plotar as contagens em um histograma
plot_histogram(counts)
```

```text
{'001': 1024}
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/394fb643-f19a-47e5-be57-02a5bfe0d231-1.svg)

Obtivemos a resposta correta $|01\rangle$. Preste atenção à ordem dos qubits.

## 3. Experimento com dispositivos reais {#3-experiment-with-real-devices}

### Passo 2: Otimizar para o hardware alvo. {#step-2-optimize-for-target-hardware-real}

```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
real_backend = service.backend("ENTER-QPU-NAME-HERE")
```

```python
# Você também pode identificar o dispositivo menos ocupado

real_backend = service.least_busy(simulator=False, operational=True)
print("O dispositivo menos ocupado é ", real_backend)
```

```python
# Transpilar o circuito para as portas nativas executáveis no hardware
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

pm = generate_preset_pass_manager(backend=real_backend, optimization_level=1)
target_circuit = pm.run(grover_circuit)

target_circuit.draw(output="mpl", idle_wires=False)
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/c0c698e1-0708-4715-a005-853248a79874-0.svg)

Ao transpilar o circuito, ele foi convertido para um circuito utilizando as portas nativas do dispositivo.

### Passo 3: Executar o circuito. {#step-3-executing-the-circuit-real}

```python
sampler = Sampler(real_backend)
job_real = sampler.run([target_circuit])
job_id = job_real.job_id()
print("job id:", job_id)
```

```text
job id: cw69csv19rzg0080yfkg
```

```python
# Verificar o status do job
job_real.status()
```

```text
'QUEUED'
```

```python
# Se a sessão do Notebook foi desconectada, você também pode verificar o status do job executando o código abaixo
job_real = service.job(job_id)  # Insira seu job-id entre as aspas
job_real.status()
```

```text
'DONE'
```

```python
# Executar após o job ter sido concluído com sucesso
result_real = job_real.result()
print(result_real[0].data.meas.get_counts())
```

```text
{'101': 540, '001': 2253, '011': 476, '000': 251, '110': 105, '100': 100, '010': 168, '111': 203}
```

### Passo 4: Pós-processamento dos resultados. {#step-4-post-processing-the-results-real}

```python
plot_histogram(result_real[0].data.meas.get_counts())
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/6c425810-eaec-4d61-a76e-34583664542d-0.svg)

## 4. Uma busca de Grover com 3 qubits {#4-a-3-qubit-grover-search}

Agora, vamos experimentar um exemplo de busca de Grover com 3 qubits.

```python
n = 3
qr = QuantumRegister(n, "q")
anc = QuantumRegister(1, "ancilla")
grover_circuit = QuantumCircuit(qr, anc)
```

```python
# o número de iterações
num_iterations = 2
```

```python
def oracle(circuit):
    circuit.mcx([0, 1, 2], 3)
    circuit.barrier()
```

Desta vez, $|111\rangle$ é o estado "bom".

```python
# Vamos fazer a busca de Grover
initialization(grover_circuit)

for i in range(0, num_iterations):
    oracle(grover_circuit)
    diffusion(grover_circuit)

# Limpa o bit ancilla
grover_circuit.h(n)
grover_circuit.x(n)

grover_circuit.draw(output="mpl", idle_wires=False)
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/eab82e12-1450-47cf-94e0-e631c463575e-0.svg)

```python
grover_circuit.measure_all()

# Define o backend
backend = AerSimulator()

# Transpila para o backend
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(grover_circuit)

# Executa o job
sampler = Sampler(backend)
job = sampler.run([isa_qc], shots=1024)
result = job.result()

# Imprime os resultados
counts = result[0].data.meas.get_counts()
print(counts)

# Plota as contagens em um histograma
plot_histogram(counts)
```

```text
{'0111': 977, '0100': 11, '0001': 8, '0000': 8, '0011': 5, '0010': 12, '0110': 3}
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/21f8e416-9574-4883-b6a5-f7b38908706d-1.svg)

$|0111\rangle$ é observado com a maior probabilidade, como esperado. Note que duas iterações são ótimas nesse caso. Porém, a probabilidade de obter a resposta correta não é 100%, o que é comum na busca de Grover.

#### O que acontece se iterarmos 3 vezes?

Agora, vamos tentar iterar 3 vezes.

```python
n = 3
qr = QuantumRegister(n, "q")
anc = QuantumRegister(1, "ancillary")
grover_circuit = QuantumCircuit(qr, anc)
```

```python
# o número de iterações
num_iterations = 3
```

```python
# Vamos fazer a busca de Grover
initialization(grover_circuit)

for i in range(0, num_iterations):
    oracle(grover_circuit)
    diffusion(grover_circuit)

# Limpa o bit ancilla
grover_circuit.h(n)
grover_circuit.x(n)

grover_circuit.draw(output="mpl", idle_wires=False, fold=-1, scale=0.5)
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/31b2963c-d1af-4f7f-8e61-5c81f37e19c1-0.svg)

```python
grover_circuit.measure_all()

# Define o backend
backend = AerSimulator()

# Transpila para o backend
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(grover_circuit)

# Executa o job
sampler = Sampler(backend)
job = sampler.run([isa_qc], shots=1024)
result = job.result()

# Imprime os resultados
counts = result[0].data.meas.get_counts()
print(counts)

# Plota as contagens em um histograma
plot_histogram(counts)
```

```text
{'0010': 88, '0001': 103, '0000': 94, '0111': 334, '0100': 112, '0110': 106, '0101': 99, '0011': 88}
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/17361c99-50c4-4a47-8d26-4c7a97bce3bf-1.svg)

$|0111\rangle$ ainda é observado com a maior probabilidade, embora a probabilidade de obter a resposta correta tenha diminuído um pouco.

#### E com 4 vezes?

Agora, vamos tentar iterar 4 vezes.

```python
n = 3
qr = QuantumRegister(n, "q")
anc = QuantumRegister(1, "ancillary")
grover_circuit = QuantumCircuit(qr, anc)
```

```python
# o número de iterações
num_iterations = 4
```

```python
# Vamos fazer a busca de Grover
initialization(grover_circuit)

for i in range(0, num_iterations):
    oracle(grover_circuit)
    diffusion(grover_circuit)

# Limpa o bit ancilla
grover_circuit.h(n)
grover_circuit.x(n)

grover_circuit.draw(output="mpl", idle_wires=False, fold=-1, scale=0.5)
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/7297a8bd-3ace-481e-9631-6559c468b8e4-0.svg)

```python
grover_circuit.measure_all()

# Define o backend
backend = AerSimulator()

# Transpila para o backend
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(grover_circuit)

# Executa o job
sampler = Sampler(backend)
job = sampler.run([isa_qc])
result = job.result()

# Imprime os resultados
counts = result[0].data.meas.get_counts()
print(counts)

# Plota as contagens em um histograma
plot_histogram(counts)
```

```text
{'0110': 127, '0000': 135, '0001': 150, '0101': 164, '0010': 153, '0011': 131, '0100': 150, '0111': 14}
```

![Saída da célula de código anterior](/learning/images/courses/utility-scale-quantum-computing/grovers-algorithm/extracted-outputs/65017ef5-503f-47a4-83d8-92605521329f-1.svg)

$|0111\rangle$ é observado com a menor probabilidade, e a chance de obter a resposta correta diminuiu ainda mais.
Isso demonstra a importância de escolher o número ideal de iterações para o algoritmo de Grover a fim de alcançar os melhores resultados.

```python
# Veja a versão do Qiskit
import qiskit

qiskit.__version__
```

```text
'2.0.2'
```
