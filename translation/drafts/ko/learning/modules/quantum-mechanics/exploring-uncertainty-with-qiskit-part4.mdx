### 일관성 검증 {#verify-consistency}

계속 진행하기 전에, 이 결과가 이전의 발견과 일관되는지 확인해 봅시다. 우리는 상태 $|\psi\rangle = \frac{1}{\sqrt{2}}\begin{pmatrix} 1 \\ 1 \end{pmatrix}$를 사용했고, $\Delta X \Delta Z  = 0.171$임을 확인했습니다. 이제 이 곱은 다음 값 이상이어야 함을 알 수 있습니다.
$$
|\langle Y \rangle|=\frac{1}{\sqrt{2}}\begin{pmatrix} 1 & 1 \end{pmatrix}\begin{pmatrix}0 & -i \\ i & 0\end{pmatrix}\frac{1}{\sqrt{2}}\begin{pmatrix} 1 \\ 1 \end{pmatrix}
$$
$$
|\langle Y \rangle| = \frac{1}{2}\begin{pmatrix} 1 & 1 \end{pmatrix}\begin{pmatrix} -i \\ i \end{pmatrix} = \frac{1}{2}(-i+i) = 0.
$$

따라서 실제로 $\Delta X \Delta Z  = 0.171 \geq |\langle Y \rangle|=0$이 성립합니다.
아래 문제들을 풀며 이 결과에 대한 직관을 쌓아보세요.

### 이해도 확인
아래 문제들을 읽고 답을 생각해 본 뒤, 삼각형을 클릭하여 해답을 확인하세요.
<details>
<summary>

다음 항목들을 하나의 묶음으로 함께 답하세요.

(a) $X$에 대한 불확정도가 0이 되는 상태는 무엇이라고 예상하나요?

(b) $Z$에 대한 불확정도가 0이 되는 상태는 무엇이라고 예상하나요?

(c) 기댓값 $\langle Y \rangle$가 0이 되는 상태는 무엇인가요?

(d) 위 문항들의 답은 $\Delta X \Delta Z \geq |\langle Y \rangle|$와 일관성이 있나요?

(e) Estimator를 사용하여 이를 명시적으로 확인하는 코드를 작성하세요.

</summary>

__답:__

(a) $X$ 연산자의 고유상태가 $X$에 대한 불확정도 0을 가질 것으로 예상할 수 있습니다. 실제로 $|\psi\rangle = |+\rangle_x$를 사용하면,
$$\Delta X = \sqrt{1-\langle X \rangle^2} = \sqrt{1-1^2} = 0.$$

(b) $Z$ 연산자의 고유상태가 $Z$에 대한 불확정도 0을 가질 것으로 예상할 수 있습니다. 실제로 $|\psi\rangle = |1\rangle$을 사용하면,
$$\Delta Z = \sqrt{1-\langle Z \rangle^2} = \sqrt{1-(-1)^2} = 0.$$

(c) 측정 시 $y$축 양의 방향 성분과 음의 방향 성분이 동일한 빈도로 나타나는 상태에서 $\langle Y \rangle=0$을 기대할 수 있습니다. 여기에는 $X$와 $Z$의 고유상태들이 포함됩니다.

(d) 그렇습니다. $X$ 또는 $Z$의 고유상태에서 불확정도의 곱 $\Delta X \Delta Z$는 매우 작은 값을 가질 것으로 예상됩니다: $\Delta X \Delta Z \approx 0.$ 그러한 상태에서 $\langle Y \rangle=0$도 기대할 수 있으므로 불확정성 관계가 충족될 수 있습니다.

(e) 다음과 같은 코드로 이를 검증할 수 있습니다.
```
obs1 = SparsePauliOp.from_list(
    [("X", 1.000)]
)
obs2 = SparsePauliOp.from_list(
    [("Y", 1.000)]
)
obs3 = SparsePauliOp.from_list(
    [("Z", 1.000)]
)
qc = QuantumCircuit(1,1)
qc.ry(pi/2,0)

job = estimator.run([(qc, [[obs1], [obs2], [obs3]])], precision=0.001)
res=job.result()
```
이 코드는 모든 기댓값을 반환합니다. 모든 기댓값을 가져와 불확정도를 계산하려면 다음을 사용할 수 있습니다.
```
xs=res[0].data.evs[0]
ys=abs(res[0].data.evs[1])
zs=res[0].data.evs[2]

import math
prodxz=((1-xs[i]*xs[i])**0.5)*(1-zs[i]*zs[i])**0.5

```
</details>

<details>
<summary>

다음 항목들을 하나의 묶음으로 함께 답하세요.

(a) $\langle Y \rangle$의 기댓값이 크게 나오는 상태를 생각해 볼 수 있나요?

(b) 그 상태에서 $X$의 불확정도는 크다고 예상하나요, 아니면 작다고 예상하나요?

(c) 그 상태에서 $Z$의 불확정도는 크다고 예상하나요, 아니면 작다고 예상하나요?

(d) 위 문항들의 답은 $\Delta X \Delta Z \geq |\langle Y \rangle|$와 일관성이 있나요?

(e) Estimator를 사용하여 이를 명시적으로 확인하는 코드를 작성하세요.

</summary>

__답:__

(a) $Y$의 고유상태인 $|+\rangle_y$에서 $\langle Y \rangle\approx 1$을 기대할 수 있습니다.

(b) 상태 $|+\rangle_y$에서 $X$를 측정하면 양의 결과와 음의 결과가 동일한 빈도/확률로 나타나므로, $X$의 불확정도가 클 것으로 예상됩니다.

(c) 상태 $|+\rangle_y$에서 $Z$를 측정하면 양의 결과와 음의 결과가 동일한 빈도/확률로 나타나므로, $Z$의 불확정도가 클 것으로 예상됩니다.

(d) 그렇습니다. $Y$의 고유상태, 특히 $|+\rangle_y$에서 불확정도의 곱 $\Delta X \Delta Z$는 큰 값을 가질 것으로 예상됩니다. 또한 그 상태에서 $\langle Y \rangle\approx 1$도 기대할 수 있습니다. 따라서 $\langle Y \rangle$와 $\Delta X \Delta Z$ 모두 이 상태에서 상당히 크며, 불확정성 관계가 다시 충족될 수 있는 것은 충분히 타당합니다.

(e) 다음과 같은 코드로 이를 검증할 수 있습니다.
```
obs1 = SparsePauliOp.from_list(
    [("X", 1.000)]
)
obs2 = SparsePauliOp.from_list(
    [("Y", 1.000)]
)
obs3 = SparsePauliOp.from_list(
    [("Z", 1.000)]
)
qc = QuantumCircuit(1,1)
qc.rx(-pi/2,0)

job = estimator.run([(qc, [[obs1], [obs2], [obs3]])], precision=0.001)
res=job.result()
```
이 코드는 모든 기댓값을 반환합니다. 모든 기댓값을 가져와 불확정도를 계산하려면 다음을 사용할 수 있습니다.
```
xs=res[0].data.evs[0]
ys=abs(res[0].data.evs[1])
zs=res[0].data.evs[2]

import math
prodxz=((1-xs[i]*xs[i])**0.5)*(1-zs[i]*zs[i])**0.5

```
</details>
