위의 결과는 히스토그램입니다. 3단계에서 Circuit을 실행할 때 선택한 shots 수에 따라, 각 실행에서 측정된 두 Qubit의 상태를 나타내는 막대가 하나 또는 두 개 표시될 수 있습니다. Qiskit과 이 노트북에서는 항상 "리틀 엔디언(little endian)" 표기법을 사용합니다. 즉, Qubit 0부터 n까지의 상태가 오른쪽에서 왼쪽으로 오름차순으로 표기되므로, Qubit 0은 항상 가장 오른쪽에 위치합니다.

따라서 Qubit 0이 중첩 상태에 있었기 때문에, Circuit은 $x=0$과 $x=1$ *모두*에 대해 함수를 *동시에* 평가했습니다 — 고전 컴퓨터는 이를 수행할 수 없습니다! 그런데 함수 $f(x)$에 대해 알고 싶을 때 문제가 생깁니다 — Qubit을 측정하면 상태가 붕괴됩니다. "shots = 1"을 선택해 Circuit을 한 번만 실행하면, 위 히스토그램에 막대가 하나만 표시되어 함수에 대한 정보가 불완전해집니다.

### 이해도 확인
아래 질문을 읽고 답을 생각한 후, 삼각형을 클릭하여 정답을 확인하세요.
<details>
<summary>

위 알고리즘을 몇 번 실행해야 함수 $f(x)$를 알 수 있나요? 이것이 고전적인 경우보다 더 나은가요? 이 문제를 풀기 위해 고전 컴퓨터와 양자 컴퓨터 중 어느 것을 선택하겠나요?

</summary>

__정답:__

측정을 하면 중첩이 붕괴되어 값이 하나만 반환되기 때문에, $f(0)$과 $f(1)$ 두 출력값을 모두 얻으려면 Circuit을 *최소* 두 번 실행해야 합니다. 최선의 경우, 처음 두 번의 쿼리에서 $f(0)$과 $f(1)$을 모두 계산하는 고전적인 경우와 동일한 성능을 보입니다. 하지만 최종 측정이 확률적이어서 처음 두 번 모두 같은 $f(x)$ 값이 반환될 수 있으므로, 두 번 이상 실행해야 할 가능성도 있습니다. 이 경우에는 고전 컴퓨터를 선택하겠습니다.

</details>

따라서 양자 병렬성은 올바르게 활용될 때 강력할 수 있지만, 양자 컴퓨터가 거대한 고전적 병렬 프로세서처럼 작동한다고 말하는 것은 옳지 않습니다. 측정 행위가 양자 상태를 붕괴시키기 때문에, 계산의 단일 출력값에만 접근할 수 있습니다.
## Deutsch 알고리즘

양자 병렬성만으로는 고전 컴퓨터에 비해 이점을 얻을 수 없지만, 이를 또 다른 양자 현상인 간섭(interference)과 결합하면 속도 향상을 이룰 수 있습니다. 현재 "Deutsch 알고리즘"으로 알려진 알고리즘이 이를 달성한 첫 번째 사례입니다.

### 문제 정의

문제는 다음과 같습니다:

입력 비트 $x = \{0,1\}$와 입력 함수 $f(x) = \{0,1\}$이 주어졌을 때, 함수가 *균형(balanced)*인지 *상수(constant)*인지 판별하세요. 즉, 균형 함수라면 함수의 출력이 절반은 0이고 나머지 절반은 1입니다. 상수 함수라면 함수의 출력이 항상 0이거나 항상 1입니다. 단일 비트를 다른 단일 비트로 매핑하는 네 가지 가능한 함수 표를 다시 살펴보겠습니다:

| $x$    |  $f_1(x)$ | $f_2(x)$ | $f_3(x)$ | $f_4(x)$ |
| ----------- | ------- |-------| -------- | ------- |
| 0  | 0   | 0  | 1   | 1 |
| 1  | 0   | 1  | 0 | 1 |

첫 번째와 마지막 함수인 $f_1(x)$와 $f_4(x)$는 상수 함수이고, 가운데 두 함수인 $f_2(x)$와 $f_3(x)$는 균형 함수입니다.
### 알고리즘

Deutsch가 이 문제에 접근한 방식은 "쿼리 모델(query model)"을 통해서였습니다. 쿼리 모델에서 입력 함수($f_i(x)$)는 "블랙박스" 안에 들어 있습니다 — 내용에 직접 접근할 수는 없지만, 블랙박스에 쿼리를 보내면 함수의 출력값을 반환받을 수 있습니다. 이 정보를 제공하는 것을 "오라클(oracle)"이라고도 합니다. 쿼리 모델에 대한 자세한 내용은 Fundamentals of Quantum Algorithms 강좌의 [Lesson 1: Quantum Query Algorithms](/learning/courses/fundamentals-of-quantum-algorithms/quantum-query-algorithms/introduction)를 참고하세요.

쿼리 모델에서 양자 알고리즘이 고전 알고리즘보다 효율적인지 판단하려면, 각 경우에 블랙박스에 몇 번 쿼리해야 하는지 비교하면 됩니다. 고전적인 경우에는 블랙박스 안의 함수가 균형인지 상수인지 알기 위해, $f(0)$과 $f(1)$ 모두를 얻으려면 박스에 두 번 쿼리해야 합니다.

그러나 Deutsch의 양자 알고리즘에서는 단 한 번의 쿼리로 정보를 얻는 방법을 찾아냈습니다! 위의 "양자 병렬성" Circuit에서 한 가지를 수정하여, Qubit 0에만 중첩 상태를 준비하는 대신 *두 Qubit 모두*에 중첩 상태를 준비했습니다. 그러면 함수의 두 출력값인 $f(0)$과 $f(1)$이 간섭을 일으켜, 둘 다 0이거나 둘 다 1이면(함수가 상수이면) 0을 반환하고, 서로 다르면(함수가 균형이면) 1을 반환합니다. 이렇게 Deutsch는 단 한 번의 쿼리로 상수 함수와 균형 함수를 구별할 수 있었습니다.

다음은 Deutsch 알고리즘의 Circuit 다이어그램입니다:

![Circuit diagram of Deutsch's algorithm](/learning/images/modules/computer-science/deutsch-jozsa/Deutsch_algo.avif)

이 알고리즘이 어떻게 작동하는지 이해하기 위해, 위 다이어그램에 표시된 세 지점에서의 Qubit 양자 상태를 살펴보겠습니다. 정답을 클릭하기 전에 직접 상태를 계산해 보세요:

### 이해도 확인
아래 질문들을 읽고 답을 생각한 후, 삼각형을 클릭하여 정답을 확인하세요.
<details>
<summary>

$|\pi_1\rangle$ 상태는 무엇인가요?

</summary>

__정답:__

Hadamard를 적용하면 $|0\rangle$ 상태는 $\frac{1}{\sqrt{2}}(|0\rangle+|1\rangle)$으로, $|1\rangle$ 상태는 $\frac{1}{\sqrt{2}}(|0\rangle-|1\rangle)$으로 변환됩니다. 따라서 전체 상태는 다음과 같습니다: $|\pi_1\rangle = [\frac{|0\rangle-|1\rangle}{\sqrt{2}}][\frac{|0\rangle+|1\rangle}{\sqrt{2}}]$

</details>

<details>
<summary>

$|\pi_2\rangle$ 상태는 무엇인가요?

</summary>

__정답:__

$U_f$를 적용하기 전에, 이 게이트가 무엇을 하는지 기억하세요. Qubit 0의 상태에 따라 Qubit 1의 상태를 변경합니다. 따라서 Qubit 0의 상태를 인수분해하는 것이 합리적입니다: $|\pi_1\rangle = \frac{1}{2} (|0\rangle-|1\rangle)|0\rangle+\frac{1}{2}(|0\rangle-|1\rangle)|1\rangle$. 그러면 $f(0)=f(1)$이면 두 항이 같은 방식으로 변환되어 두 항 사이의 상대적 부호가 양수로 유지되지만, $f(0)\neq f(1)$이면 두 번째 항이 첫 번째 항에 비해 마이너스 부호를 얻어 Qubit 0의 상태가 $\frac{1}{\sqrt{2}}(|0\rangle+|1\rangle)$에서 $\frac{1}{\sqrt{2}}(|0\rangle-|1\rangle)$으로 변합니다. 따라서:
$$
|\pi_2\rangle = \begin{cases}
\pm[\frac{|0\rangle-|1\rangle}{\sqrt{2}}][\frac{|0\rangle+|1\rangle}{\sqrt{2}}] & \text{if} & f(0) = f(1) \\
\pm[\frac{|0\rangle-|1\rangle}{\sqrt{2}}][\frac{|0\rangle-|1\rangle}{\sqrt{2}}] &\text{if} & f(0) \neq f(1) \\
\end{cases}
$$

</details>

<details>
<summary>

$|\pi_3\rangle$ 상태는 무엇인가요?

</summary>

__정답:__

이제 Qubit 0의 상태는 함수에 따라 $\frac{1}{\sqrt{2}}(|0\rangle+|1\rangle)$ 또는 $\frac{1}{\sqrt{2}}(|0\rangle-|1\rangle)$ 중 하나입니다. Hadamard를 적용하면 각각 $|0\rangle$ 또는 $|1\rangle$이 됩니다.

$$
|\pi_3\rangle = \begin{cases}
\pm[\frac{|0\rangle-|1\rangle}{\sqrt{2}}]|0\rangle & \text{if} & f(0) = f(1) \\
\pm[\frac{|0\rangle-|1\rangle}{\sqrt{2}}]|1\rangle &\text{if} & f(0) \neq f(1) \\
\end{cases}
$$

</details>

위 질문들의 정답을 살펴보면, 다소 놀라운 일이 발생한다는 것을 알 수 있습니다. $U_f$가 Qubit 0의 상태에 직접적으로 아무것도 하지 않음에도 불구하고, Qubit 0의 상태에 따라 Qubit 1을 변경하기 때문에, Qubit 0에 위상 이동이 발생할 수 있습니다. 이것이 바로 "위상 킥백(phase-kickback)" 현상으로, Fundamentals of Quantum Algorithms 강좌의 [Lesson 1: Quantum Query Algorithms](/learning/courses/fundamentals-of-quantum-algorithms/quantum-query-algorithms/introduction)에서 더 자세히 다룹니다.

이제 이 알고리즘이 어떻게 작동하는지 이해했으니, Qiskit으로 구현해 보겠습니다.

```python
## Deutsch's algorithm:

## Step 1: Map the problem

# first, convert oracle circuit (above) to a single gate for drawing purposes. otherwise, the circuit is too large to display
blackbox = twobit_function(
    3
).to_gate()  # you may edit the number (1-4) inside "twobit_function()" to select among the four valid functions
blackbox.label = "$U_f$"

qc_deutsch = QuantumCircuit(2, 1)

qc_deutsch.x(1)
qc_deutsch.h(range(2))

qc_deutsch.barrier()
qc_deutsch.compose(twobit_function(2), inplace=True)
qc_deutsch.barrier()

qc_deutsch.h(0)
qc_deutsch.measure(0, 0)

qc_deutsch.draw("mpl")
```

![Output of the previous code cell](/learning/images/modules/computer-science/deutsch-jozsa/extracted-outputs/4d9129df-f2ef-4f94-9508-21ed986fd823-0.avif)

```python
# Step 2: Transpile
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

target = backend.target
pm = generate_preset_pass_manager(target=target, optimization_level=3)

qc_isa = pm.run(qc_deutsch)
```

```python
# Step 3: Run the job on a real quantum computer

job = sampler.run([qc_isa], shots=1)
# job = sampler_sim.run([qc_isa],shots=1) # uncomment this line to run on simulator instead
res = job.result()
counts = res[0].data.c.get_counts()
```

```python
# Step 4: Visualize and analyze results

## Analysis
print(counts)
if "1" in counts:
    print("balanced")
else:
    print("constant")
```

```text
{'1': 1}
balanced
```
