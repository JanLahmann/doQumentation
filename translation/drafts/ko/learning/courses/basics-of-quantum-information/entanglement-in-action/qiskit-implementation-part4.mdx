#### 기하학적 그림 {#geometric-picture}

위에서 설명한 전략을 기하학적으로 생각하면, Alice와 Bob이 선택한 다양한 각도 간의 관계를 이해하는 데 도움이 될 수 있습니다.

Alice가 실질적으로 하는 일은 질문 $x$에 따라 각도 $\alpha$를 선택한 뒤, 자신의 Qubit에 $U_{\alpha}$를 적용하고 측정하는 것입니다.
마찬가지로, Bob은 $y$에 따라 각도 $\beta$를 선택한 뒤, 자신의 Qubit에 $U_{\beta}$를 적용하고 측정합니다.
우리는 $\alpha$와 $\beta$를 다음과 같이 선택했습니다.

$$
\begin{aligned}
\alpha & = \begin{cases}
0 & x=0\\
\pi/4 & x=1
\end{cases}\\[4mm]
\beta & = \begin{cases}
\pi/8 & y = 0\\
-\pi/8 & y = 1
\end{cases}
\end{aligned}
$$

지금은 $\alpha$와 $\beta$를 임의의 값으로 두겠습니다.
$\alpha$를 선택함으로써, Alice는 다음과 같이 생긴 정규직교 기저 벡터를 정의합니다.

![Alice의 기저](/learning/images/courses/basics-of-quantum-information/entanglement-in-action/qiskit-implementation/alpha-basis.avif)

Bob도 마찬가지이며, 그의 각도는 $\beta$입니다.

![Bob의 기저](/learning/images/courses/basics-of-quantum-information/entanglement-in-action/qiskit-implementation/beta-basis.avif)

벡터의 색상은 Alice와 Bob의 답에 대응합니다: 파란색은 $0$, 빨간색은 $1$입니다.

이제 ($3$)과 ($4$)를 결합하면 다음 공식을 얻습니다.

$$
\langle \psi_{\alpha} \otimes\psi_{\beta} \vert \phi^+ \rangle
= \frac{1}{\sqrt{2}} \langle \psi_{\alpha} \vert \psi_{\beta} \rangle;
$$

이것은 모든 실수 $\alpha$와 $\beta$에 대해 성립합니다.

위에서 진행한 분석과 동일한 방식으로, $\alpha$와 $\beta$를 변수로 두고 계산하면 다음을 얻습니다.

$$
  \begin{aligned}
  & \bigl(U_{\alpha} \otimes U_{\beta}\bigr) \vert \phi^+\rangle\\[1mm]
  & \qquad =
    \vert 00 \rangle \langle \psi_{\alpha} \otimes \psi_{\beta}\vert \phi^+\rangle
    + \vert 01 \rangle \langle \psi_{\alpha} \otimes\psi_{\beta + \pi/2}\vert \phi^+\rangle \\
  & \qquad \qquad + \vert 10 \rangle \langle \psi_{\alpha+\pi/2} \otimes \psi_{\beta}\vert \phi^+\rangle
    + \vert 11 \rangle \langle \psi_{\alpha+\pi/2} \otimes \psi_{\beta+\pi/2}\vert \phi^+\rangle\\[2mm]
  & \qquad = \frac{
        \langle \psi_\alpha \vert \psi_\beta \rangle \vert 00\rangle
      + \langle \psi_\alpha \vert \psi_{\beta+\pi/2} \rangle \vert 01\rangle
      + \langle \psi_{\alpha+\pi/2} \vert \psi_\beta \rangle \vert 10\rangle
      + \langle \psi_{\alpha+\pi/2} \vert \psi_{\beta+\pi/2} \rangle \vert 11\rangle
      }{\sqrt{2}}.
  \end{aligned}
$$

이로부터 다음 두 공식을 도출합니다.

$$
\begin{aligned}
\operatorname{Pr}(a = b)  & = \frac{1}{2} \vert \langle \psi_\alpha \vert \psi_\beta \rangle \vert^2
+ \frac{1}{2} \vert \langle \psi_{\alpha+\pi/2} \vert \psi_{\beta+\pi/2} \rangle \vert^2
= \cos^2(\alpha - \beta)\\[2mm]
\operatorname{Pr}(a \neq b) & = \frac{1}{2} \vert \langle \psi_\alpha \vert \psi_{\beta+\pi/2} \rangle \vert^2
+ \frac{1}{2} \vert \langle \psi_{\alpha+\pi/2} \vert \psi_\beta \rangle \vert^2
= \sin^2(\alpha - \beta).
\end{aligned}
$$

이 방정식들은 Alice와 Bob이 선택한 기저를 겹쳐 놓은 위 그림들과 연결 지을 수 있습니다.

#### 전략 탐색 {#explore-the-strategy}

$(x,y) = (0,0)$인 경우, Alice와 Bob은 $\alpha = 0$, $\beta = \pi/8$을 선택하며, 그들의 기저를 겹치면 다음 그림을 얻습니다.

![Alice와 Bob의 기저 경우 1](/learning/images/courses/basics-of-quantum-information/entanglement-in-action/qiskit-implementation/strategy00.avif)

빨간색 벡터 사이의 각도는 $\pi/8$이며, 이는 두 파란색 벡터 사이의 각도와 동일합니다.
Alice와 Bob의 결과가 일치할 확률은 이 각도의 코사인 제곱으로,

$$
\cos^2\Bigl(\frac{\pi}{8}\Bigr) = \frac{2 + \sqrt{2}}{4},
$$

불일치할 확률은 이 각도의 사인 제곱으로,

$$
\sin^2\Bigl(\frac{\pi}{8}\Bigr) = \frac{2 - \sqrt{2}}{4}
$$

입니다.

$(x,y) = (0,1)$인 경우, Alice와 Bob은 $\alpha = 0$, $\beta = -\pi/8$을 선택하며, 그들의 기저를 겹치면 다음 그림을 얻습니다.

![Alice와 Bob의 기저 경우 1](/learning/images/courses/basics-of-quantum-information/entanglement-in-action/qiskit-implementation/strategy01.avif)

빨간색 벡터 사이의 각도는 다시 $\pi/8$이고, 파란색 벡터 사이의 각도도 마찬가지입니다.
Alice와 Bob의 결과가 일치할 확률은 다시 이 각도의 코사인 제곱으로,

$$
\cos^2\Bigl(\frac{\pi}{8}\Bigr) = \frac{2 + \sqrt{2}}{4},
$$

불일치할 확률은 이 각도의 사인 제곱으로,

$$
\sin^2\Bigl(\frac{\pi}{8}\Bigr) = \frac{2 - \sqrt{2}}{4}
$$

입니다.

$(x,y) = (1,0)$인 경우, Alice와 Bob은 $\alpha = \pi/4$, $\beta = \pi/8$을 선택하며, 그들의 기저를 겹치면 다음 그림을 얻습니다.

![Alice와 Bob의 기저 경우 1](/learning/images/courses/basics-of-quantum-information/entanglement-in-action/qiskit-implementation/strategy10.avif)

기저는 바뀌었지만 각도는 바뀌지 않았습니다 — 다시 한번 같은 색상의 벡터 사이 각도는 $\pi/8$입니다.
Alice와 Bob의 결과가 일치할 확률은

$$
\cos^2\Bigl(\frac{\pi}{8}\Bigr) = \frac{2 + \sqrt{2}}{4},
$$

불일치할 확률은

$$
\sin^2\Bigl(\frac{\pi}{8}\Bigr) = \frac{2 - \sqrt{2}}{4}
$$

입니다.

$(x,y) = (1,1)$인 경우, Alice와 Bob은 $\alpha = \pi/4$, $\beta = -\pi/8$을 선택합니다. 그들의 기저를 겹치면 무언가 달라진 것을 알 수 있습니다.

![Alice와 Bob의 기저 경우 1](/learning/images/courses/basics-of-quantum-information/entanglement-in-action/qiskit-implementation/strategy11.avif)

각도를 선택한 방식 때문에, 이번에는 같은 색상의 벡터 사이 각도가 $\pi/8$이 아닌 $3\pi/8$입니다.
Alice와 Bob의 결과가 일치할 확률은 여전히 이 각도의 코사인 제곱이지만, 이번에는 그 값이 다음과 같습니다.

$$
\cos^2\Bigl(\frac{3\pi}{8}\Bigr) = \frac{2 - \sqrt{2}}{4}.
$$

결과가 불일치할 확률은 이 각도의 사인 제곱으로, 이 경우에는 다음과 같습니다.

$$
\sin^2\Bigl(\frac{3\pi}{8}\Bigr) = \frac{2 + \sqrt{2}}{4}.
$$
