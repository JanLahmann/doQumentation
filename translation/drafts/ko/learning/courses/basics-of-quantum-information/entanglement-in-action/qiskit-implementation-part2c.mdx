#### 전략 설명 {#strategy-description}

이제 양자 전략을 설명할 수 있습니다.

- **준비 단계:** Alice와 Bob은 게임을 시작할 때 e-비트를 공유합니다. Alice는 Qubit $\mathsf{A}$를 보유하고, Bob은 Qubit $\mathsf{B}$를 보유하며, 두 Qubit $(\mathsf{X},\mathsf{Y})$는 함께 $\vert\phi^+\rangle$ 상태에 있습니다.

- **Alice의 행동:**
  - Alice가 질문 $x=0$을 받으면, 자신의 Qubit $\mathsf{A}$에 $U_{0}$을 적용합니다.
  - Alice가 질문 $x=1$을 받으면, 자신의 Qubit $\mathsf{A}$에 $U_{\pi/4}$를 적용합니다.

  Alice가 $\mathsf{A}$에 수행하는 연산은 다음과 같이 표현할 수도 있습니다:
  $$
  \begin{cases}
  U_0 & \text{$x = 0$인 경우}\\
  U_{\pi/4} & \text{$x = 1$인 경우}
  \end{cases}
  $$

  Alice는 이 연산을 적용한 후, $\mathsf{A}$를 표준 기저 측정으로 측정하고 측정 결과를 자신의 답 $a$로 설정합니다.

- **Bob의 행동:**
  - Bob이 질문 $y=0$을 받으면, 자신의 Qubit $\mathsf{B}$에 $U_{\pi/8}$을 적용합니다.
  - Bob이 질문 $y=1$을 받으면, 자신의 Qubit $\mathsf{B}$에 $U_{-\pi/8}$을 적용합니다.

  Alice의 경우와 마찬가지로, Bob이 $\mathsf{B}$에 수행하는 연산을 다음과 같이 나타낼 수 있습니다:
  $$
  \begin{cases}
  U_{\pi/8} & \text{$y = 0$인 경우}\\
  U_{-\pi/8} & \text{$y = 1$인 경우}
  \end{cases}
  $$

  Bob은 이 연산을 적용한 후, $\mathsf{B}$를 표준 기저 측정으로 측정하고 측정 결과를 자신의 답 $b$로 설정합니다.

이 전략을 나타내는 양자 Circuit 다이어그램은 다음과 같습니다:

![CHSH game circuit](/learning/images/courses/basics-of-quantum-information/entanglement-in-action/qiskit-implementation/CHSH.avif)

이 다이어그램에는 두 개의 일반적인 제어 Gate가 있습니다. 위쪽에 $U_{-\pi/8}$에 대한 것, 아래쪽에 $U_{\pi/4}$에 대한 것입니다.
또한 제어 Gate처럼 보이는 두 개의 Gate도 있는데, 위쪽에 $U_{\pi/8}$, 아래쪽에 $U_{0}$이며, 제어를 나타내는 원이 채워져 있지 않습니다.
이는 제어 비트가 $0$으로 설정될 때 Gate가 수행되는 다른 종류의 제어 Gate를 나타냅니다 (일반적인 제어 Gate처럼 $1$일 때가 아닙니다).
즉, 실질적으로 Bob은 $y=0$이면 자신의 Qubit에 $U_{\pi/8}$을, $y=1$이면 $U_{-\pi/8}$을 적용하고,
Alice는 $x=0$이면 자신의 Qubit에 $U_0$을, $x=1$이면 $U_{\pi/4}$를 적용합니다. 이는 위에서 설명한 프로토콜과 일치합니다.

이제 Alice와 Bob의 이 전략이 얼마나 잘 작동하는지 알아봐야 합니다.
네 가지 가능한 질문 쌍을 각각 살펴보면서 이를 확인해 보겠습니다.
