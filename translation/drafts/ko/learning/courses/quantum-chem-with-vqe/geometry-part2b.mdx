## 4단계: 후처리 {#step-4-post-processing}

시뮬레이터와 실제 하드웨어 모두에서, 각 원자간 거리에 대해 계산된 바닥 상태 에너지를 그래프로 나타내어 가장 낮은 에너지가 달성되는 지점을 확인할 수 있습니다. 그 지점이 자연에서 발견되는 원자간 거리여야 하며, 실제로도 근접한 값을 보입니다. 다른 앤사츠, 최적화기를 사용하거나, 각 기하학적 단계에서 계산을 여러 번 실행하고 여러 무작위 초기 조건에 대해 평균을 취하면 더 부드러운 곡선을 얻을 수 있습니다.

```python
# Here we can plot the results from this simulation.
plt.plot(distances_sim, vqe_energies_sim, label="VQE Energy")
plt.xlabel("Atomic distance (Angstrom)")
plt.ylabel("Energy")
plt.legend()
plt.show()
```

![Output of the previous code cell](/learning/images/courses/quantum-chem-with-vqe/geometry/extracted-outputs/e81f3ead-27ac-415e-a9f2-64a51d4b7aa3-0.avif)

최적화 단계 수를 단순히 늘린다고 해서 시뮬레이터 결과가 개선되지는 않을 것입니다. 모든 최적화가 실제로 최대 반복 횟수보다 적은 수의 반복 내에 필요한 허용 오차로 수렴했기 때문입니다.

실제 하드웨어의 결과는 약간 다른 범위의 값이 샘플링되었다는 점을 제외하면 비슷한 수준입니다.

```python
plt.plot(distances, vqe_energies, label="VQE Energy")
plt.xlabel("Atomic distance (Angstrom)")
plt.ylabel("Energy")
plt.legend()
plt.show()
```

![Output of the previous code cell](/learning/images/courses/quantum-chem-with-vqe/geometry/extracted-outputs/de8f53cf-9547-4578-b6cb-20d2b5602ee0-0.avif)

H2의 결합 길이는 0.74 옹스트롬, 전체 에너지는 -1.17 하트리여야 합니다. 시뮬레이터보다 실제 하드웨어 결과가 이 값에 더 가깝게 나왔습니다. 이는 두 경우 모두 노이즈가 존재했거나 시뮬레이션되었지만, 실제 하드웨어의 경우에만 오류 완화가 적용되었기 때문으로 보입니다.

### 마무리 {#closing}

이것으로 양자 화학을 위한 VQE 강좌를 마칩니다. 양자 컴퓨팅에 사용되는 기본 정보 이론에 대해 더 알고 싶으시다면, John Watrous의 [양자 정보의 기초](/learning/courses/basics-of-quantum-information) 강좌를 확인해 보세요. VQE 워크플로우의 추가적인 간단한 예시는 [VQE를 활용한 하이젠베르크 체인의 바닥 상태 에너지 추정 튜토리얼](/tutorials/spin-chain-vqe)을 참조하세요. 또는 [튜토리얼](https://docs.quantum.ibm.com/tutorials)과 [강좌](/learning)를 탐색하여 양자 컴퓨팅의 최신 기술에 관한 더 많은 교육 자료를 찾아보세요.

이 강좌의 시험을 꼭 응시하세요. 80% 이상의 점수를 받으시면 Credly 배지를 받으실 수 있으며, 이메일로 자동 발송됩니다. IBM Quantum&reg; 네트워크의 일원이 되어 주셔서 감사합니다!

```python
import qiskit
import qiskit_ibm_runtime

print(qiskit.version.get_version_info())
print(qiskit_ibm_runtime.version.get_version_info())
```

```text
1.3.2
0.35.0
```
