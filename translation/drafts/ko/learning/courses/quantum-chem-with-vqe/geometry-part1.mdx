---
title: "분자 기하학"
sidebar_label: "분자 기하학"
description: "이 레슨에서는 간단한 분자의 기하학을 변화시키며 각 단계에서 에너지를 최소화합니다."
notebook_path: "learning/courses/quantum-chem-with-vqe/geometry.ipynb"
---

{/* doqumentation-source-hash: b2bf1f51 */}

<OpenInLabBanner notebookPath="learning/courses/quantum-chem-with-vqe/geometry.ipynb" />



{/* cspell:ignore pxxr prqs nelecas mcscf chmax Dmax vmax ecore ncas Excp disp */}

# 분자 기하학 결정하기

이전 섹션에서는 VQE를 구현하여 분자의 바닥 상태 에너지를 결정했습니다. 이는 양자 컴퓨팅의 유효한 활용 사례이지만, 분자의 구조를 결정하는 것이 더욱 유용할 것입니다.

## Step 1: 고전적 입력을 양자 문제로 매핑하기

이원자 수소의 기본 예제를 그대로 사용하면, 변화시켜야 할 기하학적 매개변수는 결합 길이뿐입니다. 이를 위해 이전과 동일한 방식으로 진행하되, 초기 분자 구성에 변수(인수로 전달되는 결합 길이 *x*)를 사용합니다. 이는 비교적 간단한 변경이지만, 변수가 페르미온 해밀토니안 구성에서 시작하여 매핑을 거쳐 비용 함수까지 전파되므로, 전체 과정의 함수들에 해당 변수를 포함시켜야 합니다.

먼저 이전에 사용했던 패키지 일부를 불러오고 Cholesky 함수를 정의합니다.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q matplotlib numpy pyscf qiskit qiskit-aer qiskit-ibm-runtime scipy
```

```python
from qiskit.quantum_info import SparsePauliOp
import matplotlib.pyplot as plt
import numpy as np

#!pip install pyscf==2.4.0
from pyscf import ao2mo, gto, mcscf, scf

def cholesky(V, eps):
    # see https://arxiv.org/pdf/1711.02242.pdf section B2
    # see https://arxiv.org/abs/1808.02625
    # see https://arxiv.org/abs/2104.08957
    no = V.shape[0]
    chmax, ng = 20 * no, 0
    W = V.reshape(no**2, no**2)
    L = np.zeros((no**2, chmax))
    Dmax = np.diagonal(W).copy()
    nu_max = np.argmax(Dmax)
    vmax = Dmax[nu_max]
    while vmax > eps:
        L[:, ng] = W[:, nu_max]
        if ng > 0:
            L[:, ng] -= np.dot(L[:, 0:ng], (L.T)[0:ng, nu_max])
        L[:, ng] /= np.sqrt(vmax)
        Dmax[: no**2] -= L[: no**2, ng] ** 2
        ng += 1
        nu_max = np.argmax(Dmax)
        vmax = Dmax[nu_max]
    L = L[:, :ng].reshape((no, no, ng))
    print(
        "accuracy of Cholesky decomposition ",
        np.abs(np.einsum("prg,qsg->prqs", L, L) - V).max(),
    )
    return L, ng

def identity(n):
    return SparsePauliOp.from_list([("I" * n, 1)])

def creators_destructors(n, mapping="jordan_wigner"):
    c_list = []
    if mapping == "jordan_wigner":
        for p in range(n):
            if p == 0:
                ell, r = "I" * (n - 1), ""
            elif p == n - 1:
                ell, r = "", "Z" * (n - 1)
            else:
                ell, r = "I" * (n - p - 1), "Z" * p
            cp = SparsePauliOp.from_list([(ell + "X" + r, 0.5), (ell + "Y" + r, -0.5j)])
            c_list.append(cp)
    else:
        raise ValueError("Unsupported mapping.")
    d_list = [cp.adjoint() for cp in c_list]
    return c_list, d_list
```

해밀토니안을 정의하기 위해 이전 예제와 동일하게 PySCF를 사용하되, 이제 원자 간 거리의 역할을 하는 변수 `x`를 포함합니다. 이 함수는 이전과 마찬가지로 핵심 에너지, 단일 전자 에너지, 이중 전자 에너지를 반환합니다.

```python
def ham_terms(x: float):
    distance = x
    a = distance / 2
    mol = gto.Mole()
    mol.build(
        verbose=0,
        atom=[
            ["H", (0, 0, -a)],
            ["H", (0, 0, a)],
        ],
        basis="sto-6g",
        spin=0,
        charge=0,
        symmetry="Dooh",
    )

    # mf = scf.RHF(mol)
    # mx = mcscf.CASCI(mf, ncas=2, nelecas=(1, 1))
    # mx.kernel()

    mf = scf.RHF(mol)
    mf.kernel()
    if not mf.converged:
        raise RuntimeError(f"SCF did not converge for distance {x}")

    mx = mcscf.CASCI(mf, ncas=2, nelecas=(1, 1))
    casci_energy = mx.kernel()
    if casci_energy is None:
        raise RuntimeError(f"CASCI failed for distance {x}")

    # Other variables that might come in handy:
    # active_space = range(mol.nelectron // 2 - 1, mol.nelectron // 2 + 1)
    #    E1 = mf.kernel()
    # mo = mx.sort_mo(active_space, base=0)
    #    E2 = mx.kernel(mo)[:2]

    h1e, ecore = mx.get_h1eff()
    h2e = ao2mo.restore(1, mx.get_h2eff(), mx.ncas)
    return ecore, h1e, h2e
```

위의 구성은 원자 종류, 기하학, 전자 궤도를 기반으로 페르미온 해밀토니안을 만드는 것을 떠올려보세요. 아래에서는 이 페르미온 해밀토니안을 파울리 연산자로 매핑합니다. 이 `build_hamiltonian` 함수에도 기하학적 변수가 인수로 포함됩니다.

```python
def build_hamiltonian(distx: float) -> SparsePauliOp:
    ecore = ham_terms(distx)[0]
    h1e = ham_terms(distx)[1]
    h2e = ham_terms(distx)[2]

    ncas, _ = h1e.shape

    C, D = creators_destructors(2 * ncas, mapping="jordan_wigner")
    Exc = []
    for p in range(ncas):
        Excp = [C[p] @ D[p] + C[ncas + p] @ D[ncas + p]]
        for r in range(p + 1, ncas):
            Excp.append(
                C[p] @ D[r]
                + C[ncas + p] @ D[ncas + r]
                + C[r] @ D[p]
                + C[ncas + r] @ D[ncas + p]
            )
        Exc.append(Excp)

    # low-rank decomposition of the Hamiltonian
    Lop, ng = cholesky(h2e, 1e-6)
    t1e = h1e - 0.5 * np.einsum("pxxr->pr", h2e)

    H = ecore * identity(2 * ncas)
    # one-body term
    for p in range(ncas):
        for r in range(p, ncas):
            H += t1e[p, r] * Exc[p][r - p]
    # two-body term
    for g in range(ng):
        Lg = 0 * identity(2 * ncas)
        for p in range(ncas):
            for r in range(p, ncas):
                Lg += Lop[p, r, g] * Exc[p][r - p]
        H += 0.5 * Lg @ Lg

    return H.chop().simplify()
```

VQE 자체를 실행하는 데 필요한 나머지 패키지들, 예를 들어 efficient_su2 ansatz와 SciPy 최소화 함수를 불러옵니다.

```python
# General imports

# Pre-defined ansatz circuit and operator class for Hamiltonian
from qiskit.circuit.library import efficient_su2
from qiskit.quantum_info import SparsePauliOp

# SciPy minimizer routine
from scipy.optimize import minimize

# Plotting functions

# Qiskit Runtime tools
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
```

비용 함수도 다시 정의하겠습니다. 이 함수는 항상 완전히 구성되고 매핑된 해밀토니안을 인수로 받으므로, 함수 자체에는 아무런 변경이 없습니다.

```python
def cost_func(params, ansatz, H, estimator):
    pub = (ansatz, [H], [params])
    result = estimator.run(pubs=[pub]).result()
    energy = result[0].data.evs[0]
    return energy

# def cost_func_sim(params, ansatz, H, estimator):
#    energy = estimator.run(ansatz, H, parameter_values=params).result().values[0]
#    return energy
```

## Step 2: 양자 실행을 위한 문제 최적화

해밀토니안은 각각의 새로운 기하학마다 바뀌기 때문에, 연산자의 트랜스파일링도 각 단계마다 달라집니다. 그럼에도 불구하고, 사용하려는 하드웨어에 맞는 일반적인 패스 매니저를 각 단계에 적용할 수 있도록 미리 정의할 수 있습니다.

여기서는 사용 가능한 가장 한가한 Backend를 사용합니다. 해당 Backend를 AerSimulator의 모델로 활용하여, 실제 Backend의 노이즈 동작 등을 시뮬레이터가 모방할 수 있게 합니다. 이러한 노이즈 모델이 완벽하지는 않지만, 실제 하드웨어에서 어떤 결과를 기대할 수 있는지 파악하는 데 도움이 될 수 있습니다.

```python
# Here, we select the least busy backend available:
backend = service.least_busy(operational=True, simulator=False)
print(backend)
# Or to select a specific real backend use the line below, and substitute 'ibm_strasbourg' for your chosen device.
# backend = service.get_backend('ibm_strasbourg')
```

```python
# To run on a simulator:
# -----------
from qiskit_aer import AerSimulator

backend_sim = AerSimulator.from_backend(backend)
```

Circuit을 최적화하는 데 도움이 되는 패스 매니저 및 관련 패키지를 가져옵니다. 이 단계와 바로 위의 단계는 해밀토니안과 무관하므로, 이전 레슨과 동일합니다.

```python
from qiskit.transpiler import PassManager
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager
from qiskit.transpiler.passes import (
    ALAPScheduleAnalysis,
    PadDynamicalDecoupling,
    ConstrainedReschedule,
)
from qiskit.circuit.library import XGate

target = backend.target
pm = generate_preset_pass_manager(target=target, optimization_level=3)
pm.scheduling = PassManager(
    [
        ALAPScheduleAnalysis(target=target),
        ConstrainedReschedule(
            acquire_alignment=target.acquire_alignment,
            pulse_alignment=target.pulse_alignment,
            target=target,
        ),
        PadDynamicalDecoupling(
            target=target,
            dd_sequence=[XGate(), XGate()],
            pulse_alignment=target.pulse_alignment,
        ),
    ]
)
```
