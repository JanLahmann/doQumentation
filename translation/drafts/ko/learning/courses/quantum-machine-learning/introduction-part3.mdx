## Qiskit의 statevector 기반 Primitive를 사용하여 문제 평가하기

```python
sampler = StatevectorSampler()
counts = (
    sampler.run([overlap_circ], shots=num_shots).result()[0].data.meas.get_int_counts()
)

# Step 4: Analyze and post-processing

# Find the probability of 0.
counts.get(0, 0.0) / num_shots
```

```text
--2025-05-09 10:04:28--  https://raw.githubusercontent.com/qiskit-community/prototype-quantum-kernel-training/main/data/dataset_graph7.csv
Resolving raw.githubusercontent.com (raw.githubusercontent.com)... 185.199.110.133, 185.199.109.133, 185.199.108.133, ...
Connecting to raw.githubusercontent.com (raw.githubusercontent.com)|185.199.110.133|:443... connected.
HTTP request sent, awaiting response... 200 OK
Length: 49405 (48K) [text/plain]
Saving to: 'dataset_graph7.csv.2'

dataset_graph7.csv. 100%[===================>]  48.25K  --.-KB/s    in 0.03s   

2025-05-09 10:04:29 (1.37 MB/s) - 'dataset_graph7.csv.2' saved [49405/49405]
```

```text
0.8199
```

위의 모든 단계를 이해하지 않아도 되지만, 우리가 이 작업을 왜 수행하는지 알 수 있도록 결과를 이해해 보겠습니다. 머신러닝의 많은 과정에서 이진 분류(및 기타 용도)의 일환으로 내적(inner product)을 사용합니다. 양자역학은 이와 명백한 연관성을 가지고 있습니다. 초기 상태 $|\psi\rangle$에 대해 다양한 상태 $|\phi_i\rangle$를 측정할 확률은 내적을 통해 $P_i = |\langle\phi_i|\psi\rangle|^2$으로 주어지기 때문입니다. 즉, 위에서 한 작업은 두 데이터 포인트의 특징(feature)을 포함하는 양자 Circuit을 생성하고, 이를 양자 벡터 공간에 매핑한 뒤, 측정을 통해 해당 공간에서의 내적을 추정하는 것입니다. 이것이 바로 양자 커널 추정(quantum kernel estimation)의 예입니다. 여기서는 두 데이터 포인트(14번째와 19번째)에 대해서만 이 과정을 구현했습니다. 모든 가능한 쌍에 대해 이 과정을 수행하면, 출력값(이 경우 0.821...)을 이용하여 훈련 데이터셋의 모든 포인트 간의 중첩(overlap)을 나타내는 행렬을 구성할 수 있습니다. 이것이 바로 "커널 행렬(kernel matrix)"입니다.

### 이해도 확인

아래 질문을 읽고 답을 생각해 본 후, 삼각형을 클릭하여 정답을 확인하세요.

<details>
<summary>

위 과정에서 14번째와 19번째 데이터 포인트에 대한 커널 행렬 항목을 계산했습니다. 동일한 데이터 포인트를 두 번 사용(예: 14번째와 14번째)하면 어떤 값을 얻어야 할까요? 다시 말해, 커널 행렬의 대각 항목은 어떤 값이어야 할까요? 노이즈가 없는 상황을 가정하되, 노이즈가 있을 경우 답과 다른 값이 나올 수 있음에 유의하세요.

</summary>

__정답:__

대각 항목은 1.0이어야 합니다. 이 과정은 벡터와 자기 자신의 정규화된 내적을 계산하는 것으로, 그 값은 항상 1이어야 합니다.

</details>
