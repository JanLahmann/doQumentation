## 강좌 학습 목표

이 강좌를 완료하면 다음과 같은 핵심 기술과 역량을 쌓을 수 있습니다. 학습자는 아래 사항을 수행할 수 있게 됩니다:

1. QML이 무엇인지, 그리고 양자 컴퓨팅이 고전적 머신러닝과 어떻게 연결되는지 설명합니다.

2. 양자 컴퓨팅 용어와 핵심 개념을 ML 워크플로에 적용합니다.

3. QML 워크플로(다양한 유형)의 핵심 구성 요소를 식별합니다.

4. 다양한 유형의 QML을 식별하고 서로 구별합니다.

5. Qiskit 패턴을 따르고 Qiskit Runtime Primitives를 사용하여 양자 커널 방법과 변분 양자 분류기를 구현합니다.

6. QML이 가장 유망한 분야와 그렇지 않은 분야를 파악합니다.

7. 예제 문제를 자신의 데이터셋에 맞게 조정합니다.

8. 학습 시간, 노이즈, 다중 상태 읽기에서의 오류 누적 등 QML의 문제점을 인식합니다.

9. QML이 자신의 조직에 어떤 이점을 줄 수 있는지 권장 사항을 제시합니다.

## 강좌 구성

이 강좌는 여러 레슨으로 구성되어 있습니다. 각 레슨에는 진행하면서 새로운 기술을 연습하거나 이해도를 확인할 수 있도록 본문 곳곳에 체크인 질문이 포함되어 있습니다. 이 질문들은 필수 사항이 아닙니다.

강좌 말미에는 20문항으로 구성된 퀴즈가 있습니다. Credly를 통해 양자 머신러닝 배지를 획득하려면 퀴즈에서 최소 70% 이상의 점수를 받아야 합니다. 70% 이상 득점하면 배지가 자동으로 이메일로 전송됩니다. 퀴즈는 최대 두 번까지 제출할 수 있습니다. 첫 번째 제출 후에는 틀린 문항을 다시 풀 기회가 주어집니다. 두 번째 제출 후에는 점수가 최종 확정됩니다. 자세한 내용은 퀴즈를 참고하세요.

강좌 구성은 다음과 같습니다:

- 레슨 1: 소개 및 개요
- 레슨 2: 머신러닝 복습
- 레슨 3: 데이터 인코딩
- 레슨 4: 양자 커널 방법 및 서포트 벡터 머신
- 레슨 5: 변분 양자 분류기 / 신경망
- 배지 취득을 위한 시험

## 첫 번째 QML 코드 실행

전체 내용을 세세하게 분석하기 전에 먼저 전체 그림을 보는 것이 도움이 될 때가 많습니다. 아래 코드 셀들은 양자 커널 방법의 간단한 예시를 실행합니다. 구체적으로는 단일 커널 행렬 요소를 계산합니다. 커널 방법이나 양자 커널에 익숙하지 않은 사용자도 걱정할 필요가 없습니다. 이 강좌의 여러 레슨에서 이 셀들이 수행하는 작업을 상세히 분석할 것입니다.

이 코드를 통해 Qiskit 패턴도 함께 소개합니다. Qiskit 패턴은 유틸리티 규모의 양자 컴퓨팅 문제에 접근하기 위한 프레임워크입니다. 이 프레임워크는 매우 범용적인 네 가지 단계로 구성되어 있으며, 대부분의 문제에 적용할 수 있습니다(다만 일부 워크스트림에서는 특정 단계가 여러 번 반복될 수 있습니다).

### Qiskit 패턴:

* 1단계: 고전적 입력을 양자 문제로 매핑
* 2단계: 양자 실행을 위한 문제 최적화
* 3단계: Qiskit Runtime Primitives를 사용하여 실행
* 4단계: 분석 / 후처리

아래 셀들에서는 각 단계에 대해 간략하게만 설명하며, 더 자세히 학습할 수 있는 레슨을 안내합니다.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy pandas qiskit
```

```python
# Import some qiskit packages required for setting up our quantum circuits.
from qiskit.circuit import Parameter, ParameterVector, QuantumCircuit
from qiskit.circuit.library import unitary_overlap

# Import StatevectorSampler as our sampler.
from qiskit.primitives import StatevectorSampler

# Step 1: Map classical inputs to a quantum problem:

# Start by getting some appropriate data. The data imported below consist of 128 rows or data points.
# Each row has 14 columns that correspond to data features, and a 15th column with a label (+/-1).
!wget https://raw.githubusercontent.com/qiskit-community/prototype-quantum-kernel-training/main/data/dataset_graph7.csv

# Import some required packages, and write a function to pull some training data out of the csv file you got above.
import pandas as pd
import numpy as np

def get_training_data():
    """Read the training data."""
    df = pd.read_csv("dataset_graph7.csv", sep=",", header=None)
    training_data = df.values[:20, :]
    ind = np.argsort(training_data[:, -1])
    X_train = training_data[ind][:, :-1]

    return X_train

# Prepare training data
X_train = get_training_data()

# Empty kernel matrix
num_samples = np.shape(X_train)[0]

# Prepare feature map for computing overlap between two data points.
# This could be pre-built feature maps like ZZFeatureMap, or a custom quantum circuit, as shown here.
num_features = np.shape(X_train)[1]
num_qubits = int(num_features / 2)
entangler_map = [[0, 2], [3, 4], [2, 5], [1, 4], [2, 3], [4, 6]]
fm = QuantumCircuit(num_qubits)
training_param = Parameter("θ")
feature_params = ParameterVector("x", num_qubits * 2)
fm.ry(training_param, fm.qubits)
for cz in entangler_map:
    fm.cz(cz[0], cz[1])
for i in range(num_qubits):
    fm.rz(-2 * feature_params[2 * i + 1], i)
    fm.rx(-2 * feature_params[2 * i], i)

# Pick two data points, here 14 and 19, and assign the features to the circuits as parameters.
x1 = 14
x2 = 19
unitary1 = fm.assign_parameters(list(X_train[x1]) + [np.pi / 2])
unitary2 = fm.assign_parameters(list(X_train[x2]) + [np.pi / 2])

# Create the overlap circuit
overlap_circ = unitary_overlap(unitary1, unitary2)
overlap_circ.measure_all()
overlap_circ.draw("mpl", scale=0.6, style="iqp")

# Step 2: Optimize problem for quantum execution

# Use Qiskit Runtime service to get the least busy backend for running on real quantum computers.
# from qiskit_ibm_runtime import QiskitRuntimeService

# service = QiskitRuntimeService(channel="ibm_quantum")
# backend = service.least_busy(
#    operational=True, simulator=False, min_num_qubits=overlap_circ.num_qubits
# )

# Transpile the circuits optimally for the chosen backend using a pass manager.
# from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager
# pm = generate_preset_pass_manager(optimization_level=3, backend=backend)
# overlap_ibm = pm.run(overlap_circ)

# Step 3: Execute using Qiskit Runtime Primitives

# Specify the number of shots to use.
num_shots = 10_000
```
