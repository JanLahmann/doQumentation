## RSA 해독 {#breaking-rsa}

위에서 설명한 <DefinitionTooltip definition="A public-key cryptosystem named after Ron Rivest, Adi Shamir, and Leonard Adleman. Developed in 1977.">RSA</DefinitionTooltip> 알고리즘의 유용성과 보안성은 두 가지 수학적 가정에 기반합니다.

1. $(e, n)$에만 접근 가능한 상황에서 <DefinitionTooltip definition="Given an integer $a$, and modulus $m$, the remainder from ax/m is 1. This is only the case for coprimes.">모듈러 곱셈 역원</DefinitionTooltip> $d$를 구하는 것은 계산적으로 불가능합니다.

2. RSA 설정에서, 모듈러 지수 연산은 <DefinitionTooltip definition="A one-way function where it is easy to compute an output from the inputs, but hard to determine the inputs from the output.">단방향 트랩도어 함수</DefinitionTooltip>처럼 작동합니다. 암호화에 사용될 때, 이 연산은 해독할 수 없는 암호문을 생성하며, 개인 키에 접근하지 않고는 암호문에서 평문을 복구하기 위해 연산을 역으로 수행하는 것이 불가능합니다. 그러나 트랩도어 역할을 하는 개인 키에 접근할 수 있다면 암호문을 쉽게 복호화할 수 있습니다.

<DefinitionTooltip definition="A public-key cryptosystem named after Ron Rivest, Adi Shamir, and Leonard Adleman. Developed in 1977.">RSA</DefinitionTooltip> 알고리즘에 대한 가장 대표적인 공격은 <DefinitionTooltip definition="The modulus is the remainder left over after dividing by the value specified. modulo is the operation of doing this.">모듈러스</DefinitionTooltip> $n$을 인수분해하여 개인 키 숫자 $d$를 효율적으로 복구함으로써 가정 1을 무너뜨리는 것을 목표로 합니다. 아래에서 설명하겠지만, $n$의 <DefinitionTooltip definition="prime factors of a number n, are those primes that are multiplied together to make n.">소인수</DefinitionTooltip> $p$와 $q$ 또는 토티엔트 <DefinitionTooltip definition="The greek symbol, phi, often used to indicate Euler's totient.">$φ$</DefinitionTooltip>$(n)$에 접근할 수 있다면 $d$를 쉽게 복구할 수 있습니다. $p$, $q$, 그리고 <DefinitionTooltip definition="The greek symbol, phi, often used to indicate Euler's totient.">$φ$</DefinitionTooltip>$(n)$은 키 생성 과정에서 비밀로 유지되다가 이후에 폐기된다는 점을 상기하세요. 고전 컴퓨터나 양자 컴퓨터를 사용하여 이 정보를 복구한 제3자는 사실상 개인 키를 발견하게 되어 RSA를 해독할 수 있습니다. 따라서 소인수분해는 RSA를 해독하는 데 필요한 핵심 계산 기본 요소입니다.

### 고전 컴퓨팅과 RSA {#classical-computing-and-rsa}

대형 정수의 소인수분해는 고전 컴퓨터에서 <DefinitionTooltip definition="Growing faster than a polynomial; for example, an exponential function.">초다항식</DefinitionTooltip> 또는 <DefinitionTooltip definition="Scaling slower than exponential. For example polynomial and linear scaling.">지수 미만</DefinitionTooltip>의 확장성을 보이는 것으로 알려져 있습니다. $10^{100}$보다 큰 정수를 인수분해하기 위한 가장 잘 알려진 고전 알고리즘은 <DefinitionTooltip definition="General Number Field Sieve - An efficient classical algorithm for factoring large integers.">*일반 수체 체*</DefinitionTooltip> [(GNFS)](https://en.wikipedia.org/w/index.php?title=General_number_field_sieve&oldid=1151781536)입니다.

<details>

<summary>
수학적 세부 사항
</summary>

$L_n[\frac{1}{3}, (\frac{64}{9})^{(\frac{1}{3})}] = e^{[((\frac{64}{9})^{(\frac{1}{3})} + o(1)) (log(n))^{\frac{1}{3}}(log(log(n))^{\frac{2}{3}})]}$

인수분해할 정수 $n$의 함수로서의 확장성입니다.

이 확장성은 $n$을 표현하는 데 필요한 비트 수에 대해 초다항식입니다.

따라서, 소인수분해는 고전 컴퓨터에서 비효율적인 것으로 간주됩니다.

</details>

현재, [고전 하드웨어에서 인수분해된 가장 큰 정수](https://en.wikipedia.org/w/index.php?title=RSA_Factoring_Challenge&oldid=1163481797)는 829비트 또는 250자리 10진수 범위에 있습니다. 지난 수십 년간 목격된 고전 컴퓨팅 성능의 기하급수적 성장을 고려할 때, 1024비트 RSA는 더 이상 단기적으로 안전하다고 여겨지지 않으며 이제는 사용이 중단되었습니다. 그럼에도 불구하고, 예측 가능한 미래에는 $10^{617}$ 범위의 2048비트 정수를 인수분해하는 것이 현재로서는 고전 시스템에서 불가능하다고 여겨지므로, 지속적인 실행 가능성이 있습니다. 그러나 양자 컴퓨터의 등장은 이 평가를 무효화합니다.

### 쇼어의 양자 알고리즘과 RSA {#shors-quantum-algorithm-and-rsa}

오늘날 아마도 가장 잘 알려진 양자 알고리즘은 정수의 소인수를 찾기 위한 <DefinitionTooltip definition="A quantum algorithm which can find prime factors efficiently. Developed by the mathematician, Peter Shor, in 1994.">쇼어의 알고리즘</DefinitionTooltip>입니다. [1994년 피터 쇼어에 의해 소개되었을 때](https://doi.org/10.1109/SFCS.1994.365700), 이 알고리즘은 소인수분해라는 실용적으로 매우 중요한 문제에서 고전 알고리즘보다 <DefinitionTooltip definition="Growing faster than a polynomial; for example, an exponential function.">초다항식 속도 향상</DefinitionTooltip>을 제공하는 최초의 양자 알고리즘으로 인정받았습니다.

쇼어의 알고리즘은 $n$을 비트 수라 할 때 $O(n^2)$으로 소수를 인수분해할 수 있습니다.

<details>
<summary>
쇼어의 알고리즘에 대한 수학적 설명
</summary>

<DefinitionTooltip definition="A public-key cryptosystem named after Ron Rivest, Adi Shamir, and Leonard Adleman. Developed in 1977.">RSA</DefinitionTooltip>의 맥락에서, <DefinitionTooltip definition="A quantum algorithm which can find prime factors efficiently. Developed by the mathematician, Peter Shor, in 1994.">쇼어의 알고리즘</DefinitionTooltip>은 <DefinitionTooltip definition="Modular exponentiation is the remainder when an integer is raised to a power (exponent) and then divided by a number (modulus). For example $x^e mod m$.">모듈러 지수 함수</DefinitionTooltip> $f(x) = a^x (mod~n)$의 <DefinitionTooltip definition="The interval at which a function repeats itself.">주기성</DefinitionTooltip>을 활용하며, <DefinitionTooltip definition="The modulus is the remainder left over after dividing by the value specified. modulo is the operation of doing this.">모듈러스</DefinitionTooltip> $n$의 효율적인 소인수분해를 가능하게 하는 양자 <DefinitionTooltip definition="The interval at which a function repeats itself.">주기</DefinitionTooltip> 탐색 기본 요소를 제공합니다.

RSA를 해독하기 위한 쇼어의 전반적인 방식을 단순화한 고수준 개요는 다음과 같습니다:

1. 공개 키의 일부로 공개된 모듈러스 $n$이 주어지면, $n$과 서로소인 수 $a$를 선택합니다. 즉, $gcd(a,n) = 1$이어야 합니다. $n = p*q$가 정확히 두 개의 소인수 $(p, q)$를 가진다는 것을 알고 있으므로, 임의로 선택한 $n$보다 작은 거의 모든 수는 $n$과 서로소일 가능성이 높습니다.

2. $a$를 선택했다면, $a^r \equiv 1 (mod~n)$이 되는 지수 $r$을 찾습니다. 이는 $a^r - 1 \equiv 0 (mod~n)$을 의미합니다. 위의 <DefinitionTooltip definition="Consistency, or equivalence ie. all behaving in the same way.">합동</DefinitionTooltip>이 성립하는 <DefinitionTooltip definition="The number of times a number is multiplied by itself.">지수</DefinitionTooltip> $r$의 존재는 <DefinitionTooltip definition="Modular exponentiation is the remainder when an integer is raised to a power (exponent) and then divided by a number (modulus). For example $x^e mod m$.">모듈러 지수</DefinitionTooltip>의 <DefinitionTooltip definition="The interval at which a function repeats itself.">주기성</DefinitionTooltip>에 의해 보장됩니다.

3. $r$이 짝수라면, $a^r - 1 \equiv 0 (mod~n) \implies (a^{r/2} - 1) (a^{r/2} + 1) =  \gamma*n$이 어떤 정수 $\gamma$에 대해 성립합니다. 우변이 $p$와 $q$를 소인수로 갖기 때문에, 이 후자 등식의 좌변도 $p$와 $q$를 두 소인수로 포함해야 합니다. $r$이 홀수라면, 1단계로 돌아가서 $a$에 대해 다른 값을 시도합니다.

4. $gcd((a^{r/2} - 1), n)$ 또는 $gcd((a^{r/2} + 1), n)$을 구하기 위해 <DefinitionTooltip definition="An efficient way of calculating the greatest common divisor of two whole numbers. It improves on the original Euclidean algorithm.">확장 유클리드 알고리즘</DefinitionTooltip>을 사용합니다. 계산된 <DefinitionTooltip definition="Greatest Common Divisor - the largest positive whole number that can divide two or more of the given whole numbers.">GCD</DefinitionTooltip>는 소인수 $p$ 또는 $q$ 중 하나를 매우 높은 확률로 식별합니다. $n$을 이 소인수로 나누어 다른 소인수를 복구합니다.

5. $p, q$를 알게 되면, 원래 RSA 알고리즘의 단계를 사용하여 토티엔트 $\phi(n)$을 재구성하고, 알려진 공개 키 숫자 $e$의 <DefinitionTooltip definition="Given an integer $a$, and modulus $m$, the remainder from ax/m is 1. This is only the case for coprimes.">모듈러 역원</DefinitionTooltip>으로서 개인 키 숫자 $d$를 생성합니다.

</details>

2023년 8월, Oded Regev는 다차원 접근법을 사용하여 $O(n^{1.5})$의 결과를 얻는 쇼어의 원래 알고리즘에 대한 [개선안을 발표했습니다](https://doi.org/10.48550/arXiv.2308.06572). 시간, 비용 또는 필요한 큐비트 수를 개선할 수 있는 [Ragavan and Vaikuntanathan](https://doi.org/10.48550/arXiv.2310.00899)의 연구를 포함하여 이 분야에서 추가적인 연구가 계속되고 있습니다. 이러한 알고리즘을 실제 RSA 암호화에 적용하는 것이 언제 진정으로 실현 가능해질지는 말할 수 없지만, 점점 가까워지고 있습니다.

#### RSA 암호화 해독을 보여주는 Python 예제 {#python-example-demonstrating-breaking-rsa-encryption}

다음 코드 셀에서는 공개 키만 주어진 상황에서 개인 키를 찾는 예제를 보여줍니다. 이것은 무차별 대입 고전 계산을 사용하지만, 쇼어의 알고리즘이 어떻게 사용될 수 있는지—대형 키를 포함하여—를 보여줍니다.

<Admonition type="note">
이 섹션에서는 Python 코드의 일부로 수학적 계산을 상세히 보여줄 것입니다.
</Admonition>

이 예제에서, 공개 키는 $(5, 247)$이며, 개인 키를 복구할 것입니다.

**1단계:** 첫 번째 단계는 247과 서로소인 숫자를 선택하는 것입니다. 우리가 추측하는 거의 모든 숫자가 가능합니다. 6을 선택해 보겠습니다.

```python
n = 247  # the modulus
e = 5  # public key number
a = 6  # an integer coprime to n
assert gcd(a, n) == 1
print(f"Checked {n} and {a} are coprime")
```

**2단계:** 다음으로 $6^r \equiv 1 (mod~247)$이 되는 <DefinitionTooltip definition="The interval at which a function repeats itself.">주기</DefinitionTooltip> $r$을 찾아야 합니다. 이 예제에서는 무차별 대입으로 $r$을 고전적으로 계산하지만, <DefinitionTooltip definition="An open-source SDK for working with quantum algorithms and computers. Developed by IBM.">Qiskit</DefinitionTooltip>을 사용하여 양자 컴퓨터에서 쇼어의 알고리즘을 사용할 수도 있습니다.

```python
r = 0
rem = 100
while rem != 1:
    r += 1
    rem = (a**r) % n

print(f"period r is: {r}")
assert a**r % n == 1

print(f"Checked {a}^{r} mod {n} is 1")
```

**3단계:** <DefinitionTooltip definition="The interval at which a function repeats itself.">주기</DefinitionTooltip> $r = 36$이 짝수이므로, $f1 = (a^{r/2}-1), f2=(a^{r/2}+1)$을 계산할 수 있습니다.

```python
# explicitly use as integer
f1 = int(a ** (r / 2) - 1)
f2 = int(a ** (r / 2) + 1)

print(f"f1 = {f1}")
print(f"f2 = {f2}")
```

**4단계:** 그 인수들 중 하나와 $n$의 <DefinitionTooltip definition="Greatest Common Divisor - the largest positive whole number that can divide two or more of the given whole numbers.">GCD</DefinitionTooltip>를 찾습니다. 이미 찾은 소인수로 $n$을 나누어 두 번째 소인수를 구합니다.

```python
q_found = gcd(f1, n)
print(f"One possible prime factor of n ({n}) is: {q_found}")

# explicit int (to avoid floating point)
p_found = int(n / q_found)
print(f"The second prime factor of n ({n}) is: {p_found}")

assert n == p_found * q_found
```

**5단계:** $n = 247$의 소인수를 $p_{found}=13$과 $q_{found}=19$로 복구했으므로, 토티엔트 $\phi_{found} = (p_{found}-1)*(q_{found}-1)$를 계산합니다.

개인 키는 공개 키 숫자 $e=5$의 <DefinitionTooltip definition="Given an integer $a$, and modulus $m$, the remainder from ax/m is 1. This is only the case for coprimes.">모듈러 역원</DefinitionTooltip>입니다.

```python
# Compute the totient
phi_found = (p_found - 1) * (q_found - 1)
print(f"The totient is: {phi_found}")

# Recover the private key number d_found by satisfying (d_found * e) % phi_found = 1
d_found = 1
while True:
    if (d_found * e) % phi_found == 1:
        break
    else:
        d_found += 1
print("Private Key number:", d_found)
```

위의 방식에서, 2단계는 [쇼어의 알고리즘](https://en.wikipedia.org/w/index.php?title=Shor%27s_algorithm&oldid=1164409434)이 두 가지 기본적인 양자 기본 요소, 즉 <DefinitionTooltip definition="A quantum algorithm that can be used to find periodic patterns. Named after the fourier transform in classical physics which can split a signal into a set of sine waves.">*양자 푸리에 변환*</DefinitionTooltip>과 <DefinitionTooltip definition="A quantum algorithm used to estimate phase in qubits, named after phase estimation in classical physics.">*양자 위상 추정*</DefinitionTooltip>을 사용하는 핵심 <DefinitionTooltip definition="The interval at which a function repeats itself.">주기</DefinitionTooltip> 탐색 연산입니다. 쇼어의 알고리즘의 양자적 측면에 대한 자세한 설명은 [양자 알고리즘의 기초](/learning/courses/fundamentals-of-quantum-algorithms)의 위상 추정 및 인수분해 레슨을 참조하세요. 1단계와 3~5단계는 고전 컴퓨터에서 쉽게 수행할 수 있는 비교적 간단한 연산들입니다.

선택 사항으로, [쇼어의 알고리즘](https://www.youtube.com/watch?v=EdJ7RoWcU48&t=325s) 구현에 대한 상세한 시각적 안내도 있습니다.

양자 컴퓨터에서, 쇼어의 알고리즘은 모듈러스 $n$에 대해 $O((log~n)^2 (log~log~n))$만큼 유리한 <DefinitionTooltip definition="An algorithm which scales similar to logarithmic scaling, that is, efficiently compared to the size of data. A binary search is a simple example.">다중로그 확장성</DefinitionTooltip>을 보이거나, $n$을 표현하는 데 필요한 비트 수에 대해 <DefinitionTooltip definition="Scales relative to a power, for example n^3. Grows quicker than linear scaling, but slower than exponential.">다항식 확장성</DefinitionTooltip>을 보일 수 있습니다. 이는 고전적인 <DefinitionTooltip definition="General Number Field Sieve - An efficient classical algorithm for factoring large integers.">GNFS 알고리즘</DefinitionTooltip>에 비해 <DefinitionTooltip definition="Growing faster than a polynomial, for example an exponential function.">초다항식 속도 향상</DefinitionTooltip>입니다.

[최근의 자원 추정](https://quantum-journal.org/papers/q-2021-04-15-433/)에 따르면, 하드웨어 구성에 관한 특정 가정을 기반으로, 쇼어의 알고리즘을 사용하여 2048비트 RSA를 해독하는 데 수만 개에서 수백만 개의 <DefinitionTooltip definition="A quantum bit, the equivalent of a bit in classical computing. A qubit behaves very differently since it can be in superposition, (imagine a combination of states), but once measured the superposition collapses, and the result will be 0 or 1.">큐비트</DefinitionTooltip>가 필요할 것으로 보입니다. 향후 수년 내에 수만 개의 큐비트를 갖춘 양자 컴퓨터가 등장할 가능성이 전혀 없지 않으므로, 자원 추정의 하한값은 접근 가능해질 수 있습니다.
