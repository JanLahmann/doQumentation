### 1.2 Krylov 방법의 유형 {#types-of-krylov-methods}

"Krylov 부분공간 방법"은 대형 선형 시스템 및 고유값 문제를 푸는 데 사용되는 여러 반복적 기법을 통칭합니다. 이 모든 방법의 공통점은 Krylov 부분공간으로부터 근사 해를 구성한다는 것입니다.

$$\mathcal{K}^r(A,|v\rangle ) = \text{span}\{|v\rangle, A|v\rangle, A^2|v\rangle, ..., A^{r-1}|v\rangle\},$$

여기서 $|v\rangle$은 초기 추정값입니다(참고문헌 [\[5\]](#references) 참조). 이 방법들은 수렴 속도, 메모리 사용량, 전체 계산 비용 등의 요소를 균형 있게 고려하면서 이 부분공간에서 최적의 근사값을 선택하는 방식에서 차이가 있습니다. 이 단원의 초점은 Krylov 부분공간 방법의 맥락에서 양자 컴퓨팅을 활용하는 것이며, 이러한 방법들에 대한 포괄적인 논의는 범위를 벗어납니다. 아래의 간략한 정의는 맥락 파악을 위한 것으로, 이 방법들을 더 깊이 탐구하기 위한 몇 가지 참고문헌을 포함합니다.

__켤레 기울기(CG, conjugate gradient) 방법__: 이 방법은 대칭 양정치 선형 시스템을 푸는 데 사용됩니다[\[6\]](#references). 각 반복 단계에서 오차의 $A$-노름을 최소화하므로, 이산화된 타원형 편미분방정식(PDE)에서 비롯된 시스템에 특히 효과적입니다[\[7\]](#references). 다음 절에서는 이 접근 방식을 사용하여 Krylov 부분공간이 선형 시스템의 개선된 해를 탐색하기 위한 효과적인 부분공간인 이유를 설명합니다.

__일반화 최소 잔차(GMRES, generalized minimal residual) 방법__: 이 방법은 일반적인 비대칭 선형 시스템을 푸는 데 설계되었습니다. 각 반복 단계에서 Krylov 공간에 대한 잔차 노름을 최소화하므로 강건하지만, 대형 시스템에서는 메모리 사용량이 많을 수 있습니다[\[7\]](#references).

__최소 잔차(MINRES, minimal residual) 방법__: 이 방법은 대칭 부정치 선형 시스템을 푸는 데 사용됩니다. GMRES와 유사하지만 행렬의 대칭성을 활용하여 계산 비용을 줄입니다[\[8\]](#references).

주목할 만한 다른 접근 방식으로는 고유값 문제를 위한 Arnoldi 방법과 밀접하게 관련된 __완전 직교화 방법(FOM, full orthogonalization method)__, __쌍켤레 기울기(BiCG, bi-conjugate gradient) 방법__, 그리고 __유도 차원 축소(IDR, induced dimension reduction) 방법__이 있습니다.

### 1.3 Krylov 부분공간 방법이 효과적인 이유 {#why-the-krylov-subspace-method-works}

여기서는 최급강하법(steepest descent)의 관점을 통해, Krylov 부분공간 방법이 고유벡터 근사값의 반복적 정제를 통해 행렬 고유값을 효율적으로 근사하는 방법임을 설명합니다. 기저 상태의 초기 추정값이 주어졌을 때, 가장 빠른 수렴을 이끌어내는 초기 추정값에 대한 연속적 보정의 공간이 바로 Krylov 부분공간임을 논증합니다. 단, 수렴 거동에 대한 엄밀한 증명은 다루지 않습니다.

관심 행렬 $A$가 대칭 양정치라고 가정합니다. 이를 통해 위에서 언급한 CG 방법과의 관련성이 가장 잘 드러납니다. 여기서는 희소성에 대한 가정을 하지 않으며, $A$가 에르미트 행렬이어야 한다고 주장하는 것도 아닙니다(에르미트 행렬이어야 하는 것은 해밀토니안인 경우입니다).

일반적으로 다음 형태의 문제를 풀고자 합니다.
$$
A|x\rangle=|b\rangle.
$$
$|b\rangle=c|x\rangle$으로 볼 수도 있는데, 여기서 $c$는 어떤 상수이며, 이는 고유값 문제에 해당합니다. 하지만 현재 문제 설정은 보다 일반적으로 유지합니다.

근사 해인 벡터 $|x_0\rangle$에서 시작합니다. 이 추정값 $|x_0\rangle$과 1.1절의 $|v_0\rangle$ 사이에 유사점이 있지만, 여기서는 이를 활용하지 않습니다. 추정값 $|x_0\rangle$에는 오차가 있으며, 이를 $|e_0\rangle$이라 합니다.
$$
|e_0\rangle:=|x\rangle−|x_0\rangle.
$$
또한 잔차 $R_0$를 다음과 같이 정의합니다.
$$
|R_0\rangle=|b\rangle−A|x_0\rangle.
$$
여기서 대문자 $R$은 잔차를 Krylov 부분공간의 차원 $r$과 구별하기 위해 사용합니다.

![실제 고유벡터 x, 추정값 x 0, 그리고 두 벡터 사이의 오차를 그래픽으로 나타낸 그림.](/learning/images/courses/quantum-diagonalization-algorithms/krylov/kqd-fig2.avif)

이제 다음 형태의 보정 단계를 수행하고자 합니다.
$$
|x_1\rangle=|x_0\rangle+|p_0\rangle,
$$
이를 통해 근사값이 개선되기를 기대합니다. 여기서 $|p_0\rangle$은 아직 결정되지 않은 벡터입니다. 보정 후의 오차를 $|e_1\rangle$이라 하면,
$$
|e_1\rangle=|x\rangle−|x_1\rangle=|x\rangle−(|x_0\rangle+|p_0\rangle)=|e_0\rangle−|p_0\rangle.
$$

![실제 고유벡터와 초기 추정값의 업데이트. 업데이트된 추정값이 실제 고유벡터에 더 가깝습니다.](/learning/images/courses/quantum-diagonalization-algorithms/krylov/kqd-fig3.avif)

행렬에 의해 변환될 때 오차가 어떻게 동작하는지에 관심이 있습니다. 따라서 오차의 $A$-노름을 계산해 봅니다.
$$
\begin{aligned}
∥|e_0\rangle−|p_0\rangle∥_A^2&=\left(\langle e_0|A−\langle p_0|A\right)\left(|e_0\rangle−|p_0\rangle\right)\\
 & = \langle e_0|A|e_0 \rangle − \langle e_0|A|p_0\rangle − \langle p_0|A|e_0\rangle+\langle p_0|A|p_0\rangle\\
 & = \langle e_0|A|e_0\rangle−2\langle e_0|A|p_0\rangle+\langle p_0|A|p_0\rangle\\
 & = d−2\langle R_0|p_0\rangle +\langle p_0|A|p_0\rangle,
 \end{aligned}
$$
여기서 $A$의 대칭성과 $A |e_0\rangle = |R_0\rangle$을 이용하였습니다. $d$는 $|p_0\rangle$에 무관한 상수입니다. 1.2절에서 언급했듯이, 오차의 $A$-노름이 최소화할 수 있는 유일한 양은 아니지만 좋은 선택입니다. 보정 벡터 $|p_0\rangle$의 선택에 따라 이 값이 어떻게 달라지는지 살펴봅니다. 함수 $f$를 다음과 같이 정의합니다.
$$
f(|p_0\rangle)=\langle p_0|A|p_0\rangle−2\langle R_0|p_0\rangle+d.
$$
$f$는 보정값 $|p_0\rangle$의 함수로서 $A$-노름으로 측정한 오차 $|e_1\rangle$입니다. 따라서 $f(|p_0\rangle)$을 최소화하는 $|p_0\rangle$을 선택하고자 합니다. 이를 위해 $f$의 기울기를 계산합니다. $A$의 대칭성을 이용하면
$$
\nabla f(|p_0\rangle) = 2(A|p_0\rangle−|R_0\rangle).
$$
기울기는 최급상승 방향을 가리키므로, 그 반대 방향이 함수가 가장 빠르게 감소하는 방향, 즉 __최급강하__ 방향입니다. 초기 추정값 $|x_0\rangle$에서 $|p_0\rangle=0$일 때,
$\nabla f(0) = -2|R_0\rangle.$
따라서 함수 $f$는 잔차 $|R_0\rangle$ 방향으로 가장 많이 감소합니다. 그러므로 초기 선택은 어떤 스칼라 $\alpha_0$에 대해 $|p_0\rangle=\alpha_0 |R_0\rangle$를 추가함으로써 가장 큰 이득을 얻을 수 있습니다.

다음 단계에서도 마찬가지로 벡터 $|p_1\rangle$을 선택하여 현재 근사값에 더합니다. 앞과 동일한 논리로 어떤 스칼라 $\alpha_1$에 대해 $|p_1\rangle = \alpha_1 |R_1\rangle$을 선택합니다. 이를 반복하면 $k$번째 반복에서의 벡터는
$$
|x_{k+1}\rangle=|x_0\rangle+\alpha_0 |R_0\rangle+\alpha_1 |R_1\rangle+⋯+\alpha_k |R_k\rangle.
$$
즉, 개선된 추정값을 선택하는 공간을 $|R_0\rangle$, $|R_1\rangle$ 등을 순서대로 추가하여 구성하게 됩니다. $k$번째 추정 벡터는 다음 공간에 속합니다.
$$
|x_{k+1}\rangle\in |x_0\rangle+\text{span}\{|R_0\rangle,|R_1\rangle,…,|R_k\rangle \}.
$$
이제 다음 관계식을 이용하면
$$
|R_{k+1}\rangle=|b\rangle−A |x_{k+1}\rangle=|b\rangle−A(|x_k\rangle+\alpha_k |R_k\rangle)=|R_k\rangle−\alpha_k A |R_k\rangle,
$$
다음이 성립함을 알 수 있습니다.
$$
\text{span} \{|R_0\rangle,|R_1\rangle,…,|R_k\rangle \}=\text{span} \{|R_0\rangle,A|R_0\rangle,…,A^{k}|R_0\rangle \}.
$$
즉, 올바른 해 $|x\rangle$을 가장 효율적으로 근사하기 위해 구성하는 공간은 $|R_0\rangle$에 행렬 $A$를 연속적으로 적용하여 구성되는 공간과 정확히 일치합니다. Krylov 부분공간은 바로 연속적인 최급강하 방향의 벡터들로 생성되는 공간입니다.

마지막으로, 이 접근 방식의 스케일링에 대한 수치적 주장은 하지 않았으며, 희소 행렬에 대한 비교 이점도 논의하지 않았음을 다시 한번 강조합니다. 이는 오직 Krylov 부분공간 방법의 사용을 동기화하고 직관적인 이해를 돕기 위한 것입니다. 이제 이 방법들의 수치적 거동을 탐구해 보겠습니다.

### 이해도 확인 {#check-your-understanding}

아래 질문을 읽고 답을 생각한 후, 삼각형을 클릭하여 정답을 확인하세요.

<details>
<summary>

위의 과정에서 오차의 $A$-노름을 최소화하는 방법을 제안했습니다. 기저 상태와 그 고유값을 구할 때 최소화할 수 있는 다른 양으로는 무엇이 있을까요?

</summary>

__정답:__

오차의 $A$-노름 대신 잔차 벡터를 사용하는 방법도 고려할 수 있습니다. 오차 벡터 자체를 고려하는 것이 유용한 경우도 있을 수 있습니다.
</details>
