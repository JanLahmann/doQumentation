---
title: "비트, 게이트, 그리고 회로"
sidebar_label: "비트, 게이트, 그리고 회로"
description: "양자 비트(Qubit)와 게이트를 사용하는 회로 모델로 양자 계산을 배웁니다."
notebook_path: "learning/courses/utility-scale-quantum-computing/bits-gates-and-circuits.ipynb"
---

{/* doqumentation-source-hash: 4c037410 */}

<OpenInLabBanner notebookPath="learning/courses/utility-scale-quantum-computing/bits-gates-and-circuits.ipynb" />



# 양자 비트, 게이트, 그리고 회로

<Admonition type="note">

Kifumi Numata (19 Apr 2024)

원본 강의 PDF를 다운로드하려면 [여기](https://ibm.ent.box.com/public/static/vee9e1kkxxiih5g8yqdhoo6t0o3dw7xf.zip)를 클릭하세요. 정적 이미지로 구성된 일부 코드 스니펫은 더 이상 사용되지 않을 수 있습니다.

*이 실험을 실행하는 데 걸리는 QPU 시간은 약 5초입니다.*

</Admonition>
## 1. 소개
비트, 게이트, 회로는 양자 컴퓨팅의 기본 구성 요소입니다. 여기서는 양자 비트와 게이트를 활용한 회로 모델로 양자 계산을 배우고, 중첩, 측정, 얽힘에 대해서도 복습합니다.

이번 강의에서 배울 내용:
- 단일 Qubit 게이트
- 블로흐 구
- 중첩
- 측정
- 2-Qubit 게이트와 얽힘 상태

강의 마지막에는 유틸리티 규모의 양자 컴퓨팅에서 필수적인 Circuit 깊이에 대해서도 배웁니다.
## 2. 다이어그램으로 나타내는 계산

Qubit 또는 비트를 사용할 때는, 입력을 원하는 출력으로 변환하기 위해 이들을 조작해야 합니다. 비트가 매우 적은 간단한 프로그램에서는 이 과정을 *회로 다이어그램*이라고 불리는 도식으로 나타내는 것이 유용합니다.

아래 왼쪽 그림은 고전 회로의 예시이고, 오른쪽 그림은 양자 Circuit의 예시입니다. 두 경우 모두 입력은 왼쪽에, 출력은 오른쪽에 있으며 연산은 기호로 표현됩니다. 연산에 사용되는 기호는 주로 역사적인 이유로 "게이트"라고 불립니다.

!["고전 논리 회로와 양자 Circuit"](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/classical-vs-quantum.avif)
## 3. 단일 Qubit 양자 게이트

### 3.1 양자 상태와 블로흐 구

Qubit의 상태는 $|0\rangle$과 $|1\rangle$의 중첩으로 표현됩니다. 임의의 양자 상태는 다음과 같이 나타낼 수 있습니다.

$$
|\psi\rangle =\alpha|0\rangle+ \beta|1\rangle
$$

여기서 $\alpha$와 $\beta$는 $|\alpha|^2+|\beta|^2=1$을 만족하는 복소수입니다.

$|0\rangle$과 $|1\rangle$은 2차원 복소 벡터 공간의 벡터입니다.

$$
|0\rangle = \begin{pmatrix}
1 \\0
\end{pmatrix},
|1\rangle = \begin{pmatrix}
0\\1
\end{pmatrix}
$$

따라서 임의의 양자 상태는 다음과 같이도 표현됩니다.

$$
|\psi\rangle = \alpha\begin{pmatrix}
1 \\ 0
\end{pmatrix} + \beta\begin{pmatrix}0\\
1
\end{pmatrix} = \begin{pmatrix}
\alpha \\ \beta
\end{pmatrix}
$$

이로부터, 양자 비트의 상태는 $|0\rangle$과 $|1\rangle$을 정규 직교 기저로 하는 2차원 복소 내적 공간에서의 단위 벡터임을 알 수 있습니다. 이 벡터는 1로 정규화됩니다.

$$
\langle\psi|\psi\rangle = \begin{pmatrix}
\alpha^* & \beta^*
\end{pmatrix}
\begin{pmatrix}
\alpha \\
\beta
\end{pmatrix} = 1
$$

$ |\psi\rangle =\begin{pmatrix}
\alpha \\ \beta
\end{pmatrix}$는 상태 벡터(statevector)라고도 합니다.

단일 Qubit 양자 상태는 다음과 같이도 표현할 수 있습니다.

$$
|\psi\rangle
=\cos\frac{\theta}{2}|0\rangle+e^{i\varphi}\sin\frac{\theta}{2}|1\rangle
=\left( \begin{pmatrix} \cos\frac{\theta}{2}\\ e^{i\varphi}\sin\frac{\theta}{2}
\end{pmatrix}\right)
$$

여기서 $\theta$와 $\varphi$는 다음 그림에서 블로흐 구의 각도입니다.

![블로흐 구](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/bloch.avif)
다음 몇 개의 코드 셀에서는 Qiskit의 구성 요소들로 기본 계산을 구성해 나갑니다. 빈 Circuit을 만든 다음 양자 연산을 추가하고, 게이트에 대해 설명하며 그 효과를 시각화합니다.
셀을 실행하려면 "Shift" + "Enter"를 누르세요. 먼저 라이브러리를 가져옵니다.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q qiskit qiskit-aer qiskit-ibm-runtime
```

```python
# Import the qiskit library
from qiskit import QuantumCircuit
from qiskit_aer import AerSimulator
from qiskit.quantum_info import Statevector
from qiskit.visualization import plot_bloch_multivector
from qiskit_ibm_runtime import Sampler
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager
from qiskit.visualization import plot_histogram
```

#### 양자 Circuit 준비
단일 Qubit Circuit을 생성하고 그려 봅니다.

```python
# Create the single-qubit quantum circuit
qc = QuantumCircuit(1)

# Draw the circuit
qc.draw("mpl")
```

![이전 코드 셀의 출력](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/fdef9137-9e39-4b05-afa9-9091766053f6-0.avif)

#### X Gate

X Gate는 블로흐 구의 $x$ 축을 중심으로 한 $\pi$ 회전입니다.
X Gate를 $|0\rangle$에 적용하면 $|1\rangle$이 되고, $|1\rangle$에 적용하면 $|0\rangle$이 됩니다. 따라서 고전적인 NOT Gate와 유사한 연산이며, 비트 플립(bit flip)이라고도 합니다. X Gate의 행렬 표현은 다음과 같습니다.

$$
X = \begin{pmatrix}
0 & 1 \\
1 & 0 \\
\end{pmatrix}
$$

```python
qc = QuantumCircuit(1)  # Prepare the single-qubit quantum circuit

# Apply a X gate to qubit 0
qc.x(0)

# Draw the circuit
qc.draw("mpl")
```

![이전 코드 셀의 출력](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/b3468430-0cbc-44ab-bd21-901436662a4b-0.avif)

IBM Quantum&reg;에서 초기 상태는 $|0\rangle$으로 설정되어 있으므로, 위 양자 Circuit의 행렬 표현은 다음과 같습니다.

$$
X|0\rangle=  \begin{pmatrix}
0 & 1 \\
1 & 0
\end{pmatrix}
\begin{pmatrix}
1 \\ 0
\end{pmatrix}
 =\begin{pmatrix}
0 \\ 1
\end{pmatrix} = |1\rangle
$$

다음으로, 상태 벡터 시뮬레이터를 사용하여 이 Circuit을 실행해 보겠습니다.

```python
# See the statevector
out_vector = Statevector(qc)
print(out_vector)

# Draw a Bloch sphere
plot_bloch_multivector(out_vector)
```

```text
Statevector([0.+0.j, 1.+0.j],
            dims=(2,))
```

![이전 코드 셀의 출력](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/efb727e8-f51e-4903-aa0f-e4c099316815-1.avif)

수직 벡터는 행 벡터로 표시되며, 복소수의 허수 부분은 $j$로 인덱싱됩니다.

#### H Gate
아다마르(Hadamard) Gate는 블로흐 구에서 $x$ 축과 $z$ 축의 중간 축을 기준으로 한 $\pi$ 회전입니다. H Gate를 $|0\rangle$에 적용하면 $\frac{|0\rangle + |1\rangle}{\sqrt{2}}$와 같은 중첩 상태가 만들어집니다. H Gate의 행렬 표현은 다음과 같습니다.

$$
H = \frac{1}{\sqrt{2}}\begin{pmatrix}
1 & 1 \\
1 & -1 \\
\end{pmatrix}
$$

```python
qc = QuantumCircuit(1)  # Create the single-qubit quantum circuit

# Apply an Hadamard gate to qubit 0
qc.h(0)

# Draw the circuit
qc.draw(output="mpl")
```

![이전 코드 셀의 출력](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/574c35f0-26a9-423a-8d65-7140ba7398e3-0.avif)

```python
# See the statevector
out_vector = Statevector(qc)
print(out_vector)

# Draw a Bloch sphere
plot_bloch_multivector(out_vector)
```

```text
Statevector([0.70710678+0.j, 0.70710678+0.j],
            dims=(2,))
```

![이전 코드 셀의 출력](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/7adf0e93-6db3-4aa8-800f-c91c8cc46001-1.avif)

이는 다음과 같습니다.
$$
H|0\rangle= \frac{1}{\sqrt{2}} \begin{pmatrix}
1 & 1 \\
1 & -1
\end{pmatrix}
\begin{pmatrix}
1 \\0
\end{pmatrix}
 =\frac{1}{\sqrt{2}}\begin{pmatrix}
1 \\
1
\end{pmatrix}
=\begin{pmatrix}
0.707 \\
0.707
\end{pmatrix}
=\frac{1}{\sqrt{2}}(|0\rangle+|1\rangle)
$$

이 중첩 상태는 매우 흔하고 중요하기 때문에 고유한 기호가 붙어 있습니다.

$$
|+\rangle \equiv \frac{1}{\sqrt{2}}(|0\rangle+|1\rangle).
$$

$|0\rangle$에 $H$ Gate를 적용함으로써 $|0\rangle$과 $|1\rangle$의 중첩 상태를 만들었습니다. 계산 기저(블로흐 구 그림에서 z 방향)로 측정하면 각 상태가 동일한 확률로 나타납니다.
#### $|-\rangle$ 상태

대응하는 $|-\rangle$ 상태가 있다는 것을 짐작하셨을 것입니다.
$$
|-\rangle \equiv \frac{|0\rangle -|1\rangle}{\sqrt{2}}.
$$
이 상태를 만들려면 먼저 X Gate를 적용하여 $|1\rangle$을 만든 다음, H Gate를 적용합니다.

```python
qc = QuantumCircuit(1)  # Create the single-qubit quantum circuit

# Apply a X gate to qubit 0
qc.x(0)

# Apply an Hadamard gate to qubit 0
qc.h(0)

# draw the circuit
qc.draw(output="mpl")
```

![이전 코드 셀의 출력](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/b8e96fc4-6708-415e-b8fc-ac69eaeae750-0.avif)

```python
# See the statevector
out_vector = Statevector(qc)
print(out_vector)

# Draw a Bloch sphere
plot_bloch_multivector(out_vector)
```

```text
Statevector([ 0.70710678+0.j, -0.70710678+0.j],
            dims=(2,))
```

![이전 코드 셀의 출력](/learning/images/courses/utility-scale-quantum-computing/bits-gates-and-circuits/extracted-outputs/f42b82aa-7a15-4f81-8530-b997d4c6e0de-1.avif)

이는 다음과 같습니다.

$$
H|1\rangle= \frac{1}{\sqrt{2}} \begin{pmatrix}
1 & 1 \\\
1 & -1
\end{pmatrix}
\begin{pmatrix}
0 \\\
1
\end{pmatrix}
 =\frac{1}{\sqrt{2}}\begin{pmatrix}
1 \\\
-1
\end{pmatrix}
=\begin{pmatrix}
0.707 \\\
-0.707
\end{pmatrix}
=\frac{1}{\sqrt{2}}(|0\rangle-|1\rangle) = |-\rangle
$$

$|1\rangle$에 $H$ Gate를 적용하면 $|0\rangle$과 $|1\rangle$의 동등한 중첩 상태가 만들어지지만, $|1\rangle$의 부호가 음수가 됩니다.
