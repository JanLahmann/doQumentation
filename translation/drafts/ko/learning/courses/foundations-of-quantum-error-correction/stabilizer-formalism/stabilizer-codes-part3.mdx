## 오류 감지 {#detecting-errors}

$n$-Qubit 스태빌라이저 코드가 스태빌라이저 생성원 $P_1,\ldots, P_r$로 기술되는 경우, 오류 감지는 다음과 같은 방식으로 작동합니다.

오류를 감지하기 위해 모든 스태빌라이저 생성원을 관측량으로 측정합니다.
스태빌라이저 생성원이 $r$개이므로 측정 결과도 $r$개이며, 각 결과는 $+1$ 또는 $-1$ 중 하나입니다 (또는 $+1$을 $0$, $-1$을 $1$에 대응시키는 이진 값으로 나타낼 수도 있습니다).
이 $r$개의 결과를 벡터 또는 문자열로 묶어 *신드롬(syndrome)*으로 해석합니다.
신드롬 $(+1,\ldots,+1)$은 오류가 감지되지 않았음을 나타내며, 신드롬 내에 $-1$이 하나라도 존재하면 오류가 감지된 것입니다.

특히 $E$가 가상의 오류를 나타내는 $n$-Qubit 파울리 연산이라고 가정합시다.
(오류로서 파울리 연산만 고려하는 이유는, 오류의 이산화가 임의의 스태빌라이저 코드에 대해서도 9-Qubit 쇼어 코드와 동일한 방식으로 작동하기 때문입니다.)
$E$가 오류로 감지되는지 여부는 다음 세 가지 경우에 따라 결정됩니다.

### 오류 감지 경우 {#error-detection-cases}

1. 연산 $E$는 스태빌라이저의 한 원소에 비례합니다.

   $$
   E = \pm Q \; \text{for some}\; Q \in \langle P_1,\ldots,P_r\rangle
   $$

   이 경우, $E$는 모든 스태빌라이저 생성원과 교환 가능하므로 신드롬 $(+1,\ldots,+1)$이 나타납니다. 즉, $E$는 오류로 감지되지 않습니다.

2. 연산 $E$는 스태빌라이저의 원소에 비례하지 않지만, 그럼에도 모든 스태빌라이저 생성원과 교환 가능합니다.

   $$
   E\neq \pm Q\; \text{for} \; Q \in \langle P_1,\ldots,P_r\rangle, \;\text{but}\;
   E P_k = P_k E \;\text{for every}\; k\in\{1,\ldots,r\}
   $$

   이는 코드 공간의 벡터를 어떤 비자명한 방식으로 변화시키는 오류입니다. 하지만 $E$가 모든 스태빌라이저 생성원과 교환 가능하기 때문에 신드롬은 $(+1,\ldots,+1)$이 되어, $E$는 코드에 의해 감지되지 않습니다.

3. 연산 $E$는 스태빌라이저 생성원 중 적어도 하나와 반교환 관계에 있습니다.

   $$
   P_k E = -E P_k \; \text{for at least one} \; k\in\{1,\ldots,r\}
   $$

   신드롬이 $(+1,\ldots,+1)$과 다르므로, 오류 $E$는 코드에 의해 감지됩니다.

첫 번째 경우에서 오류 $E$는 문제가 되지 않습니다. 이 연산은 코드 공간의 벡터에 대해 무관한 전역 위상을 주입할 수 있을 뿐, 실질적으로 아무런 변화도 일으키지 않기 때문입니다: 모든 인코딩된 상태 $\ket{\psi}$에 대해 $E \ket{\psi} = \pm\ket{\psi}$가 성립합니다. 본질적으로 이것은 실제 오류가 아닙니다 — $E$가 가질 수 있는 비자명한 작용은 코드 공간 외부에서만 발생합니다 — 따라서 $E$가 오류로 감지되지 않는 것은 적절한 일입니다. 아무런 조치도 필요하지 않으니까요.

두 번째 경우는 직관적으로 나쁜 경우입니다.
오류가 스태빌라이저 생성원과 *반교환* 관계에 있을 때 신드롬에 $-1$이 나타나 오류를 알리는데, 이 경우에는 그런 일이 발생하지 않습니다.
따라서 코드 공간의 벡터를 비자명한 방식으로 변화시키는 오류 $E$가 코드에 의해 감지되지 않는 상황이 됩니다.
예를 들어, 3-비트 반복 코드의 경우 연산 $E = X\otimes X\otimes X$가 이 범주에 해당합니다.

이러한 오류 $E$가 코드 공간의 일부 벡터를 비자명한 방식으로 변화시켜야 한다는 사실은 다음과 같이 논증할 수 있습니다.
$E$가 $P_1,\ldots,P_r$과 교환 가능하지만 스태빌라이저 원소에 비례하지 않는다는 가정에서, $E$를 $P_1,\ldots,P_r$에 추가하여 스태빌라이저 생성원으로 포함시키면 새로운 유효한 스태빌라이저 코드를 얻을 수 있습니다.
그런데 이 새로운 코드의 코드 공간은 원래 코드 공간의 절반 크기를 가지므로, $E$의 원래 코드 공간에 대한 작용이 항등 연산에 비례할 수 없다는 결론을 내릴 수 있습니다.

세 번째 경우, 즉 오류 $E$가 스태빌라이저 생성원 중 적어도 하나와 반교환 관계에 있는 경우,
신드롬에 적어도 하나의 $-1$이 나타나 문제가 있음을 나타냅니다.
이미 논의했듯이, 신드롬이 $E$를 고유하게 식별하지는 않으므로 각 신드롬에 대한 정정 연산을 선택해야 하며, 이것이 오류 $E$를 정정할 수도 있고 그렇지 않을 수도 있습니다.
이 단계는 레슨의 마지막 부분에서 곧 다루겠습니다.

### 스태빌라이저 코드의 거리 {#distance-of-a-stabilizer-code}

용어 측면에서, 스태빌라이저 코드의 *거리*는 위의 두 번째 범주에 해당하는 파울리 연산 $E$의 *최소 가중치*를 의미합니다 — 즉, 코드 공간을 비자명한 방식으로 변화시키지만 코드가 이를 감지하지 못하는 연산입니다.
스태빌라이저 코드가 $[[n,m,d]]$ 스태빌라이저 코드라고 불릴 때, 이중 대괄호를 사용하는 이 표기는 다음을 의미합니다:

1. 인코딩은 길이 $n$의 Qubit으로 이루어지며,
2. 코드는 $m$ Qubit의 인코딩을 허용하고,
3. 코드의 거리는 $d$입니다.

예시로 7-Qubit 스타인 코드를 살펴보겠습니다.
이 코드의 스태빌라이저 생성원은 다음과 같습니다:

$$
\begin{array}{ccccccc}
Z & Z & Z & Z & \mathbb{I} & \mathbb{I} & \mathbb{I} \\[1mm]
Z & Z & \mathbb{I} & \mathbb{I} & Z & Z & \mathbb{I} \\[1mm]
Z & \mathbb{I} & Z & \mathbb{I} & Z & \mathbb{I} & Z \\[1mm]
X & X & X & X & \mathbb{I} & \mathbb{I} & \mathbb{I} \\[1mm]
X & X & \mathbb{I} & \mathbb{I} & X & X & \mathbb{I} \\[1mm]
X & \mathbb{I} & X & \mathbb{I} & X & \mathbb{I} & X
\end{array}
$$

이 코드의 거리는 3이며, 다음과 같이 논증할 수 있습니다.

먼저 가중치가 최대 2인 임의의 파울리 연산 $E$를 고려하고, 이 연산이 여섯 개의 스태빌라이저 생성원 모두와 교환 가능하다고 가정합니다.
그러면 $E$는 반드시 항등 연산이어야 하며, 이는 (언제나 그렇듯이) 스태빌라이저의 원소입니다.
이를 통해 코드의 거리가 2보다 엄격하게 크다는 것을 알 수 있습니다.
특히 $E$가 다음의 형태를 취한다고 가정합니다:

$$
E = P \otimes Q \otimes \mathbb{I} \otimes \mathbb{I} \otimes \mathbb{I} \otimes \mathbb{I} \otimes \mathbb{I}
$$

여기서 $P$와 $Q$는 비항등 파울리 행렬일 수 있습니다.
이것은 하나의 경우에 불과하며, $E$의 텐서 인수들 중 비항등 파울리 행렬이 올 수 있는 다른 모든 위치에 대해서도 다음의 논증을 반복해야 하지만, 모든 가능한 위치에 대해 논증은 본질적으로 동일합니다.

연산 $E$는 여섯 개의 스태빌라이저 생성원 모두와 교환 가능하므로, 특히 다음 두 생성원과도 교환 가능합니다:

$$
\begin{gathered}
Z \otimes \mathbb{I} \otimes Z \otimes \mathbb{I} \otimes Z \otimes \mathbb{I} \otimes Z\\[1mm]
X \otimes \mathbb{I} \otimes X \otimes \mathbb{I} \otimes X \otimes \mathbb{I} \otimes X
\end{gathered}
$$

오류 $E$에서 텐서 인수 $Q$는 이 두 스태빌라이저 생성원에서 항등 행렬과 위치가 맞습니다 (그래서 이 두 생성원이 선택된 것입니다).
$E$의 오른쪽 5개 위치에 항등 행렬이 있으므로, $P$는 $X$와 $Z$ 모두와 교환 가능해야 합니다. 그렇지 않으면 $E$가 두 생성원 중 하나와 반교환 관계가 되기 때문입니다.
그런데 $X$와 $Z$ 모두와 교환 가능한 파울리 행렬은 항등 행렬뿐이므로, $P = \mathbb{I}$입니다.

이를 알고 나면, 왼쪽에서 두 번째 위치에 $X$와 $Z$를 갖는 두 개의 스태빌라이저 생성원을 선택하여 유사한 결론을 도출할 수 있습니다: $Q = \mathbb{I}$입니다.
따라서 $E$는 항등 연산입니다.

즉, 가중치가 최대 2인 오류가 항등 연산이 아닌 이상 이 코드에서 감지되지 않을 방법은 없습니다 (항등 연산은 스태빌라이저에 속하므로 실제 오류가 아닙니다).
반면에, 여섯 개의 스태빌라이저 생성원 모두와 교환 가능하지만 스태빌라이저 원소에 비례하지 않는 가중치 3의 파울리 연산들이 존재합니다. 예를 들어 $\mathbb{I}\otimes\mathbb{I}\otimes\mathbb{I}\otimes\mathbb{I}\otimes X\otimes X\otimes X$
및 $\mathbb{I}\otimes\mathbb{I}\otimes\mathbb{I}\otimes\mathbb{I}\otimes Z\otimes Z\otimes Z$가 그런 경우입니다.
이로써 이 코드의 거리가 3임이 증명됩니다.

## 오류 정정 {#correcting-errors}

이 레슨의 마지막 주제는 스태빌라이저 코드의 오류 *정정*입니다.
이전과 마찬가지로 $n$-Qubit 스태빌라이저 생성원 $P_1, \ldots, P_r$로 지정된 스태빌라이저 코드가 있다고 가정합니다.

$n$-Qubit 파울리 연산들은, 이 코드를 사용해 인코딩된 상태에 영향을 미칠 수 있는 오류로서, 어떤 신드롬을 일으키는지에 따라 동일한 크기의 집합으로 분할됩니다.
신드롬의 종류는 $2^r$가지이고 파울리 연산은 $4^n$개이므로, 각 신드롬을 일으키는 파울리 연산은 $4^n/2^r$개가 됩니다.
해당 신드롬에 대해서는 이 오류들 중 어느 것이라도 원인일 수 있습니다.

하지만 각 신드롬을 일으키는 $4^n/2^r$개의 파울리 연산 중에는 동등하다고 봐야 하는 것들이 있습니다.
특히 두 파울리 연산의 곱이 스태빌라이저 원소에 비례한다면, 그 두 연산은 오류로서 사실상 동등합니다.

달리 말하면, 오류 $E$를 정정하기 위해 정정 연산 $C$를 적용할 때, 합성 연산 $CE$가 스태빌라이저 원소에 비례하면 정정이 성공합니다.
스태빌라이저에 $2^r$개의 원소가 있으므로, 각 정정 연산 $C$는 $2^r$개의 서로 다른 파울리 오류를 정정합니다.
이렇게 하면 각 가능한 신드롬과 일치하는 파울리 연산의 동치류가 $4^{n-r}$개 남습니다.

이는 $n=r$인 경우 (이때는 1차원의 자명한 코드 공간을 갖는 경우)를 제외하고는, 스태빌라이저 코드로 감지된 모든 오류를 정정하는 것은 불가능하다는 뜻입니다.
따라서 각 신드롬에 대해 *하나*의 정정 연산만을 선택하여, 해당 신드롬을 일으키는 동치류 중 하나만 정정하는 것을 목표로 해야 합니다.

각 신드롬에 대해 어떤 정정 연산을 수행할지 선택하는 자연스러운 전략은, 해당 신드롬을 일으키는 오류 중 *가장 낮은 가중치*를 가진 파울리 연산을 선택하는 것입니다.
실제로 주어진 신드롬과 일치하는 최저 가중치 오류가 여러 개일 수 있으며, 이 경우에는 그 중 하나를 선택하면 됩니다.
낮은 가중치의 파울리 연산일수록 측정된 신드롬에 대한 더 그럴듯한 설명이 된다는 생각에 기반합니다.
물론 일부 잡음 모델에서는 이것이 사실이 아닐 수 있으며, 선택된 잡음 모델을 기반으로 주어진 신드롬을 일으키는 가장 *가능성 높은* 오류를 계산하는 대안적인 전략도 있습니다.
하지만 이 레슨에서는 단순하게 최저 가중치 정정만 고려하겠습니다.

거리 $d$의 스태빌라이저 코드에서, 측정된 신드롬과 일치하는 최저 가중치 파울리 연산을 정정 연산으로 선택하는 전략은 $d/2$보다 엄격하게 작은 가중치, 즉 가중치가 최대 $(d-1)/2$인 오류를 항상 정정할 수 있습니다.
이를 통해 예를 들어 7-Qubit 스타인 코드가 임의의 가중치-1 파울리 오류를 정정할 수 있으며, 오류의 이산화에 의해 스타인 코드가 하나의 Qubit에 대한 임의의 오류를 정정할 수 있음을 알 수 있습니다.

이것이 어떻게 작동하는지 아래 다이어그램을 통해 살펴보겠습니다.
왼쪽 원은 신드롬 $(+1,\ldots,+1)$을 일으키는 모든 파울리 연산을 나타냅니다. 이는 오류가 발생하지 않아 아무 문제없음을 나타내는 신드롬입니다.
이 연산들 중에는 스태빌라이저의 원소 (또는 더 정확히는 스태빌라이저 원소에 *비례*하는 연산)도 있고, 코드 공간을 어떤 방식으로 변화시키지만 코드에 의해 감지되지 않는 비자명한 오류들도 있습니다.
거리의 정의에 따라 이 범주의 모든 파울리 연산은 가중치가 최소 $d$이어야 하는데, 이는 $d$가 이 연산들의 최소 가중치로 정의되기 때문입니다.

오른쪽 원은 서로 다른 신드롬 $s\neq(+1,\ldots,+1)$을 일으키는 파울리 연산들을 나타내며, 가중치가 $d/2$보다 엄격하게 작은 오류 $E$를 포함합니다.

![Lowest-weight error correction diagram](/learning/images/courses/foundations-of-quantum-error-correction/stabilizer-formalism/lowest-weight-correction.svg)

신드롬 $s$에 대해 선택된 정정 연산 $C$는 다이어그램의 오른쪽 원으로 표현된 집합에서 가장 낮은 가중치를 가진 파울리 연산입니다 (동점이면 그 중 하나를 선택합니다).
따라서 $C = E$일 수도 있지만, 반드시 그럴 필요는 없습니다.
하지만 확실히 말할 수 있는 것은, $C$는 $E$의 가중치보다 클 수 없다는 점입니다. $C$는 이 집합에서 최소 가중치를 가지기 때문입니다 — 따라서 $C$의 가중치는 $d/2$보다 엄격하게 작습니다.

이제 오류 $E$가 발생한 후 얻은 상태에 정정 연산 $C$를 적용할 때 어떤 일이 일어나는지 살펴보겠습니다.
원래 인코딩이 $\vert\psi\rangle$였다고 가정하면, 적용 후 $CE\vert\psi\rangle$가 남습니다.
목표는 $CE$가 스태빌라이저의 원소에 비례함을 보이는 것으로, 이는 정정이 성공하여 (전역 위상을 제외하고) 원래 인코딩된 상태 $\vert\psi\rangle$가 남는다는 것을 의미합니다.

첫째, $E$와 $C$가 동일한 신드롬을 일으키므로, 합성 연산 $CE$는 모든 스태빌라이저 생성원과 교환 가능해야 합니다.
특히 $P_k$가 스태빌라이저 생성원 중 임의의 하나라면, 다음이 성립해야 합니다:

$$
P_k E = \alpha E P_k
\quad\text{and}\quad
P_k C = \alpha C P_k
$$

동일한 값 $\alpha\in\{+1,-1\}$에 대해 이 식이 성립하는데, 이는 $C$와 $E$ 모두가 생성하는 신드롬 $s$의 $k$번째 항목이기 때문입니다.
따라서 다음이 성립합니다:

$$
P_k (CE) = \alpha C P_k E = \alpha^2 (CE) P_k = (CE) P_k,
$$

즉 $P_k$는 $CE$와 교환 가능합니다.
이로써 $CE$는 신드롬 $(+1,\ldots,+1)$을 생성하므로 다이어그램의 왼쪽 원에 속함을 보였습니다.

둘째, 합성 연산 $CE$의 가중치는 $C$와 $E$의 가중치의 합을 초과할 수 없으며 — 이는 파울리 연산의 곱에 대해 잠시 생각해보면 알 수 있습니다 — 따라서 $CE$의 가중치는 $d$보다 엄격하게 작습니다.
이는 $CE$가 우리 코드의 스태빌라이저 원소에 비례한다는 것을 의미하며, 이것이 우리가 보이고자 했던 것입니다.
각 신드롬을 일으키는 오류 집합의 최저 가중치 대표를 정정 연산으로 선택함으로써, 코드의 거리의 절반보다 작은 가중치를 가진 임의의 파울리 오류를 정정할 수 있음이 보장됩니다.

하지만 한 가지 문제가 있습니다.
일반적인 스태빌라이저 코드에서 주어진 신드롬을 일으키는 최저 가중치 파울리 연산을 계산하는 것은 계산적으로 어려운 문제입니다.
(실제로 이는 고전 코드의 경우에도 마찬가지인데, 이 맥락에서 고전 코드는 스태빌라이저 생성원 내의 텐서 인수로 $\mathbb{I}$와 $Z$ 행렬만 등장하는 스태빌라이저 코드로 생각할 수 있습니다.)
따라서 인코딩 단계와 달리, 이번에는 Clifford 연산이 도움이 되지 않습니다.

해결책은 좋은 정정 연산을 효율적으로 계산할 수 있는 특정 코드를 선택하는 것인데, 이를 위한 간단한 방법은 없습니다.
간단히 말해, 좋은 정정 연산을 효율적으로 계산할 수 있는 스태빌라이저 코드를 고안하는 것은 양자 코드 설계의 예술적인 부분입니다.
다음 레슨에서 이 예술성이 어떻게 발휘되는지 살펴볼 것입니다.
