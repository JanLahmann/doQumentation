---
title: تثبيت Qiskit SDK من المصدر
description: تعلّم كيف تثبّت نسخة التطوير من Qiskit.

---

{/* doqumentation-source-hash: 3602a588 */}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


{/* cspell:ignore rustup */}

# تثبيت Qiskit SDK و Qiskit Runtime من المصدر

يتيح لك تثبيت Qiskit SDK من المصدر الوصول إلى نسخة التطوير الحالية، بدلاً من استخدام النسخة المتاحة في مستودع Python Package Index (PyPI). هذا يمكّنك من فحص أحدث إصدار من كود Qiskit وتوسيعه بكفاءة أكبر.

## إنشاء بيئة افتراضية جديدة وتفعيلها {#create-and-activate-a-new-virtual-environment}

1. انتقل إلى مجلد مشروعك وأنشئ بيئة بسيطة تحتوي على Python فقط.

        <OperatingSystemTabs>
          <TabItem value="mac" label="macOS">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="linux" label="Linux">
            ```shell
            python3 -m venv .venv
            ```
          </TabItem>

          <TabItem value="win" label="Windows">
            ```text
            python -m venv .venv
            ```
          </TabItem>
         </OperatingSystemTabs>

1. فعّل البيئة الجديدة.

         <OperatingSystemTabs>
           <TabItem value="mac" label="macOS">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="linux" label="Linux">
             ```shell
             source .venv/bin/activate
             ```
           </TabItem>

           <TabItem value="win" label="Windows">
             ```text
             .venv\Scripts\Activate.ps1
             ```
           </TabItem>
         </OperatingSystemTabs>

## تثبيت مترجم Rust {#install-the-rust-compiler}

يجب أن يكون مترجم Rust مثبّتاً على نظامك لتجميع Qiskit. لتثبيت مترجم Rust، استخدم أداة التثبيت متعددة المنصات [`rustup`](https://rustup.rs/) أو [طريقة تثبيت أخرى.](https://forge.rust-lang.org/infra/other-installation-methods.html)

## تثبيت Qiskit {#install-qiskit}

اتبع هذه الخطوات لتثبيت Qiskit:

1. استنسخ مستودع Qiskit.

```bash
git clone https://github.com/Qiskit/qiskit.git
```

2. انتقل إلى مجلد `qiskit`.

```bash
cd qiskit
```

3. (اختياري) إذا كنت تريد تشغيل الاختبارات أو فحوصات linting، ثبّت متطلبات المطوّر.

```bash
pip install -r requirements-dev.txt
```

4. ثبّت `qiskit`.

  * **التثبيت العادي**:

    ```bash
    pip install .
    ```

  * **وضع التعديل المباشر (Editable mode)**: في هذا الوضع لا تحتاج إلى إعادة تثبيت Qiskit عند إجراء تغييرات على كود المشروع.

    ```bash
    pip install -e .
    ```

    في وضع التعديل المباشر، تُبنى الامتدادات المُجمَّعة في _وضع التصحيح (debug mode)_ بدون تحسينات. هذا يؤثر على أداء وقت التشغيل للكود المُجمَّع. لبناء الامتدادات المُجمَّعة مع تفعيل التحسينات، شغّل الأمر التالي لإعادة بناء الملف الثنائي في _وضع الإصدار (release mode)_:

    ```bash
    python setup.py build_rust --release --inplace
    ```

    <Admonition type="note">
    إذا كنت تعمل على كود Rust في Qiskit، فستحتاج إلى إعادة بناء كود الامتداد في كل مرة تُجري فيها تغييراً محلياً. في وضع التعديل المباشر، يُبنى امتداد Rust فقط عند تشغيل أمر التثبيت، لذا فإن التغييرات المحلية التي تجريها على كود Rust لن تنعكس في الحزمة المثبّتة ما لم تُعد بناء الامتداد بتشغيل `build_rust` من جديد (مع `--release` أو بدونها، حسب ما إذا كنت تريد البناء في وضع الإصدار أو وضع التصحيح).
    </Admonition>

## تثبيت Qiskit Runtime {#install-qiskit-runtime}

اتبع هذه الخطوات إذا كنت تريد تثبيت Qiskit Runtime:

1. استنسخ مستودع Qiskit Runtime.

```bash
git clone https://github.com/Qiskit/qiskit-ibm-runtime.git
```

2. انتقل إلى مجلد `qiskit-ibm-runtime`.

```bash
cd qiskit-ibm-runtime
```

3. ثبّت `qiskit-runtime`. نوصي باستخدام [بيئة افتراضية](https://docs.python.org/3/library/venv.html) لتجنّب التأثير على تثبيت Python العام.

  * **التثبيت العادي**:

    ```bash
    pip install .
    ```

  * **وضع التعديل المباشر (Editable mode)**: في هذا الوضع لا تحتاج إلى إعادة تثبيت Qiskit عند إجراء تغييرات على كود المشروع.

    ```bash
    pip install -e .
    ```

    في وضع التعديل المباشر، تُبنى الامتدادات المُجمَّعة في _وضع التصحيح (debug mode)_ بدون تحسينات.

4. (اختياري) إذا كنت تريد تشغيل الاختبارات أو فحوصات linting، ثبّت متطلبات المطوّر. نوصي باستخدام [بيئة افتراضية](https://docs.python.org/3/library/venv.html) لتجنّب التأثير على تثبيت Python العام.

```bash
pip install -e ".[dev]"
```

## الخطوات التالية {#next-steps}

<Admonition type="tip" title="توصيات">
  - اقرأ [إرشادات المساهمة](https://github.com/Qiskit/qiskit/blob/main/CONTRIBUTING.md) للمساهمة في Qiskit SDK مفتوح المصدر.
  - [شغّل برنامج Hello world](/guides/hello-world).
  - جرّب أحد الدروس التعليمية، مثل [خوارزمية Grover](/tutorials/grovers-algorithm).
</Admonition>
