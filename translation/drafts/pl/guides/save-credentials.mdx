---
title: Zapisywanie danych logowania
description: Zapisuj swoje dane logowania zamiast wprowadzać je ręcznie za każdym razem
---

<!-- hash:a870ce51 -->

# Zapisywanie danych logowania {#save-your-login-credentials}
Jeśli pracujesz w zaufanym środowisku Python (np. na własnym laptopie lub stacji roboczej), możesz użyć metody `save_account()`, aby zapisać swoje dane logowania lokalnie, a następnie użyć ich do inicjalizacji usługi.

<Admonition type="note" title="Uwagi">
      * Jeśli używasz komputera publicznego lub innego niezaufanego środowiska, zamiast tego postępuj zgodnie z instrukcjami w [Initialize the service in an untrusted environment](/guides/cloud-setup-untrusted).
      * Postępuj zgodnie z [tymi instrukcjami](/guides/cloud-setup-rest-api), jeśli chcesz połączyć się za pomocą REST API zamiast Qiskit.
      * W razie potrzeby użyj [tych informacji](/guides/quickstart-steps-org#firewall), aby skonfigurować zaporę sieciową umożliwiającą dostęp do punktów końcowych API IBM Quantum.
</Admonition>

## Przed rozpoczęciem {#before-you-begin}

1. Upewnij się, że masz [konto IBM Cloud.](https://quantum.cloud.ibm.com/registration)
1. Upewnij się, że pracujesz w aktywnym środowisku Python z [zainstalowanym Qiskit SDK i Qiskit Runtime](/guides/install-qiskit#local).
1. Aktywuj wirtualne środowisko Python i uruchom Pythona w swoim wirtualnym środowisku.
1. Zaloguj się do [IBM Quantum Platform](https://quantum.cloud.ibm.com) za pomocą konta IBMid lub Google.

<span id="find-credentials"></span>
## Znajdowanie danych dostępowych {#find-your-access-credentials}

1. Upewnij się, że właściwe konto i region są wybrane w przełączniku kont w nagłówku.
1. Znajdź swój klucz API. Z [pulpitu nawigacyjnego](https://quantum.cloud.ibm.com/) utwórz klucz API, a następnie skopiuj go w bezpieczne miejsce, abyś mógł go użyć do uwierzytelnienia. Pamiętaj, że tego samego klucza API możesz użyć do połączenia z dowolnym regionem.
1. Opcjonalnie: Znajdź instancję, której chcesz użyć, na stronie [Instances](https://quantum.cloud.ibm.com/instances). Najedź kursorem na jej CRN, kliknij ikonę kopiowania, a następnie zapisz go w bezpiecznym miejscu, abyś mógł go użyć do identyfikacji instancji.

<span id="cloud-save"></span>
## Zapisywanie danych dostępowych {#save-your-access-credentials}

Zapisz swoje dane logowania, uruchamiając odpowiedni kod raz dla każdego konta, które chcesz zapisać. Po zapisaniu danych logowania załaduj je, postępując zgodnie z instrukcjami w [Initialize the Qiskit Runtime service.](/guides/initialize-account)

<span id="save-instance"></span>
### Zapisywanie danych logowania z dostępem do konkretnej instancji: {#save-credentials-that-access-a-specific-instance}
Jeśli masz wiele instancji i chcesz łatwo poinformować Qiskit Runtime, której instancji użyć, zapisz dane logowania zawierające CRN instancji.

    ```python
    from qiskit_ibm_runtime import QiskitRuntimeService

    QiskitRuntimeService.save_account(
      token="<your-api-key>", # Use the 44-character API_KEY you created and saved from the IBM Quantum Platform Home dashboard
      name="<account-name>", # Optional
      instance="<IBM Cloud CRN or instance name>", # Optional
      set_as_default=True, # Optional
      overwrite=True, # Optional
    )
    ```

<span id="auto-select"></span>
### Zapisywanie danych logowania do automatycznego wyboru instancji: {#save-credentials-for-automatic-instance-selection}
Jeśli nie podasz CRN instancji i przekażesz te dane logowania do Qiskit Runtime, odpowiednia instancja zostanie wybrana automatycznie na podstawie podanych opcji.

    ```python
    from qiskit_ibm_runtime import QiskitRuntimeService

    QiskitRuntimeService.save_account(
      token="<your=api-key>", # Use the 44-character API_KEY you created and saved from the IBM Quantum Platform Home dashboard
      name="<account-name>", # Optional
      instance="<instance-to-use>", # Optional
      plans_preference="<plan_types>", # Optional
      region="<region>", # Optional
      tags="<instance-tags>", # Optional
      set_as_default=True, # Optional
      overwrite=True, # Optional
    )
   ```

### Dostępne opcje {#available-options}

- **`token`**: Klucz API IBM Cloud. Twój token jest poufny. Nie udostępniaj tokenu w publicznym kodzie.
- **`instance`**: Opcjonalnie podaj instancję do użycia przez jej IBM Cloud CRN lub nazwę instancji.
- **`plans_preference`**: Opcjonalnie ustaw typy planów do priorytetyzowania. Jest ignorowane, jeśli instancja jest określona. Dostępne opcje to `open`, `pay-as-you-go`, `flex`, `premium` i `on-prem`. Instancje określonego typu planu są wykluczane, jeśli nazwa planu nie jest podana. Na przykład, jeśli podany zostanie [`open`], dostępne będą tylko instancje planu Open. Jest ignorowane, jeśli `instance` jest podane.
- **`region`**: Opcjonalnie ustaw region do użycia. Akceptowane wartości to `us-east` i `eu-de`. Jest ignorowane, jeśli `instance` jest podane.
- **`tags`**: Opcjonalnie podaj tagi instancji. Przyjmuje listę ciągów nazw tagów. Jest ignorowane, jeśli `instance` jest podane.
- **`name`**: Opcjonalnie nadaj nazwę temu zestawowi danych logowania konta.
- **`set_as_default`**: Ustaw wartość na `True`, aby zapisać te dane jako domyślne. Jeśli zapiszesz tylko jedno konto, zostanie ono automatycznie ustawione jako domyślne.
- **`overwrite`**: Ustaw tę wartość na `True`, aby zaktualizować swoje domyślne dane logowania.

## Przykłady {#examples}

### Przykład 1 {#example-1}
Ten przykład zapisuje dane logowania dla konkretnych instancji, które umożliwiają dostęp w ramach planów open i premium. Dane logowania open są ustawione jako domyślne.

```python
from qiskit_ibm_runtime import QiskitRuntimeService

QiskitRuntimeService.save_account(token="<API_TOKEN>", instance="<CRN_for_premium_instance>",
                                  name="premium")
QiskitRuntimeService.save_account(token="<API_TOKEN>", instance="<CRN_for_open_instance>",
                                  name="open", set_as_default=True)
 ```

### Przykład 2 {#example-2}
Ten przykład zapisuje dane logowania do automatycznego wyboru instancji. Qiskit Runtime będzie szukać tylko instancji premium w regionie UE dostępnych dla konta. Te dane logowania stają się nowym domyślnym.

```python
QiskitRuntimeService.save_account(
    token="your-api-key",
    set_as_default=True,
    overwrite = True,

    # Set instance "filters" instead of specifying the instance:
    region="eu-de",
    plans_preference=["premium"]
  )
 ```

## Wyświetlanie zapisanych danych logowania {#view-saved-credentials}

Aby wyświetlić wszystkie zapisane dane logowania, uruchom `service.saved_accounts()`. Pamiętaj, że jeśli zapisałeś domyślne dane logowania bez ich nazywania, noszą one nazwę `default-ibm-quantum-platform`. Jeśli nadałeś swojemu domyślnemu nazwy logowania, zobaczysz `"is_default_account": true` w wynikach.

## Uwagi {#considerations}

* Jeśli zapisujesz wiele kont, użyj parametru `name`, aby je rozróżnić.
* Dane logowania są zapisywane do `$HOME/.qiskit/qiskit-ibm.json`. Nie edytuj tego pliku ręcznie.
* Jeśli nie zapiszesz swoich danych logowania, musisz je podawać za każdym razem, gdy rozpoczynasz nową sesję (tworzysz instancję usługi Qiskit Runtime).
* Jeśli ręcznie podasz swoje dane logowania, zapisane konto nie zostanie użyte.

# Następne kroki {#next-steps}

<Admonition type="tip" title="Rekomendacje">
  -  [Zainicjalizuj usługę Qiskit Runtime](/guides/initialize-account) w zaufanym środowisku Python.
  -  [Zainicjalizuj usługę Qiskit Runtime w **niezaufanym środowisku**.](/guides/cloud-setup-untrusted)
  -  [Wyświetl dostępne QPU.](/guides/qpu-information#available)
  -  [Skonfiguruj Qiskit SDK lokalnie.](/guides/configure-qiskit-local)
  -  Postępuj zgodnie z instrukcjami w [Hello world](/guides/hello-world), aby napisać i uruchomić program kwantowy.
  -  [Skonfiguruj połączenie z IBM Quantum Platform za pomocą REST API.](/guides/cloud-setup-rest-api)
  -  Wypróbuj [samouczek.](https://docs.quantum.ibm.com/tutorials)
</Admonition>
