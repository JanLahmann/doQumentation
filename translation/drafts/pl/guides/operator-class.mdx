---
title: "Klasa Operator"
sidebar_label: "Klasa Operator"
description: "Szczegółowe omówienie użycia klasy Operator w Qiskit."
notebook_path: "guides/operator-class.ipynb"
---

{/* doqumentation-source-hash: f33a6b98 */}

<OpenInLabBanner notebookPath="guides/operator-class.ipynb" />



{/*
  DO NOT EDIT THIS CELL!!!
  This cell's content is generated automatically by a script. Anything you add
  here will be removed next time the notebook is run. To add new content, create
  a new cell before or after this one.
*/}

<details>
<summary><b>Wersje pakietów</b></summary>

Kod na tej stronie został opracowany przy użyciu poniższych wymagań.
Zalecamy korzystanie z tych lub nowszych wersji.

```
qiskit[all]~=2.3.0
```
</details>
Ta strona pokazuje, jak używać klasy [`Operator`](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator). Ogólne omówienie reprezentacji operatorów w Qiskit, w tym klasy `Operator` i innych, znajdziesz w artykule [Przegląd klas operatorów](./operators-overview).

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit
```

```python
import numpy as np
from qiskit.circuit import QuantumCircuit
from qiskit.circuit.library import CXGate, RXGate, XGate
from qiskit.quantum_info import Operator, Pauli, process_fidelity
```

## Konwertowanie klas na obiekty Operator {#convert-classes-to-operators}

Kilka innych klas w Qiskit można bezpośrednio przekonwertować na obiekt `Operator` za pomocą metody inicjalizacji operatora. Na przykład:

* Obiekty `Pauli`
* Obiekty `Gate` i `Instruction`
* Obiekty `QuantumCircuit`

Warto zauważyć, że ostatni punkt oznacza, że możesz używać klasy `Operator` jako symulatora unitarnego do obliczania końcowej macierzy unitarnej dla Circuit, bez konieczności wywoływania Backend symulatora. Jeśli Circuit zawiera nieobsługiwane operacje, zostanie zgłoszony wyjątek. Nieobsługiwane operacje to: pomiar, reset, operacje warunkowe lub Gate, która nie posiada definicji macierzowej ani rozkładu na Gate z definicjami macierzowymi.

```python
# Create an Operator from a Pauli object

pauliXX = Pauli("XX")
Operator(pauliXX)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j],
          [1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an Operator for a Gate object
Operator(CXGate())
```

```text
Operator([[1.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 1.+0.j],
          [0.+0.j, 0.+0.j, 1.+0.j, 0.+0.j],
          [0.+0.j, 1.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

```python
# Create an operator from a parameterized Gate object
Operator(RXGate(np.pi / 2))
```

```text
Operator([[0.70710678+0.j        , 0.        -0.70710678j],
          [0.        -0.70710678j, 0.70710678+0.j        ]],
         input_dims=(2,), output_dims=(2,))
```

```python
# Create an operator from a QuantumCircuit object
circ = QuantumCircuit(10)
circ.h(0)
for j in range(1, 10):
    circ.cx(j - 1, j)

# Convert circuit to an operator by implicit unitary simulation
Operator(circ)
```

```text
Operator([[ 0.70710678+0.j,  0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          ...,
          [ 0.        +0.j,  0.        +0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.        +0.j,  0.        +0.j,  0.70710678+0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j],
          [ 0.70710678+0.j, -0.70710678+0.j,  0.        +0.j, ...,
            0.        +0.j,  0.        +0.j,  0.        +0.j]],
         input_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2), output_dims=(2, 2, 2, 2, 2, 2, 2, 2, 2, 2))
```

## Używanie obiektów Operator w Circuit {#use-operators-in-circuits}

Unitarne obiekty `Operator` można bezpośrednio wstawiać do `QuantumCircuit` za pomocą metody `QuantumCircuit.append`. Konwertuje to `Operator` na obiekt `UnitaryGate`, który jest dodawany do Circuit.

Jeśli operator nie jest unitarny, zostanie zgłoszony wyjątek. Można to sprawdzić funkcją `Operator.is_unitary()`, która zwraca `True`, gdy operator jest unitarny, i `False` w przeciwnym razie.

```python
# Create an operator
XX = Operator(Pauli("XX"))

# Add to a circuit
circ = QuantumCircuit(2, 2)
circ.append(XX, [0, 1])
circ.measure([0, 1], [0, 1])
circ.draw("mpl")
```

![Output of the previous code cell](/docs/images/guides/operator-class/extracted-outputs/223c07c9-1b1d-4a33-a397-091cac2ec9e3-0.svg)

Zauważ, że w powyższym przykładzie operator jest inicjalizowany z obiektu `Pauli`. Jednak obiekt `Pauli` można też wstawić bezpośrednio do Circuit, gdzie zostanie przekonwertowany na sekwencję jednoQubitowych bramek Pauli:

```python
# Add to a circuit
circ2 = QuantumCircuit(2, 2)
circ2.append(Pauli("XX"), [0, 1])
circ2.measure([0, 1], [0, 1])
circ2.draw()
```

```text
┌────────────┐┌─┐   
q_0: ┤0           ├┤M├───
     │  Pauli(XX) │└╥┘┌─┐
q_1: ┤1           ├─╫─┤M├
     └────────────┘ ║ └╥┘
c: 2/═══════════════╩══╩═
                    0  1
```

## Łączenie obiektów Operator {#combine-operators}

Operatory można łączyć na kilka sposobów.

### Iloczyn tensorowy {#tensor-product}

Dwa operatory $A$ i $B$ można połączyć w operator iloczynu tensorowego $A\otimes B$ przy użyciu funkcji `Operator.tensor`. Jeśli oba $A$ i $B$ są operatorami jednoQubitowymi, to `A.tensor(B)` = $A\otimes B$ będzie miało podsystemy zaindeksowane tak, że macierz $B$ odpowiada podsystemowi 0, a macierz $A$ podsystemowi 1.

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.tensor(B)
```

```text
Operator([[ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j],
          [ 0.+0.j, -0.+0.j,  0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

### Rozwinięcie tensorowe {#tensor-expansion}

Ściśle powiązaną operacją jest `Operator.expand`, która działa jak iloczyn tensorowy, ale w odwrotnej kolejności. Dla dwóch operatorów $A$ i $B$ mamy `A.expand(B)` = $B\otimes A$, gdzie podsystemy są zaindeksowane tak, że macierz $A$ odpowiada podsystemowi 0, a macierz $B$ podsystemowi 1.

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.expand(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j],
          [ 0.+0.j,  0.+0.j, -0.+0.j, -1.+0.j],
          [ 0.+0.j,  0.+0.j, -1.+0.j, -0.+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```
### Składanie {#composition}

Możesz również złożyć dwa operatory $A$ i $B$, aby zrealizować mnożenie macierzy, korzystając z metody `Operator.compose`. `A.compose(B)` zwraca operator z macierzą $B.A$:

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B)
```

```text
Operator([[ 0.+0.j,  1.+0.j],
          [-1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

Możesz też złożyć w odwrotnej kolejności, stosując $B$ przed $A$, używając argumentu `front` metody `compose`: `A.compose(B, front=True)` = $A.B$:

```python
A = Operator(Pauli("X"))
B = Operator(Pauli("Z"))
A.compose(B, front=True)
```

```text
Operator([[ 0.+0.j, -1.+0.j],
          [ 1.+0.j,  0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

### Składanie podsystemów {#subsystem-composition}

Zauważ, że poprzednie składanie wymaga, aby całkowity wymiar wyjściowy pierwszego operatora $A$ był równy całkowitemu wymiarowi wejściowemu składanego operatora $B$ (analogicznie, wymiar wyjściowy $B$ musi być równy wymiarowi wejściowemu $A$ przy składaniu z `front=True`).

Możesz też złożyć mniejszy operator z wybranym podzbiorem podsystemów większego operatora, używając argumentu `qargs` metody `compose`, zarówno z `front=True`, jak i bez niego. W takim przypadku odpowiednie wymiary wejściowe i wyjściowe składanych podsystemów muszą być zgodne. *Pamiętaj, że mniejszy operator musi zawsze być argumentem metody* `compose`*.*

Na przykład, aby złożyć bramkę dwu-qubitową z operatorem trzy-qubitowym:

```python
# Compose XZ with a 3-qubit identity operator
op = Operator(np.eye(2**3))
XZ = Operator(Pauli("XZ"))
op.compose(XZ, qargs=[0, 2])
```

```text
Operator([[ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  1.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
           -1.+0.j],
          [ 1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j],
          [ 0.+0.j,  0.+0.j,  0.+0.j, -1.+0.j,  0.+0.j,  0.+0.j,  0.+0.j,
            0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

```python
# Compose YX in front of the previous operator
op = Operator(np.eye(2**3))
YX = Operator(Pauli("YX"))
op.compose(YX, qargs=[0, 2], front=True)
```

```text
Operator([[0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.-1.j, 0.+0.j],
          [0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j],
          [0.+0.j, 0.+0.j, 0.+1.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j, 0.+0.j]],
         input_dims=(2, 2, 2), output_dims=(2, 2, 2))
```

### Kombinacje liniowe {#linear-combinations}

Operatory można też łączyć za pomocą standardowych operacji liniowych: dodawania, odejmowania i mnożenia skalarnego przez liczby zespolone.

```python
XX = Operator(Pauli("XX"))
YY = Operator(Pauli("YY"))
ZZ = Operator(Pauli("ZZ"))

op = 0.5 * (XX + YY - 3 * ZZ)
op
```

```text
Operator([[-1.5+0.j,  0. +0.j,  0. +0.j,  0. +0.j],
          [ 0. +0.j,  1.5+0.j,  1. +0.j,  0. +0.j],
          [ 0. +0.j,  1. +0.j,  1.5+0.j,  0. +0.j],
          [ 0. +0.j,  0. +0.j,  0. +0.j, -1.5+0.j]],
         input_dims=(2, 2), output_dims=(2, 2))
```

Ważna uwaga: o ile `tensor`, `expand` i `compose` zachowują unitarność operatorów unitarnych, kombinacje liniowe jej nie zachowują — w ogólności suma dwóch operatorów unitarnych daje operator nieunitarny:

```python
op.is_unitary()
```

```text
False
```

### Niejawna konwersja do operatorów {#implicit-conversion-to-operators}

Zwróć uwagę, że we wszystkich poniższych metodach, jeśli drugi obiekt nie jest jeszcze obiektem `Operator`, jest on niejawnie konwertowany przez metodę. Oznacza to, że macierze można przekazywać bezpośrednio, bez wcześniejszej jawnej konwersji do `Operator`. Jeśli konwersja nie jest możliwa, zgłaszany jest wyjątek.

```python
# Compose with a matrix passed as a list
Operator(np.eye(2)).compose([[0, 1], [1, 0]])
```

```text
Operator([[0.+0.j, 1.+0.j],
          [1.+0.j, 0.+0.j]],
         input_dims=(2,), output_dims=(2,))
```

## Porównywanie operatorów {#compare-operators}

Operatory implementują metodę równości, której można użyć do sprawdzenia, czy dwa operatory są w przybliżeniu równe.

```python
Operator(Pauli("X")) == Operator(XGate())
```

```text
True
```

Pamiętaj, że porównanie sprawdza, czy każdy element macierzy operatorów jest w przybliżeniu równy; dwa operatory unitarne różniące się globalną fazą nie są uznawane za równe:

```python
Operator(XGate()) == np.exp(1j * 0.5) * Operator(XGate())
```

```text
False
```

### Wierność procesu {#process-fidelity}

Możesz też porównywać operatory za pomocą funkcji `process_fidelity` z modułu *Quantum Information*. Jest to wielkość teorioiformacyjna określająca, jak bliskie sobie są dwa kanały kwantowe; w przypadku operatorów unitarnych nie zależy od globalnej fazy.

```python
# Two operators which differ only by phase
op_a = Operator(XGate())
op_b = np.exp(1j * 0.5) * Operator(XGate())

# Compute process fidelity
F = process_fidelity(op_a, op_b)
print("Process fidelity =", F)
```

```text
Process fidelity = 1.0
```

Pamiętaj, że wierność procesu jest ogólnie poprawną miarą bliskości tylko wtedy, gdy operatory wejściowe są unitarne (lub CP w przypadku kanałów kwantowych); wyjątek jest zgłaszany, jeśli dane wejściowe nie są CP.

## Następne kroki {#next-steps}

<Admonition type="tip" title="Rekomendacje">
  - Zapoznaj się z dokumentacją referencyjną [Operator API](https://docs.quantum.ibm.com/api/qiskit/qiskit.quantum_info.Operator#operator).
</Admonition>
