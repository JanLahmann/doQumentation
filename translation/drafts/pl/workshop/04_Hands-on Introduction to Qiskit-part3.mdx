## 2.3 Wykonanie przy użyciu kwantowych primitives

Komputery kwantowe mogą dawać losowe wyniki, dlatego zazwyczaj zbierasz próbkę wyjść, uruchamiając Circuit wiele razy. Możesz oszacować wartość obserwabli, korzystając z klasy `Estimator`. `Estimator` to jeden z dwóch [primitives](https://docs.quantum.ibm.com/guides/get-started-with-primitives); drugim jest `Sampler`, który służy do pobierania danych z komputera kwantowego. Obiekty te posiadają metodę `run()`, która wykonuje wybrany zestaw Circuit, obserwabli i parametrów (jeśli dotyczy), korzystając z [primitive unified bloc (PUB).](https://docs.quantum.ibm.com/guides/primitives#sampler)
Jeśli uruchamiasz ten kod na prawdziwym sprzęcie kwantowym, rozważ zastosowanie [technik mitygacji i tłumienia błędów](https://quantum.cloud.ibm.com/docs/en/guides/error-mitigation-and-suppression-techniques), aby zredukować szumy wewnętrzne komputera kwantowego.

```python
# Konstruowanie instancji Estimator.
estimator = Estimator(mode=backend)
estimator.options.resilience_level = 1
estimator.options.default_shots = 5000
```

Prześlij zadanie przy użyciu primitiva Estimator.

```python
# Jeden PUB z jednym Circuit uruchamianym względem sześciu różnych obserwabli.
job = estimator.run([(isa_circuit, mapped_observables)]) 

# Użyj ID zadania, aby później pobrać dane
print(f">>> Job ID: {job.job_id()}")
```

```text
>>> Job ID: 97ecd036-1767-49b0-a1dc-c71638c3c3c4
```

```text
/Users/jma/miniconda3/envs/3122/lib/python3.12/site-packages/qiskit_ibm_runtime/fake_provider/local_service.py:187: UserWarning: The resilience_level option has no effect in local testing mode.
  warnings.warn("The resilience_level option has no effect in local testing mode.")
```

Po przesłaniu zadania możesz poczekać na jego zakończenie w bieżącej instancji Pythona lub użyć `job_id`, aby pobrać dane później. (Szczegóły znajdziesz w [sekcji dotyczącej pobierania zadań](https://docs.quantum.ibm.com/guides/monitor-job#retrieve-job-results-at-a-later-time).)

Po zakończeniu zadania sprawdź jego wyniki za pomocą atrybutu `result()` zadania.

```python
# To jest wynik całego przesłania.  Przesłałeś/-aś jeden PUB,
# więc zawiera jeden wynik wewnętrzny (i pewne własne metadane).
job_result = job.result()

# To jest wynik naszego pojedynczego PUB, który miał sześć obserwabli,
# więc zawiera informacje o wszystkich sześciu.
pub_result = job.result()[0]
```

Teraz możemy również wykonać Circuit przy użyciu primitiva `Sampler`.

```python
# Dołączamy pomiary do Circuit
qc.measure_all()
sampler = Sampler(mode=backend)
```

```python
qc.draw(output="mpl")
```

![Quantum circuit diagram](./_04_Hands-on Introduction to Qiskit_imgs/output_17.png)

Prześlij zadanie przy użyciu primitiva Sampler.

```python
job_sampler = sampler.run(pm.run([qc]))

# Użyj ID zadania, aby później pobrać dane
print(f">>> Job ID: {job_sampler.job_id()}")
# Pobierz wyniki
results_sampler = job_sampler.result()
```

```text
>>> Job ID: a6ee4d2f-c80d-4a86-9a76-e4b1a74502e7
```

## 2.4 Analiza wyników

Krok analizy to zazwyczaj miejsce, w którym możesz przeprowadzić postprocesing wyników, korzystając na przykład z mitygacji błędów pomiarowych lub ekstrapolacji zerowego szumu (ZNE). Możesz podać te wyniki do innego przepływu pracy w celu dalszej analizy lub przygotować wykres kluczowych wartości i danych. Ogólnie rzecz biorąc, ten krok jest specyficzny dla twojego problemu. W tym przykładzie wykreśl każdą z wartości oczekiwanych zmierzonych dla naszego Circuit.

Wartości oczekiwane i odchylenia standardowe dla obserwabli podanych do Estimator są dostępne przez atrybuty `PubResult.data.evs` i `PubResult.data.stds` wyniku zadania. Aby uzyskać wyniki z Sampler, użyj funkcji `PubResult.data.meas.get_counts()`, która zwróci `dict` pomiarów w postaci bitstring jako kluczy i zliczeń jako odpowiadających im wartości. Więcej informacji znajdziesz w artykule [Zacznij korzystać z Sampler.](https://docs.quantum.ibm.com/guides/get-started-with-primitives#get-started-with-sampler)

```python
# Wykreśl wynik
from matplotlib import pyplot as plt
values = pub_result.data.evs
errors = pub_result.data.stds
# rysowanie wykresu
# Wykres z paskami błędów
plt.errorbar(observables_labels, values, yerr=errors, fmt='-o', capsize=5)
plt.xlabel("Observables")
plt.ylabel("Values")
plt.title("Plot of Observables vs Values with Error Bars")
plt.grid(True)
plt.tight_layout()
plt.show()
```

![Plot output](./_04_Hands-on Introduction to Qiskit_imgs/output_18.png)

Widzimy, że obserwable $ZZI$ i $III$ mają wartość oczekiwaną równą 1, ponieważ $ZZI$ wprowadza dwa znaki minus, które się znoszą, a $III$ działa jako tożsamość, pozostawiając stan GHZ niezmieniony. Pozostałe obserwable mają wartość oczekiwaną równą 0, gdyż ich operatory $Z$ wprowadzają nieparzystą liczbę znaków minus lub operatory $X$ odwracają pewną liczbę Qubitów, powodując ortogonalność nakładających się stanów.

Teraz wykreślamy wyniki dla Sampler.

```python
counts_list = results_sampler[0].data.meas.get_counts()
print(counts_list)
print(f"Outcomes : {counts_list}")
display(plot_histogram(counts_list, title="GHZ state"))
```

```text
{'111': 480, '000': 503, '101': 8, '100': 9, '001': 3, '011': 6, '010': 10, '110': 5}
Outcomes : {'111': 480, '000': 503, '101': 8, '100': 9, '001': 3, '011': 6, '010': 10, '110': 5}
```

![Code output](./_04_Hands-on Introduction to Qiskit_imgs/output_19.png)
