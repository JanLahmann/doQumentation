---
title: "Kwantowa diagonalizacja Kryłowa"
sidebar_label: "Kwantowa diagonalizacja Kryłowa"
description: "Opisana jest kwantowa diagonalizacja Kryłowa (QKD), zaczynając od klasycznych metod Kryłowa. Omówiono zbieżność i zużycie zasobów."
notebook_path: "learning/courses/quantum-diagonalization-algorithms/krylov.ipynb"
---
{/* doqumentation-source-hash: 97b22ac8 */}

<OpenInLabBanner notebookPath="learning/courses/quantum-diagonalization-algorithms/krylov.ipynb" />



{/* cspell:ignore Hndt longrightarrow Bigg Jörg Liesen Zdenek Strakos Yousef Saad MINRES vstar nabla */}

# Kwantowa diagonalizacja Kryłowa {#krylov-quantum-diagonalization}

W tej lekcji dotyczącej kwantowej diagonalizacji Kryłowa (KQD) odpowiemy na następujące pytania:

* Czym jest metoda Kryłowa w ogólnym ujęciu?
* Dlaczego metoda Kryłowa działa i pod jakimi warunkami?
* Jaką rolę odgrywa obliczanie kwantowe?

Część kwantowa obliczeń opiera się w dużej mierze na pracy z Ref [\[1\]](#references).

Poniższe wideo przedstawia ogólny przegląd metod Kryłowa w obliczeniach klasycznych, motywuje ich zastosowanie oraz wyjaśnia, jaką rolę może odgrywać obliczanie kwantowe w tym nurcie pracy. W dalszym tekście znajdują się bardziej szczegółowe informacje oraz implementacja metody Kryłowa zarówno klasycznie, jak i przy użyciu komputera kwantowego.

<IBMVideo id="134325510" title="W tym nagraniu Chris Porter przedstawia przegląd klasycznych metod Kryłowa i wyjaśnia, dlaczego są one użyteczne. Tłumaczy, jak obliczanie kwantowe może odgrywać rolę w tym podejściu do diagonalizacji. Poniższy tekst zawiera więcej szczegółów."/>
## 1. Wprowadzenie do metod Kryłowa {#1-introduction-to-krylov-methods}

__Metoda podprzestrzeni Kryłowa__ może odnosić się do dowolnej z kilku metod zbudowanych wokół tak zwanej __podprzestrzeni Kryłowa__. Pełny przegląd tych metod wykracza poza zakres tej lekcji, lecz Ref [\[2-4\]](#references) mogą dostarczyć znacznie szerszego kontekstu. Skupimy się tutaj na tym, czym jest podprzestrzeń Kryłowa, jak i dlaczego jest użyteczna w rozwiązywaniu problemów wartości własnych, a w końcu jak może być zaimplementowana na komputerze kwantowym.
__Definicja:__ Dla symetrycznej, dodatnio półokreślonej macierzy $A$ o wymiarze $N\times N$, przestrzeń Kryłowa $\mathcal{K}^r$ rzędu $r$ jest przestrzenią rozpinaną przez wektory otrzymane przez mnożenie wyższych potęg macierzy $A$, aż do $r-1\leq N$, przez wektor odniesienia $\vert v \rangle$.

$$
\mathcal{K}^r = \text{span}\left\{ \vert v \rangle, A \vert v \rangle, A^2 \vert v \rangle, ..., A^{r-1} \vert v \rangle \right\}
$$

Chociaż wektory powyżej rozpinają to, co nazywamy podprzestrzenią Kryłowa, nie ma powodu sądzić, że będą one ortogonalne. Często stosuje się iteracyjny proces ortonormalizacji podobny do __ortogonalizacji Grama-Schmidta__. Tutaj proces jest nieco inny, ponieważ każdy nowy wektor jest czyniony ortogonalnym do pozostałych w momencie jego generowania. W tym kontekście nazywa się to __iteracją Arnoldiego__. Zaczynając od wektora początkowego $|v\rangle$, generuje się następny wektor $A|v\rangle$, a następnie zapewnia się, że ten drugi wektor jest ortogonalny do pierwszego przez odjęcie jego rzutu na $|v\rangle$. Czyli

$$
\begin{aligned}
|v_0\rangle &=\frac{|v\rangle}{\left|\left| |v\rangle \right|\right|}\\

|v_1\rangle &=\frac{A|v\rangle-\langle v_0|A|v\rangle |v_0\rangle}{\left|\left|A|v\rangle-\langle v_0|A|v\rangle |v_0\rangle \right|\right|}
\end{aligned}
$$

Łatwo teraz zauważyć, że $|v_0\rangle \perp |v_1\rangle,$ ponieważ
$$
\langle v_0 | v_1\rangle=\frac{\langle v_0 | A|v\rangle-\langle v_0 |A|v\rangle\langle v_0|v_0\rangle}{\left|\left| A|v\rangle-\langle A|v\rangle|v_0\rangle |v_0\rangle \right|\right|}=0
$$

Robimy to samo dla kolejnego wektora, zapewniając, że jest ortogonalny do obu poprzednich:
$$
|v_2\rangle=\frac{A |v_1\rangle-\langle v_0|A |v_1\rangle |v_0\rangle-\langle v_1|A |v_1\rangle |v_1\rangle}{\left|\left| A |v_1\rangle-\langle v_0|A |v_1\rangle |v_0\rangle-\langle v_1|A |v_1\rangle |v_1\rangle\right|\right|}
$$
Jeśli powtórzymy ten proces dla wszystkich $r$ wektorów, otrzymamy kompletną ortonormalną bazę dla przestrzeni Kryłowa. Zauważ, że proces ortogonalizacji w tym miejscu daje zero, gdy $r>m$, ponieważ $m$ ortogonalnych wektorów z konieczności rozpina pełną przestrzeń. Proces również da zero, jeśli dowolny wektor jest wektorem własnym $A$, ponieważ wszystkie kolejne wektory będą wielokrotnościami tego wektora.
### 1.1 Prosty przykład: Kryłow ręcznie {#11-a-simple-example-krylov-by-hand}

Przejdźmy krok po kroku przez generowanie podprzestrzeni Kryłowa na trywialnie małej macierzy, tak aby zobaczyć ten proces. Zaczynamy od interesującej nas początkowej macierzy $A$:
$$
A=\begin{pmatrix}4&-1&0\\-1&4&-1\\0&-1&4\end{pmatrix}
$$
Dla tego małego przykładu możemy łatwo wyznaczyć wektory i wartości własne nawet ręcznie. Tutaj pokazujemy rozwiązanie numeryczne.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q matplotlib numpy qiskit qiskit-ibm-runtime scipy sympy
```

```python
# One might use linalg.eigh here, but later matrices may not be Hermitian. So we use linalg.eig in this lesson.

import numpy as np

A = np.array([[4, -1, 0], [-1, 4, -1], [0, -1, 4]])
eigenvalues, eigenvectors = np.linalg.eig(A)
print("The eigenvalues are ", eigenvalues)
print("The eigenvectors are ", eigenvectors)
```

```text
The eigenvalues are  [2.58578644 4.         5.41421356]
The eigenvectors are  [[ 5.00000000e-01 -7.07106781e-01  5.00000000e-01]
 [ 7.07106781e-01  1.37464400e-16 -7.07106781e-01]
 [ 5.00000000e-01  7.07106781e-01  5.00000000e-01]]
```

Zapisujemy je tutaj do późniejszego porównania:
$$
\begin{aligned}
a_0&=2.59,&|0\rangle&=&\begin{pmatrix}1/2\\-\sqrt{2}/2\\1/2\end{pmatrix}\\
\\
a_1&=4,&|1\rangle&=&\begin{pmatrix}\sqrt{2}/2\\0\\-\sqrt{2}/2\end{pmatrix}\\
\\
a_2&=5.41,&|2\rangle&=&\begin{pmatrix}1/2\\\sqrt{2}/2\\1/2\end{pmatrix}
\end{aligned}
$$
Chcielibyśmy zbadać, jak ten proces działa (lub zawodzi), w miarę jak zwiększamy wymiar naszej podprzestrzeni Kryłowa, $r$. W tym celu zastosujemy następujący proces:

* Wygeneruj podprzestrzeń pełnej przestrzeni wektorowej, zaczynając od losowo wybranego wektora $|v\rangle$ (nazwij go $|v_0\rangle$, jeśli jest już znormalizowany, jak powyżej).
* Zrzutuj pełną macierz $A$ na tę podprzestrzeń i znajdź wartości własne tej zrzutowanej macierzy $\tilde{A}$.
* Zwiększ rozmiar podprzestrzeni, generując więcej wektorów, dbając o to, aby były one ortonormalne, przy użyciu procesu podobnego do ortogonalizacji Grama-Schmidta.
* Zrzutuj $A$ na większą podprzestrzeń i znajdź wartości własne otrzymanej macierzy $\tilde{A}$.
* Powtarzaj to, aż wartości własne zbiegną się (lub, w tym zabawkowym przypadku, aż wygenerujesz wektory rozpinające pełną przestrzeń wektorową oryginalnej macierzy $A$).

Normalna implementacja metody Kryłowa nie musiałaby rozwiązywać problemu wartości własnych dla macierzy rzutowanej na każdą podprzestrzeń Kryłowa w miarę jej budowania. Można by skonstruować podprzestrzeń o żądanym wymiarze, zrzutować macierz na tę podprzestrzeń i zdiagonalizować zrzutowaną macierz. Rzutowanie i diagonalizowanie przy każdym wymiarze podprzestrzeni jest wykonywane tylko w celu sprawdzenia zbieżności.

#### Wymiar $r=1$: {#dimension-r1}

Wybieramy losowy wektor, powiedzmy
$$
|v_0\rangle=\begin{pmatrix}1\\0\\0\end{pmatrix}
$$

Jeśli nie jest on już znormalizowany, znormalizuj go.

Teraz rzutujemy naszą macierz $A$ na podprzestrzeń tego jednego wektora:
$$
\tilde{A}_0=\langle v_0| A|v_0\rangle=\begin{pmatrix}1&0&0\end{pmatrix}\begin{pmatrix}4&-1&0\\-1&4&-1\\0&-1&4\end{pmatrix}\begin{pmatrix}1\\0\\0\end{pmatrix}=(4)
$$
To jest nasze rzutowanie macierzy na naszą podprzestrzeń Kryłowa, gdy zawiera ona tylko pojedynczy wektor, $|v_0\rangle$. Wartość własna tej macierzy wynosi trywialnie 4. Możemy traktować to jako nasze oszacowanie zerowego rzędu wartości własnych (w tym przypadku tylko jednej) macierzy $A$. Chociaż jest to słabe oszacowanie, jest to poprawny rząd wielkości.
#### Wymiar $r=2$: {#dimension-r2}

Teraz generujemy kolejny wektor w naszej podprzestrzeni poprzez działanie $A$ na poprzedni wektor:

$$
A|v_0\rangle=\begin{pmatrix}4&-1&0\\-1&4&-1\\0&-1&4\end{pmatrix}\begin{pmatrix}1\\0\\0\end{pmatrix}=\begin{pmatrix}4\\-1\\0\end{pmatrix}
$$

Teraz odejmujemy rzut tego wektora na poprzedni wektor, aby zapewnić ortogonalność.
$$
|v_1\rangle=A|v_0\rangle-\langle v_0 |A|v_0\rangle|v_0\rangle
$$
$$
|v_1\rangle=\begin{pmatrix}4\\-1\\0\end{pmatrix}-\begin{pmatrix}1& 0& 0\end{pmatrix}\begin{pmatrix}4\\-1\\0\end{pmatrix}\begin{pmatrix}1\\0\\0\end{pmatrix}=\begin{pmatrix}0\\-1\\0\end{pmatrix}
$$

Jeśli nie jest już znormalizowany, znormalizuj go. W tym przypadku wektor był już znormalizowany, więc

$$
|v_1\rangle=\begin{pmatrix}0\\-1\\0\end{pmatrix}
$$

Teraz rzutujemy naszą macierz A na podprzestrzeń tych dwóch wektorów:
$$
\tilde{A}_1= \begin{pmatrix} 1&0&0\\0&-1&0 \end{pmatrix} \begin{pmatrix}4&-1&0\\-1&4&-1\\0&-1&4\end{pmatrix}\begin{pmatrix}1&0\\0&-1\\0&0\end{pmatrix}=\begin{pmatrix}1&0&0\\0&-1&0\end{pmatrix}\begin{pmatrix}4&1\\-1&-4\\0&1\end{pmatrix}=\begin{pmatrix}4&1\\1&4\end{pmatrix}
$$

Pozostaje nam jeszcze problem wyznaczenia wartości własnych tej macierzy. Ale ta macierz jest nieco mniejsza niż pełna macierz. W problemach dotyczących bardzo dużych macierzy praca z tą mniejszą podprzestrzenią może być niezwykle korzystna.

$$
\det(\tilde{A_1}-\lambda I)=0
$$
$$
\begin{vmatrix} 4-\lambda&1\\1&4-\lambda\end{vmatrix} =(4-\lambda)^2-1=0
$$
$$
4-\lambda=±1→\lambda=3,5
$$
Chociaż nadal nie jest to dobre oszacowanie, jest lepsze niż oszacowanie zerowego rzędu. Wykonamy jeszcze jedną iterację, aby upewnić się, że proces jest jasny. Jednak podważa to sens metody, ponieważ w następnej iteracji będziemy diagonalizować macierz 3x3, co oznacza, że nie zaoszczędziliśmy czasu ani mocy obliczeniowej.
#### Wymiar $r=3$: {#dimension-r3}

Teraz generujemy kolejny wektor w naszej podprzestrzeni poprzez działanie A na poprzedni wektor:
$$
A|v_1\rangle=\begin{pmatrix}4&-1&0\\-1&4&-1\\0&-1&4\end{pmatrix}\begin{pmatrix}0\\-1\\0\end{pmatrix}=\begin{pmatrix}1\\-4\\1\end{pmatrix}
$$
Teraz odejmujemy rzut tego wektora na oba nasze poprzednie wektory, aby zapewnić ortogonalność.
$$
\begin{aligned}
|v_2\rangle&=A|v_1\rangle-\langle v_0 |A|v_1\rangle|v_0\rangle-\langle v_1 |A|v_1\rangle|v_1\rangle\\
|v_2\rangle&=\begin{pmatrix}1\\-4\\1\end{pmatrix}-\begin{pmatrix}1& 0& 0 \end{pmatrix}\begin{pmatrix}1\\-4\\1\end{pmatrix}\begin{pmatrix}1\\0\\0\end{pmatrix}-\begin{pmatrix}0&-1& 0\end{pmatrix}\begin{pmatrix}1\\-4\\1\end{pmatrix}\begin{pmatrix}0\\-1\\0\end{pmatrix}=\begin{pmatrix}0\\0\\1\end{pmatrix}
\end{aligned}
$$

Jeśli nie jest już znormalizowany, znormalizuj go. W tym przypadku wektor był już znormalizowany, więc
$$
|v_2 \rangle=\begin{pmatrix}0\\0\\1\end{pmatrix}
$$
Teraz rzutujemy naszą macierz $A$ na podprzestrzeń tych wektorów:
