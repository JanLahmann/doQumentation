<details>

<summary>
Szczegóły matematyczne
</summary>

$L_n[\frac{1}{3}, (\frac{64}{9})^{(\frac{1}{3})}] = e^{[((\frac{64}{9})^{(\frac{1}{3})} + o(1)) (log(n))^{\frac{1}{3}}(log(log(n))^{\frac{2}{3}})]}$

jako funkcja liczby całkowitej $n$ poddawanej faktoryzacji.

To skalowanie jest superwielomianowe w liczbie bitów wymaganych do reprezentacji $n$.

Dlatego faktoryzacja liczb pierwszych jest uważana za nieefektywną na komputerach klasycznych.

</details>

Obecnie [największe liczby całkowite rozłożone na czynniki na sprzęcie klasycznym](https://en.wikipedia.org/w/index.php?title=RSA_Factoring_Challenge&oldid=1163481797) mieszczą się w zakresie 829 bitów lub 250 cyfr dziesiętnych. Biorąc pod uwagę wykładniczy wzrost mocy obliczeniowej komputerów klasycznych obserwowany w ciągu ostatnich kilku dekad, 1024-bitowy RSA nie jest już uważany za bezpieczny w najbliższym czasie i jest obecnie wycofywany. Niemniej jednak w dającej się przewidzieć przyszłości faktoryzacja 2048-bitowych liczb całkowitych, których wielkość mieści się w zakresie $10^{617}$, jest obecnie uważana za niewykonalną w systemach klasycznych, co sugeruje dalszą żywotność. Pojawienie się komputerów kwantowych unieważnia jednak tę ocenę.
### Kwantowy algorytm Shora i RSA {#shors-quantum-algorithm-and-rsa}

Prawdopodobnie najbardziej znanym algorytmem kwantowym jest dzisiaj <DefinitionTooltip definition="Algorytm kwantowy, który potrafi efektywnie znajdować czynniki pierwsze. Opracowany przez matematyka Petera Shora w 1994 roku.">algorytm Shora</DefinitionTooltip> do znajdowania czynników pierwszych liczb całkowitych. Kiedy został [przedstawiony przez Petera Shora w 1994 roku](https://doi.org/10.1109/SFCS.1994.365700), został uznany za pierwszy algorytm kwantowy, który oferował <DefinitionTooltip definition="Rosnące szybciej niż wielomian; na przykład funkcja wykładnicza.">przyspieszenie superwielomianowe</DefinitionTooltip> w stosunku do algorytmów klasycznych w problemie o dużym znaczeniu praktycznym, a mianowicie faktoryzacji liczb pierwszych.

Algorytm Shora może faktoryzować liczby pierwsze z $O(n^2)$, gdzie $n$ to liczba bitów.
<details>
<summary>
Matematyczne wyjaśnienie algorytmu Shora
</summary>

W kontekście <DefinitionTooltip definition="Kryptosystem z kluczem publicznym nazwany na cześć Rona Rivesta, Adiego Shamira i Leonarda Adlemana. Opracowany w 1977 roku.">RSA</DefinitionTooltip>, <DefinitionTooltip definition="Algorytm kwantowy, który potrafi efektywnie znajdować czynniki pierwsze. Opracowany przez matematyka Petera Shora w 1994 roku.">algorytm Shora</DefinitionTooltip> działa poprzez wykorzystanie <DefinitionTooltip definition="Odstęp, w którym funkcja się powtarza.">okresowości</DefinitionTooltip> <DefinitionTooltip definition="Potęgowanie modularne to reszta z dzielenia, gdy liczba całkowita jest podniesiona do potęgi (wykładnika), a następnie podzielona przez liczbę (modulus). Na przykład $x^e mod m$.">funkcji wykładniczej modularnej</DefinitionTooltip> $f(x) = a^x (mod~n)$ i dostarcza kwantowego prymitywu do znajdowania <DefinitionTooltip definition="Odstęp, w którym funkcja się powtarza.">okresu</DefinitionTooltip>, który umożliwia efektywną faktoryzację liczby <DefinitionTooltip definition="Modulus to reszta pozostała po dzieleniu przez określoną wartość. Modulo to operacja wykonywania tego.">modulus</DefinitionTooltip> $n$.

Uproszczony wysokopoziomowy zarys ogólnego schematu Shora do łamania RSA jest następujący:

1. Mając modulus $n$, który jest publikowany jako część klucza publicznego, wybierz liczbę $a$ względnie pierwszą z $n$, to znaczy $gcd(a,n) = 1$. Ponieważ wiemy, że $n = p*q$ ma dokładnie dwa czynniki pierwsze $(p, q)$, prawie każda liczba mniejsza od $n$, którą wybierzemy losowo, będzie prawdopodobnie względnie pierwsza z $n$.

2. Po wybraniu $a$, znajdź wykładnik $r$ taki, że $a^r \equiv 1 (mod~n)$. Oznacza to $a^r - 1 \equiv 0 (mod~n)$. Istnienie <DefinitionTooltip definition="Liczba, ile razy liczba jest mnożona przez siebie.">wykładnika</DefinitionTooltip> $r$ takiego, że powyższa <DefinitionTooltip definition="Spójność lub równoważność, tj. zachowywanie się w ten sam sposób.">kongruencja</DefinitionTooltip> zachodzi, jest gwarantowane przez <DefinitionTooltip definition="Odstęp, w którym funkcja się powtarza.">własność okresowości</DefinitionTooltip> <DefinitionTooltip definition="Potęgowanie modularne to reszta z dzielenia, gdy liczba całkowita jest podniesiona do potęgi (wykładnika), a następnie podzielona przez liczbę (modulus). Na przykład $x^e mod m$.">potęgowania modularnego</DefinitionTooltip>.

3. Jeśli $r$ jest parzyste, $a^r - 1 \equiv 0 (mod~n) \implies (a^{r/2} - 1) (a^{r/2} + 1) =  \gamma*n$ dla pewnej liczby całkowitej $\gamma$. Lewa strona tej ostatniej równości musi zawierać $p$ i $q$ jako dwa ze swoich czynników pierwszych, ponieważ zawiera je prawa strona. Jeśli $r$ jest nieparzyste, wróć do kroku 1 i spróbuj innego wyboru $a$.

4. Użyj <DefinitionTooltip definition="Efektywny sposób obliczania największego wspólnego dzielnika dwóch liczb całkowitych. Jest ulepszeniem oryginalnego algorytmu Euklidesa.">rozszerzonego algorytmu Euklidesa</DefinitionTooltip> do znalezienia $gcd((a^{r/2} - 1), n)$ lub $gcd((a^{r/2} + 1), n)$. Obliczony <DefinitionTooltip definition="Greatest Common Divisor - największa dodatnia liczba całkowita, która może dzielić dwie lub więcej podanych liczb całkowitych.">GCD</DefinitionTooltip> bardzo prawdopodobnie zidentyfikuje jeden z czynników pierwszych $p$ lub $q$. Podziel $n$ przez ten czynnik pierwszy, aby odzyskać drugi.

5. Gdy znane są $p, q$, użyj kroków z oryginalnego algorytmu RSA do rekonstrukcji tocjentu $\phi(n)$ i wygenerowania liczby klucza prywatnego $d$ jako <DefinitionTooltip definition="Dla liczby całkowitej $a$ i modulusu $m$, reszta z ax/m wynosi 1. Ma to miejsce tylko dla liczb względnie pierwszych.">odwrotności modularnej</DefinitionTooltip> znanej liczby klucza publicznego $e$.

</details>
W sierpniu 2023 roku Oded Regev [opublikował ulepszenie](https://doi.org/10.48550/arXiv.2308.06572) oryginalnego algorytmu Shora przy użyciu podejścia wielowymiarowego, co daje $O(n^{1.5})$. W tej dziedzinie trwają dalsze badania, w tym prace [Ragavana i Vaikuntanathana](https://doi.org/10.48550/arXiv.2310.00899), które mogą poprawić czas, koszt lub liczbę wymaganych kubitów. Chociaż nie możemy powiedzieć, kiedy uruchamianie takich algorytmów przeciwko rzeczywistemu szyfrowaniu RSA stanie się naprawdę wykonalne, staje się to coraz bliższe.
#### Przykład w Pythonie demonstrujący łamanie szyfrowania RSA {#python-example-demonstrating-breaking-rsa-encryption}

W poniższych komórkach kodu ilustrujemy przykład znajdowania klucza prywatnego mając jedynie klucz publiczny. Wykorzysta to klasyczne obliczenia siłowe (brute-force), ale pokazuje, jak można by wykorzystać algorytm Shora - w tym dla dużych kluczy.

<Admonition type="note">
W tej sekcji pokażemy obliczenia matematyczne szczegółowo jako część kodu Pythona
</Admonition>

W tym przykładzie mamy klucz publiczny $(5, 247)$ i odzyskamy klucz prywatny.

**Krok 1:** Pierwszym krokiem jest wybranie liczby względnie pierwszej z 247. Prawie każda liczba, którą zgadniemy, wykona to zadanie. Wybierzmy 6.

```python
n = 247  # the modulus
e = 5  # public key number
a = 6  # an integer coprime to n
assert gcd(a, n) == 1
print(f"Checked {n} and {a} are coprime")
```

**Krok 2:** Następnie musimy znaleźć <DefinitionTooltip definition="Odstęp, w którym funkcja się powtarza.">okres</DefinitionTooltip> $r$ taki, że $6^r \equiv 1 (mod~247)$. W tym przykładzie obliczamy $r$ klasycznie metodą siłową, ale moglibyśmy również użyć algorytmu Shora na komputerze kwantowym przy użyciu <DefinitionTooltip definition="Open-source'owy SDK do pracy z algorytmami i komputerami kwantowymi. Opracowany przez IBM.">Qiskit</DefinitionTooltip>.

```python
r = 0
rem = 100
while rem != 1:
    r += 1
    rem = (a**r) % n

print(f"period r is: {r}")
assert a**r % n == 1

print(f"Checked {a}^{r} mod {n} is 1")
```

**Krok 3:** Ponieważ <DefinitionTooltip definition="Odstęp, w którym funkcja się powtarza.">okres</DefinitionTooltip> $r = 36$ jest parzysty, możemy obliczyć $f1 = (a^{r/2}-1), f2=(a^{r/2}+1)$.

```python
# explicitly use as integer
f1 = int(a ** (r / 2) - 1)
f2 = int(a ** (r / 2) + 1)

print(f"f1 = {f1}")
print(f"f2 = {f2}")
```

**Krok 4:** Znajdź <DefinitionTooltip definition="Greatest Common Divisor - największa dodatnia liczba całkowita, która może dzielić dwie lub więcej podanych liczb całkowitych.">GCD</DefinitionTooltip> dowolnego z tych czynników z $n$. Po prostu podziel $n$ przez już znaleziony czynnik pierwszy, aby uzyskać drugi czynnik pierwszy.