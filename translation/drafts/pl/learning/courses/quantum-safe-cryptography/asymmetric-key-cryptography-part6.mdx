
Z powyższego wyjścia komórki Pythona widzimy, że:

$h = 2 \implies k=4 \mathrm{~lub~}  4 = log_5(2) (mod~7)$

$h = 6 \implies k=3 \mathrm{~lub~}  3 = log_5(6) (mod~7)$

W zwykłej arytmetyce liczb rzeczywistych potęgowanie jest funkcją <DefinitionTooltip definition="Zachowanie lub zależność, która konsekwentnie zmienia się w jednym kierunku bez odwróceń; na przykład w funkcji monotonicznie rosnącej wartości stale rosną.">monotoniczną</DefinitionTooltip>, a znalezienie logarytmu dowolnej liczby przy dowolnej podstawie jest obliczeniowo łatwe. W przeciwieństwie do tego, jak widać z prostego przykładu $(\mathbb{Z}_7)^{\times}$ powyżej, potęgowanie modularne jest niemonotoniczne i choć jest okresowe z okresem $p-1$, poza tym jest wysoce nietrywialne. Dlatego obliczenie jego odwrotności, czyli logarytmu dyskretnego, okazuje się nieefektywne dla dużych $p$ na komputerach klasycznych.

Obserwacja ta stanowi podstawę zarówno wymiany kluczy Diffiego-Hellmana (DH), jak i algorytmu podpisu cyfrowego (DSA), które zostaną omówione w następnym rozdziale.

DLP można rozszerzyć na cykliczne podgrupy w następujący sposób:

-   Rozważmy $(\mathbb{Z}_p)^{\times}$ zdefiniowane powyżej oraz element $g \in (\mathbb{Z}_p)^{\times}$ rzędu pierwszego $r$, to znaczy $g^r \equiv 1 (~mod~p)$.
-   Zbiór potęg całkowitych $g$: $\{g^k~(mod~p) | 1 \le k \le r\} = \langle g \rangle$ jest cykliczną podgrupą $(\mathbb{Z}_p)^{\times}$ rzędu $r$.
-   DLP można określić na $\langle g \rangle$ wybierając $h \in \\langle g \rangle$ i pytając o $1 \le a \le r$ takie, że $ g^a~(mod~p) = h$
### Wymiana kluczy Diffiego-Hellmana

W 1976 roku [Whitfield Diffie i Martin Hellman zaproponowali protokół wymiany kluczy](https://doi.org/10.1109/TIT.1976.1055638), który umożliwia utworzenie wspólnego klucza tajnego przez niezabezpieczone kanały komunikacyjne. Tajny klucz może być następnie używany przez strony go współdzielące do szyfrowania symetrycznego. Algorytm opiera się na DLP.

![Rysunek 1. Wymiana kluczy Diffiego-Hellmana](/learning/images/courses/quantum-safe-cryptography/asymmetric-key-cryptography/key_exchange_dh.avif)

*Rysunek 1. Wymiana kluczy Diffiego-Hellmana*

<details>

<summary>
Szczegóły matematyczne wymiany kluczy Diffiego-Hellmana
</summary>

Przy założeniu, że dwiema komunikującymi się stronami są Alicja i Bob, protokół działa następująco:

- Najpierw Alicja i Bob uzgadniają dużą liczbę pierwszą $p$ oraz pierwiastek pierwotny lub generator $a$.
- Następnie Alicja losowo wybiera tajny wykładnik $k_A$ taki, że $1 \le k_A \le p-2$ i oblicza $h_A = a^{k_A}~(mod~p)$. $k_A, h_A$ są odpowiednio kluczem prywatnym i publicznym Alicji.
- Następnie Bob losowo wybiera tajny wykładnik $k_B$ taki, że $1 \le k_B \le p-2$ i oblicza $h_B = a^{k_B}~(mod~p)$. $k_B, h_B$ są odpowiednio kluczem prywatnym i publicznym Boba.
- Następnie Alicja wysyła Bobowi $h_A$, a Bob wysyła Alicji $h_B$ przez niezawodny, ale niekoniecznie bezpieczny kanał.
- Następnie Alicja używa otrzymanego $h_B$, aby obliczyć wspólny klucz tajny $ \kappa = h_B^{k_A}~(mod~p)$.
- Na koniec Bob używa otrzymanego $h_A$, aby obliczyć wspólny klucz tajny $ \kappa = h_A^{k_B}~(mod~p)$.

Przy tym protokole:

- Alicja i Bob mają gwarancję otrzymania tego samego tajnego klucza $\kappa$, ponieważ $h_B^{k_A}~(mod~p) = (a^{k_B})^{k_A}~(mod~p) = a^{k_A k_B}~(mod~p) = (a^{k_A})^{k_B}~(mod~p) = h_A^{k_B}~(mod~p) $.
- Osoba trzecia, która przechwyci $h_A$ lub $h_B$, nie może skonstruować tajnego klucza $\kappa$, ponieważ nie ma dostępu odpowiednio do $k_B$ lub $k_A$.
- Wyodrębnienie $k_A$ lub $k_B$ przy użyciu informacji publicznych $a$, $p$, $h_A$ i $h_B$ jest obliczeniowo trudne, gdyż jest równoważne rozwiązaniu DLP na $(\mathbb{Z}_p)^{\times}$.

</details>
#### Ilustracja protokołu Diffiego-Hellmana w Pythonie

Przyjrzyjmy się prostemu przykładowi protokołu DH w Pythonie przy użyciu małych liczb pierwszych:

<Admonition type="note">
W tej sekcji pokażemy szczegółowe obliczenia matematyczne jako część kodu Pythona
</Admonition>

**Krok 1:** Alicja i Bob uzgadniają liczbę pierwszą $p$ oraz pierwiastek pierwotny $a$. Wybierzmy $p=11, a=7$.

```python
# Step 1: Choose a prime `p` and a primitive root `a`
p = 11
a = 7

print(f"prime: {p}")
print(f"primitive root: {a}")
```

**Kroki 2, 3:** Alicja wybiera tajny wykładnik $k_A$ i oblicza $h_A = a^{k_A}~(mod~p)$. Podobnie Bob wybiera tajny wykładnik $k_B$ i oblicza $h_B = a^{k_B}~(mod~p)$.

```python
k_A = 4  # Alice's private key
h_A = (a ** (k_A)) % p  # Alice's public key

print(f"Alice's private key is {k_A} and public key is {h_A}")

k_B = 8  # Bob's private key
h_B = (a ** (k_B)) % p  # Bob's public key

print(f"Bob's private key is {k_B} and public key is {h_B}")
```

**Krok 4:** Obie strony rozgłaszają swoje klucze publiczne $h_A$ i $h_B$.

**Kroki 5, 6:** Każda ze stron łączy swój klucz prywatny z kluczem publicznym drugiej strony, aby utworzyć wspólny klucz tajny.

```python
secret_key_alice = h_B**k_A % p
secret_key_bob = h_A**k_B % p
assert secret_key_alice == secret_key_bob
print(f"The shared secret key is: {secret_key_bob}")
```

Alicja i Bob mogą teraz używać wspólnego klucza tajnego do szyfrowania symetrycznego.
#### Bezpieczeństwo wymiany kluczy Diffiego-Hellmana

Jak zauważono powyżej, bezpieczeństwo DH opiera się na trudności obliczeniowej rozwiązania DLP z dużymi liczbami pierwszymi $p$. W typowych zastosowaniach NIST zaleca 2048- lub 3072-bitowe liczby pierwsze dla wymiany kluczy DH, co jest uznawane za wystarczająco bezpieczne przeciwko próbom rozwiązania DLP przy użyciu komputerów klasycznych.

**Ataki typu man-in-the-middle (MITM)**: Fakt, że DH jest interaktywnym schematem, w którym wspólny sekret zależy od połączenia klucza prywatnego jednej strony z kluczem publicznym drugiej strony, czyni go podatnym na tak zwany atak *man-in-the-middle (MITM)*.

<details>

<summary>
Szczegóły matematyczne bezpieczeństwa DH i ataków MITM
</summary>

W tym scenariuszu osoba trzecia — powiedzmy Ewa — przechwytuje klucze publiczne $h_A, h_B$ podczas transmisji i zastępuje swoim kluczem publicznym $h_E$ każdy z $h_A$ i $h_B$ przed przekazaniem ich odpowiednio do Boba i Alicji.

Wtedy zamiast używać $h_B$ do utworzenia swojego wspólnego sekretu, Alicja użyje $h_E$, myśląc, że używa klucza publicznego Boba. Podobnie, zamiast używać $h_A$ do utworzenia swojego wspólnego sekretu, Bob użyje $h_E$, myśląc, że używa klucza publicznego Alicji.

Ponieważ $h_E$ został użyty do utworzenia wspólnego sekretu Alicji (Boba), tekst jawny zaszyfrowany przez Alicję (Boba) może zostać odszyfrowany przez Ewę.

Dlatego wymiana kluczy DH jest zwykle używana w połączeniu z algorytmem podpisu cyfrowego, aby zapewnić, że każda strona używa uwierzytelnionego klucza publicznego do utworzenia wspólnego sekretu.

</details>
### Algorytm podpisu cyfrowego (DSA)

Chociaż ogólne kryptosystemy, takie jak [RSA](https://en.wikipedia.org/w/index.php?title=RSA_(cryptosystem)&oldid=1163962255), zapewniają funkcjonalność podpisu cyfrowego, w 1994 roku NIST przyjął specjalistyczny schemat podpisu oparty na potęgowaniu modularnym i problemie logarytmu dyskretnego jako federalny standard podpisów cyfrowych. Schemat ten, znany po prostu jako [*Algorytm podpisu cyfrowego*](https://en.wikipedia.org/w/index.php?title=Digital_Signature_Algorithm&oldid=1148950522) (DSA), obejmuje cztery odrębne fazy:
1. **Generowanie kluczy**:

   Klucze DSA generowane są z:
     - 2 liczb pierwszych spełniających określone reguły
       -   $p$ - zwykle 256 bitów (długość tę nazwiemy $N$)
       -   $q$ - zwykle 3072 bity (długość tę nazwiemy $L$)
     - Kryptograficznej funkcji skrótu, która będzie konwertować ciągi o długości $L$ na $N$
     - Dodatkowego parametru $g$ (szczegóły poniżej)

   Z tego wybieramy liczbę losową $x$ jako klucz prywatny i jesteśmy w stanie obliczyć klucz publiczny $y$

   <details>
   <summary>
   Szczegóły matematyczne generowania kluczy
   </summary>

   - **Generowanie parametrów:** Matematycznie DSA obejmuje cykliczną podgrupę $(\mathbb{Z}_p)^{\times}$ generowaną przez element $g$ rzędu pierwszego q < p. Pierwszym krokiem w DSA jest zatem wybór dwóch liczb pierwszych p, q w celu ustanowienia niezbędnych struktur matematycznych.

     - Wybierz $N$-bitową liczbę pierwszą $q$.
     - Wybierz $L$-bitową liczbę pierwszą $p$ taką, że $p-1$ jest wielokrotnością $q$. Wybór $p$ określa grupę $(\mathbb{Z}_p)^{\times}$
     - Wybierz losowo liczbę całkowitą $1 < h < p-1$ i oblicz $g = h^{(p-1)/q}~mod~p$. Jeśli $g=1$, co zdarza się rzadko, spróbuj innego h.
     - Sprawdź, czy $g^q~mod~p = 1$. g jest zatem generatorem cyklicznej podgrupy $\langle g \rangle$ grupy $(\mathbb{Z}_p)^{\times}$ rzędu pierwszego q.

     Parametry $(q, p, g)$ określają instancję algorytmu i są informacją publiczną. Zwykle w realistycznych zastosowaniach $ N \sim 256$ i $L \sim 3072$.

     Protokół wymaga również kryptograficznej funkcji skrótu $H : \{0,1\}^L \rightarrow \{0, 1\}^N$, która odwzorowuje binarne ciągi $L$-bitowe na ciągi $N$-bitowe, na przykład SHA-256.

   - **Generowanie pary kluczy:** Podpisujący musi wygenerować parę kluczy po swojej stronie.

     - Wybierz losową tajną liczbę całkowitą $ x \in \{1,2...q-1\}$. $x$ jest kluczem prywatnym.
     - Wygeneruj $ y = g^{x}~mod~p$. $y$ jest kluczem publicznym podpisującego.

   </details>
2. **Dystrybucja kluczy:**

   Następujące informacje są udostępniane każdemu, kto chce zweryfikować podpis
   - Użyte parametry $(p,q,g)$, które opisują algorytm
   - Funkcja skrótu $H$
   - klucz publiczny $y$
1. **Podpisywanie:**

   - Podpisujący może teraz podpisać wiadomość $m$. Wynikowym podpisem jest $(r,s)$
   - Wiadomość i podpis są teraz wysyłane do odbiorcy

   <details>
   <summary>
   Szczegóły matematyczne podpisywania wiadomości
   </summary>

   Podpisujący podpisuje wiadomość $m$ w następujący sposób:

   - Wybierz tajną liczbę całkowitą k losowo z $\{1,2...q-1\}$
   - Oblicz $r = (g^k~mod~p)~mod~q$. W rzadkim przypadku, gdy $r=0$, spróbuj innego losowego k.
   - Oblicz $s = (k^{-1} (H(m) + xr))~mod~q$. W rzadkich przypadkach, gdy $s=0$, spróbuj innego losowego k.
   - Podpisem jest krotka $(r, s)$.
   - Podpisujący przekazuje weryfikatorowi zarówno wiadomość $m$, jak i podpis $(r,s)$.

   Ponieważ zarówno $r$, jak i $s$ są liczbami całkowitymi modulo $q$, rozmiar podpisu jest rzędu $N$-bitów, a nie dłuższych $L$-bitów.

   </details>