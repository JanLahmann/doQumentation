```python
from qiskit import QuantumCircuit

# create a 1-qubit quantum state psi from theta, varphi parameters
qc = QuantumCircuit(1, 1)
qc.u(theta, varphi, 0.0, 0)

# measure in computational basis
qc.measure(0, 0)

qc.draw(output="mpl")
```

![Wynik poprzedniej komórki kodu](/learning/images/courses/utility-scale-quantum-computing/teleportation/extracted-outputs/b1327267-f2fe-4a61-91d9-328c672b557e-0.avif)

Korzystając z `AerSimulator`, zmierzymy go w bazie obliczeniowej, aby oszacować $r_z$.

```python
# see if the expected value of measuring in the computational basis
# approaches the limit of rz
from qiskit_aer import AerSimulator
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager
from qiskit_ibm_runtime import Sampler
from qiskit.visualization import plot_histogram

# Define backend
backend = AerSimulator()
nshots = 1000  # or 10000
# nshots = 10000

# Transpile to backend
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(qc)

# Run the job
sampler = Sampler(mode=backend)
job = sampler.run([isa_qc], shots=nshots)
result = job.result()

# Extract counts data
counts = result[0].data.c.get_counts()
print(counts)

# Plot the counts in a histogram

plot_histogram(counts)
```

```text
{'1': 375, '0': 625}
```

![Wynik poprzedniej komórki kodu](/learning/images/courses/utility-scale-quantum-computing/teleportation/extracted-outputs/6d5006d5-6eac-4015-be9d-bd5f01e046b7-1.avif)

```python
rz_approx = (counts["0"] - counts["1"]) / nshots

print("rz = ", rz, " and approx of rz = ", rz_approx)
```

```text
rz =  0.2577405946274022  and approx of rz =  0.25
```

Korzystając z metody kwantowej tomografii stanu, oszacowaliśmy wartość $r_z$. W tym przypadku, ponieważ wybraliśmy parametr dla "losowego" stanu, znamy wartość $r_z$ i możemy sprawdzić naszą pracę. Ale z samej swojej natury praca w skali użyteczności nie zawsze jest tak trywialna do sprawdzenia. Więcej o sprawdzaniu wyników kwantowych omówimy później w tym kursie. Na razie po prostu zauważmy, że nasze oszacowanie było rozsądnie dokładne.
### Ćwiczenie 1: Oszacuj wartość $r_x$ {#exercise-1-estimate-r_x-value}
Przypomnijmy, że komputery kwantowe IBM&reg; mierzą wzdłuż osi $z$ (czasem mówi się "w bazie $z$" lub "w bazie obliczeniowej"). Jednak używając rotacji przed pomiarem, możemy zmierzyć rzut stanu kwantowego również na oś x. Mówiąc dokładniej, jeśli obrócimy nasz układ tak, że rzeczy, które wskazywały wzdłuż $x$, teraz wskazują wzdłuż $z$, wówczas możemy zachować ten sam sprzęt pomiarowy wzdłuż $z$, ale dowiedzieć się o stanie, który chwilę temu był wzdłuż $x$. W ten sposób większość komputerów kwantowych (oraz wszystkie komputery kwantowe IBM) wykonują pomiary wzdłuż wielu osi.

Z tym zrozumieniem spróbuj napisać kod, aby oszacować wartość $r_x,$ używając kwantowej tomografii stanu.

__Rozwiązanie__:

```python
# create a 1-qubit quantum state psi from theta, varphi parameters
qc = QuantumCircuit(1, 1)
qc.u(theta, varphi, 0.0, 0)

qc.h(0)
qc.measure(0, 0)

qc.draw(output="mpl")
```

![Wynik poprzedniej komórki kodu](/learning/images/courses/utility-scale-quantum-computing/teleportation/extracted-outputs/fcecbe31-9196-4804-8afa-e23e313880df-0.avif)

```python
# Define backend
backend = AerSimulator()
nshots = 10000

# Transpile to backend
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(qc)

# Run the job
sampler = Sampler(mode=backend)
job = sampler.run([isa_qc], shots=nshots)
result = job.result()

# Extract counts data
counts = result[0].data.c.get_counts()
print(counts)

# Plot the counts in a histogram
plot_histogram(counts)
```

```text
{'1': 5925, '0': 4075}
```

![Wynik poprzedniej komórki kodu](/learning/images/courses/utility-scale-quantum-computing/teleportation/extracted-outputs/c876f970-cd7a-43d6-8c65-b0298daa8b0b-1.avif)

```python
rx_approx = (counts["0"] - counts["1"]) / nshots

print("rx = ", rx, " and approx of rx = ", rx_approx)
```

```text
rx =  -0.1791150283307452  and approx of rx =  -0.185
```

### Ćwiczenie 2: Oszacuj wartość $r_y$ {#exercise-2-estimate-r_y-value}
Korzystając z tych samych argumentów logicznych co wcześniej, możemy obrócić układ przed pomiarem, aby dowiedzieć się o $r_y$.
Spróbuj samodzielnie napisać kod, aby oszacować wartość $r_y$ przy użyciu kwantowej tomografii stanu. Możesz zacząć od poprzedniego przykładu, ale wykonać inne rotacje. (Aby uzyskać więcej informacji o różnych bramkach, w tym ```sdg```, zapoznaj się [z dokumentacją API.](https://docs.quantum.ibm.com/api/qiskit/qiskit.circuit.library.SdgGate))

__Rozwiązanie__:

```python
# create a 1-qubit quantum state psi from theta, varphi parameters
qc = QuantumCircuit(1, 1)
qc.u(theta, varphi, 0.0, 0)

qc.sdg(0)
qc.h(0)
qc.measure(0, 0)

qc.draw(output="mpl")
```

![Wynik poprzedniej komórki kodu](/learning/images/courses/utility-scale-quantum-computing/teleportation/extracted-outputs/aad8d350-6ecd-436f-8cc2-46de5a72b1f2-0.avif)

```python
# Define backend
backend = AerSimulator()
nshots = 10000

# Transpile to backend
pm = generate_preset_pass_manager(backend=backend, optimization_level=1)
isa_qc = pm.run(qc)

# Run the job
sampler = Sampler(mode=backend)
job = sampler.run([isa_qc], shots=nshots)
result = job.result()

# Extract counts data
counts = result[0].data.c.get_counts()
print(counts)

# Plot the counts in a histogram
plot_histogram(counts)
```

```text
{'1': 9759, '0': 241}
```

![Wynik poprzedniej komórki kodu](/learning/images/courses/utility-scale-quantum-computing/teleportation/extracted-outputs/0c8397a3-f9bc-42c5-b872-caec6dc4396b-1.avif)

```python
ry_approx = (counts["0"] - counts["1"]) / nshots

print("ry = ", ry, " and approx of ry = ", ry_approx)
