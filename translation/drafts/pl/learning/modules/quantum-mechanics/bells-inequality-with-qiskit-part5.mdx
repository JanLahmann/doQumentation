### Krok 2: Zoptymalizuj swój Circuit kwantowy pod kątem uruchamiania na prawdziwym sprzęcie

Choć początkowo użyliśmy symulatora do debugowania kodu, naprawdę chcemy uruchomić obliczenia na prawdziwym sprzęcie. W końcu symulator jedynie udaje, że jest mechanicznie kwantowy, opierając się na powyższych równaniach. Gdyby symulator powiedział nam, że te równania są poprawne, niespecjalnie by nas to przekonało. Chcemy, żeby prawdziwy komputer kwantowy powiedział nam, co się dzieje! Wybierzmy więc komputer kwantowy, którego chcemy użyć. Czasami ważne może być wybranie konkretnego urządzenia o pożądanych właściwościach, ale zazwyczaj po prostu chcemy użyć tego, które jest najmniej obciążone.

Poniżej znajduje się kod do jednorazowego zapisania swoich poświadczeń. Pamiętaj, żeby usunąć te informacje z notatnika po zapisaniu ich w swoim środowisku, tak aby przypadkowo nie udostępnić swoich poświadczeń przy dzieleniu się notatnikiem. Więcej wskazówek znajdziesz w artykułach [Skonfiguruj swoje konto IBM Cloud](/guides/initialize-account) oraz [Inicjalizacja usługi w niezaufanym środowisku](/guides/cloud-setup-untrusted).

```python
from qiskit_ibm_runtime import QiskitRuntimeService

# Składnia do jednorazowego zapisania tokenu. Usuń te linie po zapisaniu poświadczeń.
# QiskitRuntimeService.save_account(channel='ibm_quantum_platform', instance = '<YOUR_IBM_INSTANCE_CRN>', token='<YOUR-API_KEY>', overwrite=True, set_as_default=True)
# service = QiskitRuntimeService(channel='ibm_quantum_platform')

# Wczytaj zapisane poświadczenia
service = QiskitRuntimeService()

backend = service.least_busy(
    operational=True, min_num_qubits=qcs[0].num_qubits, simulator=False
)
```

```python
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

target = backend.target
pm = generate_preset_pass_manager(target=target, optimization_level=3)

qcs_isa = qcs

for i in range(0, len(qcs)):
    qcs_isa[i] = pm.run(qcs[i])
    qcs_isa[i].draw(output="mpl", idle_wires=False, style="iqp")
```

```python
qcs_isa[2].draw(output="mpl", idle_wires=False, style="iqp")
```

![Output of the previous code cell](/learning/images/modules/quantum-mechanics/bells-inequality-with-qiskit/extracted-outputs/5308a428-dedb-40f4-ab46-95f58d913623-0.avif)

### Krok 3: Wykonaj zadanie na komputerach kwantowych IBM przy użyciu Runtime Primitives

Teraz, gdy zoptymalizowaliśmy nasze Circuity do uruchamiania na prawdziwym sprzęcie kwantowym i zdebugowaliśmy kod przy użyciu symulatorów, jesteśmy gotowi do zebrania statystyk z prawdziwego komputera kwantowego i rozstrzygnięcia sporu między Einsteinem a Bornem.

```python
from qiskit_ibm_runtime import SamplerV2 as Sampler
# from qiskit_ibm_runtime import Session
# sampler.options.default_shots = 1000

# Uruchom zadanie, które zwróci shots dla wszystkich 100 zestawów wartości parametrów.
# Najlepsza praktyka to użycie Session, jak pokazano poniżej. Jest to dostępne dla użytkowników Premium Plan, Flex Plan i On-Prem (IBM Quantum Platform API) Plan.
# result_list = [None] * len(qcs)
# real_counts_list = [None] * len(qcs)
# with Session(backend=backend) as session:
#    sampler = Sampler(mode=session)

#    for i in range(0, len(qcs)):
#        # Zdefiniuj primitive unified bloc (pub)
#        pub = qcs[i]
#        job = sampler.run([pub], shots=10000)
#        # Wyodrębnij wynik dla 0. PUB (w tym przykładzie jest tylko jeden PUB).
#        result_list[i] = job.result()
#        data_pub = result_list[i][0].data
#        counts = data_pub.c.get_counts()
#        real_counts_list[i] = counts
#    #   plot_histogram(counts)

# Użytkownicy Open mogą nadal przeprowadzać ten eksperyment, ale bez rezerwowania sesji,
# co oznacza możliwość wielokrotnego oczekiwania w kolejce.
from qiskit_ibm_runtime import Batch

batch = Batch(backend=backend)
sampler = Sampler(mode=batch)

result_list = [None] * len(qcs)
real_counts_list = [None] * len(qcs)

for i in range(0, len(qcs)):
    # Define the primitive unified bloc (pub)
    pub = qcs[i]
    job = sampler.run([pub], shots=10000)
    # Extract the result for the 0th pub (this example only has one pub).
    result_list[i] = job.result()
    data_pub = result_list[i][0].data
    counts = data_pub.c.get_counts()
    real_counts_list[i] = counts

# Close the batch because no context manager was used.
batch.close()
```

```python
outcomes = ("00", "01", "10", "11")

# Here we convert "None"s into 0's so that we can sum.

for i in range(0, len(qcs)):
    for j in range(0, len(outcomes)):
        if real_counts_list[i].get(outcomes[j]) is None:
            real_counts_list[i].update({outcomes[j]: 0})

# Here we create a dictionary that holds all the outcomes and sums over their appearances in each of the circuits.

real_total_counts = {}
for i in range(0, len(outcomes)):
    real_total_counts[outcomes[i]] = sum(
        real_counts_list[j].get(outcomes[i]) for j in range(0, len(qcs))
    )

print(real_total_counts)
plot_histogram(real_total_counts)
```

```text
{'00': 7542, '01': 7503, '10': 7304, '11': 7651}
```

![Output of the previous code cell](/learning/images/modules/quantum-mechanics/bells-inequality-with-qiskit/extracted-outputs/c7ee41f3-b642-4694-873f-bea0c47bacce-1.avif)

```python
# Ta składnia pozwala uruchomić zadanie na symulatorze, jeśli wyczerpałeś/aś przydzielony czas na prawdziwych komputerach kwantowych IBM.
# Jednak zdecydowanie zalecamy uruchamianie tego na prawdziwych komputerach kwantowych, ponieważ jest to sprawdzian zachowania rzeczywistych układów kwantowych.

# To używa lokalnego symulatora
# from qiskit_aer import AerSimulator

# To generuje symulator naśladujący rzeczywisty system kwantowy
# backend_sim = AerSimulator.from_backend(backend)

# Importuje Estimator, tym razem z qiskit (importujemy z Runtime dla prawdziwego sprzętu)
# from qiskit.primitives import BackendSamplerV2
# sampler = BackendSamplerV2(backend = backend_sim)

# result_list = [None] * len(qcs)
# counts_list = [None] * len(qcs)
# for i in range(0, len(qcs)):
# Define the primitive unified bloc (pub)
#    pub = qcs[i]
#    job = sampler.run([pub], shots=10000)
# Extract the result for the 0th pub (this example only has one pub).
#    result_list[i] = job.result()
#    data_pub = result_list[i][0].data
#    counts = data_pub.c.get_counts()
#    counts_list[i] = counts

# data_pubs = (result_list[0][0].data,result_list[1][0].data,result_list[2][0].data)
# outcomes = ("00", "01", "10", "11")

# Here we convert "None"s into 0's so that we can sum.

# for i in range(0, len(qcs)):
#    for j in range(0, len(outcomes)):
#        if counts_list[i].get(outcomes[j]) is None:
#            counts_list[i].update({outcomes[j]: 0})

# Here we create a dictionary that holds all the outcomes and sums over their appearances in each of the circuits.

# total_counts = {}
# for i in range(0, len(outcomes)):
#    total_counts[outcomes[i]] = sum(
#        counts_list[j].get(outcomes[i]) for j in range(0, len(qcs))
#    )

# print(total_counts)
# plot_histogram(total_counts)
```

```python
counts_list
```

```text
[None, None, None]
```
