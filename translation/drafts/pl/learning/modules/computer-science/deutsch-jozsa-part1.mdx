---
title: "Algorytm Deutscha-Jozsy"
sidebar_label: "Algorytm Deutscha-Jozsy"
description: "Dowiedz się, jak algorytm Deutscha-Jozsy wykorzystuje kwantowy równoległość i interferencję, aby osiągnąć wykładnicze przyspieszenie w porównaniu z algorytmami klasycznymi."
notebook_path: "learning/modules/computer-science/deutsch-jozsa.ipynb"
---
{/* doqumentation-source-hash: 0828194f */}

<OpenInLabBanner notebookPath="learning/modules/computer-science/deutsch-jozsa.ipynb" />



{/* cspell:ignore blackbox  Hadamards */}
# Algorytm Deutscha-Jozsy {#the-deutsch-jozsa-algorithm}

Do realizacji tego modułu Qiskit w Classrooms studenci muszą mieć działające środowisko Python z zainstalowanymi następującymi pakietami:
- `qiskit` v2.1.0 lub nowszy
- `qiskit-ibm-runtime` v0.40.1 lub nowszy
- `qiskit-aer` v0.17.0 lub nowszy
- `qiskit.visualization`
- `numpy`
- `pylatexenc`

Aby skonfigurować i zainstalować powyższe pakiety, zapoznaj się z przewodnikiem [Instalacja Qiskit](/guides/install-qiskit).
Aby uruchamiać zadania na prawdziwych komputerach kwantowych, studenci muszą założyć konto w IBM Quantum&reg;, postępując zgodnie z krokami opisanymi w przewodniku [Konfiguracja konta IBM Cloud](/guides/cloud-setup).

Ten moduł był testowany i wykorzystał cztery sekundy czasu QPU. Jest to jedynie szacunek. Twoje rzeczywiste zużycie może się różnić.

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-aer qiskit-ibm-runtime
```

```python
# Uncomment and modify this line as needed to install dependencies
#!pip install 'qiskit>=2.1.0' 'qiskit-ibm-runtime>=0.40.1' 'qiskit-aer>=0.17.0' 'numpy' 'pylatexenc'
```

Obejrzyj omówienie modułu przez dr Katie McCormick poniżej lub kliknij [tutaj](https://youtu.be/QcK0GK7DUh8?si=8e0Lmjgylxmgl2y7), aby obejrzeć je na YouTube.

-------

<IBMVideo id="134413695" title="Katie McCormick introduces one of the first quantum algorithms developed: the Deutsch algorithm and its extension, the Deutsch-Jozsa algorithm."/>
## Wprowadzenie {#intro}

Na początku lat 80. XX wieku fizycy kwantowi i informatycy mieli mgliste przeczucie, że mechanika kwantowa mogłaby zostać wykorzystana do wykonywania obliczeń znacznie potężniejszych niż te, które są możliwe na klasycznych komputerach. Ich rozumowanie było następujące: klasycznemu komputerowi trudno jest symulować układy kwantowe, ale *kwantowy* komputer powinien być w stanie robić to wydajniej. A jeśli komputer kwantowy mógłby efektywniej symulować układy kwantowe, być może istniały też inne zadania, które mógłby wykonywać sprawniej niż komputer klasyczny.

Logika była słuszna, jednak szczegóły wymagały doprecyzowania. Nastąpiło to w 1985 roku, gdy David Deutsch opisał pierwszy „uniwersalny komputer kwantowy". W tej samej pracy podał pierwszy przykład problemu, który komputer kwantowy może rozwiązać wydajniej niż klasyczny. Ten pierwszy przykład zabawkowy jest dziś znany jako „algorytm Deutscha". Poprawa w algorytmie Deutscha była skromna, ale kilka lat później Deutsch współpracował z Richardem Jozsą, by jeszcze bardziej poszerzyć przepaść między komputerami klasycznymi a kwantowymi.

Te algorytmy — Deutscha oraz rozszerzenie Deutscha-Jozsy — nie są szczególnie użyteczne, ale wciąż mają ogromne znaczenie z kilku powodów:

1. Z historycznego punktu widzenia były jednymi z pierwszych algorytmów kwantowych, które udowodniono jako lepsze od swoich klasycznych odpowiedników. Ich zrozumienie może pomóc zrozumieć, jak z biegiem czasu ewoluowało myślenie społeczności o obliczeniach kwantowych.
2. Pomagają zrozumieć pewne aspekty odpowiedzi na zaskakująco subtelne pytanie: Co daje obliczeniom kwantowym ich moc? Czasami komputery kwantowe porównuje się do ogromnych, wykładniczo skalujących się równoległych procesorów. Ale to nie do końca prawda. Choć część odpowiedzi na to pytanie tkwi w tak zwanej „kwantowej równoległości", wydobycie jak największej ilości informacji w jednym przebiegu jest subtelną sztuką. Algorytmy Deutscha i Deutscha-Jozsy pokazują, jak można to osiągnąć.

W tym module poznamy algorytm Deutscha, algorytm Deutscha-Jozsy i to, czego uczą nas o mocy obliczeń kwantowych.
## Kwantowa równoległość i jej ograniczenia {#quantum-parallelism-and-its-limits}

Część mocy obliczeń kwantowych pochodzi z „kwantowej równoległości", czyli zasadniczo zdolności do wykonywania operacji na wielu danych wejściowych jednocześnie, ponieważ stany wejściowe Qubitów mogą znajdować się w superpozycji wielu klasycznie dozwolonych stanów. JEDNAK mimo że Circuit kwantowy może być w stanie ewaluować wiele stanów wejściowych naraz, wydobycie wszystkich tych informacji za jednym razem jest niemożliwe.

Aby zrozumieć, co mam na myśli, powiedzmy, że mamy bit $x$ oraz pewną funkcję zastosowaną do tego bitu, $f(x)$. Istnieją cztery możliwe funkcje binarne przekształcające jeden bit w inny pojedynczy bit:

| $x$    |  $f_1(x)$ | $f_2(x)$ | $f_3(x)$ | $f_4(x)$ |
| ----------- | ------- |-------| -------- | ------- |
| 0  | 0   | 0  | 1   | 1 |
| 1  | 0   | 1  | 0 | 1 |

Chcemy dowiedzieć się, którą z tych funkcji (1–4) jest nasze $f(x)$. Klasycznie musielibyśmy uruchomić funkcję dwukrotnie — raz dla $x=0$, raz dla $x=1$. Zobaczmy jednak, czy da się to zrobić lepiej za pomocą Circuit kwantowego. O funkcji możemy się dowiedzieć, korzystając z poniższego Gate:

![quantum_parallelism](/learning/images/modules/computer-science/deutsch-jozsa/quantum-parallelism.avif)

Tutaj Gate $U_f$ oblicza $f(x)$, gdzie $x$ jest stanem Qubitu 0, i aplikuje to do Qubitu 1. Tak więc stan wynikowy $|x\rangle|y\oplus f(x)\rangle$ po prostu staje się $|x\rangle|f(x)\rangle$, gdy $|y\rangle = |0\rangle$. Zawiera to wszystkie informacje potrzebne do poznania funkcji $f(x)$: Qubit 0 mówi nam, czym jest $x$, a Qubit 1 — czym jest $f(x)$. Jeśli więc zainicjalizujemy $|x\rangle = \frac{1}{\sqrt{2}}(|0\rangle+|1\rangle)$, to końcowy stan obu Qubitów będzie wynosił: $|y\rangle|x\rangle = \frac{1}{\sqrt{2}}(|f(0)\rangle|0\rangle+|f(1)\rangle|1\rangle)$. Ale jak uzyskać dostęp do tej informacji?

### 2.1. Wypróbuj w Qiskit: {#21-try-it-on-qiskit}

Używając Qiskit, losowo wybierzemy jedną z czterech możliwych funkcji powyżej i uruchomimy Circuit. Twoim zadaniem jest wykorzystanie pomiarów Circuit kwantowego do poznania funkcji w jak najmniejszej liczbie uruchomień.

W tym pierwszym eksperymencie i przez cały moduł będziemy korzystać z frameworku obliczeń kwantowych zwanego „wzorcami Qiskit" (Qiskit patterns), który dzieli przepływy pracy na następujące kroki:

- Krok 1: Mapowanie klasycznych danych wejściowych na problem kwantowy
- Krok 2: Optymalizacja problemu pod kątem wykonania kwantowego
- Krok 3: Wykonanie za pomocą Qiskit Runtime Primitives
- Krok 4: Post-processing i klasyczna analiza

Zacznijmy od załadowania niezbędnych pakietów, w tym prymitywów Qiskit Runtime. Wybierzemy też najmniej obciążony dostępny komputer kwantowy.

Poniżej znajduje się kod do zapisania twoich danych uwierzytelniających przy pierwszym użyciu. Pamiętaj, aby usunąć te informacje z notebooka po zapisaniu ich w swoim środowisku, by twoje dane uwierzytelniające nie zostały przypadkowo udostępnione podczas dzielenia się notebookiem. Więcej wskazówek znajdziesz w artykułach [Konfiguracja konta IBM Cloud](/guides/initialize-account) i [Inicjalizacja usługi w środowisku niezaufanym](/guides/cloud-setup-untrusted).

```python
# Load the Qiskit Runtime service
from qiskit_ibm_runtime import QiskitRuntimeService

# Load the Runtime primitive and session
from qiskit_ibm_runtime import SamplerV2 as Sampler

# Syntax for first saving your token.  Delete these lines after saving your credentials.
# QiskitRuntimeService.save_account(channel='ibm_quantum_platform', instance = '<YOUR_IBM_INSTANCE_CRN>', token='<YOUR_API_KEY>', overwrite=True, set_as_default=True)
# service = QiskitRuntimeService(channel='ibm_quantum_platform')

# Load saved credentials
service = QiskitRuntimeService()

# Use the least busy backend, or uncomment the loading of a specific backend like "ibm_brisbane".
# backend = service.least_busy(operational=True, simulator=False, min_num_qubits = 127)
backend = service.backend("ibm_brisbane")
print(backend.name)

sampler = Sampler(mode=backend)
```

```text
ibm_brisbane
```

Poniższa komórka pozwoli ci przełączać się między korzystaniem z symulatora a prawdziwym sprzętem w trakcie notebooka. Zalecamy uruchomienie jej teraz:

```python
# Load the backend sampler
from qiskit.primitives import BackendSamplerV2

# Load the Aer simulator and generate a noise model based on the currently-selected backend.
from qiskit_aer import AerSimulator
from qiskit_aer.noise import NoiseModel

# Alternatively, load a fake backend with generic properties and define a simulator.

noise_model = NoiseModel.from_backend(backend)

# Define a simulator using Aer, and use it in Sampler.
backend_sim = AerSimulator(noise_model=noise_model)
sampler_sim = BackendSamplerV2(backend=backend_sim)

# You could also define a simulator-based sampler using a generic backend:
# backend_gen = GenericBackendV2(num_qubits=18)
# sampler_gen = BackendSamplerV2(backend=backend_gen)
```

Po załadowaniu niezbędnych pakietów możemy przejść do przepływu pracy według wzorców Qiskit. W kroku mapowania poniżej najpierw tworzymy funkcję wybierającą spośród czterech możliwych funkcji przekształcających jeden bit w inny pojedynczy bit.

```python
# Step 1: Map

from qiskit import QuantumCircuit

qc = QuantumCircuit(2)

def twobit_function(case: int):
    """
    Generate a valid two-bit function as a `QuantumCircuit`.
    """
    if case not in [1, 2, 3, 4]:
        raise ValueError("`case` must be 1, 2, 3, or 4.")

    f = QuantumCircuit(2)
    if case in [2, 3]:
        f.cx(0, 1)
    if case in [3, 4]:
        f.x(1)
    return f

# first, convert oracle circuit (above) to a single gate for drawing purposes. otherwise, the circuit is too large to display
# blackbox = twobit_function(2).to_gate()  # you may edit the number inside "twobit_function()" to select among the four valid functions
# blackbox.label = "$U_f$"

qc.h(0)
qc.barrier()
qc.compose(twobit_function(2), inplace=True)
qc.measure_all()

qc.draw("mpl")
```

![Output of the previous code cell](/learning/images/modules/computer-science/deutsch-jozsa/extracted-outputs/5e67183b-42b9-44c2-bd4b-b5e2d192a796-0.avif)

W powyższym Circuit Gate Hadamarda „H" przenosi Qubit 0, który początkowo jest w stanie $|0\rangle$, do stanu superpozycji $\frac{1}{\sqrt{2}}(|0\rangle+|1\rangle)$. Następnie $U_f$ ewaluuje funkcję $f(x)$ i aplikuje jej wynik do Qubitu 1.

Teraz musimy zoptymalizować i transpilować Circuit do uruchomienia na komputerze kwantowym:

```python
# Step 2: Transpile
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

target = backend.target
pm = generate_preset_pass_manager(target=target, optimization_level=3)

qc_isa = pm.run(qc)
```

Na koniec uruchamiamy nasz transpilowany Circuit na komputerze kwantowym i wizualizujemy wyniki:

```python
# Step 3: Run the job on a real quantum computer

job = sampler.run([qc_isa], shots=1)
# job = sampler_sim.run([qc_isa],shots=1) # uncomment this line to run on simulator instead
res = job.result()
counts = res[0].data.meas.get_counts()
```

```python
# Step 4: Visualize and analyze results

## Analysis
from qiskit.visualization import plot_histogram

plot_histogram(counts)
```

![Output of the previous code cell](/learning/images/modules/computer-science/deutsch-jozsa/extracted-outputs/6d2904cc-c730-4dca-a167-438018230299-0.avif)

Powyżej widnieje histogram naszych wyników. W zależności od liczby pomiarów (shots), którą wybrałeś(-aś) do uruchomienia Circuit w kroku 3 powyżej, możesz zobaczyć jeden lub dwa słupki reprezentujące zmierzone stany obu Qubitów w każdym przebiegu. Jak zawsze w Qiskit i w tym notebooku, używamy notacji „little endian", co oznacza, że stany Qubitów od 0 do n są zapisywane w kolejności rosnącej od prawej do lewej, więc Qubit 0 jest zawsze skrajnie po prawej.

Ponieważ Qubit 0 był w stanie superpozycji, Circuit ewaluował funkcję dla *obu* wartości $x=0$ i $x=1$ *jednocześnie* — czego klasyczne komputery nie potrafią! Jednak haczyk pojawia się, gdy chcemy dowiedzieć się czegoś o funkcji $f(x)$ — gdy mierzymy Qubity, ich stan ulega kolapsowi. Jeśli wybierzesz „shots = 1", aby uruchomić Circuit tylko raz, zobaczysz tylko jeden słupek w histogramie powyżej, a twoje informacje o funkcji będą niepełne.

### Sprawdź swoje zrozumienie {#check-your-understanding}
Przeczytaj poniższe pytanie(-a), zastanów się nad odpowiedzią, a następnie kliknij trójkąt, aby ujawnić rozwiązanie.
<details>
<summary>

Ile razy musimy uruchomić powyższy algorytm, aby poznać funkcję $f(x)$? Czy jest to lepsze niż w przypadku klasycznym? Czy wolisz mieć klasyczny czy kwantowy komputer, aby rozwiązać ten problem?

</summary>

__Odpowiedź:__

Ponieważ pomiar spowoduje kolaps superpozycji i zwróci tylko jedną wartość, musimy uruchomić Circuit *przynajmniej* dwa razy, aby uzyskać oba wyjścia funkcji $f(0)$ i $f(1)$. W najlepszym przypadku działa to tak samo jak w przypadku klasycznym, gdzie obliczamy zarówno $f(0)$, jak i $f(1)$ w pierwszych dwóch zapytaniach. Jednak istnieje szansa, że będziemy musieli uruchomić go więcej niż dwa razy, ponieważ końcowy pomiar jest probabilistyczny i może zwrócić tę samą wartość $f(x)$ za pierwszym i drugim razem. W tym przypadku wolałbym(-abym) mieć klasyczny komputer.

</details>

Tak więc, choć kwantowa równoległość może być potężna, gdy jest właściwie wykorzystana, nieprawdą jest twierdzenie, że komputer kwantowy działa jak ogromny, klasyczny procesor równoległy. Akt pomiaru powoduje kolaps stanów kwantowych, więc możemy uzyskać dostęp tylko do jednego wyjścia obliczenia.
## Algorytm Deutscha {#deutschs-algorithm}

Podczas gdy sama kwantowa równoległość nie daje nam przewagi nad komputerami klasycznymi, możemy ją połączyć z innym zjawiskiem kwantowym — interferencją — aby osiągnąć przyspieszenie. Algorytm znany dziś jako „algorytm Deutscha" jest pierwszym przykładem algorytmu, który to osiąga.

### Problem {#the-problem}

Problem brzmiał następująco:

Mając dany bit wejściowy $x = \{0,1\}$ oraz wejściową funkcję $f(x) = \{0,1\}$, określ, czy funkcja jest *zrównoważona* (balanced) czy *stała* (constant). To znaczy, jeśli jest zrównoważona, to wynik funkcji wynosi 0 przez połowę czasu i 1 przez drugą połowę. Jeśli jest stała, wynik funkcji to albo zawsze 0, albo zawsze 1. Przypomnij sobie tabelę czterech możliwych funkcji przekształcających jeden bit w inny pojedynczy bit:

| $x$    |  $f_1(x)$ | $f_2(x)$ | $f_3(x)$ | $f_4(x)$ |
| ----------- | ------- |-------| -------- | ------- |
| 0  | 0   | 0  | 1   | 1 |
| 1  | 0   | 1  | 0 | 1 |

Pierwsza i ostatnia funkcja, $f_1(x)$ i $f_4(x)$, są stałe, natomiast dwie środkowe, $f_2(x)$ i $f_3(x)$, są zrównoważone.
### Algorytm {#the-algorithm}

Podejście Deutscha do tego problemu polegało na zastosowaniu „modelu zapytań" (query model). W modelu zapytań funkcja wejściowa ($f_i(x)$ powyżej) jest zawarta w „czarnej skrzynce" — nie mamy bezpośredniego dostępu do jej zawartości, ale możemy wysyłać zapytania do czarnej skrzynki, a ona zwróci nam wynik funkcji. Mówimy niekiedy, że „wyrocznia" (oracle) dostarcza tych informacji. Więcej o modelu zapytań znajdziesz w [Lekcji 1: Kwantowe Algorytmy Zapytań](/learning/courses/fundamentals-of-quantum-algorithms/quantum-query-algorithms/introduction) kursu Podstawy Algorytmów Kwantowych.

Aby określić, czy algorytm kwantowy jest bardziej wydajny niż klasyczny w modelu zapytań, możemy po prostu porównać liczbę zapytań, które musimy wykonać do czarnej skrzynki w każdym przypadku. W przypadku klasycznym, aby stwierdzić, czy funkcja zawarta w czarnej skrzynce jest zrównoważona czy stała, musielibyśmy zapytać skrzynkę dwa razy, aby uzyskać zarówno $f(0)$, jak i $f(1)$.

W kwantowym algorytmie Deutscha znalazł on jednak sposób na uzyskanie informacji za pomocą tylko jednego zapytania! Dokonał jednej modyfikacji powyższego Circuit „kwantowej równoległości": przygotował stan superpozycji na *obu* Qubitach, a nie tylko na Qubicie 0. Następnie dwa wyjścia funkcji, $f(0)$ i $f(1)$, interferowały, zwracając 0, jeśli oba wynosiły 0 lub oba wynosiły 1 (funkcja była stała), i zwracając 1, jeśli były różne (funkcja była zrównoważona). W ten sposób Deutsch mógł odróżnić funkcję stałą od zrównoważonej za pomocą jednego zapytania.

Oto diagram Circuit algorytmu Deutscha:

![Circuit diagram of Deutsch's algorithm](/learning/images/modules/computer-science/deutsch-jozsa/Deutsch_algo.avif)

Aby zrozumieć, jak działa ten algorytm, przyjrzyjmy się stanom kwantowym Qubitów w trzech punktach zaznaczonych na powyższym diagramie. Spróbuj samodzielnie wyznaczyć stany przed kliknięciem, aby zobaczyć odpowiedzi:

### Sprawdź swoje zrozumienie {#check-your-understanding-2}
