
    # Etykiety i tytuł
    ax.set_xlabel("X")
    ax.set_ylabel("Y")
    ax.set_zlabel("Z")
    ax.set_title("Parameterized 1-Qubit Circuit on 3D Bloch Sphere")

    # Ustaw limity osi i wyrównaj je
    ax.set_xlim([-1, 1])
    ax.set_ylim([-1, 1])
    ax.set_zlim([-1, 1])

    # Zapewnij równy stosunek proporcji dla wszystkich osi
    ax.set_box_aspect([1, 1, 1])  # Równe skalowanie dla osi x, y, z

    # Pokaż siatkę
    ax.grid(True)

    plt.show()

num_samples = 5000  # Liczba losowych stanów
theta_vals = np.random.uniform(0, 2 * np.pi, num_samples)
phi_vals = np.random.uniform(0, 2 * np.pi, num_samples)
lam_vals = np.random.uniform(0, 2 * np.pi, num_samples)
```

Sprawdźmy, jak działa nasz pierwszy ansatz.

```python
# Lista do przechowywania współrzędnych XZ sfery Blocha
bloch_vectors = []

# Generuj stany kwantowe i wyodrębnij wektory Blocha
for i in range(num_samples):
    # Utwórz Circuit i przypisz parametry
    qc = ansatz1
    bound_qc = qc.assign_parameters({theta: theta_vals[i]})  # , lam: lam_vals[i]})
    state = Statevector.from_instruction(bound_qc)
    rho = DensityMatrix(state)

    X = rho.expectation_value(Pauli("X")).real
    Y = rho.expectation_value(Pauli("Y")).real
    Z = rho.expectation_value(Pauli("Z")).real
    bloch_vectors.append([X, Y, Z])  # Zapisz komponenty X, Z

# Konwertuj do tablicy numpy w celu wykreślenia
bloch_vectors = np.array(bloch_vectors)

plot_bloch(bloch_vectors)
```

![Wynik poprzedniej komórki kodu](/learning/images/modules/computer-science/vqe/extracted-outputs/f7d886a0-d410-418d-a7a3-81a9134cbfd0-0.avif)

Możemy zauważyć, że nasz pierwszy ansatz zwraca stany kwantowe rozmieszczone w kształcie pierścienia na sferze Blocha. Ma to sens, ponieważ podaliśmy ansatzowi tylko jeden parametr obrotu. Może więc generować wyłącznie stany obrócone wokół jednej osi. Startując z punktu $(0,0,1)$ i obracając się wokół jednej osi, zawsze otrzymamy pierścień. Sprawdźmy teraz nasz drugi ansatz, który posiada dwa ortogonalne Gate'y obrotowe — `Rx` i `Rz`.

```python
bloch_vectors = []

# Generuj stany kwantowe i wyodrębnij wektory Blocha
for i in range(num_samples):
    # Utwórz Circuit i przypisz parametry
    qc = ansatz2
    bound_qc = qc.assign_parameters(
        {theta: theta_vals[i], phi: phi_vals[i]}
    )  # , lam: lam_vals[i]})
    state = Statevector.from_instruction(bound_qc)
    rho = DensityMatrix(state)

    X = rho.expectation_value(Pauli("X")).real
    Y = rho.expectation_value(Pauli("Y")).real
    Z = rho.expectation_value(Pauli("Z")).real
    bloch_vectors.append([X, Y, Z])  # Zapisz komponenty X, Z

# Konwertuj do tablicy numpy w celu wykreślenia
bloch_vectors = np.array(bloch_vectors)

plot_bloch(bloch_vectors)
```

![Wynik poprzedniej komórki kodu](/learning/images/modules/computer-science/vqe/extracted-outputs/607e4c0f-cb14-4036-901b-021cc8e11b79-0.avif)

Tutaj widać, że nasz drugi ansatz pokrywa większą część sfery Blocha — zauważ jednak, że punkty są gęściej skupione wokół biegunów, a bardziej rozproszone wokół równika. Czas sprawdzić nasz ostatni ansatz.

```python
bloch_vectors = []

# Generuj stany kwantowe i wyodrębnij wektory Blocha
for i in range(num_samples):
    # Utwórz Circuit i przypisz parametry
    qc = ansatz3
    bound_qc = qc.assign_parameters(
        {theta: theta_vals[i], phi: phi_vals[i], lam: lam_vals[i]}
    )
    state = Statevector.from_instruction(bound_qc)
    rho = DensityMatrix(state)

    X = rho.expectation_value(Pauli("X")).real
    Y = rho.expectation_value(Pauli("Y")).real
    Z = rho.expectation_value(Pauli("Z")).real
    bloch_vectors.append([X, Y, Z])  # Zapisz komponenty X, Z

# Konwertuj do tablicy numpy w celu wykreślenia
bloch_vectors = np.array(bloch_vectors)

plot_bloch(bloch_vectors)
```

![Wynik poprzedniej komórki kodu](/learning/images/modules/computer-science/vqe/extracted-outputs/21ff021a-7cfe-4ba9-a827-edac71c0871c-0.avif)

Tutaj widać równomierniej rozmieszczone stany kwantowe generowane przez nasz ostatni ansatz.

Jak wspomniano, najlepszym podejściem jest zdobycie wiedzy o stanie podstawowym, którego szukamy, i użycie ansatzu dobrze dopasowanego do badania stanów bliskich temu stanowi podstawowemu. Na przykład gdybyśmy wiedzieli, że nasz stan podstawowy leży blisko bieguna, moglibyśmy wybrać ansatz 2. Dla uproszczenia zostaniemy przy ansatzu 3, który równomiernie bada całą sferę Blocha.

Teraz, gdy wybraliśmy nasz ansatz, narysujmy Circuit.

```python
# Wstępnie zdefiniowany Circuit ansatzu i klasa operatora dla Hamiltonianu

ansatz = ansatz3

num_params = ansatz.num_parameters
print("This circuit has ", num_params, "parameters")

ansatz.draw("mpl", style="iqp")
```

```text
This circuit has  3 parameters
```

![Wynik poprzedniej komórki kodu](/learning/images/modules/computer-science/vqe/extracted-outputs/20221e72-51dc-49d9-8a09-9b64de7f535f-1.avif)

### Krok 2: Optymalizacja pod kątem docelowego sprzętu {#step-2-optimize-for-target-hardware}

Podczas uruchamiania obliczeń na prawdziwym komputerze kwantowym nie interesuje nas wyłącznie logika Circuit kwantowego. Ważne są też takie kwestie, jak to, jakie operacje może wykonać dany komputer kwantowy, oraz gdzie na tym komputerze znajdują się używane przez nas Qubity — czy są bezpośrednio obok siebie, czy daleko od siebie? Dlatego kolejnym krokiem jest przepisanie naszego Circuit z użyciem Gate'ów natywnych dla komputera kwantowego, którego będziemy używać, z uwzględnieniem rozkładu Qubitów. Można to osiągnąć przez `transpilację` — po tym procesie możesz zobaczyć, jak nasz prosty ansatz zostaje przekształcony w inny zestaw Gate'ów, a nasze abstrakcyjne Qubity zostaną zmapowane na fizyczne Qubity prawdziwego komputera kwantowego.

```python
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

config = backend.configuration()

print("Backend: {config.backend_name}")
print("Native gates: ", config.supported_instructions, ",")

target = backend.target

pm = generate_preset_pass_manager(target=target, optimization_level=3)

ansatz_isa = pm.run(ansatz)

ansatz_isa.draw(output="mpl", idle_wires=False, style="iqp")
```

```text
Backend: {config.backend_name}
Native gates:  ['ecr', 'id', 'delay', 'measure', 'reset', 'rz', 'sx', 'x'] ,
```

![Wynik poprzedniej komórki kodu](/learning/images/modules/computer-science/vqe/extracted-outputs/b5e08232-d651-4df4-bcb7-533513144eff-1.avif)

Widać, że Gate'y `rx, rz` naszego ansatzu zostały przekształcone w serię Gate'ów `rz, sx`, które są natywnymi Gate'ami naszego Backend. Można też zauważyć, że nasz `q0` jest teraz zmapowany na piąty fizyczny Qubit. Musimy również zmapować nasz Hamiltonian zgodnie z tymi zmianami, tak jak pokazano w poniższym kodzie:

```python
Hamiltonian_isa = Hamiltonian.apply_layout(layout=ansatz_isa.layout)
```

### Krok 3: Wykonanie na docelowym sprzęcie {#step-3-execute-on-target-hardware}

Teraz czas uruchomić nasze VQE na prawdziwym QPU. W tym celu potrzebujemy najpierw funkcji kosztu dla procesu optymalizacji, która oblicza wartość oczekiwaną Hamiltonianu dla stanu kwantowego generowanego przez ansatz. Nie martw się! Nie musisz kodować wszystkiego samodzielnie. Przygotowaliśmy dla ciebie funkcję — wystarczy, że uruchomisz poniższą komórkę.

```python
def cost_func(params, ansatz, hamiltonian, estimator):
    """Return estimate of energy from estimator

    Parameters:
        params (ndarray): Array of ansatz parameters
        ansatz (QuantumCircuit): Parameterized ansatz circuit
        hamiltonian (SparsePauliOp): Operator representation of Hamiltonian
        estimator (EstimatorV2): Estimator primitive instance
        cost_history_dict: Dictionary for storing intermediate results

    Returns:
        float: Energy estimate
    """
    pub = (ansatz, [hamiltonian], [params])
    result = estimator.run(pubs=[pub]).result()
    energy = result[0].data.evs[0]

    cost_history_dict["iters"] += 1
    cost_history_dict["prev_vector"] = params
    cost_history_dict["cost_history"].append(energy)
    print(f"Iters. done: {cost_history_dict['iters']} [Current cost: {energy}]")

    return energy
```

Na koniec przygotowujemy parametry początkowe dla naszego ansatzu i procesu optymalizacji. Możesz po prostu użyć samych zer lub wartości losowych. Poniżej wybraliśmy konkretne parametry początkowe, ale możesz swobodnie zakomentować lub odkomentować linie w komórce, aby losować parametry równomiernie z przedziału od 0 do $2\pi$.

```python
# x0 = np.random.uniform(0, 2*pi, 3)
x0 = [1, 1, 0]
```

```python
# QPU Est. 2min for ibm_brisbane

from scipy.optimize import minimize
from qiskit_ibm_runtime import Batch

batch = Batch(backend=backend)

cost_history_dict = {
    "prev_vector": None,
    "iters": 0,
    "cost_history": [],
}
estimator = Estimator(mode=batch)
estimator.options.default_shots = 10000

res = minimize(
