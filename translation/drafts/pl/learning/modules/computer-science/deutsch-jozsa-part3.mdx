else:
    print("balanced")  # anything other than all zeroes means the function is balanced.
```

```text
{'110': 1}
balanced
```

Powyżej pierwszy wiersz wyniku to ciąg bitów wyników pomiarów. Drugi wiersz pokazuje, czy ten ciąg bitów sugeruje, że funkcja była zrównoważona, czy stała. Jeśli ciąg bitów zawierał same zera, funkcja była stała; jeśli nie — była zrównoważona. Tak więc, wykonując tylko jeden przebieg powyższego Circuit kwantowego, możemy ustalić, czy funkcja jest stała, czy zrównoważona!

### Sprawdź swoje rozumienie {#check-your-understanding}
Przeczytaj poniższe pytania, zastanów się nad odpowiedziami, a następnie kliknij trójkąty, aby ujawnić rozwiązania.

<details>
<summary>

Ile zapytań potrzebowałby klasyczny komputer, żeby z całkowitą pewnością (100%) stwierdzić, czy dana funkcja jest stała, czy zrównoważona? Pamiętaj, że klasycznie jedno zapytanie pozwala na zastosowanie funkcji tylko do jednego ciągu bitów.

</summary>

__Odpowiedź:__

Istnieje $2^n$ możliwych ciągów bitów do sprawdzenia, a w najgorszym przypadku trzeba by przetestować $2^n/2+1$ z nich. Na przykład, jeśli funkcja byłaby stała, a ty ciągle mierzysz „1" jako wynik funkcji, nie możesz być pewny/a, że jest ona naprawdę stała, dopóki nie sprawdzisz ponad połowy wyników. Do tego momentu możesz mieć po prostu pecha i stale mierzyć „1" na zrównoważonej funkcji. To jak rzucanie monetą w kółko i lądowanie za każdym razem na orle. Jest to mało prawdopodobne, ale nie niemożliwe.

</details>

<details>
<summary>

Jak zmieniłaby się twoja powyższa odpowiedź, gdybyś musiał/a mierzyć tylko do momentu, aż jeden wynik (zrównoważony lub stały) stanie się bardziej prawdopodobny niż drugi? Ile zapytań byłoby potrzebnych w tym przypadku?

</summary>

__Odpowiedź:__

W tym przypadku wystarczyłoby zmierzyć dwa razy. Jeśli dwa pomiary dają różne wyniki, wiesz, że funkcja jest zrównoważona. Jeśli dwa pomiary dają ten sam wynik, funkcja może być zrównoważona lub stała. Prawdopodobieństwo, że jest zrównoważona przy tym zestawie pomiarów, wynosi: $\frac{1}{2}\frac{2^n /2 - 1}{2^n-1}$. Jest to mniej niż 1/2, więc w tym przypadku bardziej prawdopodobne jest, że funkcja jest stała.

</details>

Tak więc algorytm Deutscha–Jozsy zademonstrował wykładnicze przyspieszenie względem *deterministycznego* algorytmu klasycznego (takiego, który zwraca odpowiedź z 100% pewnością), ale nie wykazał znaczącego przyspieszenia względem *probabilistycznego* (takiego, który zwraca wynik, który *najprawdopodobniej* jest poprawny).

### Problem Bernsteina–Vazirانiego {#the-bernstein-vazirani-problem}

W 1997 roku Ethan Bernstein i Umesh Vazirani wykorzystali algorytm Deutscha–Jozsy do rozwiązania bardziej szczegółowego, ograniczonego problemu w porównaniu z problemem Deutscha–Jozsy. Zamiast jedynie rozróżniać między dwiema różnymi klasami funkcji, jak w przypadku D-J, Bernstein i Vazirani użyli algorytmu Deutscha–Jozsy do faktycznego nauczenia się ciągu zakodowanego w funkcji. Oto ten problem:

Funkcja $f:\{0,1\}^n \rightarrow \{0,1\}$ nadal przyjmuje $n$-bitowy ciąg i zwraca pojedynczy bit. Jednak teraz, zamiast obiecywać, że funkcja jest zrównoważona lub stała, mamy gwarancję, że funkcja jest iloczynem skalarnym wektora wejściowego $x$ i pewnego tajnego $n$-bitowego ciągu $s$, modulo 2. (Ten iloczyn skalarny modulo 2 nazywany jest „binarnym iloczynem skalarnym".) Zadanie polega na ustaleniu, jaki jest tajny $n$-bitowy ciąg.

Innymi słowy, dana jest nam czarnoskrzynkowa funkcja $f: {0,1}^n \rightarrow {0,1}$ spełniająca $f(x) = s \cdot x$ dla pewnego ciągu $s$, a my chcemy poznać ciąg $s$.

Zobaczmy, jak algorytm D-J rozwiązuje ten problem:

1. Najpierw Gate Hadamarda jest stosowana do $n$ Qubitów wejściowych, a Gate NOT oraz Hadamarda do wyjściowego Qubitu, tworząc stan:

$$
|\Psi\rangle = |-\rangle_{n} \otimes |+\rangle_{n-1} \otimes |+\rangle_{n-2} \otimes ... \otimes |+\rangle_0
$$

   Stan Qubitów od 1 do $n$ można zapisać prościej jako sumę po wszystkich $2^n$ stanach bazowych $n$-Qubitów $|00...00\rangle, |00...01\rangle, |000...11\rangle, ..., |111...11\rangle$. Zbiór tych stanów bazowych oznaczamy $\Sigma^n$. (Więcej szczegółów znajdziesz w kursie [Podstawy algorytmów kwantowych](/learning/courses/fundamentals-of-quantum-algorithms/quantum-query-algorithms/deutsch-jozsa-algorithm).)

$$
|\Psi\rangle = |-\rangle \otimes \frac{1}{\sqrt{2^n}}\sum\limits_{x \in \Sigma^n}{|x\rangle}
$$

2. Następnie Gate $U_f$ jest stosowana do Qubitów. Gate ta przyjmuje pierwsze n Qubitów jako wejście (które są teraz w równej superpozycji wszystkich możliwych $n$-bitowych ciągów) i stosuje funkcję $f(x)=s \cdot x$ do wyjściowego Qubitu, tak że ten Qubit jest teraz w stanie: $ |- \oplus f(x)\rangle$. Dzięki mechanizmowi phase kickback stan tego Qubitu pozostaje niezmieniony, ale niektóre składniki stanu Qubitów wejściowych nabierają znaku minus:

$$
|\Psi\rangle = |-\rangle \otimes \frac{1}{\sqrt{2^n}}\sum\limits_{x \in \Sigma^n}{(-1)^{f(x)}|x\rangle}
$$

3. Teraz kolejna warstwa Gate Hadamarda jest stosowana do Qubitów od 0 do $n-1$. Śledzenie znaków minus w tym przypadku może być trudne. Pomocne jest wiedząc, że zastosowanie warstwy Gate Hadamarda do $n$ Qubitów w standardowym stanie bazowym $|x\rangle$ można zapisać jako:

$$
H^{\otimes n} |x\rangle = \frac{1}{\sqrt{2^n}}\sum\limits_{y \in \Sigma^n}{(-1)^{x \cdot y}|y\rangle}
$$

Zatem stan staje się:

$$
|\Psi\rangle = |-\rangle \otimes \frac{1}{2^n}\sum\limits_{x \in \Sigma^n}\sum\limits_{y \in \Sigma^n}{(-1)^{(s \cdot x) + (x \cdot y)}|y\rangle}
$$

4. Kolejny krok to pomiar pierwszych $n$ bitów. Ale co zmierzymy? Okazuje się, że powyższy stan upraszcza się do: $|\Psi\rangle = |-\rangle \otimes |s\rangle$, ale jest to dalekie od oczywistości. Jeśli chcesz prześledzić matematykę, zajrzyj do kursu [Podstawy algorytmów kwantowych](/learning/courses/fundamentals-of-quantum-algorithms/quantum-query-algorithms/deutsch-jozsa-algorithm#the-bernstein-vazirani-problem) Johna Watrusa. Sedno jest jednak takie, że mechanizm phase kickback powoduje, że Qubity wejściowe znajdą się w stanie $|s\rangle$. Żeby więc dowiedzieć się, jaki był tajny ciąg $s$, wystarczy zmierzyć Qubity!

### Sprawdź swoje rozumienie {#check-your-understanding-bv}
Przeczytaj poniższe pytania, zastanów się nad odpowiedziami, a następnie kliknij trójkąty, aby ujawnić rozwiązania.

<details>
<summary>

Zweryfikuj, że stan z Kroku 3 powyżej jest rzeczywiście stanem $|s\rangle$ dla szczególnego przypadku $n=1$.

</summary>

__Odpowiedź:__

Gdy jawnie wypisujemy dwie sumy, powinniśmy otrzymać stan z czterema składnikami (pomińmy na chwilę stan wyjściowy $|-\rangle$):

$$
|\Psi\rangle = \frac{1}{2}[|0\rangle + (-1)^s |0\rangle + |1\rangle + (-1)^{(s+1)} |1\rangle]
$$

Jeśli $s=0$, pierwsze dwa składniki dodają się konstruktywnie, a ostatnie dwa znoszą się, pozostawiając $|\Psi\rangle = |0\rangle$. Jeśli $s=1$, ostatnie dwa składniki dodają się konstruktywnie, a pierwsze dwa znoszą się, pozostawiając $|\Psi\rangle = |1\rangle$. Tak więc w obu przypadkach $|\Psi\rangle = |s\rangle$. Mamy nadzieję, że ten najprostszy przypadek daje ci intuicję co do tego, jak działa przypadek ogólny z $n$ Qubitami: wszystkie składniki różne od $|s\rangle$ interferują destruktywnie, pozostawiając tylko stan $|s\rangle$.

</details>

<details>
<summary>
Jak ten sam algorytm może rozwiązywać zarówno problem Bernsteina–Vazirانiego, jak i problem Deutscha–Jozsy? Żeby to zrozumieć, pomyśl o funkcjach Bernsteina–Vazirانiego, które mają postać $f(x) = s \cdot x$. Czy te funkcje spełniają również obietnicę problemu Deutscha–Jozsy? To znaczy, ustal, czy funkcje tej postaci spełniają obietnicę problemu Deutscha–Jozsy: że są albo *stałe*, albo *zrównoważone*. Jak pomaga nam to zrozumieć, w jaki sposób ten sam algorytm rozwiązuje dwa różne problemy?

</summary>

__Odpowiedź:__

Każda funkcja Bernsteina–Vazirانiego postaci $f(x) = s \cdot x$ spełnia również obietnicę problemu Deutscha–Jozsy: jeśli s=00...00, funkcja jest stała (zawsze zwraca 0 dla każdego ciągu x). Jeśli s jest jakimkolwiek innym ciągiem, funkcja jest zrównoważona. Tak więc zastosowanie algorytmu Deutscha–Jozsy do jednej z tych funkcji rozwiązuje jednocześnie oba problemy! Zwraca ciąg, a jeśli ten ciąg to 00...00, wiemy, że jest stały; jeśli zawiera co najmniej jedno „1", wiemy, że jest zrównoważony.

</details>

Możemy również zweryfikować eksperymentalnie, że ten algorytm skutecznie rozwiązuje problem Bernsteina–Vazirانiego. Najpierw tworzymy funkcję B-V, która żyje wewnątrz czarnej skrzynki:

```python
# Krok 1: Mapowanie problemu

def bv_function(s):
    """
    Create a Bernstein-Vazirani function from a string of 1s and 0s.
    """
    qc = QuantumCircuit(len(s) + 1)
    for index, bit in enumerate(reversed(s)):
        if bit == "1":
            qc.cx(index, len(s))
    return qc

display(bv_function("1000").draw("mpl"))
```

![Output of the previous code cell](/learning/images/modules/computer-science/deutsch-jozsa/extracted-outputs/45449a26-0bd0-4244-87be-3309937955b9-0.avif)

```python
string = "1000"  # secret string that we'll pretend we don't know or have access to
n = len(string)

qc = QuantumCircuit(n + 1, n)
qc.x(n)
qc.h(range(n + 1))
qc.barrier()
# qc.compose(oracle, inplace = True)
qc.compose(bv_function(string), inplace=True)
qc.barrier()
qc.h(range(n))
qc.measure(range(n), range(n))

qc.draw("mpl")
```

![Output of the previous code cell](/learning/images/modules/computer-science/deutsch-jozsa/extracted-outputs/0cf6f2bc-3b5e-46d2-ab82-1a190e77c42b-0.avif)

```python
# Krok 2: Transpilacja
from qiskit.transpiler.preset_passmanagers import generate_preset_pass_manager

target = backend.target
pm = generate_preset_pass_manager(target=target, optimization_level=3)

qc_isa = pm.run(qc)
```

```python
# Krok 3: Uruchomienie zadania na prawdziwym komputerze kwantowym

job = sampler.run([qc_isa], shots=1)
# job = sampler_sim.run([qc_isa],shots=1) # odkomentuj tę linię, aby uruchomić na symulatorze
res = job.result()
counts = res[0].data.c.get_counts()
```

```python
# Krok 4: Wizualizacja i analiza wyników

## Analiza
print(counts)
```

```text
{'0000': 1}
```

Tak więc, wykonując tylko jedno zapytanie, algorytm Deutscha–Jozsy zwróci ciąg $s$ użyty w funkcji: $f(x)=x \cdot s$, gdy zastosujemy go do problemu Bernsteina–Vazirانiego. Klasyczny algorytm wymagałby $n$ zapytań, aby rozwiązać ten sam problem.

## Podsumowanie {#conclusion}

Mamy nadzieję, że analizując te proste przykłady, daliśmy ci lepszą intuicję co do tego, w jaki sposób komputery kwantowe są w stanie wykorzystać superpozycję, splątanie i interferencję, aby osiągnąć swoją przewagę nad klasycznymi komputerami.

Algorytm Deutscha–Jozsy ma ogromne historyczne znaczenie, ponieważ był pierwszym, który zademonstrował jakiekolwiek przyspieszenie względem algorytmu klasycznego, choć było to tylko przyspieszenie wielomianowe. Algorytm Deutscha–Jozsy to dopiero początek tej historii.

Po tym, jak wykorzystali algorytm do rozwiązania swojego problemu, Bernstein i Vazirani użyli go jako podstawy do bardziej złożonego, rekurencyjnego problemu zwanego *rekurencyjnym problemem próbkowania Fouriera*. Ich rozwiązanie oferowało ponad-wielomianowe przyspieszenie względem algorytmów klasycznych. A jeszcze przed Bernsteinem i Vazirannim Peter Shor opracował swój słynny algorytm, który umożliwił komputerom kwantowym faktoryzację dużych liczb wykładniczo szybciej niż jakikolwiek klasyczny algorytm. Wyniki te, razem wzięte, pokazały ekscytujące możliwości przyszłych komputerów kwantowych i zainspirowały fizyków oraz inżynierów do urzeczywistnienia tej przyszłości.

## Pytania {#questions}

Wykładowcy mogą poprosić o wersje tych notebooków z kluczami odpowiedzi i wskazówkami dotyczącymi umiejscowienia w typowych programach nauczania, wypełniając tę [krótką ankietę](https://ibm.biz/classrooms_instructor_key_request) dotyczącą sposobu korzystania z notebooków.

### Kluczowe pojęcia {#critical-concepts}
- Algorytmy Deutscha i Deutscha–Jozsy wykorzystują równoległość kwantową w połączeniu z interferencją, aby szybciej niż klasyczny komputer znaleźć odpowiedź na problem.
- Mechanizm phase kickback to kontrointuicyjne zjawisko kwantowe, które przenosi operacje na jednym Qubicie do fazy innego Qubitu. Algorytmy Deutscha i Deutscha–Jozsy wykorzystują ten mechanizm.
- Algorytm Deutscha–Jozsy oferuje wielomianowe przyspieszenie względem dowolnego deterministycznego algorytmu klasycznego.
- Algorytm Deutscha–Jozsy można zastosować do innego problemu, zwanego problemem Bernsteina–Vazirانiego, aby znaleźć ukryty ciąg zakodowany w funkcji.

### Prawda/fałsz {#truefalse}
1. P/F Algorytm Deutscha jest szczególnym przypadkiem algorytmu Deutscha–Jozsy, gdzie dane wejściowe to jeden Qubit.
2. P/F Algorytmy Deutscha i Deutscha–Jozsy wykorzystują superpozycję kwantową i interferencję, aby osiągnąć swoją wydajność.
4. P/F Algorytm Deutscha–Jozsy wymaga wielokrotnych ewaluacji funkcji, aby określić, czy funkcja jest stała, czy zrównoważona.
5. P/F „Algorytm Bernsteina–Vazirانiego" to w rzeczywistości ten sam algorytm co Deutscha–Jozsy, zastosowany do innego problemu.
6. P/F Algorytm Bernsteina–Vazirانiego może jednocześnie znaleźć wiele tajnych ciągów.

### Pytania otwarte {#short-answer}

1. Ile czasu zajęłoby klasycznemu algorytmowi rozwiązanie problemu Deutscha–Jozsy w najgorszym przypadku?

2. Ile czasu zajęłoby klasycznemu algorytmowi rozwiązanie problemu Bernsteina–Vazirانiego? Jakie przyspieszenie oferuje w tym przypadku algorytm DJ?

3. Opisz mechanizm phase kickback i to, jak działa on w celu rozwiązania problemów Deutscha–Jozsy i Bernsteina–Vazirانiego.

### Zadanie dla zaawansowanych {#challenge-problem}
1. Algorytm Deutscha–Jozsy: Przypomnij sobie, że powyżej miałeś/aś pytanie, w którym należało wyznaczyć pośrednie stany Qubitów $\pi_1$ i $\pi_2$ algorytmu Deutscha. Zrób to samo dla pośrednich stanów $(n+1)$-Qubitowych $\pi_1$ i $\pi_2$ algorytmu Deutscha–Jozsy, dla szczególnego przypadku $n=2$. Następnie zweryfikuj, że $\pi_3 = |-\rangle \otimes \sum\limits_{x_0...x_n}(-1)^{f(x_0...x_n)}|x_0 ... x_n\rangle$, znowu dla szczególnego przypadku $n=2$.
