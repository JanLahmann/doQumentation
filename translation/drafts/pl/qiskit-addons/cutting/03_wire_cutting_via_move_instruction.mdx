---
title: "Cięcie przewodów jako dwu-Qubitowa instrukcja `Move`"
sidebar_label: "Cięcie przewodów jako dwu-Qubitowa instrukcja `Move`"
notebook_path: "qiskit-addons/cutting/03_wire_cutting_via_move_instruction.ipynb"
---
{/* doqumentation-source-hash: 781aa50d */}

import TutorialFeedback from '@site/src/components/TutorialFeedback';

<OpenInLabBanner notebookPath="qiskit-addons/cutting/03_wire_cutting_via_move_instruction.ipynb" />


W tym samouczku zrekonstruujemy wartości oczekiwane dla siedmio-qubitowego Circuit, dzieląc go na dwa czworo-qubitowe Circuit za pomocą cięcia przewodów.

Oto kroki, które wykonamy w ramach tego [wzorca Qiskit](https://quantum.cloud.ibm.com/docs/guides/intro-to-patterns):

- **Krok 1: Odwzorowanie problemu na Circuit kwantowe i operatory**:
    - Odwzoruj hamiltonian na Circuit kwantowy.
- **Krok 2: Optymalizacja pod kątem docelowego sprzętu** [_Używa dodatku cutting_]:
    - <font color='#0F62FE'>Podziel Circuit i obserwowalną.</font>
    - Wykonaj transpilację podeksperymentów pod kątem sprzętu.
- **Krok 3: Wykonanie na docelowym sprzęcie**:
    - Uruchom podeksperymenty uzyskane w Kroku 2, korzystając z prymitywu `Sampler`.
- **Krok 4: Przetwarzanie końcowe wyników** [_Używa dodatku cutting_]:
    - <font color='#0F62FE'>Połącz wyniki Kroku 3, aby zrekonstruować wartość oczekiwaną rozpatrywanej obserwowalnej.</font>
## Krok 1: Odwzorowanie {#step-1-map}

### Tworzenie Circuit do cięcia {#create-a-circuit-to-cut}

Na początek tworzymy Circuit inspirowany rys. 1(a) z [arXiv:2302.03366v1](https://arxiv.org/abs/2302.03366v1).

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-addon-cutting qiskit-aer qiskit-ibm-runtime
```

```python
import numpy as np
from qiskit import QuantumCircuit

qc_0 = QuantumCircuit(7)
for i in range(7):
    qc_0.rx(np.pi / 4, i)
qc_0.cx(0, 3)
qc_0.cx(1, 3)
qc_0.cx(2, 3)
qc_0.cx(3, 4)
qc_0.cx(3, 5)
qc_0.cx(3, 6)
qc_0.cx(0, 3)
qc_0.cx(1, 3)
qc_0.cx(2, 3)
```

```text
<qiskit.circuit.instructionset.InstructionSet at 0x7f16ab191a80>
```

```python
qc_0.draw("mpl")
```

![Quantum circuit diagram](./output_1.png)

### Definiowanie obserwowalnej {#specify-an-observable}

```python
from qiskit.quantum_info import SparsePauliOp

observable = SparsePauliOp(["ZIIIIII", "IIIZIII", "IIIIIIZ"])
```

## Krok 2: Optymalizacja {#step-2-optimize}

### Tworzenie nowego Circuit z instrukcjami `Move` umieszczonymi w wybranych miejscach cięcia {#create-a-new-circuit-where-move-instructions-have-been-placed-at-the-desired-cut-locations}

Mając powyższy Circuit, chcemy umieścić dwa cięcia przewodów na środkowej linii Qubit, tak aby Circuit mógł zostać podzielony na dwa Circuit po cztery Qubit każdy. Jednym ze sposobów jest ręczne wstawienie dwu-qubitowych instrukcji `Move`, które przenoszą stan z jednego przewodu Qubit na drugi. Instrukcja `Move` jest koncepcyjnie równoważna operacji reset na drugim Qubit, po której następuje Gate SWAP. Efektem tej instrukcji jest przeniesienie stanu pierwszego (źródłowego) Qubit do drugiego (docelowego) Qubit przy jednoczesnym odrzuceniu przychodzącego stanu drugiego Qubit. Aby działało to zgodnie z zamierzeniem, ważne jest, aby drugi (docelowy) Qubit nie był splątany z resztą układu; w przeciwnym razie operacja reset spowoduje częściowy kolaps stanu reszty układu.

W tym miejscu budujemy nowy Circuit z jednym dodatkowym Qubit i operacjami `Move` na właściwych pozycjach. W tym przykładzie możemy ponownie wykorzystać Qubit: źródłowy Qubit pierwszej instrukcji `Move` staje się docelowym Qubit drugiej operacji `Move`.

Uwaga: jako alternatywę dla bezpośredniej pracy z instrukcjami `Move` możesz oznaczyć miejsca cięcia przewodów za pomocą jednoQubitowej instrukcji `CutWire`. Funkcja `cut_wires` służy do przekształcenia instrukcji `CutWire` w instrukcje `Move` na nowo przydzielonych Qubit. Jednak w odróżnieniu od metody ręcznej ta automatyczna metoda nie pozwala na ponowne wykorzystanie przewodów Qubit. Szczegółowe informacje znajdziesz w [przewodniku instruktażowym](../how-tos/how_to_specify_cut_wires.ipynb) dotyczącym `CutWire`.

```python
from qiskit_addon_cutting.instructions import Move

qc_1 = QuantumCircuit(8)
for i in [*range(4), *range(5, 8)]:
    qc_1.rx(np.pi / 4, i)
qc_1.cx(0, 3)
qc_1.cx(1, 3)
qc_1.cx(2, 3)
qc_1.append(Move(), [3, 4])
qc_1.cx(4, 5)
qc_1.cx(4, 6)
qc_1.cx(4, 7)
qc_1.append(Move(), [4, 3])
qc_1.cx(0, 3)
qc_1.cx(1, 3)
qc_1.cx(2, 3)

qc_1.draw("mpl")
```

![Quantum circuit diagram](./output_2.png)

### Tworzenie obserwowalnej do nowego Circuit {#create-observable-to-go-with-the-new-circuit}

Ta obserwowalna odpowiada `observable`, ale musimy poprawnie uwzględnić dodatkowy przewód Qubit (tzn. wstawiamy "I" pod indeksem 4). Zauważ, że w Qiskit reprezentacja łańcuchowa Qubit-0 odpowiada skrajnie prawemu znakowi Pauliego.

```python
observable_expanded = SparsePauliOp(["ZIIIIIII", "IIIIZIII", "IIIIIIIZ"])
```

### Rozdzielanie Circuit i obserwowalnych {#separate-the-circuit-and-observables}

Podobnie jak w poprzednich samouczkach, Qubit dzielące tę samą etykietę partycji zostaną zgrupowane razem, a nielokalne Gate obejmujące więcej niż jedną partycję zostaną przecięte.

```python
from qiskit_addon_cutting import partition_problem

partitioned_problem = partition_problem(
    circuit=qc_1, partition_labels="AAAABBBB", observables=observable_expanded.paulis
)
subcircuits = partitioned_problem.subcircuits
subobservables = partitioned_problem.subobservables
bases = partitioned_problem.bases
```

### Wizualizacja rozłożonego problemu {#visualize-the-decomposed-problem}

```python
subobservables
```

```text
{'A': PauliList(['IIII', 'ZIII', 'IIIZ']),
 'B': PauliList(['ZIII', 'IIII', 'IIII'])}
```

```python
subcircuits["A"].draw("mpl")
```

![Quantum circuit diagram](./output_3.png)

```python
subcircuits["B"].draw("mpl")
```

![Quantum circuit diagram](./output_4.png)

### Obliczanie narzutu próbkowania dla wybranych cięć {#calculate-the-sampling-overhead-for-the-chosen-cuts}

W tym miejscu tniemy dwa przewody, co skutkuje narzutem próbkowania równym $4^4$.

Więcej informacji na temat narzutu próbkowania wynikającego z cięcia Circuit znajdziesz w [materiałach wyjaśniających](../explanation/index.rst).

```python
print(f"Sampling overhead: {np.prod([basis.overhead for basis in bases])}")
```

```text
Sampling overhead: 256.0
```

### Generowanie podeksperymentów do uruchomienia na Backend {#generate-the-subexperiments-to-run-on-the-backend}

Funkcja `generate_cutting_experiments` przyjmuje argumenty `circuits`/`observables` jako słowniki odwzorowujące etykiety partycji Qubit na odpowiednie `subcircuit`/`subobservables`.

Aby zasymulować wartość oczekiwaną pełnowymiarowego Circuit, z rozkładu quasiprawdopodobieństwa rozłożonych Gate generowanych jest wiele podeksperymentów, które następnie są wykonywane na jednym lub kilku Backend. Liczba próbek pobieranych z rozkładu jest kontrolowana przez `num_samples`, a dla każdej unikalnej próbki podawany jest jeden łączny współczynnik. Więcej informacji na temat sposobu obliczania współczynników znajdziesz w [materiałach wyjaśniających](../explanation/index.rst).

```python
from qiskit_addon_cutting import generate_cutting_experiments

subexperiments, coefficients = generate_cutting_experiments(
    circuits=subcircuits, observables=subobservables, num_samples=np.inf
)
```

### Wybór Backend {#choose-a-backend}

Używamy tu fikcyjnego Backend, co spowoduje, że Qiskit Runtime będzie działać w trybie lokalnym (tj. na lokalnym symulatorze).

```python
from qiskit_ibm_runtime.fake_provider import FakeManilaV2

backend = FakeManilaV2()
```

### Przygotowanie podeksperymentów dla Backend {#prepare-the-subexperiments-for-the-backend}

Przed przesłaniem Circuit do Qiskit Runtime musimy je transpilować z naszym Backend jako celem.

```python
from qiskit.transpiler import generate_preset_pass_manager

# Transpile the subexperiments to ISA circuits
pass_manager = generate_preset_pass_manager(optimization_level=1, backend=backend)
isa_subexperiments = {
    label: pass_manager.run(partition_subexpts)
    for label, partition_subexpts in subexperiments.items()
}
```

## Krok 3: Wykonanie {#step-3-execute}

### Uruchamianie podeksperymentów za pomocą prymitywu Sampler środowiska Qiskit Runtime {#run-the-subexperiments-using-the-qiskit-runtime-sampler-primitive}

```python
from qiskit_ibm_runtime import SamplerV2, Batch

# Submit each partition's subexperiments to the Qiskit Runtime Sampler
# primitive, in a single batch so that the jobs will run back-to-back.
with Batch(backend=backend) as batch:
    sampler = SamplerV2(mode=batch)
    jobs = {
        label: sampler.run(subsystem_subexpts, shots=2**12)
        for label, subsystem_subexpts in isa_subexperiments.items()
    }
```

```text
/home/garrison/Qiskit/qiskit-ibm-runtime/qiskit_ibm_runtime/session.py:157: UserWarning: Session is not supported in local testing mode or when using a simulator.
  warnings.warn(
```

```python
# Retrieve results
results = {label: job.result() for label, job in jobs.items()}
```

## Krok 4: Przetwarzanie końcowe {#step-4-post-process}

### Rekonstrukcja wartości oczekiwanej {#reconstruct-the-expectation-value}

Zrekonstruuj wartości oczekiwane dla każdego członu obserwowalnej, a następnie połącz je, aby odtworzyć wartość oczekiwaną oryginalnej obserwowalnej.

```python
from qiskit_addon_cutting import reconstruct_expectation_values

reconstructed_expval_terms = reconstruct_expectation_values(
    results,
    coefficients,
    subobservables,
)
reconstructed_expval = np.dot(reconstructed_expval_terms, observable.coeffs)
```

### Porównanie zrekonstruowanej wartości oczekiwanej z dokładną wartością oczekiwaną dla oryginalnego Circuit i obserwowalnej {#compare-the-reconstructed-expectation-value-with-the-exact-expectation-value-from-the-original-circuit-and-observable}

```python
from qiskit_aer.primitives import EstimatorV2

estimator = EstimatorV2()
exact_expval = estimator.run([(qc_0, observable)]).result()[0].data.evs
print(f"Reconstructed expectation value: {np.real(np.round(reconstructed_expval, 8))}")
print(f"Exact expectation value: {np.round(exact_expval, 8)}")
print(f"Error in estimation: {np.real(np.round(reconstructed_expval-exact_expval, 8))}")
print(
    f"Relative error in estimation: {np.real(np.round((reconstructed_expval-exact_expval) / exact_expval, 8))}"
)
```

```text
Reconstructed expectation value: 1.51319069
Exact expectation value: 1.59099026
Error in estimation: -0.07779957
Relative error in estimation: -0.04890009
```

<TutorialFeedback />
