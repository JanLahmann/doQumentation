---
title: "Cięcie bramek w celu zmniejszenia głębokości Circuit"
sidebar_label: "Cięcie bramek w celu zmniejszenia głębokości Circuit"
notebook_path: "qiskit-addons/cutting/02_gate_cutting_to_reduce_circuit_depth.ipynb"
---

<!-- source hash: 04893c53 -->

import TutorialFeedback from '@site/src/components/TutorialFeedback';

<OpenInLabBanner notebookPath="qiskit-addons/cutting/02_gate_cutting_to_reduce_circuit_depth.ipynb" />


W tym samouczku zmniejszymy głębokość Circuit, tnąc odległe bramki i unikając w ten sposób bramek SWAP, które w przeciwnym razie zostałyby wprowadzone podczas routingu.

Oto kroki, które podejmiemy w tym [wzorcu Qiskit](https://quantum.cloud.ibm.com/docs/guides/intro-to-patterns):

- **Krok 1: Odwzorowanie problemu na Circuit i operatory kwantowe**:
    - Odwzorowanie hamiltonianu na Circuit kwantowy.
- **Krok 2: Optymalizacja pod kątem docelowego sprzętu** [_Używa dodatku cutting_]:
    - <font color='#0F62FE'>Cięcie Circuit i obserwabli.</font>
    - Transpilacja subeksperymantów dla sprzętu.
- **Krok 3: Uruchomienie na docelowym sprzęcie**:
    - Uruchomienie subeksperymantów uzyskanych w Kroku 2 za pomocą primitywu `Sampler`.
- **Krok 4: Przetwarzanie końcowe wyników** [_Używa dodatku cutting_]:
    - <font color='#0F62FE'>Połączenie wyników Kroku 3 w celu odtworzenia wartości oczekiwanej danej obserwabli.</font>
## Krok 1: Odwzorowanie {#krok-1-odwzorowanie}

### Tworzenie Circuit do uruchomienia na Backend {#tworzenie-circuit-do-uruchomienia-na-backend}

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-addon-cutting qiskit-aer qiskit-ibm-runtime
```

```python
from qiskit.circuit.library import efficient_su2

circuit = efficient_su2(num_qubits=4, entanglement="circular")
circuit.assign_parameters([0.4] * len(circuit.parameters), inplace=True)
circuit.draw("mpl", scale=0.8)
```

![Diagram Circuit kwantowego](./output_1.png)

### Określanie obserwabli {#określanie-obserwabli}

```python
from qiskit.quantum_info import SparsePauliOp

observable = SparsePauliOp(["ZZII", "IZZI", "-IIZZ", "XIXI", "ZIZZ", "IXIX"])
```

## Krok 2: Optymalizacja {#krok-2-optymalizacja}

### Wybór Backend {#wybór-backend}

Możesz podać fałszywy Backend lub sprzętowy Backend z Qiskit Runtime.

```python
from qiskit_ibm_runtime.fake_provider import FakeManilaV2

backend = FakeManilaV2()
```

### Transpilacja Circuit, wizualizacja zamian i sprawdzenie głębokości {#transpilacja-circuit}

Wybieramy układ wymagający dwóch zamian do wykonania bramek między Qubitami 3 i 0 oraz kolejnych dwóch zamian, aby Qubity wróciły do swoich początkowych pozycji.

```python
from qiskit.transpiler import generate_preset_pass_manager

pass_manager = generate_preset_pass_manager(
    optimization_level=1, backend=backend, initial_layout=[0, 1, 2, 3]
)

transpiled_qc = pass_manager.run(circuit)
print(f"Transpiled circuit depth: {transpiled_qc.depth(lambda x: len(x.qubits) >= 2)}")
```

```text
Transpiled circuit depth: 30
```

```python
transpiled_qc.draw("mpl", scale=0.4, idle_wires=False, fold=-1)
```

![Diagram Circuit kwantowego](./output_2.png)

### Zastępowanie odległych bramek przez `TwoQubitQPDGate` poprzez podanie ich indeksów {#zastępowanie-odległych-bramek}

`cut_gates` zastąpi bramki o podanych indeksach przez `TwoQubitQPDGate` i zwróci listę instancji `QPDBasis` — po jednej dla każdej dekompozycji bramki.

```python
from qiskit_addon_cutting import cut_gates

# Find the indices of the distant gates
cut_indices = [
    i
    for i, instruction in enumerate(circuit.data)
    if {circuit.find_bit(q)[0] for q in instruction.qubits} == {0, 3}
]

# Decompose distant CNOTs into TwoQubitQPDGate instances
qpd_circuit, bases = cut_gates(circuit, cut_indices)

qpd_circuit.draw("mpl", scale=0.8)
```

![Diagram Circuit kwantowego](./output_3.png)

### Generowanie subeksperymantów do uruchomienia na Backend {#generowanie-subeksperymantów}

`generate_cutting_experiments` przyjmuje Circuit zawierający instancje `TwoQubitQPDGate` oraz obserwable w postaci `PauliList`.

Aby zasymulować wartość oczekiwaną pełnego Circuit, z rozłożonych bramek' wspólnego rozkładu quasiprawdopodobieństwa generowanych jest wiele subeksperymantów, które są następnie wykonywane na jednym lub kilku Backend. Liczba próbek pobranych z rozkładu jest kontrolowana przez `num_samples`, a jeden łączny współczynnik jest podawany dla każdej unikalnej próbki. Więcej informacji na temat sposobu obliczania współczynników znajdziesz w [materiałach wyjaśniających](../explanation/index.rst).

**Uwaga:** Argument ``observables`` funkcji `generate_cutting_experiments` ma typ `PauliList`. Współczynniki i fazy składowych obserwabli są ignorowane podczas dekompozycji problemu i wykonania subeksperymantów. Można je ponownie zastosować podczas odtwarzania wartości oczekiwanej.

```python
import numpy as np
from qiskit_addon_cutting import generate_cutting_experiments

# Generate the subexperiments and sampling coefficients
subexperiments, coefficients = generate_cutting_experiments(
    circuits=qpd_circuit, observables=observable.paulis, num_samples=np.inf
)
```

### Obliczanie narzutu próbkowania dla wybranych cięć {#obliczanie-narzutu-próbkowania}

Tutaj przecinamy trzy bramki CNOT, co skutkuje narzutem próbkowania $9^3$.

Więcej informacji na temat narzutu próbkowania wynikającego z cięcia Circuit znajdziesz w [materiałach wyjaśniających](../explanation/index.rst).

```python
print(f"Sampling overhead: {np.prod([basis.overhead for basis in bases])}")
```

```text
Sampling overhead: 729.0
```

### Demonstracja, że subeksperymenty QPD będą płytsze po przecięciu odległych bramek {#demonstracja-płytkości-subeksperymantów}

Oto przykład losowo wybranego subeksperymant wygenerowanego z Circuit QPD. Jego głębokość zmniejszyła się o ponad połowę. Wiele takich probabilistycznych subeksperymantów musi zostać wygenerowanych i ocenionych, aby odtworzyć wartość oczekiwaną głębszego Circuit.

```python
# Transpile the decomposed circuit to the same layout
transpiled_qpd_circuit = pass_manager.run(subexperiments[100])

print(
    f"Original circuit depth after transpile: {transpiled_qc.depth(lambda x: len(x.qubits) >= 2)}"
)
print(
    f"QPD subexperiment depth after transpile: {transpiled_qpd_circuit.depth(lambda x: len(x.qubits) >= 2)}"
)
transpiled_qpd_circuit.draw("mpl", scale=0.8, idle_wires=False, fold=-1)
```

```text
Original circuit depth after transpile: 30
QPD subexperiment depth after transpile: 7
```

![Diagram Circuit kwantowego](./output_4.png)

### Przygotowanie subeksperymantów dla Backend {#przygotowanie-subeksperymantów}

```python
# Transpile the subeperiments to the backend's instruction set architecture (ISA)
isa_subexperiments = pass_manager.run(subexperiments)
```

## Krok 3: Uruchomienie {#krok-3-uruchomienie}

### Uruchamianie subeksperymantów za pomocą primitywu Sampler Qiskit Runtime {#uruchamianie-subeksperymantów}

```python
from qiskit_ibm_runtime import SamplerV2

# Set up the Qiskit Runtime Sampler primitive.  For a fake backend, this will use a local simulator.
sampler = SamplerV2(backend)

# Submit the subexperiments
job = sampler.run(isa_subexperiments)
```

```python
# Retrieve the results
results = job.result()
```

## Krok 4: Przetwarzanie końcowe {#krok-4-przetwarzanie-końcowe}

### Odtwarzanie wartości oczekiwanej {#odtwarzanie-wartości-oczekiwanej}

Odtwarzanie wartości oczekiwanych dla każdej składowej obserwabli i ich łączenie w celu odtworzenia wartości oczekiwanej pierwotnej obserwabli.

```python
from qiskit_addon_cutting import reconstruct_expectation_values

reconstructed_expval_terms = reconstruct_expectation_values(
    results,
    coefficients,
    observable.paulis,
)
# Reconstruct final expectation value
reconstructed_expval = np.dot(reconstructed_expval_terms, observable.coeffs)
```

### Porównanie odtworzonej wartości oczekiwanej z dokładną wartością oczekiwaną dla oryginalnego Circuit i obserwabli {#porównanie-wartości-oczekiwanej}

```python
from qiskit_aer.primitives import EstimatorV2

estimator = EstimatorV2()
exact_expval = estimator.run([(circuit, observable)]).result()[0].data.evs
print(f"Reconstructed expectation value: {np.real(np.round(reconstructed_expval, 8))}")
print(f"Exact expectation value: {np.round(exact_expval, 8)}")
print(f"Error in estimation: {np.real(np.round(reconstructed_expval-exact_expval, 8))}")
print(
    f"Relative error in estimation: {np.real(np.round((reconstructed_expval-exact_expval) / exact_expval, 8))}"
)
```

```text
Reconstructed expectation value: 0.44018555
Exact expectation value: 0.50497603
Error in estimation: -0.06479049
Relative error in estimation: -0.12830408
```

<TutorialFeedback />
