---
title: "Cięcie bramek w celu zmniejszenia szerokości Circuit"
sidebar_label: "Cięcie bramek w celu zmniejszenia szerokości Circuit"
notebook_path: "qiskit-addons/cutting/01_gate_cutting_to_reduce_circuit_width.ipynb"
---

<!-- source hash: 3647f99e -->

import TutorialFeedback from '@site/src/components/TutorialFeedback';

<OpenInLabBanner notebookPath="qiskit-addons/cutting/01_gate_cutting_to_reduce_circuit_width.ipynb" />


W tym notebooku przeprowadzimy kolejne kroki [wzorca Qiskit](https://quantum.cloud.ibm.com/docs/guides/intro-to-patterns), używając **cięcia Circuit** do zmniejszenia liczby Qubitów w Circuit. Będziemy ciąć bramki, aby umożliwić odtworzenie wartości oczekiwanej czteroqubitowego Circuit przy użyciu jedynie eksperymentów dwuqubitowych.

Oto kroki, które podejmiemy:

- **Krok 1: Odwzorowanie problemu na Circuit i operatory kwantowe**:
    - Odwzorowanie hamiltonianu na Circuit kwantowy.
- **Krok 2: Optymalizacja pod kątem docelowego sprzętu** [_Używa dodatku cutting_]:
    - <font color='#0F62FE'>Cięcie Circuit i obserwabli.</font>
    - Transpilacja subeksperymantów dla sprzętu.
- **Krok 3: Uruchomienie na docelowym sprzęcie**:
    - Uruchomienie subeksperymantów uzyskanych w Kroku 2 za pomocą primitywu `Sampler`.
- **Krok 4: Przetwarzanie końcowe wyników** [_Używa dodatku cutting_]:
    - <font color='#0F62FE'>Połączenie wyników Kroku 3 w celu odtworzenia wartości oczekiwanej danej obserwabli.</font>
## Krok 1: Odwzorowanie {#krok-1-odwzorowanie}

### Tworzenie Circuit do cięcia {#tworzenie-circuit-do-cięcia}

```python
# Added by doQumentation — required packages for this notebook
!pip install -q numpy qiskit qiskit-addon-cutting qiskit-aer qiskit-ibm-runtime
```

```python
from qiskit.circuit.library import efficient_su2

qc = efficient_su2(4, entanglement="linear", reps=2)
qc.assign_parameters([0.4] * len(qc.parameters), inplace=True)

qc.draw("mpl", scale=0.8)
```

![Diagram Circuit kwantowego](./output_1.png)

### Określanie obserwabli {#określanie-obserwabli}

```python
from qiskit.quantum_info import SparsePauliOp

observable = SparsePauliOp(["ZZII", "IZZI", "-IIZZ", "XIXI", "ZIZZ", "IXIX"])
```

## Krok 2: Optymalizacja {#krok-2-optymalizacja}

### Rozdzielenie Circuit i obserwabli zgodnie z zadanym podziałem Qubitów {#rozdzielenie-circuit-i-obserwabli}

Każda etykieta w `partition_labels` odpowiada Qubitowi `circuit` o tym samym indeksie. Qubity mające wspólną etykietę partycji zostaną zgrupowane, a bramki nielokalne obejmujące więcej niż jedną partycję zostaną przecięte.

**Uwaga:** Argument ``observables`` funkcji `partition_problem` ma typ `PauliList`. Współczynniki i fazy składowych obserwabli są ignorowane podczas dekompozycji problemu i wykonania subeksperymantów. Można je ponownie zastosować podczas odtwarzania wartości oczekiwanej.

```python
from qiskit_addon_cutting import partition_problem

partitioned_problem = partition_problem(
    circuit=qc, partition_labels="AABB", observables=observable.paulis
)
subcircuits = partitioned_problem.subcircuits
subobservables = partitioned_problem.subobservables
bases = partitioned_problem.bases
```

### Wizualizacja zdekompanowanego problemu {#wizualizacja-zdekompanowanego-problemu}

```python
subobservables
```

```text
{'A': PauliList(['II', 'ZI', 'ZZ', 'XI', 'ZZ', 'IX']),
 'B': PauliList(['ZZ', 'IZ', 'II', 'XI', 'ZI', 'IX'])}
```

```python
subcircuits["A"].draw("mpl", scale=0.8)
```

![Diagram Circuit kwantowego](./output_2.png)

```python
subcircuits["B"].draw("mpl", scale=0.8)
```

![Diagram Circuit kwantowego](./output_3.png)

### Obliczanie narzutu próbkowania dla wybranych cięć {#obliczanie-narzutu-próbkowania}

Tutaj przecinamy dwie bramki CNOT, co skutkuje narzutem próbkowania $9^2$.

Więcej informacji na temat narzutu próbkowania wynikającego z cięcia Circuit znajdziesz w [materiałach wyjaśniających](../explanation/index.rst).

```python
import numpy as np

print(f"Sampling overhead: {np.prod([basis.overhead for basis in bases])}")
```

```text
Sampling overhead: 81.0
```

### Generowanie subeksperymantów do uruchomienia na Backend {#generowanie-subeksperymantów}

`generate_cutting_experiments` przyjmuje argumenty `circuits`/`observables` jako słowniki odwzorowujące etykiety partycji Qubitów na odpowiednie `subcircuit`/`subobservables`.

Aby zasymulować wartość oczekiwaną pełnego Circuit, z rozłożonych bramek' wspólnego rozkładu quasiprawdopodobieństwa generowanych jest wiele subeksperymantów, które są następnie wykonywane na jednym lub kilku Backend. Liczba próbek pobranych z rozkładu jest kontrolowana przez `num_samples`, a jeden łączny współczynnik jest podawany dla każdej unikalnej próbki. Więcej informacji na temat sposobu obliczania współczynników znajdziesz w [materiałach wyjaśniających](../explanation/index.rst).

```python
from qiskit_addon_cutting import generate_cutting_experiments

subexperiments, coefficients = generate_cutting_experiments(
    circuits=subcircuits, observables=subobservables, num_samples=np.inf
)
```

### Wybór Backend {#wybór-backend}

Używamy tutaj fałszywego Backend, co spowoduje uruchomienie Qiskit Runtime w trybie lokalnym (tzn. na lokalnym symulatorze).

```python
from qiskit_ibm_runtime.fake_provider import FakeManilaV2

backend = FakeManilaV2()
```

### Przygotowanie subeksperymantów dla Backend {#przygotowanie-subeksperymantów}

Musimy transpilować Circuit z naszym Backend jako celem przed przesłaniem ich do Qiskit Runtime.

```python
from qiskit.transpiler import generate_preset_pass_manager

# Transpile the subexperiments to ISA circuits
pass_manager = generate_preset_pass_manager(optimization_level=1, backend=backend)
isa_subexperiments = {
    label: pass_manager.run(partition_subexpts)
    for label, partition_subexpts in subexperiments.items()
}
```

## Krok 3: Uruchomienie {#krok-3-uruchomienie}

### Uruchamianie subeksperymantów za pomocą primitywu Sampler Qiskit Runtime {#uruchamianie-subeksperymantów}

```python
from qiskit_ibm_runtime import SamplerV2, Batch

# Submit each partition's subexperiments to the Qiskit Runtime Sampler
# primitive, in a single batch so that the jobs will run back-to-back.
with Batch(backend=backend) as batch:
    sampler = SamplerV2(mode=batch)
    jobs = {
        label: sampler.run(subsystem_subexpts, shots=2**12)
        for label, subsystem_subexpts in isa_subexperiments.items()
    }
```

```text
/home/garrison/Qiskit/qiskit-ibm-runtime/qiskit_ibm_runtime/session.py:157: UserWarning: Session is not supported in local testing mode or when using a simulator.
  warnings.warn(
```

```python
# Retrieve results
results = {label: job.result() for label, job in jobs.items()}
```

## Krok 4: Przetwarzanie końcowe {#krok-4-przetwarzanie-końcowe}

### Odtwarzanie wartości oczekiwanej {#odtwarzanie-wartości-oczekiwanej}

Odtwarzanie wartości oczekiwanych dla każdej składowej obserwabli i ich łączenie w celu odtworzenia wartości oczekiwanej pierwotnej obserwabli.

```python
from qiskit_addon_cutting import reconstruct_expectation_values

# Get expectation values for each observable term
reconstructed_expval_terms = reconstruct_expectation_values(
    results,
    coefficients,
    subobservables,
)

# Reconstruct final expectation value
reconstructed_expval = np.dot(reconstructed_expval_terms, observable.coeffs)
```

### Porównanie odtworzonej wartości oczekiwanej z dokładną wartością oczekiwaną dla oryginalnego Circuit i obserwabli {#porównanie-wartości-oczekiwanej}

```python
from qiskit_aer.primitives import EstimatorV2

estimator = EstimatorV2()
exact_expval = estimator.run([(qc, observable)]).result()[0].data.evs
print(f"Reconstructed expectation value: {np.real(np.round(reconstructed_expval, 8))}")
print(f"Exact expectation value: {np.round(exact_expval, 8)}")
print(f"Error in estimation: {np.real(np.round(reconstructed_expval-exact_expval, 8))}")
print(
    f"Relative error in estimation: {np.real(np.round((reconstructed_expval-exact_expval) / exact_expval, 8))}"
)
```

```text
Reconstructed expectation value: 0.6991539
Exact expectation value: 0.56254612
Error in estimation: 0.13660778
Relative error in estimation: 0.24283836
```

<TutorialFeedback />
